# 112-STRICT-K3 — `DEC-240`: حسم `GAP-STRICT-K3-01`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | حسم `GAP-STRICT-K3-01` |
| **حالة الاعتماد** | **مشتق** — اختيار المسار له سند من خط الأساس · مسّ محرّك $K_3$ بإذن |
| **الأدوات** | **15/15** · البصمات الثلاث ثابتة |

---

# القسم 1: لماذا هذا المسار — سندٌ لا ترجيح

أمرك قال «حسم» ولم يسمِّ مساراً. والفحص أظهر أن خط الأساس **يحسم بنفسه**:

| | بوابة `strict` | النسختان | الرسالة |
| :--- | :--- | :---: | :--- |
| $K_2$ | `run(…, strict)` | **متماثلة** | **نصّ** (`DEC-235`/`236`) |
| $K_3$ | بايثون وحدها | ❌ | **قاموس** |

$K_3$ خالفت في الأمرين معاً. فالحسم **ردٌّ إلى نمط الدائرة الشقيقة**، لا اختيار بين بديلين.

---

# القسم 2: الحسم

## بايثون — `StrictGateError`

```python
class StrictGateError(RuntimeError):
    def __init__(self, gaps, violations, missing_content):
        self.gaps = list(gaps); self.violations = list(violations)
        self.missing_content = list(missing_content)
        super().__init__(strict_gate_message(...))
```

الرسالة **نصّ صريح** (`ن-8`)، و**الحمولة البنيوية محفوظة في السمات** — فلا تُفقد قدرة القارئ البرمجي مقابل صياغة الرسالة.

## JS — نظير حرفي

`StrictGateError` باسم مطابق ورسالة مطابقة، و`K3.run(sp, TL, G5, { content, strict })` — نظير توقيع بايثون. و`reports.js` يمرّر `K3_CONTENT_ADAPTER` كما يمرّر `k3_report.py:83` حزمته.

## الصياغة الموحَّدة

```
بوابة strict — الإصدار موقوف · فجوات: … · مخالفات: … · محتوى مفقود: …
```

الفارغ يُصاغ **`لا شيء`** صراحةً — لا فراغاً ضمنياً.

---

# القسم 3: عيبان حُسما في الطريق

## `DEF-K3-GAPKEY-01` — انهيار بدل بلاغ

```python
gaps.append(TEMPLATE_GAPS[(c.a, c.b, c.state)])   # و TEMPLATE_GAPS = {}
```

`TEMPLATE_GAPS` **فارغ منذ `DEC-137`**. فالسطر الذي كُتب ليقول «تُرفع فجوةً ولا تُملأ بالتخمين» كان **يرفع `KeyError`** — انهياراً لا بلاغاً. صار `.get(key, "خلية {code} بلا قالب كلفة معتمد")`.

## `DEF-GAPS-JS-01` — حقل مثبَّت وتخطٍّ صامت

`template_gaps: []` مثبَّتة في `reports.js`، والخلية بلا قالب تُتخطّى بـ`continue` صامت. صارت تُسجَّل فجوةً **بالنصّ نفسه** في الطرفين، والحقل يُملأ من المحرّك.

## تصويب وصف — أُثبته ولا أُخفيه

وصفتُ `template_gaps` ابتداءً بأنها **«ثالث ظهور لنمط الحقل الذي لا يفحص»**. الفحص أظهر التقدير **مبالَغاً**: `TEMPLATE_GAPS` فارغ بقرار `DEC-137`، فالمسار **خامل في الطرفين** لا حارساً كاذباً.

لكن حسمه كان **لازماً**: بناء بوابة `strict` على مُدخل مثبَّت يكرّر الخطيئة التي أُغلقت مرّتين.

---

# القسم 4: القياس

`parity_messages` وُسِّع بثلاثة مسارات `strict` — **فراغ · واحد · عدّة** ⇒ **15 مساراً** · الاسم والنصّ متطابقان.

## تصويبان في المقاييس نفسها

| المقياس | العيب | الإصلاح |
| :--- | :--- | :--- |
| `parity_messages` | يعدّ «لا استثناء في الطرفين» **تباعداً** | اتّفاقٌ يغطّي **الفرع السالب** للبوابة. التباعد أن يرفع أحدهما دون الآخر |
| `guard_interp` (`ح-6`) | يلتقط **استيعاب القاموس** استيفاءً | قاعدة استبعاد صريحة — **لا تسجيل ضجيج** |

> **مبدأ:** حين يُبلّغ حارسٌ عن ضجيج، **يُصلَح الحارس لا يُوسَّع سجلّه**. السجلّ الذي يبتلع الضجيج يفقد معناه، ويصير هو نفسه حقلاً لا يفحص.

`ح-6` بعد التدقيق: **60 استيفاءً** · 6 مصاغة صراحةً · 54 مسجَّلة · **صفر يتيم**. ومُعاد اختباره على العيب المعلوم فسقط عليه.

---

# القسم 5: اختبار القبول

| | النتيجة |
| :--- | :--- |
| الأدوات | **15/15** |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **ثابتة** |
| الأداتان | 12/12 بصمة حزمة · نحو سليم · 142 تقريراً · **صفر تباعد في النصّ وفي كتلة التدقيق** |

| الأداة | الحجم | `SHA` |
| :--- | ---: | :--- |
| `Kashaf_v2_FIXED_DEC-230-240.html` | 1,114,835 | `74f0e6704e0951f5…` |
| `Supervisor.html` | 364,182 | `b7fd5019e85c31c4…` |

---

**[نهاية `112-STRICT-K3` — `DEC-240` · `GAP-STRICT-K3-01` مُغلقة]**
