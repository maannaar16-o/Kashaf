# 74-K3-DRYRUN-02_Five_Edge_Cases.md
## الاختبارات الجافة الخمسة — الحالات الحرجة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `74-K3-DRYRUN-02` |
| **الإصدار** | **v0.1 — 🟡 `[مقترح]`** (القرار المقترح: `DEC-133`) |
| **الغرض** | تشغيل الحالات التي **لم يبلغها** `SYN-01`: العتبات القصوى والصمت |
| **الحالة** | 🧪 **حالات مُصطنعة** — لا تخصّ أشخاصاً |
| **السند** | `SPEC` v2.0 · `M3-06` v1.0 · `65` · `71` · `55-USER-K3-CMP` v1.1 · `M3-01 §4` |
| **النتيجة الإجمالية** | 🔴 **ثلاثة عيوب بنيوية مكتشفة** — اثنان منها ما كان الفحص النظري ليكشفهما |

---

# القسم 1: `SYN-02` — ملاحظة محدودة مع أقطاب

| `EP` | `IR` | `BI` | `CF` | `ST` |
| :---: | :---: | :---: | :---: | :---: |
| **34%** ↓ | 30% ↓ | 76% ↑ | 58% ⚪ | 46% ↓ |

**المسار:** `G1` ✅ **`low_trust`** ← `C-12` وكل ما بعده متحفّظ · `G2` سؤال `FR-02` وحده · **الخلايا 4:** `[28]` `EP.IR-WW` · `[3]` `IR.BI-WS` · `[16]` `IR.ST-WW` · `[18]` `BI.ST-SW` · **`CF` محايدة ⇒ أزواجها الثلاثة ساقطة.**

**الأنماط:** `HF-01` ❌ (حافة محور واحدة فقط — `IR–CF` ساقطة) · `HF-02` ❌ · **`HF-04`** ← **انظر العيب ①**.

**✅ سلوك صحيح مؤكَّد:** `HF-01` **لم يُعلن** رغم أن `IR` منخفضة وحافة محورية نشطة — **الشرط الناقص لم يُكمَّل بالتخمين**.

---

# القسم 2: `SYN-03` — الخمس في النطاق العالي (`HF-05`)

| `EP` | `IR` | `BI` | `CF` | `ST` |
| :---: | :---: | :---: | :---: | :---: |
| 88% ↑ | 82% ↑ | 79% ↑ | 74% ↑ | 91% ↑ |

**المسار:** `G1` ❌ · **`G2`** الأسئلة الأربع + جملة `HF-05` · **الخلايا 7 — كلها `SS`** · **الأنماط:** لا شيء (كلها تحتاج طرفاً ضعيفاً) · **`G4`: قوي**.

**المخرج (القسم ٦):** `T-01` **مرة واحدة** بذكر المواضع السبعة (`س-3`) + نصّ الحالة «قوي» + التحفّظ المشدَّد.

**✅ تأكيدان مهمّان:**
① **`س-3` منعت سبع جمل متطابقة** — لولاها لصار القسم ٦ ركاماً.
② **ترتيب `ق7` أثبت قيمته هنا تحديداً:** كتلة التحقق (٥) **تسبق** «قدراتك الخمس جميعها في النطاق العالي» (٦). فيصل القارئ إلى الجملة الأكثر إغراءً بالطمأنينة **وقد قرأ تحفّظها**. لو انعكس الترتيب لقرأ المديح أولاً.

---

# القسم 3: `SYN-04` — الخمس في النطاق المحدود

| `EP` | `IR` | `BI` | `CF` | `ST` |
| :---: | :---: | :---: | :---: | :---: |
| 28% ↓ | 33% ↓ | 41% ↓ | 22% ↓ | 37% ↓ |

**المسار:** `G1` ✅ `low_trust` · `G2` ⛔ لا كتلة · **الخلايا 7 — كلها `WW`** · **`G4`: ضعيف**.
**الأنماط:** **`HF-01`** ✅ تام (حافتا المحور) · **`HF-04`** ✅ تام (قناتان نشطتان `[16]`+`[24]` و`ST ≤50%`).
**`G3`:** `HF-01` أعلى بنيوياً ← `RQ-01` · `HF-04` ← `C-15` وقائعَ بلا تفسير.

**✅ تأكيد:** التحفّظ الإلزامي في `HF-04` — «قد يكون **الاحتمال سليماً يحمل عبئاً مضاعفاً**» — ظهر في أخطر موضع ممكن: ملف كل درجاته منخفضة. بلا هذا التحفّظ يقرأ صاحبه ضعفاً أصيلاً خماسياً.

---

# القسم 4: `SYN-05` — حالة `OUT`

| `EP` | `IR` | `BI` | `CF` | `ST` |
| :---: | :---: | :---: | :---: | :---: |
| 55% ⚪ | **`OUT`** (`SP ≈ −7.6%`) | 52% ⚪ | 61% ⚪ | 48% ↓ |

**المسار:** `G1` ❌ · `G2` ⛔ · نصّ `OUT` (`§8.1`) + مدخل «حالة الضغط القصوى» ✅.
**الخلايا:** ⛔ **متوقفة — انظر العيب ②.**

---

# القسم 5: `SYN-06` — لا قطب إطلاقاً (اختبار الصمت)

| `EP` | `IR` | `BI` | `CF` | `ST` |
| :---: | :---: | :---: | :---: | :---: |
| 57% ⚪ | 63% ⚪ | 55% ⚪ | 68% ⚪ | 51% ⚪ |

**المسار:** `G1` ❌ · `G2` ⛔ · **الخلايا: صفر** · الأنماط: صفر · **`G4`: ← انظر العيب ③.**
**المتوقع (`§8.4`):** التقرير يقف عند الأقسام الخمسة — **الصمت مشروع**.

---

# القسم 6: 🔴 العيوب الثلاثة

## 6.1 `GAP-DRY-03` — **شرط «القناة النشطة» في `HF-04` غير متماثل** 🔴 **عالية**

**النص القائم** (`65 §3.3` · `M3-06 §2`): «≥ قناتين بقناة نشطة (`CPL-*.ST-WS` أو `CPL-*.ST-WW`)».

**والخلل:** «القناة النشطة» **تختلف باختلاف المصدر** — بنصّ `M3-01 §4` نفسه:

| القناة | متى تكون نشطة | الترميز |
| :--- | :--- | :--- |
| `IR→ST` شحنة مزمنة | `IR` **ضعيفة** | `WS`/`WW` ✅ |
| `CF→ST` نمط جامد | `CF` **ضعيفة** | `WS`/`WW` ✅ |
| **`BI→ST` كبح متراكم** | **`BI` قويّة** (مع غياب التسليم) | 🔴 **`SW`/`SS`** |

فالشرط الحالي **يستبعد قناة `BI` النشطة** ويحسب مكانها `CPL-BI.ST-WS` — وهي بنصّ الخلية 19 **قناة خفيفة لا نشطة**. النتيجة: **الترميز عُكس في `BI`**.

**أثره في `SYN-02`:** `ST = 46%` وقناتان تصبّان فعلياً (`[16]` من التهدئة · `[18]` احتقان الكبح) — و`HF-04` **لم يُعلن**. **إنذار تشبّع ضائع (سلب كاذب).**

**المطلوب:** تصحيح الشرط ليصير: `CPL-IR.ST-W*` · `CPL-CF.ST-W*` · **`CPL-BI.ST-S*`** — ثلاث قنوات بشرط كلٍّ منها.

## 6.2 `GAP-DRY-04` — **حالة `OUT` غير معرَّفة في طبقة الاقتران** 🔴 **عالية**

`ح-1` تعرّف قطبين: `≤50%` و`>70%`. و**`OUT` (`SP < 0`) غير مذكور** — وهو موصوف في `DEC-064` **فجوةً تشغيلية** لا نطاقاً.

| الاحتمال | الأثر |
| :--- | :--- |
| **(أ) `OUT` = قطب ضعيف** | تُفعَّل خلايا على قيمة تسمّيها المواصفة «فجوة تشغيلية» |
| **(ب) `OUT` مستبعَد** | صاحب أشدّ الحالات **لا ينال قراءة مركّبة إطلاقاً** |

**والخيار (ب) هو ما يحدث اليوم بحكم الصمت** — بلا قرار.
**المطلوب:** قرار صريح. **ترجيحي: (ب) مع تصريح** — تُستبعد المهارة `OUT` من الاقتران، **ويُعلَن ذلك** بجملة وصل، لا أن يختفي القسم بلا تفسير.

## 6.3 `GAP-DRY-05` — **نصّ الحالة «مركّب» يَعِد بما لا يوجد** 🟡 **متوسطة**

نصّ «مركّب» ينتهي بـ: «**وفيما يلي أين تقع تلك الكلفة**». وفي `SYN-06` **لا خلايا ولا كلفة** — فالجملة تَعِد بقائمة فارغة.

**والتضارب أدقّ:** `G4` تنتج «مركّب» (لأن التكوين ليس كله عالياً ولا كله محدوداً)، بينما `§8.4` تقول **لا قراءة مركّبة**. فأي منهما يُنفَّذ؟

**المطلوب:** أحد أمرين — **(أ)** `§8.4` تُلغي نصّ `G4` أيضاً لا القسم ٦ وحده · **(ب)** حالة رابعة **«متوازن»**: «قدراتك الخمس في النطاق الأوسط — لا جانب يفرض نفسه على البقية».
**ترجيحي: (ب)** — الصمت التام يترك القارئ بلا إجابة، والحالة الرابعة **تصف الواقع بصدق** بلا اختراع كلفة.

---

# القسم 7: ✅ ما أثبتته الاختبارات

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | **الشرط الناقص لا يُكمَّل** — `HF-01` لم يُعلن بحافة واحدة (`SYN-02`) | ✅ |
| 2 | **`س-3`** منعت 7 جمل متطابقة (`SYN-03`) | ✅ |
| 3 | **ترتيب `ق7`** أثبت قيمته: التحفّظ قبل «كل قدراتك عالية» | ✅ |
| 4 | **تحفّظ `HF-04`** ظهر في أخطر موضع: ملف كله منخفض (`SYN-04`) | ✅ |
| 5 | **`G2` انتقائية** — سؤال واحد في `SYN-02`، أربعة في `SYN-03`، صفر في `SYN-04` | ✅ |
| 6 | **الحياد يُسقط أزواجه** — `CF` المحايدة أسقطت ثلاثة أزواج بلا أثر جانبي | ✅ |
| 7 | نصّ `OUT` ومدخل الضغط الأقصى | ✅ |

---

# القسم 8: بنود الاعتماد المطلوبة (`DEC-133` المقترح)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | **تصحيح شرط `HF-04`** — ثلاث قنوات بشرط كلٍّ منها (`GAP-DRY-03`) 🔴 | ⬜ |
| 2 | **حسم `OUT`** في طبقة الاقتران (`GAP-DRY-04`) 🔴 | ⬜ |
| 3 | **حسم الحالة «مركّب» عند صفر خلايا** (`GAP-DRY-05`) | ⬜ |
| 4 | اعتماد `SYN-01…06` **حزمة انحدار مرجعية** — تُعاد بعد كل تعديل | ⬜ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| الحالات المشغَّلة | **5/5** |
| عيوب مكتشفة | 🔴 **3** — عاليتان ومتوسطة |
| منها ما كان الفحص النظري ليكشفه | **1 من 3** فقط (`OUT`) |
| سلوكيات مؤكَّدة | **7** |
| القياس | ⚪ لم يُمسّ |
| **حالة الوثيقة** | 🟡 **`[مقترح]` — بانتظار `DEC-133`** |
