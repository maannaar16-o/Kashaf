# 64-K3-COMPOSITION_Layer_Methodology.md
## مخطط منهج طبقة التركيب — قراءة الاقترانات المفعَّلة معاً (`GAP-CPL-01`) + فصل طبقات `ST` (`GAP-URS-02`)

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `64-K3-COMPOSITION-METHOD` |
| **الإصدار** | **v1.1 — ✅ معتمد** (`DEC-106` · مُحدَّث بـ`DEC-112`: إضافة المستوى الثاني) |
| **الغرض** | تثبيت **منهج** طبقة التركيب قبل بنائها — على سابقة `61-METHOD` قبل `62-REGISTER` |
| **السند** | `DEC-104` (طبقة تركيب فوق السجل) · `DEC-103` (دور `ST` المزدوج مفصول الطبقة) · `DEC-101` · `DEC-100` · `M3-03 §1` · `56-K3-URS §4/§5` |
| **الحدّ الحاكم** | طبقة **تفسير** حصراً · صفر أوزان · صفر عتبات مستحدثة · **لا مقارنة مقدارية** · لا صيغة تجميع رقمية |

---

# القسم 1: القرارات المستوعَبة

| القرار | النص | الأثر |
| :--- | :--- | :--- |
| **`DEC-103`** | **`GAP-URS-02` محسومة — الخيار (ج): `ST` دور مزدوج مفصول الطبقة.** طرفاً في عائلة التحميل · مُعاملاً في طبقة العرض النهائي فقط · **مع نص مانع للازدواج** (§5) | يفتح بناء المرحلة (ب) بكامل 16 خلية |
| **`DEC-104`** | **`GAP-CPL-01` محسومة — تُبنى طبقة تركيب فوق السجل** (لا منع تركيب) | هذه الوثيقة منهجها |
| **`DEC-105`** | **`GAP-CPL-02` محسومة — الكود المعتمد `62-K3-COUPLING-REG`** (لا `52-MATRIX-K3-02`) | ✅ مغلقة |

---

# القسم 2: موقع الطبقة في المكدس

```
① المُدخَلات      : درجات المهارات الخمس ← نطاقات DEC-064 (قياس — لا يُمسّ)
② طبقة الاقتران   : 62-REGISTER (28 خلية) — أي الخلايا تُفعَّل (خلية = حافة واحدة)
③ طبقة التركيب    : ← هذه الوثيقة — على مستويين:
     ③-أ  التفعيل والترتيب : الخلايا المفعَّلة تُقرأ معاً (§3 · §4)
     ③-ب  التعرّف على النمط: 65-PATTERN-RECOG (DEC-112) — متى تُقرأ خلايا
                              متجمّعة نمطاً واحداً (HF-01/02/04) بدل قراءات موزّعة
④ طبقة العرض      : وَسْما الميتا: الثقة (EP) · الحمل (ST)   [غير رقميين]
⑤ المخرج          : قراءة نصية + كتلة M3-07 الموحّدة (S5)
```

**ترتيب التنفيذ داخل ③:** يسبق المستوى (أ) — تُفعَّل الخلايا وتُرتَّب — ثم يعمل (ب) على المفعَّل منها. **والنمط المتعرَّف عليه يتقدّم على خلاياه** (`65 §4/ع-1`).
**قفل ازدواج:** `HF-03` (الثقة) و`HF-05` (كتلة `M3-07`) **لا يُنفَّذان في ③-ب** — موضعهما ④ و⑤ (`65 §2.1`).

**الحدّ بين ② و③:** الخلية تصف **حافة**؛ التركيب يصف **نمطاً**. لا يُنشئ التركيب آلية جديدة — يُنظّم قراءة آليات معتمدة.
**بعد `DEC-101`:** مخرج الطبقة ③ **هو** مادة `URS` v3.0. لذا اعتماد هذه الوثيقة **شرط سابق** للخطوة ④.

---

# القسم 3: فضاء التفعيل — الحدّ البنيوي **[مشتق]**

الاقتران يُستدعى عند الأقطاب فقط (`ح-1`)، فالخلايا المفعَّلة في ملف واحد محدودة بنيوياً:

| الحالة | العدد الأقصى للخلايا المتزامنة |
| :--- | :--- |
| العناصر الأربعة (`IR`/`BI`/`CF`/`ST`) كلها عند قطب | **6** (`C(4,2)=6` أزواج) |
| + `EP` عند قطب أيضاً | **10** (`C(5,2)=10`) |
| أي عنصر في النطاق المحايد (`50–70%`) | تسقط كل أزواجه — **الصمت مشروع** |

**النتيجة:** لا يواجه التركيب 40 خلية أبداً — بل **6 كحدّ أقصى عملي** (10 مع `EP`). **`GAP-CPL-01` أصغر عملياً مما بدت.**

---

# القسم 4: القواعد الثلاث المطلوبة

## 4.1 الترتيب — **بنيوي لا مقداري**

| الرتبة | العائلة | السند |
| :--- | :--- | :--- |
| **1** | المحور (`IR–BI` · `IR–CF`) | موضع **الجذر** المحتمل — المركز يبثّ للذراعين |
| **2** | الجانبي (`BI–CF`) | تكامل الذراعين |
| **3** | التحميل (`×–ST`) | **موضع الظهور** لا الجذر — `ST` المصبّ الأعظم |
| **4** | التضخيم (`EP–×`) | يُقرأ **وسماً** لا نمطاً (`ق-ج`) — §5.2 |

**السند:** «الجذر ≠ موضع الظهور» (`M3-03 §1`) + قاعدته التشغيلية: لا يُسند عطل لعنصر إلا بعد قراءة الملف كاملاً.
🔒 **حدّ نقاء:** هذا ترتيب **عرض بنيوي**، لا ترتيب أهمية ولا شدة ولا أولوية. أي قراءة له كتفضيل = حكم مقارن محظور (`NORM-01`).

## 4.2 حدّ الإشباع — **بالبنية لا بالعدد**

**القاعدة المقترحة:** المخرج **ليس سرداً للخلايا**. الخلايا مادة داخلية للمحرك (`DEC-100 ق-أ`)، وطبقة التركيب تُنتج **قراءة واحدة مركّبة** تستوعبها بالترتيب أعلاه.
**الأثر:** ينتفي الإشباع بلا اختراع سقف عددي. *(إن أُريد سقف عرض صريح لاحقاً، فهو قرار مستقل.)*

## 4.3 القراءات المتجاورة الظاهرة التعارض — **قاعدة عدم التعارض**

> **كل خلية تصف حافة مختلفة؛ فلا تناقض بين خليتين — بل توصيف موزّع.**

مثال: `CPL-IR.BI-WS` (احتواء بكلفة) مع `CPL-IR.CF-WW` (تفاقم) في ملف واحد ← لا تعارض: الأولى تصف حافة البوابة، الثانية حافة المراجعة. **القراءة الصحيحة:** نظام يحتوي مخرجه ولا يُنجز تكيّفه.
**القاعدة:** يُحلّ التعارض الظاهري **بإسناد كل عبارة إلى حافتها**، لا بترجيح إحداهما ولا بحذفها.
**الكُلف تُجمع وصفياً لا حسابياً:** تُذكر كل كلفة بموضعها؛ يُحظر جمعها في مقدار أو «مستوى استنزاف».

---

# القسم 5: نص فصل الطبقات — مانع الازدواج (`DEC-103`)

## 5.1 `ST`

| الدور | الطبقة | ما يُقال |
| :--- | :--- | :--- |
| **طرف** | ② الاقتران | **القناة**: أي بقايا عمل تنزل حملاً على `ST` (شحنة مزمنة · كبح متراكم · جمود مستمر) — آلية |
| **مُعامل** | ④ العرض | **السعة**: وَسْم واحد يؤطّر القراءة كلها — «ملف النظام **تحت الحمل**، لا في الراحة» (`URS §5`) |

> ### 🔒 نص مانع الازدواج
> **طبقة العرض لا تعيد اشتقاق الحمل من خلايا التحميل، ولا تُنتج أي مقدار أو مستوى استنزاف.** دورها **وَسْم إطاري واحد**، لا حساب ولا تكرار. وبالمقابل: **خلايا التحميل لا تصف سعة `ST` ولا تحكم على حالتها** — تصف القناة النازلة إليها فقط. كل حمل يُذكر **مرة واحدة، في طبقته**.

**تحصين إضافي:** `ST` منخفضة بعد تحميل من ≥ قناتين **لا تُقرأ ضعفاً أصيلاً** — قد تكون سليمة بعبء مضاعف (`HF-04`).

## 5.2 `EP` — التماثل والفارق **[مشتق]**

`EP` تبقى **مُضاعِفاً لا طرفاً** (`DEC-100 ق-ج`) — لم يُغيّرها `DEC-103`. لكن سابقة فصل الطبقات تُتيح للمرحلة (ج) مساراً متماثلاً: **خلايا `EP` تُبنى وصفاً لقناة التضخيم (طبقة ②)، ويبقى أثرها في المخرج وَسْم ثقة واحداً (طبقة ④)** — بلا نمط مستقل وبلا ازدواج.
🟡 **هذا `[مقترح]` لا يُعتمد هنا** — موضعه قرار المرحلة (ج).

**حصيلة طبقة العرض:** وَسْمان اثنان فقط، كلاهما **غير رقمي**: **الثقة** (`EP` — `URS §4`) و**الحمل** (`ST` — `URS §5`).

---

# القسم 6: مخرج طبقة التركيب

| ينتج | لا ينتج |
| :--- | :--- |
| وصف **نمط معالجة** واحد مستوعب للخلايا المفعَّلة بالترتيب البنيوي | ❌ تسمية أو تصنيف (`URS §6/4`) |
| بيان **الكُلف** كل في موضعها | ❌ درجة مركّبة أو مؤشر تجميعي |
| وَسْمَي الثقة والحمل | ❌ ترتيب مقداري بين العناصر (`NORM-01`) |
| إحالة إلزامية لكتلة `M3-07` عند أي طرف `>70%` | ❌ ادّعاء أصالة القوة (`POLES-01`) |

---

# القسم 7: حدود النقاء

1. **لا آلية جديدة** — التركيب ينظّم قراءة خلايا معتمدة ولا يستحدث علاقة.
2. **احتمال لا حتمية** في كل عبارة مركّبة.
3. **احتواء ≠ تعويض** يبقى نافذاً عبر التركيب كما داخل الخلية.
4. **صفر لغة عيادية · صفر سمة ثابتة** (`P = C + G`).
5. **`EP ≤50%`** ← التركيب كله يُقدَّم **غير حاسم** (`HF-03` · `ح-3`).
6. **الصمت مشروع** — ملف بلا أقطاب ينتج قراءة مهارة-بمهارة بلا طبقة تركيب.

---

# القسم 8: الفجوات

| الكود | البيان | الخطورة | المطلوب |
| :--- | :--- | :--- | :--- |
| `GAP-CMP-01` | **صياغة** القراءة المركّبة (لغة المخرج) غير محدَّدة — لأنها من نصيب الخطوة ⑤ (ربط المحرك) | منخفضة | تُحسم مع صياغة المحرك |
| `GAP-M3-01-01` (موروثة) | حافات العودة مشتقة منطقياً غير محقّقة ميدانياً | متوسطة | تبقى توصف ولا تُقاس |

---

# القسم 9: بنود الاعتماد المطلوبة (`DEC-106` المقترح)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | موقع الطبقة في المكدس الخماسي (§2) | ⬜ |
| 2 | الحدّ البنيوي للتفعيل: 6 خلايا (10 مع `EP`) — لا 40 (§3) | ⬜ |
| 3 | الترتيب البنيوي: محور ← جانبي ← تحميل ← تضخيم (§4.1) | ⬜ |
| 4 | حدّ الإشباع بالبنية: قراءة مركّبة واحدة لا سرد خلايا (§4.2) | ⬜ |
| 5 | قاعدة عدم التعارض + جمع الكُلف وصفياً لا حسابياً (§4.3) | ⬜ |
| 6 | **نص مانع الازدواج لـ`ST`** (§5.1) — تنفيذ `DEC-103` | ⬜ |
| 7 | مخرج الطبقة وحدوده (§6) + حدود النقاء (§7) | ⬜ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| `GAP-URS-02` | ✅ **محسومة** (`DEC-103` — الخيار ج) · نصها التنفيذي في §5.1 |
| `GAP-CPL-01` | ✅ **مغلقة** — الاتجاه (`DEC-104`) والمنهج (`DEC-106`) |
| المرحلة (ب) | 🟢 **مفتوحة للبناء** |
| صفر هلوسة | ✅ لا أوزان ولا عتبات ولا سقوف عددية مخترعة |
| **حالة الوثيقة** | ✅ **v1.1 — معتمدة (`DEC-106` + `DEC-112`)؛ حاكمة على قراءة السجل كله وعلى بناء `URS` v3.0** |
