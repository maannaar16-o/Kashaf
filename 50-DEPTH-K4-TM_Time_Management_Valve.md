# 50-DEPTH-K4-TM_Time_Management_Valve.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `50-DEPTH-K4-TM` |
| **السلسلة** | `50-DEPTH` — **بطاقة الصمام** بقالب $K_4$ المخصص (`131 §2` · `TRF-009`) على القالب المرجعي `50-DEPTH-K4-WM` |
| **الصمام** | تقدير الوقت وإدارة الزمن (`K4-TM` / Time Estimation & Management) — **المحطة `6/7` على المسار** |
| **الإصدار** | v1.0 |
| **حالة الاعتماد** | ✅ **مختوم بأمر المالك «نفذ طبقة العمق»** — 2026-08-12 (`DEC-261` · `131-K4-DEPTH`) |
| **السند المؤسس** | `14-CORE-K4 §2/6` و`§3/أ` · `129-K4-DUAL-CHARTER` (`DEC-258`) · `130-K4-RELATIONS` (`DEC-259`/`DEC-260` — ومنه قيد «تفاقم» `K4-REL-12`) · `19-SEED-K4` v1.1 |
| **وسم عام** | كل اشتقاق **`[اشتقاق غير محقّق ميدانياً]`** (`TRF-002`) |

## 0.1 أقفال العزل المنطبقة على هذا الصمام

| القفل | النص الحاكم | المصدر |
| :--- | :--- | :--- |
| **داخلي — `TM` ⚔️ `PF`** | «الالتزام بالأولويات يحدد "ما الذي يجب أن يُنفذ"... بينما تقدير الوقت يحدد **"المدة والسرعة الواقعية"** (الإحساس الزمني للإنجاز)... والخلل في الثانية ينتج "خطة غير واقعية زمنياً"». | `14-CORE-K4 §3/أ` — **[معتمد]** |
| **خارجي — مقابل $K_2$-`R`** | تقييم الموارد **نظرياً** عملية معرفية ($K_2$)؛ `TM` **إحساس زمني ميداني** «في ظل ظروف واقعية» — واقعيةُ التفكير لا تُقرأ إحساساً زمنياً، ولا العكس. | `14-CORE-K4 §3/ج` — **[تخصيص مشتق]** |
| **سقف عام** | لا قراءة عبر جدار العزل (`DEC-205` قياساً) · **لا قراءة فارق زمني (`DEC-244`)** — إدارةُ الزمن مهارةً غير قراءةِ الفارق الزمني بين تطبيقين · `DEC-246`. | `129 §2/④` — **[معتمد]** |

## 0.2 قاعدة الوسم

كما في القالب المرجعي (`50-DEPTH-K4-WM §0.2`) — والبطاقة شكل تفسيري لا يقيس.

---

# القسم 1: بطاقة الصمام — `K4-TM`

## 1.1 الهوية التنفيذية والوظيفة — **[معتمد]**

| الحقل | البيان |
| :--- | :--- |
| **التعريف الحرفي** | «مهارة تنفيذية لإدراك الزمن الحقيقي اللازم للإنجاز، وتحليل الجهد الزمني الفعلي في ظل ظروف واقعية وتحديد وقت تسليم واقعي.» (`14-CORE-K4 §2/6`) |
| **سؤال التشغيل الداخلي** | *كم يستغرق هذا فعلاً — ومتى أسلّمه واقعياً؟* — **[مشتق]** من شقّي التعريف (إدراك الزمن الحقيقي · وقت التسليم الواقعي) |
| **وعاء الحساب (11 بنداً)** | 9(أ) · 17(ب) · 30(ب) · 44(ب) · 56(ب) · 66(ب) · 75(ب) · 83(ب) · 87(ب) · 90(ب) · 92(ب) |

## 1.2 الموضع على المسار — **[معتمد الترتيب · مشتق القراءة]**

| الحقل | البيان |
| :--- | :--- |
| **المحطة** | السادسة من سبع (`129 §2/①`) |
| **ما يستقبله (يستهلكه)** | خطة الأولويات المختارة ونظام الموارد المرتب من `PF`/`OR` — ما يحتاج إطاراً زمنياً واقعياً **[مشتق]** |
| **ما يسلّمه** | **نافذة زمنية واقعية** — وقت تسليم صادق تُصرف داخله المثابرة في الإكمال لا في الإنقاذ **[مشتق]** |
| **قيد القراءة** | الترتيب سند قراءة لا قياس (`129 §2/①`) |

## 1.3 مظهر الانسداد — قراءتا الانقطاع وعنق الزجاجة

| القراءة | البيان | الوسم |
| :--- | :--- | :--- |
| **الانقطاع عند المحطة** | «**خطة غير واقعية زمنياً**» — نص الجدار حرفاً: الزمن المُدرَك أقصر من الحقيقي ووقت التسليم غير صادق | **[معتمد]** `§3/أ` |
| **عنق الزجاجة (ما بعده)** | «سوء تقدير الزمن يستهلك المثابرة في **سباقات إنقاذ متكررة**» — مثال الميثاق المختوم؛ وهو الصمام الأعلى حملاً في قيود الهدر المثبتة (فاعلُ ثلاثةٍ منها — عدٌّ وصفي للسجل لا قياس) | **[معتمد]** `129 §3/①` + **[إحالة]** `130 §2` |
| **حدّ القراءتين** | وصفيتان — لا عتبة (`129 §2/①`) | **[معتمد]** |

## 1.4 المشغّل والمعطّل — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المشغّل الأمثل** | ظروف واقعية يُحلَّل عليها الجهد الزمني الفعلي — فالتعريف يقيّد الإدراك بـ«ظروف واقعية» لا بأمنية الخطة |
| **المعطّل** | (أ) تقدير على الحالة المثلى لا الظروف الفعلية. (ب) غياب نافذة إغلاق مع ضعف إكمال — فيتفاقم الأثران معاً (مشهد `K4-REL-12` بالإحالة) |

## 1.5 موقع الصمام في الشبكة — **[إحالة معتمدة إلى `130`]**

الصمام طرف في **أربعة قيود** — فاعلُ هدرٍ ثلاث مرات حال ضعفه، وطرفُ قيد «تفاقم» الأول:

| القيد | موقع `TM` فيه | النوع |
| :--- | :--- | :--- |
| `K4-REL-07` — `TM (ضعيف) → OR (قوي)` | **فاعل هدر**: النظام المتقن بلا إدراك زمني يبقى ترتيباً لا إنجازاً في وقته | هدر وتثبيط |
| `K4-REL-08` — `TM (ضعيف) → PER (قوي)` | **فاعل هدر**: المثابرة تُصرف في تعويض خطط غير واقعية بدل الإكمال في وقته | هدر وتثبيط |
| `K4-REL-11` — `TM (ضعيف) → PF (قوي)` | **فاعل هدر**: خطة الأولويات الصحيحة الاتجاه تسقط قابليتها للتنفيذ | هدر وتثبيط |
| `K4-REL-12` — `TM (ضعيف) × PER (ضعيف)` | **طرف تفاقم** (بنية متبادلة بلا اتجاه — النوع الخامس `DEC-260`): كلُّ ضعفٍ يوسّع أثر الآخر | **تفاقم** |

**فجواته الموثقة** (`TRF-007`): `WM–TM` · `TI–TM` · `F–TM` — لا قيد لها.
**مادة واردة:** الاحتكاك الثلاثي `PF+TM+PER` (`SEED-K4-08`) — لمرحلة الأنماط (`130 §4/ب`).

## 1.6 الحدود وجدران العزل

مفصّلة في `§0.1` — الجدار الحاكم: الفاصل الداخلي `TM`⚔️`PF` (الإسناد قائم: خطة غير واقعية ⇒ وقت · انشغال بثانوي ⇒ أولويات؛ النص المانع في `K4-REL-11`)؛ ويلزم هذه البطاقةَ خاصةً قيدُ `DEC-244`: لا يُستدل من مهارة الزمن على أي قراءة فارق زمني بين تطبيقين — **[معتمد]**.

## 1.7 ديون الوعاء المثبتة — **[معتمد — `128 §4`]**

لا دَين مثبتاً على وعاء `TM`.

---

# القسم 2: سلّم النطاقات وقراءته لهذا الصمام

**[معتمد — `DEC-261` · `TRF-010/011`]** البنية والدلالة كما في القالب المرجعي — القراءة وصفية، ولا يُستدل من نطاق `TM` على قيود شبكته (وهي الأكثر) ولا العكس:

| الكتلة | المدى (SP) | الدلالة | قراءة `TM` **[مشتق]** |
| :--- | :--- | :--- | :--- |
| `OUT` | SP<0% | فجوة تشغيلية — لا يُصنَّف | — |
| `L−` / `L` | 0–40% | **حضور محدود** | الإدراك الزمني غير منتظم — التقدير يجنح عن الزمن الحقيقي |
| `M` | 40–50% | **حضور محدود** (طرفه الأعلى) | — |
| `M+` | 50–70% | **كفاءة أساسية** | تقدير وتسليم واقعيان في الظروف العادية |
| `H` / `H+` | 70–100% | **قدرة عالية** | إحساس زمني صادق حتى مع تغيّر الظروف وحمل معتدل |
| `H++` | SP>100% | **قدرة عالية** (ذروتها) | — |

---

# القسم 3: الملحق القياسي — **[معتمد]**

| العنصر | القيمة |
| :--- | :--- |
| وعاء `K4-TM` | 9(أ) · 17(ب) · 30(ب) · 44(ب) · 56(ب) · 66(ب) · 75(ب) · 83(ب) · 87(ب) · 90(ب) · 92(ب) — 11 بنداً |
| `MaxRaw` | 66 · `SS = x − 2z + y` ≡ `x + 3y − 22` (`DEC-028`) · `SS_max = 77` |
| النسبة المعيارية | `SP = SS ÷ 66 × 100` (`41 §3.3` · `DEC-065`) · `SP_max` ≈ 116.7% |
| ديون عاملية على الوعاء | لا شيء (`128 §4`) |

---

# القسم 4: سجل الفجوات والإحالات

| البند | البيان |
| :--- | :--- |
| فجوات الشبكة الثلاث (`§1.5`) | موثقة في `130 §3` — لا تُملأ هنا |
| بذور التصميم | `SEED-K4-03` (دليله في `K4-REL-07`) · `SEED-K4-04` (دليله في `K4-REL-12` — حالة الضعفين المعتمدة) · `SEED-K4-08` (الثلاثي — مرحلة الأنماط) |
| `GAP-K4-CASES-01` | مفتوحة (`129 §6`) — تسري على هذه البطاقة |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| بطاقة صمام على قالب `131 §2` | ✅ |
| النصوص المعتمدة بموضعها | ✅ التعريف · «خطة غير واقعية زمنياً» · مثال سباقات الإنقاذ المختوم |
| الشبكة إحالةً (`م-2`) | ✅ أربعة قيود (منها قيد التفاقم الأول) + ثلاث فجوات |
| قيد `DEC-244` مثبَّت على البطاقة | ✅ `§0.1`/`§1.6` |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة (`DEC-261`)** |
