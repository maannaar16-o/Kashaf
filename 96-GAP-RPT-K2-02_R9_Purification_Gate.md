# 96-GAP-RPT-K2-02 — بوابة التطهير `R9` : المسح والحكم

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | مراجعة سيادية · قياس وتدقيق |
| **حالة الاعتماد** | **معتمد** (المسح والقياس) · **مقترح** (المسارات — §6) |
| **نطاق الاستدلال** | `51-MATRIX-06 §3.1/R9` + س68 · `55-USER-K2-* §4` (٨ قواميس) · `k2_engine.audit_isolation` · `k3_engine.G5_CHECKS` |
| **الغرض** | مادة قرار `DEC-212` |
| **الإصدار** | v1.0 |

---

# القسم 1: القواميس مستخرَجة وكاملة ✅

| القاموس | البنود | القاموس | البنود |
| :--- | :-: | :--- | :-: |
| `PUR-A` | 8 | `PUR-S` | 9 |
| `PUR-R` | 9 | `PUR-E` | 10 |
| `PUR-C` | 9 | `PUR-St` | 10 |
| `PUR-O` | 8 | `PUR-H` | 9 |
| | | **الإجمالي** | **72** |

**`51-MATRIX-06` س68 يقول «~72 بنداً».** المطابقة العددية التامّة **تحقّق مستقلّ** على أن الاستخراج كامل ولم يسقط منه شيء.

كل بند ثلاثي: **❌ محظور · السبب · ✅ البديل المعتمد**.

---

# القسم 2: 🔴 اكتشاف أول — بوابة $K_2$ **ميتة بنيوياً**

```python
audit = audit_isolation(*(content.texts_for(prof) if content else []))
```

`k2_report` يستدعي `run()` **بلا `content`** ⇒ الوسيط `()` فارغ ⇒ **البوابة تُعيد `[]` دائماً**.

ووسم `isolation: "clean"` في كل تقرير صادر **لم يكن نتيجة فحص — بل نتيجة عدم فحص**.

## 🔴 اكتشاف ثانٍ — وحين فُحص المتن فعلاً، البوابة **أطلقت**

```
audit_isolation(نصّ التقرير) → ['انفعال']
```

**الموضع** (`E/U-11` — «ما لا تعنيه هذه العدسة»):

> «…فالفهم عدسة **والانفعال دائرة أخرى**.»

| الحقل | البيان |
| :--- | :--- |
| **الطبيعة** | 🟢 **ليس خرقاً — بل إنفاذ للقفل** |
| **الوظيفة** | السطر يقيم جدار العزل `K2-E ≠ K3` صراحةً: يمنع قراءة «المتفهم» كانفعال |
| **المشكلة** | البوابة **لا تميّز الذكر من الخلط** — تحظر اللفظ لا الاستعمال |

⇒ **إنذار كاذب**، لكنه كشف أن البوابة معطَّلة أصلاً.

---

# القسم 3: 🔴 اكتشاف ثالث — `R9` غير قابل للتنفيذ كما صيغ

اختبرتُ القواميس الـ72 على تقرير حقيقي:

| نمط المطابقة | إصابات | صحيحة | كاذبة |
| :--- | :-: | :-: | :-: |
| العبارة المحظورة **كاملة** | **0** | 0 | 0 |
| شظايا (تقسيم على `/`) | **7** | **0** | **7 · 100٪** |

**الشظايا السبع وسياقها الفعلي:**

| الشظية | أين ظهرت |
| :--- | :--- |
| «استراتيجي» | **اسم عدسة في اللوحة**: `الاستراتيجي (St)` |
| «اجتماعي» | **اسم عدسة في اللوحة**: `الاجتماعي (S)` |
| «عبقري» | داخل نصّ معتمد ينفي العبقرية: «ويفتقدها **عبقري**» |
| «جامد» | «القوالب **الجامدة**» — وصف معتمد |
| «بيروقراطي» | «**البيروقراطية** التي تعاقب» |
| «حساس» | «**حساسية** موقعه بين الناس» |
| «بارد» | «الوضع **البارد**» — حالة ضغط معتمدة |

**اثنتان منها تُطلقان على اسمَي عدستين في اللوحة** ⇒ البوابة الشظوية **تُنذر في كل تقرير بلا استثناء**.

## السبب البنيوي

قواميس `PUR` **إرشاد تحريري لكاتب بشري**، لا رموز قابلة للفحص الآلي. وعباراتها توصيفية («أنت صاحب رؤية / استراتيجي / موجّه») — الشرطة المائلة فيها **بدائل صياغة** لا كلمات مستقلة.

**والأهمّ:** صنف الخطأ الذي تحرسه `PUR` — أن يكتب كاتبٌ توصيفاً محظوراً — **مستحيل بنيوياً في محرك يجمّع ولا يؤلّف**. المحرك لا ينتج إلا ما في الحزم.

---

# القسم 4: ما يبقى قابلاً للتنفيذ

| الطبقة | قابلة؟ | الحال |
| :--- | :---: | :--- |
| **العبارة الكاملة** على المخرج | ✅ | صفر إصابة اليوم · تحرس ضد **تلوث حزمة مستقبلي** |
| **العبارة الكاملة** على الحزم عند التحميل | ✅ | نظير `g5_scan_content` في $K_3$ |
| **الفئات** (عيادي · تفاضلي · سببي · تنبؤي) | ✅ | $K_3$ يملكها · $K_2$ يملك **11 لفظاً فقط** بلا تصنيف |
| **الشظايا** | ❌ | 100٪ إنذار كاذب |

## الفجوة بين الدائرتين

| | $K_2$ | $K_3$ |
| :--- | :--- | :--- |
| البوابة | `_FORBIDDEN` — **11 لفظاً · بلا فئات · معطَّلة** | `G5_CHECKS` — **7 فئات · 33 رمزاً · نشطة** |
| مرحلتان (محتوى + مخرج) | ❌ | ✅ |

---

# القسم 5: أثر جانبي — «انفعال» في `_FORBIDDEN`

لو فُعِّلت بوابة $K_2$ كما هي، **لأوقفت كل تقرير مركزه `E` أو يعرض `E`** — بسبب سطر معتمد يقيم جدار العزل.

⇒ تفعيل البوابة يستلزم **تمييز الذكر من الخلط**. وأبسط صياغة تحقّقه: حظر **العبارات المركّبة** («انفعال المستفيد» · «شدة الاستثارة») لا **الألفاظ المفردة**.

**هذا تحليل، لا تعديل.** تغيير `_FORBIDDEN` يمسّ قائمة قائمة ⇒ قرارك.

---

# القسم 6: المسارات

| المسار | الوصف | الأثر |
| :--- | :--- | :--- |
| **أ — تنفيذ حرفي** | شظايا `PUR` بوابةً | ❌ إنذار في كل تقرير · مرفوض بالقياس |
| **ب — العبارة الكاملة** | `PUR` بوابة على العبارات التامّة · مرحلتان | ✅ صفر إنذار كاذب · يحرس ضد تلوث الحزم · **لكنه لا يمنع صياغة محظورة لم تُكتب حرفياً** |
| **ج — الفئات** | نقل بنية `G5` إلى $K_2$ مع فئاته الخاصة المستخرَجة من «السبب» في القواميس الـ72 | ✅ يحرس **الصنف** لا اللفظ · يوازن الدائرتين · **يحتاج تصنيف 72 بنداً** |
| **د — ب + ج** | الطبقتان معاً | أقوى تغطية · أعلى كلفة |
| **هـ — الإقرار بالحدّ** | توثيق أن `PUR` **إرشاد تحريري لا بوابة آلية** · وإغلاق `R9` بوصفه غير قابل للأتمتة | صادق · لكنه يترك $K_2$ بلا بوابة فعّالة |

---

# القسم 7: المطلوب

| # | البند | الكود |
| :--- | :--- | :--- |
| 1 | مسار `R9` — أ / ب / ج / د / هـ | `DEC-212` |
| 2 | تفعيل بوابة $K_2$ على المتن (هي معطَّلة اليوم) | `DEC-213` |
| 3 | معالجة «انفعال» في `_FORBIDDEN` — عبارات مركّبة بدل ألفاظ مفردة | `DEC-214` |

> **البند 2 مستقلّ عن 1:** البوابة معطَّلة أياً كان مسارك. وتفعيلها بلا البند 3 يوقف كل تقرير يعرض `E`.

**الترقيم التالي:** `DEC-215`.

---

> **[نهاية الوثيقة — `96-GAP-RPT-K2-02` v1.0 · القواميس مستخرَجة 72/72 · البوابة معطَّلة · التنفيذ الحرفي مرفوض بالقياس]**
