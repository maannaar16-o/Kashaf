# 149-WORKSHOP-SURFACE — `DEC-279`: سطح الورشة وخادمُه — **قفلٌ يُضيَّق لا يُرفع**

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفّذ المرحلة ②**» ثم «**واصل**» — 2026-08-14 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-14 |
| **الأثر** | مسار الورشة **يعمل من طرفٍ إلى طرف**: بوابة إذنٍ ورمز · الاستبيان نفسه · توليدٌ محلي · إرسالٌ إلى خادمٍ محلي · **أداةُ المشرف بوابةَ قبول** · مخزنٌ خارج المستودع — والبصمات الستّ **حرفياً** والبوابة **24/24** |
| **التغيير المرافق** | `CHG-098` |
| **الحاكم** | `DEC-277` (مسار الورشة وشرطه) · `DEC-250` (وعد الموقع العام) · `DEC-110` (قفل صفر الشبكة) · `DEC-271` (تغطية المشرف للدوائر الثلاث) · `DEC-252` (صيغة الأرشيف) · `DEC-035` (تسلسل الاستبيان) · `CHG-054` (المولَّد لا يُرفع) |
| **الكود** | `149` — تمديد بمسوّغه: سطحُ تشغيلٍ مستجدّ خلف إذنٍ ورمز |

---

# §1 المبدأ الحاكم — **مسارٌ ثانٍ معلَن، لا وعدٌ منقوض**

`DEC-277 §1` قيّد القاعدة: **الوعد المنشور لا يُنقَض ليُضاف مسار — يُضاف المسار معلناً شرطه**. وهذا التنفيذ يجسّدها في الكود لا في العبارة:

| السطح | قفلُ شبكته | وعدُه للقارئ |
| :--- | :--- | :--- |
| **التطبيق العام** `docs/kashaf.html` | `CPL-08A-03` — **كل اتصالٍ ممنوع** (`DEC-110`) | «إجاباتك لا تغادر جهازك» — **كما هو، بلا حرف** |
| **سطح الورشة** `Workshop.html` | `CPL-WS-01` — كل اتصالٍ ممنوع **إلا `POST` واحداً إلى `/submit` على الأصل نفسه** | لافتةٌ في أعلى الصفحة: «مسار ورشة — نتيجتك تصل مدرّبك. هذا غير الموقع العام» |

⇒ **القفل ضُيِّق ولم يُرفع.** والفارق ليس لفظياً: القفل المرفوع يفتح كل وجهة، والمضيَّق يفتح **وجهةً واحدة بمقاسها** ويرمي ما عداها.

> **وزمنُ تشغيل التطبيق العام غير مركَّبٍ هنا أصلاً** (`CPL_OFFLINE_RUNTIME` غائب) — فلا يُقال إن قفلاً معلَناً «عُدِّل»؛ هذه صفحةٌ أخرى بقفلها الخاص.

---

# §2 القفل **مقيسٌ بتشغيله** لا بقراءة رمزه

الحقل الذي يقرأ وجود سلسلةٍ في صفحة **لا يقيس شيئاً**: قفلٌ وُسِّع خطأً يُبقي رمزَه كاملاً. وهذا عين النمط المرفوض في `00-HANDOVER §6①`.

فيُركَّب القفل في `_ws_lock_probe.js` على **نافذةٍ وهمية** وتُجرَّب عليه عشرُ محاولات:

| المحاولة | المتوقَّع |
| :--- | :---: |
| `XMLHttpRequest` · `WebSocket` · `EventSource` · `sendBeacon` | ❌ مرفوضة |
| `fetch` إلى **أصلٍ أجنبي** ولو كان مساره `/submit` | ❌ مرفوضة |
| `fetch` إلى **مسارٍ آخر** على الأصل نفسه | ❌ مرفوضة |
| `fetch` بـ`GET` إلى `/submit` · و`fetch` **بلا خيارات** | ❌ مرفوضة |
| `POST` إلى `/submit` على الأصل نفسه | ✅ **يمرّ — وإلى الأصل نفسه** |

**وحُقن توسيعٌ للقفل فسقط الفحص** بأسماء الثغرات الأربع التي انفتحت.

---

# §3 الخادم — `stdlib` وحدها · مساران لا ثالث

| القيد | التنفيذ |
| :--- | :--- |
| **صفر اعتمادية** | `http.server` من المكتبة القياسية — كبقية المشروع بقصد |
| **مساران لا ثالث** | `GET /` صفحةَ الورشة · `POST /submit` الاستقبال · **وما عداهما `404`** |
| **صفر خدمة ملفّات** | لا مسار يُشتقّ من الطلب أصلاً ⇒ **«اجتياز المسار» ساقطٌ بالبناء لا بالتنقية** — وفُحص `GET /../workshop_store.py` |
| **الربط `127.0.0.1`** | وفتحُه على الشبكة **بعَلَمٍ صريح** يطبع تنبيهه — فالمالك يفتح ما يفتح عالماً بأنه فتحه (`م-4`) |
| **حدٌّ معلَن للحمولة** | `4 MiB` — الأكبر يُردّ ولا يُقرأ |
| **سجلٌّ بلا حمولة** | لا نصّ تقريرٍ يمرّ في سجلّ الطرفية |

**ولا اعتماد يُطلب ولا يُقبل**: لا حقل كلمة مرور في الصفحة (مفحوصٌ آلياً)، وبوابةُ القبول **أداةُ المشرف** والرمزُ المُصدَر.

---

# §4 بوابة القبول — الحكم **قبل** التخزين

`workshop_store.accept` يفحص ويحكم ثم يخزّن، **ولا حالة وسطى**:

1. **البنية والإذن والرمز** — نصّ الإذن يُطابَق **حرفياً** بنصّه المعتمد، لا بمعناه.
2. **`SV.grade` على كل دائرة** — بما فيها سطح `K4` العابر (`DEC-271`).
3. **دائرةٌ ساقطة ⇒ لا تخزين**، ويُردّ الخطأ بحكمه.

**وحُقن تخزينٌ للمردود في الخادم فسقط الفحص.** ⇒ الضمانة ليست في نيّة الخادم، بل في أن **الفحص يفتح المخزن بعد الردّ ويعدّ ما فيه**.

---

# §5 البرهان الطرفيّ — **ما تُصدره الصفحة فعلاً يقبله المخزن فعلاً**

الفحص السابع لا يقرأ شكل الحمولة؛ **ينفّذ طرفيها**:

```
reports.js  (توأم JS — وهو ما يعمل في الصفحة)
    ↓ buildReportK2/K3/K4 + buildCrossingSurface
ws_payload.js  (الوحدة نفسها المضمومة في الصفحة)
    ↓ build(schema, consent, code, gen)
workshop_store.accept  (بايثون — المخزن وبوابته)
    ⇒ ثلاث دوائر نظيفة ومخزَّنة
```

## وتشغيلٌ في متصفحٍ حقيقي — لا محاكاة

شُغِّل الخادم و`Chromium` على الصفحة المولَّدة:

| المقياس | الملاحَظ |
| :--- | :--- |
| البوابة | «لا استقبال بلا إذنٍ صريح» عند تركِ الموافقة · «صيغة الرمز غير صحيحة» عند رمزٍ مشوَّه |
| التسلسل | «الكتلة ١ من ١٠ · المرحلة الأولى — الاختيار · **الخطوة ١ من ٢٨٢**» · زرّان لا غير |
| القفل حيّاً | `fetch` إلى `https://example.com` **رُمي** برسالته |
| الإرسال | «**وصلت نتيجتك** — الرمز `…` · وحكم أداة المشرف على الدوائر الثلاث: مقبول» |
| الشاشة | **صفر `SP%`** · **صفر خطأ طرفية** · `dir="rtl"` · **صفر حقل كلمة مرور** |

> **وهذا أقوى من مطابقة مخطَّط:** المخطَّط يُطابَق فيبقى احتمالُ أن السطح لا يبنيه. وهنا **السطح بناه والمخزن قبِله** — بالتنفيذ.

---

# §6 السطح نحيلٌ بقصد — وما لم يُنقل إليه

جلسةٌ مُشرَف عليها لا تطبيقٌ شخصي. فلا ملفّات ولا أرشيف ولا مرآة ولا نسخٌ احتياطية ولا إسهامٌ طوعي:

| المنقول | المتروك | سببُ تركه |
| :--- | :--- | :--- |
| البنود والخرائط والمحرّكات والتقارير **من الحزمة عينها** | `IndexedDB` وطبقة الملفّات | الجلسة واحدة ومُشرَف عليها |
| تسلسل `DEC-035` كاملاً (اختيارٌ ثم تقييمٌ جملةً جملة، بلا كشف الاختيار) | المرآة (`DEC-256`) | تُقرأ مع المدرّب لا في نافذة |
| عقد المدخل و`missingItems` قبل التوليد | الإسهام الطوعي (`DEC-253`) | مجهَّلٌ بالتصميم — ولا يجتمع مع مسارٍ مُهوّى |
| الاستمرارية في `sessionStorage` وحده | `localStorage` | تنجو من تحديث الصفحة ولا تُعمِّر بعد إغلاقها |

**ومفتاح الاستمرارية بصمةُ البناء** — درس `GAP-Q-01`: بناءٌ آخر ⇒ إجاباتٌ تُطرح ولا تُصحَّح بخريطةٍ جديدة.

**وقيد `DEC-244` معلَنٌ في شاشة الختام**: لا يُقرأ فارقٌ بين قياسين مهما تكرّر القياس.

---

# §7 مصدرُ حقيقةٍ واحد للنصّ الحاكم

`SCHEMA` و`CONSENT_TEXT` وصيغةُ الرمز **تُحقَن من `workshop_store.py` وحده** في البناء، ويستدعيها السطح بالمرجع.

**والفحص يقيس ذلك عدداً**: نصّ الإذن يظهر في الصفحة **مرّةً واحدة** (موضع الحقن)، **ولا نسخة له في كود السطح**. وحُقنت نسخةٌ ثانية فسقط الفحص بعدّها («٢ موضعاً»).

> فنصٌّ حاكمٌ له نسختان **له صيغتان**، وإحداهما ستتخلّف يوماً. وهذا `م-2` مقيساً لا موعوداً.

---

# §8 الحرّاس مُختبَرون على فشلهم — ثلاث حقنات

| الحقنة | النتيجة |
| :--- | :--- |
| توسيع القفل (رفع شرط الأصل والمسار) | ❌ **رُصد** — بأسماء الثغرات الأربع المنفتحة |
| الخادم يخزّن حمولةً ردَّها المشرف | ❌ **رُصد** — «تُردّ ولا تُخزَّن» سقط |
| نسخةٌ ثانية لنصّ الإذن في كود السطح | ❌ **رُصد** — «٢ موضعاً» |

---

# §9 ما لم يُمسّ

- ❌ **طبقة القياس** — البنود والخرائط و`SP` كما هي؛ السطح يقرأ ولا يعرّف.
- ❌ **الموقع العام وتطبيقه** — `docs/` كما هي بايتاً بايت، وقفلُها ووعدُ تذييلها. **والفحص الرابع يقيسه**: صفر أثرٍ لمسار الورشة في التطبيق العام.
- ❌ **البصمات الستّ** — خمسٌ تكافؤية وسادسةٌ انحدارية، **حرفياً**.
- ❌ **بيانات المشاركين خارج المستودع** — `workshop_data/` مُسقَطة، ويفحص التتبّع فحصٌ مستقل.
- ❌ **`Workshop.html` مولَّدة لا تُرفع** — أُضيفت إلى قائمة المولَّدات في `gate.py` (`CHG-054`).

---

# §10 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| المسار من طرفٍ إلى طرف | ✅ إذنٌ ورمز → استبيان → توليد → إرسال → حكم → تخزين |
| القفل | ✅ **مقيسٌ بتشغيله** — ٩ محاولاتٍ مرفوضة · منفذٌ واحد يمرّ |
| بوابة القبول | ✅ المعطوب **يُردّ ولا يُخزَّن** — عبر الخادم أيضاً |
| البرهان الطرفيّ | ✅ حمولةُ `JS` قبِلها مخزنُ بايثون |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** |
| البصمات | ✅ **ستٌّ حرفياً** |
| الأدوات | **24/24** |

---

**[نهاية `149-WORKSHOP-SURFACE` — `DEC-279` · الترقيم التالي `DEC-280` · `CHG-098` · المرحلة ② منتهية]**
