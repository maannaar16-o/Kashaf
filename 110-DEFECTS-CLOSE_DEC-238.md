# 110-DEFECTS-CLOSE — `DEC-238`: حسم العيوب الثلاثة

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | حسم العيوب الثلاثة التي كشفها `DEC-237` |
| **حالة الاعتماد** | **مشتق** — مسّ عقد المحتوى وقيمة مُصدَّرة **بإذن صريح** |
| **الأدوات** | **14/14** · `parity_surface` **اخضرّ** |
| **البصمات الثلاث** | ثابتة |

**ترتيب الحسم لم يكن اعتباطياً:** العقد أولاً كي **يصدُق** الحقل، ثم نقله إلى JS كي يُضاف **صادقاً**، ثم التقريب. إضافة حقل يُبلّغ كذباً أسوأ من غيابه.

---

# ① `DEF-K3-MISS-01` — العقد يطابق الواقع

## ما كان

`REQUIRED_EXTERNAL` يطلب المفتاح الموحَّد `circle_map`. وقد انقسم بـ`DEC-138`/`79 §2.1-2.2` إلى `circle_map_shared` و`circle_map_tail` — **ولم يُحدَّث العقد**.

فحمل **كل تقرير $K_3$** الوسم `missing_content: ['circle_map']`، والمحتوى **حاضر في المتن** (مُتحقَّق بالمطابقة النصّية).

## الإصلاح

المفتاحان المنقسمان يحلّان محلّ الموحَّد، كلٌّ بسنده (`79 §2.1` · `79 §2.2`). العقد صار **9 مفاتيح** يقابلها **9 موَرَّدة** ⇒ `missing_content = []`.

## لماذا لم تتغيّر البصمات

`missing()` تُستعمل في **موضعين فقط**: بوابة `strict` (`k3_engine.py:366`) وحقل التدقيق (`:390`). **لا تحجب محتوىً.** فُحص هذا **قبل** المسّ لا بعده — إذ لو كانت تحجب لأضاف الإصلاح نصّاً وغيّر البصمة.

---

# ② `GAP-AUDIT-JS-01` — الحقل يُضاف فاحصاً لا مُقرِّراً

`K3_EXTERNAL_CONTRACT` في `reports.js` نظيرٌ لـ`REQUIRED_EXTERNAL` **بالمفاتيح والترتيب نفسه** — والترتيب ملزِم لأن `missing()` تُرجِع بترتيب **العقد** لا بترتيب الفقد.

وكل مفتاح يشير إلى **مصدره الفعلي في الحزم**:

| المفتاح | المصدر |
| :--- | :--- |
| `covenant_opening` · `out_text` · `reflective_frame` | `PACKS.THREE_K3` |
| `circle_map_shared` · `circle_map_tail` | `PACKS.CIRCLE_K3` |
| `trust_banner` | `PACKS.BANNER_K3` |
| `skill_sections` | `PACKS.SECTIONS_K3` |
| `verify_block` · `separation_qs` | `PACKS.TEXTLAYER_K3` |

## مُبرهَن أنه يفحص

بإفراغ `BANNER_K3.trust_banner` أبلغ الحقل `["trust_banner"]`. **لو ثُبِّت على `[]` لكان تكراراً لخطيئة `auditIsolation([])`** التي أُغلقت بـ`DEC-236`.

⇒ كتلة التدقيق: **25 حقلاً في الطرفين** · صفر فرق بنيوي.

---

# ③ `DEF-T6-ROUND-01` — تقريب صريح لا سلوك لغة

| الطرف | كان | عند `0.125` |
| :--- | :--- | :---: |
| بايثون | `round(w, 2)` — مصرفي (نحو الزوجي) | `0.12` |
| JS | `Math.round(w*100)/100` — نصفٌ لأعلى | `0.13` |

## الصيغة المعتمدة — صريحة في الطرفين

```
_round2(v) = floor(v × 100 + 0.5) / 100
```

متطابقة **بتّاً** في اللغتين (`IEEE-754`: `w` نسبةُ صحيحين، و`w×100` يعطي البتّات نفسها، و`floor` واحدة). ولا تتّكل على سلوك أيّهما.

هذا مبدأ `DEC-235` نفسه، مطبَّقاً على **العدد** بدل النصّ. 12 حقلاً استوى.

---

# القسم 4: اختبار القبول

| | النتيجة |
| :--- | :--- |
| `parity_surface` | ✅ **اخضرّ** — 13 ثابتاً · 56 بنداً · 13 شبكة خام · **3,214 حقل تدقيق** · 1,041 قيمة حدّية |
| الأدوات | **14/14** |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **ثابتة** |
| الأداتان مقابل المرجع | 142 تقريراً · **صفر تباعد في النصّ وفي كتلة التدقيق معاً** |

| الأداة | الحجم | `SHA` |
| :--- | ---: | :--- |
| `Kashaf_v2_FIXED_DEC-230-238.html` | 1,113,032 | `dcd16a159bfbd5a8…` |
| `Supervisor.html` | 362,379 | `13a27f35b6558696…` |

---

# القسم 5: مبدآن تكرّرا حتى صارا قاعدة

## أ · صيغة صريحة بدل سلوك لغة

ثلاث مرّات في هذه الجلسة كان العلاج واحداً:

| الموضع | السلوك الضمني | الصيغة الصريحة |
| :--- | :--- | :--- |
| `DEC-235` — قائمة | `['A', …]` مقابل `A,…` | الفاصل `·` |
| `DEC-235` — عدد | `4.0` مقابل `4` | `_num` |
| `DEC-238` — تقريب | مصرفي مقابل نصفٍ لأعلى | `_round2` |

**[مقترح استراتيجي — غير معتمد]** ترقيته إلى بند في `00-PROTOCOL`:
> *كل قيمة تعبر إلى مخرج تُصاغ بصيغة صريحة، ولا يُتّكل على تصيير اللغة.*

القيمة: يمنع الصنف كلّه بدل ملاحقة أفراده. الحدّ: لا يمسّ منطقاً ولا وزناً — قاعدة صياغة لا قياس. **لم يرد به أمر.**

## ب · الحقل الذي لا يفحص يكذب

`auditIsolation([])` — و`missing_content` لو ثُبِّت على `[]`. كلاهما حقلٌ يُبلّغ سلامةً **لأنه لا يفحص**. رُفض النمط مرّتين في جلستين متتاليتين.

**الحقل يُضاف فاحصاً أو لا يُضاف.**

---

**[نهاية `110-DEFECTS-CLOSE` — `DEC-238` · العيوب الثلاثة مُغلقة]**
