# 160-TEAM-SURFACE — `DEC-289`: **سطح الفريق** — والحرسُ يُقلب كما وُعد

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**قم ببناء سطح الفريق**» — 2026-08-15 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-15 |
| **الأثر** | طبقة الفريق **لها توأمٌ مقيسٌ بتكافؤ** · وسطحٌ محلّي `Team.html` · **والحرس مقلوبٌ لا محذوف** · البوابة **25 ← 27** · **وبصمةٌ سابعة تكافؤية** |
| **التغيير المرافق** | `CHG-109` |
| **الحاكم** | `DEC-278 §5` (**قاعدة القلب** — منصوصةٌ قبل البناء بيوم) · `DEC-199`/`DEC-200` · `DEC-277 §2` (رفع `ح-4` محصورٌ بسطح المالك) · `DEC-269` (التنفيذ لا القراءة) |
| **الكود** | `160` — تمديد بمسوّغه: سطحٌ وتوأمٌ لطبقةٍ قائمة |

---

# §1 القلب — **وعدٌ كُتب قبل أن يُحتاج إليه**

`DEC-278 §5` قال يوم أُجّلت التوأمة:

> «**قاعدة القلب**: حين يُبنى سطح المرحلة ③، **يُقلب الحرس لا يُحذف** — من «لا سطح بلا توأم» إلى «التوأم مقيسٌ بتكافؤ».»

**فنُفِّذت حرفياً.** والفحص السادس في `test_report_team` لم يُحذف؛ **تبدّل مضمونه**:

| قبل | بعد |
| :--- | :--- |
| صفر `RawahilTeam` في حزم المتصفح | **التوأم قائم** · **كامل بستّ دوالّ حاكمة** · **وأداة تكافؤه مدرَجةٌ في البوابة ببصمتها** |
| — | **وطبقة الفريق خارج كل سطحٍ عام**: القلب رفع الحظر عن **سطح المالك وحده** — لا عن التطبيق المنشور ولا صفحة الورشة |

> **والحرس الذي يُحذف عند أول حاجةٍ إليه لم يكن حرساً.** وهذا ما يجعل الفارق بين التأجيل والتهرّب مكتوباً لا متروكاً للنيّة.

---

# §2 التوأم — **بصمة تكافؤ لا انحدار، وحقَّ للاسم**

`DEC-278 §5` سمّى بصمة الطبقة **انحدارية** وقال إن تسميتها تكافؤاً **ادّعاءٌ كاذب** — لأنه لم يكن ثمّة طرفان.

**والآن صار ثمّة طرفان**، فأُضيفت `parity_team.py` ببصمةٍ **تكافؤية**، **وبقيت البصمة الانحدارية** ترسو الجانب البايثوني. ⇒ **سبعُ بصمات: ستٌّ تكافؤية وواحدةٌ انحدارية.**

**والمقيس ثلاثة**: المتن **حرفاً بحرف** · كتلة التدقيق كاملةً · **ورسالةُ كل خرقٍ لعقد المدخل بنصّها** — فعقدٌ يوقف برسالتين مختلفتين **عقدان**.

---

# §3 🔎 وأول تشغيلٍ للتكافؤ كشف تباعداً — في حدٍّ لغويّ دقيق

| الجانب | السلوك على `"NaN"` |
| :--- | :--- |
| بايثون | `float("NaN")` **تنجح** ⇒ تسقط في فحص الانتهاء ⇒ «قيمة **غير منتهية**» |
| `JS` (قبل) | `Number("NaN")` ⇒ `NaN` ⇒ «قيمة **غير عددية**» |

**فالتوأم كان يصنّف الخطأ صنفاً آخر** — ورسالتان مختلفتان لخرقٍ واحد. وعولج بدالّة `pyFloat` تحاكي دلالة `float()` بالضبط: تقبل `"NaN"`/`"inf"`/`"Infinity"` بإشارتها وبأي حالة أحرف، وتردّ ما ترفع عنده `float` استثناءً.

> **ولم يكن ليُكتشف بلا توأم**: لا فحصٌ في بايثون يرى ما تفعله `JS`. **فالتوأمة ليست تكلفةً على الصحّة — هي أداةُ كشفها.**

---

# §4 🔎 وحقنةٌ «نجحت» فكشفت ثغرةً في الحالات لا في الكود

حُقن **قلبُ ترتيب كسر التعادل** (`DEF-K2-04`) — **فمرّ التكافؤ**. والسبب أن **لا حالةَ في `team_cases.json` فيها عضوٌ بمهيمنَين متعادلَين**: القاعدة كانت **منفَّذةً وغير مقيسة**.

⇒ فأُضيفت حالة **«تعادل-مهيمنَين»** (`A` و`S` عند القيمة نفسها، والفصل بترتيب `41 §5.2`). ثم أُعيدت الحقنة **فسقطت**.

**وبصمتان تُزاحان بإعلان:**

| البصمة | قبل | بعد |
| :--- | :--- | :--- |
| `test_report_team` (انحدار) | `21532a8aba568a2d` | **`29ed15b4accf7a5a`** |
| `parity_team` (تكافؤ) | `0f40bdf8b1d755a6` | **`618bc83fd757bd60`** |

**وسببُهما الحالةُ الجديدة وحدها** — والخمس الأخرى **حرفياً**.

> **والدرس يُقيَّد: حقنةٌ تمرّ تتّهم الحالات قبل أن تبرّئ الكود.** وهو أخو الدرس المقيَّد في `DEC-285 §5` (حقنةٌ نجحت كذباً)، من الجهة الأخرى: تلك أخطأت الطبقة، وهذه كشفت فراغاً في التغطية.

---

# §5 السطح — أداةُ مالكٍ محلّية **لا تُنشر**

`Team.html` (مولَّدة بـ`build_team_html.py`، **مُسقَطة** كـ`Supervisor.html`):

| القيد | التنفيذ |
| :--- | :--- |
| **قفل صفر الشبكة كامل** | `CPL-08A-03` — لا تحتاج شبكةً البتّة |
| **لا تُنشر** | فحصٌ مستقل: ليست في `docs/` · ولا يُشار إليها من صفحةٍ منشورة · ولا هي متبَعة |
| **لماذا محلّية** | **مدخلُها خامٌ** — فهي في نطاق رفع `ح-4` المحصور بسطح المالك (`DEC-277 §2`). **ومخرجُها بلا خامٍ** بقفل الميثاق، لكنّ المدخل وحده يكفي |
| **خرق العقد** | **يُعرض حكماً لا انهياراً** (درس `DEC-280 §5`) |

**والحزمة المولَّدة تُنفَّذ لا تُقرأ** (`test_team_build`): تُستخرج كتلُها **كلُّها** وتُشغَّل في `node` بنافذةٍ وهمية، ويُقابَل مخرجُها بمخرج بايثون **حرفاً بحرف** — `725d7b9bac0d404a`.

**وشُغِّلت في `Chromium`**: تسعةُ أقسام · تسعةُ جداول · التنبيه الإلزامي في الرأس · **صفر رقمٍ خام في المتن** · **صفر رمز `K3`/`K4`** · `fetch` أجنبيّ **مرفوض** · صفر خطأ طرفية.

---

# §6 الحرّاس — ثلاث حقنات

| الحقنة | النتيجة |
| :--- | :--- |
| التوأم ينتقي بديلاً للتقاطع الخالي | ❌ **رُصد** — جانب `JS` سقط عند خليةٍ لا وجود لها |
| قلبُ كسر التعادل | ❌ **رُصد بعد سدّ ثغرة الحالات** — وقبلها مرّ، وهو الكشف نفسه |
| نزعُ أداة التكافؤ من البوابة | ❌ **رُصد** — الحرسُ المقلوب يوجب القياس لا يكتفي بوجود التوأم |

---

# §7 ما لم يُمسّ

- ❌ **طبقة القياس** · **`k2_engine`** — العتبات تُقرأ منه في الجانبين.
- ❌ **الموقع العام وصفحة الورشة** — ولا سطر؛ **ومقيسٌ** أن طبقة الفريق خارجهما.
- ❌ **البصمات الخمس الأخرى** — حرفياً.
- ❌ **الميثاق `56-TEAM-00`** — السطح يعرض ما بناه المحرّك، ولا يضيف قاعدة.

---

# §8 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| القلب | ✅ **نُفِّذ كما وُعد** — الحرس تبدّل مضمونه ولم يُحذف |
| التوأم | ✅ **مقيسٌ بتكافؤ**: 8 تركيبات · 8 متون · 8 كتل تدقيق · 6 رسائل خرق · صفر تباعد |
| التسمية | ✅ **تكافؤية الآن بحقّ** — والانحدارية باقيةٌ ترسو بايثون |
| السطح | ✅ محلّي · بقفلٍ كامل · **وحزمتُه تُنفَّذ لا تُقرأ** |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** — وواحدةٌ كشفت ثغرة تغطية |
| الأدوات | **27/27** · **سبع بصمات** |

---

**[نهاية `160-TEAM-SURFACE` — `DEC-289` · الترقيم التالي `DEC-290` · `CHG-109`]**
