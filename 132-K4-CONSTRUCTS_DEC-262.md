# 132-K4-CONSTRUCTS — `DEC-262`: بُنى $K_4$ الأصيلة — القراءتان المساريتان مصاغتين + سجل الأنماط

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفذ المرحلة ٤**» — 2026-08-12 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-12 |
| **الأثر على المحرّكات والحزم والأداة** | **صفر** — وثيقة تفسيرية · البصمات الثلاث ثابتة · لا قياس، لا وزن، لا عتبة جديدة (ن-7/④) |
| **التغيير المرافق** | `CHG-079` |
| **الحاكم** | `129-K4-DUAL-CHARTER` (`DEC-258`): القراءتان المشتقتان `§2/①` · قاعدة الترجيح `§2/③` — `130-K4-RELATIONS` (`DEC-259`/`DEC-260`) — `131-K4-DEPTH` (`DEC-261`): بطاقات العمق وسلّم النطاقات — `DEC-136` (كل نقل بقيده: `TRF-012…014`) |
| **الكود المحجوز** | هذه الوثيقة تستوفي حجز `132` — المرحلة ٤ (`77-LOG §2`) |

---

# §0 المنهج

- **«الأصيلة» تعريفاً:** البُنى التي تقوم من شكل $K_4$ المختوم نفسه (مسار + شبكة) ومن مواده المُعاد تأسيسها — لا من نقل بنيةٍ من دائرة أخرى (`TRF-013` قيد الرفض).
- **كل اشتقاق موسوم** `[اشتقاق غير محقّق ميدانياً]` (`TRF-002`) — سارٍ على الوثيقة كلها.
- **سقف «ما لا يُدَّعى» العام** (`129 §4`) سارٍ: لا سببية مثبتة · لا مفردة عيادية (التطهير اللغوي الهندسي — `14-CORE-K4 §5`) · لا قراءة عبر جدار العزل · لا استدلال درجة↔علاقة.
- **شرط الاستدعاء فارغ** في كل بنية وقيد أدناه — يُملأ في مرحلة القراءة بقرارها. هذه المرحلة **تصوغ البُنى ولا تشغّلها**: لا تقرير يُبنى ولا قارئ يُخاطَب.

---

# §1 القراءة الأولى مصاغةً — «مواضع الانقطاع»

## ① التعريف (مختوم سلفاً — `129 §2/①`)

> أين يسقط التنفيذ على التتابع `WM ← TI ← F ← PF ← OR ← TM ← PER`؟

## ② المادة المزدوجة للقراءة

| المصدر | المضمون |
| :--- | :--- |
| **مظاهر الانسداد السبعة** | الموثقة في بطاقات العمق (`50-DEPTH-K4-* §1.3`) بنصوص الجدران المعتمدة حيث وُجدت («لم يبدأ أصلاً» · «بدأ ونسي الخطوات» · «انشغال بمهام ثانوية» · «خطة غير واقعية زمنياً» · «التطبيق اللوجستي فوضوي» · توقف بملل رغم الهدوء · انجراف وعجز استعادة) |
| **نطاقات الصمامات** | على سلّم `DEC-261` — **وصفاً** لا حكماً |

## ③ خطوات القراءة المصاغة — **[مشتق]**

1. **جدران العزل أولاً** — قبل أي إسناد: تمييز $K_1$ قبل `TI` (النية تولَّد هناك) · تمييز $K_3$ قبل `PER`/`F` (شرط «رغم الهدوء النفسي») · تمييز $K_2$ قبل `OR`/`TM`/`PF` (الخطة الذهنية غير الميدان) · الجداران الداخليان (`TI`⚔️`WM` · `TM`⚔️`PF`). **وعند الغموض يُعرض ويُسأل (`م-4`) — لا إسناد قسري.**
2. تُقرأ المحطات **بترتيب المسار** محطةً محطة، بسؤال التشغيل الداخلي لكل بطاقة.
3. المحطة التي يظهر عندها مظهرُ انسدادٍ موثق تُسجَّل **«موضع انقطاع»** — وصفاً لا حكماً.
4. **قد تتعدد المواضع** — تُسجَّل كلها بترتيب ورودها على المسار، **ولا تُرتَّب أهميةً** (الترتيب سند قراءة لا قياس — `129 §2/①`).
5. ما بعد موضع الانقطاع **لا يُحكم عليه من عنده**: محطة لاحقة لم تصلها مادة عمل لا يُقرأ صمتُها ضعفاً — الصمت مشروع.

## ④ حدود القراءة

❌ لا عتبة تُخترع · ❌ لا سببية (الموضع وصفُ سقوطٍ لا تفسيرُ سببه) · ❌ لا قراءة من موضع الانقطاع على درجة الوعاء ولا العكس بغير المادة المزدوجة (`②`) · المقارنة داخل الفرد الواحد حصراً (`DEC-205` قياساً).

---

# §2 القراءة الثانية مصاغةً — «عنق الزجاجة»

## ① التعريف (مختوم سلفاً — `129 §2/①`)

> أضعف محطة تخنق ما بعدها.

## ② قاعدة «الأضعف» — بلا عتبة مخترعة **[مشتق على مختوم]**

**الأضعف = المحطة ذات النطاق الأدنى على سلّم `DEC-261` داخل ملف الفرد الواحد** — مقارنة رتبية بين نطاقات المحطات السبع (الحدود مختومة سلفاً؛ لا رقم جديد). لا «أضعف مطلق» عبر الأفراد ولا عبر الدوائر (`DEC-205` قياساً).

## ③ قاعدة التعادل — العرض المزدوج قياساً على المختوم

عند تساوي أدنى نطاق بين محطتين فأكثر: **لا كسر تعادل يُخترع** — `DEC-150` أقفل اختراع كسر التعادل نصاً، وسابقة `R11` (العرض المزدوج الموسوم — `DEC-039`) هي النموذج: **تُعرض محطات النطاق الأدنى معاً أعناقاً موصوفة جميعاً، ويُسأل عند الحاجة إلى ترجيح (`م-4`)**.

## ④ قراءة «تخنق ما بعدها» — حدّها الفاصل

- **حيث قام قيد شبكة مثبت** يربط المحطة الأدنى بمحطةٍ بعدها (مثال: أدنى النطاقات `TM` وبعده `PER` — قيدا `K4-REL-08`/`K4-REL-12` قائمان): تُقرأ الخنقة **بقيدها** — والشبكة تتقدم بنص قاعدة الترجيح (`129 §2/③`).
- **حيث لا قيد**: تبقى القراءة **وصفَ موضعٍ على التتابع** («المحطة الأدنى تسبق كذا») — **ولا تُؤلَّف خنقة**: لا استدلال من الدرجة على علاقة (سقف `129 §4`).

## ⑤ حدود القراءة

❌ لا سببية من عنق الزجاجة · ❌ لا يُقرأ منه قيام قيد شبكة ولا العكس · ❌ كونُ محطةٍ عنقاً لا يجعلها «أهم» المهارات (الصمامات لازمة معاً — `TRF-011`) · وتظل القراءتان معاً تحت قاعدة الترجيح المختومة **كما هي** — هذه الوثيقة تصوغ ولا تعدّل (`م-2`: إحالة لا إعادة تشريع).

---

# §3 العلاقة بين القراءتين — تحديد لا تشريع

| الحالة | الحكم (إحالة إلى المختوم) |
| :--- | :--- |
| سؤال اتجاه/تسلسل | قراءة المسار (`§1`/`§2`) — `129 §2/③` |
| سؤال تفاعل (لماذا لا تثمران معاً؟) | الشبكة بقيودها — `129 §2/③` |
| تعارض القراءتين وقيدٌ مثبت قائم | الشبكة تتقدم — `129 §2/③` |
| تعارضٌ بلا قيد مثبت | تُعرض القراءتان ويُسأل (`م-4`) — `129 §2/③` |

---

# §4 سجل أنماط $K_4$ — يُفتتح

## ① قالب قيد النمط الملزم — **[على قالب قيد العلاقة (`129 §4`) بحقلين مستجدّين]**

| حقل القيد | الضابط |
| :--- | :--- |
| الكود | `K4-PAT-NN` — تسلسلي، لا أرقام تُعاد |
| **البنية النطاقية الإسمية** | الصمامات المعنية وحالاتها **بأسماء نطاقات سلّم `DEC-261` حصراً** (حضور محدود · كفاءة أساسية · قدرة عالية · `OUT`) — لا رقم ولا عتبة جديدة |
| الدليل البنيوي | بذرة بسند مُعاد تأسيسه (`DEC-257`) و/أو قيود شبكة قائمة — نصاً بموضعه (`TRF-012`) |
| **فائض النمط فوق مكوناته** | حقل إلزامي (`TRF-014`): ماذا يحمل النمط زائداً على قراءاته المنفردة؟ **ما لا فائض له لا يُسجَّل نمطاً** |
| ما لا يُدَّعى | إلزامي — تحت السقف العام (`§0`) |
| شرط الاستدعاء | فارغ حتى مرحلة القراءة |
| الوسم | `[اشتقاق غير محقّق ميدانياً]` دائم (`TRF-002`) |

## ② الأنماط المسندة (أربعة)

### `K4-PAT-01` — **الاحتكاك الثلاثي** `PF + TM + PER`

| الحقل | البيان |
| :--- | :--- |
| البنية النطاقية | المحطات الثلاث `PF`/`TM`/`PER` في **الحضور المحدود** معاً |
| الدليل البنيوي | **مزدوج**: `SEED-K4-08` نصاً («احتكاك ثلاثي واضح» — وتدريبها **مركّب من ثلاثة**: «بطاقة الأهم + نافذة + قفل إكمال») + **مثلث كامل الأضلاع في السجل**: `PF–TM` (`K4-REL-11`) · `TM–PER` (`K4-REL-08`/`K4-REL-12`) · `PF–PER` (`K4-REL-09`) — أربعة قيود على أضلاعه الثلاثة |
| فائض النمط | **التزامن على حامل واحد**: الأضلاع الثلاثة المثبتة قائمة معاً في الملف نفسه — والبذرة تقرأ العلاج مركّباً لا ثلاثة تدريبات متفرقة؛ وهو ما ختمه `130 §4/ب`: «لا يُفكَّك قسراً إلى أزواج» |
| ما لا يُدَّعى | ❌ لا أثر ثلاثي مستقل **يُقاس** فوق الأضلاع — الفائض قراءة تزامن وصفية · ❌ لا يُعمَّم على ثلاثيات أخرى — كلٌّ بدليله |
| شرط الاستدعاء | — |

### `K4-PAT-02` — **الارتفاع العام** (الملف كامل القدرة)

| الحقل | البيان |
| :--- | :--- |
| البنية النطاقية | المحطات السبع في **القدرة العالية** |
| الدليل البنيوي | `SEED-K4-05` نصاً: «كل الصمامات مرتفعة» — والتدريب المقصود **واحد للملف**: «تثبيت طريقة العمل» |
| فائض النمط | قراءة **ملف** لا سبع قراءات: طريقة عمل قائمة تُثبَّت — لا مهارة تُبنى |
| ما لا يُدَّعى | ❌ لا «كمال تنفيذي» ولا حصانة من الانقطاع · ❌ لا يُقرأ تعزيزاً شاملاً بين الأزواج — قيد التعزيز المثبت واحد (`K4-REL-01`) والباقي فجوات موثقة |
| شرط الاستدعاء | — |

### `K4-PAT-03` — **الإسناد الشامل** (الملف كامل الحضور المحدود)

| الحقل | البيان |
| :--- | :--- |
| البنية النطاقية | المحطات السبع في **الحضور المحدود** |
| الدليل البنيوي | `SEED-K4-06` نصاً: «كل الصمامات منخفضة» — والتدريب المقصود واحد متدرج: «بروتوكول تدريجي قصير» |
| فائض النمط | قراءة ملفٍ تقتضي **مدخلاً واحداً متدرجاً** بدل سبع معالجات متوازية — وهو مقصد البذرة نصاً |
| ما لا يُدَّعى | ❌ لا مفردة عيادية بأي صيغة (`14-CORE-K4 §5` — التطهير اللغوي الهندسي) · ❌ لا يُقرأ تفاقماً شاملاً — قيد التفاقم المثبت زوجي واحد (`K4-REL-12`) · ❌ لا يُنسب لضعف $K_1$/$K_3$ ولا يُنفى — جدران العزل قائمة |
| شرط الاستدعاء | — |

### `K4-PAT-04` — **الصمام الحرج المفرد** (انفراد الأدنى)

| الحقل | البيان |
| :--- | :--- |
| البنية النطاقية | محطة واحدة في **الحضور المحدود** وسائر المحطات في **الكفاءة الأساسية فأعلى** |
| الدليل البنيوي | `SEED-K4-07` نصاً: «صمام واحد حرج» (مثال المصمم: `K4-PF` — وتدريبه «بطاقة الأهم الآن») |
| فائض النمط | انفرادُ الأدنى يجعل قراءة عنق الزجاجة (`§2`) **وحيدة الاتجاه بلا تعادل** — الملف كله يقرأ من محطة واحدة |
| ما لا يُدَّعى | ❌ لا يُحسب أثره مرتين — **نص مانع للازدواج (`م-1`)**: بنية «انفراد الأدنى» تُقرأ عند التشغيل **بقراءة عنق الزجاجة مرة واحدة**، وقيد النمط يحفظ مقصد البذرة (التدريب المفرد) مادةً لطبقتها متى بُنيت · ❌ «حرج» وصف موضع لا لفظ عيادي |
| شرط الاستدعاء | — |

## ③ قاعدة تشغيل — لا نمط: **المدخلات الناقصة**

`SEED-K4-10` («مدخلات ناقصة / تقرير فجوة») تلتقي بكتلة `OUT` المختومة في السلّم («فجوة تشغيلية — بيانات متعارضة، لا يُصنَّف»): **الوعاء الناقص/المتعارض يُنتج تقرير فجوة تشغيلية، ولا يُقرأ فوقه نمط ولا قراءة مسار** — قاعدة تشغيل تُقيَّد هنا ولا تُسجَّل نمطاً (النمط قراءة مضمون، وهذه حارسة مدخل).

## ④ فراغ موثق — مثلثٌ كامل الأضلاع بلا نمط

**`TM · OR · PER`** مثلثٌ ثانٍ كامل الأضلاع في السجل (`K4-REL-07` · `K4-REL-03` · `K4-REL-08`/`12`) — **ولم تقم له بذرة ولا نص يسنده نمطاً**. بقاعدة الصمت المشروع (`TRF-012`): **يُسجَّل فراغاً موثقاً لا نمطاً** — اكتمال الأضلاع وحده ليس دليل نمط (الفائض شرط التسجيل)؛ إن قدّم المالك مادته قام بها في حينه.

---

# §5 ما لا تدّعيه هذه المرحلة

- ❌ لا تقيس ولا تضيف بنداً أو وزناً أو عتبة أو مستوى — طبقة القياس لم تُمسّ (`instrument_pin`).
- ❌ لا تشغّل قراءة ولا تبني تقريراً — البُنى مصاغة وشرط استدعائها فارغ حتى مرحلة القراءة بقرارها.
- ❌ لا تعدّل قاعدة الترجيح ولا تعريفَي القراءتين المختومين — صياغةٌ تحتهما (`م-2`).
- ❌ لا تُثبت علاقة جديدة — سجل العلاقات مرجع الشبكة الوحيد؛ والأنماط لا تتحول قواعد معرفية ولا معايير (القاعدة المركزية لبذور `19-SEED-K4` نفسها).
- ❌ لا صدق ميداني (`DEC-246`) — كل شيء `[اشتقاق غير محقّق ميدانياً]`.

---

# §6 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| طبيعة التغيير | وثائقي صرف — لا كود، لا حزم، لا أداة |
| قيود النقل | `TRF-012…014` مقيَّدة **قبل** الاستعمال (منها قيد رفض `TRF-013`) — `77-LOG` v1.4 |
| بوابة النقد الذاتي (`م-7`) | البذور تُستشهد بتصنيفها النافذ («مقاصد تصميم» — `DEC-257`) لا بسندها الشبحي · العناوين تطابق المضامين (`Check-10`) · لا نمط بلا فائض مكتوب |
| الأدوات الستّ عشرة | 16/16 |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **حرفياً** |

---

**[نهاية `132-K4-CONSTRUCTS` — `DEC-262` · الترقيم التالي `DEC-263` · `CHG-079` · أنماط `K4-PAT-01…04` · التالي `K4-PAT-05`]**
