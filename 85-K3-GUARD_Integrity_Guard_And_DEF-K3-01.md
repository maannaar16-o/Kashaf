# 85-K3-GUARD — حارس سلامة مخرج $K_3$ + `DEF-K3-01`

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | قياس وتدقيق · صياغة تشغيلية |
| **حالة الاعتماد** | **معتمد** (الحارس · النتائج · الدليل — مقيسة) · **مقترح** (الإصلاح · §6) |
| **نطاق الاستدلال** | `k3_report.py` · `79-K3-USERMAP §2.1/§2.2` · `55-USER-K3-* §1` · `80-K3-TEXTS` · `DEC-064/133` · `golden_k3` (٦ حالات) |
| **السند الحاكم** | **`ن-7`** (`DEC-193`): ما يُفحص آلياً يُحرَس آلياً — يوقف الإصدار ولا يعود بقيمة فارغة صامتة |
| **الإصدار** | v1.0 |

---

# القسم 1: الحارس — خمسة فحوص

| الكود | الفحص | يمنع |
| :--- | :--- | :--- |
| **`ص-1`** | اكتمال `skill_sections`: ٥ مهارات × `U01`·`U08`·`U09`·`U10`، غير فارغة | قسماً مهارياً يُصدَر بمتن ناقص |
| **`ص-2`** | اكتمال `three_texts` و`circle_map_by_skill` | نصّاً مفقوداً يمرّ صامتاً |
| **`ص-3`** | **نقاء القسم العام** — قسم عام لا يحمل خاتمة ملف مهاري | 🎯 **الفحص 10** |
| **`ص-4`** | تغطية **صريحة** لكل نطاق يخرجه `band()` — بلا وسم افتراضي | وسماً يُسحب من افتراضٍ لا من اعتماد |
| **`ص-5`** | كل وسم في عنوان مهاري من المعتمد الأربعة | لغةً خارج `DEC-064` |

**الربط الصريح** — يُثبَّت في الحارس بدل بقائه ضمنياً في الكود:

| المفتاح | المصدر (`55-USER-K3-* §1`) | يُعرض تحت |
| :--- | :--- | :--- |
| `U01` | ما هذه المهارة (الوظيفة المركزية) | افتتاح كل قسم مهاري |
| `U08` | إشارات القوة | «ما يظهر عندك» — `core`/`high` |
| `U09` | إشارات تحتاج انتباهاً | «ما يحتاج انتباهاً» — `limited` |
| `U10` | حالة الضغط القصوى (عابرة) | فرع `OUT` |

`enforce()` يرفع `K3IntegrityError` عند أي شذوذ — **إيقاف لا تحذير**.

---

# القسم 2: نتيجة أول تشغيل

| الفحص | النتيجة |
| :--- | :--- |
| `ص-1` اكتمال الحزم | ✅ سليمة — ٥ مهارات × ٤ مفاتيح |
| `ص-2` النصوص الثلاثة وخرائط الدائرة | ✅ سليمة |
| `ص-5` وسوم النطاق في العناوين | ✅ سليمة |
| **`ص-3`** نقاء القسم العام | 🟠 **شذوذ — `DEF-K3-01`** · **٦ من ٦** حالات |
| **`ص-4`** تغطية النطاقات | 🟡 **شذوذ آلية** — `OUT` بلا وسم صريح |

---

# القسم 3: 🟠 `DEF-K3-01` — تسرّب خاتمة مهارية إلى قسم عام

## 3.1 السند

`79-K3-USERMAP §2` يفصل صراحةً بين نصّين:

| القسم | العنوان الحرفي | الوظيفة |
| :--- | :--- | :--- |
| `§2.1` | **المقطع المشترك — يُصدَّر به الخمسة** | نصّ عام واحد |
| `§2.2` | **الخاتمة الخاصة بكل ملف** | خمس خاتمات، لكلٍّ ملفها |

## 3.2 ما يحدث فعلاً

حزمة `circle_map_by_skill` **دمجت النصّين** في سلسلة واحدة لكل مهارة. و`k3_report` يعرض في القسم **② «موقعك من النظام»** — وهو قسم عام يُعرض مرة واحدة — النسخة المثبَّتة `CIRCLE_MAPS["EP"]`.

**النتيجة في كل تقرير صادر:** يقرأ المستخدم في قسم عام، **قبل تقديم أي مهارة**:

> «وهذه المهارة — **الملاحظة** — هي **منبع الصورة كلها**…»

**«هذه المهارة»** بلا مرجع سابق. والأقسام المهارية تأتي بعدُ في ③.

## 3.3 الأثر

| البند | البيان |
| :--- | :--- |
| **مرجع معلَّق** | إشارة إلى مهارة لم تُقدَّم — عنوان «موقعك من النظام» يحمل **خاتمة ملف `EP`** |
| **تفضيل غير مبرَّر** | `EP` تُصدَّر في القسم العام لكل مستخدم، **بلا سند من ملفه** |
| **التغطية** | **٦ من ٦** حالات `golden_k3` — أي كل مخرج |
| **ما لا يحدث** | ✅ لا إسناد كاذب لملف المستخدم (بخلاف `DEF-K2-01`) — لذلك 🟠 عالية لا 🔴 حرجة |

---

# القسم 4: 🟡 `ص-4` — وسم `OUT` يُسحب من افتراض

```python
BAND_LABEL = {"limited": …, "core": …, "high": …}      # OUT غائب
… BAND_LABEL.get(b, 'قراءة خاصة')                       # ← افتراضي صامت
```

**النصّ صحيح ومعتمد** (`80-K3-TEXTS`: «{القدرة} — قراءة خاصة»). **الآلية معيبة**: أي قيمة غير متوقَّعة من `band()` تُنتج الوسم نفسه بلا إنذار. هذا ما تحظره `ن-7` نصّاً.

**الإصلاح:** إدراج `"OUT": "قراءة خاصة"` صراحةً وحذف الافتراضي — فيصير أي مخرج غير معروف **إيقافاً** لا تمويهاً.

---

# القسم 5: ما ثبتت سلامته

| البند | الحالة |
| :--- | :---: |
| $K_3$ سليم من `DEF-K2-01` | ✅ يستدعي بمفاتيح مسمّاة (`U01`/`U08`/`U09`/`U10`) لا بمواضع |
| ربط المفتاح بالعنوان (`U08`←القوة · `U09`←الانتباه) | ✅ مطابق لـ`55-USER-K3-* §1` |
| اكتمال الحزم الثلاث | ✅ صفر نقص |
| ترقيم الأقسام (٨ ظاهرة من ٩) | ✅ سليم — ③ لافتة الثقة بلا رقم بنصّ `ر-4` |

---

# القسم 6: الإصلاح المقترح 🟡 `[مقترح — يحتاج اعتماداً]`

| الخيار | الوصف | الأثر |
| :--- | :--- | :--- |
| **أ** | القسم ② يعرض **المقطع المشترك فقط** (`§2.1`)؛ الخاتمات تُسقَط | أبسط — يفقد خمس خاتمات معتمدة |
| **ب** | الخاتمة تُنقل إلى **رأس القسم المهاري المقابل** في ③ | يحفظ النصوص · يستلزم فصل الحزمة حقلين |
| **ج** | **أ + ب معاً** — المشترك في ②، وكل خاتمة في قسمها | ✅ **الأقرب لنية `79 §2`** حرفياً |

**متطلب تنفيذي مشترك:** فصل `circle_map_by_skill.json` إلى حقلين — `shared` (نصّ واحد) و`tail[skill]` (خمسة) — فينتفي الدمج الذي أنتج العيب من أصله.

---

# القسم 7: ملاحظة منهجية — الحارس أخطأ أولاً

النسخة الأولى من `ص-3` **أعطت «سليم» في الحالات الست**: كانت تبحث عن أول عنوان `###` لتحديد بداية الأقسام المهارية، فوقعت على `### قبل أن تقرأ` داخل القسم ①، وتوقف مسحها قبل موضع العيب.

صُحِّح بتمييز العنوان المهاري بوسم نطاق معتمد في متنه — فأخرج العيب في **٦ من ٦**.

> **الدرس:** الحارس نفسه مخرج آلي، ويخضع لما يحرسه. **حارس لم يُختبَر على عيب معلوم = طمأنينة زائفة** — وهذا يُضاف إلى شروط `ن-7`.

---

# القسم 8: المطلوب من مالك المشروع

| # | البند | الكود |
| :--- | :--- | :--- |
| 1 | اعتماد حارس $K_3$ (`ص-1…ص-5`) + شرط «الحارس يُختبَر على عيب معلوم» ضمن `ن-7` | `DEC-194` |
| 2 | اختيار مسار إصلاح `DEF-K3-01` — **أ / ب / ج** + فصل الحزمة حقلين | `DEC-195` |
| 3 | إدراج `"OUT"` صراحةً وحذف الوسم الافتراضي | `DEC-196` |

**بانتظار الختم أيضاً:** `DEC-190` · `DEC-191` · `DEC-192` · `DEC-193`.
**الترقيم التالي:** `DEC-197`.

---

> **[نهاية الوثيقة — `85-K3-GUARD` v1.0 · الحارس مبنيّ ومُختبَر · العيبان مُشخَّصان · الإصلاح بانتظار الاعتماد]**
