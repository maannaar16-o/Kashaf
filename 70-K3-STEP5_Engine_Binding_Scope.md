# 70-K3-STEP5_Engine_Binding_Scope.md
## الخطوة ⑤ — نطاق ربط المحرك: ما يتغيّر في `57-K3-ENGINE-SPEC` وما يصمد

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `70-K3-STEP5-SCOPE` |
| **الإصدار** | **v1.0 — ✅ معتمد** (`DEC-125`) |
| **الطبيعة** | **تقرير نطاق وفجوات — لا يعدّل المواصفة.** يحدد ما يجب تغييره قبل كتابة v2.0 |
| **السند** | `57-K3-ENGINE-SPEC` v1.1 (`DEC-093`) · `57-K3-ENGINE_Blueprint` · `62`/`62-B`/`62-C` · `64` v1.1 · `65` · `67`/`68` · `URS` v3.0 (`DEC-121`) · `M3-00` v2.0 |
| **قاعدة أسبقية مستبقاة** | عند تعارض المواصفة وخط الأساس → **خط الأساس يحكم** · وعند التعارض مع `M3-06` → **`M3-06` يحكم** |
| **🔒 حدّ حاكم** | **القياس لا يُمسّ.** `instrument_pin` (`40 v5.0` + `41 v4.2`) ثابت · الأوعية والمعادلات والترميز الثماني بلا تغيير |

---

# القسم 1: 🔴 ما يجب تغييره — بالبند

## 1.1 الحدّ الوجودي (§0) — **أخطر بند في المواصفة**

**النص القائم:**
> «الطبقة البنيوية حصراً (`DEC-089`): عتبات مطلقة · صفر مقارنات · **صفر إسناد جذر** · **صفر `URS`**.»

**حالته:** 🔴 **ثلاثة أرباعه سقط.**

| القيد | الحالة الآن |
| :--- | :--- |
| عتبات مطلقة | ✅ **باقٍ** — وهو أساس شرعية طبقة الاقتران |
| صفر مقارنات | ✅ **باقٍ** — قيد دائم (`DEC-114`) |
| **صفر إسناد جذر** | 🔄 **يُستبدل:** الإسناد **يُعرض ويُسأل** عبر `67`/`68` (`DEC-117`) — ويبقى **الإسناد الحاسم** محظوراً |
| **صفر `URS`** | 🔄 **يسقط:** `URS` v3.0 معتمدة وغير محجوبة (`DEC-121`) |

## 1.2 الطبقات المجمَّدة (§9.2 `audit`)

| القائم | المطلوب |
| :--- | :--- |
| `"frozen_layers": ["G3","G4","URS"]` | `G4` **رُفع** (`DEC-115`) · `URS` **مبنيّ** (`DEC-121`) · `G3` **مرفوع مشروطاً** ببروتوكول `67` — يُعاد الحقل إلى `[]` مع حقل جديد `conditional_layers: ["G3:via-67"]` |

## 1.3 الطبقات الغائبة عن المعمار

المعمار السباعي الحالي (`L1…L7` · `G1`/`G2`/`G5`) بُني **قبل** وجود طبقتَي الاقتران والتركيب. المطلوب إدراجهما:

| الطبقة الجديدة | موضعها | مصدرها |
| :--- | :--- | :--- |
| **الاقتران** — أي الخلايا تُفعَّل | بعد `L1-b` (التصنيف) وقبل `L3` | `62`/`62-B`/`62-C` (28 خلية) |
| **التركيب (أ)** — ترتيب وقراءة مركّبة | بعد الاقتران | `64` v1.1 §4 |
| **التركيب (ب)** — التعرّف على النمط | بعد (أ) | `65` |
| **بوابة الجذر `G3`** — عرض وسؤال | بعد التعرّف | `67` · بنك `68` |
| **العرض** — وَسْما الثقة والحمل | قبل `L7` | `URS` v3.0 §5/§6 |

## 1.4 بنية التقرير (§6)

| # | القسم القائم | الأثر |
| :--- | :--- | :--- |
| ٧ | «**الإطار التأملي** — مبدأ السلسلة و**التعويض اللامتماثل** كمبادئ عامة، **بلا ربط بدرجاتك**» | 🔄 **مصدره تغيّر** (`M3-04` v1.1: احتواء وتضخيم لا تعويض) · **وعلّته سقطت**: «بلا ربط بدرجاتك» كان تحايلاً على غياب `URS`؛ والآن الربط مشروع |
| **جديد** | **القراءة المركّبة** (البصمة) — بعد الأقسام الخمسة | مصدرها طبقة التركيب |
| **جديد شرطي** | **سؤال موضع الجذر** — واحد لكل تقرير | `67` · `68` · `REFLECT §1.4` |
| ٥ | كتلة التحقق `G2` | ✅ بلا تغيير — **ولا تُدمج بسؤال الجذر** |

## 1.5 وَسْما العرض

| الوَسْم | الحالة في v1.1 | المطلوب |
| :--- | :--- | :--- |
| **الثقة** (`EP`) | ✅ موجود — لافتة الثقة `G1` | يبقى · + **قفل: لا يعبر إلى `K2`/`K4`** (`DEC-108`) |
| **الحمل** (`ST`) | ❌ **غائب** | يُضاف: «ملف النظام **تحت الحمل**، لا في الراحة» + **نص مانع الازدواج** (`ح-9`) |

---

# القسم 2: ✅ ما يصمد بلا تغيير — تحقّق مباشر

| البند | الحكم |
| :--- | :--- |
| الأوعية الخمسة (55 بنداً) · المعادلات `SS`/`SP` · الترميز الثماني | ✅ **القياس لم يُمسّ** |
| التثليث `DEC-064` لغةً وحيدة · **قفل `ق1`** (حظر عرض الرقم) | ✅ — وهو ما يجعل الاقتران الفئوي مشروعاً أصلاً |
| بوابة الثقة `G1` بلا إيقاف | ✅ متطابقة مع `ح-3` |
| بوابة الزيف `G2` | ✅ متطابقة مع `ح-4` · و`FR-05` معطَّل لـ`EP` — يطابق `68` |
| **ترتيب العرض `EP → IR → BI → CF → ST`** | ✅ **يصمد** — يطابق مسار المعالجة (منبع→مركز→أطراف→مصبّ) حرفياً |
| بوابة العزل `G5` وقواميسها | ✅ — وتُضاف إليها فحوص جديدة (§4) |
| **بنك الصياغة: «المحرك يجمّع ولا يؤلّف»** | ✅ **يصمد بوصفه مبدأً** — لكنه يفرض الفجوة الحاكمة (§3) |
| `DEBT-K3-EPPURITY-01` · `GAP-Q-09` | ✅ ديون مقبولة موثّقة — بلا تغيير |

---

# القسم 3: 🔑 الفجوة الحاكمة — `GAP-CMP-01`

**المشكلة بدقة:** بنك الصياغة يحصر مصادر النص في وثائق مستخدم معتمدة (`U-01` · `§2` · `U-08…11` · `REFLECT`). و**الخلايا الـ28 مستهلَك داخلي للمحرك أولاً** (`DEC-100 ق-أ`) — أي أن حقولها ③/⑤ **مكتوبة بلغة معمارية لا بلغة المستخدم**.

**⇒ لا يملك المحرك اليوم مصدراً مشروعاً لصياغة القراءة المركّبة.** وإنتاجها بالتأليف يخرق «يجمّع ولا يؤلّف» — الضمانة البنيوية لبوابة `G5`.

## الخيارات

| # | الخيار | القيمة | الكلفة |
| :--- | :--- | :--- | :--- |
| **(أ)** | **بنك صياغة مفتاحه: نوع العلاقة × موضع الكلفة** — مجموعة مغلقة من الجمل تُركَّب آلياً | ✅ يحفظ «يجمّع ولا يؤلّف» · **يتوسّع بلا تضخّم**: 5 أنواع × مواضع الكلفة المحدودة، لا 28 نصاً | بناء بنك جديد |
| (ب) | **نص مستخدم لكل خلية** (28) | أدقّ | ثقيل · تكرار كبير · يتضخّم مع أي توسعة |
| (ج) | **لا تُعرض القراءة المركّبة في هذا الإصدار** — تبقى داخلية | يحفظ `ق-أ` حرفياً · صفر مخاطرة | التقرير يظل بلا بصمة رغم اعتمادها |

**🟡 ترجيحي: (أ)** — لأن ما يميّز الخلايا في **لغة المستخدم** ليس هويتها الـ28، بل **نوع العلاقة وموضع الكلفة**؛ والمعمار الداخلي يبقى دقيقاً 28 بينما الواجهة تُركَّب من مجموعة مغلقة.
**🔒 وفي كل الأحوال:** لا يُبنى البنك بالتأليف الحرّ — مادته من الحقول المعتمدة (③ الآلية · ⑤ الكلفة · ⑥ ما لا يُدَّعى)، ومصدره الملزم النهائي `55-USER-K3` نظير `REFLECT`.

---

# القسم 4: فحوص `G5` المطلوب إضافتها

| # | الفحص الجديد | سنده |
| :--- | :--- | :--- |
| 1 | حظر **ترتيب المهارات** أو التنبؤ بأيّها «ينكسر أولاً» | `DEC-115` |
| 2 | حظر «قوة س **تحمل عبء** ص» — إسناد سببي مقارن | `NORM-01` · `M3-04` v1.1 |
| 3 | حظر تسمية **الشخص** بالنمط (`HF`) — يُسمّى النمط لا الشخص | `65 §5` |
| 4 | حظر **الإسناد الحاسم** للجذر بلا المكوّنات الأربعة | `67 §2` |
| 5 | حظر **ذكر الحمل مرتين** (خلية تحميل + وَسْم العرض) | `ح-9` |
| 6 | حظر **تنبؤ زمني** بموعد أو ترتيب | `TEMPORAL-01` |
| 7 | حظر **قراءة «محايد على الحافة» راحةً أو أفضلية** | `ح-10` |
| 8 | حظر **عبور وَسْم الثقة** إلى `K2`/`K4` | `DEC-108` |
| 9 | حظر أي نسبة أثر إلى `K4` | `RSK-005` |

---

# القسم 5: `audit` المحدَّث — الحقول المطلوبة

`cells_activated` (أكواد `CPL-…`) · `patterns_recognized` (`HF-…` + جزئي/تام) · `containment_state` (قوي/ضعيف/مركّب) · `cost_map` (موضع كل كلفة) · `root_question` (الكود · الاستجابة · مصدر الإسناد) · `conditional_layers` · `urs_version`.
**+ يبقى:** `sp` · `codes` · `bands` · `gates_fired` · `entries_used` · `instrument_pin` · `accepted_debts` · `inherited_gaps`.
🔒 **`audit` داخلي بالكامل** — لا يظهر للمستخدم (`ق1`/`DEC-092`).

---

# القسم 6: خطة التنفيذ المقترحة

| المرحلة | المحتوى | الحالة |
| :--- | :--- | :--- |
| **①** | حسم `GAP-CMP-01` | ✅ **محسوم — الخيار (أ)** (`DEC-125`) |
| ② | بناء بنك الصياغة المركّبة | ✅ **أُنجز** — `71-CMP-PHRASEBANK` (بانتظار `DEC-126`) |
| ③ | إعادة بناء `57-K3-ENGINE-SPEC` → **v2.0** — **تمرّ ببوابة النقد** (`DEC-120`) | ⬜ |
| ④ | تحديث `M3-06` (فهرس الاستدعاء) بقواعد سحب الخلايا | ⬜ |
| ⑤ | تحديث `57-K3-ENGINE_Blueprint` (المعمار السباعي → بطبقاته الجديدة) | ⬜ |
| ⑥ | اختبار جاف على حالة مُصطنعة | ⬜ |

---

# القسم 7: الفجوات

| الكود | البيان | الخطورة |
| :--- | :--- | :--- |
| **`GAP-CMP-01`** | لغة القراءة المركّبة — لا مصدر مشروع اليوم | 🔴 **حاجبة** |
| **`GAP-ENG-01`** 🔄 | **`M3-06` هو سجل الاستدعاء السيادي بقاعدة أسبقية**، وهو مبنيّ قبل طبقة الاقتران — قد يتعارض مع قواعد سحب الخلايا | **عالية** — تُفحص قبل v2.0 |
| `GAP-ENG-02` | `57-Blueprint` §2 (المعمار السباعي) لم يُراجع بعد الطبقات الجديدة | متوسطة |
| `RSK-005` | يحدّ أي صياغة تمسّ `K4` | عالية مؤجلة |

---

# القسم 8: محضر الاعتماد (`DEC-125`)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | تقرير النطاق أساساً لبناء `SPEC` v2.0 | ✅ معتمد |
| 2 | **استبدال الحدّ الوجودي** (§1.1): إسناد الجذر **يُعرض ويُسأل** · `URS` مفعَّل · العتبات المطلقة وحظر المقارنة **باقيان** | ✅ معتمد |
| 3 | إدراج الطبقات الخمس الجديدة في المعمار (§1.3) | ✅ معتمد |
| 4 | إضافة **وَسْم الحمل** ونص مانع الازدواج (§1.5) | ✅ معتمد |
| 5 | فحوص `G5` التسعة الجديدة (§4) | ✅ معتمد |
| 6 | حقول `audit` الجديدة (§5) | ✅ معتمد |
| 7 | **حسم `GAP-CMP-01`** | ✅ **الخيار (أ)** — بحكم الترجيح المعلن في §3 · **📌 يُسحب بكلمة إن قُصد غيره** |
| 8 | خطة التنفيذ السداسية (§6) | ✅ معتمد |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| القياس | ⚪ **لم يُمسّ** — `instrument_pin` ثابت |
| ما يصمد من `SPEC` v1.1 | ✅ **8 بنود** — منها الأوعية والمعادلات وبنك الصياغة مبدأً |
| ما يتغيّر | 🔄 **5 مواضع** — أخطرها الحدّ الوجودي |
| فجوة حاجبة | 🔴 `GAP-CMP-01` |
| فجوة مكتشفة عند المسح | 🔄 `GAP-ENG-01` (`M3-06` وقاعدة أسبقيته) |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة (`DEC-125`)؛ أساس بناء `SPEC` v2.0** |
