# 52-MATRIX-K3-06_Retrieval_Index.md

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `52-MATRIX-K3-06` — فهرس الاستدعاء |
| **الإصدار** | **v1.2 — ✅ معتمد** (`DEC-128` · `DEC-133` · `DEC-134` · `DEC-135`) · مرّت **بوابة النقد الذاتي** |
| **🔄 علّة إعادة البناء** | v0.2 **مسودة** كانت تحمل أسبقية سيادية على مواصفة **معتمدة** (`GAP-ENG-01`) · وستة مواضع تقادم، أخطرها ترميز `CF-01…06` **الملغى** |
| **الوظيفة** | ⚙️ **واجهة الحزمة مع المحرك** — ماذا يُسحب ومتى |
| **السند** | مصفوفات الحزمة · `P7` في `55-USER-K3-*` · `DEC-085` · **`DEC-099`** · **`DEC-113/115/117`** · `62`/`64`/`65`/`67`/`68`/`71` · `URS` v3.0 |
| **🔄 نطاق الأسبقية (`DEC-127`/ج)** | **`M3-06` يحكم في قواعد السحب حصراً** — أي المداخل والخلايا والقوالب تُسحب ومتى. **والمواصفة `57-SPEC` تحكم في التشغيل والصياغة وبنية التقرير.** لا أسبقية خارج نطاق كلٍّ منهما |
| **الحدّ الحاكم** | صفر عتبات مستحدثة (`DEC-064` حرفياً) · صفر تأليف · صفر مقارنة |

---

# القسم 1: خط أنابيب الاستدعاء الإلزامي

> **الترتيب مُلزِم.** تخطّي أي بوابة أو طبقة يُبطل التقرير.

```
[الدرجات: EP · IR · BI · CF · ST]  ← نطاقات DEC-064
              │
   ┌──────────▼──────────┐
   │ G1: بوابة الثقة     │ ✅ EP محدودة؟ → وسم الملف كله «غير حاسم»
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ G2: بوابة الزيف     │ 🛡️ أي من IR/BI/CF/ST >70%؟ → كتلة تحقق موحّدة
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ 🔄 L-CPL: الاقتران  │ تفعيل الخلايا عند الأقطاب فقط (28 خلية)
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ 🔄 L-CMP-أ: التركيب │ ترتيب بنيوي + قراءة مركّبة (قوالب 71)
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ 🔄 L-CMP-ب: النمط   │ تعرّف HF-01/02/04 من تجمّع خلايا
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ G3: بوابة الجذر     │ 🔄 مرفوعة **مشروطة** — تُنتج فرضية+بديل+وسم+سؤال
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ G4: بوابة الاحتواء  │ 🔄 مرفوعة — حالة: قوي / ضعيف / مركّب
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ 🔄 L-DSP: العرض     │ وَسْما الثقة والحمل — غير رقميين
   └──────────┬──────────┘
   ┌──────────▼──────────┐
   │ G5: بوابة العزل     │ 16 فحصاً
   └──────────┬──────────┘
         [سرد التقرير]
```

---

# القسم 2: قواعد الاستدعاء

## `G1` — بوابة الثقة ✅ فعّالة

| الشرط | الإجراء |
| :--- | :--- |
| `EP ≤ 50%` | 🔴 وسم الملف كله **«غير حاسم»** · تُقدَّم كل الأحكام احتمالية موسومة · **ولا إيقاف** |
| `EP > 50%` | متابعة |

**🔒 قفل `DEC-108`:** هذا الوَسْم **لا يعبر الدائرة** — يُحظر تعديل يقين قراءة `K2`/`K4` به.
**دَين مقبول:** `DEBT-K3-EPPURITY-01` — وعاء `EP` مختلط عاملياً (`DEC-094`)؛ يُقيَّد في `audit`.

## `G2` — بوابة الزيف 🛡️ إلزامية

| الشرط | الإجراء |
| :--- | :--- |
| `IR`/`BI`/`ST`/`CF` > 70% | تحفّظ `FR-01`/`FR-02`/`FR-03`/`FR-04` + سؤاله |
| الأربع > 70% | الكتلة بأسئلتها الأربع + جملة `HF-05` |
| `EP` مرتفعة | ⛔ **لا تحفّظ** — `FR-05` معطَّل |
| الإخراج | **كتلة واحدة موحّدة** من `REFLECT §1.3` **حرفياً** |

## 🔄 `L-CPL` — طبقة الاقتران

| القاعدة | البيان |
| :--- | :--- |
| **الاستدعاء عند الأقطاب فقط** | 🔄 **`SP < 50%`** ضعيف · **`50–70%` محايد — لا سحب** (والحدّ `50.0` **محايد**، `DEC-133`) · `>70%` قوي |
| **المصدر** | `62` (8) · `62-B` (16) · `62-C` (4) — **28 خلية** |
| **الحدّ البنيوي** | 6 خلايا كحدّ أقصى متزامن (10 مع `EP`) — لا 28 |
| **`EP ≤50%`** | تُلحق **لاحقة التعاقب** بكل خلية طرفُها `IR` (`62-C §3`) |
| **لا قدرة في قطب** | ⛔ **لا سحب** — الصمت مشروع (`ح-7`) |
| 🔄 **مهارة في `OUT`** | ⛔ **تُستبعد من الاقتران** — `OUT` فجوة تشغيلية لا نطاق · **ويُصرَّح باستبعادها** بجملة `C-16` (`DEC-133`) |

## 🔄 `L-CMP-أ` — التركيب والقراءة

الترتيب البنيوي: **المحور ← الجانبي ← التحميل ← التغذية** · الكُلف تُجمع **وصفياً لا حسابياً** · **المخرج قراءة مركّبة واحدة لا سرد خلايا** · الصياغة من `71` حصراً (`ب-1…ب-8`).

## 🔄 `L-CMP-ب` — التعرّف على النمط

| النمط | شرط التعرّف |
| :--- | :--- |
| `HF-01` | `IR ≤50%` **و** حافتا المحور مفعَّلتان معاً |
| `HF-02` جذر `CF` | `CPL-IR.CF-SW` **و** `CPL-CF.ST-W*` |
| `HF-02` جذر `BI` | `CPL-BI.CF-SW` **و** `CPL-BI.ST-S*` |
| `HF-04` | **≥ قناتين نشطتين** (التعريف المركزي: `M3-04` v1.2 §2.1 — `IR.ST-W*` · `CF.ST-W*` · **`BI.ST-S*`**) **و** `ST` في النطاق المحدود |

**🔒 `HF-03` و`HF-05` لا يُنفَّذان هنا** — موضعهما `G1` و`G2` (منع الازدواج — `65 §2.1`). · **النمط يتقدّم على خلاياه** (`ع-1`).

## `G3` — بوابة الجذر 🔄 **مرفوعة مشروطة** (`DEC-117`)

**لا يُصدر المحرك حكم جذر.** المخرج بأربعة مكوّنات **مجتمعة** — سقوط أحدها يُبطله:
① الوقائع · ② الفرضية **ومعها البديل** · ③ وَسْم «اشتقاق بنيوي غير محقّق ميدانياً» · ④ سؤال التحقق من بنك `68` (`RQ-01…04`).

| القاعدة | البيان |
| :--- | :--- |
| الحدّ العددي 🔄 | **سؤال واحد** لكل تقرير · لا يُدمج بكتلة `G2` · و**`RQ-05`** (تحقق الكلفة) تُطرح **فقط** عند صفر أنماط مع خلايا كلفة مفعَّلة — ولا تزاحم سؤال الجذر أبداً (`ت-2` · `DEC-135`) |
| التعدد 🔄 | **①** نمط منفرد ← يُطرح سؤاله **أياً كانت عائلته** · **②** نمطان فأكثر ← الأعلى بنيوياً والباقي **وقائعُ بلا فرضية** (`C-15`) · **③** صفر أنماط ← **لا سؤال** ولو فُعّلت خلايا (`DEC-134`) |
| الاستجابة | تأكيد ← «مُقرَّر من الشخص» · نفي ← **تُسحب ولا تُعاد** · صمت ← معروضة لا مُسنَدة |
| 🔴 الباقي محظوراً | **الإسناد الحاسم** بلا المكوّنات الأربعة |

**🔄 أين ذهب ترميز `CF-01…06` الملغى** `[مشتق — المرجع الحاكم `M3-03` v2.0]`:

| الرمز الملغى | مضمونه اليوم |
| :--- | :--- |
| `CF-01` التنظيم الأعمى | خلية `CPL-EP.IR-WS` + وَسْم الثقة (`HF-03`) |
| `CF-02` الهدوء الكاذب | `FR-01` في `M3-07` — **قطب زائف لا نمط** |
| `CF-03` الانهيار البطيء | `CPL-CF.ST-WS` + `HF-04` |
| `CF-04` التراكم الصامت | `HF-02` بجذر `BI` — و`GAP-PAT-01` قائمة عليه |
| `CF-05` العمى عن الحمل | `HF-03` + `HF-04` |
| `CF-06` الانهيار المُقنَّع | `HF-05` — يُنفَّذ في `G2` |

## `G4` — بوابة الاحتواء 🔄 **مرفوعة** (`DEC-115`)

| الحالة | شرطها المطلق | التحفّظ |
| :--- | :--- | :--- |
| **قوي** | الخمس `>70%` | 🛡️ توقيع `HF-05` — كتلة `G2` إلزامية |
| **ضعيف** | الخمس `≤50%` | 🔴 `EP` داخله ⇒ غير حاسمة · `HF-04` قائم |
| **مركّب** | أي تكوين آخر | الصمت مشروع عن المحايد |

**🔒 محظور:** ترتيب المهارات · «أيّها ينكسر أولاً» · «قوة س **تحمل عبء** ص» · أي تسمية.
**المصدر:** `URS` v3.0 §4 · الصياغة من `71 §3.1`.

## 🔄 `L-DSP` — طبقة العرض

| الوَسْم | النص | القيد |
| :--- | :--- | :--- |
| **الثقة** (`EP`) | من `G1`/`REFLECT` | لا يعبر الدائرة |
| **الحمل** (`ST`) | «تُقرأ الصورة وأنت **تحت الحمل، لا في الراحة**» (`71 §3.2`) | **مرة واحدة** · ولا يُعاد اشتقاق حمل منه (`ح-9`) |

## `G5` — بوابة العزل ✅ **16 فحصاً**

**السبعة المستبقاة:** نسبة سلوك ملتبس بلا سؤال فصل (`M3-05 §4`) · ألفاظ قواميس التطهير · خلط `EP`/`K2-E` (`X-01`) · خلط `ST`/مثابرة `K4` (`X-02`) · نسبة «السكون بعد الحظر» لـ`BI` (`X-03`) · ادّعاء اتجاه/قطب داخل مهارة (`DEC-076`) · صعود/هبوط لمهارة مفردة (`DEC-082`).

**🔄 التسعة الجديدة:** ترتيب المهارات أو التنبؤ بالانكسار · إسناد سببي مقارن · تسمية الشخص بالنمط · إسناد جذر حاسم · ذكر الحمل مرتين · تنبؤ زمني · تقديم «محايد على الحافة» أفضليةً · عبور وَسْم الثقة إلى `K2`/`K4` · نسبة أي أثر إلى `K4` (`RSK-005`).

---

# القسم 3: خرائط السحب

## 3.1 مداخل طبقة المستخدم (`P7`) — ✅ مستبقاة

| الشرط | المداخل |
| :--- | :--- |
| دائم | التعريفية + أقفال العزل + المدخل الختامي |
| `SP ≥ 50%` | إشارات القوة |
| `SP ≤ 50%` | إشارات تحتاج انتباهاً |
| `SP > 70%` | ➕ تحفّظ `G2` + سؤال التحقق |
| حمل مرتفع / `OUT`–`L` | «حالة الضغط القصوى» |

**ترتيب عرض الأقسام الخمسة:** `EP → IR → BI → CF → ST` — يطابق مسار المعالجة.

## 3.2 🔄 سحب القوالب المركّبة

| المفتاح | المصدر |
| :--- | :--- |
| نوع العلاقة × موضع الكلفة | `71 §2` — القوالب `T-01…T-10` |
| حالة الاحتواء | `71 §3.1` |
| الوَسْمان | `71 §3.2/3.3` |
| سؤال الجذر | `68` · `REFLECT §1.4` |
| جمل الوصل | **قائمة مغلقة** — `GAP-CMP-03` |

**قفل التزاوج:** المحرك يقرأ وثائق **موسومة الإصدار**؛ أي تعديل في توأم العمق أو في السجل **يجمّد السحب** حتى المزامنة.

---

# القسم 4: حدود النقاء (واجهة المحرك)

1. **ترتيب الأنابيب مُلزِم** — `G1` قبل كل شيء.
2. **`G2` لا تُتخطّى** — خصوصاً في الملفات المرتفعة.
3. **صيغة ترجيح لا حسم** في `G3`/`G4` (`DEC-085`).
4. **لا تشخيص · لا خفض درجات · لا لغة عيادية.**
5. 🔄 **`URS` مفعَّل** — `GAP-URS-01` مغلقة (`DEC-113`/`DEC-115`).
6. 🔄 **لا تعويض في أي اتجاه**؛ وقوةٌ تعمل على مادة معطوبة واردة من **موقع سابق** في المسار (منبع `EP` → مركز `IR` → أطراف → مصبّ `ST`) **تُضخّم أثر العطل ولا تلغيه** (`ح-12`).
7. 🔄 **الإطار البنيوي يُعرض أداةَ تأمل:** «قوة عنصر لا تُصلح عطل آخر — قد تحتوي أثره بكلفة، وقد تُضخّمه». **وإسناد الجذر يُعرض ويُسأل** (`G3`).
8. 🔄 **صفر تأليف** — كل نص من `71`/`REFLECT`/`55-USER` أو من جمل الوصل المغلقة.
9. 🔄 **الأسبقية محدودة النطاق** (§0) — قواعد السحب حصراً.

---

# القسم 5: الفجوات

| الكود | البيان | الحالة |
| :--- | :--- | :--- |
| ~~`GAP-URS-01`~~ | العتبات وصيغة التجميع | ✅ **مغلقة** — والعتبات المستعملة `DEC-064` حرفياً |
| `GAP-CMP-02` | صياغة القوالب النهائية في `55-USER-K3` | 🟡 مفتوحة |
| `GAP-CMP-03` | قائمة جمل الوصل المغلقة | 🟡 مفتوحة |
| ~~`GAP-M3-05-01`~~ | ✅ **مغلقة منذ `DEC-090`** — قيدها هنا كان **نقلاً متقادماً**. المستهلك محدَّد: **المستخدم لا المحرك**؛ الأسئلة في `REFLECT §2`، و`M3-05` المواصفة البنيوية (`DEC-135`/بند 7) |
| **`DEBT-K3-FIELD-01`** 🔄 | **دَين التحقق الميداني** — يدمج `GAP-M3-03-02` · `GAP-PAT-01` · `GAP-M3-01-01` · `GAP-M3-04-01` · `GAP-CMP-04`. **مطوَّق ببروتوكول `67` ووَسْم «غير محقّق ميدانياً»** (`DEC-135`) | 🟡 مفتوح ومطوَّق |
| `RSK-005` | حدود `K3`/`K4` — فحص `G5` رقم 16 يطوّقه | 🔴 مفتوح |

---

# القسم 6: سجل إعادة البناء — v0.2 ← v1.0

| # | v0.2 | v1.0 | السند |
| :--- | :--- | :--- | :--- |
| 1 | **مسودة** بأسبقية سيادية على مواصفة معتمدة | **معتمدة** بأسبقية **محدودة النطاق** | `DEC-127` (أ)+(ج) |
| 2 | 5 بوابات | **5 بوابات + 4 طبقات** | `DEC-102…117` |
| 3 | `G3` مجمَّدة بترميز `CF-01…06` **الملغى** | **مرفوعة مشروطة** + جدول مآل الرموز | `DEC-099` · `DEC-117` |
| 4 | `G4` مجمَّدة | **مرفوعة** — ثلاث حالات | `DEC-115` |
| 5 | «الحلقة القوية **تحمل عبء** الضعيفة» | 🔴 **محذوفة** + فحص `G5` يحظرها | `NORM-01` |
| 6 | «نظام متسلسل · لا تعويض صاعد» | **لا تعويض في أي اتجاه** + التضخيم بالموقع | `DEC-116` |
| 7 | «لا إنتاج `URS`» | **`URS` مفعَّل** | `DEC-121` |
| 8 | `G5` بسبعة فحوص | **16 فحصاً** | `70 §4` |

**المستبقى بلا مساس:** مبدأ الأنابيب مُلزِم الترتيب · `G1` · `G2` وإلزاميتها وتعطيل `FR-05` · خريطة `P7` · ترتيب العرض الخماسي · العتبات المسنودة · «ترجيح لا حسم» · قفل التزاوج.

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| الخلل الحوكمي | ✅ **مُزال** — معتمدة بأسبقية محدودة النطاق |
| خط الأنابيب | ✅ 5 بوابات + 4 طبقات |
| `G3`/`G4` | ✅ **مرفوعتان** (`G3` مشروطة) |
| ترميز `CF-01…06` | ✅ **مُصفّى** — بجدول مآل لكل رمز |
| `G5` | ✅ **16 فحصاً** |
| صفر عتبات مستحدثة | ✅ `DEC-064` حرفياً |
| بوابة النقد | ✅ مُطبَّقة |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة (`DEC-128`)؛ تحلّ محل v0.2 · واجهة الحزمة مع المحرك مثبَّتة** |
