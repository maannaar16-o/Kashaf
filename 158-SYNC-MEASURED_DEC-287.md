# 158-SYNC-MEASURED — `DEC-287`: **خطُّ الأساس يُقاس لا يُتذكَّر** — وصنفٌ تكرّر أربعاً

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**① و②**» على توصيتي — 2026-08-15 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-15 |
| **الأثر** | `gate.py` يقيس **خطّ الأساس** في `00-HANDOVER` · الوثيقتان مزامَنتان · وملفُّ `GAP-X-EXH-01` مرفوع · البوابة **25/25** والبصمات الستّ **حرفياً** |
| **التغيير المرافق** | `CHG-106` |
| **الحاكم** | `DEC-273/③` (الوثائق تُقابَل بالبوابة لا تُصدَّق) · `DEC-281 §5` · `CHG-096` · `م-2` |
| **الكود** | `158` — تمديد بمسوّغه: توسيعُ فحصٍ قائمٍ إلى حقلٍ كان خارجه |

---

# §1 ما وُجد — **ادّعاءُ قياسٍ أوسع من القياس**

`00-HANDOVER` كان يقول في ختامه:

> «خط الأساس `DEC-280` · الترقيم التالي `DEC-281` · `CHG-099` … **والمزامنة صارت مقيسة**: `gate.py` يقابل هذه الوثيقة بالبوابة»

**والواقع `DEC-286`/`CHG-105` — ستّة قرارات.**

**والجملةُ الثانية تجعل الأولى أسوأ:** الوثيقة **تفاخر بأنها مقيسة**، والمقيسُ منها كان **قائمةَ الأدوات والبصمات وحدها**. فرقمُ خط الأساس كان خارج الفحص، **وقارئُ الجملة يظنّه داخله**.

> **ووعدُ قياسٍ أوسع من القياس أخطر من غياب القياس**: الغيابُ يُبقي الحذر، والوعدُ يُنيمه.

---

# §2 الصنف تكرّر **أربع مرّات** — وهذا هو المسوّغ

| # | الحادثة | ما تخلّف |
| :--- | :--- | :--- |
| ① | `CHG-096` | رؤوس `51-MATRIX-*` قالت «مسودة» بعد اعتمادها **بعام** |
| ② | `DEC-273/③` | قائمة الأدوات في ثلاثة مواضع تنجرف — فصارت **مقابَلة بالبوابة** |
| ③ | `DEC-281 §5` | الميثاق يصف نفسه «بلا تشغيل» **وقد شُغِّل** |
| ④ | **هذه** | خط الأساس متخلّفٌ ستّة قرارات — **وفي سطرٍ يفاخر بالقياس** |

⇒ **والذي يتكرّر أربعاً لا يُعالَج بالانتباه.** فالعلاج **توسيعُ الفحص** لا إضافةُ تذكير.

---

# §3 التنفيذ — الفحص يشتقّ ولا يُخزَّن

خطُّ الأساس **لا يُكتب في البوابة**؛ يُشتقّ من السجلّ:

| الحقل | مصدرُه المشتقّ |
| :--- | :--- |
| «خط الأساس» | **أعلى `DEC` مختوم** في `01-MASTER` (بعد استثناء المحجوز) |
| «الترقيم التالي» | **الأعلى + 1** — الحقلُ نفسه الذي يفحصه `DEC-274` |
| «`CHG`» | **أعلى تغييرٍ مختوم** في `02-MASTER` |

⇒ **لا نسخةَ ثانيةً للرقم** (`م-2`): البوابة تحسبه، ولا تحفظه.

**وحُفظت صيغةُ السطر عن قصد:** فحصٌ لا يقرأ سطراً **يقول ذلك** ولا يمرّ صامتاً — «السطر غير مقروء — تُحفظ صيغته ليبقى مقيساً».

---

# §4 الحرّاس مُختبَرون على فشلهم — حقنتان

| الحقنة | النتيجة |
| :--- | :--- |
| خطُّ أساسٍ متخلّف (`DEC-284` بدل `286`) | ❌ **رُصد** — «المُعلَن … · المتوقَّع …» بالأرقام الثلاثة |
| **كسرُ صيغة السطر** (فلا يبقى مقروءاً) | ❌ **رُصد** — «السطر غير مقروء» · **فالحذفُ لا يُنجي من الفحص** |

**والحقنة الثانية هي المهمّة:** فحصٌ يقرأ حقلاً يُخدَع بحذفه لا بتغييره فقط. فقياسُ **وجود الصيغة** جزءٌ من قياس قيمتها.

---

# §5 و`CLAUDE.md` — ما لا يذكره الدليلُ يُخرَق

أُضيفت فقرةُ **حقوق صاحب البيان وموضع الهوية** (`DEC-286`): `export`/`forget` · المحو الكامل أو الرفض · إعلانُ ما مُحي · و`workshop_codes.json` **خارج** `workshop_data/`.

> **والدليلُ يقود وكيلاً لا يقرأ سجلّ القرارات كلَّه.** فحقٌّ لصاحب بيانٍ غيرُ مذكورٍ فيه **يُعاد بناؤه أو يُخرَق** — وكلاهما وقع في مشاريع أقلَّ حراسةً.

---

# §6 وملفّ `GAP-X-EXH-01` — **رُفع ولم يُحسم**

`157-EXH-DOSSIER`: الحدُّ الحاكم بنصّه · أين ينقطع (**حالةٌ خارج تعريفَي الطرفين لا بينهما**) · ما يفعله النظام اليوم · **وثلاثة خيارات** بكلفة كلٍّ.

**وقراءتي مرفوعةٌ لا منفَّذة:** أرجّح **إعلانَ الحدّ دائماً** (`م-8`) — لأن الاعتراف قائمٌ ويعمل، ولأن `DEC-072 §2.5` أبقى الحدّية مفتوحةً **قصداً**، ولأن المادّة البديلة **ستكون تأليفاً بلا سند**.

**ولم أكتب سطراً في الدستور.** فالنظرية شأنُ المالك، **والملفُّ يُعرض ليُقرَّر لا ليُقرِّر**.

---

# §7 🔎 وأوقفتني البوابة على ختمي نفسه

أُغلق القرار وسُجّل، **فسقط الفحصُ الذي بنيتُه فيه للتوّ**: «المُعلَن `DEC-286` · المتوقَّع `DEC-287`». فالوثيقة صارت متخلّفةً **بقرارٍ واحد — قرارِها هي**.

> **والحرسُ الذي يمسك بانٍيه أول ما يعمل حرسٌ حقيقي.** وهذا ما وقع في `DEC-278 §8` مع فحص الانجراف الوثائقي — **وتكرارُه دليلُ صحّته لا عيبٌ فيه**.

---

# §8 ما لم يُمسّ

- ❌ **طبقة القياس** · **المحرّكات والتقارير والحزم** · **البصمات الستّ حرفياً**.
- ❌ **السلوك كلُّه** — قرارٌ يوسّع فحصاً ويزامن وثيقتين ويرفع ملفّاً.
- ❌ **`GAP-X-EXH-01`** — **مفتوحٌ كما كان**، وقد صار مرئياً في صفحةٍ واحدة.

---

# §9 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| خطّ الأساس | ✅ **مقيسٌ ومشتقّ** — لا نسخة ثانية للرقم |
| المزامنة | ✅ `00-HANDOVER` و`CLAUDE.md` مطابقان للواقع |
| الحرّاس | ✅ **حقنتان · إصابتان** — منهما **الحذف** لا التغيير وحده |
| الملفّ | ✅ `157-EXH-DOSSIER` مرفوعٌ بثلاثة خيارات وقراءةٍ مرجَّحة |
| الأدوات | **25/25** |

---

**[نهاية `158-SYNC-MEASURED` — `DEC-287` · الترقيم التالي `DEC-288` · `CHG-106`]**
