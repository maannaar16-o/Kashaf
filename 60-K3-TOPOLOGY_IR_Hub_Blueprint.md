# 60-K3-TOPOLOGY_IR_Hub_Blueprint.md
## مخطط الطوبولوجيا الجديدة لدائرة الانفعال — `IR` مركزاً (للاعتماد قبل إعادة بناء `M3-01`/`M3-03`)

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `60-K3-TOPOLOGY` |
| **الإصدار** | v1.0 — ✅ **معتمد** (`DEC-098`) |
| **حالة الاعتماد** | ✅ **معتمد بقرار مالك المشروع** — بنية ملزمة لإعادة بناء `M3-01`/`M3-03` وما يليهما |
| **الغرض** | البنية الطوبولوجية المرجعية لدائرة الانفعال — تحكم `M3-01`/`M3-03` وطبقة الاقتران و`URS` |
| **السند** | `58-VISION §2/§3` (`DEC-095`) · تعريفات المالك المحدَّثة · مثاله التطبيقي (`IR`/`BI`/`CF`) · `59-IMPACT` |
| **الحدّ الحاكم** | طبقة **تفسير** حصراً — لا مساس بالقياس |

---

# القسم 1: الشكل العام المقترح

```
                    ┌────────────────────────────────────┐
                    │        EP — المُضاعِف الإدراكي        │
                    │   يرفع دقة مُدخَلات العناصر الأربعة    │
                    │   + يساهم بذاته في خفض حدة الشحنة    │
                    └───┬────────┬────────┬────────┬─────┘
                        ▼        ▼        ▼        ▼
                        │   ┌─────────────┐   │
      خارج K3  ◄─── BI ◄┼───┤     IR      ├───┼──► CF
   (سلوك → K4)          │   │ المركز Hub  │   │
        ▲               │   │ شدة واتجاه  │   │        ▲
        │               │   └──────┬──────┘   │        │
        │               │          │          │        │
        └───────────── تكامل جانبي BI↔CF ──────────────┘
              (تسليم المحظور للمراجعة — قاعدة DEC-072 باقية)
                        │          │          │
                        ▼          ▼          ▼
                    ┌────────────────────────────────────┐
                    │      ST — حامل النظام عبر الزمن       │
                    │  مُكامل الحمل المتبقي من الجميع        │
                    │        + خط الدفاع الأخير             │
                    └────────────────────────────────────┘
```

**القراءة:** `IR` **محور** الدائرة (الأساس الداخلي) · `BI` و`CF` **ذراعاه المباشرتان** (امتداد خارجي/امتداد مراجعة) · `EP` **طبقة تضخيم** فوق الجميع · `ST` **قاعدة حمل** تحت الجميع. **السلسلة الخطية `EP→IR→BI→CF→ST` تُلغى.**

---

# القسم 2: العلاقات العشر — من قفزات مشتقة إلى حافات مباشرة

**اكتشاف بنيوي:** في الطوبولوجيا القديمة كانت 4 علاقات مباشرة + 6 "مسارات مشتقة". **في بنية المركز، العلاقات العشر كلها (`C(5,2)=10`) تصبح مباشرة** — منظّمة في **أربع عوائل**:

| العائلة | الأزواج | طبيعة الاقتران | ما تستوعبه من البنية القديمة |
| :--- | :--- | :--- | :--- |
| **① المحور** (Hub) | `IR–BI` · `IR–CF` | امتداد مباشر: `BI` بوابة ما-بعد-الشعور لحالة `IR`؛ `CF` ذراع المراجعة وعدم التصعيد | `H2` · (جزء من `H3`) |
| **② التضخيم** (Amplify) | `EP–IR` · `EP–BI` · `EP–CF` · `EP–ST` | `EP` يرفع دقة مُدخَل كل عنصر (فهم الموقف → شحنة أخف، كبح أصوب، مراجعة أدق، رصد حمل مبكر) | `H1` · `P1` · `P2` · `P3` |
| **③ التحميل** (Load) | `IR–ST` · `BI–ST` · `CF–ST` | بقايا عمل كل ذراع تنزل حملاً على `ST` (شحنة مزمنة · كبح متراكم · جمود مستمر) | `H4` · `P5` · `P6` |
| **④ الجانبي** (Lateral) | `BI–CF` | تكامل الذراعين: المحظور يُسلَّم للمراجعة (**قاعدة `DEC-072` باقية نصاً**) | `H3` |

**2 + 4 + 3 + 1 = 10 ✓** — لا قفزات، لا ازدواج، كل زوج في عائلة واحدة.

---

# القسم 2-ب: الديناميكية الدورانية — توليد السياق الداخلي وعودة الشحنة **[من مثال المالك المعتمد]**

البنية ليست ممرّاً أحادي الاتجاه؛ إنها **دورة معالجة**:

```
 [سياق خارجي]                         [سياق داخلي مولَّد]
 استفزاز · توتر · ملل …              شحنة ناتجة عن التقاء ضعف
        │                            عنصرٍ بمستجدّ (ضيق الجمود مثلاً)
        ▼                                   │
   ┌─────────────┐      عودة الشحنة         │
   │  IR المركز  │◄───────────────────────┘
   └──┬───────┬──┘
خدمة ▼       ▼ خدمة
     BI      CF ──(عند الضعف: فشل التكيّف يولّد شحنة → تعود للمركز)
      │       │
      ▼       ▼
      … التحميل على ST + الامتداد العابر للدوائر (VR-7) …
```

**القواعد الثلاث المستخلصة:**

| # | القاعدة | البيان |
| :--- | :--- | :--- |
| **د-1** | **مصدرا السياق مزدوجان** | خارجي (`VR-6`) + **داخلي مولَّد**: ضعف عنصر × مستجدّ ← شحنة جديدة تدخل الدورة كسياق تتفاعل معه بقية العناصر |
| **د-2** | **الحافات المحورية ثنائية** | **خدمة** (المركز→الذراع: يضبط ويمكّن) + **عودة** (الذراع→المركز: شحنة الفشل تعود للاحتواء). *(ينسخ حظر التغذية العكسية القديم — الذي كان وليد البنية الخطية الملغاة — في طبقة التفسير حصراً)* |
| **د-3** | **احتواء الشحنة ≠ تعويض الوظيفة** | المركز القوي يحتوي **شحنة** عطل الطرف (فلا تفسد القرار/السلوك) بكلفة — **ولا يُغني عن وظيفته** (التكيّف لا يحدث). *(الصيغة الدقيقة الموجَّهة للخطوة ④ / `DEC-087`)* |
| **د-4** | **عناصر أي دائرة مولِّدة للسياق** *(مُعمَّمة)* | لكل عنصر — في `K2` أو `K4` أو `K3` نفسها — **خاصيته وحساسيته**؛ اصطدامها بواقع مخالف يولّد **شحنة بطابعها** تدخل دورة `K3` وتُعالَج وفق معادلة الفرد. أمثلة معتمدة: عدسة تحليلية × إغفال المنطق · قوة مبادرة (`K4`) × جمود المحيط. **الواجهة العابرة ثنائية: الدائرة تولّد ← `K3` تعالج ← المعالجة تنعكس على تعبير تلك الدائرة** (`VR-7` بالاتجاهين). |
| **د-5** | **الفجوة العلائقية مولِّدة للسياق** | التفاعل بين **معادلتين بشريتين مختلفتين** يولّد سياقاً انفعالياً **لدى الطرفين** (قوة عنصرٍ عند أحدهما × ضعفه عند الآخر ← احتكاك). **كل طرف يعالج نسخته وفق معادلته الانفعالية الخاصة** — فقد يحتوي أحدهما ما يفيض به الآخر. بُعد **علائقي (Dyadic)** مستقل عن المصادر الفردية. |

**مصادر السياق الأربعة (مكتملة):** ① خارجي موضوعي (`VR-6`) · ② داخلي من `K3` (**د-1**) · ③ من عناصر أي دائرة (**د-4**) · ④ **علائقي من فجوة المعادلات** (**د-5**).

**النتيجة الحاكمة:** السياق الانفعالي **ليس معطىً موضوعياً** — إنه ناتج تفاعل معادلة الفرد مع البيئة ومع معادلات الآخرين. (التشغيل الفعلي لـ`Personality Profile Fluidity`: نفس الحدث لا يولّد نفس السياق عند معادلتين مختلفتين.)

> ### 📌 مبدأ التمثيل لا الحصر — **[نص المالك، حاكم]**
> *«هذه العناصر معدّة للتمثيل وليس الحصر؛ هذه التفاعلات تنعكس على **جميع العناصر وجميع الدوائر** في نسق ديناميكي.»*
> **الأثر الملزم:** القواعد `د-1…د-5` **عامة على كل عنصر وكل دائرة**؛ الأمثلة المذكورة **توضيحية معتمدة** لا قوائم مغلقة. ويُحظر معاملتها كسجلّ حصري، كما يُحظر توليد حالات جديدة واعتمادها بلا إقرار صريح (تبقى `[مقترح]`).

**الغاية:** هذا التفاعل الدوراني — داخل الدائرة وعابراً للدوائر — هو ما يُنتج في الطبقة التفسيرية **البصمة الفريدة** لكل فرد (`Personality Profile Fluidity`). البصمة = **نمط معالجة ديناميكي**، لا ملف درجات ساكن. *(تُوجَّه لإعادة تعريف `URS` في الخطوة ④)*

---

# القسم 3: مثال المالك التطبيقي كنموذج تحقّق (Validation Case)

مثالك المعتمد في `58-VISION` يُقرأ على البنية الجديدة قراءةً مباشرة:

| الحالة | القراءة الطوبولوجية |
| :--- | :--- |
| **`IR` قوي + `BI` ضعيف** | المركز يضبط الداخل (قرار سليم نسبياً) لكن **ذراع البوابة تسرّب** → يُبدي رأيه ولو جارحاً. عطل طرف لا مركز. |
| **`IR` ضعيف + `BI` قوي** | المركز يغلي (شحنة غير مضبوطة تؤثر على القرار) لكن **الذراع تحتوي المخرج** → لا يعلن؛ يغلي داخلياً. **احتواء طرفي بكلفة.** |
| **+ `CF` قوي** | ذراع المراجعة تلتقط لاحقاً قرار الغليان وتعدّله → «التريث يساعده في مراجعة قراره». |

✅ **البنية الجديدة تعيد إنتاج مثالك حرفياً** — وهذا اختبار الصلاحية الأول.

## 3.2 نموذج التحقّق الثاني: `IR` قوي × `CF` ضعيف (المثال الثاني للمالك)

| المرحلة | القراءة الطوبولوجية |
| :--- | :--- |
| تغيير مفاجئ يصطدم بـ`CF` ضعيفة | فشل تكيّف → **توليد سياق داخلي** (ضيق/توتر) — قاعدة **د-1** |
| الشحنة المولَّدة تعود للمركز | **عودة الذراع→المركز** — قاعدة **د-2** |
| `IR` القوي يحتوي الشحنة | القرار والسلوك يبقيان سليمين — **بكلفة احتواء**، والتكيّف نفسه **لا يحدث** — قاعدة **د-3** |
| استمرار الدورة | التفاعل يمتد لبقية العناصر وعبر الدوائر (`VR-7`) ← **البصمة الفريدة** |

✅ **اختبار الصلاحية الثاني ناجح** — البنية v0.2 تمثّل المثالين معاً؛ v0.1 كانت تعجز عن الثاني.

## 3.3 نموذج التحقّق الثالث: عابر الدوائر — تحليليٌّ أمام إغفال المنطق (المثال الثالث للمالك)

| المرحلة | القراءة الطوبولوجية |
| :--- | :--- |
| عدسة تحليلية مهيمنة تصطدم بمن يغفلون التسلسل المنطقي/الحقائق | **حساسية عدسة × انتهاك بيئي** → توليد شحنة بطابع العدسة — قاعدة **د-4** |
| الشحنة تدخل دورة `K3` | تُعالَج وفق **معادلة الفرد الانفعالية** (قوة/ضعف عناصره) |
| `IR` ضعيف في هذا السياق | المركز لا يحتوي → **تبرّم** ظاهر (وقد يتصاعد عبر `BI`/`CF` بحسب حالهما) |
| الدورة تستمر | «تدور الدوائر في سياق ديناميكي انسجامي» — المعالجة تنعكس بدورها على تعبير العدسة (`VR-7`) |

✅ **اختبار الصلاحية الثالث ناجح** — البنية v0.3 تمثّل التوليد العابر للدوائر الذي لم يكن ممثَّلاً قبلها.

## 3.4 نموذج التحقّق الرابع: علائقي — فجوة المبادرة بين شخصين (المثال الرابع للمالك)

| المرحلة | القراءة الطوبولوجية |
| :--- | :--- |
| صاحب مبادرة عالية (`K4`) يتعامل مع ضعيف المبادرة | **فجوة معادلتين** → توليد توتر لدى الطرفين — قاعدة **د-5** |
| التوتر يدخل دورة `K3` لدى كل طرف على حدة | يُعالَج وفق **معادلة كلٍّ منهما** — لا نتيجة واحدة للحدث الواحد |
| صاحب المبادرة `IR` قوي | يحتوي التوتر → يستمر التفاعل بلا انفجار (**بكلفة احتواء**، والفجوة الوظيفية قائمة — **د-3**) |
| صاحب المبادرة `IR` ضعيف | لا احتواء → التوتر يظهر (ويتصاعد عبر `BI`/`CF` بحسب حالهما) |

✅ **اختبار الصلاحية الرابع ناجح** — البنية v0.4 تمثّل التوليد العلائقي، وتفسّر لماذا **الحدث الواحد يُنتج مسارين مختلفين** بحسب معادلة كل طرف.

**📌 بذرة للطبقة العابرة (تسجيل أمامي):** «كل حالة لها سياقاتها الخاصة» (نص المالك) يعني أن الطبقة العابرة ستحتاج **سجلّ سياقات مولَّدة لكل عدسة** (حساسيات العدسات الثماني × عناصر `K3` الخمسة). المثال التحليلي وحده معتمد الآن؛ **بقية العدسات تُشتق لاحقاً كمقترحات تحتاج اعتماداً** — موضعها الطبيعي خطوة الطبقة العابرة، لا الخطوة ③.

---

# القسم 4: ما يصمد من البنية القديمة (لا يُهدم)

| المكوّن | مصيره في البنية الجديدة |
| :--- | :--- |
| **تفرّع `BI`** (السماح → خارج `K3` نحو السلوك/`K4`) | ✅ باقٍ — `BI` تبقى بوابة الدائرة السلوكية الوحيدة |
| **قاعدة `DEC-072`** (السكون بعد الحظر) | ✅ باقية نصاً — ضمن العائلة الجانبية `BI↔CF` |
| **`ST` مُكامل زمني / خط دفاع أخير / المصبّ الأعظم** | ✅ باقٍ ويتعزّز — الآن يستقبل حملاً من **ثلاث** قنوات مباشرة |
| **مبدأ «الجذر ≠ موضع الظهور»** (`M3-03`) | ✅ باقٍ ويغتني — عطل المركز يظهر على الأطراف، وعطل الأطراف يظهر على `ST` |
| **`M3-07`** (القراءات الزائفة) | ✅ غير متأثرة إطلاقاً (عتبات مطلقة) |
| **دور `EP` المزدوج** (`DEC-085`) | 🔄 **يتعمّم** — من «ثقة + تغذية `ST`» إلى **تضخيم رباعي** لكل العناصر |

---

# القسم 5: أثر أمامي على الخطوات التالية (تنبيه، لا تنفيذ)

| الخطوة | الأثر المتوقع |
| :--- | :--- |
| **③ طبقة الاقتران الفئوي** | العوائل الأربع = **هيكل سجل الاقتران** الجاهز (10 أزواج × حالات قوي/ضعيف — على نمط `CSLIM`) |
| **④ مراجعة `DEC-087`** | الصيغة الجديدة المكتملة (بعد **د-3**): **الاحتواء متبادل الاتجاه، والتعويض معدوم في الاتجاهين** — الأطراف تحتوي شحنة المركز الضعيف بكلفة (الغليان)، والمركز يحتوي شحنة الطرف الضعيف بكلفة (جهد الاحتواء) — **ولا أحد يُغني عن وظيفة الآخر** (لا `BI` قوي يُصلح غليان `IR`، ولا `IR` قوي يُنجز تكيّف `CF`). الجوهر اللامتماثل باقٍ: **احتواء شحنة ≠ تعويض وظيفة** |
| **`FR-05`** (يقظة `EP` المفرطة — معلَّق) | موقع `EP` الجديد كمضاعِف قد يعيد فتح السؤال — يُترك لخطوته |
| **⚠️ البُعد العلائقي (د-5)** | يمسّ وثائق **خارج نطاق التقرير الفردي**: بروتوكول تركيب الفرق ومصفوفات التوافق الثنائي في خط الأساس. **تنبيه استراتيجي مسجَّل — لا تنفيذ**؛ موضعه قرار معماري مستقل بعد اكتمال مسار `K3` |

---

# القسم 6: نقطة قرار واحدة مفتوحة — السياقات الانفعالية (`VR-6`)

ربطُ أنواع السياقات (قصير-حار: استفزاز · ممتد: توتر/قلق · بارد: ملل) بالعناصر الأمامية لكل مسار هو **جوهر «المسار الانفعالي»** في تصورك، ويُقترح مبدئياً: الحار القصير → جبهة `IR–BI` · الممتد → جبهة `ST–CF` · **البارد (الملل) → تقاطع مع حدود `K4`** (`X-02`: التوقف الهادئ ملَلاً = مثابرة).
✅ **محسوم (`DEC-100`):** الحار القصير → جبهة `IR–BI` · الممتد → جبهة `ST–CF` · **البارد (الملل) → يُسند لتقاطع `K4`** (`X-02`) لأنه حالة هدوء لا شحنة. **البند المؤجَّل مغلق.**

---

# ختم: المطلوب لاعتماد هذا المخطط

| # | بند الاعتماد |
| :--- | :--- |
| 1 | الشكل العام: `IR` محور · `BI`/`CF` ذراعان · `EP` مُضاعِف رباعي · `ST` قاعدة حمل |
| 2 | العوائل الأربع للعلاقات العشر (محور/تضخيم/تحميل/جانبي) |
| **2-ب** | **الديناميكية الدورانية (د-1…د-5):** توليد السياق الداخلي · ثنائية الحافات · احتواء ≠ تعويض · **عناصر أي دائرة مولِّدة (مُعمَّمة)** · **الفجوة العلائقية مولِّدة** — مع **مبدأ التمثيل لا الحصر** |
| 3 | قائمة «ما يصمد» (§4) — خاصة بقاء تفرّع `BI` وقاعدة `DEC-072` |
| 4 | التوجيه الأمامي لـ`DEC-087` (الصيغة المكتملة: احتواء متبادل، تعويض معدوم) — **توجيه فقط، يُنفَّذ في ④** |
| 5 | ✅ موضع «السياق البارد» (§6) — **محسوم بـ`DEC-100`** |
