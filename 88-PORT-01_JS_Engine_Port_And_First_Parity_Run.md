# 88-PORT-01 — المرحلة ① : نقل المحرّكين إلى JS + أول تشغيل تكافؤ

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** (النقل · التكافؤ · إصلاح `DEF-K3-02` بـ`DEC-201`) · 🟡 فجوة (`G3`) |
| **نطاق الاستدلال** | `k2_engine.py` · `k3_engine.py` · `57-K3-ENGINE-SPEC v2.1 §audit` (س178–196) · `87-PARITY` v1.1 |
| **الحصيلة** | **114 حالة · صفر تباعد** · بصمة شاملة واحدة للطرفين |
| **الإصدار** | **v1.2** — `DEC-201` في المصدر · `DEC-203` صيغة `G3` المركَّبة |

---

# القسم 1: نطاق النقل

| المُنقَل | المؤجَّل للمرحلة ③ |
| :--- | :--- |
| $K_2$: القياس · الترميز · التصنيف · التركيب (`ت-7`) · `ت-8` · `FRIC-02` · تدقيق العزل | حزم النصّ (`k2_content` · `COMPOSE-*` · `k2_userlayer_pack`) |
| $K_3$: القياس · الترميز · التثليث · الأقطاب · **28 خلية** · الأنماط · البوابات `G1…G4` | التركيب النصّي (`CONNECTIVES` · `TEMPLATES` · `ROOT_QUESTIONS`) · `G5` |

**استثناء موثَّق:** عبارات `ت-8` و`FRIC-02` مولَّدة داخل محرك $K_2$ لا من حزمة محتوى. نُقلت **حرفياً** — التكافؤ يقتضي تطابق المخرج، وتعديلها تأليف محظور.

---

# القسم 2: أداة التكافؤ — تنفيذ `DEC-199`

**التقنين** (`87-PARITY §4`) مُنفَّذ في الطرفين بدالتين متطابقتين المنطق:

| القاعدة | التنفيذ |
| :--- | :--- |
| مفاتيح مرتَّبة | فرز بنقطة الترميز حصراً — **صفر `localeCompare`** |
| عدد بمنزلة ثابتة | `f"{round(v,1):.1f}"` مقابل `(Math.round(v*10)/10).toFixed(1)` |
| البصمة | `SHA-256` · تطابق تامّ بلا هامش |

**مجموعة الانحدار — 114 حالة:**

| المجموعة | العدد | المحتوى |
| :--- | :---: | :--- |
| $K_2$ | 45 | 4 حقيقية · 8 مراكز تركيبية · 32 حدّية · تعادل تامّ |
| $K_3$ | 30 | 6 حقيقية · 20 حدّية · `ALL-HIGH` · `ALL-LOW` · `WITH-OUT` · تعادل |
| سلوك الفشل | 5 | `y+z ≠ 7` · `y+z ≠ 11` · بُعد ناقص + الحالات الموجبة |
| الحدود | 34 | `−0.1` … `100.1` عبر `octal_code` · `comp_state` · `band` · `pole` |

---

# القسم 3: 🔴 `DEF-K3-02` — `gates_fired` يخالف السند في الطرفين

## 3.1 الرصد

أول تشغيل أخرج **30 تباعداً** في $K_3$، كلها في الحقل نفسه:

| الطرف | المخرج |
| :--- | :--- |
| **بايثون** | `"G1:low_trust=False"` · `"G2:high=['IR', 'BI', 'CF', 'ST']"` |
| **JS** | `"G1:low_trust=false"` · `"G2:high=IR,BI,CF,ST"` |
| **السند** (`57 §audit` س189) | `"G1:low_trust=false"` · `"G2:high=[IR,ST]"` |

## 3.2 الحكم — الدرجة الوسطى من سلّم `DEC-200`

> **كلاهما يخالف السند ⇒ يُصحَّحان معاً. التطابق ليس المعيار، الموافقة هي المعيار.**

| الطرف | المخالفة |
| :--- | :--- |
| بايثون | `False` بحرف كبير · اقتباسات وفراغات داخل القائمة |
| JS | القوسان المعقوفان غائبان |

## 3.3 السبب الجذري

`gates_fired` **يُدمج بيانات مبنينة داخل سلسلة** عبر تنسيق لغة الاستضافة:

```python
f"G1:low_trust={low_trust}"      # يطبع repr البوليان في بايثون
f"G2:high={high}"                # يطبع repr القائمة في بايثون
```

⇒ الحقل **غير قابل للنقل بحكم بنائه**. أي بيئة ثانية تُنتج تنسيقاً مختلفاً، ولا واحدة منها تلتزم السند إلا بالمصادفة.

## 3.4 ⭐ التحقق العملي من `DEC-200`

هذا العيب **يبرهن القاعدة التي ختمتَها قبل جولة واحدة**:

> لو حكمت قاعدة «بايثون هو المرجع بحكم الأسبقية»، لَوَرِثَ JS `low_trust=False` — و**السند يكتبها `false` صراحةً**. ولأصبح التطابق مع الخطأ شهادة سلامة.

`DEC-200` منعت ذلك، فصار الحكم للوثيقة لا للتنفيذ الأسبق.

## 3.5 الإصلاح — منفَّذ في المصدر (`DEC-201` ✅)

أُدرجت `_gates_fired()` في `k3_engine.py`، ونظيرها حرفاً بحرف في `engines.js`. الحقل يُبنى الآن من **بيانات مبنينة** لا من تنسيق لغة الاستضافة:

```python
"G1:low_trust=" + ("true" if low_trust else "false")
"G2:high=[" + ",".join(high) + "]"
```

**ثم رُفع التطبيع من أداة التكافؤ بالكامل** — فما يُقارَن الآن هو مخرج المحرّكين خامّاً، لا مخرجاً مُصلَحاً في الأداة.

| التحقق | النتيجة |
| :--- | :---: |
| التكافؤ بعد رفع التطبيع | ✅ **114 حالة · صفر تباعد** · البصمة `ef7b161006c30529` |
| `test_golden_k3` (الحساب) | ✅ لا انحدار |
| `test_report_k3` (السحب) | ✅ لا انحدار |
| حارس $K_3$ (`ص-1…ص-6`) | ✅ صفر شذوذ |

## 3.6 `G3` — محسومة بـ`DEC-203`

**السؤال كان:** هل `"G3:asked"` في `57 §11` س189 تنسيق ملزِم أم قيمة توضيحية؟

**التشخيص:** حقول الكتلة ثلاثة أصناف لا صنفان:

| الصنف | العلامة | أمثلة | الحكم |
| :--- | :--- | :--- | :--- |
| ① قائمة خيارات | فاصل `\|` | `"full\|partial"` · `"strong\|weak\|composite"` | تعداد ملزِم |
| ② عيّنة | قيمة مفردة بلا فاصل | `"CPL-IR.BI-WS"` · `[IR,ST]` | التنسيق ملزِم · المضمون حرّ |
| ③ ثابت حرفي | لا يتغيّر بتغيّر الملف | `instrument_pin` | ملزِم كما هو |

`G1`/`G2` من ② ⇒ الحكم فيهما قاطع. أما `G3:asked` **فلا تحمل علامة أيّ صنف**، والسند يحمل قرينة لكل قراءة ولا يحسم:

- **يرجّح «الحالة»:** `gates_fired` سجل **إطلاق**؛ و`RQ` مثبَت أصلاً في `root_question.code` ⇒ تكراره ازدواج.
- **يرجّح «العيّنة»:** `G2` في الحقل نفسه يحمل مضموناً لا حالة؛ والسطر التالي `"conditional_layers": ["G3:via-67"]` يستعمل `G3:` **بلاحقة مضمون** — فللنمط سابقة داخل السند.

**القرار (`DEC-203`) — الصيغة المركَّبة:**

| الحالة | المخرج |
| :--- | :--- |
| سؤال مُطلَق | `G3:asked:RQ-01` |
| بلا سؤال | `G3:none` |

**لماذا هي الأسلم:** تصدُق على القراءتين معاً — تحمل **الحالة** فتُقرأ آلياً بلا تحليل نصّي، وتحمل **الكود** فلا تُفقَد الإحالة. ولا تُلغي أيّاً من القرينتين.

> **مشتق لا معتمد:** صيغة فرع «بلا سؤال» (`G3:none`) لم تُنصّ في القرار. أبقيتُها كما كانت — وهي توافق نمط `G4:suppressed` في حالة العدم. **قابلة للتصويب.**

---

# القسم 4: ملاحظة على المقارن — `ن-7/②` أثبت نفسه

التشغيل الأول أخرج أيضاً **٥ تباعدات حدّية كاذبة**: `None` مقابل `null`.

مصدرها **المقارن لا المحرّكان** — كنتُ أدمج قيم الحدود في سلسلة بتنسيق كل لغة، فقِست شيئين معاً: دلالة المحرك **و** تنسيق السلسلة. صُحِّح إلى مقارنة قيم مبنينة، فسقطت الخمسة وبقي العيب الحقيقي.

> نفس السبب الجذري في الموضعين — **دمج بيانات في سلسلة بتنسيق اللغة**. مرة في مقارني، ومرة في المحرك. والفارق أن الأول كان يُخفي، والثاني كان يُنتَج.

---

# القسم 5: النتيجة

```
المجموعة    الحالات   الحالة
─────────────────────────────────────────
k2          45        ✅ متطابق
k3          30        ✅ متطابق
failure      5        ✅ متطابق
boundary    34        ✅ متطابق
─────────────────────────────────────────
✅ تكافؤ تامّ — 114 حالة · صفر تباعد
   البصمة الشاملة: ef7b161006c30529 (الطرفان)
```

**وبلا انحدار:** `test_golden_k2` مرّ بعد النقل — طبقة الحساب البايثونية لم تُمسّ.

---

# القسم 6: المخرجات

| الملف | الوصف |
| :--- | :--- |
| `engines.js` | منطق $K_2$ و$K_3$ — صفر نصّ محتوى |
| `k3_engine_FIXED_DEC-201.py` | المحرك البايثوني بعد إصلاح `gates_fired` |
| `parity_py.py` | الطرف البايثوني + المقارن + مولّد تقرير التباعد |
| `parity_js.js` | الطرف JS |
| `parity_cases.json` | مجموعة الانحدار — 114 حالة |

---

# القسم 7: المطلوب

| # | البند | الكود |
| :--- | :--- | :--- |
| 1 | ~~إصلاح `DEF-K3-02` في المصدر~~ | `DEC-201` ✅ |
| 2 | حسم النقطة المعلَّقة في `87-PARITY §4-ب`: عند صمت السند — تجميد المنتج كله أم النقطة وحدها؟ | `DEC-202` |
| 3 | ~~`G3` — الصيغة المركَّبة~~ | `DEC-203` ✅ |
| 4 | توثيق أن `57 §11` **مثال لا عقد** + تثبيت تصنيف الحقول الثلاثة (§3.6) | `DEC-204` |

**الترقيم التالي:** `DEC-205`.

**المرحلة ② (تضمين حزم المحتوى) جاهزة للبدء** — لا يحجبها أيّ من البندين.

---

> **[نهاية الوثيقة — `88-PORT-01` v1.2 · النقل منفَّذ · التكافؤ تامّ على 114 حالة · `DEF-K3-02` مرصود]**
