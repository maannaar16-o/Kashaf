# 00-PATCH_Blueprint_Archival.md
## رقعة `DEC-130` — أرشفة `57-K3-ENGINE_Blueprint` ونقل ما يصمد منه

| الحقل | البيان |
| :--- | :--- |
| **السند** | **`DEC-130`** · `SPEC` v2.0 (`DEC-129`) · `DEC-091/092/093` · قاعدة **مصدر واحد للحقيقة** |
| **النوع** | أرشفة + نقل — **لا حذف** |
| **الحالة** | ✅ معتمدة (`DEC-130`) |

---

# ① أرشفة `57-K3-ENGINE_Blueprint`

**التدخل:** يُستبدل رأس الحوكمة (§0) بالكتلة التالية، ويبقى بقية النص **كما هو** بوصفه سجلاً تاريخياً.

> # القسم 0: رأس الحوكمة
>
> | الحقل | البيان |
> | :--- | :--- |
> | **كود الوثيقة** | `57-K3-ENGINE_Blueprint` — التصور الشامل (**سابق للمواصفة**) |
> | **الإصدار** | v0.1 |
> | **🗄️ الحالة** | **مؤرشفة — مستوعَبة بالكامل في `57-K3-ENGINE-SPEC` v2.0** (`DEC-130`) |
> | **القيد الحاكم** | 🔴 **لا تُحدَّث · ولا يُستشهد بها تشغيلياً · ولا تُسحب منها صياغة.** المرجع التشغيلي الوحيد: **`SPEC` v2.0**؛ ومرجع قواعد السحب: **`M3-06` v1.0** |
> | **ما نُقل منها** | §8 مصفوفة الالتزام → **`SPEC` v2.0 §14** (محدَّثة) · §7 واجهة دمج `56-REPORT-ENGINE` → **`SPEC` v2.0 §14.1** و`02-MASTER` |
> | **قيمتها الباقية** | **سجل نشأة**: تُظهر كيف صُمّم المحرك بمقابس فارغة محجوزة قبل أن يوجد ما يملؤها |
>
> **⚠️ تنبيه للقارئ — مواضع سقط مرجعها في هذه الوثيقة:**
> · §1.2 «ما لا يفعله»: **إسناد الجذر** و**حكم الاحتواء** و**البصمة** لم تعد محظورة — انظر `DEC-115`/`DEC-117`/`DEC-121`.
> · §2 «`G3`/`G4` غائبتان بنيوياً» — أُضيفتا في `SPEC` v2.0 §6 **دون هدم**، كما تنبّأت الوثيقة نفسها.
> · §3 بنية التقرير (8 أقسام) — صارت **9** وحُذف ختم الشفافية من الواجهة (`DEC-092`).
> · §7 «قسم `URS` محجوز حتى تكتمل المعايرة» — **أُتيح بلا معايرة**: الحاجب كان بنيوياً لا قياسياً، وسقط بـ`DEC-113`/`DEC-114`.
> · §9 نقاط القرار الست — **محسومة** (`DEC-091/092/093`)؛ و`ق5` حُسم **بخلاف توصية الوثيقة**: المعتمد بروتوكول `41 §7` لا استبعاد المهارة الناقصة.

---

# ② نقل الحجز المستقبلي إلى `02-MASTER-Tracking_And_Risks`

**التدخل:** إضافة قيد في سجل المتابعة.

> | البند | البيان |
> | :--- | :--- |
> | **`FUT-01` — دمج `56-REPORT-ENGINE`** | محجوز بـ`DEC-062`. عقد مخرج `SPEC` v2.0 §11 مصمَّم **وحدةً قابلة للتضمين**، و`audit` يُعلم المحرك الأمّ بالطبقات المشروطة والديون المقبولة. **لا يُبنى الآن.** |

---

# ③ ما لم يُنقل — وعلّته

| القسم | العلّة |
| :--- | :--- |
| §1–§6 | مستوعَبة في `SPEC` v2.0 بتفصيل أكبر وباعتمادات · نقلها يُنشئ **مصدراً ثانياً** |
| §9 | محسومة، وإحدى توصياتها لم تُعتمد |
| §7 (ثلاث حجوزات) | **امتلأت**: `L4.5` ← `G3`/`G4` · قسم `URS` ← القسم ٦ · الإصدار الأحدث ← **المسار (ب)** |

---

# ختم الرقعة

| البند | الحالة |
| :--- | :--- |
| `Blueprint` | 🗄️ **مؤرشف** — لا يُحدَّث ولا يُستشهد به تشغيلياً |
| مصفوفة الالتزام | ✅ منقولة محدَّثة → `SPEC` v2.0 §14 |
| واجهة `56-REPORT-ENGINE` | ✅ منقولة → `SPEC §14.1` + `02-MASTER` كـ`FUT-01` |
| أثر على القياس أو الأحكام | ⚪ **صفر** |
| مصادر الحقيقة التشغيلية | **اثنان لا ثلاثة:** `SPEC` v2.0 (تشغيل وصياغة) · `M3-06` v1.0 (قواعد السحب) |
