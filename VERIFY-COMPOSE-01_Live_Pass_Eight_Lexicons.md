# VERIFY-COMPOSE-01_Live_Pass_Eight_Lexicons.md
## تمريرة تحقّق حية — المعاجم الثمانية (طبقة التركيب الأعلى $K_2$)

| الحقل | البيان |
| :--- | :--- |
| **النوع** | تحقّق حي مُصطنع — **[مقترح · خارج النموذج · قابل للتنفيذ]** (لا يمسّ النظرية) |
| **الغرض** | كشف احتكاكات التركيب مبكراً قبل مزامنة المسترات — نظير تشغيل `E` (`FRIC-01…03` · 14/21) |
| **النطاق** | المعاجم الثمانية `COMPOSE-{E,H,C,S,St,R,A,O}` |
| **الطريقة** | ملف مُصطنع واحد → استدعاء تركيب فعلي على مركزه + اختبار تدوير على الثمانية |

---

# 1. الملف المُصطنع `SYN-COMPOSE-01`

> متجه `SP` مُصطنع (مرجع النطاقات `41-MEASURE` · عتبة الاشتعال `SP > 50%` — `DEC-033`). مصمَّم لإشعال الحالات الثلاث (`D`/`M`/`L`) دفعةً.

| العدسة | `SP` | الحالة | العدسة | `SP` | الحالة |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `A` | **82** | مركز/`D` | `C` | 47 | `L` |
| `St` | 73 | `D` | `H` | 43 | `L` |
| `O` | 62 | `M` | `S` | 36 | `L` |
| `R` | 56 | `M` | `E` | 28 | `L` |

**المركز = `A`** (أعلى `SP`) — اختيار مقصود: `A` مركز **ثلاثي التغطية** (`E·O·R`)، فيُختبر أعقد معجم.

---

# 2. الاستدعاء الفعلي — معجم المركز `A` (21 → المستدعى 7)

> القاعدة: يُستدعى لكل عدسة مرافقة **سطرٌ واحد** بحالتها في الملف (`DEC-157`).

| السطر | حالة العدسة | القراءة الناتجة | الطبقة (`ت-7`) |
| :--- | :--- | :--- | :--- |
| `A-St-D` | `St`=73 `D` | حفر مجهري يختبر جسور الرؤية — تآزر | مراجعة |
| `A-O-M` | `O`=62 `M` | إغلاق انتقائي — الشلل مُغطّى جزئياً | مراجعة |
| `A-R-M` | `R`=56 `M` | حسم فوري انتقائي — الشلل مُغطّى جزئياً | مراجعة |
| `A-E-L` | `E`=28 `L` | الإشارة الإنسانية خارج الخريطة — غطاء بيني | **تسليم** |
| `A-C-L` | `C`=47 `L` | نسف معيار بلا بروتوكول انتقال | **تسليم** |
| `A-S-L` | `S`=36 `L` | موضوعية بلا تغليف | **تسليم** |
| `A-H-L` | `H`=43 `L` | تقاربي يُغلق الاحتمالات باكراً | **تسليم** |

**أسئلة التسليم (`ت-7`): 4** (حالات `L` وحدها) · **للمراجعة: 3**. ✅ مطابق للقاعدة.

**فحص منطق التغطية:** عمى `A` المزدوج → (ب) إغفال الإشارة: `E`=`L` → **مكشوف · غطاء بيني** (متسق). (أ) الشلل: `O`=`M` + `R`=`M` → **مُغطّى جزئياً من الجهتين** (متسق). ✅
**فحص الأقفال:** `A-E-L` مؤطّر بنيوياً (لا «برودة») · `A-S-L` (لا «تدمير علاقات») · `A-H-L` (لا «قتل إبداع») · `K_2\text{-E}≠K_3` على `A-E`. ✅ الأقفال أطلقت صحيحاً.

---

# 3. اختبار التدوير — الثمانية على المتجه نفسه

> ترقية كل عدسة مركزاً افتراضياً وفحص اتساق استدعائها + الأقفال + التعارض العابر للمعاجم.

| المركز الافتراضي | عيّنة الاستدعاء | الاتساق |
| :--- | :--- | :--- |
| `O` (`H·St·R`) | `O-H-L` (كسر مكشوف) · `O-St-D` (تقادم مُغطّى) · `O-R-M` (ارتجال جزئي) · `O-A-D` | ✅ متسق |
| `St` (`R·O`) | `St-R-M` · `St-O-M` · `St-A-D` · `St-H-L` (قفل `St≠H`) | ✅ |
| `R` (`A·St`) | `R-A-D` (جذر مُغطّى) · `R-St-D` (مآل مُغطّى) · `R-H-L` · `R-E-L` | ✅ |
| `H`·`E`·`C`·`S` | (استدعاء نمطي — بلا تعارض عابر) | ✅ |

**النتيجة:** لا تعارض صريح عابر للمعاجم (متوقَّع — مركز واحد لكل ملف). الأقفال المميِّزة (`St≠H`, `A≠E`, `O≠St`, `R≠K_4`) أطلقت في مواضعها.

---

# 4. سجل الاحتكاكات — 3 مرصودة

## `FRIC-COMPOSE-01` — تداخل وصف حالة `M` مع كتلة الشدة `§3.2` · خطورة **متوسطة-منخفضة**
- **الرصد:** وصف حضور أسطر `M` («… متاح انتقائياً») يقارب حرفياً لغة `§3.2` («متاح ويُستدعى انتقائياً»). عند التسليم قد يتكرر المضمون (مخالفة `ت-6`).
- **الأثر:** ازدواج طفيف في التقرير، لا تناقض.
- **الإصلاح [مقترح]:** تقليم أسطر `M` لتحمل **التلوين الخاص بالعدسة المرافقة فقط**، ونزع العبارة النمطية العامة. يسري على الثمانية.
- **التصنيف:** نظافة معجمية — لا يمسّ النظرية.

## `FRIC-COMPOSE-02` — قراءة التغطية الموزّعة الجزئية بلا خيط تجميعي · خطورة **منخفضة**
- **الرصد:** حين تكون كل حلفاء مركز ثلاثي/ثنائي في حالة `M` (كـ`A-O-M`+`A-R-M` هنا)، يستلم القارئ سطرين/ثلاثة تغطية جزئية دون تجميع (`DEC-163` يمنع السطر التجميعي).
- **الأثر:** عبء تركيب على القارئ، لا خطأ.
- **الإصلاح [مقترح — طبقة التجميع]:** خيط **غير تفسيري** يسرد بلا ترجيح («تغطية الشلل موزّعة جزئياً عبر `O` و`R`») — يحترم `DEC-163` (سرد لا وزن).
- **التصنيف:** تجميع تقرير — لا يمسّ النظرية.

## `FRIC-COMPOSE-03` — نص العمى المحايد يناقض سطر التغطية `D` · خطورة **متوسطة** 🔴
- **الرصد:** نص العمى المحايد يصف الانكشاف **تقريرياً بلا شرط** (مثال `A`: «الإشارة تغيب عن الخريطة أصلاً»)؛ بينما سطر `A-E-D` يقول «الإشارة تدخل الخريطة كبيانات». لملفٍ عدسته المُغطِّية في `D`، يجتمع النصان فيتناقضان.
- **الجذر:** التحييد نقل **العلاج** للمعجم، لكن نص العمى ظلّ يُثبِت **الانكشاف حاضراً** — فيصطدم بسطر «مُغطّى داخلياً». (هذا بالضبط صنف الاحتكاك الذي أنشأ `DEBT-K2-NEUTRAL-01`، حُلّ نصفه.)
- **الإصلاح [مقترح — تعديل محرك `56-REPORT-ENGINE`]:** **قاعدة تكييف الإخراج:** إذا استقر سطر التغطية على `D`، يُقدَّم نص العمى بصيغة «حدّ بنيوي **يغطّيه ملفك داخلياً عبر [الحليف]**» لا كانكشاف نشط؛ وإذا استقر على `L` يبقى الانكشاف كما هو؛ وعند `M` يُقدَّم مشروطاً.
- **التصنيف:** قاعدة تجميع محرك — **يحتاج اعتمادك** (تعديل `56-REPORT-ENGINE`).

---

# 5. الحكم

| البند | النتيجة |
| :--- | :--- |
| بنية المعاجم الثمانية | ✅ صامدة — لا احتكاك يستدعي تغييراً نظرياً |
| قواعد `DEC-157`/`ت-7`/`DEC-163` | ✅ أطلقت صحيحاً في الاستدعاء الحي |
| الأقفال المميِّزة | ✅ فعّالة |
| الاحتكاكات | **3** — كلها **طبقة تجميع/محرك أو نظافة معجمية · صفر [داخل النموذج] نظري** |
| المقارنة بتشغيل `E` | مماثل: الطبقة سليمة بنيوياً، والإصلاحات صقل لا هدم |

> **خلاصة:** الطبقة جاهزة للاعتماد التشغيلي بعد إدماج إصلاحَي `01`/`02` (نظافة/تجميع) واعتماد قاعدة `FRIC-03` في المحرك.

---
***[نهاية التمريرة — `SYN-COMPOSE-01` · مركز `A` · 3 احتكاكات · صفر تغيير نظري]***
