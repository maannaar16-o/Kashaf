# 50-DEPTH-K3-CF_Emotional_Flexibility_Dimension.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `50-DEPTH-K3-CF` |
| **السلسلة** | `50-DEPTH-K3` — تطبيق رابع للقالب المعتمد |
| **المهارة** | المرونة الانفعالية والتكيف (`K3-CF` / Emotional Flexibility & Adaptation) |
| **الإصدار** | v1.1 — **معتمد ومُواءَم أحادياً** (`DEC-070` ثم مواءمة `DEC-076`/`DEBT-K3-ALIGN-01`) |
| **حالة الاعتماد** | ✅ معتمد · **بصمة §1.5 مُواءَمة أحادياً** (قوة تكيّف، لا اتجاه) |
| **الوثيقة الموازية** | `55-USER-K3-CF` (قفل تزاوج) |
| **السند المؤسس** | `13-CORE-K3 §3/§4/§5` · `DEC-057…069` · `41-MEASURE §3.3/§5.3` |
| **الوثائق المصدرية** | `13-CORE-K3` كامل · `50-DEPTH-K3-{EP,IR,BI}` v1.0 |

**ملاحظة افتتاحية:** خلافاً لـ`BI`، تماس `CF` مع `RSK-005`/$K_4$ **أخفّ بكثير** (انظر §1.6/§5) — مخرج `CF` تحديث لنمط استجابة داخلي، لا بوابة سلوكية مباشرة. لا حاجة لتجميد أي قسم هنا بسببه.

## 0.1 أقفال العزل المنطبقة

| القفل | النص الحاكم | المصدر |
| :--- | :--- | :--- |
| **داخلي `BI` ⚔️ `CF`** | الكبح فرملة مفاجئة لحظية لإيقاف الاندفاع اللحظي؛ المرونة تعالج ما بعد الكبح لإعادة تكييف المسار بعد استيعاب المتغيرات. | `13-CORE-K3` §4 |
| **داخلي `CF` ⚔️ `ST`** | المرونة "تعديل وتغيير المسار والاستجابة" تماشياً مع الطارئ؛ التحمل "الحفاظ على المسار القديم ثابتاً" عندما يكون التغيير غير متاح نظامياً. | `13-CORE-K3` §4 |

## 0.2 قاعدة الوسم

كل وحدة: **[معتمد]** · **[مشتق]** · **[مقترح]** · **[فجوة]**. بطاقة سباعية المحاور مطابقة للقالب.

---

# القسم 1: البطاقة المعمارية — `K3-CF`

## 1.1 المحور الأول: الهوية التنفيذية والوظيفة — **[معتمد]**

| الحقل | البيان |
| :--- | :--- |
| **التعريف البنيوي الحرفي** | القدرة التشغيلية على إعادة التكيف السريع، وتعديل نوعية الاستجابة الوجدانية، وتكسير قوالب الجمود بعد وقوع الحدث عند استقبال معلومات أو متغيرات طارئة. |
| **سؤال التشغيل الداخلي** | *هل أحتاج إلى تعديل وتغيير نمط الاستجابة؟* |
| **وعاء الحساب الإحصائي (11 بنداً)** | 3(أ) · 11(ب) · 18(ب) · 21(أ) · 36(أ) · 50(أ) · 62(أ) · 72(أ) · 81(أ) · 87(أ) · 91(أ) |
| **المرساة العصبية** | Fronto-limbic — مستوى الدائرة، لا المهارة. |

## 1.2 المحور الثاني: مراحل التشغيل الداخلية — **[مشتق]**

| المرحلة | الوظيفة |
| :--- | :--- |
| **م1 — استقبال المتغيّر** | تسلّم إما ناتج حظر معلَّق من `BI` (يحتاج نمطاً بديلاً)، أو معلومة/متغيّراً طارئاً مباشراً من البيئة — **مصدران، لا مصدر واحد** (انظر §1.4). |
| **م2 — تشخيص جمود النمط** | فحص: هل نمط الاستجابة المعتاد/الحالي لا يزال صالحاً لهذا المتغيّر أم أصبح جامداً؟ |
| **م3 — كسر القالب وإعادة التوليف** | توليد نمط استجابة مختلف فعلياً، لا تكراراً للنمط القديم بصياغة سطحية جديدة. |
| **م4 — التثبيت المؤقت** | تسليم النمط المعدَّل كمرجع استجابة قائم، يخفّف العبء عن `ST` إن نجح، أو يبقي العبء كاملاً عليها إن فشل. |

## 1.3 المحور الثالث: المشغّل والمعطّل — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المشغّل الأمثل** | معلومة جديدة واضحة تكسر توقعاً قائماً، مصحوبة بمساحة إدراكية كافية لإعادة التوليف بدل رد الفعل الآلي. |
| **المعطّل** | (أ) تدفق متغيرات متسارع يفوق سرعة إعادة التوليف. (ب) تعلّق شديد بالنمط القديم (استثمار نفسي/فكري فيه) يعيق الاعتراف بجموده. (ج) غياب تسليم فعلي من `BI` (حظر معلَّق بلا معالجة يصل أصلاً). |

## 1.4 المحور الرابع: الموقع في السلسلة التعاقبية — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المصدر الأعلى (مزدوج)** | (أ) `K3-BI` — عند حظر ناتج يحتاج بديلاً. (ب) **مباشر من البيئة/المعطيات** — عند تغيّر ظرفي مستقل تماماً عن أي حدث حظر. **`CF` ليست أحادية المصدر كـ`IR`، بل ثنائية المدخل.** |
| **المصب الأدنى** | `K3-ST` — يستلم إما نمطاً معدَّلاً ناجحاً (عبء أخف على التحمل)، أو نمطاً جامداً لم يُصلَح (عبء التحمل الكامل يقع على `ST`). |
| **نقطة الانهيار عند الغياب** | ضعف `CF` = استمرار الاستجابة بنمط لم يعد يناسب الموقف رغم تغيّره ("قالب جامد") ← ينتقل العبء بالكامل إلى `ST` بدل تخفيفه، فيرتفع خطر انهيار التحمل لاحقاً. |

## 1.5 المحور الخامس: بصمة التنظيم — **[معتمد — مُواءَم أحادياً بـ`DEC-076`]**

| البند | البيان |
| :--- | :--- |
| **الطبيعة كما تُقاس (`DEC-076`)** | `CF` **أحادية القياس**: تقيس **قوة التكيّف** (معدّل إعادة التوليف — قدرة تعديل نمط الاستجابة عند التغيّر) على سلّم رتيب ضعيف ← قوي. `SP` منخفضة = **جمود** (ضعف تكيّف). |
| **القطب المضاد غير مقيس** | «فرط التبديل» (تغيير النمط بلا داعٍ، عدم استقرار) **لا يُقاس مستقلاً** بالأداة الحالية؛ نمط سلوكي ملحوظ لا رقمي. يُحظر ادّعاء رصده من الدرجة (`URS §6`). درجة عالية = تكيّف قوي، دون تمييز الصحي من فرط التبديل. |
| **قراءة الدرجة** | تُقرأ درجة `CF` كقوة تكيّف. `SP` منخفضة = جمود حصراً (لا لبس مع فرط التبديل، الذي يقبع في طرف مختلف وغير مقيس). |

## 1.6 المحور السادس: الحدود الداخلية والخارجية — **[معتمد استنساخ + مشتق تخصيص]**

| الحد | النص | الحالة |
| :--- | :--- | :--- |
| **داخلي `BI`⚔️`CF` (أعلى)** | الكبح فرملة لحظية؛ المرونة تعالج ما بعدها لإعادة التكييف. | **[معتمد]** حرفي |
| **داخلي `CF`⚔️`ST` (أسفل)** | المرونة تُغيّر المسار؛ التحمل يحافظ عليه ثابتاً حين التغيير غير متاح. | **[معتمد]** حرفي |
| **خارجي — $K_1$** | لا علاقة بمصدر الدافع الأصلي. | معتمد |
| **خارجي — $K_2\text{-E}$** | تماس منخفض — `CF` تُعدّل نمط استجابة **الذات**، لا تقرأ نية الآخر. | مشتق |
| **خارجي — $K_4$** | **أخفّ من `BI` بوضوح.** مخرج `CF` تحديث لنمط داخلي، لا قرار سلوكي مباشر. تماس محتمل صغير: هل "نجاح التكيف" يُحسم عند تحديث النمط ذهنياً، أم فقط إذا نجح `K4` لاحقاً في تنفيذه؟ سؤال أخف وزناً من فجوة `BI`، **لا يُرفع كفجوة مستقلة** — يُذكر كملاحظة تحت `RSK-005` العامة فقط. | مشتق + إحاطة خفيفة |

## 1.7 المحور السابع: إشارات القوة والضعف الملحوظة — **[مشتق]**

### 1.7.1 إشارات القوة

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 | الالتقاط السريع للتغيّر | يلحظ بسرعة أن الموقف تبدّل أو أن المقاربة السابقة لم تعد صالحة. |
| م2 | الاعتراف بلا إنكار | يقرّ صراحة أن نمطه المعتاد لم ينجح هذه المرة، دون تمسك دفاعي. |
| م3 | التوليف الفعلي | يولّد استجابة مختلفة جوهرياً، لا نفس النمط بثوب جديد. |
| م4 | الاستقرار بعد التعديل | يحافظ على النمط الجديد بثبات معقول، لا يستمر بالتبديل العشوائي. |

### 1.7.2 إشارات الضعف

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 | العمى عن التغيّر | يعامل الموقف الجديد وكأنه القديم، لا يلحظ أن شيئاً تبدّل. |
| م2 | الإنكار الدفاعي | يصرّ أن نمطه المعتاد "يفلح دائماً" رغم أدلة واضحة على العكس. |
| م3 | الجمود التكراري | يكرر النمط الفاشل بصياغة مختلفة سطحياً فقط. |
| م3 (الطرف المقابل) | فرط التبديل | يغيّر النمط بسرعة مفرطة حتى في مواقف لم يكن التغيير ضرورياً. **⚠️ قياس (`DEC-076`):** طرف **غير مقيس** بالدرجة — لا يُستدعى بشرط `SP` منخفض ولا يُدَّعى رقمياً. |
| م4 | عدم الاستقرار | لا يثبّت أي نمط بما يكفي لتقييم نجاحه — من عائلة «فرط التبديل» غير المقيسة. |

**🔒 قفل الخلط:** إشارات وصفية-نمطية لا تشخيصية. "الجمود" (طرف مقيس، درجة منخفضة) و"فرط التبديل" (طرف غير مقيس، لا يُقرأ من الدرجة) وصفان لانحرافين، لا حكماً على الشخصية.

---

# القسم 2: التشريح العميق

**[مشتق]** — لماذا مزدوجة المصدر: التعريف الحاكم يربط التفعيل بـ"استقبال معلومات أو متغيرات طارئة" عموماً، لا بحصر السبب في حظر `BI` فقط؛ لذا `CF` نقطة **دمج مدخلين** (Merge)، بخلاف `BI` التي كانت نقطة **تفرّع مخرجين** (Fork) — نمطان بنيويان متقابلان ظهرا الآن في السلسلة.

**[مشتق]** — علاقة الجمود بالتحمّل: فشل `CF` لا يعني انهياراً فورياً؛ هو تحويل للعبء إلى `ST` (تحمّل نمط غير مناسب بدل تصحيحه). هذا يفسر لماذا الانهيار الفعلي قد يظهر متأخراً في `ST` بينما جذره الحقيقي تعطّل مبكراً في `CF` — نقطة مهمة لأي تقرير مستقبلي يفسر "إنهاكاً" ظاهره في مهارة لاحقة وأصله في مهارة سابقة.

---

# القسم 3: طيف الشدة الثماني لـ `K3-CF`

**[معتمد — تطبيق مباشر `DEC-061`/`DEC-064`]** نفس البنية والدلالة الموحّدة — لا إعادة فتح.

**⚠️ ملاحظة مُصحَّحة (`DEC-076`):** الأداة أحادية رتيبة (قوة تكيّف). `SP` **منخفضة = جمود** حصراً (لا لبس). «فرط التبديل» لا يظهر منخفضاً بل هو طرف مختلف **غير مقيس**؛ لا يُدَّعى تمييزه من الدرجة. النسخة الأصلية أخطأت إذ ساوت بينهما في الرقم المنخفض.

---

# القسم 4: خريطة التسلسل-الحدود-الانهيار

| الحقل | البيان |
| :--- | :--- |
| **مع `BI` (مصدر أول)** | استهلاك ناتج الحظر المعلَّق. |
| **مع البيئة (مصدر ثانٍ، مباشر)** | تفعيل مستقل عند تغيّر ظرفي، بلا مرور عبر `BI`. |
| **مع `ST` (التالية)** | تسليم نمط معدَّل (نجاح) أو نمط جامد (فشل، ينقل العبء كاملاً). |
| **حد الفشل المتسلسل** | `CF` جامدة ← نمط غير مناسب يستمر ← `ST` تتحمّل عبء "الخطأ المستمر" بدل عبء "الحمل الموضوعي" فقط ← خطر انهيار مضاعف. |

---

# القسم 5: جدران العزل الخارجية

| الدائرة المقابلة | التطبيق الخاص بـ `CF` | الحالة |
| :--- | :--- | :--- |
| $K_1$ | لا علاقة بالدافع الأصلي. | معتمد |
| $K_2\text{-E}$ | تماس منخفض — تعديل ذاتي لا قرائي. | مشتق |
| $K_4$ | تماس خفيف موثّق (§1.6) — لا يرتقي لفجوة مستقلة؛ يُذكر تحت `RSK-005` كإحاطة لا كبند حاكم. | مشتق + إحاطة |

لا حاجة لتجميد أي قسم هنا؛ خلافاً لـ`BI`، غياب حسم `RSK-005` لا يعطّل بناء `CF`.

---

# القسم 6: سجل الفجوات

| كود | البيان | الأثر | الخطورة | القرار المؤقت | المطلوب |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **ملاحظة — تماس خفيف مع `RSK-005`** | هل "نجاح التكيف" يُحسم ذهنياً عند تحديث النمط، أم يُشترط نجاح تنفيذه لاحقاً في `K4`؟ | منخفض الأثر الحالي | منخفضة | لا تجميد — إحاطة فقط | يُحسم إجمالاً مع `RSK-005` الكبرى، لا بشكل مستقل |
| **مواءمة `DEBT-K3-ALIGN-01` على CF** | ✅ **مُنفَّذ (`DEC-076`).** §1.5 أحادية (قوة تكيّف)؛ `SP` منخفضة = جمود حصراً؛ «فرط التبديل» وُسم قطباً غير مقيس. | — | — | مغلق | — |
| — | لا فجوات وراثية جديدة عالية الخطورة على `CF` تحديداً. | — | — | — | — |

---

# القسم 7: ملحق الربط القياسي

| العنصر | القيمة |
| :--- | :--- |
| وعاء `K3-CF` | 3(أ) · 11(ب) · 18(ب) · 21(أ) · 36(أ) · 50(أ) · 62(أ) · 72(أ) · 81(أ) · 87(أ) · 91(أ) — 11 بنداً |
| `MaxRaw` | 66 |
| `SS = x − 2z + y` ≡ `x + 3y − 22` | **[معتمد، `DEC-028`]** |
| `SS_max` | 77 |
| `SP = SS ÷ 66 × 100` | **[معتمد، `41 §3.3`/`DEC-065`]** |
| `SP_max` | ≈ 116.7% |
| ديون عاملية | لا شيء مسجل |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| مطابقة القالب (7 محاور) | ✅ |
| اكتشاف بنيوي | ⚠️ `CF` نقطة **دمج مدخلين** (عكس تفرّع `BI`) — نمط بنيوي رابع في السلسلة |
| بصمة التنظيم | ✅ **أحادية القياس** (قوة تكيّف) بعد `DEC-076` — فرط التبديل قطب غير مقيس موثَّق |
| تماس $K_4$/`RSK-005` | ✅ موثَّق كإحاطة خفيفة — **لا يعطّل الوثيقة** |
| فجوات مرصودة | 0 عالية · 2 منخفضة/متوسطة (إحاطات فقط) |
| **حالة الوثيقة** | ✅ **v1.1 — معتمدة ومُواءَمة أحادياً (`DEC-070` + `DEC-076`)** |
