# 136-K4-ENGINE — `DEC-266`: القرار التمكيني وعقد التقرير والمحرك التوأم لدائرة الإنجاز

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفذ المرحلة ٨**» — 2026-08-12 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-12 |
| **الأثر على المحرّكات والحزم والأداة** | **مستجدّ محصور**: محرك وحزمة وتقرير **جديدة لـ$K_4$ وحدها** · البصمات الثلاث القائمة **ثابتة حرفياً** · طبقة القياس لم تُمسّ (`instrument_pin`) · **ولا مسّ بمخرجات $K_2$/$K_3$** |
| **التغيير المرافق** | `CHG-083` |
| **الحاكم** | `DEC-199`/`DEC-200` (التكافؤ المقيس) · `DEC-186` (يُرفع بشرطه — §1) · `129…135` (البُنى المختومة) · `DEC-183` (حارسا المخرج) |
| **الكود المحجوز** | هذه الوثيقة تستوفي حجز `136` — المرحلة ٨ (`77-LOG §2`) |

---

# §1 القرار التمكيني — رفع `DEC-186` عن $K_4$ بشرطه

## ① ما كان

`DEC-186` نصّ أن $K_1$/$K_4$ **لوحة تشخيصية داخلية** موسومة `[خارج نطاق التقرير — لا محرك معتمد]`، لا تدخل أي تصدير. والعلّة في نصّه: **لا محرك معتمد**.

## ② ما تغيّر — الشرط استُوفي بالبناء لا بالقرار

| شرط الرفع | حاله |
| :--- | :--- |
| بُنى مختومة يُشتق منها التقرير | ✅ `129…135` — الشكل · الشبكة · العمق والنطاقات · القراءتان والأنماط · العبور · المميّز والقراءة الزائفة · طبقة المستخدم |
| محرك معتمد **بنسختين متكافئتين** | ✅ `k4_engine.py` + وحدة `K4` في `engines.js` — **تكافؤ مقيس** (§4) |
| حزمة محتوى **بصفر تأليف** | ✅ `k4_contentpack.json` — كل نص من مصدر مختوم، **والفحص آلي** (§5) |
| حارسا المخرج نافذان | ✅ `ح-4`/`ح-5` على كل إصدار (`sp_gate.output_gate`) |
| عزل ثلاثي مصان | ✅ فحص آلي: صفر رمز $K_2$/$K_3$ في محرك $K_4$ ومخرجه (§5) |

## ③ نص الرفع المختوم

> **يُرفع `DEC-186` عن $K_4$ حصراً**: صار لها **محرك معتمد** وتقرير بعقدٍ مختوم، فتخرج من «لوحة داخلية» إلى **تقرير معتمد**.
> **وثلاثة قيود تبقى نافذة بلا استثناء:**
> ① **$K_1$ لم تُمسّ** — `DEC-186` قائم عليها كما هو (لا محرك لها).
> ② **العزل الثلاثي**: $K_4$ **لا تقابل بنداً** من $K_2$ أو $K_3$ في أي شاشة أو تصدير — والتقابل البندي محظور أصلاً (`DEC-050`).
> ③ **تقارير الدوائر منفصلة** — والقراءة العابرة سطحها الخاص (`133 §3`)، لم يُبنَ بعد.

---

# §2 حدود المحرك — ما لا يفعله

| الحدّ | التنفيذ في الكود |
| :--- | :--- |
| **صفر عتبة مستحدثة** (`ن-7/④`) | حدود النطاقات منقولة كما هي (`TRF-010`) بحدود $K_3$ الدنيا (`<`) — لا رقم جديد في `k4_engine.py` |
| **صفر ترجيح مخترَع** | تعادل المحطة الأدنى **يُعرض بكامله** (`bottleneck().tie`) — لا كسر تعادل (`DEC-150` قفلاً · `R11` قياساً) |
| **المقارنة رتبية لا رقمية** | `BAND_RANK` على النطاقات — لا مقارنة بين قيم `SP` (وفاءً لـ`132 §2/②`) |
| **لا نمط فوق نقص** | `recognize_patterns` يعود فارغاً عند أي `OUT` (`132 §4/③`) |
| **المحايد لا يُستدعى** | `state()` يعيد `None` لـ`core` و`OUT` — فلا يُفعَّل قيد بحال محايدة |
| **لا استدلال درجة↔علاقة** | القيود تُفعَّل بشرطها البنيوي، ولا يُشتق من عددها حكمٌ على درجة ولا العكس |
| **لا خفض درجة** | التحفّظ **نصّي** حصراً (`134 §4/⑥`) — لا تعديل حسابي في أي موضع |
| **صفر تأليف** | كل نص من `k4_contentpack.json`؛ والنص الغائب **فجوة تُرفع** (`ContentGapError`) لا فراغ يُملأ |

---

# §3 عقد التقرير — الأقسام والقواعد

## ① الأقسام (مشتقة من البُنى لا مبتكرة)

| # | العنوان المختوم | مصدره البنيوي | شرطه |
| :--- | :--- | :--- | :--- |
| ⓪ | *(إخطار الفجوة — بلا عنوان)* | `132 §4/③` | عند أي `OUT` |
| ١ | **لوحة محطاتك** | بطاقات العمق (`131`) + السلّم | دائم |
| ٢ | **مواضع الانقطاع** | `132 §1` | عند وجود محطة في الحضور المحدود |
| ٣ | **المحطة الأدنى** | `132 §2` | عند وجود محطة مصنَّفة |
| ٤ | **كيف تعمل محطاتك معاً** | `130` (`K4-REL-01…12`) | عند تفعُّل قيد |
| ٥ | **تحفّظ القراءة** | `134 §3`/`§4` | عند وقوع `PER`/`OR`/`F` في القدرة العالية |
| ٦ | **أسئلة للفرز** | `134 §2` (`K4-LK-01…03`) | عند التباس المشهد |
| ٧ | **تدريبات** | `135 §3` (مسنودة بالبذور) | لمحطات الانقطاع حصراً |
| ٨ | **بصمة القراءة** | كتلة التدقيق | دائم |

## ② قواعد العرض النافذة

| الرمز | القاعدة |
| :--- | :--- |
| `ر-1` | ترقيم **المعروض فعلاً** — لا يقفز عند غياب قسم شرطي |
| `ر-2` | صفر رقم أو نسبة في أي عنوان أو متن — حارسا `ح-4`/`ح-5` على كل إصدار |
| `ر-3` | التحفّظ **كتلة واحدة موحَّدة** مهما تعدّدت الصمامات، **تُصدَّر بالتأكيد** أولاً |
| `ر-4` | المحطة الأدنى تُعرض **بكامل المتعادلين** — لا كسر تعادل |
| `ر-5` | عند `OUT` يُصدَّر **تقرير فجوة**: لا نمط ولا قراءة مسار فوق النقص |

## ③ النصوص العامة المختومة (مصدر حزمة `notice`)

هذه الجمل **تُختم هنا** ومنها تُنقل إلى الحزمة حرفياً — والفحص الآلي يطابقها بهذه الوثيقة:

- الترتيب هنا سند قراءة لا قياس: المحطات السبع لازمة معاً.
- بعض محطاتك خارج نطاق التصنيف — بياناتها متعارضة أو ناقصة. هذا تقرير فجوة تشغيلية: لا يُقرأ فوق النقص نمطٌ ولا قراءة مسار.
- أكثر من محطة في النطاق الأدنى — تُعرض جميعاً بلا ترجيح.
- وهذه قراءة موضعٍ على التتابع، لا قولٌ بأنها تخنق ما بعدها.
- تدريبات مسنودة إلى مقاصد التصميم المختومة — تعمل داخل ما هو مكتسب وقابل للتغيير، بلا وعد نتيجة.
- مشهدٌ يحتمل أكثر من قراءة — والفرز بالمحرّك لا بالأثر:

## ④ كتلة التدقيق — الحقول الملزمة

`sp` · `codes` · `bands` · `constraints_activated` · `constraint_map` · `patterns_recognized` · `interruption_points` · `bottleneck` · `choke_readings` · `lookalike_flags` · `reading_reserve` · `excluded_out` · `gap_report` · `engine_version` · `spec_version` · `instrument_pin` · `missing_content` · `accepted_debts` · `open_debts` · `sections_rendered` · `pack_sha` · `report_sha256`.

**تمييز لازم** (`00-HANDOVER §6①`): `accepted_debts` و`open_debts` **حقلان إعلانيان مصرَّح بعدم قياسهما** — مصدرهما سجل الحوكمة لا الكود، وهو الاستثناء المعلَن نفسه المقرَّر في $K_3$. وكل حقل سواهما **يقيس ما يعلنه**.

---

# §4 التكافؤ المقيس — البصمة الرابعة

| المقياس | النتيجة |
| :--- | :--- |
| أداة القياس | `parity_k4.py` ⇄ `parity_k4_js.js` — تقنين مطابق حرفياً لأداة $K_2$/$K_3$ (`87-PARITY §4`) |
| مجموعة الانحدار | `parity_cases_k4.json` — **127 حالة**: البذور العشر مترجمةً نطاقات · حدود النطاقات على كل محطة (`-0.1` … `116.7`) · تركيبات القيود والأنماط والتعادل و`OUT` · **و6 حالات عقد مدخل** (منها ثلاث لغير المنتهي — §5/②) |
| نطاق القياس | **المنطق والنص معاً** — كتلة التدقيق **ونصّ التقرير الكامل** يدخلان البصمة |
| النتيجة | ✅ **260 حالة · صفر تباعد** |
| **بصمة $K_4$** | **`8fa51aecbed95e69`** — بايثون = JS |

**قاعدة التجميد نافذة:** أي تباعد يجمّد الطرفين — لا أسبقية لطرف (`DEC-200`).

---

# §5 الفحوص المستجدة — تقيس ما تعلنه

## ① جدول الفحوص

| الفحص | ما يقيسه فعلاً |
| :--- | :--- |
| **صفر تأليف** | كل نص في الحزمة **موجود حرفياً** في مصدره المختوم: نصوص الصمامات والتحفّظ والفرز والتدريبات في `55-USER-K4-*`، والعناوين والإخطارات في هذه الوثيقة. أي نص بلا مصدر **يوقف الإصدار** |
| **العزل الثلاثي** | صفر رمز من $K_2$ (`A/R/O/C/S/E/St/H`) أو $K_3$ (`EP/IR/BI/CF/ST`) في محرك $K_4$ وحزمته ومخرجه |
| **اكتمال الحزمة** | مفاتيح كل صمام + التحفّظ للثلاثة المثبتة + الفرز الثلاثي + التدريبات — **وتدريبٌ حيث صُرِّح بالفراغ يُرفع مخالفةً** (`WM`) |
| **حارسا المخرج** | `ح-4`/`ح-5` يُمرَّران على كل تقرير في مجموعة الانحدار |
| **عقد المدخل** | وعاء ناقص · قيمة غير عددية · مدخل فارغ · **`NaN`/`±inf`** ⇒ `InputContractError` في الطرفين بالرسالة نفسها |


## ② 🔎 تباعدٌ رصده حارس `ح-6` — ويُثبَّت مقيساً

حارس الاستيفاء (`ح-6`) أوقف الإصدار على استيفاءات غير مسجَّلة في الوحدتين الجديدتين، فقاد فحصُها إلى **عيبٍ حقيقي لم تكشفه مجموعة الانحدار**:

> **بايثون كان يقبل `float("nan")` بلا استثناء** — ثم تسقط المقارنات الثلاث في `band()` جميعاً فيُصنَّف الوعاء **«قدرة عالية»** صامتاً؛ بينما JS يرفضه بعقد المدخل. **تصنيفٌ كاذب وتباعدٌ معاً.**

**الحكم:** الرفض صار صريحاً في الطرفين (`_validate` يردّ غير المنتهي)، **وأُضيفت ثلاث حالات** (`NaN` · `inf` · `-inf`) إلى مجموعة الانحدار — فالعيب **مقيسٌ لا مُصلَحٌ فحسب**. وسُجِّلت الاستيفاءات النصية الأحد عشر في `interp_registry.json` بأسبابها المكتوبة (أسماء صمامات · أوسمة نطاق · رموز عرض — لا عدد فيها).

**درسٌ يُقيَّد:** الحارس القائم أفاد الدائرة الجديدة قبل أن تُصدِر سطراً واحداً — وهو عائد بناء الحرّاس مبكراً.

---

# §6 ما لا تدّعيه هذه المرحلة

- ❌ لا تمسّ طبقة القياس ولا الأداة (`instrument_pin`) — ولا تعدّل بنداً ولا وزناً ولا عتبة.
- ❌ **لا ترفع `DEC-186` عن $K_1$** — الرفع لـ$K_4$ حصراً وبشرطه.
- ❌ لا تفتح تقابلاً بندياً ولا تخلط دائرة بأخرى في أي شاشة أو تصدير.
- ❌ لا تبني سطح القراءة العابرة ولا السطح المركَّب — كلاهما مقرَّر لا مبنيّ.
- ❌ لا تخفض درجة ولا تشخّص — التحفّظ نصّي وسؤال تأمّل.
- ❌ لا صدق ميداني (`DEC-246`) — كل الطبقة التفسيرية `[اشتقاق غير محقّق ميدانياً]`.

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| البصمات الثلاث القائمة | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **حرفياً** |
| **بصمة $K_4$ المستجدة** | **`8fa51aecbed95e69`** — الطرفان متطابقان |
| البوابة | **16 ← 19 أداة** (تُضاف `parity_k4.py` · `test_report_k4.py` · `k4_content.py`) — والحارسان `ح-4`/`ح-5`/`ح-6` نافذة على $K_4$ كما على سابقتيها |
| الملفات المستجدة | `k4_engine.py` · `k4_report.py` · `k4_content.py` · `k4_contentpack.json` · `parity_k4.py` · `parity_k4_js.js` · `parity_cases_k4.json` · `test_report_k4.py` — وامتداد `engines.js`/`reports.js` |

---

**[نهاية `136-K4-ENGINE` — `DEC-266` · الترقيم التالي `DEC-267` · `CHG-083` · بصمة $K_4$ `8fa51aecbed95e69`]**
