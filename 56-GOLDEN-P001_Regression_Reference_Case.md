# 56-GOLDEN-P001_Regression_Reference_Case.md

> **وضع التشغيل:** صياغة تشغيلية · **حالة الاعتماد:** [معتمد — مثبّت بقرار مالك المشروع] · **التصنيف:** [خارج النموذج — قابل للتنفيذ]
> **الوظيفة:** حالة مرجعية معيارية (Golden Case) لاختبار انحدار المحرك. أي تعديل مستقبلي في `M-06`/`56-REPORT-ENGINE` يُعاد تمريره على هذه البصمة؛ اختلاف المخرج دون سبب موثّق = **إنذار انحراف**.
> **قفل الثبات:** هذه الوثيقة **مجمّدة** — لا تُعدَّل. أي تحديث يُنشئ نسخة مؤرّخة جديدة (`P-001-v2`…). خصوصية البيانات مطوّقة بالترميز `P-001` وحوكمة الموافقة المؤكدة.

---

# القسم 1: البصمة المدخلية المثبّتة [معتمد من المصدر]

**المصدر:** حالة حقيقية مستخرجة من تقرير `41 v4.1` خارجي (مُصدَّر 2026-06-29).
**مستوى الإدخال:** SP النهائية لكل بُعد `K_2` — **لا `x/y/z`** (طبقة الحساب §2 متخطّاة؛ قيد تشغيلي مثبّت).

| البُعد | SP المثبّتة | الرمز `41` §4 |
| :--- | :--- | :---: |
| E المتفهم | 109.5% | H++ |
| O المنظم | 95.2% | H+ |
| C المحافظ | 71.4% | H |
| A التحليلي | 69.0% | M+ |
| S الاجتماعي | 69.0% | M+ |
| R الواقعي | 66.7% | M+ |
| H التصوري | 47.6% | M |
| St الاستراتيجي | 45.2% | M |

---

# القسم 2: المخرجات المتوقعة المثبّتة (Expected Outputs)
**[مشتق — نواتج تصنيف واستدعاء ثابتة تُقارَن عند كل اختبار انحدار]**

## 2.1 التصنيف (يجب أن يتطابق حرفياً)
| المؤشر | القيمة المرجعية |
| :--- | :--- |
| المشتعلة (SP>50) | E · O · C · A · S · R (ستة) |
| المنطفئة | H · St |
| المركز (الأعلى SP) | **E** |
| أعلى ثلاثة | E · O · C |
| المهيمنة (>70) | E · O · C |
| المساندة (50–70) | A · S · R |
| نقطة العمى (≤50) | H · St |

## 2.2 الاستدعاءات المتوقعة (قواعد `M-06`)
| القاعدة | الناتج المرجعي |
| :--- | :--- |
| R2 (المركز) | كتالوج `K2-E` كامل |
| R3 (المساندان المهيمنان) | O · C — المداخل الخمسة |
| R4 (أزواج مشتعلة) | هجائن E-O (`DY-16`) · E-C (`DY-20`) · O-C (`DY-14`) |
| R5 (قطبي) | **PT-01** (A⚔E — طرفاه مشتعلان) |
| R6 (فجوة تغطية) | E × St (أدنى فعّال؛ لا OUT في الحالة) |
| R7 (ارتداد المركز) | `FB-E` (E→A داخلي) |
| R8/R9 (بوابات) | LD (E≠K3) · PUR نظيف |
| R11 (عرض مزدوج St+H) | **لا يُفعَّل** (St وH منطفئان — دون عتبة الاشتعال) |

## 2.3 وسوم إلزامية متوقعة
`[Pilot]` · `[GAP-A-01]` · `[GAP-A-02]` · `RSK-016` (لتقرير St ضمن نقاط العمى).

## 2.4 البصمة التوصيفية المرجعية
محرّك **إنساني–هيكلي**: E(H++) فوق O(H+) فوق C(H)؛ هشاشة محدّدة في القطاع الاستشرافي–التجريدي (St·H منطفئان).

---

# القسم 3: بروتوكول اختبار الانحدار
**[مشتق — إجرائي]**

| الخطوة | الإجراء |
| :--- | :--- |
| 1 | عند أي تعديل في `M-06`/المحرك/`55-USER`/`51-MATRIX` |
| 2 | أعد تمرير بصمة §1 عبر المحرك المعدَّل |
| 3 | قارن الناتج بـ §2 (تطابق حرفي مطلوب) |
| 4 | أي اختلاف → **إنذار انحراف**: تقرير فجوة (§10) يحدد الموضع والسبب |
| 5 | اختلاف **مبرَّر بتعديل موثّق** (مثل ترقية نص كتلة) → يُوثّق كتحديث متوقع، لا انحراف |

## 3.1 نقاط تحقق حرجة (الأكثر حساسية للانحراف)
1. **المركز = E** (لا ينزاح باختلاف صياغة).
2. **PT-01 يُفعَّل · R11 لا يُفعَّل** (حساس لعتبة الاشتعال 50 وحد القطبية).
3. **استبعاد لا-OUT في R6** (الحالة بلا OUT — أدنى فعّال = St).
4. **صفر لمس K1/K3/K4** رغم توفّر بياناتها في المصدر.

---

# القسم 4: حدود الحالة المرجعية (نطاق التغطية)
**[فجوة سياقية موسومة — لا تعطّل الوظيفة]**

| الحد | البيان |
| :--- | :--- |
| مستوى الإدخال | SP فقط — لا تختبر طبقة الحساب (§2: `SS=x+3y−14`). تغطية الحساب تبقى على `56-TEST-02` (بلغ H++ بـ`x/y/z`) |
| النطاق | K2 حصراً؛ لا تغطي K1/K3/K4 (خارج محرّك v1) |
| الوضع | الفرد فقط؛ لا تغطي الفريق ولا القائد (GAP-ENG-01) |
| الاشتعال المزدوج St+H | **لا تغطيه** (منطفئان هنا) — تغطية R11 تبقى على `56-TEST-02` |

**توصية تغطية [مشتق]:** الحالتان المرجعيتان معاً تغطيان الطيف: `56-TEST-02` (الحساب + H++ + R11) · `P-001` (تصنيف حقيقي + PT + بصمة إنسانية-هيكلية). أي حالة مرجعية ثالثة مستقبلية يُفضّل أن تحمل بُعداً في OUT لتغطية مسار R6/الكتلة OUT بحالة حقيقية.

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| البصمة المدخلية | ✅ مثبّتة [معتمد من المصدر] |
| المخرجات المتوقعة | ✅ مثبّتة [مشتق] |
| بروتوكول الانحدار + نقاط التحقق الحرجة | ✅ |
| حدود التغطية | ✅ موسومة (تكامل مع `56-TEST-02`) |
| قفل الثبات (مجمّدة — لا تُعدَّل) | ✅ |
| **حالة الوثيقة** | ✅ **حالة مرجعية معيارية مثبّتة — مرجع اختبار الانحدار** |
