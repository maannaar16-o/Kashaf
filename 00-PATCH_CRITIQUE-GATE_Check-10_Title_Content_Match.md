# 00-PATCH_CRITIQUE-GATE — الفحص 10: مطابقة العنوان للمضمون

| الحقل | البيان |
| :--- | :--- |
| **الوثيقة الهدف** | `00-PROTOCOL_Self_Critique_Gate.md` — `00-PROTOCOL-CRITIQUE-GATE` v1.0 (`DEC-120`) |
| **نوع الرقعة** | إضافة فحص + شرط تفعيل ثانٍ + قاعدة تنفيذ — **v1.0 ← v1.1** |
| **حالة الاعتماد** | 🟡 **مقترح** — الجوهر معتمد بأمر المالك؛ **الموضع والترقيم مصوَّبان في §0** |
| **الكود المقترح** | `DEC-193` |
| **الأصل** | `DEF-K2-01` — استدعاء المداخل بالرقم بدل الوظيفة (`84-K2-DEFECT` v1.1) |

---

# القسم 0: تصويبان إلزاميان قبل الرقعة

## 0.1 🔴 تصادم ترقيم — `ن-5` **مشغولة**

الاسم المقترح في الجلسة (`ن-5`) **يصطدم بقاعدة معتمدة قائمة**:

> `ن-5` = «لمالك المشروع **إعفاء** أي وثيقة من البوابة بكلمة» — `00-PROTOCOL-CRITIQUE-GATE §3`

وعائلة `ن-1…ن-6` **مرجوعٌ إليها في سبع وثائق** (`01-MASTER` ×2 · `02-MASTER` · `00-PATCHPACK` · `00-SESSION-CLOSE_2026-07-21` · `79` · `82`). **إعادة الترقيم محظورة** — تكسر إحالات معتمدة.

## 0.2 ⚠️ خطأ تسمية في مخرجي أنا

في `84-K2-DEFECT` §5 وسمتُ صنف الفشل بـ«`ن-1` — تتبّع السند». **غير صحيح**: `ن-1` قاعدة تنفيذ («النقد يُعرض قبل طلب الاعتماد»)، و«تتبّع السند» هو **الفحص 1** من قائمة الفحص. الوثيقة مُصوَّبة إلى **v1.1**.

> البروتوكول يحوي عائلتين متمايزتين: **قائمة الفحص** (1…9 · §2 — *ماذا يُفحص*) و**قواعد التنفيذ** (`ن-1…ن-6` · §3 — *كيف يُدار النقد*). الخلط بينهما هو نفسه صنف `DEF-K2-01`: عنوان لا يحمل مضمونه.

**⇒ الموضع الصحيح: `الفحص 10` في §2 — لا `ن-5`.**

---

# القسم 1: الفجوة الحاسمة — البوابة **ما كانت لتُفعَّل أصلاً**

شرط التفعيل في §1 **ضيّق بقصد**:

> «تُفعَّل البوابة حين تُعاد بناء **وثيقة قائمة** كان أساسها **قد تغيّر** بقرار لاحق»

و`DEF-K2-01` **لم ينشأ من إعادة بناء وثيقة**. نشأ من **كود يُنتج مخرجاً متكرراً**. فلو كان الفحص 10 قائماً منذ `DEC-120`، **لما فُعِّل ولما التقط العيب**.

| الحقيقة المقيسة | الأثر |
| :--- | :--- |
| أربعة تقارير مُصدَرة قرأها بشر ومرّت | الفحص البشري بالقراءة **لا يلتقط هذا الصنف** — النص سليم، والخلل في الإسناد |
| العيب تكرّر آلياً في كل إصدار | خطر المخرج الآلي **تراكمي**، بخلاف الوثيقة التي تُراجَع مرة |

**الاستنتاج:** إضافة الفحص إلى قائمة بشرية **وحدها** تمنح طمأنينة زائفة. المخرج الآلي يحتاج **حارساً آلياً يعمل في كل تشغيل**.

---

# القسم 2: التصميم — طبقتان لا واحدة

| الطبقة | الأداة | تُفعَّل على | الحالة |
| :--- | :--- | :--- | :---: |
| **بشرية** | `الفحص 10` في قائمة الفحص | الوثائق (شرط §1 الأصلي) + **المخرج الآلي** (شرط جديد) | 🟡 هذه الرقعة |
| **آلية** | حارس تنفيذي (`validate_slots()` نظير) | كل تشغيل للمحرك — يوقف الإصدار عند الشذوذ | ✅ **مُنفَّذ في $K_2$** (`DEC-190`) · ⬜ يلزم نظيره في $K_3$ |

---

# القسم 3: نصوص الرقعة — جاهزة للّصق

## رقعة 1 — §2 · صفّ يُضاف إلى قائمة الفحص

```
| **10** | **مطابقة العنوان للمضمون** | هل يحمل كل عنوان/حقل/فتحة **مضمونه المقصود** — أم استُدعي المحتوى بموضع أو رقم فانزاح عن عنوانه؟ | `DEF-K2-01`: «بصمتك» عرضت «ما لا تعنيه هذه العدسة» في 6 من 8 مراكز |
```

**وتُعدَّل ترويسة القسم:**
```
# القسم 2: قائمة الفحص العشرة
```

**ويُضاف تحت سطر «الفحص 1 هو الأثقل»:**
```
**الفحص 10 هو الأخفى** — لا يظهر في القراءة بل في مطابقة المحتوى بعنوانه. مرّ `DEF-K2-01` على أربعة تقارير مقروءة لأن النص كان سليماً والإسناد خاطئاً.
```

## رقعة 2 — §1 · شرط تفعيل ثانٍ

```
> **شرط ثانٍ (`DEC-193`):** تُفعَّل البوابة كذلك على **كل مخرج آلي متكرر** (محرك تقارير · مولّد وثائق · أداة تفاعلية) عند **أول إصدار** وعند **كل تعديل يمسّ طبقة السحب أو الإسناد** — بقائمة مختصرة: الفحوص **1 · 4 · 10** حصراً.
```

**ويُضاف صفّ إلى جدول ✅/❌:**
```
| مخرج آلي متكرر عند أول إصدار أو تعديل طبقة السحب *(`DEF-K2-01`)* | تشغيل روتيني لمحرك لم يتغيّر |
```

## رقعة 3 — §3 · قاعدة تنفيذ جديدة

```
| **ن-7** | **ما يُفحص آلياً يُحرَس آلياً.** كل فحص قابل للترميز يُنفَّذ حارساً داخل المحرك **يوقف الإصدار** عند الشذوذ، ولا يعود بقيمة فارغة صامتة (بروتوكول صفر هلوسة · البند 10). **ويُشترط في الحارس:** ① **يُختبَر على عيب معلوم قبل اعتماده** — حارس لم يسقط على عيب واقع لا يُعتمد *(`DEC-194` — بانتظار الختم)*. ② **كل فحص يقيس شيئاً واحداً** — الفحص الذي يخلط عيبين متمايزين يُنتج إنذاراً كاذباً على أحدهما ويطمس الآخر *(`DEC-198` ✅)*. ③ **تكافؤ النسخ** — متى نُفِّذ المنطق نفسه في بيئتين (بايثون · JS)، **يُشترط تطابق مخرج النسختين على مجموعة الانحدار نفسها** قبل اعتماد أيّهما، ويُعاد الفحص عند كل تعديل في أيّ منهما *(`DEC-199` ✅)*. القائمة البشرية تكمّل الحارس ولا تُغني عنه |
```

## رقعة 4 — ختم الحالة

```
| قائمة الفحص | 10 بنود، كلها مشتقة من عيوب واقعة |
| قواعد التنفيذ | `ن-1 … ن-7` (و`ن-7` بثلاثة شروط: اختبار على عيب معلوم · فحص واحد لكل قياس · تكافؤ النسخ) |
| شرط التفعيل | إعادة بناء وثيقة فوق أساس متغيّر · **أو** مخرج آلي متكرر (فحوص 1·4·10) |
| **حالة الوثيقة** | ✅ **v1.1 — معتمدة (`DEC-120` + `DEC-193`)** |
```

---

# القسم 4: مزامنة الإحالات

| الوثيقة | الموضع | التعديل |
| :--- | :--- | :--- |
| `01-MASTER-Governance` | س48 · س698 | «قائمة فحص تسعة · `ن-1…ن-6`» → «**عشرة** · `ن-1…**ن-7**`» |
| `02-MASTER-Tracking` | س562 | تحديث عائلة `ن` |
| `00-PATCHPACK_2026-07-21` | س69 | تحديث الوصف |

---

# القسم 5: حدود الرقعة — ما لا تفعله

1. **لا تُطبَّق أثراً رجعياً** (§4/3 من البروتوكول الأصل) — `DEF-K2-01` يُعالَج بـ`DEC-190/191` لا بهذه الرقعة.
2. **لا تُوسِّع البوابة إلى كل وثيقة** — علّة التضييق في §1 قائمة؛ التوسعة محصورة في **المخرج الآلي** وبقائمة **ثلاثة فحوص** لا عشرة.
3. **لا تمسّ البنية ولا القياس ولا الدوائر** — إجرائية بحتة، كأصلها.
4. **لا تُغيّر ترقيم `ن-1…ن-6`** ولا نصّ أي فحص قائم.

---

# القسم 6: المطلوب من مالك المشروع

| # | البند | الحالة |
| :--- | :--- | :---: |
| 1 | اعتماد `DEC-193` بالموضع المصوَّب: **الفحص 10** (لا `ن-5`) + شرط التفعيل الثاني + `ن-7` | ⬜ |
| 2 | إقرار تصويب `84-K2-DEFECT` إلى v1.1 | ⬜ |
| 3 | بناء حارس نظير لـ$K_3$ قبل نقله إلى JS | ⬜ |

**بانتظار الختم أيضاً:** `DEC-190` (إصلاح `DEF-K2-01`) · `DEC-191` (إعادة تأسيس `golden_k2`) · `DEC-192` (تصويب وسم «صفر انحدار»).
**الترقيم التالي:** `DEC-194`.

---

> **[نهاية الرقعة — v1.2 · 🟡 `[مقترح]` · لا يُلصق بند قبل `DEC-193`]**
