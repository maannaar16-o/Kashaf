# 86-K3-FIX — تنفيذ `DEC-195/ج` و`DEC-196` + تعارض `TC-K3-02`

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق · إدارة فجوات |
| **حالة الاعتماد** | **معتمد ومنفَّذ** (`DEC-195/ج` · `DEC-196`) · **تعارض مجمَّد** (`TC-K3-02`) |
| **نطاق الاستدلال** | `79-K3-USERMAP §2.1/§2.2` · `80-K3-TEXTS` · `71-K3-CMP-PHRASEBANK §19` · `55-USER-K3-CMP` · `00-INDEX` · `golden_k3` (٦ حالات) |
| **الإصدار** | v1.0 |

---

# القسم 1: ما نُفِّذ

| # | البند | السند | الحالة |
| :--- | :--- | :--- | :---: |
| 1 | فصل `circle_map` إلى `shared` + `tail[skill]` → `circle_map_v2.json` | `79 §2.1/§2.2` | ✅ |
| 2 | القسم ② «موقعك من النظام» → **المقطع المشترك فقط** | `79 §2.1` | ✅ |
| 3 | القسم ③ → **خاتمة كل مهارة في قسمها** قبل `U01` | `79 §2.2` | ✅ |
| 4 | إلغاء الاسم `CIRCLE_MAPS` عمداً — دمجُه هو ما أنتج العيب | `ن-7` | ✅ |
| 5 | `BAND_LABEL["OUT"] = "قراءة خاصة"` + `band_label()` يوقف عند نطاق مجهول | `80-K3-TEXTS` · `DEC-196` | ✅ |
| 6 | تحديث `k3_guard` لإنفاذ البنية المفصولة | `ن-7` | ✅ |
| 7 | `test_report_k3.py` — انحدار طبقة السحب | `DEC-193` | ✅ |

**تحقّق البنية المفصولة:** المقطع المشترك **متطابق حرفياً عبر الخمسة** (٤٨٣ حرفاً) — دليل مستقل على صحة موضع القطع. والخاتمات خمس متمايزة (٢٢٢–٢٩٢ حرفاً).

---

# القسم 2: برهان الحارس — شرط `ن-7` مُستوفى

| الحالة | النتيجة |
| :--- | :--- |
| **قبل الإصلاح** | ❌ **٥ انحرافات** — الحزمة مدموجة · `OUT` بلا وسم صريح · `band_label` غائبة |
| **بعد الإصلاح** | ✅ **لا انحدار** — ٦/٦ حالات · ٥ أقسام مهارية لكلٍّ · صفر تسرّب |
| `test_golden_k3` (طبقة الحساب) | ✅ **لا انحدار** — الإصلاح لم يمسّها |

---

# القسم 3: المخرج بعد الإصلاح

**القسم ②** — نصّ عام واحد بلا أي إشارة مهارية:

> «مهاراتك الخمس في هذا الجانب ليست صفات منفصلة، بل **نظام واحد يعمل معاً**…»

**القسم ③** — كل خاتمة تحت عنوان مهارتها، فالمرجع حاضر:

> `### الملاحظة — كفاءة أساسية`
> «وهذه المهارة — **الملاحظة** — هي **منبع الصورة كلها**…»

`DEF-K3-01` **مُغلق**: لا مرجع معلَّق، ولا تفضيل `EP` في قسم عام.

---

# القسم 4: 🔴 تقرير تعارض `TC-K3-02` — اسمان لمهارة واحدة

## 4.1 ما ظهر

الإصلاح جعل العنوان والخاتمة **متجاورين سطرين**، فانكشف اختلاف التسمية:

| المهارة | العنوان (`USER_NAME`) | الخاتمة (`79 §2.2`) | الحكم |
| :--- | :--- | :--- | :---: |
| `EP` | الملاحظة | الملاحظة | ✅ |
| **`IR`** | **التهدئة** | **تهدئة الشدة** | ❌ |
| **`BI`** | **المنع** | **كبح الاندفاع** | ❌ |
| **`CF`** | **المراجعة** | **التكيّف** | ❌ |
| `ST` | الاحتمال | الاحتمال | ✅ |

**ما يقرأه المستخدم:**
```
### المنع — قدرة عالية
وهذه المهارة — كبح الاندفاع — أحد الطريقين…
```
اسمان بلا رابط. و`CF` أشدّها: **«المراجعة»** و**«التكيّف»** لا يوحيان بمعنى واحد.

## 4.2 الوثيقتان المتعارضتان

| الطرف | النصّ | الأسماء |
| :--- | :--- | :--- |
| **أ** | `71-K3-CMP-PHRASEBANK §19`: «المصطلحات **في الواجهة** — من `55-USER-K3` **حصراً**» · و`55-USER-K3-CMP`: «**تُنسخ حرفياً** من الأسماء المعتمدة» | الملاحظة · التهدئة · المنع · المراجعة · الاحتمال |
| **ب** | `79-K3-USERMAP §2.2` (نصّ الخاتمات المعتمد بـ`DEC-138`) · `00-INDEX` · `52-MATRIX-K3-07` · `55-USER-K3-REFLECT` | ملاحظة المشاعر · تهدئة الشدة · كبح الاندفاع · التكيّف · الاحتمال |

**نوع التعارض:** الطرف **أ** يحصر الواجهة في المجموعة القصيرة؛ والطرف **ب** نصٌّ معتمد يظهر **في الواجهة** بالمجموعة الطويلة.

**قاعدة الأسبقية:** ❌ **غير منصوصة** ⇒ **تجميد الحكم** (البند 9/14). لا أُرجّح ولا أُعيد صياغة نصّ معتمد.

## 4.3 توصيف دقيق للأثر

| البند | البيان |
| :--- | :--- |
| **التعارض سابق للإصلاح** | قائم منذ `DEC-138`؛ كان **كامناً** لأن أربع خاتمات من خمس لم تكن تُعرض أصلاً |
| **الإصلاح لم يُنشئه** | كشفه — وهذا مقتضى `DEC-195/ج` لا عيب فيه |
| **الخطورة** | 🟠 عالية — لبس مصطلحي في الواجهة، لا خطأ إسناد |
| **الحالة** | مُبلَّغ في `test_report_k3` كوسم `⚠️` **لا يُحتسب انحداراً** — تعارض مجمَّد لا عيب |

## 4.4 المسارات — للاختيار لا للتنفيذ

| المسار | الوصف | الكلفة |
| :--- | :--- | :--- |
| **أ** | توحيد `USER_NAME` على المجموعة الطويلة | يخالف حصر `71 §19` · يمسّ `55-USER-K3-CMP` |
| **ب** | تعديل الخاتمات إلى المجموعة القصيرة | يمسّ نصّاً معتمداً بـ`DEC-138` |
| **ج** | إثبات الاسمين معاً في العنوان: `### المنع (كبح الاندفاع) — قدرة عالية` | لا يمسّ نصّاً معتمداً · يزيد طول العنوان |
| **د** | تجميد وتوثيق كفجوة معلومة | يبقى اللبس في الواجهة |

---

# القسم 5: ملاحظة منهجية — الاختبار أخطأ أولاً

النسخة الأولى من فحص «كل قسم يحمل خاتمته» فحصت **تشابه الاسم نصّياً**، فأعطت **١٨ انحرافاً كاذباً**: الخاتمات صحيحة، والاسم مختلف.

صُحِّح إلى **مطابقة هوية** (`CIRCLE_TAIL[s] in seg`) — وهو الفحص الحقيقي. ونُقل اختلاف التسمية إلى **فحص مستقل** يُبلِّغ ولا يُسقط.

> **الدرس:** فحص يخلط عيبين متمايزين يُنتج إنذاراً كاذباً على أحدهما ويطمس الآخر. **كل فحص يقيس شيئاً واحداً.** يُضاف إلى شروط `ن-7`.

---

# القسم 6: الحالة بعد التنفيذ

| البند | الحالة |
| :--- | :---: |
| $K_2$ — الحساب · السحب | ✅ محروستان |
| $K_3$ — الحساب · السحب | ✅ **محروستان** |
| `DEF-K2-01` · `DEF-K3-01` | ✅ **مغلقان** |
| `TC-K3-02` تسمية الواجهة | 🟠 **مجمَّد** — `DEC-197` |
| المرحلة ① (النقل إلى JS) | 🟢 **الدائرتان جاهزتان** |

**`TC-K3-02` لا يحجب النقل** — لبس مصطلحي لا خطأ إسناد، ويُحلّ بتبديل جدول أسماء لا ببناء. لكن حلّه **قبل** النقل أرخص: بعده يلزم تعديل نسختين.

---

# القسم 7: المطلوب من مالك المشروع

| # | البند | الكود |
| :--- | :--- | :--- |
| 1 | حسم `TC-K3-02` — أ / ب / **ج** / د | `DEC-197` |
| 2 | إضافة شرط «كل فحص يقيس شيئاً واحداً» إلى `ن-7` | `DEC-198` |

**الترقيم التالي:** `DEC-199`.

---

> **[نهاية الوثيقة — `86-K3-FIX` v1.0 · `DEC-195/ج` و`DEC-196` منفَّذتان ومُتحقَّق منهما · `TC-K3-02` مجمَّد]**
