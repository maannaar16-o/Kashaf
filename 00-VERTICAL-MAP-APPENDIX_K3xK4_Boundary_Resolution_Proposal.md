# 00-VERTICAL-MAP-APPENDIX_K3xK4_Boundary_Resolution_Proposal.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `00-VERTICAL-MAP-APPENDIX` — مقترح حسم `RSK-005` |
| **التصنيف الاستراتيجي (`§1-أ` من البروتوكول)** | **[داخل النموذج — يحتاج اعتماداً]** — يمس حدود العلاقة بين دائرتين، لا مجرد أداة تطبيقية. **لا يُدمج تلقائياً في خط الأساس.** |
| **حالة الاعتماد** | ✅ **معتمد بقرار مالك المشروع (`DEC-072`)** — **قابل للتعديل مستقبلاً** (ليس مجمَّداً كالحالات المرجعية `Golden`)؛ أي قاعدة أدناه تُراجَع إذا ظهر دليل ميداني/تحليلي يناقضها |
| **علاقته بـ`DEC-050`** | **يلتزم حرفياً** بتجميد "التقابل البندي K3/K4"؛ لا يقترح أي وزن أو بند أو مصفوفة جديدة — قواعد تشخيصية وظيفية فقط (سؤال/جواب)، لا درجات. |
| **السند** | `02-MASTER RSK-005` (الخطر الأصلي) · `00-VERTICAL-MAP DEC-050` · `13-CORE-K3 §5` · `14-CORE-K4` (نص الجدار + المثال التمييزي) · `50-DEPTH-K3-{EP,IR,BI,CF,ST}` v1.0 (مصدر الأسئلة المفتوحة المحدَّدة) |
| **الوظيفة** | تحويل الخطر العام "قد يحدث تسرب وظيفي" إلى **إجراء تشخيصي قابل للتطبيق** حالة بحالة، مع تحديد صريح لما يبقى مفتوحاً حتى بعد هذا المقترح. |

## 0.1 قاعدة الوسم

كل قاعدة أدناه: **[معتمد — `DEC-072`، قابل للتعديل]**. الاعتماد ليس تجميداً؛ أي قاعدة تُراجَع عند ظهور دليل يناقضها (انظر §4 للبنود التي تبقى مفتوحة بنيوياً بلا أفق إغلاق حالياً).

---

# القسم 1: المبدأ العام (تأكيد، لا اقتراح جديد)

**[معتمد أصلاً — تذكير]** $K_3$ تدير **الحالة** (State) — ما يُشعر به داخلياً. $K_4$ تدير **السلوك** (Behavior) — ما يُنفَّذ خارجياً وميدانياً.

**السؤال التشخيصي الجذري المقترح (يُشتق من مبدأ `14-CORE-K4` نفسه، لا يخترع جديداً):**
> *"هل رافق التوقف/الانحراف مؤشر داخلي محسوس (قلق، إنهاك، اندفاع، تهديد مُدرَك)، أم كانت الحالة الداخلية هادئة/محايدة بينما تعطّل التنفيذ الخارجي فقط؟"*
> — الإجابة الأولى تُحيل لـ$K_3$ (وتحديد أي مهارة منها يحتاج القسم 2)؛ الثانية تُحيل لـ$K_4$.

---

# القسم 2: تطبيق المبدأ على كل مهارة — الوضع الفعلي بعد المراجعة

## 2.1 `K3-EP` × $K_4$ — **[معتمد: لا حاجة لقاعدة جديدة]**
تماس شبه معدوم (مُدخل إدراكي بحت). الجدار العام كافٍ. لا فجوة.

## 2.2 `K3-IR` × $K_4$ (تحديداً `K4-TI` المبادرة) — **[معتمد: قاعدة جديدة]**
> نقطة التباس محتملة لم تكن موثّقة صراحة: نجاح `IR` في "الاستثارة" (رفع الشحنة الداخلية، §1.5 من وثيقة `IR`) **لا يستلزم** نجاح `K4-TI` في "البدء الفعلي". شخص يشحن نفسه داخلياً بنجاح تام (نشاط IR سليم) وقد لا يبدأ الفعل مطلقاً (ضعف K4-TI) — **تركيبة واردة لا تناقض فيها**، بنفس منطق المثال الذي يستخدمه `14-CORE-K4` نفسه بين $K_1$ وضعف المبادرة.
**القاعدة المقترحة:** "الاستثارة الداخلية الناجحة" تُقاس بالحالة الشعورية المرصودة (طاقة، تأهب)، لا بحدوث الفعل التالي. لا يُستنتج ضعف `IR` من مجرد عدم البدء.

## 2.3 `K3-CF` × $K_4$ — **[معتمد: تأكيد كفاية الجدار العام]**
موثّق مسبقاً في `50-DEPTH-K3-CF §1.6` كتماس خفيف. لا قاعدة إضافية مطلوبة الآن — الجدار العام يكفي: نجاح `CF` = تحديث النمط الداخلي؛ تنفيذه لاحقاً شأن `K4` منفصل.

## 2.4 `K3-BI` × $K_4$ — **[معتمد: القاعدة الأهم — حل بإعادة توزيع داخل $K_3$ نفسها]**

السؤال المفتوح كان: *"سكون تام بعد حظر ناجح — من يملكه؟"* الحل المقترح **لا يخترع قاعدة K4 جديدة**، بل يلاحظ أن الجواب موجود أصلاً **داخل تسلسل $K_3$ نفسه** (`BI→CF→ST`)، لا في جدار خارجي جديد:

| الحالة الفعلية بعد الحظر | الإسناد المقترح | السبب |
| :--- | :--- | :--- |
| نشأت نيّة/بديل داخلي واضح، لكنه لم يُنفَّذ | **`K4` (`TI`/`PF`)** | مهمة `BI` انتهت بنجاح (الحظر تمّ)؛ التقصير في التنفيذ لا في الحظر |
| لا نيّة ولا بديل نشأ إطلاقاً — فراغ تام | **`K3-CF`** | فشل توليد استجابة بديلة، لا فشل `BI` نفسها |
| شعور بالتجمّد/الإرهاق يمنع حتى المحاولة | **`K3-ST`** | استنزاف سعة الاحتمال، لا فشل بوابة `BI` |

**الأثر:** `BI` ذاتها لا تحتاج "امتداداً" نحو $K_4$ إطلاقاً — سؤالها ("هل أسمح؟") ينتهي عند لحظة القرار. ما بعده يُسند لمهارات $K_3$ التالية أو لـ$K_4$ حسب الجدول، **دون توسيع تعريف `BI` نفسها**.

## 2.5 `K3-ST` × `K4-PER`/`K4-SP` — **[معتمد: تأكيد + حدّ صريح للتغطية]**
المعيار الحرفي القائم (قلق واعٍ ⚔️ هدوء+ملل، موثَّق في `50-DEPTH-K3-ST`) **يُثبَّت كقاعدة رسمية**. **يبقى مفتوحاً صراحة، لا يُحسم هنا:** حالات الإنهاك الجسدي/المعرفي الصامت بلا قلق شعوري واعٍ — لا يوجد في خط الأساس الحالي مؤشر كافٍ للفصل فيها. **لا أقترح حلاً مخترعاً لهذه الحالة الحدّية.**

---

# القسم 3: إجراء تشخيصي موحّد (خلاصة تطبيقية، بلا درجات جديدة)

**[معتمد]** عند مواجهة حالة "توقف/لم يحدث" غامضة، بالترتيب:
1. هل رافقها مؤشر شعوري داخلي محسوس؟ لا → `K4`. نعم → تابع.
2. هل المؤشر ناتج بعد حظر ناجح لاندفاع؟ نعم → طبّق جدول §2.4. لا → تابع.
3. هل المؤشر "طاقة/تأهب" بلا بدء فعل؟ → لا يُقرأ ضعف `IR`؛ افحص `K4-TI` منفصلاً (§2.2).
4. هل المؤشر "قلق واعٍ" ممتد زمنياً؟ → `K3-ST`. هل "هدوء+ملل" ممتد؟ → `K4-PER` (§2.5).

---

# القسم 4: ما يبقى مفتوحاً صراحة (صفر هلوسة — لا إغلاق كاذب)

| البند | لماذا لم يُحسم |
| :--- | :--- |
| الإنهاك الصامت بلا قلق واعٍ (`ST`) | لا مؤشر باطني كافٍ في خط الأساس؛ يحتاج محتوى جديداً في `13-CORE-K3` نفسها — قرار مالك مشروع، لا اجتهاداً هنا |
| التداخل الإحصائي المحتمل (تباين مشترك فعلي بين `BI`/`K4-TI` عند القياس الميداني) | سؤال تحليل عاملي تجريبي مستقبلي، لا يُحسم بالتعريف وحده |
| تسمية `K4-PER`/`K4-SP` | خارج نطاق هذا المقترح — قرار توحيد ترميز داخلي في $K_4$ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| التزام بحظر `DEC-050` (لا تقابل بندي) | ✅ — قواعد تشخيصية فقط، صفر أوزان جديدة |
| تغطية المهارات الخمس | ✅ 5/5 — 3 تأكيد كفاية · 2 قاعدة جديدة (`IR`، `BI`) |
| فجوات متبقية بعد المقترح | 3 — موثّقة صراحة، غير مُغلقة قسراً |
| **حالة `RSK-005`** | **Mitigated (`DEC-072`)** — لا Closed؛ البند 1 في §4 يبقى Open دائماً بحكم طبيعته |
| **حالة الوثيقة** | ✅ **معتمدة (`DEC-072`) — قابلة للتعديل مستقبلاً بلا حاجة لإجراء تجميد خاص** |
