# 92-INTEGRATE-04b — المرحلة ④-ب : دمج المحرّكين في الأداة

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** (`DEC-205`) |
| **نطاق الاستدلال** | `Kashaf.html` (`CPL-08A-03`) · `41 §5.2/§5.3` · جدار العزل `§6` · `DEC-183/186/205` |
| **المخرج** | `Kashaf_v2.html` — **0.99 ميغابايت** · بلا شبكة |
| **الإصدار** | v1.0 |

---

# القسم 1: `DEC-205` — تبويبان معزولان بتصديرين مستقلين

| القاعدة | التنفيذ |
| :--- | :--- |
| لا شاشة تجمع «المتفهم `E`» و«الملاحظة `EP`» | تبويبان لا يُعرضان معاً أبداً |
| لا سطر يربط عدسة $K_2$ بمهارة $K_3$ | لا دالة في `bridge.js` تُمرّر قيمة بين الدائرتين |
| لافتة ثقة `EP` لا تعبر (`DEC-108`) | داخل تقرير $K_3$ حصراً |
| تصدير مستقلّ | كل تبويب يصدّر مستنده وحده — `Markdown` · `JSON` · طباعة |

**عزل الفشل أيضاً:** توليد كل دائرة في كتلة `try` منفصلة، فتعطّل إحداهما لا يُسقط الأخرى — الجدار يعمل في مسار الخطأ كما في مسار النجاح.

**`DEC-186`:** $K_1$/$K_4$ في لوحة مطويّة موسومة `[خارج نطاق التقرير — لا محرك معتمد]`، **ولا تدخل أي تصدير**.

---

# القسم 2: الجسر — `bridge.js`

**لا يُعيد حساباً ولا يُعرّف مصفوفة.** مصفوفات البنود تُقرأ من `K2_MAP`/`K3_MAP` القائمتين في الأداة — وقد فُحصتا مقابل `41 §5.2/§5.3` فطابقتا 1:1 في أول جلسة.

**ربط الأسماء العربية بأكواد المحرك — مقيس من `41 §5.3` لا مفترَض:**

| اسم الأداة | كود المحرك | نصّ السند |
| :--- | :---: | :--- |
| قوة الملاحظة | `EP` | قوة الملاحظة الانفعالية |
| التحكم الانفعالي | `IR` | التحكم الانفعالي الداخلي |
| كبح جماح النفس | `BI` | كبح جماح النفس الميكانيكي |
| المرونة | `CF` | المرونة الانفعالية والتكيف |
| تحمل الضغوط | `ST` | تحمل الضغوط والإنقاذ العصبي |

**عقد المدخل مُنفَّذ:** بند متروك يوقف التوليد بـ`BridgeContractError` ولا يُكمل بصفر — `41 §7`.

---

# القسم 3: بنية الملف المدموج

```
[React + CPL-08A-03 زمن التشغيل بلا شبكة]     الأصل
[<style> أنماط التقريرين]                     جديد
[engines.js + packs.js + reports.js]          173 ألف حرف
[bridge.js]                                    جديد
[dualreport.js]                                جديد
[سكربت التطبيق — الاستبيان]                    الأصل + استدعاء مُستبدَل
[كتلة الإصدار]                                 الأصل
```

**التعديل على الأصل سطرٌ واحد:** استدعاء `ReportView` استُبدل بـ`DualReportView`. الاستبيان والتخزين والنسخ الاحتياطي **لم تُمسّ**.

**عشر كتل سكربت · كلها سليمة نحوياً** (`node --check`).

---

# القسم 4: التحقق — المحاكاة الكاملة

شُغّلت الأداة كما يفعل المتصفح (تحميل الكتل ثم التوليد من 94 إجابة تركيبية):

| الفحص | النتيجة |
| :--- | :---: |
| `verifyPacks()` — سبع حزم | ✅ |
| `validateSlots()` | ✅ صفر شذوذ |
| توليد $K_2$ | ✅ 5,771 حرفاً · 8 أقسام · مركز `R` |
| توليد $K_3$ | ✅ 9,136 حرفاً |
| **`DEC-183`** — أرقام عشرية في المخرجين | ✅ **صفر في الاثنين** |
| عقد المدخل عند بند متروك | ✅ أوقف بالرسالة الصحيحة |

## التكافؤ على مستوى الأداة — `DEC-199`

مخرج **الأداة نفسها** قُورن بمخرج بايثون على الإجابات نفسها:

```
K2 الأداة ↔ بايثون: ✅ متطابق   (5,770 حرفاً)
K3 الأداة ↔ بايثون: ✅ متطابق   (9,135 حرفاً)
```

> هذا أعلى مستوى تحقّق في المشروع: ليس تكافؤ وحدتين معزولتين، بل **تكافؤ المنتج النهائي** بعد المرور بالاستبيان والجسر والعرض.

**حالة حدّية عرضت نفسها:** الإجابات التركيبية أنتجت `C = 50.0` و`E = 50.0` بالضبط — الحدّ الذي أوجب `DEC-184`. وطبّقت الأداة حدود $K_2$ العليا (`≤`) لا حدود $K_3$ الدنيا، فصنّفتهما «منطفئة» بالقاعدة الصحيحة لدائرتهما.

---

# القسم 5: الانحدار الشامل

| المجموعة | النتيجة |
| :--- | :---: |
| `parity_py` (114 حالة منطق) | ✅ `ef7b161006c30529` |
| `parity_reports` (75 تقريراً) | ✅ `80cb7c9650144fbb` |
| `test_packs` (`ح-1·ح-2·ح-3`) | ✅ |
| `test_golden_k2` · `test_report_k2` | ✅ |
| `test_golden_k3` · `test_report_k3` | ✅ |
| **الأداة المدموجة ↔ بايثون** | ✅ **متطابقة** |

---

# القسم 6: ما بقي

| البند | الحالة |
| :--- | :---: |
| المرحلة ⑤ — حسم `TC-K2-01` ← بنية $K_2$ التساعية (`DEC-189 §8.1`) | ⬜ |
| `GAP-RPT-K2-01` `R1` كتل الشدة | ⬜ |
| `GAP-RPT-K2-02` `R9` قواميس التطهير | ⬜ |
| `GAP-RPT-K2-03` `R11` العرض المزدوج `St+H` | ⬜ |
| `RSK-DUP-01` مانع الازدواج | ⬜ |
| معاينة ميدانية للأداة على متصفح فعلي | ⬜ |

**تحفّظ صريح:** التحقق أعلاه **محاكاة في `node`** لا تشغيل في متصفح. المنطق والنصّ مُثبَتان؛ أما التفاعل الفعلي (النقر · التبويب · حوار التنزيل · الطباعة) **فيلزم فتح الملف في متصفح**. لم أدّعِ ما لم أقِسه.

**الترقيم التالي:** `DEC-206`.

---

# القسم 7: المخرجات

| الملف | الوصف |
| :--- | :--- |
| **`Kashaf_v2.html`** | **الأداة المدموجة — 0.99 ميغابايت · تعمل بلا شبكة** |
| `bridge.js` | الجسر: إجابات ← `sp` |
| `dualreport.js` | التبويبان + التصديرات المستقلة |
| `styles.css` | أنماط العرض والطباعة |

---

> **[نهاية الوثيقة — `92-INTEGRATE-04b` v1.0 · الأداة مدموجة ومتكافئة · التحقق محاكاة لا تشغيل متصفح]**
