# 162-BRIDGE-HARDENING — `DEC-291`: **الجسر يقيس الانتقال والصدق** — وحدُّ الدَور

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «`KASHAF-AI-BRIDGE-HARDENING-002` — إصلاح `AIB-01` و`AIB-02`» — 2026-08-16 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-16 |
| **الأثر** | بروتوكول الجسر **`1.0 ← 1.1`** · مدقّقٌ يقيس ثلاثة أشياء لا واحداً · **٢٧ فحصاً بحقنةٍ مستقلّة لكل عيب** |
| **التغيير المرافق** | `CHG-112` |
| **الحاكم** | `DEC-272` (الجسر) · `DEC-274` (الترقيم) · `م-8` (الحدّ يُعلَن) · `ن-8` |
| **الكود** | `162` |
| **صفر مساس** | القياس · الأوزان · المستويات · المعادلات · المحتوى التفسيري |

---

# §1 العيبان — **رُصدا بالتشغيل لا بالمراجعة**

`AIB-01` و`AIB-02` لم يُكتشفا بقراءة الكود: كُشفا حين **شُغِّل الجسر أول مرّة** (`KASHAF-AI-BRIDGE-DEMO-001`)، وحين **فُحص التسليم قبل تسليمه**.

| العيب | ما كان يمرّ |
| :--- | :--- |
| `AIB-01` | قفزةٌ من `queued` إلى `approved` · واختلافُ حالة المهمّة عن حالة التسليم · وتناقضُ `next_actor` مع `to_actor` — **ثلاثتها معاً** |
| `AIB-02` | `last_commit` يشير إلى **التزامٍ يتيم** خلّفه `--amend`: موجودٍ في المستودع، **وليس في تاريخ `main`** |

> **والجامعُ بينهما واحد**: المدقّق كان يفحص أن الحقل **نصٌّ** لا أنه **صادق** — وهو الصنف الذي قِيس في `DEC-290` على ادّعاءات النطاق. **فحقلٌ يُقرأ ولا يُقاس صدقُه يُنيم الحذر.**

---

# §2 الانتقال يُقاس بين لقطتَين — لا يُصدَّق من حقلٍ يصف نفسه

آلة الحالات صارت **منصوصةً صراحةً** (`TRANSITIONS`)، و`approved` **نهائيةٌ لا انتقال بعدها**.

**والمصدر هو التاريخ المسجَّل**: تُقرأ لقطةُ الملفَّين عند كل التزامٍ مسّ `HANDOFF.json`، ويُقابَل كلُّ زوجٍ متتالٍ. **ولو قيس من حقلٍ يعلن حالته السابقة لكان القياسُ إقراراً** — والمُقِرّ هو المتّهم.

⇒ **ويلزم منه أثرٌ عمليّ مُعلَن: انتقالٌ واحدٌ لكل التزام.** فدمجُ انتقالَين في التزامٍ واحد **يُخفي أحدهما** فيُرفض. وهذه الدفعة نفسها **نُفِّذت بالقاعدة**: التزامٌ يفتح المهمّة، وآخرُ يبدؤها، وثالثٌ ينفّذ، ورابعٌ يختم، وخامسٌ يسلّم.

---

# §3 🔎 حدُّ الدَور — **ملفٌّ لا يحوي بصمةَ نفسه**

المطلوب في `§5` من الأمر: **يُعالَج بوضوح استحالةُ كتابة بصمة الالتزام داخل الملفّ الذي فيه** — وتُحدَّد الدلالة **بلا ادّعاءٍ دائري**.

**والاستحالة رياضية لا هندسية**: كتابةُ البصمة تغيّر المحتوى، وتغيُّرُ المحتوى يغيّر البصمة.

⇒ **فلم تُحَل، بل حُدّت الدلالة بما لا يستلزمها**:

> `last_commit` = **آخر التزامٍ تنفيذيّ يسبق التزامَ سجلّ التسليم.**

| الادّعاء | الحال |
| :--- | :--- |
| «هذه بصمتي» | ❌ **مستحيل — فلا يُدَّعى** |
| «هذه بصمةُ ما تُراجعه» | ✅ **ممكنٌ ومقيس** |
| بصمةُ التزام السجلّ نفسه | ⛔ **لا تُكتب قصداً** — يقرؤها المراجع **رأساً للفرع**، وهي معلومةٌ له بلا كتابة |

**والدائرية تُرفض قياساً لا وعداً**: إن ساوت البصمةُ آخرَ التزامٍ مسّ `HANDOFF.json` سقط التسليم بوصفه **ادّعاءً دائرياً**.

> **وهذا هو الفرق بين حلّ المشكلة وإعلان حدّها**: الأولى كانت مستحيلة، والثانية صادقة — **والصدقُ المحدود أنفع من الوعد الواسع** (`م-8`).

---

# §4 الاتساق والدورة

| القاعدة | المسوّغ |
| :--- | :--- |
| `TASK_QUEUE.status` = `HANDOFF.status` | ملفّان يتناقضان **يجعلان القارئ يختار**، والاختيار ليس قياساً |
| `next_actor` = `to_actor` | وإلّا **صار الدور مجهولاً**: الطابور يقول واحداً والسجلّ آخر |
| `active_task_id` = `HANDOFF.task_id` | «المهمّة النشطة» لا تعني شيئاً إن خالفت ما يُسلَّم |
| `iteration` تتغيّر **في `ready_for_codex → changes_requested` وحدها** وبمقدار **واحدٍ بالضبط**، ولا تنقص | العدّاد الذي يُزاد بلا سببٍ **يستهلك الدورات الثلاث بلا مراجعة** |
| بلوغُ `max_iterations` **يوجب** `owner_decision_required` | **وإلّا كان الحدّ زينة**: يُرصد بلوغُه ولا يترتّب عليه شيء |

---

# §5 الحرّاس — **حقنةٌ مستقلّة لكل عيب**

`§6` من الأمر نصّ ألّا يُكتفى بحقنةٍ تجمع مخالفات. **والمسوّغ مقيس**: حقنةٌ تجمع عيبَين **تُثبت أن أحدهما رُصد ولا تقول أيّهما**.

**٢٧ فحصاً**: سبعةٌ تقيس أن **الانتقالات المشروعة تمرّ** (فحرسٌ يرفض الصحيح لا يصلح) · **وأحدَ عشر حقنةً سلبية**، كلٌّ وحدها · **وستٌّ على صدق البصمة** · واثنتان على الاستثناء وعدّه.

**وحُقن ضدّ `git` الحقيقي أيضاً** لا الوهميّ وحده: بصمةٌ **يتيمة حقيقية** (التي خلّفها `--amend` في التسليم السابق) · **وميتة** · **ودائرية** — **ثلاث إصابات**.

> **والحقنة الوهمية تقيس المنطق، والحقيقية تقيس الوصل** — والاكتفاء بالأولى هو درس `DEC-269` عينه.

---

# §6 حدّان مُعلَنان

- **التاريخ يجب أن يكون كاملاً**: مستنسخٌ ضحل يجعل الانتقال والبلوغ **متعذّرَي القياس** — **فيُقال ولا يُمرَّر صامتاً**، و`CI` استُنسخ بـ`fetch-depth: 0`. **وقبل هذا القرار كان `CI` يستنسخ ضحلاً** — فلو أُضيف الفحص بلا هذا لكان **يمرّ في الطرفية ويكذب في `CI`**.
- **ولا أثر رجعي**: لقطاتٌ أقدم من البروتوكول `1.1` **تُستثنى من قياس الانتقال** — لا تسامحاً، بل لأن قاعدةً تُطبَّق على ماضٍ لم تكن قائمةً فيه **تُدين ما لم يكن مخالفاً**، وهو أخو ما صانه `DEC-274` في الترتيب الزمني و`CHG-111` في وسم اللقطة. **ويُعلَن عددُ المستثنى** فلا يُسكت عنه.

---

# §6/ب 🔎 وثغرةٌ في حرسٍ آخر — رُصدت ولم تُصلَح **لأن النطاق يُصان**

ختمُ هذا القرار **أسقط العلامة ⑦** (`DEC-290`): قاعدتُها «أعلى مدىً على السطر = الأعلى المختوم»، **وجلسةٌ تختم قراراً واحداً لا مدى لها** — فبقي رأسُ `01-MASTER` عند `290`.

**والإصلاح في `gate.py` مُجرَّب وناجح**: يُضمّ الرقم المفرد إلى تعداد سطرٍ يعدّد أصلاً (والضمّ آمنٌ لأن المأخوذ الأعلى، فاستشهادٌ قديم لا يرفعه) مع نزع «الترقيم التالي» الذي له قاعدتُه.

⇒ **ثم أُعيد `gate.py` إلى حاله**: **ليس في `allowed_paths` لهذه المهمّة.** وحُلّت الحالة **داخل النطاق** بإضافة مدىً صريحٍ إلى السطر (`DEC-183…291`) — وهو **صادقٌ لا التفافي**.

**والثغرة مرفوعةٌ لا مطمورة** (`AIB-03`): تعود عند أول جلسةٍ تختم قراراً واحداً.

> **وهذا هو الاتّساق الذي يقيسه الجسر**: امتنعتُ عن `scripts/` في المهمّة السابقة لأنها خارج نطاقها، وأمتنع عن `gate.py` هنا للسبب عينه — **ولو كان النطاق يُخرَق حين يكون الإصلاح جاهزاً بين يديّ لما كان نطاقاً.**

---

# §7 ما لم يُمسّ

- ❌ **طبقة القياس** · الأوزان · المستويات · المعادلات · **والمحتوى التفسيري** — ولا حرف.
- ❌ **البصمات السبع** — حرفياً · والبوابة **27/27**.
- ❌ **ولا دمجٌ تلقائي**: `PR` مستقلٌّ يُترك لمراجعة Codex وقرار المالك.

---

# §8 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| `AIB-01` | ✅ **مُغلق** — الانتقال والاتساق والعدّاد مقيسةٌ كلُّها |
| `AIB-02` | ✅ **مُغلق** — البصمة قائمةٌ وبالغةٌ وغير دائرية |
| حدُّ الدَور | ✅ **مُعلَنٌ لا مُدَّعى** — الدلالة محدودةٌ بما لا يستلزم المستحيل |
| الحرّاس | ✅ **٢٧ فحصاً** · حقنةٌ مستقلّة لكل عيب · **وثلاث إصابات على `git` الحقيقي** |
| البوابة | **27/27 أداة · سبع بصمات · سبع علامات** |

---

**[نهاية `162-BRIDGE-HARDENING` — `DEC-291` · الترقيم التالي `DEC-292` · `CHG-112`]**
