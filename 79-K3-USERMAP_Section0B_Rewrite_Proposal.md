# 79-K3-USERMAP_Section0B_Rewrite_Proposal.md
## إعادة صياغة `§0-ب` في `55-USER-K3-*` — من السلسلة إلى البنية المحورية

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `79-K3-USERMAP` |
| **الإصدار** | **v0.1 — 🟡 `[مقترح]`** (القرار المقترح: `DEC-138`/البند 3) |
| **الغرض** | إغلاق `GAP-PACK-04`: نصّ يقرؤه المستخدم في **كل تقرير** ويصف بنيةً **ملغاة** |
| **السند** | `DEC-098` (البنية المحورية) · `60-K3-TOPOLOGY` v1.0 · `DEC-074` (قالب طبقة المستخدم) |
| **🔒 الحدّ** | **يمسّ نصّاً معتمداً بلغة المستخدم** — لا يُدرَج إلا باعتماد صريح · مرّ **ببوابة النقد** (`DEC-120`) |

---

# القسم 1: التشخيص — ما سقط بالضبط

| # | العبارة القائمة | الحكم | السند |
| :--- | :--- | :--- | :--- |
| ① | «**حلقات في سلسلة واحدة متصلة**» · «حلقات متصلة» | 🔴 **سقطت** — الطوبولوجيا الخطية ملغاة | `DEC-098` |
| ② | «**الحلقة الثالثة**» (`BI`) · «**الحلقة الرابعة**» (`CF`) | 🔴 **سقطت — الأخطر:** `BI` و`CF` **شقيقتان بلا ترتيب**، وعلاقتهما **جانبية** لا تعاقبية | `DEC-098` |
| ③ | «كلٌّ **تسلّم عملها للتالية**» (اتجاه واحد) | 🔴 **ناقصة** — الاحتواء **متبادل**؛ الشحنة تعود إلى المركز (**د-2**) | `DEC-098`/`DEC-116` |
| ④ | «`BI` تستلم الشحنة **بعد أن هدّأتها** الحلقة السابقة» | ✅ **يصمد** — `IR` مركز يبثّ إلى `BI` فعلاً | `M3-01` |
| ⑤ | «ضعف حلقة مبكرة يُثقل كل ما بعدها» (`EP`) | ✅ **يصمد** — أثر `EP` تعاقبي عبر `IR` | `DEC-108` v2 |
| ⑥ | تسلسل الأفعال: **تلاحظ ← تنظّم ← تقرر ← تكيّف ← تحتمل** | ✅ **يصمد** — يطابق مسار المعالجة | `60-TOPOLOGY` |
| ⑦ | الاستعارات الوظيفية: «مُبرِّد النظام» · «الفرملة» · «مُبدِّل المسار» · «خط الدفاع الأخير» | ✅ **تصمد** — لا تتعلق بالطوبولوجيا | — |

**⇒ ثلاث عبارات تسقط، وأربع تصمد.** التعديل **جرّاحي لا إعادة كتابة**.

---

# القسم 2: النصوص المقترحة (تحلّ محل `§0-ب`)

## 2.1 المقطع المشترك — يُصدَّر به الخمسة

> **بلغة المستخدم:** مهاراتك الخمس في هذا الجانب ليست صفات منفصلة، بل **نظام واحد يعمل معاً**:
>
> > **تلاحظ** ما تشعر به ← ثم **تنظّم** شدته ← ومن هناك يتفرّع طريقان معاً: **تقرر** هل تُخرجه سلوكاً، و**تكيّف** استجابتك إن تغيّر الموقف ← وما لا يُحسم في حينه **تحتمله** عبر الوقت.
>
> وما يميّز هذا النظام أمران: **تنظيم الشدة قلبه** — منه تتفرّع البقية وإليه تعود؛ و**القرار والتكيّف طريقان متوازيان**، لا واحد قبل الآخر. لذلك تُقرأ نتيجة كل مهارة **في ضوء موقعها من النظام**، لا وحدها.

*(🆕 «وإليه تعود» — عنصر جديد يمثّل حافات العودة **د-2**؛ انظر §4.)*

## 2.2 الخاتمة الخاصة بكل ملف

| الملف | النص المقترح *(يلي المقطع المشترك)* |
| :--- | :--- |
| **`EP`** | «وهذه المهارة — **الملاحظة** — هي **منبع الصورة كلها**: ما تلتقطه أنت هو المادة التي يعمل عليها كل ما بعده. **ولو كانت الملاحظة غير دقيقة من البداية، عمل ما بعدها على معلومة ناقصة** — لا لخلل فيه، بل لأن ما وصله كان ناقصاً.» |
| **`IR`** | «وهذه المهارة — **تهدئة الشدة** — هي **قلب النظام**: تستلم ما لاحظته، وتُعيد شدّته المرتفعة إلى مستوى متّزن، ثم يتفرّع منها الطريقان. **وما يعجز طرف عن تصريفه يعود إليها**. أنت هنا "مُبرِّد النظام": حين ترتفع الحرارة، تعيدها إلى مستوى يسمح بالتفكير الصافي.» |
| **`BI`** | «وهذه المهارة — **كبح الاندفاع** — **أحد الطريقين** المتفرّعين من القلب: تستلم الشحنة بعد أن هدّأها، ثم تقف **عند البوابة** التي تقرر: هل يمر هذا الفعل/القول الآن أم يُحظر؟ أنت هنا "الفرملة": توقف الاندفاع اللحظي قبل أن يتحول إلى فعل يصعب التراجع عنه.» |
| **`CF`** | «وهذه المهارة — **التكيّف** — **الطريق الآخر** المتفرّع من القلب: حين يتغيّر الموقف أو يفشل ردّك المعتاد، مهمتها أن **تكسر القالب القديم وتبني استجابة جديدة** تناسب ما استجدّ. أنت هنا "مُبدِّل المسار": حين لا يعود الطريق المعتاد صالحاً، تجد طريقاً آخر.» |
| **`ST`** | «وهذه المهارة — **الاحتمال** — **مصبّ النظام كله**، وهي مختلفة عن سابقاتها: لا تعالج لحظةً واحدة، بل تحافظ على تماسكك **عبر الزمن** تحت ضغط مستمر. **وما لم يُعالَج في موضعه ينتهي إليها تحمله.** هي **خط الدفاع الأخير**.» |

**العناوين:** «إطار السلسلة — …» ← **«موقعك من النظام — كيف تقرأ نتيجتك (P5)»**.

---

# القسم 3: 🔍 نتيجة بوابة النقد الذاتي (`DEC-120`)

**تُطبَّق:** إعادة بناء نصّ قائم فوق أساس متغيّر. **النتيجة: صفر أخطاء صريحة · عيبان منهجيان عولجا · بند مرفوع للقرار.**

| الفحص | النتيجة |
| :--- | :--- |
| **1** تتبّع السند | ✅ كل عبارة رُدّت إلى سندها في §1 قبل الكتابة |
| **2** محور التصنيف | ✅ لا تصنيف هنا |
| **3** مصطلح مستورد | 🟡 **عولج:** أول مسودة كتبت «مركز» و«أطراف» — **مصطلحان معماريان**. استُبدلا بـ«قلب النظام» و«طريقان» — لغة مستخدم خالصة |
| **4** تناقض داخلي | ✅ خاتمة كل ملف تطابق المقطع المشترك |
| **5** الجسور | ✅ §3-ب (خطاف البصمة) يلي مباشرةً ولا يتعارض |
| **6** شروط غير مقيسة | ✅ لا شرط جديد |
| **7** تصريح التغطية | ✅ §1 يبيّن ما سقط وما صمد |
| **8** مصدر واحد | 🟡 **عولج:** المقطع المشترك **موحَّد نصّاً** في الخمسة — أي اختلاف بينها لاحقاً يُقرأ انحرافاً |
| **9** إنصاف القديم | ✅ النص القديم **لم يكن خطأً** — كان متسقاً مع السلسلة الخطية، وسقط مرجعه بـ`DEC-098` |

**سؤال البقاء (`ن-4`):** هل يبقى `§0-ب` أصلاً؟ **نعم** — بلا إطار يقرأ المستخدم خمس مهارات منفصلة، ويفوته أن الصورة **تفاعل**. وهو تمهيد القسم ٦.

---

# القسم 4: 🔺 البند المرفوع للقرار — حافات العودة

أضفتُ عنصرين جديدين لم يكونا في النص القديم:
- «**وإليه تعود**» (المقطع المشترك) · «**وما يعجز طرف عن تصريفه يعود إليها**» (`IR`)

**العلّة:** حذف «سلسلة» وإبقاء التدفّق أحادي الاتجاه **يُصلح التسمية لا المضمون** — وحافات العودة (**د-2**) معتمدة بـ`DEC-098` وبُني عليها **سبع خلايا** في السجل.

**لكنه محتوى جديد بلغة المستخدم** — لا نقل من نص معتمد. فيُعرض صراحةً:

| الخيار | الأثر |
| :--- | :--- |
| **(أ)** إدراجه | النص يطابق البنية المعتمدة كاملةً · **يستوجب اعتماداً صريحاً** |
| (ب) حذفه | تعديل أخفّ · لكن يبقى النص **ناقصاً** لا خاطئاً — يصف نصف البنية |

**🟡 أرجّح (أ)** — لأن `§0-ب` تمهيد للقسم ٦، والقسم ٦ يعرض كُلفاً **راجعة إلى الداخل** (`T-02`). فقارئ لم يُقدَّم له مسار العودة يجد الكلفة الراجعة بلا إطار يفسّرها.

---

# القسم 5: بنود الاعتماد المطلوبة

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | إسقاط «السلسلة/الحلقات» و**ترتيب `BI`/`CF`** من `§0-ب` في الخمسة | ⬜ |
| 2 | اعتماد المقطع المشترك + الخواتم الخمس (§2) | ⬜ |
| 3 | **حافات العودة:** الخيار (أ) أم (ب) | ⬜ **مرفوع** |
| 4 | تغيير العناوين إلى «موقعك من النظام» | ⬜ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| عبارات تسقط | **3** — أخطرها ترتيب `BI`/`CF` |
| عبارات تصمد | **4** — منها الاستعارات الوظيفية كلها |
| بوابة النقد | ✅ مُطبَّقة — عيبان عولجا |
| مصطلح معماري في الواجهة | ✅ **صفر** |
| **حالة الوثيقة** | 🟡 **`[مقترح]` — بانتظار قرار المالك** |
