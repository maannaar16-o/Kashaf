# 148-TEAM-ENGINE — `DEC-278`: محرّك تركيب الفريق وباني تقريره — **عرضٌ لخلايا مختومة لا سرد**

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفّذ المرحلة ①**» ثم «**واصل**» — 2026-08-14 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-14 |
| **الأثر** | `56-TEAM-00` **يعمل**: تسعة أقسام من خلايا مختومة · **حزمة بصفر تأليف مفكوكة آلياً** · **بصمة سادسة انحدارية** · البوابة **22 ← 23** · والبصمات الخمس التكافؤية **حرفياً** |
| **التغيير المرافق** | `CHG-097` |
| **الحاكم** | `DEC-277` (تشغيل الميثاق) · `DEC-039` (اعتماد المصفوفات) · `DEC-041`/`DEC-157` (العتبات) · `DEC-040` (الوسمان) · `DEC-254` (سابقة أدوات المشغّل) · `56-TEAM-00 §4` (الأقفال الخمسة) |
| **الكود** | `148` — تمديد بمسوّغه: طبقة تركيبٍ مستجدّة فوق $K_2$ |

---

# §1 المبدأ الحاكم — **التقرير عرضٌ لا سرد**

قفل الميثاق `§4` ينصّ: «**صفر توليد — كل خلية تستدعي وحدة معتمدة حرفياً**»، و`G4`: «**إنذار بنيوي وتوصية تشكيل — لا حكم كفاءة**».

⇒ فالتقرير **يعرض خلايا مختومة تحت عناوين مختومة، ولا جملةَ ربطٍ فيه**. وكلُّ ما يخرج منه موجودٌ حرفياً في `51-MATRIX-01/02/03/04` أو في `56-TEAM-00` أو `APPENDIX-B`.

> **والقراءة السردية شأن المدرّب لا شأن الأداة.** وهذا ليس نقصاً يُستدرك: **الأداة التي تسرد تحكم**، والميثاق يمنعها أن تحكم.

---

# §2 الحزمة — مفكوكة آلياً لا منقولة يدوياً

`build_team_pack.py` **لا يكتب جملةً واحدة**؛ يفكّ الجداول ويعيد صفّها:

| المصدر | المستخرَج |
| :--- | :--- |
| `51-MATRIX-01 §2` | **28 زوجاً**: محور الصدام · بروتوكول الاحتواء · **الكيان الهجين** · العمى المشترك |
| `51-MATRIX-02 §1.1` | **الأزواج القطبية الخمسة** بمحاور تعامدها وفلترة طرفيها |
| `51-MATRIX-04 §1` | نقطة العمى الكبرى لكل بُعد وما يغيب عن عدسته |
| `51-MATRIX-04 §2.1` | **ستّ تركيبات موثَّقة** عالية الخطورة |
| `51-MATRIX-03 §1` | مصفوفة الارتداد الداخلي تحت الضغط |
| `56-TEAM-00 §2`/`§4` | العناوين التسعة · والأقفال الخمسة |
| `APPENDIX-B §4/4` | **التنبيه الإلزامي**: «أداة تخادم وتشكيل، لا أداة تقييم أداء أو قرار توظيفي» |
| `k2_report` | **الوسمان الإجرائيان** بصيغتهما هناك حرفياً — فلا صيغة ثانية لوسمٍ واحد |

**والضمانة ليست في المولّد بل في الفحص:** `test_report_team` يطابق **كل نصٍّ في الحزمة بمصدره حرفياً** — ونصٌّ بلا مصدر يوقف الإصدار. وحُقن نصٌّ مؤلَّف فسقط الفحص.

## وحدٌّ صُحِّح في المولّد

أول فكٍّ للأقفال اشترط **شكل** العمود الثاني فالتقط **ثلاثة من خمسة**. فقُصر الفكّ على **القسم نفسه**. ⇒ **الحدّ يُعرَّف بموضعه لا بشكله.**

---

# §3 المحرّك — لا يعرّف عتبةً ولا يحسب درجة

| القيد | التنفيذ |
| :--- | :--- |
| **العتبات** | تُقرأ من `k2_engine.comp_state` (`D>70` · `M 50–70` · `L≤50`) — **مصدرُ حقيقةٍ واحد**، فلا رقم مكرَّر في هذه الطبقة |
| **الرموز** | `k2_engine.octal_code` — `H+`/`H`/`M+` كما يطلب عقد المدخل (`§1`) |
| **صفر لمس `K1`/`K3`/`K4`** | عقد المدخل **يرفض** أي حقلٍ من خارج عدسات $K_2$ الثماني — والقفل الخامس **مقيسٌ لا موعود** |
| **عقد المدخل** | عضوان حدّاً أدنى · رموز فريدة · ثماني عدسات كاملة · وغيرُ المنتهي **يُرفض صراحةً** — وإلا صُنِّف «مهيمناً» صامتاً (الدرس المقيَّد في `k4_engine`) |

---

# §4 ثلاث قراءات تشغيلية — **مقروءةٌ من تشغيلٍ معتمد لا مخترَعة** · تُرفع للمالك

الميثاق يوجب مخرجاً ولا ينصّ على قاعدة توليده في ثلاثة مواضع. والتشغيل التجريبي المعتمد `56-TEAM-P001` **منفَّذٌ بأرقامه**، فقُرئت القواعد منه:

| # | الموضع | القاعدة المقروءة | دليلها |
| :--- | :--- | :--- | :--- |
| ① | **تقاطع الزوج** (`§5`) | يُقدَّم **الزوج القطبي** إن وُجد بين مهيمنَي العضوين، وإلا **أعلى مهيمنٍ لكلٍّ** | صفوفه الثلاثة متسقة معها — وأعاد المحرّك إنتاجها كلها |
| ② | **التركيبة الموثَّقة** (`§3`) | تنطبق حين يكون **كلُّ** ما تطلبه غائباً فعلاً لا بعضُه | التقاطع الجزئي يستدعي **ثلاثاً** والتشغيل يستدعي **واحدة** |
| ③ | **الارتداد** (`§7`) | **تُعرض كل عدسةٍ مهيمنة** — لا واحدة | التشغيل يعرض لـ`T-01` عدسته **الثانية** لا الأولى ⇒ **لا قاعدة اختيارٍ تُستخرج منه**، والعرض الكامل يُغني عن قاعدةٍ تُخترع ويشمل ما عرضه (`م-8`) |

> **وترفَع الثلاث للمالك بوصفها قراءات** — كما رُفعت قراءتا `DEC-035` عند بناء الموقع. له إقرارُها أو تصويبُها.

---

# §5 بايثون وحدها — **بسابقةٍ مختومة، وبحرسٍ على التأجيل**

`DEC-199` يوجب التوأمة، **وعلّتها المنصوصة أن جانب `JS` مضمَّنٌ في أدوات المتصفح**. وطبقة الفريق **بلا سطح متصفح** في هذه المرحلة.

**والسابقة قائمة ومختومة:** أدوات المشغّل `contrib_pull`/`contrib_analyze` (`DEC-254`) **بايثون وحدها بلا توأم**؛ والمشرف له توأم **لأن له `Supervisor.html`**. ⇒ **التوأمة لِما يعمل في متصفح.**

| البند | القرار |
| :--- | :--- |
| **التسمية** | **بصمة انحدار لا بصمة تكافؤ** — ولا طرفان يُقاس التطابق بينهما. وتسميتها «تكافؤاً» **ادّعاءٌ كاذب** |
| **حرس التأجيل** | الفحص السادس: أي `buildReportTeam`/`TeamEngine`/`RawahilTeam` في `engines.js`/`reports.js`/`dualreport.js`/`app.js` **يُسقط البوابة** |
| **قاعدة القلب** | حين يُبنى سطح المرحلة ③، **يُقلب الحرس لا يُحذف**: من «لا سطح بلا توأم» إلى «التوأم مقيسٌ بتكافؤ» (`140 §5`) |

---

# §6 البرهان — إعادة إنتاج تشغيلٍ معتمد

`56-TEAM-P001` تشغيلٌ **معتمد ومنفَّذ**. شُغِّل المحرّك على ملفّاته فطابقه في **أربع نتائج بنيوية مستقلة**:

| النتيجة | التشغيل المعتمد | المحرّك |
| :--- | :--- | :---: |
| الكيانات الهجينة الثلاثة | `DY-05` · `DY-22` · `DY-27` | ✅ |
| القطبية البينية | `A↔E` · `C↔H` | ✅ `P1` · `P4` |
| الفجوة الجماعية | `O` و`R` بلا مهيمن | ✅ |
| التركيبة الموثَّقة | **واحدة** — «بلا أقدام على الأرض» | ✅ `H+St` |

**وهذا أقوى تحقّقٍ متاح:** ليس اختباراً كتبتُه لنفسي، بل **مخرجٌ معتمدٌ سابقٌ للبناء** أعاد المحرّك إنتاجه.

---

# §7 الحرّاس مُختبَرون على فشلهم — ثلاث حقنات

| الحقنة | النتيجة |
| :--- | :--- |
| نصٌّ مؤلَّف في الحزمة | ❌ **رُصد** — «بلا مصدر» بمساره |
| سطح متصفح بلا توأم (`dualreport.js`) | ❌ **رُصد** — «ظهرت بلا توأم» |
| لفظ تفاضلي في المتن | ❌ **رُصد** — بالسطر الحامل له |

## وفحصٌ كان يعدّ الصحيحَ خطأً — فشُدّ إلى معناه

أول صياغة لفحص «صفر مقارنة تفاضلية» رصدت «الأفضل/الأضعف» و«تقييم أداء» — **في نصوص التحريم نفسها**: قفل `§4/1` والتنبيه الإلزامي. فالفحص الساذج **يعدّ المنعَ مخالفةً**. فشُدّ إلى معناه: **لا لفظ محرَّم خارج سطرٍ مختوم من الحزمة**.

---

# §8 وأوقفتني البوابة على نفسي

أُضيفت الأداة إلى `gate.py` قبل الوثيقتين، **ففحص الانجراف الوثائقي (`DEC-273`/③) أسقط البوابة**: «في البوابة لا الوثيقة `['test_report_team.py']`».

> **الحرس الذي بنيتُه أمس منعني اليوم من تركِ وثيقةٍ متخلّفة.** وهذا ما لم تكن البوابة تفعله قبل `DEC-273` — وهو عين ما أنتج `CHG-088` و`CHG-096`.

---

# §9 ما لم يُمسّ

- ❌ **طبقة القياس** — ولا عتبة تُعرَّف هنا؛ تُقرأ من `k2_engine`.
- ❌ **أي محرك أو تقرير أو حزمة قائمة** — والبصمات الخمس التكافؤية **حرفياً**.
- ❌ **`K1`/`K3`/`K4`** — مرفوضةٌ في عقد المدخل، ومقيسةٌ في المخرج.
- ❌ **الموقع العام وتطبيقه** — لا سطر: هذه أداة مشغّل.

---

# §10 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| `56-TEAM-00` | ✅ **يعمل** — تسعة أقسام · معطَّلاً منذ اعتماده حتى اليوم |
| صفر تأليف | ✅ الحزمة **كلّها** مفكوكة من جداول مختومة · ومفحوصة نصّاً نصّاً |
| القراءات الثلاث | ✅ **مقروءة من تشغيلٍ معتمد** ومرفوعة للمالك |
| التوأمة | ✅ **مؤجَّلة بسابقةٍ مختومة ومحروسة** — والتسمية صادقة (انحدار لا تكافؤ) |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** |
| البصمات | خمسٌ تكافؤية **حرفياً** · وسادسةٌ انحدارية **`859f0a3241b5ad60`** |
| الأدوات | **23/23** |

---

**[نهاية `148-TEAM-ENGINE` — `DEC-278` · الترقيم التالي `DEC-279` · `CHG-097` · المرحلة ① منتهية]**
