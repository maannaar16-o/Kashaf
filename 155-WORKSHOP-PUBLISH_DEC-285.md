# 155-WORKSHOP-PUBLISH — `DEC-285`: **نشرُ صفحة الورشة** — قفلٌ كامل وفصلٌ مقيس

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**ارفع `Workshop.html` على الموقع**» — بعد رفعي التحفّظ وإعادته الأمر — 2026-08-15 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-15 |
| **الأثر** | `docs/workshop.html` منشورة · **بالقفل الكامل لا المضيَّق** · صفر رابطٍ إليها من صفحةٍ عامة · وخارج مخزن التطبيق · البصمات الستّ **حرفياً** والبوابة **25/25** |
| **التغيير المرافق** | `CHG-104` |
| **الحاكم** | `DEC-284` (طريق التسليم بلا خادم) · `DEC-279` (السطح وقفلُه) · `DEC-250` (وعد الموقع العام) · `DEC-110` (قفل صفر الشبكة) · `م-2` |
| **الكود** | `155` — تمديد بمسوّغه: نشرُ سطحٍ ثانٍ على الأصل العام |

---

# §1 التحفّظ الذي رُفع — وكيف عولج بدل أن يُسكَت عنه

قلتُ للمالك: **وضعُ صفحةٍ تقول «نتيجتك تصل مدرّبك» على أصل صفحةٍ تقول «إجاباتك لا تغادر جهازك» يستحق قراره الخاص.** فأمر بالرفع.

**والقرار قرارُه — والتنفيذ لا يكتفي بتنفيذه**: عولج التحفّظ في التصميم لا بالسكوت عنه.

| الخطر | المعالجة |
| :--- | :--- |
| **استثناءُ شبكةٍ يُحمَل إلى أصلٍ عام** | ❌ **لا يُحمَل**: النسخة المنشورة **لا تحتاج شبكةً أصلاً** (`DEC-284` جعل التسليم بالملف)، فتأخذ **القفل الكامل** `CPL-08A-03` — قفلَ التطبيق العام نفسه |
| **زائرٌ يقع عليها** | ❌ **صفر رابطٍ إليها** من أي صفحة عامة — **مقيسٌ في البناء**، ومنعُ فهرسة في رأسها |
| **زرُّ إرسالٍ يسقط دائماً** | ❌ **لا يُعرَض**: `DELIVERY: "file"` يُعلَن في البناء، فلا وعدٌ يُخلَف في الشاشة |
| **اختلاطٌ بالتطبيق العام** | ❌ **خارج مخزن عامل الخدمة** — لا تُثبَّت معه ولا تُخزَّن في ذاكرته |

> **والمبدأ:** `DEC-284 §1` قيّد أن **القفل نصُّه ثابت ومعناه يتغيّر بمكانه**. وهذا القرار يطبّق القاعدة على نفسها: **حين تغيّر المكان، تغيّر القفل** — لا لأن النصّ ساء، بل لأن السطح لم يعد يحتاج استثناءه.

---

# §2 سطحٌ واحد بوضعين — لا سطحان

`build_workshop_html.py` يُخرج نسختين من **مصدرٍ واحد** (`م-2`):

| الوضع | القفل | زرُّ الإرسال | أين |
| :--- | :--- | :---: | :--- |
| `server` | `CPL-WS-01` — منفذٌ واحد على الأصل نفسه | ✅ | `Workshop.html` على جهاز المالك |
| `offline` | **`CPL-08A-03`** — كل اتصالٍ ممنوع | ❌ | `docs/workshop.html` المنشورة |

**وكودُ الإرسال مضمومٌ في الحالين** — ولا يُنزَع بجراحة نصّية. **فالضمانة القفلُ لا غيابُ سلسلة**: الزرّ لا يُعرَض، **ولو بُلِغ لرماه القفل**. وهذا أصدق من ادّعاء أن الكود «ليس هناك».

---

# §3 الفصل **مقيسٌ لا موعود** — في ثلاثة مواضع

| الموضع | القياس |
| :--- | :--- |
| **الروابط** | البناء يمرّ على كل صفحةٍ عامة ويسقط إن وجد `workshop.html` فيها |
| **مخزن العامل** | `sw.js` يُسقط البناء إن ظهرت الصفحة في قائمة التخزين — **والاستثناء معلَنٌ في الكود** لا صامت |
| **عقد الصفحة** | `workshop_page_contract` في البناء: قفلٌ كامل · تسليمٌ بالملف · صفر حقل اعتماد · منعُ فهرسة · صفر مورد خارجي |

**وصفحةُ الورشة تُفحص بعقدها لا بلينت النثر**: نصُّها المرئي مولَّدٌ في المتصفح، فلينتُ الصفحات الثابتة يقيس فيها ما ليس فيها. ⇒ **عقدٌ بأسمائه بدل فحصٍ لا ينطبق.**

---

# §4 البرهان — بالتشغيل على النسخة المنشورة نفسها

فُتحت `docs/workshop.html` من `file://` في `Chromium`:

| المقياس | الملاحَظ |
| :--- | :--- |
| الأزرار | **زرٌّ واحد**: «نزّل نتيجتك لتسلّمها بنفسك» — ولا زرَّ إرسال |
| القفل حيّاً | `fetch` إلى `https://example.com` **مرفوض** |
| الملف | `rawahil-workshop-FRR8-MHBU.json` · `40,928` بايت |
| الختام | «**حُفظت** نتيجتك — سلّمه لمدرّبك ليُدخله» |
| الشاشة | **صفر `SP%`** · **صفر خطأ طرفية** |
| الإدخال | `accept` ⇒ «✅ مقبول ومخزَّن» — **بوابةُ المشرف تحكمها كما تحكم غيرها** |

---

# §5 الحرّاس مُختبَرون على فشلهم — وحقنةٌ لم تقس ما قصدتُه فأُعيدت

| الحقنة | النتيجة |
| :--- | :--- |
| رابطٌ إلى صفحة الورشة من `about.html` | ❌ **رُصد** — «الفصل منقوض» |
| نسخةٌ منشورة بالقفل المضيَّق (في `build_site`) | ❌ **رُصد** — «القفل ليس الكامل» |
| الباني نفسه يُخرج منشورةً بالاستثناء | ❌ **رُصد** — «النسخة المنشورة تحمل القفل المضيَّق» |

## 🔎 وحقنةٌ نجحت كذباً فأُعيدت في موضعها

أول تنفيذٍ للحقنة الثانية بدّل الوضع ثم شغّل الفحص — **فمرّ**. والسبب أن **البناء سقط**، فبقيت النسخة السليمة على القرص والفحصُ قرأها.

⇒ **الحقنة التي تُسقط البناء لا تختبر الفحص، بل تختبر البناء.** فأُعيدت على طبقتها الصحيحة، وقيست الطبقتان معاً.

> **ودرسٌ يُقيَّد:** حقنةٌ «تنجح» يجب أن يُسأل عنها **أين وقعت**، لا أن يُكتفى بأن الفحص أخضر.

---

# §6 ما لم يُمسّ

- ❌ **الموقع العام** — صفحاتُه الثمانية وتذييلُها ووعدُه، و`kashaf.html` بقفله. **ومقيسٌ**: لا `RAWAHIL-WORKSHOP` فيه ولا رابطَ ورشة.
- ❌ **طبقة القياس** · **المحرّكات** · **البصمات الستّ حرفياً**.
- ❌ **نصّ الإذن** — كما هو، والمشارك يقرؤه ويوافق قبل أول بند.
- ❌ **المستقبِل المنشور** — لا يزال مؤجَّلاً بأربع مشكلاته (`CHG-103`): هذه صفحةٌ منشورة **بلا خادم**، لا خادمٌ منشور.

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| النشر | ✅ `docs/workshop.html` — تسعُ صفحاتٍ في `docs/` |
| القفل | ✅ **الكامل** — ولا استثناءَ على الأصل العام |
| الفصل | ✅ **مقيسٌ في ثلاثة مواضع** |
| البرهان | ✅ **بالتشغيل على المنشورة نفسها** |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** — وواحدةٌ أُعيدت لأنها لم تقس ما قصدت |
| الأدوات | **25/25** |

---

**[نهاية `155-WORKSHOP-PUBLISH` — `DEC-285` · الترقيم التالي `DEC-286` · `CHG-104`]**
