# 104-GUARD-SP — حارس `ح-4`: منع `SP%` في أي مخرج

| الحقل | البيان |
| :--- | :--- |
| **القرار** | **`DEC-230`** |
| **الأمر** | خيار **(ب)** — إبقاء السطح الميت + إضافة حارس `ح-4` |
| **السند** | `DEC-183` (حذف `SP%` من الشاشة وكل التصديرات) · `ن-7` بشروطه الأربعة |
| **الدين المُغلق** | `DEBT-TOOL-PURGE-01` — يُحوَّل من «دين مفتوح» إلى **«مُطوَّق بحارس»** |
| **حالة الاعتماد** | **مشتق** — لا مفهوم جديد ولا وزن ولا عتبة |

---

## القسم 1: ما يقيسه الحارس — شيء واحد

`ح-4` يقيس **حضور الرمز `SP%` في نصّ قابل للعرض** — لا غير (`ن-7/②`).

**النمط:** `/SP\s*%/i` — مطابق حرفياً في بايثون وJS.

**الحدّ قاطع لا مخترَع (`ن-7/④`):** أي مطابقة واحدة توقف الإصدار. لا وزن، لا عتبة، لا «عدد مقبول».

---

## القسم 2: حدود النطاق — ولماذا سقطت بالتعريف لا بالاستثناء

| البند | داخل النطاق؟ | السند |
| :--- | :---: | :--- |
| نصّ التقرير (`full` · `brief`) | ✅ | `DEC-183` |
| تصدير `MD` · `طباعة/HTML` · `JSON` | ✅ | `DEC-183` — «كل التصديرات» |
| الحزم الاثنتا عشرة (المصدر) | ✅ | كل نصّ يعيش في حزمة يخرج للمستخدم |
| **الحقل العددي `sp`** | ❌ | `DEC-183` مصنَّف «**تعديل عرض**» في `01-MASTER` · و`100-AUDIT-REGEN` §2 يجعله **حقلاً ملزِماً** · و`DEC-220` يوجب كفاية `audit` (86 حالة تشتغل من `audit["sp"]`) |

> **لا استثناء مخترَع:** الحقل `sp` عددي بلا علامة `%` — فلا يطابق النمط أصلاً. الحدّ سقط **من التعريف**، لا بقاعدة مضافة. وقد قِيس ذلك: تصدير `JSON` الحاوي `audit` يمرّ الحارس بصفر إصابة.

**سجلّ الذِكر المقبول** (على نمط `DEC-229`): `MENTION_REGISTRY` — **فارغ اليوم**. أي ذِكر مشروع مستقبلي للرمز (مثل الاستشهاد بـ`DEC-183` داخل ترويسة حوكمية) يُسجَّل فيه **صراحةً**، ولا يُستثنى بحدس.

---

## القسم 3: استيفاء شروط `ن-7` الأربعة

| الشرط | الاستيفاء | البرهان |
| :--- | :--- | :--- |
| ① يُختبَر على **عيب معلوم** (`DEC-194`) | ✅ | ستة مقاطع **واقعية** مستخرَجة من `Kashaf_v2.html` (السطح الميت) — **6/6 أُوقفت** · صفر إنذار كاذب على ثلاثة نصوص مشروعة |
| ② **قياس واحد** (`DEC-198`) | ✅ | الرمز `SP%` وحده. كتلة الشدة «فوق 50%» لا تحوي `SP` ⇒ تمرّ. لو خُلط القياسان لأنتج إنذاراً كاذباً على 18 تقريراً |
| ③ **تكافؤ النسخ** (`DEC-199`/`200`) | ✅ | `sp_gate.py` ↔ `sp_gate.js` — تسع عيّنات، نتائج متطابقة |
| ④ **لا عتبة مخترعة** (`DEC-224`) | ✅ | يوقف عند الحالة القاطعة ويُبلِّغ الموضع والسياق — لا يخترع وزناً |

---

## القسم 4: مواضع التوصيل — سبعة

| # | الموضع | الملف |
| :--- | :--- | :--- |
| 1 | نقطة إصدار تقرير $K_2$ | `k2_report.build_report` |
| 2 | نقطة إصدار تقرير $K_3$ | `k3_report.build_report` |
| 3–5 | `buildReportK2` · `buildReportK3` · `buildReportK3Head` | `reports.js` |
| 6–8 | `exportMd` · `exportPrint` · `exportJson` | `dualreport.js` |

**التوصيل مُبرهَن لا مُدَّعى (`ح-4/د`):** بتبديل النمط مؤقتاً برمز حاضر يقيناً في المخرج، توقّف الإصدار في $K_2$ و$K_3$ معاً — أي أن الحارس **موصول عند نقطة الإصدار**، لا مجرد وحدة قائمة بذاتها.

---

## القسم 5: اختبار القبول — البصمتان لم تتغيّرا

الحارس **لا يمسّ نصّاً**؛ يوقف عند الشذوذ فحسب. فالمعيار الحاسم أن يبقى المخرج كما هو:

| الأداة | قبل | بعد |
| :--- | :--- | :--- |
| `parity_py.py` | `2711c24d8155819b` | **`2711c24d8155819b`** ✅ |
| `parity_reports.py` | `36ae94bfd5a8b60f` | **`36ae94bfd5a8b60f`** ✅ |
| `verify_regen.py` | 86 حالة | 86 حالة ✅ |
| الأدوات الستّ الأخرى | لا انحدار | لا انحدار ✅ |
| **`test_guard_sp.py`** (جديدة) | — | لا انحدار ✅ |

**الأداة المرقّعة:** 142 تقريراً مولَّداً منها **مطابقة حرفياً** لمخرج المرجع — صفر تباعد.

---

## القسم 6: الأداة النافذة

| البند | البيان |
| :--- | :--- |
| المخرَج | `Kashaf_v2_FIXED_DEC-230.html` (تسمية على عرف `*_FIXED_DEC-*` القائم) |
| الحجم | 1,104,286 → **1,106,888** بايت (+2,602) |
| `SHA-256` | `1352526c1fff7141…` → **`1097d91e668eb011…`** |
| طبقة الحزم | **12/12** بصمة سليمة · منطقة البيانات مطابقة حرفياً لـ`packs.js` |
| ترتيب التحميل | `sp_gate` **قبل** `reports` و`dualreport` — مُتحقَّق منه |

---

## القسم 7: ما لم يُغلَق — دَينان مفتوحان

### `DEBT-SP-BARE-01` — النسبة المجرّدة تنفذ من `ح-4`
`ح-4` يرصد **الرمز** `SP%`. نصٌّ يعرض `95.2%` بلا الحرفين `SP` **يمرّ**. وقد قِيس: عيّنة `«نسبة الإنجاز بلغت 95.2%»` تمرّ الحارس.

| الحقل | البيان |
| :--- | :--- |
| الخطورة | 🟡 متوسطة — لا إصابة قائمة اليوم (142 مخرجاً · صفر) |
| العلاج المقترح | حارس **`ح-5`** منفصل يرصد **نسبة الفرد** — بقياس واحد مستقلّ (`ن-7/②`)، مع تسجيل صريح لكتلة الشدة `R1` في سجلّ الذِكر المقبول |
| الحالة | **مقترح استراتيجي — غير معتمد.** لم يرد أمر به |

### `GAP-DEC-NUM-01` — تصادم ترقيم
`00-HANDOVER` §5 كان يحجز الرمز **`DEC-230`** لبند «حقول `null` في تصدير `JSON` لـ$K_3$». وقد أخذه هذا القرار.

⇒ **بند حقول `null` يُرحَّل إلى `DEC-231`** — ولا يُدمج هذا في خط الأساس قبل إقرارك، إعمالاً لانضباط الترقيم الذي أوجب تصحيح `+40` سابقاً.

---

**[نهاية `104-GUARD-SP` — `DEC-230` · بانتظار ختم المالك]**
