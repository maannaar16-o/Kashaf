# 143-CI-GATE — `DEC-273`: البوابة تُفرَض لا تُذكَر — مُشغِّل واحد و`CI`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**ادمج الـPR ثم نفّذ `DEC-273`**» — 2026-08-13 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-13 |
| **الأثر** | **البوابة صارت مفروضةً آلياً** على كل دفعة وكل طلب دمج · **وسلطتها واحدة** (`gate.py`) بدل ثلاث نسخٍ منجرفة · وأربعة فحوصٍ **على البوابة نفسها** · **صفر اعتمادية خارجية** محفوظة · البصمات الخمس ثابتة حرفياً |
| **التغيير المرافق** | `CHG-091` |
| **الحاكم** | `م-2` (مصدر حقيقة واحد) · `00-HANDOVER §6①` (الحقل يفحص أو لا يُضاف) · `00-HANDOVER §6⑦` (ما لا تشمله البوابة ينكسر صامتاً) · `CHG-054` · `ن-8` |
| **الكود** | `143` — تمديد بمسوّغه: طبقةُ فرضٍ للبوابة القائمة · **والكود `142` استُهلك بـ`DEC-272`** (§5/④) |

---

# §1 العيب — **البوابة التي يستند إليها كل قرار لم يفرضها إلا انتباهُ المنفّذ**

كل قرار في هذا المستودع يذكر «`20/20`» أو «`22/22`» برهاناً على قبوله. **ولم يكن في المستودع ما يفرض ذلك**: لا `CI`، ولا `.github/`، ولا فحصٌ عند الدفع. فالبوابة تعمل على حاسوب المنفّذ **وحده**، وأيّ دفعٍ من جلسةٍ أخرى — أو من جلسةٍ تنسى — **يهبط غير مقيس**. وطلب الدمج `#1` مرّ **بصفر فحوص**.

## وهذا الصنف انكسر ثلاث مرات في يومين

| التجلّي | ما خرج من البوابة | ما ترتّب |
| :--- | :--- | :--- |
| `DEC-269` | `build_site.py` | انكسر بناء الموقع **ومرّ صامتاً في التزامَين** · و`docs/` بقيت لا تعرف $K_4$ |
| `DEC-271`/أ | `build_supervisor_html.py` | **تعذّر توليد `Supervisor.html` أصلاً** |
| `DEC-271`/ب | كتلة تدقيق التقرير في `parity_k4` | **حقلان ملزمان غائبان** عن التوأم JS |

فأُغلقت التجلّيات الثلاثة، **والصنف بقي مفتوحاً درجةً أعلى**: البوابة كلّها في الموضع نفسه الذي كانت فيه تلك الأدوات — بلا حرس.

## وعيبٌ ثانٍ ملازم: **ثلاث نسخٍ من قائمة الأدوات بلا رابط**

كانت القائمة والبصمات مكتوبةً في `CLAUDE.md` و`00-HANDOVER §3.4` وذاكرةِ المنفّذ. **وثلاث نسخٍ بلا رابطٍ تنجرف** — وقد انجرفت فعلاً: بوابة الاستئناف سقطت عند علاماتها السبع كلّها (`CHG-088`).

---

# §2 `gate.py` — **سلطةٌ واحدة** على ما يُقاس وبأي بصمة

| البند | التنفيذ |
| :--- | :--- |
| القائمة | الأدوات **الاثنتان والعشرون** بترتيبها · ومعها **البصمات الخمس** — في موضعٍ واحد |
| المطابقة | **حرفية** (`expected in out`) — لا «قريبة» ولا مُطبَّعة (`ن-8`) · وعند السقوط يُطبَع **ما وُجد فعلاً** لا «فشل» مجرَّد |
| التهيئة | `build_packs.py` ثم إعادة التسمية القانونية — **وتخلّفها يوقف التشغيل**: لا قياس على أساسٍ ناقص |
| الوثائق | **تُقابَل به ولا تُصدَّق** (§3/③) |

> **قاعدة تُقيَّد:** ما يُذكر برهاناً في قرار **يُشغَّل من موضعٍ واحد**. والنسخة الثانية من قائمةٍ حاكمة **دَينٌ** لا توثيق.

---

# §3 أربعة فحوصٍ **على البوابة نفسها** — وكلٌّ يقيس ما يعلنه

| # | الفحص | ما يقيسه · ولماذا |
| :--- | :--- | :--- |
| ① | **البصمات الخمس حرفياً** | القاعدة كانت تُقرأ بالعين وتُقارَن بالذاكرة — **فصارت مقيسة**. وزحزحةٌ صامتة صارت **إخفاقاً** |
| ② | **صفر أداة خارج البوابة** | تُكتشَف كل `parity_*`/`test_*`/`guard_*`/`verify_*` من القرص وتُقابَل بالقائمة — **فأداةٌ تُكتب ولا تُدرَج تُرصد**. وهو **الصنف** الذي انكسر ثلاثاً |
| ③ | **صفر انجراف وثائقي** | قائمة الأدوات والبصمات في `CLAUDE.md` و`00-HANDOVER` تُقابَل بالبوابة — **فانجراف الوثيقة يوقف البوابة كما يوقفها انجراف الكود**. وهو عيبُ هذه الجلسة كلّها، مقيساً |
| ④ | **صفر ملف مولَّد متبَع** | `CHG-054` نصٌّ لا يُصان بالانتباه: `git ls-files` يعرف المتبَع فعلاً · وعند غياب `git` **يُعلَن «متعذّر» لا «مُجتاز»** |

---

# §4 الحرس مُختبَر على فشله — أربع حقنات

**حرسٌ لا يُختبَر على فشله دعوى.** فحُقنت أربع مخالفات وأُعيدت الحالة بعد كل واحدة:

| الحقنة | النتيجة |
| :--- | :--- |
| ملف `test_zzz_injected.py` بلا إدراج | ❌ **رُصد** — «مكتشَفة وغير مدرَجة» |
| بصمة `K4` القديمة `7361580541033f31` أُعيدت إلى `CLAUDE.md` | ❌ **رُصد** — «بصمات لا تعرفها البوابة» |
| بصمةٌ متوقَّعة زُيّفت في `gate.py` | ❌ **رُصد** — «المتوقَّع … · الموجود `a0f83d78b8adbf53`» |
| `git add -f packs.js` | ❌ **رُصد** — «متبَعة خطأً» |

---

# §5 `CI` — وحدٌّ مصرَّح به

`.github/workflows/gate.yml` — على كل دفعة (`push`) وكل طلب دمج (`pull_request`) وبتشغيلٍ يدوي.

| البند | البيان |
| :--- | :--- |
| الخطوات | `checkout` · `python3` · `node` · **`python3 gate.py`** — ولا قائمة ثانية تُصان في `YAML` |
| **صفر اعتمادية** | لا `pip install` ولا `npm install` — والمشروع مبنيٌّ على ذلك بقصد، **والبوابة تحفظ الشرط بعدم خرقه** |
| الصلاحية | `contents: read` — أدنى ما يكفي |

## ① سابقةٌ تستحق التصريح — **أول مجلد تفرضه المنصّة**

تسطيح المستودع **حاملٌ** (`CLAUDE.md`): الأسماء القانونية مذكورةٌ حرفياً في البيانات والفهرس، والأدوات تُحلّ أشقاءها بمسارٍ نسبي. و`.github/workflows/` **مسارٌ تفرضه المنصّة** ولا خيار فيه — فليس إعادةَ تنظيمٍ لقاعدة المعرفة، **ولا سابقةً لإخراج أي ملف من التسطيح**. وسابقتُه `docs/` و`site/` (`DEC-250`).


## ③ تعايشُ طبقتين — **ولا سلطة واحدة تبتلع الأخرى**

`DEC-272` (جسر التسليم Claude–Codex) سبق هذا القرار بدقائق وله **حرسُه وسير عملِه**: `scripts/validate_ai_handoff.py` و`.github/workflows/ai-handoff-validate.yml`. فصار في المستودع **مساران في `Actions`**، كلٌّ يفرض طبقته:

| المسار | ما يفرضه |
| :--- | :--- |
| `gate.yml` (هذا القرار) | بوابة القبول — الأدوات الاثنتان والعشرون والبصمات الخمس |
| `ai-handoff-validate.yml` (`DEC-272`) | صحّة بروتوكول التسليم وملفات حالته |

**وفحص «صفر أداة خارج البوابة» مقصورٌ على جذر المستودع بمسوّغه:** حرس `DEC-272` **مفروضٌ بمساره الخاص** لا مهملاً؛ وضمُّه إلى قائمتي كان **توسّعاً في طبقةٍ ليست لي**. فالحدّ مُعلَن: بوابتي تحرس أدوات القياس في الجذر، وطبقةُ التسليم تحرس نفسها — **ولا واحدة تدّعي حراسة الأخرى**.

## ④ 🔎 تصادم ترقيم — **وتنازلٌ لا دفعٌ قسري**

أثناء تنفيذ هذا القرار دُفع `fcfe3d4` إلى الفرع نفسه، **مستهلكاً `DEC-272` و`CHG-090` والكود `142`** لقرار آخر. فكان أمامي طريقان: أن أدفع فوقه قسراً، أو أن أتنازل.

**تنازلتُ.** قرارُه يبقى بأرقامه، وهذا القرار صار `DEC-273`/`CHG-091`/`143`، **والترتيب الزمني في `01-MASTER` مصان** (صفُّه قبل صفّي لا بعده).

> **والسبب البنيوي يُقيَّد دَيناً لا يُهمَل: `DEBT-NUM-LOCK-01`** — حقل «الترقيم التالي» في `01-MASTER` **يُقرأ ولا يُحجَز**؛ فجلستان متوازيتان تقرآن الرقم نفسه فتختمان به قرارَين. **مطوَّق الآن بتنازلٍ يدوي، وحلُّه قرار مالك** (حجزٌ آلي أو ترقيمٌ بالفرع) — ولا يُبتّ باجتهاد المنفّذ.

---

## ② حدّان مصرَّح بهما — لا نقصٌ مسكوت عنه

| الحدّ | البيان |
| :--- | :--- |
| **شوط المتصفح خارج `CI`** | يلزمه تنزيل Chromium — واختيرَ إبقاؤه **يدوياً بتصريحه** بدل تضخيم البوابة باعتمادية. فما يُفحَص آلياً: 22 أداةً منها **حرسان يشغّلان المخرَجَين المولَّدَين** (`test_site_build` · `test_supervisor_build`) — والثاني **ينفّذ حزمة `Supervisor.html`** فعلاً |
| **تمكين `Actions`** | إن كانت معطَّلة في إعدادات المستودع فلن يعمل الملف — **وهو بيد المالك**. والملف مدفوع ونافذ متى مُكِّنت |

---

# §6 ما لم يُمسّ

- ❌ **طبقة القياس** — لا بند ولا معادلة ولا وزن (`instrument_pin`).
- ❌ **أي محرك أو تقرير أو حزمة** — لم يُمسّ سطرٌ منها: `DEC-273` **طبقةُ فرضٍ لا طبقةَ سلوك**.
- ❌ **البصمات الخمس** — `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` · `e32207bbb8853560` · `a0f83d78b8adbf53` **حرفياً**.
- ❌ **تسطيح قاعدة المعرفة** — ولا ملف نُقل ولا اسمٌ غُيِّر.

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| **الصنف** | ✅ **مُغلق** — البوابة صارت محروسةً كما صارت الأدوات محروسة |
| **السلطة** | ✅ **واحدة** — والنسختان الوثائقيتان **مقابَلتان آلياً** لا مصدَّقتَين |
| الحرس على فشله | ✅ **أربع حقنات · أربع إصابات** |
| البصمات الخمس | **حرفياً** — ومطابقتها صارت **مقيسة** لا مقروءة |
| الأدوات | **22/22** · صفر أداة خارج البوابة · صفر انجراف وثائقي · صفر مولَّد متبَع |

---

**[نهاية `143-CI-GATE` — `DEC-273` · الترقيم التالي `DEC-274` · `CHG-091` · الصنف مُغلق · ودَين `DEBT-NUM-LOCK-01` مفتوح مطوَّق]**
