# 00-CROSSMAP_K1_Code_Equivalence_Table.md

> **وضع التشغيل:** صياغة تشغيلية · **حالة الاعتماد:** [معتمد كوثيقة ربط بقرار مالك المشروع — الخيار ج] · **التصنيف:** [خارج النموذج — قابل للتنفيذ] (وثيقة ترجمة، لا تعدّل أي ترميز مصدري)
> **السند:** `12-CORE` §6 · `41-MEASURE` §5.1 · `30-CORE` المحور الأول (التناظر الدلالي K1↔K2 المؤكَّد) · قرار GAP-CROSS-K1CODE (الخيار ج)
> **الوظيفة:** جدول تكافؤ رسمي يترجم بين ترميزَي مفاتيح K1 دون ترجيح أيهما (البند 11). كل طبقة تستخدم ترميزها الأصلي وتُترجم عبر هذا الجدول.

---

# القسم 1: مبدأ التكافؤ الحاكم

**[معتمد — الخيار ج]:**
1. **لا ترميز سيادي** — `12-CORE` و`41` كلاهما صالح في وثيقته (البند 11: لا ترجيح دون سند أسبقية).
2. الترجمة **ثنائية الاتجاه 1:1** عبر هذا الجدول حصراً.
3. أي أتمتة تربط K1 تمرّ بهذا الجدول (طبقة ترجمة صريحة).
4. التعارض **مصطلحي لا مفهومي** — المعنى الجامع واحد، مؤكَّد من `30-CORE`.

---

# القسم 2: جدول التكافؤ الرسمي

| # | `41-MEASURE` §5.1 [معتمد] | `12-CORE` §6 [معتمد — مطابَق بالمصدر] | الاسم المعماري (`12-CORE`) | تناظر K2 [معتمد] |
| :--- | :---: | :---: | :--- | :---: |
| 1 | `TRU` | `K1-TS` | البحث عن الحقيقة | A التحليلي |
| 2 | `EFF` | `K1-PE` | الفاعلية العملية | R الواقعي |
| 3 | `STR` | `K1-SO` | النظام والهيكلة | O المنظم |
| 4 | `CON` | `K1-SC` | الاتساق والأمان | C المحافظ |
| 5 | `BEL` | `K1-SA` | الانتماء والتواصل | S الاجتماعي |
| 6 | `EMO` | `K1-EC` | الوصال الوجداني | E المتفهم |
| 7 | `VIS` | `K1-VD` | الرؤية والبوصلة | St الاستراتيجي |
| 8 | `CRE` | `K1-PI` | الإمكان والإبداع | H التصوري |

> **⚠️ تصحيح تدقيقي (مطابقة `12-CORE` §6):** الصفّان 3 و4 كان رمزاهما `K1-SC`/`K1-SO` **متبادلين** في النسخة المشتقة الأولى. صُحّحا بالمصدر: `K1-SO`=النظام (O) · `K1-SC`=الأمان (C). أكّده عمود الهشاشة (`K1-SO`→بيروقراطية/قوالب=عمى O · `K1-SC`→تكلس/مقاومة ترقية=عمى C).

## 2.1 حالة الوسم لكل عمود
| العمود | الحالة |
| :--- | :--- |
| `41` §5.1 (`TRU…CRE`) | ✅ [معتمد] — منصوص في خط الأساس المُستلم |
| المعنى الجامع | ✅ [معتمد] — مؤكَّد من `30-CORE` المحور الأول (المخرجات الصافية) |
| تناظر K2 | ✅ [معتمد] — موثق (TRU↔A … CRE↔H) |
| `12-CORE` (`K1-TS…PI`) | ✅ **[معتمد]** — مُطابَق بنصّ `12-CORE` §6 (مصفوفة الترميز، الأسطر 131–138)؛ صُحّح تبادل `K1-SC`/`K1-SO` في الصفّين 3/4 |

---

# القسم 3: قاعدة الاستخدام

**[مشتق — إجرائي]:**
- **طبقة القياس والمحرك** (`41`, `M-06`, `56-REPORT-ENGINE`): تستخدم ترميز `41` (`TRU…CRE`) — الوضع القائم، غير متأثر.
- **طبقة الدستور** (`12-CORE`): تستخدم ترميزها `K1-XX`.
- **أي ربط K1↔K2 أو تقرير يمس K1**: يترجم عبر §2 صراحةً، مع ذكر الترميزين عند أول ورود.
- **قفل النطاق:** محرّك v1 نطاقه K2؛ هذا الجدول يفعّل مستقبلاً عند توسّع المحرك لـ K1 — جاهز سلفاً.

---

# القسم 4: سجل الفجوات

| كود | البيان | الخطورة | القرار | المطلوب |
| :--- | :--- | :--- | :--- | :--- |
| **GAP-CROSS-K1CODE** | ✅ **محسومة بالخيار ج** — جدول تكافؤ رسمي؛ الالتباس مغلق بترجمة صريحة بلا ترجيح | ~~متوسطة~~ محسومة | معتمد | — |
| **بقية مصطلحية** | ✅ **مغلقة** — ترميز `12-CORE` §6 مُطابَق بالمصدر (تصحيح تبادل `K1-SC`/`K1-SO`)؛ العمود مرفوع إلى [معتمد] | مغلقة | — |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| مبدأ التكافؤ (بلا ترجيح — البند 11) | ✅ |
| جدول 1:1 ثنائي الاتجاه | ✅ |
| أعمدة `41`/المعنى/K2 | ✅ [معتمد] |
| عمود `12-CORE` | ✅ [معتمد] — مُطابَق بالمصدر (تصحيح تدقيقي مثبت) |
| صفر تعديل على وثيقة تأسيسية | ✅ |
| **حالة الوثيقة** | ✅ **وثيقة ربط معتمدة مكتملة — GAP-CROSS-K1CODE محسومة · كل الأعمدة [معتمد]** |
