# 122-SITE — `DEC-250`: الموقع العام وتسمية «الكشاف»

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | تدشين موقع علني للمشروع يضم استبيان «الكشاف» التفاعلي كاملاً |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-10 |
| **الأثر على المحرّكات** | **صفر** — استهلاك للقراءة فقط · البصمات الثلاث ثابتة |
| **التغيير المرافق** | `CHG-068` |

---

# §1 بنود القرار

## ① التسمية العلنية

**«الرواحل»** اسم المشروع والإطار النظري (مختوم سلفاً في `01-MASTER §1`) · **«الكشاف»** يُعتمد بهذا القرار اسماً علنياً عربياً للأداة المسحية (الاستبيان ومولّد التقريرين). قبل هذا القرار كان `Kashaf` اسماً كودياً للملفات لا غير — التسمية العربية تأسيس جديد لا توثيق قائم.

## ② بنية الموقع — استثناء ضيّق من `CHG-054`

| المكوّن | الحالة |
| :--- | :--- |
| `site/` | مصدر مودَع: محتوى الصفحات · القالب · الأنماط · تطبيق الاستبيان · فحوصات البناء · مكتبات مضمومة |
| `build_site.py` | مولّد في الجذر على عرف `build_supervisor_html.py` — **البناء هو الاختبار**: كل توكيد يسقط يمنع الكتابة |
| `docs/` | **ناتج منشور لا وثيقة معرفة**: يودَع لأن GitHub Pages لا يخدم إلا المودَع، لا يُحرَّر يدوياً، ولا تستشهد به أي وثيقة. حزم المحتوى المضمّنة داخل `docs/kashaf.html` لها مقام `Kashaf.html` المحادثاتية نفسه — وسيلة تسليم أداة خارج جرد المصادر |
| `packs.js` الجذري | **يبقى خارج الإيداع** — قاعدة `CHG-054` الأصلية نافذة بلا مساس |

## ③ التطبيق العلني لبروتوكول `DEC-035` — قراءتان تشغيليتان تُختمان هنا

النص المختوم (`40-MEASURE:23`) لم يفصّل أمرين، ويُثبَّتان بهذا القرار لا باجتهاد صامت:

| القراءة | التثبيت |
| :--- | :--- |
| ترتيب عرض الجُمل في مرحلة التقييم | ترتيب البنود، الجملة (أ) ثم (ب) لكل بند |
| حدود الكتل العشر | مدايات متصلة على 1..94: أربع كتل ×10 ثم ست ×9 |

وتنفيذ الحدود الصلبة: التقييم بأزرار ١..٦ حصراً (الواجهة نقطة إنفاذ المدى) · لا يظهر أثر الاختيار السابق في شاشة التقييم · التراجع داخل المرحلة الواحدة فقط · لا تقرير على إجابات ناقصة (`missingItems` قبل التوليد).

## ④ خريطة $K_3$ — نقل بقيد مزدوج

لا خريطة بنود لـ$K_3$ في المحرك (`GAP-ITEM-K3-01`)، فنُقلت من `41 §5.3` نقلاً محكوماً: النسخة المنقولة في `build_site.py` تُطابَق آلياً عند كل بناء مع فكٍّ مستقل لجدول الوثيقة، وخريطة $K_2$ لا تُنقل أصلاً بل تُشتق من `K2.ITEM_MAP` في المحرك (مصدر الحقيقة الواحد) وتُطابَق مع `41 §5.2`. فوق ذلك يتحقق مسار `الإجابات→sp` بثلاث طرق مستقلة (الجسر · الحساب اليدوي · `scoreFromRaw`) على متجه حتمي.

## ⑤ المكتبات المضمومة — سابقة `DEC-249`

React 18.3.1 (UMD إنتاجي) ونظيره DOM يودَعان في `site/vendor/` ببصمتين مثبَّتتين في `VENDOR.md` يتحقق منهما `build_site.py` قبل كل تجميع — القبول بالبصمة لا بالمراجعة البصرية، ولا تحديث إلا بـ`CHG` جديد.

## ⑥ قيود المحتوى العلني — فحص بناء لا مراجعة تحرير

| القيد | الإنفاذ |
| :--- | :--- |
| لا نسبة مئوية في أي نص مرئي | فاحص آلي على كل صفحة · سجلّ `ح-5` هو مرجع الاستثناء الوحيد |
| لا مفردات إكلينيكية ولا ادّعاء صدق (`DEC-246`) | قائمة حظر آلية · وجُمل المحرك المختومة التي تنفي المفردة تُجرَّد بنمط سجلّ ح-5 |
| اقتباسات المصادر لا تنحرف | مراسٍ حرفية تُطابَق مع ملفات المصدر عند كل بناء |
| التقارير النموذجية | من `## ١` حتى «نهاية التقرير.» حصراً — رأس الحوكمة (وفيه `SP` الخام) والختم يسقطان بنيوياً · وسوم `r10` الإفصاحية تبقى (`DEC-219`) |
| صفحة الاستبيان | قفل صفر شبكة (`CPL-08A-03`/`DEC-110`) · `verifyPacks()` عند الإقلاع · لا لوحة K1/K4 (`DEC-186`) · لا خادم — الإجابات لا تغادر الجهاز |
| بطلان المحفوظ عند تغيّر الأداة | بصمة بناء (البنود+الخرائط) تُبطل إجابات محفوظة بأداة أقدم — تعميم درس `GAP-Q-01` |

---

# §2 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| توكيدات البناء | كلها مجتازة — 94 بنداً · كناري الأقواس (35·41·47·64) · الخرائط 8×7 و5×11 بلا تقاطع · 282 خطوة |
| فحوصات JS | تكافؤ ثلاثي المسار لـ$K_2$ · مزدوج لـ$K_3$ · التقريران يجتازان الحارسين |
| اختبار المتصفح | 39/39 — التدفق الكامل · لا نسبة غير مسجَّلة في المعروض · قفل الشبكة حي · لوحة K1/K4 غائبة |
| الأدوات الستّ عشرة | 16/16 · البصمات الثلاث حرفياً: `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` |

---

**[نهاية `122-SITE` — `DEC-250` · الترقيم التالي `DEC-251` · `CHG-068`]**
