# 00-PATCH_DEC-191/192 — إعادة التأسيس ومزامنة وسم الانحدار

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **معتمد ومنفَّذ** (`DEC-190` · `DEC-191`) · **نصّ رقعة** (`DEC-192`) |
| **السند** | `DEC-190/191/192/193` (مختومة) · `84-K2-DEFECT` v1.1 §8 |
| **الإصدار** | v1.0 |

---

# القسم 1: تصويب خطة §8 — `golden_k2.json` لم يكن يحتاج إعادة تأسيس

خطة `84 §8/4` نصّت على «إعادة تثبيت `golden_k2` مرجعاً للانحدار». **الفحص عدّل الحكم:**

| الطبقة | ما يحرسها | حالتها بعد الإصلاح |
| :--- | :--- | :---: |
| **الحساب** (`raw`→`sp` · تصنيف · `ت-8` · `ت-7` · `FRIC-02` · عقد المدخل · العزل) | `golden_k2.json` + `test_golden_k2.py` | ✅ **لا انحدار** — سليمة أصلاً ولم يمسّها الإصلاح |
| **السحب النصّي** (المدخل ← العنوان) | **لا شيء** | 🔴 **هنا مرّ `DEF-K2-01`** |

**الحكم المصوَّب:** `golden_k2.json` يبقى كما هو — **الناقص لم يكن مرجعاً بل اختباراً**. أُنشئ `test_report_k2.py` لسدّ الطبقة غير المحروسة.

> هذا نفسه تطبيق `ن-4` (سؤال البقاء): المشكلة لم تكن في المرجع، بل في غياب بوابة على طبقة كاملة.

---

# القسم 2: ما نُفِّذ

| # | البند | الحالة |
| :--- | :--- | :---: |
| 1 | `k2_report.py` — استدعاء وظيفي + حارس `_CARD` + `validate_slots()` | ✅ |
| 2 | إعادة توليد `REPORT_P-001/005/006/007_Full` بالمحرك المُصلَح | ✅ |
| 3 | `test_report_k2.py` — اختبار انحدار طبقة السحب | ✅ |
| 4 | تشغيل `test_golden_k2.py` بعد الإصلاح | ✅ **لا انحدار** |
| 5 | **اختبار الحارس على العيب المعلوم** (شرط `ن-7`) | ✅ **يُخرج ١٠ انحرافات على الكود القديم** |

## 2.1 برهان الحارس — النتيجة على الكود **قبل** الإصلاح

| المركز | «مركزك» — المدخل الثاني والثالث | «بصمتك» |
| :--- | :--- | :--- |
| `A` | ✅ المحرك · الموقع | ✅ البصمة |
| `R` | ✅ المحرك · الموقع | ❌ «الملاذ الإبداعي عند الصفر» |
| `C` | ❌ «سلطة الاعتراض» · «قراءة الآخرين» | ❌ «ما لا تعنيه هذه العدسة» |
| `O` | ❌ «المنطق الاقتصادي» · «قراءة الآخرين» | ❌ «حالة الضغط القصوى» |
| `S` | ❌ «المغلِّف لا المصدر» · «قراءة الآخرين» | ❌ «ما لا تعنيه هذه العدسة» |
| `E` | ❌ «الرادار لا يُستغَل» · «قراءة الآخرين» | ❌ «ما لا تعنيه هذه العدسة» |
| `St` | ✅ المحرك · الموقع | ✅ البصمة |
| `H` | ❌ «المخرج إمكان لا قرار» · «قراءة الآخرين» | ❌ «ما لا تعنيه هذه العدسة» |

**مطابق للتقدير في `84 §4.1` حرفياً:** «مركزك» ٥/٨ · «بصمتك» ٦/٨.
وبعد الإصلاح: **٨/٨ سليمة · ٤/٤ حالات حقيقية · صفر ازدواج مداخل**.

---

# القسم 3: نصّ رقعة `DEC-192` — جاهز للّصق

**يُضاف حيثما ورد وسم «صفر انحدار» في `01-MASTER` و`02-MASTER` و`00-SESSION-CLOSE_2026-07-27`:**

```
> **تحفّظ `DEC-192`:** وسم «صفر انحدار» الصادر قبل 2026-07-30 يخصّ **طبقة الحساب** حصراً (raw→sp · التصنيف · ت-8 · ت-7 · FRIC-02 · العزل) — وهي سليمة ومثبَتة. أما **طبقة السحب النصّي** فلم تكن محروسة، ومرّ خلالها `DEF-K2-01` على أربعة تقارير. صُحِّح بـ`DEC-190/191`، وأُنشئ لها `test_report_k2.py`.
```

**ويُضاف إلى سجل القرارات:**

```
| `DEC-190` | إصلاح `DEF-K2-01` — استدعاء المداخل بالوظيفة لا بالرقم + حارس `_CARD` يوقف الإصدار | `84-K2-DEFECT` v1.1 |
| `DEC-191` | إعادة توليد التقارير الأربعة + إنشاء `test_report_k2.py` لطبقة السحب · `golden_k2.json` يبقى كما هو | هذه الرقعة |
| `DEC-192` | تصويب وسم «صفر انحدار» بالتحفّظ أعلاه | هذه الرقعة |
| `DEC-193` | الفحص 10 (مطابقة العنوان للمضمون) + شرط تفعيل ثانٍ (مخرج آلي · فحوص 1·4·10) + `ن-7` | `00-PATCH_CRITIQUE-GATE` |
```

---

# القسم 4: وسم النسخ السابقة

النسخ الأربع السابقة **تُحفَظ ولا تُحذف**، ويُصدَّر كلٌّ منها بـ:

```
> ⛔ **[مصابة — `DEF-K2-01` — لا يُستشهد بها]** · نُسخت بـ`DEC-191`. النسخة النافذة مؤرّخة 2026-07-30.
```

والنسخ الجديدة مُصدَّرة بـ:

```
> ✅ **مُعاد التوليد بعد `DEF-K2-01` — `DEC-190/191`** · هذه النسخة تنسخ ما قبلها.
```

---

# القسم 5: الحالة بعد الختم

| البند | الحالة |
| :--- | :---: |
| $K_2$ — طبقة الحساب | ✅ محروسة (`test_golden_k2`) |
| $K_2$ — طبقة السحب | ✅ **محروسة حديثاً** (`test_report_k2` + `validate_slots`) |
| $K_3$ — الحارس | ✅ مبنيّ (`k3_guard.py`) — **بانتظار `DEC-194`** |
| $K_3$ — `DEF-K3-01` | 🟠 **مُشخَّص وغير مُصلَح** — بانتظار `DEC-195` (أ/ب/ج) |
| $K_3$ — وسم `OUT` الصامت | 🟡 بانتظار `DEC-196` |
| المرحلة ① (النقل إلى JS) | 🟢 $K_2$ جاهز · 🔴 $K_3$ محجوب بـ`DEC-195` |

> **حاجز صريح:** نقل $K_3$ إلى JS قبل حسم `DEC-195` **يُصنِّع `DEF-K3-01`** داخل الأداة الإنتاجية — وهو الخطأ نفسه الذي تفادته `DEC-190` في $K_2$.

---

> **[نهاية الرقعة — v1.0 · البنود 1–2 منفَّذة · القسمان 3–4 جاهزان للّصق]**
