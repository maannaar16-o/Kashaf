# 62-K3-COUPLING-REGISTER-C_Perception_Feed.md
## سجل الاقتران الفئوي — المرحلة (ج): حافة التغذية الإدراكية (`EP–IR`) + قراءة التعاقب — **إغلاق السجل عند 28 خلية**

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `62-K3-COUPLING-REG-C` — امتداد `62-REG` v1.0 و`62-REG-B` v1.0 |
| **الإصدار** | **v1.0 — ✅ معتمد** (`DEC-110`) |
| **الحجم** | **4 خلايا** (`EP–IR`) + قسم قراءة التعاقب (غير خلوي) |
| **السند** | `DEC-109` (تقليص السجل 40←28) · `DEC-108` v2 (عبور الأثر بالاتجاهين عبر المخرج) · `50-DEPTH-K3-EP §1.4/§1.5/§2/§4` (`DEC-067`) · `M3-07 FR-01/FR-05` · `DEC-102/106/107` |
| **الحدّ الحاكم** | البنية السباعية · `ح-1…ح-10` نافذة · صفر عتبات مستحدثة · لا مقارنة مقدارية |

---

# القسم 0: القرارات المستوعَبة

| القرار | النص |
| :--- | :--- |
| **`DEC-109`** | المرحلة (ج) = **4 خلايا (`EP–IR`) حصراً**؛ السجل الكامل **28 خلية لا 40**. تُلغى خلايا `EP–BI`/`EP–CF`/`EP–ST` المخطَّطة في `DEC-100 §6` — لا حافة مباشرة تسندها (`EP §1.4`: المصبّ `IR` حصراً). أثر `EP` على البقية **تعاقب يُقرأ قراءةً**، لا اقتران يُبنى خلايا. |
| **`DEC-108` v2** | ما يعبر عبر المخرج هو **الأثر بالاتجاهين** (رافعاً أو متدهوراً)؛ وَسْم الثقة لا يعبر — `EP` مُضاعِف داخل `K3` حصراً. الجداران (`EP ↮ K4` · `K3 ↮ K2-E`) صامدان. |

---

# القسم 1: خصوصية هذه الحافة — لماذا تختلف خلاياها عن الـ24 السابقة **[مشتق]**

| الفارق | الحافات السابقة | حافة `EP–IR` |
| :--- | :--- | :--- |
| **الاتجاه** | خدمة نازلة + عودة صاعدة | **أحادية صِرف**: تسليم `EP→IR` بلا تغذية عكسية بنيوية موثقة (`EP §4` نصاً: «لو وُجدت آلية عكسية فهي فجوة، لا تُخترع») |
| **المادة المنقولة** | شحنة / مخرج / حمل | **تمثيل إدراكي** — جودة معلومة، لا طاقة |
| **بصمة الطرف الأول** | اتجاه تنظيمي | `EP` **محايدة الاتجاه** — بصمتها **الدقة** (`§1.5`) |
| **منطق الخلية** | تبادل أدوار | **دقة المُدخل × قوة المعالجة** |
| **تحفّظ الأصالة** | على الطرفين | **أحادي الطرف**: `IR` فقط (`FR-01`) — لا قطب زائف مسجَّلاً لـ`EP` (`FR-05` مُعلَّق لا يُدَّعى) |

**قاعدة `ح-11` المقترحة لهذه الحافة:** الخلية تصف **جودة التغذية وأثرها على شرط عمل `IR`** — يُحظر وصفها تدخلاً تنظيمياً من `EP` (قفل `EP ⚔️ IR`: وعاء استقبال بلا تدخل)، ويُحظر اشتقاق حكم ثقة على التقرير من داخل الخلية (وَسْم الثقة شأن طبقة العرض حصراً — `64 §5`).

---

# القسم 2: الخلايا الأربع

## 2.1 `CPL-EP.IR-SS` — التقاط دقيق × مركز في نطاق قدرة عالية

| الحقل | البيان |
| :--- | :--- |
| **① الكود** | `CPL-EP.IR-SS` |
| **② العائلة** | ② التغذية الإدراكية (Perception Feed) |
| **③ القراءة البنيوية** | **شرط الصحة متوافر عند المُدخل** (`EP §2`: الملاحظة الحادة شرط أولي لصحة كل التنظيم اللاحق): تمثيل صادق دقيق يصل مركزاً يضبط على أساسه — تنظيم موزون على بيانات صحيحة. الأثر الرافع بصيغته المعتمدة: **تصل `IR` مادة صادقة**. |
| **④ نوع العلاقة** | **تعزيز** — بقيد `DEC-108`: رفعُ جودة مُدخل، لا رفع قدرة |
| **⑤ الكلفة** | لا كلفة موثّقة على هذه الحافة. |
| **⑥ ما لا يُدَّعى** | ❌ **«`EP` عالية ترفع `IR`»** — الدقة تحسّن المُدخل لا القدرة (قيد ①/`DEC-108`). ❌ **تحفّظ الأصالة على طرف `IR` وحده** (`FR-01`: الكبت يرفعها) — طرف `EP` بلا تحفّظ (`FR-05` مُعلَّق لا يُدَّعى). ❌ لا يُستنتج أداء جيد في `BI`/`CF`/`ST` — التعاقب يُقرأ في §3 لا هنا. ❌ لا حكم ثقة من داخل الخلية (`ح-11`). |
| **⑦ الوسم + شرط الاستدعاء** | `[مشتق]` — `EP > 70%` **و** `IR > 70%`. **إلزامي:** كتلة `M3-07` (سؤال `IR` فقط — أول خلية بتحفّظ أحادي الطرف). |

## 2.2 `CPL-EP.IR-SW` — التقاط دقيق × مركز محدود الضبط

| الحقل | البيان |
| :--- | :--- |
| **① الكود** | `CPL-EP.IR-SW` |
| **② العائلة** | ② التغذية الإدراكية |
| **③ القراءة البنيوية** | **وعي بلا إمساك:** التمثيل يصل صادقاً ودقيقاً («أعرف تماماً ماذا يحدث لي شعورياً») والمركز لا يضبط على أساسه («ولا أستطيع تعديله»). عطل المعالجة لا عطل التغذية — المادة سليمة والضبط محدود. **هنا يتجلى قيد ② من `DEC-108`:** الرفع مشروط لا تلقائي — مادة صادقة لا تُنتج وحدها تنظيماً. |
| **④ نوع العلاقة** | **محايد على الحافة** (`ح-10`) — التغذية أدّت وظيفتها كاملة؛ الخمود التالي بضعف المستهلِك لا بخلل القناة |
| **⑤ الكلفة** | لا كلفة على الحافة ذاتها؛ الشحنة غير المضبوطة تُقرأ في حافات `IR` (المحور + التحميل)، لا هنا. |
| **⑥ ما لا يُدَّعى** | ❌ **لا يُسند ضعف الضبط إلى `EP`** — التغذية سليمة («الجذر ≠ موضع الظهور»). ❌ لا يُدَّعى أن الوعي الدقيق **يخفف** الشحنة — الملاحظة استقبال بلا تدخل (قفل `EP ⚔️ IR`). ❌ لا يُقرأ التناقض الظاهر («يعرف ولا يضبط») تعارضَ بيانات — هو توصيف موزّع على حافتين (`64 §4.3`). |
| **⑦ الوسم + شرط الاستدعاء** | `[مشتق]` — `EP > 70%` **و** `IR ≤ 50%`. لا كتلة `M3-07` من هذه الخلية (لا طرف متحفَّظ عليه مرتفع). |

## 2.3 `CPL-EP.IR-WS` — التقاط محدود × مركز في نطاق قدرة عالية

| الحقل | البيان |
| :--- | :--- |
| **① الكود** | `CPL-EP.IR-WS` |
| **② العائلة** | ② التغذية الإدراكية |
| **③ القراءة البنيوية** | **«التنظيم الأعمى» المنصوص** (`EP §1.4` نقطة الانهيار عند الغياب): مركز قادر يعمل على مادة متأخرة أو مشوَّشة — ضبط قوي لكن عشوائي التوقيت/الموضع، لأن الإشارة تصل ناقصة أو بعد فوات أوانها (التأخر الإدراكي — `§1.7.2`). القدرة سليمة والمُدخل معطوب. |
| **④ نوع العلاقة** | **تثبيت بكلفة** — الضبط يقع لكن على غير هدى دقيق |
| **⑤ الكلفة** | تنظيم متأخر/في غير موضعه ← ضغط إضافي على `BI` (حد الفشل المتسلسل — `EP §4`: كبح مبني على تقدير غير دقيق للشدة) — **يُقرأ التعاقب في §3، لا يُبنى خلية**. |
| **⑥ ما لا يُدَّعى** | ❌ **قوة `IR` لا تعوّض دقة الالتقاط** (احتواء ≠ تعويض — ح-6): الضبط لا يُصلح تمثيلاً معطوباً. ❌ تحفّظ `FR-01` قائم على طرف `IR` المرتفع. ❌ لا يُدَّعى أن ضعف `EP` **سيظهر حتماً** — احتمال لا حتمية. |
| **⑦ الوسم + شرط الاستدعاء** | `[مشتق]` (مسنود بنص «التنظيم الأعمى» المعتمد) — `EP ≤ 50%` **و** `IR > 70%`. **إلزامي:** كتلة `M3-07` (سؤال `IR`). |

## 2.4 `CPL-EP.IR-WW` — التقاط محدود × مركز محدود الضبط

| الحقل | البيان |
| :--- | :--- |
| **① الكود** | `CPL-EP.IR-WW` |
| **② العائلة** | ② التغذية الإدراكية |
| **③ القراءة البنيوية** | عطل مزدوج على أول حلقتين في السلسلة التعاقبية: إشارة تصل متأخرة/مشوَّشة إلى مركز لا يضبط. المادة الخام تمرّ إلى بقية السلسلة بلا التقاط صادق ولا معايرة — أثقل نقطة انطلاق للتعاقب (§3). |
| **④ نوع العلاقة** | **تفاقم** |
| **⑤ الكلفة** | مُصدَّرة بالكامل إلى ما بعد الحلقتين؛ لا احتواء في أول السلسلة. |
| **⑥ ما لا يُدَّعى** | ❌ لا يُسند الجذر لأحد الطرفين — العطل موزّع على أول السلسلة. ❌ لا مسار زمني محتوم ولا انهيار. ❌ لا سمة ثابتة (`P = C + G`) — النطاقان حالتا تشغيل. ❌ صفر لغة عيادية. |
| **⑦ الوسم + شرط الاستدعاء** | `[مشتق]` — `EP ≤ 50%` **و** `IR ≤ 50%`. لا كتلة `M3-07`. |

---

# القسم 3: قراءة التعاقب — أثر `EP` فيما بعد `IR` **[مشتق — بديل الخلايا الملغاة]**

> **الطبيعة:** هذا القسم **ليس خلايا** ولا يُستدعى بشرط نطاقَي زوج. هو **قاعدة قراءة** تُفعَّل في طبقة التركيب (`64`) حين تكون `EP ≤ 50%` **و** توجد خلايا مفعَّلة أخرى في الملف.

## 3.1 القاعدة

عند `EP ≤ 50%`، كل خلية مفعَّلة طرفُها الأول `IR` **تُقرأ مع لاحقة التعاقب**: مخرج `IR` الذي يصل `BI`/`CF` — والحمل الذي ينزل على `ST` — مبنيان على تمثيل محدود الدقة منذ المنبع. الأثر يصل **عبر المخرج** متدهوراً (`DEC-108`)، لا عبر حافة مباشرة من `EP`.

وبالتماثل المعتمد (`DEC-108` v2): عند `EP > 70%`، يجوز للتركيب إثبات أن **شرط الصحة متوافر عند المنبع** — دون ادّعاء أن ذلك يرفع أداء أي عنصر لاحق (قيد ②: الرفع مشروط لا تلقائي).

## 3.2 حدود القراءة

1. **لا خلية ولا شرط استدعاء مستقل** — لاحقة تُلحق بقراءات قائمة، لا قراءة تنشأ من عدم.
2. **لا قفز فوق `IR`:** يُحظر ربط `EP` بـ`BI`/`CF`/`ST` مباشرة في أي صياغة («ضعف ملاحظته أضعف كبحه» ❌) — الصياغة الشرعية تمرّ بالوسيط («وصل الكبحَ تقديرٌ غير دقيق للشدة، منبعه التقاط محدود ثم ضبط على أساسه»).
3. **منفصلة تماماً عن وَسْم الثقة:** هذه قراءة **أثر** في المتن؛ وَسْم الموثوقية (`EP ≤ 50%` ← «قراءة غير حاسمة») شأن طبقة العرض وحدها (`ح-3` · `64 §5`). **لا يُدمجان في عبارة واحدة.**
4. **عند حدود `K4` تقف:** التعاقب يُقرأ داخل `K3` حتى مخرجها؛ أي نسبة أثر — رافعة أو متدهورة — إلى `K4` مجمّدة بـ`RSK-005`.
5. الجدار `K3 ↮ K2-E` صامد — لا صياغة تربط دقة `EP` بقراءة نوايا الآخرين.

---

# القسم 4: جدول الحصر النهائي — السجل الكامل 28 خلية

| المرحلة | العائلة | الخلايا | الحالة |
| :--- | :--- | :--- | :--- |
| (أ) | ① المحور: `IR–BI` · `IR–CF` | 1–8 | ✅ `DEC-102` |
| (ب) | ④ الجانبي: `BI–CF` | 9–12 | ✅ `DEC-107` |
| (ب) | ③ التحميل: `{IR,BI,CF}–ST` | 13–24 | ✅ `DEC-107` |
| (ج) | ② التغذية الإدراكية: `EP–IR` | 25–28 | 🟡 هذه الوثيقة |

| # | الكود | النطاقان | نوع العلاقة | الكلفة | `M3-07` |
| :--- | :--- | :--- | :--- | :--- | :---: |
| 25 | `CPL-EP.IR-SS` | قوي × قوي | تعزيز (جودة مُدخل) | — | ✅ أحادي (`IR`) |
| 26 | `CPL-EP.IR-SW` | قوي × ضعيف | محايد على الحافة | — | — |
| 27 | `CPL-EP.IR-WS` | ضعيف × قوي | تثبيت بكلفة | تنظيم أعمى | ✅ (`IR`) |
| 28 | `CPL-EP.IR-WW` | ضعيف × ضعيف | تفاقم | مُصدَّرة للسلسلة | — |

**التغطية الختامية للأنواع الخمسة عبر السجل (28):** تعزيز 6 · تثبيت (بأصنافه) 7 · تهذيب 2 · تفاقم 9 · محايد على الحافة 3 · مشروط 1 (`BI.ST-SS`).

---

# القسم 5: الفجوات

| الكود | البيان | الخطورة | المطلوب |
| :--- | :--- | :--- | :--- |
| **`GAP-CPL-04`** | القاعدة `ح-11` (خلية التغذية تصف جودة المُدخل لا تدخلاً تنظيمياً) مستحدثة في هذه الوثيقة — تحتاج اعتماداً ضمن `DEC-110` | منخفضة | تُعتمد مع الوثيقة |
| `GAP-M3-FR-05` (موروثة) | قطب `EP` الزائف (يقظة مفرطة) مُعلَّق — إن حُسم يوماً بالإثبات، يُضاف تحفّظ على طرف `EP` في الخلايا 25/26 | متوسطة | قرار سيادي أو دليل ميداني — **لا يُدَّعى الآن** |
| `GAP-M3-01-01` (موروثة) | لا تمسّ هذه الحافة (أحادية بلا عودة) — تبقى على حافات العودة السابقة | متوسطة | كما هي |
| `RSK-005` (مشروعية) | تحدّ قراءة التعاقب عند مخرج `K3` (§3.2/4) | عالية مؤجلة | جدول الحدود الصارم |

---

# القسم 6: محضر الاعتماد (`DEC-110`)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | الخلايا الأربع (`EP–IR`) بمحتواها | ✅ معتمد |
| 2 | القاعدة `ح-11` (جودة تغذية لا تدخّل تنظيمي) | ✅ معتمد — `GAP-CPL-04` مغلقة |
| 3 | قسم قراءة التعاقب (§3) قاعدةً لطبقة التركيب — بحدوده الخمسة | ✅ معتمد |
| 4 | التحفّظ الأحادي الطرف في خليتي 25/27 (سؤال `IR` دون `EP`) | ✅ معتمد |
| 5 | **إعلان اكتمال سجل الاقتران: 28/28 — وإغلاق الخطوة ③** | ✅ معتمد |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| الخلايا المنتَجة | 4/4 للمرحلة (ج) · **28/28 من السجل الكامل** |
| البنية السباعية | ✅ الحقل ⑥ حاضر بلا استثناء |
| الجداران | ✅ `EP ↮ K4` · `K3 ↮ K2-E` — لا صياغة تخترقهما |
| `FR-05` | ✅ غير مُدَّعى — تحفّظ أحادي الطرف موثَّق |
| صفر عتبات/أوزان/مقادير | ✅ ملتزم |
| `GAP-CPL-04` | ✅ مغلقة — `ح-11` معتمدة |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة (`DEC-110`)؛ سجل الاقتران مكتمل 28/28 · الخطوة ③ مُغلقة** |
