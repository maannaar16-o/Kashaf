# 63-K3-URS-REFRAME_Directive_For_Step4.md
## توجيه معماري معتمد — إعادة تأطير البصمة التنظيمية الجامعة من «ملف قوة» إلى «سجل اقترانات مفعَّلة»

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `63-K3-URS-REFRAME` |
| **الإصدار** | v1.0 |
| **حالة الاعتماد** | ✅ **معتمد بقرار مالك المشروع — `DEC-101`** (توجيه ملزم للخطوة ④) |
| **طبيعة الوثيقة** | **توجيه** لا بناء. **لا تُنتج بصمة، ولا ترفع حجباً، ولا يستهلكها المحرك.** |
| **السند** | `60-K3-TOPOLOGY §2-ب` (`DEC-098`) · `58-K3-VISION` `VR-2/VR-3/VR-5` (`DEC-095`) · `61-COUPLING-METHOD` (`DEC-100`) · `56-K3-URS` v2.1 |
| **الحدّ الحاكم** | طبقة **تفسير** حصراً · طبقة القياس لا تُمسّ · **حجب البناء التنفيذي لـ`URS` قائم حتى قرار مستقل** |

---

# القسم 1: نص القرار

> **`DEC-101`:** تُعاد صياغة `URS` تنفيذياً بوصفها **مجموعة الاقترانات الفئوية المفعَّلة في ملف الفرد** — أي **نمط معالجة**، لا **ملف درجات ساكن**. تحلّ هذه الصياغة محل بنية «ملف القوة الخماسي» بوصفها **آلية البناء**، مع بقاء القوى الخمس **مُدخَلات** تحدّد أي الخلايا تُستدعى.

**التصنيف الاستراتيجي:** `[داخل النموذج — معتمد]`. القرار يمسّ محوراً تفسيرياً سيادياً على مستوى الدائرة، وقد ورد اعتماده صراحةً من مالك المشروع.

---

# القسم 2: السند — لماذا هذا اشتقاق لا استحداث

| السند | النص/المضمون | الدلالة |
| :--- | :--- | :--- |
| `60-TOPOLOGY §2-ب` (معتمد `DEC-098`) | «البصمة = **نمط معالجة ديناميكي**، لا ملف درجات ساكن. *(تُوجَّه لإعادة تعريف `URS` في الخطوة ④)*» | **التوجيه كان معتمداً سلفاً**؛ الناقص كان **الآلية** |
| `DEC-100` (`ق-أ`) | سجل الاقتران **مستهلكه داخلي للمحرك أولاً** | يفتح استهلاكه من `URS` |
| `VR-3`/`VR-5` (`DEC-095`) | لا يُفسَّر بُعد منفرداً · الأبعاد **شبكة معالجة** لا صناديق | يمنع بقاء البصمة تجميعاً وصفياً للدرجات |
| `DEC-083` | «**المصفوفات تُغذّي البصمة، لا العكس**» | يصبح منفَّذاً حرفياً: مادة البصمة = مخرجات طبقة الاقتران |

**الخلاصة:** الجديد في `DEC-101` هو **تعيين آلية البناء** (مجموعة الخلايا المفعَّلة)، لا استحداث مفهوم. الوسم الدقيق للجزء المشتق: `[مشتق]` · وللتعيين الآلي: `[معتمد بـ DEC-101]`.

---

# القسم 3: ما يتغيّر — `URS` v2.1 مقابل v3.0 المقترحة

| المحور | v2.1 (القائمة) | v3.0 (بعد `DEC-101`) |
| :--- | :--- | :--- |
| **الوحدة البنائية** | خمس قوى مفردة | **خلايا اقتران مفعَّلة** (`CPL-…`) |
| **السؤال المركزي** | «أي قدراته أقوى وأيها أضعف؟» | **«كيف تتفاعل قدراته — أي اقترانات تعمل في نظامه وبأي كلفة؟»** |
| **مصدر القراءة الاتجاهية** | محصلة القوى الخمس (`DEC-082`) | **حصيلة الاقترانات وكُلفها** — أدقّ وأقرب للآلية |
| **الحاجة لصيغة تجميع رقمية** | قائمة (`GAP-URS-01`) | **منتفية بنيوياً** — المجموعة **فئوية** لا محسوبة |
| **الدرجات الخمس** | مكوّنات البصمة | **مُدخَلات استدعاء** تحدّد أي خلية تُسحب (ح-1) |

---

# القسم 4: الأثر على الحواجز — **[مشتق · يُحسم في ④ · لا رفع حجب تلقائي]**

| الحاجب | الأثر المتوقع | الحالة |
| :--- | :--- | :--- |
| **`GAP-URS-01`** (لا صيغة تجميع/عتبات) | **قد تنحلّ لا تُحلّ:** الصياغة الجديدة **لا تحتاج** صيغة تجميع أصلاً — البصمة مجموعة خلايا لا مقدار مركّب، وعتباتها هي عتبات `DEC-064` المعتمدة | 🟡 يُعرض للحسم في ④ |
| **`DEBT-K3-NORM-01`** | **نطاق الحجب يضيق:** ما يحجبه هو **المقارنة المقدارية**؛ والاقتران فئوي مطلق (`DEC-095` المخفِّف) — فيسقط الحاجب عن معظم بنية `URS` الجديدة | 🟡 يُعرض للحسم في ④ |
| **بقية الحجب على `URS`** | 🔴 **قائم:** ① الحالة «متفاوت» في `URS §2.1` («أي قوة **تنكسر أولاً**») تبقى **حكماً مقارناً** ما لم تُعَد صياغتها بالخلايا · ② شرط `DEC-083` باكتمال المصفوفات | 🔴 لم يُرفع |
| **`DEBT-K3-POLES-01`** | **غير متأثر إطلاقاً** — خلايا «قوي» تبقى بلا ادّعاء أصالة، وكتلة `M3-07` تبقى إلزامية | 🔴 قائم كما هو |

> **حدّ قاطع:** هذه الوثيقة **لا ترفع حجب `URS`**. رفعه يتطلب قراراً سيادياً مستقلاً في الخطوة ④، بعد فحص البندين المتبقيين أعلاه.

---

# القسم 5: ما يصمد من `URS` v2.1 (لا يُهدم)

✅ حدود النقاء الست (§6) بالكامل — خاصة **«قوة لا اتجاه» داخل المهارة** و**«ملف لا تسمية»** و**حظر ادّعاء الأقطاب المضادة**.
✅ **`EP` طبقة ثقة** — متطابقة مع القاعدة `ح-3` في سجل الاقتران (مُضاعِف موثوقية لا طرف).
✅ **قراءة الاحتواء الدائرية** (`DEC-082`) — تبقى الجسر الشرعي، لكن مادتها تصير **الكُلف المعلَنة في الخلايا** بدل استنتاج من شكل الدرجات.
✅ `P = C + G` · صفر لغة عيادية · احتمال لا حتمية.

---

# القسم 6: ✅ `GAP-URS-02` — محسومة (`DEC-103`)

| الحقل | البيان |
| :--- | :--- |
| **البيان** | `URS §5` يجعل **`ST` مُعامل حمل`**؛ والطوبولوجيا تجعلها **طرفاً** في عائلة التحميل (12 خلية) ← خطر ازدواج الحساب |
| **القرار** | ✅ **الخيار (ج): دور مزدوج مفصول الطبقة** — طرفاً في طبقة الاقتران (وصف القناة) · مُعاملاً في طبقة العرض (وَسْم إطاري واحد غير رقمي) |
| **نص مانع الازدواج** | `64-K3-COMPOSITION-METHOD §5.1` — **كل حمل يُذكر مرة واحدة في طبقته**؛ طبقة العرض لا تعيد اشتقاق الحمل من الخلايا، وخلايا التحميل لا تحكم على سعة `ST` |
| **الأثر على v3.0** | طبقة العرض في `URS` v3.0 تحمل **وَسْمين اثنين فقط**: الثقة (`EP` — `§4`) والحمل (`ST` — `§5`) |

---

# القسم 7: ترقية `GAP-CPL-01`

| قبل `DEC-101` | بعد `DEC-101` |
| :--- | :--- |
| مسألة **عرض** في المحرك (ترتيب/إشباع) — خطورة عالية | 🔴 **مسألة بناء البصمة نفسها** — خطورة **حرجة وحاجبة** |

**السبب:** إذا كانت البصمة = مجموعة الخلايا المفعَّلة، فإن **قاعدة التركيب** (كيف تُقرأ الخلايا معاً، وما حدّ الإشباع، وكيف تُعالَج القراءات المتجاورة) هي **قاعدة تكوين البصمة** لا تفصيل واجهة. **يجب حسمها قبل المرحلة ب.**

---

# القسم 8: أثر التسلسل

1. **اكتمال سجل الاقتران (40 خلية) صار شرطاً سابقاً لبناء `URS`** — وهذا تنفيذ حرفي لـ`DEC-083`، لا خروج عليه.
2. **الترتيب المثبَّت لا يتغيّر:** إتمام الخطوة ③ (المراحل ب/ج/د) ← ثم الخطوة ④ (مراجعة `DEC-087` + `URS` v3.0) ← ثم ⑤ (المحرك).
3. **اعتماد المرحلة (أ) صار أثقل وزناً:** الخلايا الثماني لم تعد نموذجاً مرجعياً لسجل فحسب، بل **قالب الوحدة البنائية للبصمة**. ✅ **`62-K3-COUPLING-REG` v1.0 معتمد بـ`DEC-102`** (البنود 1–5).

---

# القسم 9: حدود هذا التوجيه — ما لا يُدَّعى

1. ❌ لا يُدَّعى أن `URS` صار **قابلاً للبناء** — الحجب قائم (§4).
2. ❌ لا يُدَّعى أن `NORM-01` أو `GAP-URS-01` **أُغلقتا** — كلاهما `[مشتق]` معروض للحسم في ④.
3. ❌ لا تُعتمد أي صيغة تجميع، ولا عتبة، ولا وزن — صفر أرقام مستحدثة.
4. ❌ لا يُنشأ أي وصف اتجاهي داخل مهارة مفردة (`URS §6` بند 1 و6 نافذان).
5. ❌ `56-K3-URS` v2.1 **يبقى نافذاً كما هو** حتى تُنتج v3.0 وتُعتمد في ④؛ هذه الوثيقة **توجيه** لا نسخ.

---

# القسم 10: المطلوب — بنود الخطوة ④

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | إنتاج `56-K3-URS` **v3.0** على أساس `DEC-101` | ⬜ الخطوة ④ |
| 2 | إعادة صياغة الحالة «متفاوت» (`§2.1`) بلغة الخلايا لإزالة الحكم المقارن | ⬜ ④ |
| 3 | حسم `GAP-URS-01` (انحلال أم بقاء) | ⬜ ④ |
| 4 | تحديد **ما تبقّى** من حجب `NORM-01` على `URS` بدقة | ⬜ ④ |
| 5 | حسم `GAP-URS-02` (دور `ST` المزدوج) | ✅ **محسوم** (`DEC-103` — الخيار ج) |
| 6 | حسم `GAP-CPL-01` (قاعدة التركيب) | ✅ **محسوم اتجاهاً** (`DEC-104`) · المنهج بانتظار `DEC-106` |
| 7 | مراجعة `DEC-087` بالصيغة الموجَّهة في `60-TOPOLOGY §5` | ⬜ ④ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| `DEC-101` | ✅ معتمد — إعادة تأطير `URS` كسجل اقترانات مفعَّلة |
| بناء `URS` التنفيذي | 🔴 **محجوب — لم يُرفع** |
| `56-K3-URS` v2.1 | ✅ نافذ حتى إنتاج v3.0 في ④ |
| فجوات جديدة | `GAP-URS-02` (عالية · حاجبة للمرحلة ب) |
| فجوات مُرقّاة | `GAP-CPL-01` → **حرجة حاجبة** |
| صفر هلوسة | ✅ لا صيغ ولا عتبات ولا أوزان مستحدثة |
| **حالة الوثيقة** | ✅ **v1.0 — توجيه معتمد؛ التنفيذ في الخطوة ④** |
