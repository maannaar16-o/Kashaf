# 56-REPORT-ENGINE_Operational_Specification.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `56-REPORT-ENGINE` — مواصفة محرك التقارير التشغيلية |
| **السلسلة** | `56` — طبقة المحرك والاختبار |
| **الإصدار** | **v1.2** — نافذة (v1.0 `DEC-045` · v1.1 ملاحق `C`·`D`·`E` `DEC-144/148/150` · **v1.2: التركيب 8/8 + قاعدة `ت-8` — `DEC-169…182`**) |
| **حالة الاعتماد** | ✅ **معتمدة نافذة** |
| **التصنيف الاستراتيجي** | **[خارج النموذج — قابل للتنفيذ]** — أداة تشغيلية تستهلك المعتمد ولا تمس النظرية؛ صفر توليد نصي |
| **السند الجامع** | `41-MEASURE` §3/§4 · `M-06` v0.2 (المرجع الحاكم للاستدعاء) · `50-DEPTH` 8/8 · `55-USER` 8/8 + الترقية الثمانية (64 كتلة) · `51-MATRIX` 7/7 · `56-TEST-01` v0.2 + `56-TEST-02` (تحقق الحدّين) |
| **الوظيفة** | تحويل إجابات المقياس إلى تقرير فردي/جماعي **مُجمَّع حصراً من وحدات معتمدة v1.0 تُستدعى حرفياً** — المحرك يُركّب ولا يؤلّف |
| **الوسوم الملازمة لكل مخرج** | `[توحيد تشغيلي مؤقت — GAP-A-01]` · `[توحيد تشغيلي مؤقت — GAP-A-02]` · ~~`[التركيب الأعلى: غير مُطبَّق]`~~ **متقاعد — التركيب 8/8 مكتمل (`DEC-177`)** |
| **الملاحق النافذة** | `APPENDIX-B` (تحويل صيغة القائد) · **`APPENDIX-C`** (ازدواج الإصدار والتسليم النظيف) · **`APPENDIX-D` v1.1** (البوابة الشرطية للارتداد) · **`APPENDIX-E`** (اختيار المهيمنين بالنطاق) |
| **طبقة التركيب الأعلى** | **8/8 معتمدة** (`DEC-177`): `E`(→O) · `H`(→A·R·O) · `C`(→H·St) · `S`(→A·St) · `St`(→R·O) · `R`(→A·St) · `A`(→E·O·R) · `O`(→H·St·R). ملفات `COMPOSE-{X}` (بلا بادئة رقمية · `DEC-172`) · `E`/`H` إعادة بناء نافذة (`DEC-181/182`). 21 سطراً/مركز. |
| **الأنبوب المكوَّد** | طقم بايثون تشغيلي (`DEC-179`): `k2_engine`·`k2_content`+`contentpack`·`k2_userlayer_pack`·`k2_framing`·`k2_report`·`golden_k2`+`test` — استبيان خام → تقرير · محرك الحساب مُثبَت 8/8 (×2 مستقل) |
| **نطاق الإصدار v1** | دائرة التفكير `K_2` (الطبقات التفسيرية المعتمدة). باقي الدوائر (`K_1`، `K_3`، `K_4`) خارج نطاق هذا الإصدار حتى بناء طبقاتها الموازية — **لا يتناول المحرك أي بند من أوعيتها** |

## 0.1 المبدأ التأسيسي للمحرك
**[معتمد — مستخلص من كامل البنية]:** المحرك **مُركِّب (Assembler) لا مؤلِّف (Author)**. كل جملة في التقرير النهائي لها عنوان مصدري في سجل `M-06` §2؛ ما لا عنوان له لا يظهر. الذكاء كله في **الاختيار والترتيب**، والصفر كله في **التأليف**.

---

# القسم 1: المعمارية العامة — طبقات المحرك السبع

```
[1] الإدخال (إجابات المقياس)
      ↓
[2] الحساب (41 §3: x, y, z → SS → SP)
      ↓
[3] التصنيف (41 §4: SP → رمز ثماني → كتلة → اشتعال → مركز)
      ↓
[4] الاستدعاء (M-06: R1–R11 فردي · G1–G4 جماعي)
      ↓
[5] التجميع (هيكل التقرير — §5 أدناه)
      ↓
[6] البوابات (LD فرز المحركات · PUR تطهير لغوي · فحص الوسوم)
      ↓
[7] الإصدار (بأحد الأوضاع الثلاثة — §6 أدناه)
```

**قاعدة حاكمة:** لا تنتقل حالة من طبقة إلى التالية إلا مكتملة؛ أي نقص = تقرير فجوة (§10 من البروتوكول)، لا ملء.

---

# القسم 2: طبقة الإدخال (Input Specification)

**[معتمد — من `41` §3]** لكل وعاء من أوعية `K_2` الثمانية:

| المُدخل | التعريف | القيد |
| :--- | :--- | :--- |
| تقييمات المرحلة الثانية | القيم السداسية (1–6) لبنود الوعاء السبعة → `x` | `x ∈ [7, 42]` |
| اختيارات المرحلة الأولى | المؤكِّدة → `y` · المتروكة → `z` | **`y + z = 7`** إلزامي |

**فحص سلامة الإدخال (Validation):** رفض أي حالة تخرق `y+z=7` أو مدى `x` — بتقرير فجوة تشغيلية، لا إصلاح صامت.

---

# القسم 3: طبقتا الحساب والتصنيف

**[معتمد — استنساخ `41` §3/§4 عبر `M-06` §1]:**

| العملية | الصيغة/الحدود |
| :--- | :--- |
| الدرجة المعيارية | `SS = x − 2z + y` (تكافؤ تشغيلي: `SS = x + 3y − 14`) |
| النسبة المعيارية | `SP = SS ÷ 42 × 100` · المدى الفعلي: `[−...  , 116.7%]` (`SS_max = 49`) |
| الترميز الثماني | `SP<0`→OUT · `0≤SP≤20`→L− · `20<SP≤40`→L · `40<SP≤50`→M · `50<SP≤70`→M+ · `70<SP≤85`→H · `85<SP≤100`→H+ · `SP>100`→H++ |
| التثليث الموسوم | {OUT,L−,L,M}→نقطة عمى · {M+}→مساندة · {H,H+,H++}→مهيمنة `[GAP-A-01/ج]` |
| الاشتعال والمركز | مشتعل: SP>50% · أعلى 2–3 · المركز = الأعلى SP (`DEC-033` + `[GAP-A-02/ج]`) |

---

# القسم 4: طبقة الاستدعاء — المرجع الحاكم

**[معتمد]** المرجع الحصري: **`51-MATRIX-06` v0.2** — قواعده لا تُنسخ هنا (منع ازدواج المصدر)؛ تُستدعى:

| النطاق | القواعد |
| :--- | :--- |
| فردي | **R1–R11** (شاملة R3 المثبّتة بالمداخل الخمسة الوظيفية، وR11 العرض المزدوج St+H) |
| جماعي | **G1–G4** |
| أقفال المحرك | §3.3 الخمسة (صفر توليد · صفر مسودات · حماية DY-28 · فصل البطاقات · حظر K_3/K_4) |

**قاعدة أسبقية:** عند أي تعارض بين هذه المواصفة و`M-06`، يَحكُم `M-06` (هو سجل الاستدعاء السيادي).

---

# القسم 5: طبقة التجميع — هياكل التقرير

## 5.1 هيكل التقرير الفردي (تسلسل ثابت — [مشتق] من تتبّعَي Alpha/Beta)

| # | المقطع | مصدر الاستدعاء |
| :--- | :--- | :--- |
| 1 | رأس التقرير + الوسمان الإجرائيان + القيد المعلن (`P = C + G`: الدرجة ترتيب عدسات لا قدرة) | ثابت معتمد |
| 2 | لوحة الرموز الثمانية (جدول SP/رمز لكل بُعد) | طبقة التصنيف |
| 3 | العدسة المركز: كتلة شدتها الثمانية + كتالوجها الكامل | R1 + R2 |
| 4 | **المهيمنون المساندون: كل بُعد في الكتلة المهيمنة عدا المركز**، مرتّبين تنازلياً — كتلة + المداخل الخمسة الوظيفية. **المتعادلون متساوو الرتبة بلا لغة رتبية رقمية** | R1 + R3 · **`APPENDIX-E` §2/§3** |
| 5 | حزمة St+H الموسومة (إن اشتعلا معاً) | R11 |
| 6 | بطاقات التوتر القطبي (إن وُجد زوج قطبي مشتعل) | R5 |
| 7 | تفاعلات الأزواج المشتعلة (صدام/تكامل/مثال) | R4 |
| 8 | فجوة التغطية (المركز × أدنى فعّال غير OUT) بصياغة تخادم | R6 |
| 9 | تحت الضغط الفائق — **يُحدَّد بالبوابة الشرطية**: ① ملاذ بيني حاضر → المركز وحده · ② موافقة القطبين → المركز وحده · ③ **تعاكس القطبين → المسارات معاً (توسيع كامل)** + سطر الوصل. "حالة عابرة لا سمة" | R7 · **`APPENDIX-D` v1.1 §3** |
| 10 | نقاط العمى المعرفية: كتل الأبعاد غير المشتعلة — "غياب أولوية لا نقص" + توصية تخادم | R1 (الكتل الدنيا) |
| 11 | خاتمة البصمة: مدخل البصمة للمركز | R2 (جزئي) |

> **🔑 طبقة التركيب الأعلى (`DEC-140`/`159`/`163`/`178`):** بعد بناء المقاطع أعلاه، تُدرَج **أسطر معجم المركز** بشرط رقمي بحت (نطاق كل عدسة: مهيمن `D`>70 / مساند `M` 50–70 / منطفئ `L`≤50) وفق قواعد `ت-1…ت-8`:
> `ت-5` السطر يُدرَج في مقطع عدسته (وما يمسّ الضغط يُضاف لمقطع الضغط) · `ت-6` عند التطابق المضموني مع وحدة مستدعاة أصلاً يُنقل السطر لطبقة المراجعة · `ت-7` الأسئلة التمييزية لطبقة المراجعة، وتُنتقى للتسليم أسئلة الحالات المنطفئة وحدها.
> **`ت-8` (تكييف نص العمى · `DEC-178`):** نص العمى المحايد يُقدَّم مشروطاً بحالة تغطيته لكل نصف عمى: كل الحلفاء `D` → «مُغطّى داخلياً عبر [الحليف]» · حليف `L` → «مكشوف» + سطر بيني · مختلط → «مُغطّى غالباً عبر [الحاضر]، ومكشوف من جهة [الغائب]». تكييف **عرضي لا تفسيري** (يحترم `DEC-163`).
> **خيط `FRIC-02`:** عند تعدّد الحلفاء المنطفئين (≥2) يُدرَج **خيط سرد غير تفسيري** واحد: «تغطية العمى خارجية عبر [الحلفاء] (سرد لا ترجيح)».
> **القفل (محدَّث):** تُستدعى **المعاجم الثمانية** `E·H·C·S·St·R·A·O`؛ لا مركز غير مؤلَّف بعد الآن — **ولا تُخترع أسطر** خارجها.
> **الشرط المسبق:** ✅ **مُستوفى ومغلق** — `DEBT-K2-NEUTRAL-01` أُغلق (تحييد الأبعاد الستة · `DEC-158`).

## 5.2 هيكل التقرير الجماعي

| # | المقطع | المصدر |
| :--- | :--- | :--- |
| 1 | رأس + وسوم + قفل العرض (إنذار بنيوي لا حكم كفاءة) | G4 |
| 2 | خريطة التغطية العدسية للفريق (المرئي/الأعمى جماعياً) | G1 (M-04) |
| 3 | أزواج الاحتكاك/التكامل الأعلى احتمالاً بين الأعضاء | G2 (M-01) |
| 4 | سلاسل الارتداد المتزامنة تحت الضغط المشترك | G3 (M-03) |
| 5 | توصية التشكيل (البُعد الغائب المطلوب) | G1 + مفارقة القطبية (M-04 §2.2) |

## 5.3 قاعدة الربط النصي بين الوحدات
**[معتمد — قفل]:** يُسمح بجُمل وصل **هيكلية محايدة فقط** (عناوين مقاطع، أسطر انتقال من قائمة مغلقة تُعتمد مع هذه الوثيقة) — يُحظر أي جسر نصي يضيف تفسيراً أو يدمج مضمون وحدتين.

**إضافة `DEC-149`:** يُضاف إلى القائمة المغلقة **سطر وصل واحد**، مقفل النص، يُستدعى **حصراً** في الحالة ③ من `APPENDIX-D` (تعاكس القطبين) ومرّة واحدة مهما تعدّدت وحدات الارتداد:
> «يحمل ملفك عدستين تنهاران في اتجاهين مختلفين تحت الضغط الشديد؛ لذا قد تسلك أحد مسارين — أيّهما ظهر لك، فكلاهما حالة عابرة يصنعها الضغط لا صفة فيك.»

**إضافة `FRIC-02` (v1.2 · `DEC-178` سياق):** يُضاف سطر وصل مغلق ثانٍ يُستدعى **حصراً** عند تعدّد حلفاء العمى المنطفئين (≥2) في طبقة التركيب، مرّة واحدة:
> «تغطية عماك خارجية بالكامل عبر [الحلفاء] — سرد لا ترجيح.»
> **قفل:** غير تفسيري، لا يضيف وزناً (يحترم `DEC-163`).

---

# القسم 6: طبقة الإصدار — الأوضاع الثلاثة (المقترح المعتمد)

| الوضع | الجمهور | الصيغة | الحالة |
| :--- | :--- | :--- | :--- |
| **الفرد** | صاحب النتيجة | "أنت" المباشرة (المعتمدة) — الهيكل 5.1 كاملاً | ✅ **جاهز التشغيل** |
| **القائد/المشرف** | مسؤول عن صاحب النتيجة | صيغة الغائب + تركيز التخادم والتغطية (مقاطع 4، 8، 10 مقدَّمة) | ✅ **جاهز التشغيل** — قاعدة التحويل معتمدة (الملحق ب) |
| **الفريق** | قائد الفريق/المؤسسة | الهيكل 5.2 — بلا أفراد مسمّين إلا بإذن | ✅ جاهز (يستهلك G1–G4) |

**[مغلقة]:** قاعدة تحويل الصيغة لوضع القائد معتمدة — راجع `56-REPORT-ENGINE-APPENDIX-B` (GAP-ENG-01 مغلقة نهائياً).

## 6.1 ازدواج الإصدار الإلزامي (`DEC-141` · `APPENDIX-C`)
**[معتمد — قفل]:** كل تقرير **فردي** يُصدَر في **نسختين تُولَّدان معاً وجوباً**:

| الطبقة | الجمهور | المحتوى |
| :--- | :--- | :--- |
| **مراجعة سيادية** | مالك المشروع/المراجع | الهيكل الكامل + كل الأثر التقني (رموز الوحدات · رؤوس القواعد · الوسوم · خريطة الاستدعاء · البنود المرشّحة) |
| **تسليم نظيفة** | صاحب النتيجة | نفس المضمون التحليلي منزوع الأثر التقني · **الاسم الكامل للعدسة عند كل ظهور** · **الأمثلة المعتمدة (💡)** |

**محظورات:** تداول النظيفة بلا توأمها · أي اختلاف في **المضمون التحليلي** بين النسختين (الفرق في طبقة العرض حصراً).
**حدّ النطاق:** يسري على **وضع الفرد** حصراً — وضع القائد يبقى تحت `APPENDIX-B` (الأمثلة تبقى محذوفة فيه).

---

# القسم 7: طبقة البوابات (Quality Gates) — إلزامية قبل الإصدار

| البوابة | الوظيفة | المرجع |
| :--- | :--- | :--- |
| **بوابة LD** | كل جملة تصف سلوكاً ظاهرياً تمر على سؤال الفرز؛ غموض المحرك → وسم غموض لا تخمين | R8 · `M-05` |
| **بوابة PUR** | مسح كامل المخرج ضد المحظورات (~72 بنداً): عيادية، أحكام، أسباب نهائية، تفاضل أشخاص، خلط دوائر | R9 · قواميس §4 الثمانية |
| **بوابة الوسوم** | التحقق من حضور الوسمين الإجرائيين + وسم حزمة R11 إن استُدعيت + وسم RSK-016 عند تقارير St | R10/R11 · `DEC-029` |
| **بوابة الأقفال** | صفر وحدات K_3/K_4 · صفر مسودات · صفر توليد | `M-06` §3.3 |
| **بوابة الازدواج** 🆕 | التحقق من: ① توليد النسختين معاً · ② صفر أثر تقني في التسليم · ③ حضور الأسماء الكاملة والأمثلة المعتمدة · ④ تطابق المضمون التحليلي بين النسختين | **`APPENDIX-C` §5 · `DEC-144`** |

**فشل أي بوابة = حجب الإصدار + تقرير فجوة** — لا إصلاح صامت.

---

# القسم 8: معالجة الحالات الخاصة

| الحالة | المعالجة المعتمدة |
| :--- | :--- |
| اشتعال St+H معاً | حزمة R11 الموسومة — حظر الفصل الرقمي |
| بُعد في OUT | كتلته المخصصة (`K2-X-S-OUT`) + استبعاده من "أدنى فعّال" في R6 |
| بلوغ H++ | كتلته الفعّالة (مسار `SS=x+3y−14` معتمد — GAP-DR-01 مغلقة) |
| تقرير يتناول St | وسم داخلي بالدين العاملي RSK-016 (البند 49ب) — لا تفسير حاسم على درجة St منفردة عند تقاطعها مع اندفاع اجتماعي |
| تعذُّر حسم المهيمن (تعادل SP تام) | فجوة تشغيلية → عرض على مالك المشروع (لا كسر تعادل مخترع) |
| نقص إجابات | رفض بطبقة الإدخال — تقرير فجوة |

---

# القسم 9: حدود المحرك المعلنة (Explicit Non-Goals)

1. لا يقيس ولا يفسّر `K_1`/`K_3`/`K_4` (نطاق v1 = `K_2` حصراً).
2. لا يقدّم توصيات مهنية/حياتية آمرة — يعرض خرائط تخادم لا قرارات.
3. لا يقارن الأفراد تفاضلياً (محظور PUR).
4. لا يشخّص ولا يعالج (محظور §5 من البروتوكول) — توصيف معماري حصراً.
5. لا يعمل على مسودات — v1.0 المعتمد فقط.

---

# القسم 10: سجل الفجوات

| كود | البيان | الخطورة | المطلوب |
| :--- | :--- | :--- | :--- |
| **GAP-ENG-01** | ✅ **مغلقة** — قاعدة تحويل الصيغة معتمدة (`56-REPORT-ENGINE-APPENDIX-B`)؛ وضع القائد جاهز التشغيل | مغلقة | — |
| **GAP-ENG-02** | ✅ **مغلقة** — قائمة أسطر الوصل الستة أُقرّت (الملحق أ) بقرار اعتماد الوثيقة | مغلقة | — |
| **إحالة** | فجوات استرجاع `30-CORE` الخمس (منخفضة) موروثة في وسم بعض خلايا §1.2/§1.6 بالتوائم — لا تعطّل المحرك | منخفضة | مطابقة بصرية واحدة |

---

# الملحق أ: قائمة أسطر التقرير الهيكلية المحايدة (مغلقة)
**[معتمد بقرار مالك المشروع — إغلاق GAP-ENG-02 · مُوسَّع بـ`DEC-208`/`DEC-209`]** — الأسطر النصية الوحيدة المسموحة في التقرير، **جسوراً بين الوحدات المستدعاة أو تأطيراً داخل المقاطع**؛ أي سطر خارجها = خرق بوابة الأقفال:

| # | السطر المعتمد | موضع الاستخدام |
| :--- | :--- | :--- |
| ج-1 | «فيما يلي [اسم المقطع]:» | افتتاح أي مقطع من هيكل §5.1/§5.2 |
| ج-2 | «استُدعيت الوحدة التالية وفق القاعدة [R#/G#]:» | قبل كل وحدة مستدعاة |
| ج-3 | «اكتمل هذا المقطع؛ يليه [اسم المقطع التالي].» | خاتمة مقطع |
| ج-4 | «وسم إلزامي: [نص الوسم]» | إدراج الوسوم (GAP-A-01/02 · R11 · RSK-016 · Pilot) |
| ج-5 | «ملاحظة تخادم: راجع مقطع [اسم المقطع].» | إحالة داخلية بين مقاطع التقرير |
| ج-6 | «نهاية التقرير.» | الختام |
| **ج-7** | «هذه أسئلة تساعدك على قراءة ما سبق بنفسك — لا أحكام.» | تأطير مقطع أسئلة `ت-7` **حين توجد أسئلة تسليم** |
| **ج-8** | «غياب أولوية لا نقص — تُغطّى بالتخادم لا بالإصلاح الذاتي (`P = C + G`).» | تأطير مقطع الحالات المنطفئة |

**قفل:** الأسطر الثمانية **هيكلية خالصة** — لا يجوز تعديل صياغتها ولا حشو متغيراتها بغير أسماء المقاطع/القواعد/الوسوم الحرفية. **`ج-7` و`ج-8` تأطيريان: يُدرجان في موضعهما المنصوص وحده، ولا يُعمَّمان على مقاطع أخرى.**

**نطاق القفل (`DEC-208`):** يحكم **كل سطر في التقرير** لا الجسور وحدها.

**سند `ج-7`/`ج-8`:** مضمونهما مسنَد (`ت-7` · `COMPOSE-C §0.1`: «`L` = غياب أولوية لا نقص»)، وصياغتهما تُعتمد بهذه الرقعة — وكانا يظهران في `REPORT_P-001/005/007` قبل تقنينهما، فالرقعة **تُقنّن أمراً واقعاً** لا تُحدث نصّاً.

**حالة `ج-5` المستحدثة (`DEC-207/ب`):** يُستعمل في مقطع أسئلة `ت-7` **حين لا عدسة منطفئة** (فـ`ت-7` بلا هدف) بصيغة: «ملاحظة تخادم: راجع مقطع كيف تتركّب عدساتك.»

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| الطبقات السبع مكتملة التعريف | ✅ |
| صفر توليد — مُركِّب لا مؤلِّف | ✅ مبدأ تأسيسي + بوابات |
| المرجعية الحاكمة لـ`M-06` (منع ازدواج المصدر) | ✅ §4 |
| الأوضاع الثلاثة | ✅ **الثلاثة جاهزة التشغيل** (فرد · فريق · قائد) |
| تغطية الحالات الخاصة (R11 · OUT · H++ · RSK-016 · التعادل) | ✅ |
| مختبَر عند الحدّين | ✅ Alpha (أدنى) + Beta (أقصى H++ مزدوج) |
| **قواعد الاستدعاء `R1…R11`** | ✅ **كلها منفَّذة** (`Mode 03-Build` — `R1`·`R9`·`R10`·`R11` كانت غائبة) |
| **الملحق أ** | ✅ **ثمانية أسطر** (`ج-1…ج-8` · `DEC-209`) · نطاق القفل: كل سطر في التقرير (`DEC-208`) |
| **البنية النافذة** | ✅ **تساعية** (`DEC-189`/`DEC-211/ج₂`) — انظر §13.1 |
| **نطاقا التصدير** | ✅ `full` (السجلّ) · `brief` (القراءة) — `DEC-225/و` |
| **عقد التدقيق** | ✅ `audit` كافٍ لإعادة التوليد — مُبرهَن ميدانياً (`DEC-220`) |
| **حالة الوثيقة** | ✅ **v1.3 — معتمدة (GAP-ENG-02 مغلقة · GAP-ENG-01/وضع القائد يبقى معلّقاً)** |

---

# القسم 11: تحديث نافذ — جلسة تحقق $K_2$ الميداني (`DEC-140…168`)

## 11.1 ما تغيّر في هذه المواصفة

| الموضع | التغيير | السند |
| :--- | :--- | :--- |
| §0 (الرأس) | الإصدار v1.1 · الملاحق النافذة · وسم التركيب الأعلى | `DEC-144`/`154` |
| §5.1 صف 4 | `R3` **بالنطاق** لا بالرتبة · المتعادلون متساوو الرتبة | `DEC-150` |
| §5.1 صف 9 | ارتداد الضغط **بالبوابة الشرطية** لا بالمركز منفرداً | `DEC-148`/`151` |
| §5.1 (بعد الجدول) | إدراج **طبقة التركيب الأعلى** وقواعدها | `DEC-140`/`159`/`163` |
| §5.3 | سطر وصل واحد يُضاف للقائمة المغلقة | `DEC-149` |
| §6.1 | **ازدواج الإصدار الإلزامي** + حدّ نطاقه | `DEC-141`/`142` |
| §7 | **بوابة خامسة (الازدواج)** | `DEC-144` |

## 11.2 وثائق مرجعية تغيّرت حالتها

| الوثيقة | الحالة الجديدة | السند |
| :--- | :--- | :--- |
| `51-MATRIX-03_Fallback_Map` | مسودة `v0.1` → ✅ **معتمدة** (شرط نفاذ `APPENDIX-D`) | `DEC-147` |
| `51-MATRIX-04` §1 صف `H` | أُضيفت **`A` (البرهنة)** لمجموعة التغطية | `DEC-164` |
| `55-USER-K2-E` · `50-DEPTH-K2-H` (كتلتا العمى) | **نص محايد** يستبدل الأصل — العلاج انتقل لسطر المعجم | `DEC-158`/`161` |
| `56-GOLDEN-P001` كمعيار تسليم | → `_Delivery_CLEAN_v2` + `_Review_Layer_v2` | `DEC-145` |
| مجموعة الانحدار | حالة مفردة → **`56-GOLDEN-TRIAD`** (3 أصيلة + `P-002` مساعداً · `INV-1…6`) | `DEC-168` |

## 11.3 قيود نافذة يجب ألا تُغفَل

1. **تبسيط الأقفال** مسموح **كتأليف مرّة واحدة معتمد** فقط — محظور كتبسيط آلي زمن-تشغيل (`DEC-143`).
2. **الأمثلة (💡)** تُستدعى من `55-USER` حرفياً — يُحظر تأليف مثال، ويُحظر مطلقاً توليد مثال **مخصّص لواقع صاحب الحالة**.
3. **الملمح العام** (إن أُدرج) يحمل وسم `[اشتقاق تجميعي — بانتظار A6]` و**لا يدخل بصمة الانحدار** (`DEC-146`).
4. **التشغيل الجزئي** لطبقة التركيب مُفعَّل بخطره المقبول الموثَّق (تفاوت جودة التقارير) — التطويق: الوسم الإلزامي + **اختلاف التغطية ليس انحرافاً** (`DEC-154`).

---

---

# القسم 12: تحديث نافذ — جلسة استكمال التركيب والتكويد 2026-07-28 (`DEC-169…182`)

## 12.1 ما تغيّر
| الموضع | التغيير | القرار |
| :--- | :--- | :--- |
| §0 الرأس | v1.1 → **v1.2** · التركيب 8/8 · وسم التشغيل الجزئي متقاعد | `DEC-169…182` |
| §5.1 | إضافة **`ت-8`** (تكييف نص العمى) + خيط **`FRIC-02`** + قفل 8/8 | `DEC-178` |
| §5.3 | إضافة سطر وصل `FRIC-02` للقائمة المغلقة | `DEC-178` |
| مجموعات التغطية | `A`=`E·O·R` · `O`=`H·St·R` (ثلاثيتان) | `DEC-173/174` |
| تسمية المعاجم | `COMPOSE-{X}` بلا بادئة رقمية | `DEC-172` |
| `E`/`H` | إعادة بناء نافذة تَخلُف `62/63-COMPOSE` المفقودين (مطابقة `DEC-159/162/163`) | `DEC-181/182` |

## 12.2 الديون/الفجوات المُغلقة
`DEBT-K2-NEUTRAL-01` (تحييد ست) · `GAP-K2-COV-A` · `GAP-K2-COV-O` · `FRIC-COMPOSE-03` (بـ`ت-8`) · نواقص `E`/`H` و`55-USER` و`report_framing`.

## 12.3 التحقّق الحي
محرك الحساب مُثبَت **8/8 تام ×2 مستقل** (مقابل الأداة الإنتاجية) · **4 حالات حقيقية** (`A·C·E·S`) · 3 فروع `ت-8` · **صفر انحدار** (`golden_k2` 4 حالات).

## 12.4 قيد نافذ
معجما `E`/`H` إعادة بناء مطابقة للقرارات المُلزِمة، لا نسخة أصلية؛ أي صياغة غير مُسجَّلة في قرار غير قابلة للاسترجاع (أثر منخفض).


---

# القسم 13: تحديث نافذ — `Mode 03-Build` 2026-08-03 (`DEC-183…229`)

> **الجلسة:** دمج محرّكي $K_2$/$K_3$ مع الأداة التفاعلية. **المخرج:** `Kashaf_v2.html` — 12 حزمة · 11 مجموعة تحقّق · صفر انحراف.

## 13.1 هيكل التقرير الفردي — **البنية التساعية النافذة** (`DEC-189` · `DEC-211/ج₂`)

**هيكل §5.1 (أحد عشر مقطعاً) نُسخ ببنية تساعية** تستوعب `R1…R11` قواعدَ استدعاء داخل الأقسام لا مقاطعَ مستقلة:

| # | القسم | يستدعي |
| :--- | :--- | :--- |
| ① | قبل أن تقرأ | `PCG_COVENANT` (يُبقى حرفياً · `DEC-188`) |
| ② | لوحة رموزك الثمانية | الرمز + الحالة (**بلا `SP%`** · `DEC-183/187`) + **`R1`** كتل الشدة الثماني |
| ③ | مركزك | دورة المعالجة · المحرك · الموقع في الفريق |
| ④ | عدساتك المهيمنة والمساندة | الوظائف الثلاث نفسها لكل عدسة |
| ⑤ | كيف تتركّب عدساتك | `ت-8` · `FRIC-02` · التغطية/التلوين · **قاعدة الفلترة** · **نقطة العمى** |
| ⑥ | حين تلتقي عدستان | **`R11`** (`St+H`) · **قراءة الآخرين** · `R4`·`R5`·`R6`·`R8` |
| ⑦ | تحت الضغط | حالة الضغط القصوى · **الإزعاج** · `R7` |
| ⑧ | أسئلة تخصّك وحدك | `ت-7` — `ج-7` عند وجود أسئلة · **`ج-5`** عند غيابها (`DEC-207/ب`) |
| ⑨ | نقاط عماك · بصمتك | `ج-8` · البصمة · ما لا تعنيه · **اقتصاد الطاقة** · **الآلية الخاصة** |
| — | الختام | **`R10`** الوسمان الإجرائيان بقالب `ج-4` · ثم `ج-6` |

**السبب البنيوي:** `TC-K2-01` (تعارض `R2` «المداخل كاملة» ضد ثلاثة مداخل) كان **ظاهرياً** — `R2` يحكم التقرير و`56-ENG` يحكم قسم «مركزك». **البنية التساعية استوعبت الطرفين بلا ترجيح وثيقة على أخرى.**

## 13.2 استدعاء بالوظيفة لا بالرقم (`DEC-190` — إصلاح `DEF-K2-01`)

**`R3` كان منصوصاً ومخروقاً:** «استدعاء بالوظيفة لا بالرقم — تفاوت ترقيم `U-##` عبر الأبعاد». والمحرك كان يستدعي بثوابت رقمية، فأنتج «بصمتك» خاطئة في **6 من 8** مراكز ومرّ على **ثلاثة تقارير مرجعية من أربعة**.

**النافذ الآن:** `resolve_slot(dim, slot)` بحدّ عددي لكل وظيفة (`_CARD`) **يوقف الإصدار** عند الشذوذ ولا يُخمّن.

## 13.3 نطاقا التصدير (`DEC-225/و`)

| النطاق | التعريف | الوظيفة |
| :--- | :--- | :--- |
| **`full`** | كل عدسة معروضة بكل وظائفها (`DEC-211/ج₂`) | **تقرير السجلّ** — `R2` مستوفى · يُحفظ ويُدقَّق |
| **`brief`** | المركز كتالوجاً كاملاً · الرتبتان 2–3 بالوظائف الخمس · لا توسّع في ⑤⑥⑦⑨ | **أداة قراءة** — `R2`/`R3` حرفياً |

**قياس:** الحالة القصوى 31,590 → 12,707 حرفاً (**−60٪**) · والوسيط لا يكاد يتغيّر (79٪ من الملفات بلا توسّع).

## 13.4 حدّ نطاق العرض (`DEC-227/أ`)

> «كل العدسات» = **المركز + المهيمنة + المساندة**. المنطفئة تُستثنى من توسّع الكتالوج وتبقى بمعاملتها المنصوصة (⑨ تسمية · ⑧ أسئلة `ت-7`).

⇒ **`R2` مستوفىً شرطياً لا مطلقاً:** التغطية 94/94 تتحقق في الملف تامّ الاشتعال، وتقلّ بقدر ما فيه من منطفئة. **مقصود ومثبَّت، لا نقص.**

## 13.5 البوابات النافذة

| البوابة | الحالة |
| :--- | :--- |
| **`R9`** التطهير | ✅ **حيّة** — 72 بنداً بمطابقة **العبارة كاملة** (`DEC-212/ب`) · على المخرج **وعلى الحزم** · كانت **معطَّلة**: تُستدعى بوسيط فارغ فتُعيد `[]` (`DEC-213`) |
| **`ت-6`** منع التكرار | ✅ حارساً **بلا عتبة مخترعة** — يوقف عند الاحتواء التامّ ويُبلِّغ `t6_max_overlap` (`DEC-224`) |
| **حقول القفل** | مستثناة من بوابة المخرج **مع عدّها** (`DEC-217/ب`) · وتُفحص عند المحتوى ضد **سجلّ الذِكر المقبول** (`DEC-229`) |
| **`R1`**·**`R10`**·**`R11`** | ✅ منفَّذة — كانت غائبة |

**قاعدة مستخلصة:** أقفال العزل **تُصاغ بذكر المصطلح المحظور** ليمنع الخلط ⇒ أي بوابة تحظر الألفاظ **تُطلق على الأقفال نفسها**، وهي أكثر النصوص التزاماً. ولذلك: عبارات مركّبة بدل ألفاظ مفردة (`DEC-214`)، واستثناء موضعي معلَن ومعدود.

## 13.6 عقد التدقيق (`DEC-220…223`)

**`audit` كافٍ لإعادة توليد التقرير بالكامل** — سبعة حقول ملزِمة: `sp` · `engine_version` · `spec_version` · `instrument_pin` · `entries_used` (66 وحدة عبر خمسة أنماط) · `pack_sha` · `report_sha256`.

**مُبرهَن ميدانياً:** تقارير صادرة من متصفح المالك أُعيد توليدها حرفياً في بيئة أخرى من `audit["sp"]` وحده — ثلاث بصمات متطابقة و`pack_sha` متطابق.

## 13.7 تكافؤ النسختين (`DEC-199`/`DEC-200`)

المنطق منفَّذ في بايثون و JS. **يُشترط تطابق المخرج** على مجموعة الانحدار نفسها، **وعند التباعد يُجمَّد الطرفان** — لا أسبقية للتنفيذ الأسبق.

**التطبيق العملي:** `DEF-K3-02` أظهر أن **الطرفين معاً** يخالفان السند في `gates_fired` — فصُحّحا معاً. ولو حكمت الأسبقية، لَوَرِث JS خطأ بايثون وصار التطابق مع الخطأ **شهادة سلامة**.

## 13.8 عيوب أُغلقت في هذه الجلسة

| الكود | العيب |
| :--- | :--- |
| `DEF-K2-01` | استدعاء بالرقم — «بصمتك» خاطئة 6/8 |
| `DEF-K2-02` | القسم ⑧ يَعِد بأسئلة ولا يفي حين لا منطفئة |
| `DEF-K2-03` | تكرار كتالوج المركز حين حالته «مساندة» |
| `DEF-K2-04` | كسر التعادل تابع لترتيب مفاتيح المُدخل — **لا-حتمية**؛ صُحّح بالفرز على `LENSES` = ترتيب `41 §5.2` |
| — | بوابة العزل **ميتة**: `isolation: clean` من عدم فحص |
| — | تحذير `H++` متقادم يتسرّب لبُعد `A` (`GAP-DR-01` مغلقة · `DEC-218`) |

## 13.9 قيود نافذة يجب ألا تُغفَل

1. **صفر `SP%`** في الشاشة وكل التصديرات — $K_2$ و$K_3$ (`DEC-183`).
2. **$K_1$/$K_4$** لوحة داخلية موسومة `[خارج نطاق التقرير — لا محرك معتمد]` · **لا تدخل أي تصدير** (`DEC-186`).
3. **جدار العزل `§6`** نافذ في مسار الفشل كما في النجاح: تعطّل دائرة لا يُسقط الأخرى (`DEC-205`).
4. **الحزم لا تُحرَّر يدوياً** — مولّد واحد يُنتجها ببصماتها.
5. **لا وزن ولا عتبة ولا سطر يُخترَع** — الحدود من اختصاص المالك.

## 13.10 دَين مفتوح

**`DEBT-K2-BALANCE-01`** (كان `DEBT-K2-LEN-01` · `DEC-226`) — **مُغلق بالقبول (`DEC-243`).**

الرصد الأصلي: في الحالة القصوى تحمل ⑨·⑤·④·⑦ نسبة **81٪**، و③ «مركزك» **3٪**، و⑧ «أسئلة تخصّك» **0٪**.

**تصويب `DEC-242` — قياسٌ على 56 حالة لا على واحدة:**

| الشقّ | المسجَّل | المقيس |
| :--- | :--- | :--- |
| حجم ⑧ | «0٪» | **لا يبلغ الصفر قط** · 0/56 · أدناه **74 حرفاً** (`0.2٪` قُرِّب) |
| السببية | الطول يُزاحم ⑧ | **متغيّر واحد يحكم الاثنين**: عدد العدسات المشتعلة |

`عدد L ↔ عدد أسئلة ⑧ = +1.0000` · `الطول ↔ عدد L = −0.9984`.

**الحكم (`DEC-243` — الخيار أ):** الانكماش **سلوك صحيح لا نقص**، وسنده في المواصفة نفسها:

- `ت-7` (§أعلاه): الأسئلة **التمييزية** لطبقة المراجعة، والتسليم لأسئلة المنطفئة وحدها.
- `ج-5` (`DEC-207/ب`): حين لا عدسة منطفئة **فـ`ت-7` بلا هدف** — وسطر الاحتياط هو المخرج المنصوص.
- `COMPOSE-C §0.1`: **«`L` = غياب أولوية لا نقص»** — فالسؤال يخدم قراءة **غياب**، والعدسة المشتعلة موصوفة تفصيلاً في مقاطعها.

⇒ **الملفّ المشتعل كلّياً لا أسئلة منطفئة له بحقّ**، و`ج-5` مخرجه الصحيح.

**الأثر المتبقّي — مقبول لا ملغىً:** صاحب الملفّ الغني يتلقّى تقريراً وصفياً موسَّعاً بسطر توجيه واحد. و**سبع أسئلة تبقى في طبقة المراجعة** (المجموع ثابت 7 · `DEC-157`) — محجوبة لا معدومة. القبول قرار المالك، والأثر مُثبت ليُراجَع لا ليُنسى.


***
**[نهاية المواصفة — **v1.4** · محدَّثة 2026-08-05 (`DEC-183…243`) · البنية التساعية · `R1…R11` كلها منفَّذة · `DEBT-K2-BALANCE-01` مُغلق بالقبول · التالي `DEC-244`]**
***
