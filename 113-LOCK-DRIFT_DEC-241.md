# 113-LOCK-DRIFT — `DEC-241`: حارس `ح-7` وتضييق `GAP-LOCK-01`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | `GAP-LOCK-01` |
| **حالة الاعتماد** | **مشتق** — لا مفهوم جديد ولا عتبة |
| **الأدوات** | **16/16** · البصمات الثلاث ثابتة |

---

# القسم 1: العثرة — حقلٌ مُسجَّل لا يُقاس عليه

`DEC-217/ب` استثنى حقول القفل من بوابة المخرج، لأن القفل يُصاغ **بذكر** المصطلح المحظور ليمنع الخلط. و`DEC-229` أنشأ **سجلّ الذِكر المقبول** يرصد **الاستجداد**.

والسجلّ يخزّن لكل ذِكر:

```json
{ "kind": "منع", "term": "تشخيص",
  "context": "قراءتك معلومة تشغيلية، لا \"تشخيص\" (لغة محظورة) ولا انفعال",
  "verdict": "إنفاذ قفل — الصيغة نفيٌ/فصلٌ لا خلط" }
```

**و`scan_lock_fields()` لا يفحص `context` ولا `verdict`.** يفحص **وجود المفتاح** وحده:

```python
if f"{code}|{t}" not in LOCK_REGISTRY: hits.append(...)
```

⇒ تغيير «لا تشخيص» ← «تشخيص» **يمرّ**: المفتاح ما زال قائماً. وهذا عين النمط المرفوض في `DEC-236` (`auditIsolation([])`) و`DEC-238` (`missing_content`) و`DEC-240` (`template_gaps`) — **حقلٌ يُبلّغ سلامةً لأنه لا يفحص**.

---

# القسم 2: تصحيح حدس — أُثبته ولا أُخفيه

بدأتُ من أن الفحص يجب أن يرصد **أداة النفي** («لا» · «ليس» · «غير»). القياس نقض ذلك:

| القفل | الصيغة | نفيٌ صريح؟ |
| :--- | :--- | :---: |
| `K2-R-S-H+` | «سيادة عدسة الجدوى ≠ تنفيذ ميداني» | ❌ **فصل** |
| `K2-E-S-H+` | «سيادة الفهم المعرفي ≠ شدة الانفعال» | ❌ **فصل** |
| `K2-E-S-H` | «قراءتك معلومة تشغيلية، لا "تشخيص"» | ✅ نفي |

**معيار أداة النفي كان سيُنتج إنذارين كاذبين من ثلاثة** — وهو عين ما حذّر منه `ن-7/②` في نشأته. والمرساة المكينة هي **السياق المسجَّل نفسه**، لا خصيصةٌ لغوية فيه.

---

# القسم 3: `ح-7` — شرطان هما شيء واحد

> **أن الصياغة المعتمدة ما زالت تحكم كل ظهور للمصطلح في هذا القفل.**

| # | الشرط | ما يسدّه |
| :--- | :--- | :--- |
| ① | السياق المسجَّل حاضر **حرفياً** في القفل | **الانقلاب** — أي إعادة صياغة، ومنها إسقاط النفي |
| ② | تكرار المصطلح في القفل = تكراره في السياق | **الإلحاق** — ذِكرٌ ثانٍ خلف السياق المعتمد |

**الشرط ② ليس زيادة.** السياق المخزَّن **مبتور**: 7 أحرف غير مغطّاة في `K2-E-S-H` و17 في `K2-E-S-H+`. فإلحاق «وهذا تشخيص فعلاً» خلفه كان يمرّ بالشرط ① وحده.

---

# القسم 4: استيفاء `ن-7`

| الشرط | البرهان |
| :--- | :--- |
| ① **عيب معلوم** | العيب الموصوف **نصّاً** في `GAP-LOCK-01`: «لا تشخيص» ← «تشخيص». **أُوقف في الطرفين** بالسبب نفسه · وثغرة الإلحاق كذلك |
| ② **قياس واحد** | حكم الصياغة المعتمدة على المصطلح — لا غير. `scan_lock_fields` يبقى مستقلاً يرصد الاستجداد |
| ③ **تكافؤ النسخ** | ثلاث حالات (نظيفة · انقلاب · إلحاق) — المخرج **متطابق حرفياً** |
| ④ **لا عتبة** | أي انحراف يوقف · صفر وزن |

---

# القسم 5: الحدّ الباقي — مُصرَّح به

> `ح-7` **لا يحكم على المعنى.** يُثبت أن الصياغة المعتمدة لم تتغيّر، لا أن المعنى صحيح.

من يُعيد صياغة قفلٍ **عمداً** يظلّ محتاجاً إلى حكم بشري — والحكم الدلالي خارج قدرة الأتمتة كما نصّت الفجوة أصلاً.

⇒ **`GAP-LOCK-01` تضيق ولا تزول:**

| قبل | بعد |
| :--- | :--- |
| الانقلاب يمرّ · `pack_sha` يُبلّغ «حزمة تغيّرت» بلا تمييز | الانقلاب **يُرصد باسمه** ويوقف الإصدار |
| الحدّ الدلالي معلَن | الحدّ الدلالي **باقٍ ومعلَن** |

---

# القسم 6: اختبار القبول

| | النتيجة |
| :--- | :--- |
| الأدوات | **16/16** |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **ثابتة** |
| الأداتان | 12/12 بصمة · نحو سليم · 142 تقريراً · **صفر تباعد في النصّ وفي كتلة التدقيق** |

| الأداة | الحجم | `SHA` |
| :--- | ---: | :--- |
| `Kashaf_v2_FIXED_DEC-230-241.html` | 1,116,193 | `259a838d4f8f45a1…` |
| `Supervisor.html` | — | يُعاد توليدها بـ`build_supervisor_html.py` |

---

**[نهاية `113-LOCK-DRIFT` — `DEC-241` · `GAP-LOCK-01` ضيّقت]**
