# 100-AUDIT-REGEN — عقد إعادة التوليد (`DEC-220`)

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُبرهَن** (`DEC-220`) |
| **نطاق الاستدلال** | `57 §audit` · `87-PARITY §4` · `DEC-183` (فصل التسليم/المراجعة) · `ق1` |
| **الحصيلة** | **86/86 حالة أُعيد توليدها من الـ`audit` وحده** |
| **الإصدار** | **v1.1** — `DEC-221/222/223` منفَّذة |

---

# القسم 1: تفسير الشرط — أُثبته صراحةً

«كافٍ لإعادة توليد التقرير» يحتمل قراءتين:

| القراءة | المعنى | الحكم |
| :--- | :--- | :--- |
| **(أ) قابلية إعادة الإنتاج** | الـ`audit` يحمل ما يكفي **لإعادة التشغيل** فيُنتج المخرج نفسه | ✅ **المُنفَّذ** |
| **(ب) الاكتفاء الذاتي** | الـ`audit` يحمل **نصوص** الوحدات فيُعاد البناء بلا حزم | ❌ لم يُنفَّذ |

**سبب اختيار (أ):** (ب) تُضاعف التقرير داخل تدقيقه، وتُنشئ **نسخة ثانية من النصّ** قابلة للانجراف عن الحزمة — وهو صنف `DEF-K2-01` بعينه. و(أ) تكفي للتدقيق المستقل ما دام التوليد **حتمياً** — وقد صار كذلك بـ`DEF-K2-04`.

> **[مشتق]** — الاختيار بينهما لم يُنصّ. أُثبته ليُصوَّب إن خالف قصدك.

---

# القسم 2: الحقول السبعة الملزِمة

| الحقل | الوظيفة | $K_2$ قبل | $K_3$ قبل |
| :--- | :--- | :---: | :---: |
| `sp` | المُدخل — أساس إعادة التشغيل | ❌ | ✅ |
| `engine_version` · `spec_version` · `instrument_pin` | تثبيت المنطق | ❌ | ✅ |
| `entries_used` | الوحدات المستدعاة فعلاً | ❌ | ✅ |
| **`pack_sha`** | بصمة كل حزمة مستعملة | ❌ | ❌ |
| **`report_sha256`** | بصمة المخرج الذي يصفه | ❌ | ❌ |

**الحقلان الأخيران مستحدثان في الدائرتين.** بدونهما يُعاد التوليد **بلا وسيلة لإثبات التطابق** — أي إعادة إنتاج بلا تحقّق.

**الحصيلة:** $K_2$ = 18 حقلاً · $K_3$ = 24 حقلاً.

---

# القسم 3: البرهان — `verify_regen.py`

ستّ درجات، كلٌّ تقيس شيئاً واحداً (`ن-7/②`):

| # | الفحص | النتيجة |
| :--- | :--- | :---: |
| ① | البصمة المسجَّلة **تصف المخرج فعلاً** | ✅ 86/86 |
| ② | إعادة التوليد **من الـ`audit` وحده** تطابق الأصل نصّاً | ✅ 86/86 |
| ③ | بصمة المُعاد = البصمة المسجَّلة | ✅ 86/86 |
| ④ | بصمات الحزم ثابتة بين التوليدين | ✅ |
| ⑤ | الحقول السبعة حاضرة في الدائرتين | ✅ |
| ⑥ | **حساسية**: تغيّر حرف واحد في حزمة **يُغيّر بصمتها** | ✅ |

**الدرجة ⑥ هي الحاسمة:** بلا حساسية مُثبَتة تصير `pack_sha` زينةً لا حارساً. اختُبرت بإضافة نقطة واحدة إلى مدخل في `INTENSITY_K2` — فتغيّرت البصمة.

**والدرجة ② تُشغَّل من `audit["sp"]` حصراً** — لا يُمرَّر شيء من المُدخل الأصلي.

---

# القسم 4: التكافؤ

| البند | بايثون | JS |
| :--- | :--- | :--- |
| `report_sha256` ($K_2$) | `2507a5558646f4f9` | **مطابقة** |
| `report_sha256` ($K_3$) | `43fd2a3d75e55aa3` | **مطابقة** |
| `pack_sha` ($K_2$ — 5 حزم) | — | **5/5 مطابقة** |
| `pack_sha` ($K_3$ — 3 حزم) | — | **3/3 مطابقة** |

**التطابق ليس تحصيل حاصل:** التجزئة تعتمد تسلسلاً مقنَّناً (مفاتيح مرتَّبة بنقطة الترميز · بلا فراغ)، ونُفّذ في البيئتين بدالتين متناظرتين. ولولا قواعد `87-PARITY §4` لاختلفت البصمتان على المحتوى نفسه.

**وداخل الأداة نفسها:** `buildReportK2(audit.sp)` أعطى البصمة ذاتها — أي أن العقد يعمل في المنتج لا في المختبر فقط.

---

# القسم 5: ما فتحه هذا القرار

| البند | الأثر |
| :--- | :--- |
| **أداة المشرف** | صارت **تدقيقاً مستقلاً** لا عرضاً: تأخذ `JSON`، تُعيد التوليد، تقارن البصمة |
| **إثبات النزاهة** | تقرير + `audit` = وحدة قابلة للتحقق بلا رجوع إلى الأصل |
| **رصد انجراف الحزم** | `pack_sha` في كل تقرير ⇒ تقريران بحزم مختلفة **يُكشفان فوراً** |
| **`DEC-202/②`** | `frozen_layers` صار جزءاً من سجلّ قابل للتحقق لا حقلاً معلَّقاً |

---

# القسم 6: `DEC-222` · `DEC-223` — منفَّذان

## `DEC-222` — سجلّ الاستدعاء موسَّع

`entries_used` صار يشمل **كل وحدة مستدعاة** لا مداخل طبقة المستخدم وحدها:

| الصنف | النمط | العدد (حالة مرجعية) |
| :--- | :--- | :-: |
| مداخل طبقة المستخدم | `K2-{X}-U-##` | 46 |
| **كتل الشدة** (`R1`) | `K2-{X}-S-{band}` | 9 |
| **أسطر التركيب** (`ت-7`) | `{مركز}-{عدسة}-{حالة}` | 6 |
| **قواميس التطهير** (`R9`) | `PUR-{X}` | 4 |
| **مصفوفة التمييز** (`R11`) | `LD-02` | 1 |
| **الإجمالي** | | **66** |

**الأنماط الخمسة لا تتصادم:** الفصل بسابقة `K2-` والموضع. فـ`H-S-L` (سطر تركيب: مركز `H` · عدسة `S` · حالة `L`) و`K2-H-S-L` (كتلة شدة) **متمايزان بنيوياً**.

## `DEC-223` — بصمة `CONTENT_K2` حقيقية

كانت بصمة **كائن فارغ**؛ صارت بصمة الحزمة الفعلية:

| المصدر | البصمة |
| :--- | :--- |
| `packs_manifest.json` | `2b3db06c25525b6a` |
| `audit.pack_sha.CONTENT_K2` | `2b3db06c25525b6a` |

**التطابق مع البيان المستقلّ تحقّق ثانٍ** — البصمة تصف الحزمة المستعملة فعلاً، لا كائناً فارغاً.

## الحدّ الباقي

**العقد لا يحفظ الحزم.** إن تغيّرت حزمة، إعادة التوليد تُنتج مخرجاً مختلفاً و`pack_sha` **يكشف السبب**، لكنه **لا يستعيد** النسخة القديمة. أرشفة الحزم خارج نطاق هذا العقد.

---

# القسم 7: المطلوب

| # | البند | الحالة |
| :--- | :--- | :---: |
| `DEC-221` | إقرار تفسير (أ) — قابلية إعادة الإنتاج | ✅ |
| `DEC-222` | توسيع `entries_used` — 66 وحدة عبر خمسة أنماط | ✅ |
| `DEC-223` | بصمة `CONTENT_K2` الحقيقية | ✅ |

**لا بند معلَّق في هذا العقد.** الترقيم التالي: `DEC-224`.

---

> **[نهاية الوثيقة — `100-AUDIT-REGEN` v1.1 · 86/86 · العقد مُبرهَن في البيئتين وفي الأداة]**
