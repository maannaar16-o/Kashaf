# 127-MIRROR — `DEC-255` · `DEC-256`: اعتماد عُدّة التحقق الذاتي ومرآتها التفاعلية

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | ① اعتماد `55-USER-K3-REFLECT` · ② بناء مرآة «هل هذا أنا؟» التفاعلية في التطبيق |
| **حالة الاعتماد** | **مختومان بأمر المالك** — 2026-08-11 |
| **الأثر على المحرّكات والحزم** | **صفر** — النصوص تُستهلك من الحزم قراءةً · البصمات الثلاث ثابتة |
| **التغيير المرافق** | `CHG-073` |

---

# §1 `DEC-255` — اعتماد الوثيقة (v0.1 مسودة → v1.0 معتمدة)

**الاكتشاف الموثَّق أولاً:** نصوص العُدّة التشغيلية (`VERIFY_BLOCK` · `VERIFY_QUESTIONS` · `VERIFY_CLOSING` · `SEPARATION_QS`) **حيّة في حزم المحرك منذ بنائها** وتظهر في كل تقرير $K_3$ (القسم ٤ شرطياً والقسم ٨ دائماً)، مجمَّدة ببصمة التقارير `36ae94bfd5a8b60f`. فالاعتماد ختمُ توثيقٍ لحالةٍ قائمة، لا إجازة محتوى جديد.

**شرط الختم — فحص المطابقة الآلي: 18/18.** ثلاث فقرات الكتلة الافتتاحية · الأسئلة الأربعة بجوهرها · الخاتمة · الصفوف الثمانية بعناوينها · العدّ = 8 · ملاحظة «ليست اختباراً» — كلها متطابقة بين الوثيقة والحزم الحية (بعد تطبيع التنسيق: جدول في الوثيقة / قوائم في الحزمة). صفر فرق جوهري.

---

# §2 `DEC-256` — المرآة التفاعلية

## ① المبدأ: من نصٍّ يُقرأ إلى مرآةٍ تُعاش

الترتيب معكوس عمداً: **استحضار موقف المستخدم الحقيقي أولاً**، ثم السؤال الفاصل، ثم «ماذا يكشف» — فيهبط التمييز على موقف حاضر لا على سطر يُقرأ عابراً.

## ② البنية

| المكوّن | البيان |
| :--- | :--- |
| **المصدر الوحيد** | النصوص تُسحب من `PACKS.TEXTLAYER_K3` وقت التشغيل — **فحص بناء يمنع أي نسخة مقتبسة في كود التطبيق** (بصمات مميِّزة من النصوص يُوجب غيابها عن `app.js`) |
| **مسار البطاقات (S4)** | فكّ جدول `SEPARATION_QS` وقت التشغيل (مقدمة · 8 صفوف · خاتمة — عدّها مُتحقَّق في فحوصات البناء) · إيقاع رباعي لكل بطاقة: السلوك ← الاستحضار ← السؤال الفاصل ← ماذا يكشف |
| **مسار سؤال القوة (S5)** | البوابة من **نطاقات كتلة تدقيق السجل المؤرشف** (`bands == high` كما حسبها المحرك) — لا رقم يُنقل · `EP` مستثناة (`FR-05`) · صفر قدرات عالية ⇒ المسار لا يُعرض |
| **الدفتر** | تأمل حر اختياري يُحفظ على سجل الأرشيف نفسه (`mirror.notes`) — محلي، يدخل النسخة الاحتياطية، **ولا يدخل الإسهام المجهّل بنيوياً** (باني الحمولة محصور المفاتيح بفحص `DEC-253`) |
| **مداخلها** | زر في عارض الأرشيف (حيث `k3` موجود) وأسفل التقرير الطازج — سياق $K_3$ وحده (`DEC-205`) |

## ③ قراءة تشغيلية تُختم هنا

**الاختيار في ذهن المستخدم لا في أزرار خيارات**: السؤال الفاصل يُعرض حرفياً ويجيب عنه المستخدم داخلياً ثم يكشف «ماذا يكشف» — تفكيك نص السؤال المختوم إلى أزرار خيارات كان سيعيد تأليف نص مختوم، فرُفض. وهو أوفى لمبدأ العُدّة: «سؤال يعرف جوابه أنت وحدك».

---

# §3 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| فحص المطابقة (شرط `DEC-255`) | **18/18** |
| اختبار المتصفح | **18/18** — البوابة بالاتجاهين (ملف عالي القدرات يرى كتلة القوة بأسئلة قدراته العالية الثلاث حصراً — و`ST` عند 68.2 لم تدخل بحساب المحرك لا باجتهاد الواجهة · ملف منخفضها لا يرى المسار أصلاً) · التدفق الرباعي بنصوص الحزم حرفياً · الدفتر يُحفظ على السجل ويظهر عدُّه · التقرير المؤرشف لم يُمسّ · صفر لغة عيادية في كل الشاشات |
| فحوصات البناء الجديدة | لا نص مختوم منسوخ في `app.js` · صفوف الفصل = 8 · مفاتيح أسئلة التحقق = `BI/CF/IR/ST` |
| الأدوات الستّ عشرة | 16/16 · البصمات الثلاث حرفياً — وبصمة التقارير `36ae94bfd5a8b60f` شاهدة أن النصوص لم تُمسّ |

---

**[نهاية `127-MIRROR` — `DEC-255` · `DEC-256` · الترقيم التالي `DEC-257` · `CHG-073`]**
