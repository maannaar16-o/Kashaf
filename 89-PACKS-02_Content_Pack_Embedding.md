# 89-PACKS-02 — المرحلة ② : تضمين حزم المحتوى الخمس

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** — لا قرار معلَّق |
| **نطاق الاستدلال** | `k2_userlayer_pack` · `k2_contentpack` · `skill_sections` · `three_texts` · `circle_map_v2` (`DEC-195/ج`) |
| **الحصيلة** | **207,549 بايت** محتوى · **1,085 وحدة نصّية** · صفر انحراف عن المصدر |
| **الإصدار** | v1.0 |

---

# القسم 1: المبدأ الحاكم — التوليد لا النسخ

**لم أنسخ حرفاً بيدي.** `build_packs.py` يقرأ الحزم الخمس من مصادرها، ويسلسلها بصيغة مقنَّنة واحدة (مفاتيح مرتَّبة · بلا مسافات · فرز بنقطة الترميز — `87-PARITY §4`)، ويحسب بصمة `SHA-256` لكل واحدة، ثم يولّد `packs.js`.

**السبب:** النسخ اليدوي لـ207 كيلوبايت من النصّ العربي هو **بالضبط** الموضع الذي يتسرّب منه انجراف حرف واحد لا يُرى في القراءة — وهو صنف `DEF-K2-01` نفسه. التوليد يجعل الانجراف **مستحيلاً بالبناء** لا مرصوداً بالفحص.

---

# القسم 2: البيان

| الحزمة | المصدر | الحجم | البصمة |
| :--- | :--- | ---: | :--- |
| `USERLAYER_K2` | `55-USER-K2-*` — 8 عدسات | 105,737 | `8927e5f7eb336116` |
| `CONTENT_K2` | `COMPOSE-{X}` — 8 معاجم | 84,996 | `2b3db06c25525b6a` |
| `SECTIONS_K3` | `55-USER-K3-* §1` | 9,814 | `c85fb62b63c2666c` |
| `THREE_K3` | `80-K3-TEXTS` | 3,854 | `d6eb5c864000b5b0` |
| `CIRCLE_K3` | `79-K3-USERMAP §2` (`DEC-195/ج`) | 3,148 | `46efcf73cd28c20d` |
| **الإجمالي** | | **207,549** | ملف `packs.js` = 218,644 بايت |

---

# القسم 3: `SHA-256` مستقلّ داخل الأداة

الأداة تعمل **بلا شبكة** (`file://`)، و`crypto.subtle` غير متاح في هذا السياق ولا متزامن. فأُدرج تنفيذ `SHA-256` كامل داخل `packs.js`.

**التحقق منه لا الثقة به:**

| المتّجه | النتيجة |
| :--- | :---: |
| `SHA-256("")` = `e3b0c442…` | ✅ |
| `SHA-256("abc")` = `ba7816bf…` | ✅ |
| `SHA-256("الملاحظة")` — JS مقابل بايثون | ✅ **متطابق** (`a2bc8b7eea3a5d0b`) |

المتّجه الثالث هو المهمّ: **يثبت سلامة ترميز `UTF-8` للحرف العربي** — والحزم كلها عربية.

---

# القسم 4: `test_packs.py` — ثلاثة فحوص منفصلة

عملاً بـ`ن-7/②` (كل فحص يقيس شيئاً واحداً):

| الكود | الفحص | يمنع |
| :--- | :--- | :--- |
| `ح-1` | سلامة البصمات | انجراف `packs.js` عن بصمته المثبَتة |
| `ح-2` | مطابقة المصدر | تباعد محتوى JS عن بايثون عنصراً بعنصر |
| `ح-3` | اكتمال البنية | مفتاحاً غائباً أو فارغاً (8 عدسات · 8 معاجم · 5×4 مفاتيح) |

## برهان `ن-7/①` — الاختبار يسقط على عيب معلوم

حقنتُ **تعديل حرف واحد** في نصّ مضمَّن («كلها» ← «كلّها»)، فأخرج الاختبار:

```
❌ ح-1: CIRCLE_K3 — البصمة d088cd61dc622cc3 ≠ 46efcf73cd28c20d
❌ ح-2: CIRCLE_K3 — المحتوى مختلف عن circle_map_v2.json
```

**و`ح-3` بقي أخضر** — لأن البنية سليمة والمحتوى وحده تغيّر. هذا **دليل عملي على `DEC-198`**: لو كانت الفحوص الثلاثة مدموجة في واحد، لأعطى إنذاراً واحداً غامضاً بدل تحديد الطبقة المصابة بدقة.

---

# القسم 5: ما لم يتغيّر

| البند | الحالة |
| :--- | :---: |
| التكافؤ (114 حالة) | ✅ البصمة `ef7b161006c30529` — لم تتأثر |
| `test_golden_k2` · `test_golden_k3` | ✅ لا انحدار |
| `test_report_k2` · `test_report_k3` | ✅ لا انحدار |
| حارس $K_3$ (`ص-1…ص-6`) · `validate_slots` | ✅ صفر شذوذ |

**الحزم بيانات لا منطق** — لذلك لم تمسّ أياً من الطبقات السابقة، وهذا متوقَّع ومقيس لا مفترض.

---

# القسم 6: المخرجات

| الملف | الوصف |
| :--- | :--- |
| `packs.js` | الحزم الخمس + `SHA-256` + `verifyPacks()` — **لا يُحرَّر يدوياً** |
| `build_packs.py` | المولّد — يُعاد تشغيله عند أي تحديث لحزمة |
| `test_packs.py` | انحدار الطبقة — `ح-1 · ح-2 · ح-3` |
| `packs_manifest.json` | بيان البصمات والأحجام |

---

# القسم 7: الحالة والخطوة التالية

| المرحلة | الحالة |
| :--- | :---: |
| ① نقل المحرّكين | ✅ **مكتملة** — تكافؤ 114 حالة |
| ② تضمين الحزم | ✅ **مكتملة** — 207KB · صفر انحراف |
| ③ نقل طبقة التقارير | 🟢 **جاهزة** |
| ④ الواجهة والتصدير | ⬜ |
| ⑤ الانحدار الشامل داخل الأداة | ⬜ |

**المرحلة ③ تحمل القرارات المؤجَّلة:** `DEC-189` (الدمج الطبقي — ٩ أقسام) · `DEC-183/187` (حذف `SP%` · الوسم المزدوج) · `TC-K2-01` (نطاق كتالوج المركز — **يُحسم بالوظيفة لا بالرقم** بعد `DEC-190`) · `GAP-RPT-K2-01/02/03`.

**الترقيم التالي:** `DEC-205`.

---

> **[نهاية الوثيقة — `89-PACKS-02` v1.0 · الحزم مضمَّنة ومحروسة · لا قرار معلَّق في هذه المرحلة]**
