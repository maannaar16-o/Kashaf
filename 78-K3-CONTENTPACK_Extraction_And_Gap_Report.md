# 78-K3-CONTENTPACK_Extraction_And_Gap_Report.md
## استخراج حزمة المحتوى الخارجية — التقرير والفجوات

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `78-K3-CONTENTPACK` |
| **الإصدار** | **v0.1 — 🟡 `[مقترح]`** (القرار المقترح: `DEC-138`) |
| **الغرض** | تغذية المحرك بالمفاتيح الثمانية الخارجية — شرط أول تقرير كامل |
| **الحصيلة** | **5/8 متاح ومنقول حرفياً · 3 بلا مصدر نصّي · 1 متعارض مع خط الأساس** |
| **السند** | `REFLECT` v0.1 · `55-USER-K3-*` v1.0 · `SPEC` v2.1 §7/§8.1 · `M3-04` v1.2 |
| **🔒 المبدأ** | **صفر تأليف.** ما لا مصدر له تُرك فارغاً ورُفعت به فجوة — لم يُملأ بأقرب نص |

---

# القسم 1: ✅ المتاح — منقول حرفياً (5)

| المفتاح | المصدر | الحالة |
| :--- | :--- | :--- |
| `trust_banner` | `REFLECT §4` | ✅ صيغة مستخدم معتمدة |
| `verify_block` | `REFLECT §1.3` | ✅ كامل + 4 أسئلة بالمهارة |
| `separation_qs` | `REFLECT §2` | ✅ الثمانية كاملة |
| `skill_sections` | `55-USER-K3-*` §1 (`U-01` · `U-08/09` · `§2`) | ✅ متاح — يُحمَّل بالسحب الشرطي `P7` |
| `circle_map` | `55-USER-K3-*` §0-ب | ⚠️ **متاح ومتعارض** — انظر §3 |

**نتيجة التشغيل:** المحرك أنتج **القسم ٦ كاملاً** على `SYN-01` · **`G5` صفر مخالفة** (شاملاً فحص المحتوى المحمَّل، المرحلة الأولى من `DEC-137`/3).

---

# القسم 2: 🔴 الغائب — ثلاثة مفاتيح بلا مصدر نصّي

| # | المفتاح | ما وجدتُه فعلاً | الخطورة |
| :--- | :--- | :--- | :--- |
| **`GAP-PACK-01`** | **`covenant_opening`** (فاتحة العهد) | `SPEC §7` يسجّله «**نص ثابت**» و**لا يورده**. الموضع الوحيد الذي يذكره — `57-ENGINE_Blueprint` — **مؤرشف** (`DEC-130`) ولا يُسحب منه نص. **النص غير موجود في قاعدة المعرفة إطلاقاً** | 🔴 **حاجبة** — القسم ١ من كل تقرير |
| **`GAP-PACK-02`** | **`out_text`** | `SPEC §8.1` يورد **شذرة بعلامة حذف**: «يستهلك من طاقتك حالياً أكثر مما يخدمك… قراءة لحالتك في هذه المرحلة». نصّ ناقص لا يصلح للنسخ. *(مداخل `U-10` لكل مهارة كاملة — لكنها «حالة الضغط القصوى» لا نصّ `OUT` العام)* | 🟡 متوسطة — يظهر في حالات `OUT` فقط |
| **`GAP-PACK-03`** | **`reflective_frame`** | `M3-04` v1.2 مكتوب **بلغة معمارية بالكامل**. لا صيغة مستخدم فيه، وبنك `71`/`55-USER-K3-CMP` لا يغطّي القسم ٩ | 🟡 متوسطة — القسم ٩ الختامي |

**🔒 لم يُملأ أيٌّ منها.** الثلاثة مُدرجة في `DELIBERATELY_ABSENT` بعلّتها، والمحرك يرفع `MissingContentError` عند طلبها.

---

# القسم 3: ⚠️ `GAP-PACK-04` — تعارض حقيقي في `circle_map`

**الأخطر في هذا التقرير — ولم يكن مرصوداً.**

`55-USER-K3-*` §0-ب اسمه «**إطار السلسلة**»، ونصّه للمستخدم:

> «مهاراتك الخمس … **حلقات في سلسلة واحدة متصلة**، كلٌّ تسلّم عملها للتالية»
> «**ضعف حلقة مبكرة يُثقل كل ما بعدها**»

**والتعارض:** `DEC-098` ألغى الطوبولوجيا الخطية واعتمد **البنية المحورية** (`IR` مركزاً · حافات عودة `د-2` · `BI`/`CF` شقيقتان بلا ترتيب). فوصف «سلسلة متصلة» و«حلقة مبكرة» **سقط مرجعه** — وهو **نصّ يقرؤه المستخدم في كل تقرير**.

| ما يصمد | ما سقط |
| :--- | :--- |
| تسلسل الأفعال: **تلاحظ ← تنظّم ← تقرر ← تكيّف ← تحتمل** — يطابق مسار المعالجة حرفياً | تسمية «سلسلة» · «حلقات» · «كلٌّ تسلّم للتالية» · «حلقة مبكرة تُثقل ما بعدها» |

**⚠️ ملاحظة سيادية:** هذا **نفس العيب** الذي أصلحتُه في `SPEC` v2.0 (غيّرتُ «خريطة السلسلة» ← «خريطة الدائرة»). أصلحتُ الاسم في المواصفة **ولم أتتبّعه إلى مصدره** في طبقة المستخدم — وهو ما تقيسه القاعدة `ن-1` (تتبّع السند) من بوابة النقد.

**المطلوب:** إعادة صياغة §0-ب في الملفات الخمسة — **قرار سيادي**، لأنه يمسّ نصّاً معتمداً بلغة المستخدم (`DEC-074`).

---

# القسم 4: خيارات سدّ الفجوات الثلاث

| # | الخيار | الحكم |
| :--- | :--- | :--- |
| **(أ)** | **صياغة النصوص الثلاثة** ثم اعتمادها في `55-USER-K3-CMP` أو وثيقة نصّية جديدة | ✅ الوحيد المشروع — بشرط أن **تُعرض للاعتماد لا تُدرَج تلقائياً** |
| (ب) | سحبها من `Blueprint` المؤرشف | 🔴 **ممنوع** — `DEC-130` ينصّ: «لا يُستشهد بها تشغيلياً ولا تُسحب منها صياغة» |
| (ج) | إنتاج التقرير بدونها | 🔴 يُخرج تقريراً بلا فاتحة ولا إطار ختامي — **بنية ناقصة** |

---

# القسم 5: بنود الاعتماد المطلوبة (`DEC-138` المقترح)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | اعتماد الحزمة الجزئية (5/8) مصدراً نصّياً للمحرك | ⬜ |
| 2 | **صياغة النصوص الثلاثة** الغائبة وعرضها للاعتماد (الخيار أ) | ⬜ |
| 3 | **حسم `GAP-PACK-04`** — إعادة صياغة §0-ب في الملفات الخمسة | ⬜ **الأخطر** |
| 4 | تثبيت أن الثلاثة **لا تُسحب من `Blueprint`** المؤرشف | ⬜ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| المفاتيح المحمَّلة | **5/8** — منقولة حرفياً |
| `G5` على المحتوى | ✅ **صفر مخالفة** |
| القسم ٦ | ✅ **يُنتَج فعلياً** — أول نصّ مركّب من محتوى حقيقي |
| فجوات مرفوعة | `GAP-PACK-01…04` |
| صفر تأليف | ✅ **لم يُملأ مفتاح واحد بالتخمين** |
| **حالة الوثيقة** | 🟡 **`[مقترح]` — بانتظار `DEC-138`** |
