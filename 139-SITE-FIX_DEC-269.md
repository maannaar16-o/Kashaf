# 139-SITE-FIX — `DEC-269`: تصويب بناء الموقع وحرسه في البوابة

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفذ ① و④**» — 2026-08-13 (بعد سؤاله: «هل تقرير K4 يعمل الآن على الموقع؟») |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-13 |
| **الأثر على المحرّكات والحزم والأداة** | **سباكة لا سلوك**: البصمات **الأربع ثابتة حرفياً** (بصمة $K_4$ `7361580541033f31` لم تتحرّك) · `docs/` أُعيد توليدها · البوابة **19 ← 20** |
| **التغيير المرافق** | `CHG-086` |
| **الكود** | `139` — تمديد بمسوّغه (درس `GAP-CPL-02`): تصويبٌ عارض لا مرحلة بناء |

---

# §1 الجواب الصريح على السؤال — ووصف العيب

**كان الجواب: لا.** تقرير $K_4$ **لم يكن يعمل على الموقع، وبناء الموقع نفسه كان متوقفاً**:

```
AssertionError: بقي استيراد غير مُشيَّم في reports.js
```

## ① سببه المباشر

إضافات المرحلة ٨ و`DEC-268` أدخلت **أربعة استيرادات** إلى `reports.js` (`engines.js` مكرَّراً · `k4_contentpack.json` مرتين داخل الدوال · `crypto`)، والباني يُشيِّم استيرادات معروفة ثم **يتحقق أن لا `require` بقي** — لأنه غير موجود في المتصفح. فالحارس عمل عمله ومنع إصدار أداة معطوبة.

## ② العيب الأخطر: **البوابة لم تكن ترى الانكسار**

البوابة الـ19 **لا تشمل `build_site.py`** — فمرّ الانكسار **صامتاً في التزامَين** (`ecf7c3d` و`34ecb57`)، وبقيت `docs/` نسخةً لا تعرف $K_4$. وهذا **فراغٌ في البوابة نفسها** لا في الكود: عملٌ يُبنى ولا يُفحص.

---

# §2 البند ① — التصويب: تشييمٌ لا تسطيح

| الاستيراد | التصويب |
| :--- | :--- |
| `require("./engines.js")` مكرَّراً لـ`K4` | ضُمَّ `K4` إلى **سطر الاستيراد القائم** وحُذف المكرَّر — استيرادٌ واحد للمحرّكات |
| `require("./k4_contentpack.json")` **داخل دالّتين** | نُقل إلى **سطر واحد قابل للتشييم** (`K4_PACK`) ودالّة `k4RequirePack` تتحقق منه |
| `require("crypto")` | استُبدل بـ`PK._sha256` — **وهو سها-256 حقيقي مُصدَّر من `packs.js` وقيمته مطابقة لـ`crypto` حرفياً** (مفحوص قبل الاستبدال). فلا مسار خاص بالعقدة، ولا احتمال تباعد بين البيئتين |

## ③ حزمة $K_4$ في المتصفح — **رفضٌ مكتوب لا فشلٌ صامت**

البند ② (ضمّ الحزمة إلى `packs.js`) **مؤجَّل بأمر المالك**. فالتشييم يجعل `K4_PACK` **`null` صريحاً** في المتصفح، وأي استدعاء لتقرير $K_4$ هناك يرفع:

> `حزمة K4 غير محمَّلة — لا تقرير بلا حزمة (صفر تأليف · DEC-266)`

وهذا وفاءٌ لقاعدة «صفر تأليف»: لا تقرير بلا حزمة، ولا حزمةٌ تُلفَّق عند الغياب.

## ④ برهان أنه سباكةٌ لا سلوك

**التكافؤ أُعيد قياسه بعد التصويب مباشرة: 387 حالة · صفر تباعد · البصمة `7361580541033f31` لم تتحرّك.** فالتغيير مسّ **طريق التحميل** لا **المخرج** — وهذا هو الفرق بين السباكة والسلوك، والبصمة هي ما يفصل بينهما.

---

# §3 البند ④ — الحرس: `test_site_build.py` (البوابة 19 ← 20)

| الفحص | ما يقيسه فعلاً |
| :--- | :--- |
| **اكتمال التشييم** | يجمّع الحزمة ويتحقق أن **لا `require` باقٍ** — وهو الانكسار عينه الذي مرّ صامتاً |
| **صفر تصدير غير محروس** | التصدير **بلا** حارس `typeof module` هو التسرّب؛ أما المحروس فهو النمط الصحيح للبيئتين — والفحص الأول كان **أعمى** فعدّ الحرّاس تسرّباً، فصُوِّب ليقيس ما يعلنه |
| **$K_4$ محمَّل ولا مُسطَّح** | المحرك حاضر · الحزمة مشيَّمة إلى `null` · رسالة الرفض مكتوبة · **ولا `buildReportK4` في التطبيق** (البند ③ مؤجَّل — فالحرس يحرس التأجيل نفسه) |
| **`docs/` مزامنة** | يبني بناءً طازجاً ويقارنه بالمولَّد المُلتزَم — **فالتقادم لا يمرّ صامتاً** |

## ① الحرس **مُختبَر على فشله** لا على نجاحه فقط

لا يكفي أن يمرّ الحرس؛ يلزم أن **يكشف**. فحُقن الانكسار مرتين:

| المحاكاة | النتيجة |
| :--- | :--- |
| استيراد غير مُشيَّم (`require("os")`) | ❌ رُصد بأربعة فحوص — **بصياغة نظيفة لا رمي خام** (الاستثناء يُلتقط ويُصاغ، وبقية الفحوص تُكمل بدل أن تُبتَر) |
| تعديل بايت واحد في `docs/index.html` | ❌ «متقادمة: `index.html` — أعد التوليد» |

ثم استُرجعت الحالة وعاد الحرس أخضر. **فحرسٌ لا يُختبَر على فشله دعوى، لا قياس.**

---

# §4 ما لم يُفعل — بحدٍّ معلَن

| البند | حاله |
| :--- | :--- |
| **② ضمّ حزمة $K_4$ إلى `packs.js`** | ⏸ **مؤجَّل بأمر المالك** — وحتى ذلك، الرفض مكتوب في المتصفح |
| **③ سطح $K_4$ في التطبيق** (تبويب · جسر أوعية · السطح العابر) | ⏸ **مؤجَّل بأمر المالك** — والحرس **يحرس التأجيل**: أي تسطيح يُرصد |
| رفع `DEC-186` عن $K_1$ | ❌ لم يُمسّ — كما كان |

> **فتقرير $K_4$ اليوم يعمل في البوابة والاختبارات (387 حالة متكافئة) ولا يعمل في يد مستخدم** — وهذا **حدٌّ مصرَّح به** لا نقصٌ مسكوت عنه.

---

# §5 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| بناء الموقع | ✅ **يعمل** — 8 صفحات · بصمة البناء `8f5a97ac0dd7fdb8` · `docs/` أُعيد توليدها |
| البصمات الأربع | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` · `7361580541033f31` — **حرفياً** |
| الأدوات | **20/20** (`test_site_build.py` مستجدّة) |
| الحرس مُختبَر على فشله | ✅ انكساران محقونان · رُصدا · واسترُجعت الحالة |

---

**[نهاية `139-SITE-FIX` — `DEC-269` · الترقيم التالي `DEC-270` · `CHG-086` · البوابة 20 · البندان ② و③ مؤجَّلان بأمر المالك]**
