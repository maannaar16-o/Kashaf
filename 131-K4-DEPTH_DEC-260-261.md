# 131-K4-DEPTH — `DEC-260`/`DEC-261`: اعتماد النوع الخامس «تفاقم» + طبقة العمق (بطاقات الصمامات السبعة)

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمرا المالك (2026-08-12): «**اعتمد حالة الضعفين `TM×PER`**» و«**نفذ طبقة العمق — 7 وثائق `50-DEPTH-K4-*`**» |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-12 |
| **الأثر على المحرّكات والحزم والأداة** | **صفر** — وثائق تفسيرية · البصمات الثلاث ثابتة · لا قياس، لا وزن، لا عتبة جديدة (ن-7/④) |
| **التغييران المرافقان** | `CHG-077` (`DEC-260`) · `CHG-078` (`DEC-261`) |
| **الحاكم** | `129-K4-DUAL-CHARTER` (`DEC-258`): قفل الباب `§3/④` · قالب القيد `§4` — و`130-K4-RELATIONS` (`DEC-259`) — و`DEC-136` (التكييف المشروط، كل نقل بقيده في `77-LOG`) |
| **الكود المحجوز** | هذه الوثيقة تستوفي حجز `131` — المرحلة ٣ (`77-LOG §2`) |

---

# §1 `DEC-260` — حسم المرفوع: حالة الضعفين `TM×PER` تُعتمد نوعاً خامساً «تفاقم»

## ① نص الحسم

المرفوع في `130 §4/أ` عُرض على المالك بخياريه المصرَّحين (نوع خامس باسم يختاره / قراءة اجتماع هدرين)، **فأمر: «اعتمد حالة الضعفين `TM×PER`»** — اعتمادُ الحالة اعتمادٌ للنوع الذي رُفعت مرشَّحةً له: **النوع الخامس**. عملت بوابة قفل الباب (`129 §3/④`) عملها كما صيغت: لا نوع أُضيف ارتجالاً، ولا تعريف مُطّ — رُفع بدليله البنيوي وقرر المالك.

## ② التسمية — «تفاقم»

الأمر لم يحمل اسماً بديلاً، **فاعتُمد لفظ الميثاق المختوم نفسه**: «تفاقم ضعفٍ بضعف» (`129 §3/②` — نصٌّ سبق ختمه بـ`DEC-258`) — أدنى الأسماء اختراعاً وأصدقها نسباً. **وللمالك تبديل الاسم بقرار لاحق دون مساس بالقيد.**

## ③ التعريف المختوم والشرط البنيوي وقاعدة البنية المتبادلة

| الحقل | البيان |
| :--- | :--- |
| **التعريف** | ضعفُ مهارةٍ يضاعف أثر ضعف مهارة أخرى، فيقع الأثر المُسقِط المشترك فوق اجتماع الضعفين منفردَين |
| **الشرط البنيوي** | طرفان ضعيفان · أثرٌ مُسقِط · **بنية متبادلة** |
| **ما يميزه عن جاره** | ليس هدراً: لا قوة تُستنزف في أي من الطرفين — وهو **المرآة السالبة للتعزيز** (قويّ×قويّ رافعٌ فوق المجموع ↔ ضعيف×ضعيف مُسقِطٌ فوق المجموع) |
| **التمايز على الشبكة** | بتوسيع شبكة `129 §3/②` بحال المفعول: تعزيز (قوي×قوي·رافع) · مؤازرة (قوي×ضعيف·سانِد) · تحييد (قوي×قوي·مُسقِط) · هدر (ضعيف×قوي·مُسقِط) · **تفاقم (ضعيف×ضعيف·مُسقِط)** — خانة خامسة لا يشاركه فيها أحد؛ برهان تمايز قائم، وادّعاء الاستغراق ممتنع كما كان (`م-3`) |
| **قاعدة البنية المتبادلة** | التبادل في **صميم الشرط البنيوي** للنوع (لا فاعل ولا مفعول)، فزوج التفاقم **قيد واحد لا قيدان** — **استثناء معلَن** من قاعدة «العلاقة المتبادلة قيدان» (`129 §4`) خاص بهذا النوع حصراً |

## ④ القيد المسجَّل — `K4-REL-12`

أُلحق بذيل `130 §2` (قسم «تفاقم») بنصه الكامل: `TM (ضعيف) × PER (ضعيف)` · دليله `SEED-K4-04` نصاً («وقت منخفض + إكمال منخفض» — تدريب واحد **مركّب**: «إغلاق داخل نافذة زمنية» يخاطب الضعفين معاً، فمقصد التصميم يقرأ الأثر مركّباً لا مجموعاً) مسنوداً بتعريفَي الدستور · وبحقل «ما لا يُدَّعى» الثلاثي: **لا معامل تفاعل ولا جمع عددي** («فوق الاجتماع» قراءة وصفية لا قياس) · **لا تعميم** (ليس كل اجتماع ضعفين تفاقماً — القيد لهذا الزوج بدليله) · **لا سببية بين الضعفين**.

## ⑤ حدود الاعتماد

- ❌ لا يفتح الاعتمادُ الباب: **لا سادس ارتجالاً** — قاعدة `129 §3/④` نافذة كما هي لأي مرشَّح تالٍ.
- ❌ لا يمس تصنيف $K_3$ الخماسي (`TRF-005` — التصنيفان لا يُترجمان) ولا حالة «تحييد» (مختوم صفر القيود — `130 §5`).
- **التعديلات الموضعية المؤشَّرة بالقرار:** `129 §3/①` (صف خامس) · `129 §3/②` و`§3/④` و`§4` (إحالات حسم) · `130 §2` (قسم تفاقم + `K4-REL-12`) · `130 §3` (عدّ التغطية) · `130 §4/أ` (المرفوع حُسم) · `130 §6` (الإحصاء).

---

# §2 `DEC-261` — طبقة العمق: **بطاقة الصمام** قالباً، وسبع وثائق `50-DEPTH-K4-*`

## ① القالب — بطاقة $K_4$ المخصصة (`TRF-009`: نُقل مبدأ السلسلة لا بنية بطاقاتها)

درسُ السلسلة نفسها: بطاقة $K_3$ لم تُستنسخ من $K_2$ (`50-DEPTH-K3-EP §0.2`) — وبطاقة $K_4$ لا تُستنسخ من كليهما. **بنيتها مشتقة من شكل $K_4$ المختوم (مسار + شبكة — `129 §2`)**، محاورها:

| المحور | المضمون | الوسم الغالب |
| :--- | :--- | :--- |
| 0 · رأس الحوكمة | السند · أقفال العزل المنطبقة · قاعدة الوسم | [معتمد] |
| 1.1 · الهوية التنفيذية | التعريف الحرفي · سؤال التشغيل الداخلي · الوعاء (11 بنداً) | [معتمد] |
| 1.2 · الموضع على المسار | المحطة `N/7` · **ما يستقبله (يستهلكه) · ما يسلّمه** — بقيد «الترتيب سند قراءة لا قياس» | [مشتق] |
| 1.3 · مظهر الانسداد | قراءتا **الانقطاع** و**عنق الزجاجة** المشتقتان من المسار (`129 §2/①`) — بنصوص الجدران المعتمدة حيث وُجدت، ولا عتبة | [معتمد/مشتق] |
| 1.4 · المشغّل والمعطّل | شرطا العمل الأمثل والتعطل — من التعريف وقيود `130` بالإحالة | [مشتق] |
| 1.5 · موقع الصمام في الشبكة | قيوده في `130` (فاعلاً/مفعولاً/طرفاً) + فجواته الموثقة — **إحالة لا إعادة تعريف** (`م-2`)، وشرط الاستدعاء فارغ | [إحالة] |
| 1.6 · الحدود وجدران العزل | الجدار الحاكم للبطاقة + النصوص المانعة للازدواج القائمة | [معتمد] |
| 1.7 · ديون الوعاء | من `128 §4` إن وُجدت — وإلا فتصريح بالخلو | [معتمد] |
| 2 · سلّم النطاقات | البنية الثمانية + الثلاثية بقراءة الصمام (③ أدناه) | [معتمد/مشتق] |
| 3 · الملحق القياسي | الوعاء · `MaxRaw=66` · `SS` · `SP` · الديون | [معتمد] |
| 4 · سجل الفجوات والإحالات | فجوات الشبكة · البذور بالإحالة · `GAP-K4-CASES-01` | — |

**ما استُبعد من بطاقتَي $K_2$/$K_3$ عمداً** (كلفة النقل — `TRF-009/③`): «مراحل التشغيل الداخلية» و«بصمة التنظيم» و«إشارات القوة/الضعف الملحوظة» (توسعة `DEC-066` الخاصة بقالب $K_3$) — **لا سند لها في دستور $K_4$، ولا تُؤلَّف**؛ إن أرادها المالك قامت بقرارها وموادّها في مرحلتها.

## ② الوثائق السبع المولَّدة (v1.0 · `WM` القالب المرجعي)

| الوثيقة | الصمام | المحطة | قيود الشبكة | ديون الوعاء |
| :--- | :--- | :--- | :--- | :--- |
| `50-DEPTH-K4-WM_Working_Memory_Valve` | الذاكرة العاملة النشطة | 1/7 | 2 (`REL-04/10`) | — |
| `50-DEPTH-K4-TI_Task_Initiation_Valve` | المبادرة والبدء الفعلي | 2/7 | 2 (`REL-05/10` — كلاهما مفعول) | — |
| `50-DEPTH-K4-F_Focus_Valve` | التركيز وحجب المشتتات | 3/7 | 3 (`REL-01/02/06`) | 14ب≈27أ |
| `50-DEPTH-K4-PF_Priorities_Valve` | الالتزام بالأولويات والمسار الحرج | 4/7 | 4 (`REL-01/06/09/11`) | 41ب≈45أ |
| `50-DEPTH-K4-OR_Organization_Valve` | التنظيم المادي للأشياء | 5/7 | 4 (`REL-02/03/04/07`) | — |
| `50-DEPTH-K4-TM_Time_Management_Valve` | تقدير الوقت وإدارة الزمن | 6/7 | 4 (`REL-07/08/11/12`) | — |
| `50-DEPTH-K4-PER_Perseverance_Valve` | المثابرة وإكمال المهام | 7/7 | 5 (`REL-03/05/08/09/12`) | `RSK-018(41)` · 9ب≈94ب |

عمود «قيود الشبكة» **عدٌّ وصفي لسجل `130`** بعد `DEC-260` — لا قياس ولا ترتيب أهمية. مجموع مواقع الأطراف 24 = 2×12 قيداً ✓.

## ③ سلّم نطاقات $K_4$ — يُعتمد قراراً (لا عتبة جديدة)

- **البنية العددية** (`TRF-010` — نُقل كما هو): الكتل الثماني `OUT · L−/L · M · M+ · H/H+ · H++` بحدودها المختومة سلفاً (`0/40/50/70/100`)؛ سندها أن أوعية $K_4$ السبعة كلها 11 بنداً (`14-CORE-K4 §4` ≡ `41 §5.4`) فقيد `y+z=11` و`MaxRaw=66` قائمان، ومعادلة `SP` **عامة لكل الدوائر** بنص `41 §3.3` («للوعاء المستهدف» — `DEC-065`)، وسابقتا التبني `DEC-061`/`DEC-064`.
- **الدلالة اللغوية** (`TRF-011` — نُقل مكيَّفاً): ثلاثية القدرة العامة — **حضور محدود (`L−…M`) · كفاءة أساسية (`M+`) · قدرة عالية (`H…H++`)** — لأن صمامات $K_4$ **لازمة معاً** على مسار تنفيذي واحد، فمصطلحا «مهيمن/مساند» ($K_2$) يضللان كما ضللا في $K_3$؛ والموصوف هنا **صمام تنفيذي على مسار تلتف حوله شبكة** لا قدرة تنظيمية في سلسلة.
- **قيدا القراءة:** النطاقات **وصفية** — لا تُقرأ عبر جدار العزل (`DEC-205` قياساً)، **ولا استدلال درجة↔علاقة** (سقف `129 §4`): لا يُقرأ من نطاق صمامٍ قيامُ قيد شبكةٍ ولا العكس.

## ④ ما لا تدّعيه طبقة العمق

- ❌ لا تقيس شيئاً ولا تضيف بنداً أو وزناً أو عتبة أو مستوى — وطبقة القياس لم تُمسّ (`instrument_pin`).
- ❌ لا تُثبت علاقة ولا تملأ فجوة — الشبكة تُقرأ من `130` إحالةً.
- ❌ لا تبني طبقة تقرير ولا لغة قارئ — ذلك لمراحله (طبقة المستخدم `55-USER-K4-*` لم تُفتح).
- ❌ لا صدق ميداني — كل اشتقاق `[اشتقاق غير محقّق ميدانياً]` (`TRF-002` · `DEC-246`).

---

# §3 بوابة النقد الذاتي — فحص ما أُعيد بناؤه فوق المتغيّر (`م-7`)

- البطاقات كُتبت **بعد** حسم `DEC-260` فقرأت سجل العلاقات بحالته النافذة (12 قيداً) — لا بطاقة ورثت عدّاً ساقطاً ✓.
- مثال صف تحييد الساقط (`130 §1`) لم يتسرب لأي بطاقة — المشهد يُستشهد به في موضعه المستقر (`K4-REL-09`) حصراً ✓.
- عنوان كل وثيقة يطابق مضمونها (بوابة `Check-10`) ✓.

---

# §4 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| طبيعة التغيير | وثائقي صرف — لا كود، لا حزم، لا أداة |
| قيود النقل | `TRF-009…011` مقيَّدة **قبل** الاستعمال بأجوبتها الثلاثة كتابةً (`77-LOG` v1.3) |
| الأدوات الستّ عشرة | 16/16 |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **حرفياً** |

---

**[نهاية `131-K4-DEPTH` — `DEC-260`/`DEC-261` · الترقيم التالي `DEC-262` · `CHG-077`/`CHG-078` · قيود العلاقات حتى `K4-REL-12` · التالي `K4-REL-13`]**
