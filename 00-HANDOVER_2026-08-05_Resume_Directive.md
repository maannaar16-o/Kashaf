# 00-HANDOVER_2026-08-05 — أمر الاستئناف

| الحقل | البيان |
| :--- | :--- |
| **الجلسة المنتهية** | `Mode 03-Build` — 2026-08-05 |
| **مزامنة خط الأساس** | **2026-08-13** — أُعيدت المزامنة **في موضع الوثيقة** (`CHG-088` · §8) · **والاسم لا يُغيَّر**: مسمّىً حامل تذكره `CLAUDE.md` وبيانات الجرد |
| **آخر قرار مختوم** | **`DEC-290`** · الترقيم التالي **`DEC-291`** |
| **آخر تغيير** | **`CHG-111`** |
| **المُنتَج** | `DEC-230…290` · `CHG-053…111` · **58 وثيقة** (`104`…`161`) |
| **أدوات التحقّق** | من **8** إلى **27** |
| **البصمات السبع** | **ستٌّ تكافؤية** وسابعةٌ **انحدارية** (طبقة الفريق صار لها توأمٌ مقيس — `DEC-254`) · والثلاث الأصلية لم تتزحزح مرّة واحدة · والرابعة (دائرة الإنجاز) زُوحزحت **ثلاثاً بإعلانٍ مسبَّب** لا صمتاً (`DEC-267`/`DEC-268`/`DEC-271`) · والخامسة (أحكام المشرف على $K_4$) مستحدثة بـ`DEC-271` |

---

# §0 — كيف تُفتَح الجلسة القادمة

انسخ هذا النصّ في أول رسالة، وأرفق معه **الأداتين** (§3.1):

```
Mode 03-Build (استئناف)
خط الأساس: بعد مزامنة 00-HANDOVER_2026-08-05 §1
آخر قرار: DEC-278 · التالي DEC-279

① تحقّق من بوابة الاستئناف (§1) وأبلغ بالنتيجة.
② شغّل `python3 gate.py` — يهيّئ ويشغّل الأدوات الأربع والعشرين ويطابق البصمات الستّ حرفياً.
③ انتظر أمري بالبند التالي — لا تبدأ باجتهادك.
```

---

# §1 بوابة الاستئناف — اثنتا عشرة علامة فاصلة

| # | الوثيقة | العلامة |
| :--- | :--- | :--- |
| 1 | `01-MASTER` | **v6.4** · آخر قرار **`DEC-278`** · الترقيم التالي **`DEC-279`** |
| 2 | `02-MASTER` | **v7.3** · `CHG-053…097` · وسجلّ الحرّاس يحوي **`ح-7`** |
| 3 | `00-INDEX` | **v7.0** · الوثائق **`104`…`148`** مُدرجة · **13 حزمة** |
| 4 | `00-PROTOCOL` | يحوي **`ن-8`** — الصياغة الصريحة عند حافة الإخراج |
| 5 | `56-REPORT-ENGINE` | **v1.4** · `DEBT-K2-BALANCE-01` **مُغلق بالقبول** |
| 6 | `k3_content.py` | `REQUIRED_EXTERNAL` = **9 مفاتيح** · فيها `circle_map_shared` و`circle_map_tail` (**لا** `circle_map`) |
| 7 | الكود | `interp_registry.json` موجود · و`sp_gate.py` يحوي `PCT_REGISTRY` |
| **8** | **دائرة الإنجاز** | `k4_engine.py` · `k4_report.py` · `k4_contentpack.json` · `parity_k4.py` — **أربعتها موجودة**، ووحدة `K4` في `engines.js` توأمُها |
| **9** | **الموقع والأداة** | `build_site.py` يكتمل · و`test_site_build.py` يجتاز — أي **`docs/` مزامنة** والتشييم مكتمل |
| **10** | **أداة المشرف** | `build_supervisor_html.py` يكتمل · و`test_supervisor_build.py` يجتاز — أي **الحزمة المولَّدة تُنفَّذ** وتحكم $K_2$ و$K_4$ |
| **11** | **البوابة مفروضة** | `python3 gate.py` يُخرج «قبول» — وفيه تُقابَل هذه الوثيقة نفسها بالبوابة: **انجرافُها يوقفها** (`DEC-273`) |
| **12** | **الديون مزامَنة** | قائمة `146 §5` = `open_debts` في محرّك $K_4$ — **آخر مزامنةٍ يدوية صارت مقيسة** (`DEC-276`) |

**سقوط أي علامة ⇒ توقّف وأبلغ. لا بناء على خط أساس غير مُزامَن.**

> **العلامتان 8 و9 مُستحدَثتان بمسوّغهما:** الأولى لأن دائرة الإنجاز صارت طبقةً عاملة لا ميثاقاً (`DEC-266`)، والثانية لأن انكسار بناء الموقع **مرّ صامتاً في التزامَين** حين كانت البوابة لا تشمله (`DEC-269` · §6⑦).

---

# §2 حالة خط الأساس

## البصمات الستّ — تُطابَق حرفياً

| البصمة | الأداة | النطاق |
| :--- | :--- | :--- |
| **`2711c24d8155819b`** | `parity_py` | 125 حالة منطق |
| **`36ae94bfd5a8b60f`** | `parity_reports` | 142 تقريراً |
| **`6b324f996856eac3`** | `parity_supervisor` | 429 درجة حكم |
| **`94434230e7dbc0f0`** | `parity_k4` | 387 حالة (منطق + تقرير **وكتلة تدقيقه** + سطح عابر) |
| **`72790080dc0df8d2`** | `parity_supervisor_k4` | 80 حمولة · 982 درجة حكم |
| **`29ed15b4accf7a5a`** | `test_report_team` | **بصمة انحدار** لا تكافؤ — طبقة الفريق بايثون وحدها (سابقة `DEC-254`) |

> **قاعدة الزحزحة المُعلنة:** بصمةٌ لا تُزاح إلا بإعلانٍ مسبَّب في قرار — كما زُيحت بصمة $K_4$ عند مزامنة `open_debts` (`DEC-267`) ثم عند بناء السطحين (`DEC-268`). **والزحزحة الصامتة مرفوضة.**

## نطاق القياس — ثمانية أسطح

منطق المحرّك · نصّ التقرير · أحكام المشرف · **رسائل الاستثناءات** · **تدقيق العزل وكتلة التدقيق** · **الثوابت وبنود الأداة والخام→SP والشبكة الحدّية** · **دائرة الإنجاز: منطقها وتقريرها وسطحها العابر** · **بناء الموقع والأداة ومزامنة `docs/`**.

## الحرّاس النافذون

`ح-1`·`ح-2`·`ح-3` (الحزم) · **`ح-4`** (`SP%`) · **`ح-5`** (نسبة غير مسجَّلة) · **`ح-6`** (استيفاء غير مصاغ) · **`ح-7`** (صياغة القفل) · `ص-1…ص-3` ($K_3$) · `ت-6` (بلا عتبة).

---

# §3 إجراء التهيئة — **ثلاث خطوات بالترتيب**

## 3.1 الأداتان — **تُرفعان في المحادثة لا في قاعدة المعرفة**

| الأداة | الحجم | `SHA-256` |
| :--- | ---: | :--- |
| `Kashaf_v2_FIXED_DEC-230-245.html` | 1,116,516 | `2b89033c5a8c082c…` |
| `Supervisor.html` | 366,030 | `031c2dc71b8e1723…` |

> `Supervisor.html` **مولَّدة** — تُبنى بـ`build_supervisor_html.py` إن تعذّر رفعها.

> **مُستجدٌّ يُعلَن:** صار للاستبيان **تجسيدٌ مولَّد** في `docs/kashaf.html` (يُبنى بـ`build_site.py`، بثلاثة تبويبات تقرير — `DEC-250`…`DEC-270`). **ولا يُغني عن الأداة المختومة أعلاه ولا ينسخها**: طبقة القياس فيه **مقروءة لا معرَّفة** — خرائط الأوعية تُفكّ من الجداول المختومة وتُقابَل آلياً (`instrument_pin`).

## 3.2 توليد طبقة الحزم — **خطوة أولى إلزامية**

```bash
python3 build_packs.py        # يولّد packs.js — لا يُرفع (CHG-054)
```

**بدونها تفشل** `parity_reports` و`test_packs` بـ`Cannot find module './packs.js'`.

## 3.3 إعادة التسمية القانونية

الملفات مُسلَّمة **بأسمائها القانونية** — لا يلزم إلا:

```bash
cp k3_contentpack_FIXED_DEC-195.py  k3_contentpack.py
```

> ⚠️ **تغيّر جوهري عن الجلسة السابقة:** `k2_engine` · `k3_engine` · `k2_report` · `k3_report` · `k3_content` صارت بأسمائها القانونية. **لا تُعِد تسميتها.**

## 3.4 تشغيل الأدوات الأربع والعشرين

**بأمرٍ واحد** — و`gate.py` هو **السلطة الواحدة** على القائمة والبصمات (`DEC-273`)؛ وهذه القائمة أدناه **مقابَلةٌ به آلياً**، فانجرافُها عنه **يوقف البوابة**:

```bash
python3 gate.py                 # التهيئة + 27 أداة + 6 بصمات + 6 فحوصٍ على البوابة
```

والأدوات منفردةً — لفحص سطحٍ واحد:

```bash
python3 parity_py.py            # 2711c24d8155819b — 125 حالة منطق
python3 parity_reports.py       # 36ae94bfd5a8b60f — 142 تقريراً
python3 parity_supervisor.py    # 6b324f996856eac3 — 429 درجة حكم
python3 parity_k4.py            # 94434230e7dbc0f0 — 387 حالة K4
python3 parity_team.py          # 618bc83fd757bd60 — طبقة الفريق توأمين
python3 parity_supervisor_k4.py # 72790080dc0df8d2 — 982 درجة حكم على K4
python3 parity_messages.py      # 15 مساراً
python3 parity_isolation.py     # 826 نصّاً
python3 parity_surface.py       # 3214 حقلاً
python3 verify_regen.py         # 86 حالة
python3 test_packs.py
python3 test_golden_k2.py       ;  python3 test_golden_k3.py
python3 test_report_k2.py       ;  python3 test_report_k3.py
python3 test_guard_sp.py        ;  python3 test_guard_lock.py
python3 guard_interp.py
python3 test_report_k4.py       ;  python3 k4_content.py
python3 test_report_team.py     # 29ed15b4accf7a5a — عقد تركيب الفريق (بصمة انحدار)
python3 test_workshop.py        # عقد مسار الورشة — المشرف بوابةُ قبول
python3 test_owner_console.py   # عقد لوحة المالك — رفعٌ مشروط · عزلٌ · حدودٌ مُعلَنة
python3 test_site_build.py      # حرس بناء الموقع + مزامنة docs/
python3 test_supervisor_build.py # حرس بناء أداة المشرف + تشغيلها
python3 test_team_build.py      # حرس بناء سطح الفريق + تشغيل حزمته
python3 supervisor.py --self-test
```

**المتوقَّع: 24/24 · والبصمات الستّ حرفياً.** أي انحراف ⇒ **توقّف وأبلغ**. و`CI` يشغّل هذا نفسه على كل دفعة وطلب دمج.

---

# §4 قواعد نافذة — تسري من أول رسالة

| # | القاعدة | السند |
| :--- | :--- | :--- |
| 1 | **طبقة القياس غير قابلة للمساس** | `instrument_pin` |
| 2 | **لا قراءة لفارق زمني** — التسجيل مباح والقراءة محظورة | `DEC-244` |
| 3 | **لا ادّعاء صدق** ولا استدلال عليه بالاتّساق | `DEC-246` |
| 4 | **لا عتبة مخترعة** | `ن-7/④` |
| 5 | **صياغة صريحة عند حافة الإخراج** | `ن-8` |
| 6 | كل تعديل نصّي **في النسختين معاً** ثم يُقاس التكافؤ | `DEC-199`/`200` |
| 7 | **الرقم يُملَك بالدفع لا بالقراءة** — وعند التصادم **يتنازل المتأخّر** | `DEC-274` |

> **قاعدة الترقيم عند تعدّد الفاعلين (`DEC-274`):** قراءةُ «الترقيم التالي» من `01-MASTER` **ترشيحٌ لا حجز** — **والدفعُ يُنشئ الحقّ**. فإن سبقك أحدٌ إلى الرقم: **لا تدفع قسراً**، أعد ترقيم قرارك وتغييرك وكود وثيقتك إلى التالي الحرّ، **وصُن الترتيب الزمني** في `01-MASTER` (صفّ السابق قبل صفّك)، وقيّد التصادم في `02-MASTER`. و`gate.py` يرصد الرقم المكرَّر والكود المتصادم — **فلا يمرّ صامتاً**.

---

# §5 البنود المفتوحة — ستة، **مفروزة بما تنتظره** (`DEC-276`)

**ولا واحد منها ينتظر عملاً من المنفّذ.** والفرز أهمّ من العدّ: بندٌ ينتظر بياناتٍ ليس كبندٍ ينتظر حكماً، وكلاهما ليس كحدٍّ في أداةٍ مقفلة لا يُحلّ أصلاً.

| الصنف | البند | ما ينتظره | تطويقه النافذ |
| :--- | :--- | :--- | :--- |
| **أ · مُجهَّز** | 🟡 `GAP-ITEM-SIDE-01` | **بيانات فقط** — والقياس مبنيّ: `contrib_analyze.axis1_side` | يقف بلا مضاعِف |
| **أ · مُجهَّز** | 🟡 `GAP-VALID-01` / ② | **بيانات فقط** — والبروتوكول مختوم في `120` | الحظر المؤقّت (`118 §7`) نافذ |
| **ب · حكم بشري** | 🟡 `GAP-VALID-01` / ① | **حكم خبير** على `119` — لا يُنتجه كود | كما أعلاه |
| **ب · حكم بشري** | 🟡 `GAP-X-EXH-01` | **قرار محتوى** في `13-CORE-K3` | يُعرض ولا يُصنَّف قسراً · ومُعلَنٌ للقارئ في `K4-XR-04` |
| **ج · حدُّ أداة** | 🟠 `GAP-K4-FR-CORE` | **قرارك على أداةٍ مقفلة** — و`instrument_pin` يمنع الحلّ | عقد تحفّظ القراءة (`134`) — **وهو الحلّ المتاح لا بديلٌ مؤقّت** |
| **ج · حدُّ أداة** | 🟠 `GAP-K4-FR-04` | مادّةٌ لقطبٍ موقوف | **لا يُدَّعى ولا يُملأ** (`م-8`) |
| **د · دَين دائم** | 🟡 `DEBT-K4-FIELD-01` | **ميدانٌ كامل** — دَينٌ يُحمل لا فجوةٌ تُسدّ | **مقيس** بدرجة المشرف ⑩ · وحاضرٌ في متن كل تقرير |
| **هـ · نصّ مختوم** | ⚪ وسم الاشتقاق للقارئ | **جملةٌ واحدة تختمها** — والمواصفة جاهزة في `146 §4` | الوسم قائمٌ على وثائق القاعدة · والأثر معلَن: إزاحة بصمة $K_4$ |

**وحدٌّ مُعلَن — وقد ضاق:** مزامنة `accepted_debts` مع `02-MASTER` **ما تزال يدوية** (`DEC-245`). أمّا `open_debts` في محرّكي $K_4$ فصارت **مقيسة بالبوابة** (`DEC-276`).

> **`GAP-ENG-01`** (وضع القائد) كانت مدرجةً «الفجوة الحاكمة الوحيدة المتبقية» — **وهي مُغلقة نهائياً** بـ`DEC-051`/`DEC-052`. أُسقطت من هذه القائمة لأن إدراج مغلقٍ فيها **يجعلها حقلاً لا يفحص** (§6①).

---

# §6 محاذير مستخلصة من هذه الجلسة — **تُقرأ قبل البناء**

## ① الحقل الذي لا يفحص يكذب

رُفض النمط **أربع مرّات**: `auditIsolation([])` (`DEC-236`) · `missing_content` مثبَّت (`DEC-238`) · `template_gaps` مثبَّتة (`DEC-240`) · مفتاح القفل يُفحص وجوده لا صياغته (`DEC-241`).

> **الحقل يُضاف فاحصاً أو لا يُضاف.** واستثناؤه الوحيد: حقلٌ **يُعلن** ولا يدّعي قياساً (`accepted_debts`) — ويُصرَّح بذلك.

## ② صيغة صريحة بدل سلوك لغة

ثلاث حالات وعلاجٌ واحد: الفاصل `·` · `_num` · `_round2` (`DEC-235`/`238`). و`ن-8` يمنع الصنف كلّه.

## ③ التطابق على مُدخل ضيّق ليس تكافؤاً

`isolation` أعطى `clean` في الطرفين لأن أحدهما **لا يفحص** · و`y+z` تطابق على الصحيح لأن العيّنات كانت صحيحة. **توسيع العيّنة كشف ما لم يكشفه التطابق.**

## ④ فتّش سجلّ القرارات قبل تقدير الخطورة

كِدتُ أُعلن **`DEF-K3-OCTAL-01` حرجاً** في طبقة القياس — و`DEC-184` يحكمه. فتّشتُ وثيقة القياس والمحرّكين ولم أفتّش السجلّ.

> **لا تُقدَّر خطورة انحرافٍ عن وثيقة قبل البحث عن ناسخٍ لها** (`§11`).

## ⑤ الاتّساق ليس الصدق — ولا هو السند

ستة عشر مقياساً **كلّها داخلية**. لم تكن لتكشف ④ ولا لتمنعه.

## ⑥ حين يُبلّغ حارسٌ عن ضجيج — يُصلَح الحارس لا يُوسَّع سجلّه

السجلّ الذي يبتلع الضجيج يصير هو نفسه **حقلاً لا يفحص**.

---

# §6-ب محاذير مستجدّة من بناء دائرة الإنجاز والموقع (`DEC-250`…`DEC-270`)

## ⑦ ما لا تشمله البوابة ينكسر صامتاً

`build_site.py` كان **خارج** الأدوات التسع عشرة، فانكسر بناء الموقع عند بناء $K_4$ (استيرادات غير مُشيَّمة) **ومرّ الانكسار في التزامَين** — ولم يُكشف إلا بسؤال المالك «هل يعمل على الموقع؟». والملفات المنشورة بقيت نسخةً لا تعرف $K_4$.

> **البوابة تقيس ما فيها فقط.** كل مُنتَجٍ يراه إنسان يلزمه حرسٌ **في البوابة** لا وثيقةٌ تُذكّر بتشغيله (`DEC-269`).

## ⑧ البندُ المؤجَّل حين يُنفَّذ — **يُقلب حرسُه لا يُحذف**

حرس `DEC-269` كان يتحقّق أن $K_4$ **غير مُسطَّح** في الأداة (لأن البند كان مؤجَّلاً). فلمّا نُفِّذ **قُلب** ليحرس الوصل نفسه — ولم يُحذف. فلا تبقى فترةٌ بلا حرس (`140 §5`).

## ⑨ الفراغ الموثَّق ليس فجوةً مفتوحة — والملء بالتأليف «إغلاقٌ تجميلي مقلوب»

عند التسوية الختامية كان الطريق السهل: **اختراع** فجواتٍ ستّ ثم ملؤها تأليفاً فيبدو السجلّ عاملاً. وفُرِزت بدلاً من ذلك: ستة فراغات **موثَّقة مغلقة بقاعدتها** (قاعدة الصمت `م-8`) في مقابل أربعٍ مفتوحة مطوَّقة. **العدد ليس مقياس الجدّية؛ الفرز هو** (`137`).

## ⑩ التوأمان يتباعدان عند **حدود المدخل** لا عند وسطه

بايثون قبِل `float("nan")` بلا استثناء، فسقطت المقارنات كلّها وصُنِّف الوعاء **«قدرة عالية» صامتاً** — تصنيفٌ كاذب؛ وJS يرفضه بـ`isFinite`. تطابقُ 387 حالةً صحيحةً لم يكن ليكشفه.

> **عقد المدخل يُفحص عند غير المنتهي والفارغ والمتروك — لا عند الصحيح.** والعيب يُقاس بحالاتٍ تُضاف، لا يُصلَح بصمت (`DEC-266`).

---

# §7 قائمة الرفع

مُفصَّلة في **`00-MANIFEST_2026-08-05` v2.7** — يُستبدَل منها ويُضاف · و`00-MANIFEST_2026-08-09` جردٌ مشتقّ (**ماركاه لخط أساس `DEC-248` — يُقرأ بحذر حتى يُعاد جرده**).

**وخمسة ملفات لا تُرفع** (`CHG-054`): `packs.js` (مولَّد) · `k3_contentpack.py` (مولَّد بإعادة تسمية) · **`Supervisor.html`** (مولَّدة — أُسقطت بـ`DEC-271`) · `Kashaf.html` · `golden_k3_PRE-DEC-215.json`.

> **واستثناءٌ ضيّق مختوم:** `docs/` **تُرفع** — مُنتَجٌ منشور لا وثيقةَ قاعدةِ معرفة (`DEC-250`). وحرسُها `test_site_build.py` يمنع تقادمها صامتةً.

---

---

# §8 سجلّ المزامنة — 2026-08-13 (`CHG-088`)

## أ. ما زُومن ولماذا

هذه الوثيقة هي **بوابة الاستئناف** التي تنصّ `CLAUDE.md` على قراءتها قبل أي عمل بناء — وكانت ساقطةً عند **علاماتها السبع كلّها**: خط أساسها `DEC-248` والواقع `DEC-270`؛ وأدواتها 16 والواقع 20؛ وبصماتها ثلاث والواقع أربع. فالجلسة التي تُفتَح عليها إمّا **تتوقّف توقفاً كاذباً** عند كل علامة، أو — وهو الأسوأ — **تبني على خط أساس غير مُزامَن**، وهو عين ما تحرّمه الوثيقة بنفسها.

| ما زُومن | من | إلى |
| :--- | :--- | :--- |
| آخر قرار · الترقيم التالي | `DEC-248` · `249` | **`DEC-270`** · **`271`** |
| آخر تغيير | `CHG-066` | **`CHG-087`** |
| وثائق التحليل | `104`…`120` (17) | **`104`…`140`** (37) |
| أدوات التحقّق | 16 | **20** |
| البصمات | ثلاث | **أربع** |
| علامات البوابة | سبع | **تسع** (+ دائرة الإنجاز · + الموقع) |
| أسطح القياس | ستة | **ثمانية** |
| البنود المفتوحة | ثلاثة | **سبعة** (وأُسقطت `GAP-ENG-01` المغلقة) |
| محاذير ما قبل البناء | ①…⑥ | **+ ⑦…⑩** |

**وثلاثة قيود التزمتها هذه المزامنة:**

① **الاسم لم يُغيَّر** — `00-HANDOVER_2026-08-05_Resume_Directive.md` مسمّىً حامل تذكره `CLAUDE.md` وبيانات الجرد؛ فالتاريخ في الاسم **تاريخ تأليفٍ لا تاريخ صلاحية**، والمزامنة تُسجَّل في متنه لا في اسمه.
② **لا قرار جديد** — المزامنة **لا تقرّر شيئاً**: تُعيد المطابقة بين وثيقةٍ وواقعٍ **مختومٍ سابقاً**. وإصدار `DEC` لها يُنشئ سلطةً ثانية على ما هو محكوم (`م-2`)، فسُجِّلت **تغييراً** (`CHG-088`) وحسب.
③ **لا حذف لدرسٍ قائم** — محاذير `①…⑥` باقية بنصّها؛ المستجدّ **أُضيف** ولم يُستبدل به.

## ب. `GAP-ITEM-K3-01` — **حُسمت بأمر المالك** (`DEC-275`)

رُفعت هنا مسألةً: مسوّغ الفجوة («لا خريطة بنود ⇒ المراجعة متعذّرة») لم يبقَ دقيقاً بعد أن صارت خريطة $K_3$ مُفكّكةً من `41 §5.3` ومقابَلةً آلياً، وبعد أن أُدخلت خريطة $K_4$ محرّكَها مقروءةً لا معرَّفة.

**والحكم صدر:** أُظهرت الخريطة في `k3_engine` وتوأمه — **مقروءةً لا معرَّفة**، مقابَلةً بالجدول المختوم آلياً، **ومقيسة** بمسح `ب-٢` (55 بنداً). وأُسقطت النسخة المنقولة من طبقة البناء. و`score_from_raw` **لم يُضَف بمسوّغه**: الحساب في `bridge.spK3` وتكراره سلطةٌ ثانية على معادلةٍ واحدة.

⇒ **الدوائر الثلاث مستوية**: لكلٍّ خريطتُها في محرّكها. والبصمات الخمس لم تتحرك — فلا سلوك تغيّر. التفصيل في `145-K3-ITEMMAP`.

---

**[نهاية `00-HANDOVER_2026-08-05` — **مزامَنة 2026-08-15** · خط الأساس `DEC-290` · الترقيم التالي `DEC-291` · `CHG-111` · **27 أداة** · **سبع بصمات** · اثنتا عشرة علامة · **والمزامنة مقيسةٌ بتمامها** (`DEC-287`): `gate.py` يقابل قائمة الأدوات والبصمات **وخطَّ الأساس نفسه** — فلا رقمَ يتخلّف صامتاً]**
