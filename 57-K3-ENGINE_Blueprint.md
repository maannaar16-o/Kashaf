# 57-K3-ENGINE_Blueprint.md — التصور الشامل لمحرك تقارير دائرة الانفعال

---
 

 # القسم 0: رأس الحوكمة
>
> | الحقل | البيان |
> | :--- | :--- |
> | **كود الوثيقة** | `57-K3-ENGINE_Blueprint` — التصور الشامل (**سابق للمواصفة**) |
> | **الإصدار** | v0.1 |
> | **🗄️ الحالة** | **مؤرشفة — مستوعَبة بالكامل في `57-K3-ENGINE-SPEC` v2.0** (`DEC-130`) |
> | **القيد الحاكم** | 🔴 **لا تُحدَّث · ولا يُستشهد بها تشغيلياً · ولا تُسحب منها صياغة.** المرجع التشغيلي الوحيد: **`SPEC` v2.0**؛ ومرجع قواعد السحب: **`M3-06` v1.0** |
> | **ما نُقل منها** | §8 مصفوفة الالتزام → **`SPEC` v2.0 §14** (محدَّثة) · §7 واجهة دمج `56-REPORT-ENGINE` → **`SPEC` v2.0 §14.1** و`02-MASTER` |
> | **قيمتها الباقية** | **سجل نشأة**: تُظهر كيف صُمّم المحرك بمقابس فارغة محجوزة قبل أن يوجد ما يملؤها |
>
> **⚠️ تنبيه للقارئ — مواضع سقط مرجعها في هذه الوثيقة:**
> · §1.2 «ما لا يفعله»: **إسناد الجذر** و**حكم الاحتواء** و**البصمة** لم تعد محظورة — انظر `DEC-115`/`DEC-117`/`DEC-121`.
> · §2 «`G3`/`G4` غائبتان بنيوياً» — أُضيفتا في `SPEC` v2.0 §6 **دون هدم**، كما تنبّأت الوثيقة نفسها.
> · §3 بنية التقرير (8 أقسام) — صارت **9** وحُذف ختم الشفافية من الواجهة (`DEC-092`).
> · §7 «قسم `URS` محجوز حتى تكتمل المعايرة» — **أُتيح بلا معايرة**: الحاجب كان بنيوياً لا قياسياً، وسقط بـ`DEC-113`/`DEC-114`.
> · §9 نقاط القرار الست — **محسومة** (`DEC-091/092/093`)؛ و`ق5` حُسم **بخلاف توصية الوثيقة**: المعتمد بروتوكول `41 §7` لا استبعاد المهارة الناقصة.

---

# القسم 1: المهمة والحدود

## 1.1 ما الذي يفعله المحرك

> يحوّل **درجات المهارات الخمس الخام** إلى **تقرير سردي شخصي** بلغة المستخدم («أنت»)، يقرأ كل مهارة في **نطاقها المطلق**، محميّاً بخمس بوابات سلامة، ومُثرى بإطار السلسلة **كأداة تأمل لا كحكم**.

## 1.2 ما الذي **لا** يفعله (حدود `DEC-089` — تُعلَن في التقرير نفسه)

| محظور | السبب |
| :--- | :--- |
| مقارنة مهارتين («أقوى في الكبح من التهدئة») | لا تقنين |
| إسناد جذر («إنهاكك سببه جمود التكيّف») | `G3` مجمَّدة |
| حكم احتواء آلي («نظامك يحتوي بكلفة») | `G4` مجمَّدة |
| بصمة جامعة (`URS`) | محجوبة |
| تشخيص أقطاب («أنت كابت/متجمّد») | `M3-07` — الأداة عمياء عنها |
| أي لغة عيادية أو سمة ثابتة | الدستور · `P=C+G` |

---

# القسم 2: المعمار السباعي (`DEC-062`) — **[مقترح]**

```
      [درجات خام: 55 بنداً → أوعية المهارات الخمس]
                          │
  ┌───────────────────────▼───────────────────────┐
  │ L1: الاستقبال والحساب                          │
  │ SS = x−2z+y · SP = SS÷66×100 (41-MEASURE §3.3)│
  │ + تصنيف النطاق المطلق (DEC-064)                │
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L2: بوابة الثقة (G1) — EP ≤50%؟                │
  │ → وسم الملف كله «قراءة بتحفّظ» (لا إيقاف)      │
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L3: قراءة مهارة-بمهارة                         │
  │ سحب مداخل 55-USER وفق شروط P7 (المطلقة فقط)   │
  │ ترتيب العرض: ترتيب السلسلة EP→IR→BI→CF→ST     │
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L4: بوابة الزيف (G2) — أي درجة >70%؟           │
  │ → كتلة التحقق الموحّدة (S5) مرة واحدة          │
  │ + أسئلة «هل هذا أنا؟» (S4) دائماً              │
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L5: الإطار البنيوي التأملي                     │
  │ سرد السلسلة + التعويض اللامتماثل «كمبدأ عام»  │
  │ (معروض للتأمل — غير مُسنَد لدرجات المستخدم)     │
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L6: بوابة العزل اللغوي (G5)                    │
  │ فحص كل النص: قواميس التطهير الخمسة + المحظورات│
  └───────────────────────┬───────────────────────┘
  ┌───────────────────────▼───────────────────────┐
  │ L7: التجميع والإخراج                           │
  │ بنية التقرير (§3) + ختم الشفافية + وسم الإصدار│
  └───────────────────────────────────────────────┘
```

**قرارات معمارية مضمّنة [مقترحة]:**
- **`L2` لا توقف التقرير** عند `EP` منخفضة — تُنتجه موسوماً («قراءة بتحفّظ») مع تفسير للمستخدم. الإيقاف الكامل يحرم المستخدم من قيمة حقيقية متبقية.
- **`L5` بعد `L4` لا قبلها** — الإطار التأملي يُبنى على وعي المستخدم بحدود القياس، لا قبله.
- **`G3`/`G4` غائبتان بنيوياً** من الخط — لا «معطَّلتان»؛ عند رفع التجميد تُضافان كطبقة `L4.5` دون هدم.

---

# القسم 3: تشريح التقرير كما يقرؤه المستخدم — **[مقترح]**

| # | القسم | المصدر | ملاحظات |
| :--- | :--- | :--- | :--- |
| ١ | **فاتحة العهد** — ما هذا التقرير وما ليس هو: قدرات قابلة للتغيّر لا سمات؛ لا تشخيص؛ «الأداة تقيس ما تستطيع وأنت تعرف ما لا تستطيع» | `REFLECT` + `P=C+G` | ثابتة في كل تقرير |
| ٢ | **خريطة السلسلة** — «تلاحظ ← تهدّئ ← تكبح ← تكيّف ← تحتمل» بلغة بسيطة | `55-USER §0-ب` | النموذج الذهني قبل أي رقم |
| ٣ | **(شرطي) لافتة الثقة** — إن كانت `EP ≤50%`: «دقة ملاحظتك — أساس كل ما يلي — تحتاج دعماً، فاقرأ البقية بتحفّظ» | `G1` | تظهر قبل المهارات |
| ٤ | **خمسة أقسام مهارية** بترتيب السلسلة، لكلٍّ: ما هي (`U-01`) · نطاقك (`§2` بلغة المستخدم) · إشاراتك (قوة أو انتباه حسب النطاق) · قفل الخلط الأهم | `L3` + `P7` | **قلب التقرير** |
| ٥ | **(شرطي) كتلة التحقق الموحّدة** — إن وُجدت درجة >70% | `S5`/`REFLECT §1.3` | مرة واحدة، مصدَّرة بالتأكيد |
| ٦ | **«هل هذا أنا؟»** — أسئلة الفصل الثمانية | `S4`/`REFLECT §2` | دائماً — أداة تفسير عامة |
| ٧ | **الإطار التأملي** — مبدأ السلسلة والتعويض اللامتماثل **كمبادئ عامة**: «في أي نظام متسلسل، قوة حلقة لا تُصلح ضعف سابقتها» — **بلا ربط بدرجاتك** | `L5`/`M3-04` | الحد الفاصل: تعليم لا إسناد |
| ٨ | **ختم الشفافية** — ما قِيس وما لم يُقَس (الأقطاب، المقارنات، البصمة الجامعة: «ستتاح حين تكتمل معايرة الأداة») | `DEC-089` | صدق معلَن |

---

# القسم 4: عقد المدخلات/المخرجات — **[مقترح تقني]**

## 4.1 المدخل
```json
{
  "responses": { "item_2b": 0-6, "...": "…55 بنداً…" },
  "meta": { "date": "…", "instrument_version": "40-MEASURE vX" }
}
```
المحرك يحسب بنفسه (لا يستقبل درجات جاهزة): الأوعية → `SS` → `SP` → النطاق. **مصدر الحساب الوحيد: `41-MEASURE §3.3` + مصفوفة الأوعية المعتمدة.**

## 4.2 المخرج
```json
{
  "report_md": "…التقرير السردي…",
  "audit": {
    "sp": { "EP": …, "IR": …, "BI": …, "CF": …, "ST": … },
    "bands": { "…": "limited|core|high|out" },
    "gates_fired": ["G1:low_trust?", "G2:skills>70", "G5:violations=0"],
    "entries_used": ["K3-EP-U-01", "…"],
    "engine_version": "…", "frozen_layers": ["G3","G4","URS"]
  }
}
```
حقل `audit` **داخلي** (للحوكمة والتتبع) — لا يظهر للمستخدم.

---

# القسم 5: معالجة الحالات الخاصة — **[مقترح]**

| الحالة | المعالجة |
| :--- | :--- |
| **`OUT`** (`SP<0`) | «نطاق فجوة تشغيلية» (`DEC-064`): صياغة رعاية خاصة — «إجاباتك تشير إلى أن هذا الجانب يستهلكك أكثر مما يخدمك حالياً» + مدخل «حالة الضغط القصوى» + **لا تهويل ولا لغة أزمة** |
| **كل الدرجات >70%** | `CF-06`: كتلة التحقق تظهر **بكامل أسئلتها الأربعة** + جملة خاصة: «ملف بهذا الارتفاع الشامل يستحق أسئلة التحقق أكثر من غيره» |
| **كل الدرجات ≤50%** | لا كتلة تحقق (لا داعي) · تُضاف جملة سياق حمل: «قبل قراءة هذا كضعف شامل: هل تمر بفترة ضغط استثنائي؟» (من مداخل الضغط القصوى — **بلا افتراض**) |
| **بيانات ناقصة** (بند بلا إجابة) | **قرار مفتوح — §9/ق5** |
| **`EP` عالية + البقية منخفضة** | لا معالجة خاصة (سيناريو مشروع: يرى بدقة ويعجز عن الإدارة) — تكفي قراءة النطاقات |

---

# القسم 6: بنك الصياغة (Phrase Bank) — **[مقترح]**

مصدر كل جملة في التقرير **حصراً**: مداخل `55-USER-K3-*` المعتمدة + `REFLECT`. المحرك **يجمّع ولا يؤلّف**:

| المكوّن | المصدر الحصري |
| :--- | :--- |
| تعريف المهارة | `U-01` لكل مهارة |
| وصف النطاق | `§2` (الجداول الثلاثية) |
| الإشارات | `U-08…U-11` حسب الشرط |
| الأقفال | مداخل الأقفال الدائمة |
| كتلة التحقق | `REFLECT §1.3` **حرفياً** |
| أسئلة الفصل | `REFLECT §2` **حرفياً** |
| الربط بين الأقسام | **جمل وصل محايدة فقط** — قائمة مغلقة تُعتمد مسبقاً |

**لماذا هذا حاسم:** يجعل `G5` (العزل اللغوي) **فحصاً بنيوياً شبه مضمون** بدل رقابة لاحقة على نص حر — المحرك لا يستطيع إنتاج لغة محظورة لأنه لا يؤلّف أصلاً.

---

# القسم 7: واجهات المستقبل (لا تُبنى الآن — تُحجَز مقابسها)

| الواجهة | الحجز |
| :--- | :--- |
| **دمج `56-REPORT-ENGINE`** (`DEC-062`) | عقد المخرج (§4.2) مصمَّم ليكون وحدة قابلة للتضمين؛ `audit.frozen_layers` يُعلم المحرك الأم بما هو معطَّل |
| **رفع التجميد** (`N1–N4`) | `L4.5` فارغة محجوزة لـ`G3`/`G4` |
| **`URS`** | قسم ٩ فارغ محجوز في بنية التقرير («صورتك الجامعة — تتاح حين تكتمل المعايرة») |
| **الخيار A** (بنود الأقطاب) | `L1` يقبل `instrument_version` أحدث دون كسر |

---

# القسم 8: مصفوفة الالتزام (كل قرار حاكم → أين يتجسد)

| القرار | تجسيده في المحرك |
| :--- | :--- |
| `DEC-064` | `L1` — النطاقات الثلاثة + `OUT` هي **لغة التقرير الوحيدة** للدرجات |
| `DEC-076` | صفر ادّعاء اتجاهي داخل مهارة — بنك الصياغة لا يحتويه أصلاً |
| `DEC-082` | لا قراءة دائرية آلية (كانت تحتاج `G4`) — الإطار التأملي فقط |
| `DEC-089` | غياب `G3`/`G4` بنيوياً + ختم الشفافية |
| `DEC-090` | `L4` — كتلة واحدة + أسئلة الفصل |
| `DEC-072` | قفل «السكون بعد الحظر» ضمن مداخل `BI` المسحوبة |
| قفل التزاوج | المحرك يقرأ **نسخاً موسومة الإصدار** من وثائق المستخدم؛ تعديل توأم عمق يجمّد السحب حتى المزامنة |

---

# القسم 9: نقاط القرار المفتوحة — **تحتاج حسمك قبل التنفيذ**

| # | السؤال | الخيارات | توصية المراجع |
| :--- | :--- | :--- | :--- |
| **ق1** | **هل يرى المستخدم رقم `SP`؟** | (أ) النطاق فقط («كفاءة أساسية») · (ب) النطاق + الرقم · (ج) الرقم بارزاً | **(أ)** — الرقم بلا تقنين يوحي بدقة زائفة ويغري بمقارنات حظرناها. النطاق لغة `DEC-064` الشرعية |
| **ق2** | **أوضاع التقرير** | (أ) وضع واحد كامل · (ب) كامل + موجز | **(أ) أولاً** — الموجز لاحقاً بعد استقرار الكامل (الاختزال مخاطرة لغوية) |
| **ق3** | **موضع أسئلة «هل هذا أنا؟»** | (أ) قسم مستقل (كما §3/٦) · (ب) موزّعة كلٌّ داخل قسم مهارتها | **(أ)** — التوزيع يكرر ويطيل؛ التجميع يجعلها أداة مرجعية |
| **ق4** | **لغة العرض عند `OUT`** | تُعتمد الصياغة المقترحة (§5) أم تريد مراجعتها نصياً قبل الاعتماد؟ | مراجعتك النصية **مستحسنة** — أحسّ المناطق |
| **ق5** | **البيانات الناقصة** | (أ) رفض إنتاج التقرير · (ب) إنتاج مع استبعاد المهارة الناقصة ووسمها · (ج) تقدير إحصائي للناقص | **(ب)** — (أ) قاسٍ و(ج) **اختراع بيانات** (محظور) |
| **ق6** | **هوية التنفيذ** | (أ) مواصفة تشغيلية (Markdown محكم يُنفّذه أي مطوّر/نموذج) · (ب) كود فعلي (Python) الآن | **(أ) ثم (ب)** — المواصفة أولاً تُعتمد ثم تُترجم كوداً؛ القفز للكود يخلط القرار بالتنفيذ |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| المعمار السباعي (`DEC-062`) | ✅ مرسوم — 7 طبقات، `G3`/`G4` غائبتان بنيوياً |
| بنية التقرير (8 أقسام) | ✅ مقترحة |
| عقد المدخلات/المخرجات | ✅ مقترح |
| بنك الصياغة (يجمّع لا يؤلّف) | ✅ **الضمانة البنيوية للعزل اللغوي** |
| الحالات الخاصة | ✅ 4 معالَجة + 1 مفتوحة (ق5) |
| واجهات المستقبل | ✅ محجوزة بلا بناء |
| **نقاط قرار مفتوحة** | ⏳ **6 — لا تنفيذ قبل حسمها** |
