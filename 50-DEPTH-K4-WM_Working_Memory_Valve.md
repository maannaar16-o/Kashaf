# 50-DEPTH-K4-WM_Working_Memory_Valve.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `50-DEPTH-K4-WM` |
| **السلسلة** | `50-DEPTH` — امتداد إلى دائرة الإنجاز ($K_4$). **بطاقة الصمام** — قالب $K_4$ المخصص (`131 §2` · `TRF-009`): لا استنساخ لبطاقة $K_2$ (30 جانباً/فاستات) ولا لبطاقة $K_3$ (تعاقب 7 محاور) — البنية مشتقة من شكل $K_4$ المختوم: **مسار + شبكة** (`129 §2` · `DEC-258`) |
| **الصمام** | الذاكرة العاملة النشطة (`K4-WM` / Active Working Memory) — **المحطة `1/7` على المسار** |
| **الإصدار** | v1.0 — **معتمد قالباً مرجعياً لسلسلة `50-DEPTH-K4`** (`DEC-261`) |
| **حالة الاعتماد** | ✅ **مختوم بأمر المالك «نفذ طبقة العمق»** — 2026-08-12 (`DEC-261` · `131-K4-DEPTH`) |
| **السند المؤسس** | `14-CORE-K4 §2/1` و`§3/أ` · `129-K4-DUAL-CHARTER` (`DEC-258`) · `130-K4-RELATIONS` (`DEC-259`/`DEC-260`) · `128-K4-PHASE0` (`DEC-257`) · `19-SEED-K4` v1.1 |
| **وسم عام** | كل اشتقاق في هذه البطاقة **`[اشتقاق غير محقّق ميدانياً]`** (`TRF-002`) — لا يُكرَّر الوسم بنداً بنداً وهو سارٍ |

## 0.1 أقفال العزل المنطبقة على هذا الصمام

| القفل | النص الحاكم | المصدر |
| :--- | :--- | :--- |
| **داخلي — `TI` ⚔️ `WM`** | المبادرة تتحكم في إطلاق الحركة؛ الذاكرة العاملة تتحكم في حمل مسار التنفيذ في الوعي. «إذا لم يبدأ أصلًا فالخلل مبادرة، وإذا بدأ ونسي الخطوات أثناء التنفيذ فالخلل ذاكرة عاملة». | `14-CORE-K4 §3/أ` — **[معتمد]** |
| **خارجي — مقابل $K_2$** | $K_2$ معالجة معرفية تبني الهيكل الإدراكي («ماذا ولماذا؟»)؛ `WM` **حملٌ تنفيذي ميداني** للسياق أثناء الفعل («كيف ومتى؟» أثناء التنفيذ) — لا تُقاس جودة التفكير بحمل الذاكرة ولا العكس. | `14-CORE-K4 §3/ج` — **[تخصيص مشتق]** |
| **سقف عام** | لا قراءة عبر جدار العزل (`DEC-205` قياساً) · لا قراءة فارق زمني (`DEC-244`) · لا ادعاء صدق (`DEC-246`). | `129 §2/④` — **[معتمد]** |

## 0.2 قاعدة الوسم

**[معتمد]** نص مصدري يُستشهد بموضعه · **[مشتق]** قراءة تفسيرية سندها المعتمد حصراً · **[إحالة]** مضمون مثبت في وثيقة أخرى يُشار إليه ولا يُعاد تعريفه (`م-2`).
**تنبيه بنيوي:** البطاقة **شكل تفسيري** يخدم القراءتين المختومتين (مسار/شبكة) — لا تقيس شيئاً، ولا تضيف بنداً أو وزناً أو عتبة (ن-7/④).

---

# القسم 1: بطاقة الصمام — `K4-WM`

## 1.1 الهوية التنفيذية والوظيفة — **[معتمد]**

| الحقل | البيان |
| :--- | :--- |
| **التعريف الحرفي** | «مهارة تنفيذية لحمل الاحتفاظ بالمعلومات والأهداف والخطوات ذهنياً داخل الوعي لفترة قصيرة واستخدامها أثناء التنفيذ دون فقدان السياق.» (`14-CORE-K4 §2/1`) |
| **سؤال التشغيل الداخلي** | *ماذا يجب أن يبقى حاضراً في ذهني الآن؟* — **[معتمد نصاً]**: هو سؤال الجدار الداخلي حرفياً (`§3/أ`) |
| **وعاء الحساب (11 بنداً)** | 5(ب) · 14(أ) · 23(ب) · 26(أ) · 36(ب) · 41(أ) · 48(ب) · 54(أ) · 59(ب) · 66(أ) · 77(أ) |

## 1.2 الموضع على المسار — **[معتمد الترتيب · مشتق القراءة]**

| الحقل | البيان |
| :--- | :--- |
| **المحطة** | الأولى من سبع: `WM ← TI ← F ← PF ← OR ← TM ← PER` (سند الترتيب مزدوج — `129 §2/①`) |
| **ما يستقبله (يستهلكه)** | خريطة الفهم والقرارات المصقولة الواردة إلى الدائرة كلها من $K_2$ عبر $K_3$ (`14-CORE-K4 §1` — على مستوى الدائرة)؛ حظُّ المحطة الأولى منها **[مشتق]**: التقاطها حملاً حياً قابلاً للاستعمال أثناء الفعل |
| **ما يسلّمه** | **سياق التنفيذ** — الأهداف والخطوات والمعلومات حاضرةً في الوعي لكل المحطات بعده: بدءٌ يعرف خطواته، وتركيزٌ يعرف هدفه **[مشتق]** |
| **قيد القراءة** | «الترتيب سند قراءة لا قياس» (`129 §2/①`) — لا يُقرأ من كون `WM` أول المحطات أيُّ تفاضل درجة أو أهمية |

## 1.3 مظهر الانسداد — قراءتا الانقطاع وعنق الزجاجة

| القراءة | البيان | الوسم |
| :--- | :--- | :--- |
| **الانقطاع عند المحطة** | «بدأ **ونسي الخطوات أثناء التنفيذ**» — نص الجدار حرفاً | **[معتمد]** `§3/أ` |
| **عنق الزجاجة (ما بعده)** | كل محطة لاحقة تعمل على سياق متسرب: البدء الحاصل يسقط أثره بفقدان السياق بعده — المشهد المثبت في قيد `K4-REL-10` | **[إحالة]** `130 §2` |
| **حدّ القراءتين** | كلتاهما **وصفية** — لا عتبة تُخترع لتعريف «الأضعف»، والمقارنة بين محطات الفرد الواحد داخل $K_4$ وحده | **[معتمد]** `129 §2/①` |

## 1.4 المشغّل والمعطّل — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المشغّل الأمثل** | تنفيذ متصل بلا انقطاعات، وخطوات صيغت قبل البدء صياغة قابلة للحمل (قليلة، محددة) — فالتعريف يحدّ الحمل بـ«فترة قصيرة» |
| **المعطّل** | (أ) الانقطاعات والتحميل المتوازي — كل مقاطعة تُسقط جزءاً من السياق المحمول. (ب) اتساع المهمة فوق سعة الحمل الذهني بلا نظام خارجي يسند (انظر `K4-REL-04`) |

## 1.5 موقع الصمام في الشبكة — **[إحالة معتمدة إلى `130`]**

القيود تُقرأ من سجلها ولا يُعاد تعريفها هنا (`م-2`)؛ شرط الاستدعاء فيها جميعاً **فارغ حتى مرحلة القراءة**:

| القيد | موقع `WM` فيه | النوع |
| :--- | :--- | :--- |
| `K4-REL-04` — `OR (قوي) → WM (ضعيف)` | **مفعول مؤازرة**: النظام الخارجي يحمل خارجياً ما كان يُحمل ذهنياً — **ولا يؤدي وظيفة الحمل أثناء التنفيذ** | مؤازرة |
| `K4-REL-10` — `WM (ضعيف) → TI (قوي)` | **فاعل هدر** (حال ضعفه): فقدان السياق يُسقط أثر البدء الحاصل | هدر وتثبيط |

**فجواته الموثقة** (الصمت مشروع — `TRF-007`): `WM–F` · `WM–PF` · `WM–TM` · `WM–PER` — لا قيد لها، ولا تُملأ هنا.

## 1.6 الحدود وجدران العزل

مفصّلة في `§0.1` — الجدار الحاكم لهذه البطاقة هو **الفاصل الداخلي `TI` ⚔️ `WM`**: وظيفته **الإسناد التمييزي** (لم يبدأ ⇒ مبادرة · بدأ ونسي ⇒ ذاكرة) قائمة كما هي، وقيد `K4-REL-10` يقرأ مشهده تفاعلاً بنص مانع للازدواج (`TRF-008`/`م-1`) — **[معتمد]**.

## 1.7 ديون الوعاء المثبتة — **[معتمد — `128 §4`]**

لا دَين مثبتاً على وعاء `WM` (ديون الأداة الثلاث داخل $K_4$ تقع في أوعية `PER`/`PF`/`F`).

---

# القسم 2: سلّم النطاقات وقراءته لهذا الصمام

**[معتمد هيكلاً — `DEC-261` · `TRF-010`]** البنية العددية الثمانية تُطبَّق حرفياً: وعاء `WM` أحد عشر بنداً (`y+z=11` · `MaxRaw=66`) ومعادلة `SP` عامة بنص `41 §3.3` (`DEC-065`). **[معتمد دلالةً — `TRF-011`]** الثلاثية بقراءة القدرة (لا «مهيمن/مساند» — الصمامات لازمة معاً): **حضور محدود · كفاءة أساسية · قدرة عالية**. القراءة النطاقية **وصفية**؛ لا يُستدل منها على قيود الشبكة ولا العكس (لا استدلال درجة↔علاقة — `129 §4`).

| الكتلة | المدى (SP) | الدلالة | قراءة `WM` **[مشتق]** |
| :--- | :--- | :--- | :--- |
| `OUT` | SP<0% | فجوة تشغيلية — بيانات متعارضة، لا يُصنَّف | — |
| `L−` / `L` | 0–40% | **حضور محدود** | حمل السياق غير منتظم حتى في تنفيذ عاديّ الحمل |
| `M` | 40–50% | **حضور محدود** (طرفه الأعلى) | — |
| `M+` | 50–70% | **كفاءة أساسية** | حمل موثوق للخطوات والأهداف في الظروف العادية |
| `H` / `H+` | 70–100% | **قدرة عالية** | سياق حاضر حتى مع مقاطعات وحمل معتدل |
| `H++` | SP>100% | **قدرة عالية** (ذروتها) | — |

---

# القسم 3: الملحق القياسي — **[معتمد]**

| العنصر | القيمة |
| :--- | :--- |
| وعاء `K4-WM` | 5(ب) · 14(أ) · 23(ب) · 26(أ) · 36(ب) · 41(أ) · 48(ب) · 54(أ) · 59(ب) · 66(أ) · 77(أ) — 11 بنداً |
| السعة الخام القصوى (`MaxRaw`) | 66 (11×6) |
| معادلة الدرجة المعيارية | `SS = x − 2z + y` ≡ `x + 3y − 22` (بقيد `y+z=11`) — **[معتمد، عام `DEC-028`]** |
| الحد الأقصى للدرجة المعيارية | `SS_max = 77` |
| النسبة المعيارية | `SP = SS ÷ 66 × 100` (`41 §3.3` — عامة لكل الدوائر · `DEC-065`) · `SP_max` ≈ 116.7% |
| ديون عاملية على الوعاء | لا شيء (`128 §4`) |

---

# القسم 4: سجل الفجوات والإحالات

| البند | البيان |
| :--- | :--- |
| فجوات الشبكة الأربع (`§1.5`) | موثقة في `130 §3` — لا تُملأ في طبقة العمق |
| بذور التصميم | لا بذرة في `19-SEED-K4` تخص `WM` بعينه (`SEED-K4-10` «مدخلات ناقصة» عامة الصمامات) — يُسجَّل الفراغ ولا يُؤلَّف مشهد |
| `GAP-K4-CASES-01` | حالات تحقق المالك (تسند ترتيب المسار) ما زالت مفتوحة (`129 §6`) — تسري على هذه البطاقة كما على الميثاق |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| بطاقة صمام مخصصة (لا استنساخ `K2`/`K3`) | ✅ بنية `131 §2` — تخدم قراءتَي المسار والشبكة |
| نصوص معتمدة مستشهد بها بموضعها | ✅ التعريف · سؤال الجدار · مشهد الانسداد |
| الشبكة إحالةً لا إعادة تعريف (`م-2`) | ✅ قيدان + أربع فجوات موثقة |
| سلّم النطاقات | ✅ هيكل ثماني (`TRF-010`) + ثلاثية القدرة (`TRF-011`) |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة قالباً مرجعياً للسلسلة (`DEC-261`)** |
