# 161-RANGE-CLAIMS — `DEC-290`: **ادّعاءات النطاق تُشتقّ** — والزحف ينتقل إلى الجار

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**زحفُ الرؤوس والتذييلات**» — على خياراتٍ مرفوعة — 2026-08-15 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-15 |
| **الأثر** | **علامة سابعة** في البوابة · أربع وثائق حيّة مزامَنة · **ورقمٌ منافسٌ أُزيل** |
| **التغيير المرافق** | `CHG-110` |
| **الحاكم** | `DEC-287` (خطُّ الأساس يُقاس) · `DEC-274` (`م-2` على الرقم · وصونُ الترتيب الزمني) · `م-8` |
| **الكود** | `161` |

---

# §1 الاكتشاف — **الفحص يُخدَع بجاره**

`DEC-287` وسّع القياس إلى **حقلٍ واحد** في `00-HANDOVER`. فقِيس ذلك الحقل، **وزحف الصنفُ سطراً واحداً**:

| في الوثيقة نفسها | ماذا يقول |
| :--- | :--- |
| **ذيلها** (المقيس بـ`DEC-287`) | خط الأساس **`DEC-289`** · الترقيم التالي `DEC-290` |
| **رأسها** (غير المقيس) | آخر قرار مختوم **`DEC-270`** · الترقيم التالي `DEC-271` |

⇒ **تسعة عشر قراراً بين سطرَين في وثيقةٍ واحدة** — والفحص أخضر.

> **ودرسُ `DEC-287` كان صحيحاً وناقصاً معاً**: «الذي يتكرّر لا يُعالَج بالانتباه بل بتوسيع الفحص» — **لكن الفحص وُسِّع إلى حقلٍ لا إلى صنف**، فبقي للزحف موضعٌ يهاجر إليه. **وتوسيعُ الفحص إلى حقلٍ يعالج الحادثة؛ وتوسيعُه إلى صنفٍ يعالج الصنف.**

---

# §2 الحصيلة المقيسة — **عشرة ادّعاءاتٍ متخلّفة في أربع وثائق**

| الوثيقة | ما وجد |
| :--- | :--- |
| `02-MASTER` | رأسٌ توقّف عند `DEC-168` · وتذييلٌ عند `DEC-278`/`CHG-097` |
| `00-INDEX` | «نطاق الاستدلال» عند `DEC-168` · وتذييلٌ عند `DEC-278`/`CHG-097` |
| `00-HANDOVER` | آخر قرارٍ مختوم `DEC-270` · آخر تغيير `CHG-097` · والمُنتَج `DEC-230…278` |
| `00-MANIFEST` | **«الترقيم التالي `DEC-169`»** — في موضعين |

**والأخير ليس تجميلياً**: `DEC-274` جعل الرقم **مورداً مشتركاً يُقرأ**، ثم قال إن سلطته موضعٌ واحد (`م-2`). ووثيقةٌ تُقرأ تُعلن رقماً **متخلّفاً بمئةٍ وإحدى وعشرين** — **فاعلٌ يقرؤها يختم `DEC-169`**. والبوابة كانت تفحص التصادم في `01-MASTER` وحده.

> **الادّعاء المتقادم في وثيقةٍ تُقرأ ليس زينةً بالية، هو مصدرُ قرارٍ خاطئ.**

---

# §3 الفحص — **يشتقّ ولا يحفظ**

العلامة ⑦: في **الرأس والذيل** من كل وثيقةٍ حيّة، يُقابَل كل **ادّعاء عملة** بما يُشتقّ من السجلَّين:

| الادّعاء | المتوقَّع |
| :--- | :--- |
| مدى `DEC-A…B` / `CHG-A…B` | **`B` = الأعلى المختوم** |
| «الترقيم التالي» | الأعلى + 1 |
| «آخر قرار مختوم» · «خط الأساس» | الأعلى |
| «آخر تغيير» | أعلى `CHG` |

**والمدى يُقاس بأعلاه وحده** — فرأس `01-MASTER` يعدّد مديات الجلسات كلَّها، وإلزامُ كلٍّ منها بالأعلى **يمحو التاريخ**. المطلوب أن **يمتدّ التعداد** إلى آخر جلسة، لا أن تُزوَّر أولاها.

**والرقمان يُحسبان مرّة واحدة** — العلامة ⑤ تردّهما والعلامة ⑦ تستهلكهما (`م-2` على الكود كما على الوثائق).

---

# §4 الحيّ يُعلَن — **والخارجُ محضرٌ لا متروك**

القائمة **مُصرَّح بها ولا تُستنتج**، لأن كل معيارٍ مشتقٍّ يكذب:

- **الاسم وتاريخه لا يفصلان**: `00-HANDOVER_2026-08-05` يحمل تاريخاً **وهو حيٌّ يُزامَن** (`CHG-088` نصّ أنه «تاريخ تأليفٍ لا صلاحية»)، و`00-SESSION-*` تحمل تواريخ **وهي محاضر لحظات**.
- **وتصويبُ المحضر إعادةُ كتابةٍ لما جرى** — يمنعها ما يصون الترتيب الزمني في `DEC-274`: السجلّ يحكي ما جرى لا ما اشتُهي.

⇒ **خمسُ وثائق حيّة**، والباقي **محضرٌ مصون** لا وثيقةٌ متروكة. **والفرق مكتوبٌ لا متروكٌ للتقدير.**

و`00-MANIFEST` كان الحدّ الغامض: جردُ رفعٍ لجلسة `K3` **يُقرأ عملياً**. فعُومل حيّاً **وصُرِّح بنطاقه في رأسه** («جردُ رفعٍ لجلسةٍ بعينها — ولا يُقرأ منه رقم») ⇒ **الخطران يسقطان معاً**: لا رقمَ متخلّفٌ يُقرأ، ولا جردٌ تاريخيٌّ يُمحى.

---

# §5 🔎 والحقنة الثالثة **مرّت** — فكشفت الثقب نفسه في الفحص الجديد

| الحقنة | النتيجة |
| :--- | :--- |
| تذييل الفهرس يتخلّف **قراراً واحداً** | ❌ **رُصد** — `288≠289` |
| رقمٌ منافسٌ يعود إلى البيان (`DEC-169`) | ❌ **رُصد** |
| **حذفُ الادّعاء بدل تغييره** | ✅ **مرّ** — ثم ❌ **رُصد** بعد سدّه |

**وهذا هو نفسه ما رصده `DEC-287 §4`** («فحصٌ يقرأ حقلاً يُخدَع بحذفه لا بتغييره فقط») — **وقد أعدتُ إنتاجه في الفحص الذي كتبتُه لعلاجه.**

⇒ سُدّ بإعلان **عددٍ أدنى للادّعاءات** لكل وثيقة، كما يُعلَن `EXPECTED_COUNT`: فمسحُ حقلٍ **يُسقط** الفحص ولا يُسكته، وتغييرُ رأسٍ يصير **تصريحاً**.

> **والدرس الأعمّ: الحرسُ الجديد يُحقن بالحقنة التي أسقطت سلفه.**

---

# §6 ما لم يُمسّ

- ❌ **صفر سطرٍ في الكود التشغيلي** — لا محرّك ولا حزمة ولا نصَّ قارئ.
- ❌ **البصمات السبع حرفياً** — الفحص يقرأ وثائق ولا يمسّ مخرَجاً.
- ❌ **محاضر الجلسات** (`00-SESSION-*` · `00-PATCHPACK-*`) — **مصونةٌ بقصد**.
- ❌ **ولا رقمَ حُجز ولا قرارٌ أُعيد ترقيمه**.

**وحدٌّ مصرَّح به**: المقيس **ادّعاءات الأرقام** وحدها. أعدادٌ نثرية كـ«أدوات التحقّق من ٨ إلى ٢٧» صُوِّبت **يدوياً** في هذه الدفعة **وليست مقيسة** — والحدّ يُعلَن ولا يُوهَم بشموله (`م-8`).

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| الاكتشاف | ✅ **عشرة ادّعاءاتٍ متخلّفة** — منها رقمٌ منافسٌ يُقرأ |
| الفحص | ✅ **يشتقّ من السجلَّين** ولا يحفظ نسخةً ثانية |
| الحيّ | ✅ **مُعلَنٌ خمساً** — والمحضر مصونٌ بمسوّغه |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** — والثالثة **مرّت أولاً** فكشفت ثقب الفحص |
| الأدوات | **27/27** · **سبع بصمات** · **سبع علامات** |
| **الرابعة — بلا حقن** | ✅ **أسقطت الفحصَ عند أول تشغيلٍ حيّ**: نسيتُ سطراً في `00-INDEX` وأنا أزامن الوثائق للقرار نفسه ⇒ `289≠290`. **والحرس الذي يمسك كاتبَه في أول دقيقة يمسك غيرَه بعد شهر** |

---

**[نهاية `161-RANGE-CLAIMS` — `DEC-290` · الترقيم التالي `DEC-291` · `CHG-110`]**
