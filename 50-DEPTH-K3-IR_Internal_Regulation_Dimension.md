# 50-DEPTH-K3-IR_Internal_Regulation_Dimension.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `50-DEPTH-K3-IR` |
| **السلسلة** | `50-DEPTH-K3` — **أول تطبيق إنتاجي** للقالب المعتمد (`50-DEPTH-K3-EP` v1.0، `DEC-067`) على مهارة ثانية |
| **المهارة** | التحكم الانفعالي الداخلي (`K3-IR` / Internal Emotional Regulation) |
| **الإصدار** | v1.1 — **معتمد ومُعاد تعريفه أحادياً** (`DEC-068` ثم `DEC-076`) |
| **حالة الاعتماد** | ✅ معتمد · **`GAP-K3-IR-01` مغلقة نهائياً** بالخيار B (`DEC-076`): `IR` مهارة **خفض/تهدئة أحادية كما تُقاس**؛ اتجاه «الاستثارة» في التعريف الحاكم **مُعرَّف نظرياً لكنه غير مقيس** — يُوثَّق ولا يُدَّعى في التقارير |
| **الوثيقة الموازية** | `55-USER-K3-IR` (لا تُنتج قبل اعتماد هذه الوثيقة — قفل التزاوج) |
| **السند المؤسس** | `13-CORE-K3` v2.0 §3/§4/§5 · `DEC-030` · `DEC-057…067` (القالب وكل قراراته، مطبَّقة مباشرة دون إعادة فتح) · `41-MEASURE §3.3/§5.3` |
| **الوثائق المصدرية** | `13-CORE-K3` §§1–7 (كامل) · `41-MEASURE §5.3/§6` · `50-DEPTH-K3-EP` v1.0 (القالب البنيوي المرجعي) |

## 0.1 أقفال العزل المنطبقة على هذه المهارة

| القفل | النص الحاكم | المصدر |
| :--- | :--- | :--- |
| **قفل الوقود ($K_1 \not\rightarrow$ توليد $K_3$)** | $K_1$ يخلق النية ويحدد الوقود الخام؛ $K_3$ لا يولّد طاقة مستقلة. | `13-CORE-K3` §5 |
| **قفل الحالة ≠ السلوك ($K_3 \not\leftrightarrow K_4$)** | $K_3$ تدير الحالة (State)؛ $K_4$ تدير السلوك الحركي (Behavior). | `13-CORE-K3` §5 |
| **قفل الحدود الداخلي (`EP` ⚔️ `IR`)** | الملاحظة استقبال بلا تدخل تنظيمي؛ التحكم أول ميكانيزم إجرائي لتعديل الشحنة. | `13-CORE-K3` §4 |
| **قفل الحدود الداخلي (`IR` ⚔️ `BI`)** | التحكم يعمل حصرياً على ضبط المعايرة الانفعالية في الحالة الداخلية (Internal State)؛ الكبح صمام يعمل حصرياً على حظر المخرجات السلوكية الخارجية (External Output). | `13-CORE-K3` §4 |

## 0.2 قاعدة الوسم داخل الوثيقة

كل وحدة محتوى موسومة: **[معتمد]** · **[مشتق]** · **[مقترح]** · **[فجوة]**. البطاقة أدناه سباعية المحاور مطابقة لقالب `EP` المعتمد (`DEC-058/066`)؛ لا إعادة تفاوض على البنية، فقط على المحتوى.

---

# القسم 1: البطاقة المعمارية — `K3-IR`

## 1.1 المحور الأول: الهوية التنفيذية والوظيفة (المصدر: `13-CORE-K3` §3) — **[معتمد]**

| الحقل | البيان |
| :--- | :--- |
| **التعريف البنيوي الحرفي** | القدرة التنفيذية على تعديل وحقن الشحنة الانفعالية داخل الذات (**تهدئة أو استثارة**) لإعادة النظام إلى خط التوازن الإدراكي الطبيعي **دون كبت**. |
| **سؤال التشغيل الداخلي** | *كيف أُهَدّئ وأعزل ما أشعر به داخلياً؟* |
| **وعاء الحساب الإحصائي (11 بنداً)** | 2(ب) · 11(أ) · 20(أ) · 35(أ) · 48(أ) · 60(أ) · 71(أ) · 80(أ) · 86(أ) · 90(أ) · 93(أ) |
| **المرساة العصبية** | Fronto-limbic — على مستوى الدائرة الكاملة، لا مفصّلة لكل مهارة (كما في `EP`). |

**✅ ملاحظة اتساق (`GAP-K3-IR-01` — محسومة بـ`DEC-076`):** التعريف الحرفي ثنائي الاتجاه («تهدئة أو استثارة»)، لكن **سؤال التشغيل الداخلي أحادي**، و**الفحص الحرفي لوعاء القياس أثبت أن 11/11 بنداً تقيس اتجاه الخفض فقط** (`41-MEASURE-CONFLICT` §1). حُسم التعارض بالخيار B: **يُعتمد الاتجاه الأحادي (الخفض) هو المرجع التشغيلي والقياسي لـ`IR`**، ويُوثَّق اتجاه «الاستثارة» كبُعد **معرَّف نظرياً غير مقيس** (لا يُدَّعى في أي تقرير حتى تُوسَّع الأداة مستقبلاً — الخيار A المؤجَّل).

## 1.2 المحور الثاني: مراحل التشغيل الداخلية — **[مشتق]**

| المرحلة | الوظيفة |
| :--- | :--- |
| **م1 — استقبال التمثيل** | تسلّم التمثيل الوارد من `EP` كما هو، دون تعديل مسبق على المُدخل. |
| **م2 — تقدير الفارق عن خط التوازن** | تحديد ما إذا كانت الشحنة الواردة أعلى أو أدنى من مستوى التوازن الإدراكي المناسب للموقف. |
| **م3 — الحقن التعديلي** | تطبيق فعل تعديل فعلي — تهدئة عند فرط الاستثارة، أو تحفيز عند الخمول — لا كبتاً ولا إنكاراً للإشارة. |
| **م4 — التسليم لـ `BI`** | تسليم الحالة بعد التعديل كمدخل لقرار السماح/المنع السلوكي في `BI`. |

## 1.3 المحور الثالث: المشغّل والمعطّل — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المشغّل الأمثل** | تمثيل صادق وواضح وارد من `EP` + مساحة زمنية كافية لإجراء التعديل قبل الحاجة إلى مخرج سلوكي فوري. |
| **المعطّل** | (أ) تمثيل `EP` مشوّه أو متأخر ("تنظيم أعمى" — راجع `50-DEPTH-K3-EP §1.4`). (ب) ضغط زمني حاد يفرض قفزاً مباشراً إلى `BI` دون تعديل كافٍ. (ج) الانزلاق نحو الكبت (قمع الإشارة بدل تعديلها) — انحراف صريح عن التعريف الحاكم «دون كبت». |

## 1.4 المحور الرابع: الموقع في السلسلة التعاقبية — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المصدر الأعلى (Upstream)** | يستقبل حصرياً من `K3-EP` (التمثيل الأولي الصادق). |
| **المصب الأدنى (Downstream)** | يغذي `K3-BI` بالشحنة المعدَّلة؛ يمتد أثره لاحقاً إلى `CF`/`ST` حسب الحاجة. |
| **نقطة الانهيار عند الغياب** | ضعف `IR` = وصول شحنة خام غير معدَّلة إلى `BI` ← إما اندفاع سلوكي (كبح غير كافٍ لضبط شحنة لم تُعدَّل) أو لجوء لكبت قسري بديل غير متسق بنيوياً مع تعريف `IR` ذاته. |

## 1.5 المحور الخامس: بصمة التنظيم — **[معتمد — مُعاد تعريفه بـ`DEC-076`]**

| البند | البيان |
| :--- | :--- |
| **الاتجاهية (كما تُقاس)** | `IR` **أحادية القياس**: تقيس **قوة الخفض/التهدئة** (استعادة التوازن عند فرط الاستثارة). التعريف الحاكم يذكر «الاستثارة» أيضاً، لكن **0/11 من بنود الوعاء تقيسها** (`41-MEASURE-CONFLICT §1`)؛ فاتجاه الرفع **مُعرَّف نظرياً غير مقيس**، ولا يدخل أي حساب أو تقرير. |
| **بُعد غير مقيس موثَّق** | «التحفيز عند الخمول» يبقى مكوّناً نظرياً من التعريف، **مجمَّداً قياسياً** حتى توسعة الأداة (الخيار A، مؤجَّل). يُحظر إسناد أي درجة أو قراءة له. |
| **أثر منهجي** | تُقرأ درجة `IR` كـ**قوة قدرة التهدئة** على سلّم رتيب (ضعيف ← قوي)، لا كاتجاه. `SP` منخفضة = ضعف في التهدئة/استعادة التوازن؛ لا يُقرأ منها شيء عن التحفيز (غير مقيس أصلاً). |

## 1.6 المحور السادس: الحدود الداخلية والخارجية — **[معتمد استنساخ + مشتق تخصيص]**

| الحد | النص | الحالة |
| :--- | :--- | :--- |
| **داخلي — `EP` ⚔️ `IR` (أعلى)** | الملاحظة استقبال بلا تدخل تنظيمي؛ التحكم أول ميكانيزم لتعديل الشحنة. | **[معتمد]** استنساخ حرفي (`§4`) |
| **داخلي — `IR` ⚔️ `BI` (أسفل)** | التحكم يضبط المعايرة **الداخلية**؛ الكبح يحظر المخرج **الخارجي** حصراً. | **[معتمد]** استنساخ حرفي (`§4`) |
| **خارجي — مقابل $K_1$** | لا تولّد طاقة؛ تعدّل ما استُقبل من وقود المفتاح عبر `EP`. | **[معتمد]** |
| **خارجي — مقابل $K_4$** | تصف تعديلاً في الحالة الداخلية، لا سلوكاً حركياً ولا نتيجة ميدانية — حتى لو كان التعديل "نشيطاً" (كالتحفيز). | **[معتمد]** |
| **خارجي — مقابل التنظيم الاجتماعي/تهدئة الآخرين** | التعريف الحاكم يحصر `IR` في تعديل الشحنة **داخل الذات**؛ لا يشمل تهدئة أو تحفيز انفعالات **الآخرين** — ذاك بناء مختلف كلياً (يمس `K1-EMO`/`K2-S` لا `K3-IR`). | **[مشتق]** — قفل نطاق صريح لمنع توسّع غير مرخّص للمهارة |

## 1.7 المحور السابع: إشارات القوة والضعف الملحوظة — **[مشتق]**

> نفس منهجية `EP §1.7`: إشارات مُسندة لمراحل §1.2، لا تشخيص ولا حكم دائم.

### 1.7.1 إشارات القوة

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 — استقبال التمثيل | القبول بلا تحريف | يتقبّل ما لاحظه في `EP` كما هو، دون إنكار فوري أو تهوين. |
| م2 — تقدير الفارق | تشخيص الاتجاه الصحيح | يحدد بسرعة أهو بحاجة لتهدئة أم لتحفيز، لا يخلط بينهما. |
| م3 — الحقن التعديلي (خفض) | التهدئة الفعلية | يهدأ فعلياً خلال دقائق من موقف مستفز، لا مجرد كبت ظاهري. |
| م3 — الحقن التعديلي (رفع) | التحفيز الفعلي | يستطيع رفع طاقته ذاتياً عند الخمول قبل مهمة تتطلب حضوراً. |
| م4 — التسليم لـ `BI` | تسليم متوازن | يسلّم حالة معدَّلة تسمح بقرار سلوكي رشيد، لا شحنة متطرفة في أي اتجاه. |

### 1.7.2 إشارات الضعف

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 — استقبال التمثيل | الإنكار الفوري | يرفض أو يهوّن ما لاحظه للتو في `EP` بدل معالجته. |
| م2 — تقدير الفارق | خطأ الاتجاه | يحاول التهدئة في موقف يحتاج طاقة، أو العكس. |
| م3 — الحقن التعديلي (خفض) | اللجوء للكبت | يخفي الشعور دون تفريغه أو معالجته فعلياً — انحراف عن «دون كبت». |
| م3 — الحقن التعديلي (رفع) | العجز عن التحفيز | يبقى منطفئاً رغم حاجة الموقف لطاقة أعلى، دون قدرة على "شحن" نفسه. |
| م4 — التسليم لـ `BI` | التسليم الخام | يُوصل شحنة غير معدَّلة لـ`BI`، فيُضطر الأخير لكبح قسري لحالة لم تُهذَّب أصلاً. |

**🔒 قفل الخلط الإلزامي:** إشارات وصفية-نمطية لا تشخيصية؛ "اللجوء للكبت" وصفٌ لانحراف تشغيلي عن التعريف الحاكم، لا حكماً على شخصية صاحبه. لا علاقة بتنظيم انفعالات الآخرين (راجع §1.6).

---

# القسم 2: التشريح العميق

**[مشتق]** — دورة المعالجة: أي تمثيل وارد من `EP` يمر إلزامياً عبر `م1→م2→م3→م4`. القفز المباشر من `م1` إلى `م4` (تسليم دون تعديل) هو بالضبط ما يصفه المعطّل (ج) في §1.3 — كبت أو تجاهل بدل معالجة فعلية.

**[مشتق]** — لماذا الثنائية الاتجاهية مهمة بنيوياً: لو كانت `IR` أحادية الاتجاه (خفض فقط، كما يوحي سؤال التشغيل)، لَعجز النظام عن معالجة حالات الخمول المُعطِّلة للأداء بنفس درجة عجزه عن معالجة فرط الاستثارة. التعريف الحرفي يرفض هذا التضييق صراحة («تهدئة أو استثارة»)، لذا اعتُمد في §1.5 كأساس الوظيفة الكاملة رغم تفاوت الحقلين (§6).

**[مشتق]** — ربط بـ§1.7: كل إشارة مُسندة لمرحلة ولاتجاه (خفض/رفع) حيثما أمكن، لتفادي تسطيح "ضعف التحكم" إلى صفة واحدة تُخفي أي الاتجاهين هو موضع الضعف الفعلي.

---

# القسم 3: طيف الشدة الثماني لـ `K3-IR`

**[معتمد — تطبيق مباشر لـ `DEC-061`/`DEC-064`]** نفس البنية والدلالة المعتمدتين لكل مهارات $K_3$ — لا إعادة فتح:

| الكتلة | المدى (SP) | الدلالة المعتمدة |
| :--- | :--- | :--- |
| `OUT` | SP<0% | نطاق فجوة تشغيلية |
| `L−`/`L`/`M` | ≤50% | **نطاق الحضور المحدود** |
| `M+` | 50–70% | **نطاق الكفاءة الأساسية** |
| `H`/`H+`/`H++` | >70% | **نطاق القدرة العالية** |

**⚠️ تنبيه خاص بـ`IR`:** نظراً لثنائية الاتجاه (§1.5)، درجة SP واحدة قد تُخفي تفاوتاً حاداً بين قدرة الخفض وقدرة الرفع. يُوصى — دون حسم — بأن يلتزم أي تقرير سردي بذكر أي الاتجاهين لاحظه المحرك من إشارات §1.7، لا الاكتفاء بالرقم المجرّد. **[مقترح تشغيلي — يحتاج قراراً عند بناء المحرك، لا الآن]**

---

# القسم 4: خريطة التسلسل-الحدود-الانهيار

| الحقل | البيان |
| :--- | :--- |
| **العلاقة مع `EP` (السابقة)** | استهلاك أحادي الاتجاه للتمثيل؛ لا تغذية عكسية موثقة. |
| **العلاقة مع `BI` (التالية)** | تسليم أحادي الاتجاه للشحنة المعدَّلة؛ `BI` يستهلكها لقرار السماح/المنع. |
| **حد الفشل المتسلسل** | `IR` ضعيفة ← شحنة خام/متطرفة تصل لـ`BI` ← إما كبح متأخر (اندفاع يفلت) أو كبح مُبالغ فيه لحالة كان يمكن تعديلها سابقاً بلا قمع. |

---

# القسم 5: جدران العزل الخارجية

| الدائرة المقابلة | التطبيق الخاص بـ `IR` | الحالة |
| :--- | :--- | :--- |
| $K_1$ | لا تقيس دافع الاشتعال؛ تقيس فقط تعديل الشحنة الناتجة عنه. | معتمد |
| $K_4$ | تعديل داخلي، ولو نشيطاً (تحفيز)، لا يُقاس كسلوك ميداني. | معتمد |
| نطاق اجتماعي (تهدئة الآخرين) | خارج نطاق `IR` تماماً — راجع §1.6. | مشتق — قفل نطاق |

**⚠️ إحاطة:** `RSK-005` (حدود $K_3$/$K_4$ الصارمة) لا تزال مفتوحة؛ أثرها على `IR` أعلى قليلاً من `EP` (التحفيز الداخلي قد يُخلط ظاهرياً بطاقة الإنجاز)، لكنه يبقى غير حرج قياساً بما سيحدث عند `BI`/`ST`.

---

# القسم 6: سجل الفجوات

| كود | البيان | الأثر | الخطورة | القرار المؤقت | المطلوب |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **GAP-K3-IR-01** | ✅ **مغلقة نهائياً (`DEC-076`، الخيار B).** كُشف بالفحص الحرفي أن وعاء `IR` أحادي القطب (11/11 خفض)؛ اعتُمد الاتجاه الأحادي مرجعاً، ووُثّق «الاستثارة» كبُعد معرَّف غير مقيس. لا ادّعاء اتجاهي في أي تقرير. | — | — | — | مغلقة — راجع `41-MEASURE-CONFLICT` |
| **إحالة — RSK-005** | حدود $K_3$/$K_4$ الصارمة مفتوحة مشروعياً. | يعترض بناء `BI`/`ST` بقوة أكبر | عالية (مؤجلة) | لا تعطيل على `IR` | حسمه قبل `K3-BI` |

---

# القسم 7: ملحق الربط القياسي

| العنصر | القيمة |
| :--- | :--- |
| وعاء `K3-IR` | 2(ب) · 11(أ) · 20(أ) · 35(أ) · 48(أ) · 60(أ) · 71(أ) · 80(أ) · 86(أ) · 90(أ) · 93(أ) — 11 بنداً |
| السعة الخام القصوى (`MaxRaw`) | 66 |
| معادلة الدرجة المعيارية | `SS = x − 2z + y` ≡ `x + 3y − 22` — **[معتمد، عام `DEC-028`]** |
| الحد الأقصى للدرجة المعيارية | `SS_max = 77` |
| النسبة المعيارية | `SP = SS ÷ 66 × 100` — **[معتمد — `41-MEASURE §3.3`، `DEC-065`]** |
| `SP_max` | ≈ 116.7% |
| ديون عاملية مسجَّلة | لا شيء مسجل رسمياً؛ راجع `GAP-K3-IR-01` كتفاوت حقلي، لا ديناً عاملياً بمعنى `RSK-016` |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| مطابقة القالب المعتمد (`50-DEPTH-K3-EP` v1.0) | ✅ 7 محاور — بنية مطابقة تماماً |
| اكتشاف اتساق (غير موروث من `EP`) | ✅ `GAP-K3-IR-01` — كُشف واقع أحادية الوعاء، وحُسم بـ`DEC-076` (الخيار B) |
| بصمة التنظيم | ✅ **أحادية القياس** (قوة الخفض) بعد `DEC-076` — «الاستثارة» بُعد معرَّف غير مقيس |
| إشارات القوة/الضعف | ✅ 5+5 — تُقرأ كقوة/ضعف في التهدئة، لا كاتجاهين |
| طيف الشدة | ✅ تطبيق مباشر — سلّم رتيب (ضعيف ← قوي) |
| فجوات مرصودة | **`IR-01` مغلقة (`DEC-076`)** + إحالة `RSK-005` (مطوَّقة بـ`DEC-072`) |
| **حالة الوثيقة** | ✅ **v1.1 — معتمدة (`DEC-077`)؛ `GAP-K3-IR-01` مغلقة بالخيار B (`DEC-076`)** |
