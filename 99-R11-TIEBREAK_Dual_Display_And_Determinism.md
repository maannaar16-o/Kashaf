# 99-R11-TIEBREAK — العرض المزدوج `St+H` + `DEF-K2-04`

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** (`R11` · `DEF-K2-04`) |
| **نطاق الاستدلال** | `51-MATRIX-06/R11` + حسم `M-07/الخيار ج` · `51-MATRIX-05 §1.1/LD-02` · `41 §5.2` · `56-TEST-02 §4` |
| **الحصيلة** | **خارطة الطريق مكتملة** — `R1`…`R11` كلها منفَّذة |
| **الإصدار** | v1.0 |

---

# القسم 1: `GAP-RPT-K2-03` — مُغلق

**حزمة حادية عشرة** `LOOKALIKE_K2` (`2e5e89486d41d5a3`) — **7 أزواج تمييز** من `51-MATRIX-05`:

| الكود | الزوج | | الكود | الزوج |
| :--- | :--- | :-- | :--- | :--- |
| `LD-01` | `C ↔ O` | | `LD-05` | `R ↔ K₄` |
| `LD-02` | **`St ↔ H`** | | `LD-06` | `O ↔ K₄` |
| `LD-03` | `S ↔ E` | | `LD-07` | `E ↔ K₃` |
| `LD-04` | `A ↔ St` | | | |

## المخرج عند اشتعالهما معاً

```
**حين يشتعل التوليد والتوجيه معاً (R11):**

> وسم إلزامي: [تجاور عاملي موثق — حزمة توليد-توجيه]

- **التصوري (H):** توسيع الاحتمالات بلا قيد (تباعدي)
- **الاستراتيجي (St):** تضييق الاحتمالات نحو مآل نافع (احتمالي)

> هل يفلتر نحو مسار واحد (St)، أم يفتح بدائل لا نهائية (H)؟
> يُقرآن حزمةً واحدة لا عدستين منفصلتين — ويُحظر فصل إسهامهما رقمياً.
```

**مطابق لحسم `M-07/الخيار ج` في بنوده الثلاثة:** الوسم · `LD-02` تفسيرياً · **حظر الفصل الرقمي**. والشرط `SP > 50٪` **لكليهما** — مُختبَر بحالتَي حدّ (`50.1` يُطلق · `50.0` لا يُطلق).

---

# القسم 2: 🔴 `DEF-K2-04` — كسر التعادل غير حتمي

## الرصد

حالتا الحدّ الجديدتان (`R11-EDGE` · `R11-NONE`) — حيث `SP(H) = SP(St)` — أخرجتا **تباعداً في ترتيب اللوحة والمركز**:

| | السطر 13 من اللوحة |
| :--- | :--- |
| بايثون | `\| التصوري (H) \| M+ \| مساندة \|` |
| JS | `\| الاستراتيجي (St) \| M+ \| مساندة \|` |

## السبب الجذري

```python
ranked = sorted(sp, key=lambda d: sp[d], reverse=True)   # يفرز مفاتيح sp
```

`sorted` مستقرّ، فالتعادل يُكسر بـ**ترتيب مفاتيح القاموس الوارد من المستدعي**. أي أن `{'H':50.1,'St':50.1,…}` و`{'St':50.1,'H':50.1,…}` — وهما **المدخل نفسه رياضياً** — يعطيان **ترتيبين مختلفين**.

⇒ **ليس تباعد نسختين فحسب، بل لا-حتمية في المرجع نفسه.**

## الحكم — الدرجة الوسطى من `DEC-200`

**السند يفصل:** ترتيب `41 §5.2` هو `A·R·C·O·S·E·St·H` — **مطابق حرفياً لثابت `LENSES`**. فالفرز على `LENSES` كسرُ تعادلٍ **مسنَد**، والفرز على مفاتيح المستدعي **بلا سند**.

**التصحيح:** `sorted(LENSES, …)` بدل `sorted(sp, …)`.

> **لم أقل «JS أصاب وبايثون أخطأ».** JS صادف الترتيب الصحيح بتنفيذ صريح؛ والحكم للسند لا للتنفيذ الأسبق (`DEC-200`).

**التحقق:** `classify` صار **لا يتأثر بترتيب مفاتيح المدخل** — مُختبَر بقاموسين متعاكسي الترتيب.

---

# القسم 3: هذا العيب كان مُتنبَّأً به

`87-PARITY §6` (بند 4) نصّ قبل أربع جولات:

> «كسر التعادل عند تساوي `SP` — استقرار الفرز مضمون في الطرفين، لكن **الترتيب الابتدائي** يجب أن يُبنى بالطريقة نفسها»

**رُصد نظرياً ولم يُفحص** حتى أوجبت `R11` إضافة حالات تعادل. ⇒ **قائمة المخاطر لا تُغني عن حالة اختبار**؛ الرصد بلا حالة يبقى ورقة.

---

# القسم 4: الانحدار

| المجموعة | النتيجة |
| :--- | :---: |
| `parity_py` (**125** حالة) | ✅ `2711c24d8155819b` |
| `parity_reports` (**86** تقريراً) | ✅ `76a841de3961bf1b` |
| `audit` $K_2$ عبر 56 حالة | ✅ صفر تباعد |
| `test_packs` (**11 حزمة**) | ✅ |
| `test_golden_k2` · `test_report_k2` | ✅ |
| `test_golden_k3` · `test_report_k3` | ✅ |

**الأداة:** `Kashaf_v2.html` — **1,096,299 بايت** · إحدى عشرة حزمة.

---

# القسم 5: خارطة الطريق — مكتملة

| القاعدة | الحالة |
| :--- | :---: |
| `R1` كتل الشدة (64) | ✅ |
| `R2` كتالوج المركز | ✅ (`DEC-211/ج₂`) |
| `R3` المداخل المحورية بالوظيفة | ✅ (`DEC-190`) |
| `R4`…`R8` المصفوفات · بوابة الفرز | ✅ |
| `R9` بوابة التطهير (72 بنداً) | ✅ (`DEC-212/ب`) |
| `R10` الوسمان الإجرائيان | ⬜ **غير منفَّذ** |
| `R11` العرض المزدوج `St+H` | ✅ |

## 🟡 `R10` — بند لم يُفتح

`51-MATRIX-06/R10`: «الوسمان الإجرائيان — **كل مخرج** — `GAP-A-01/ج` + `GAP-A-02/ج`».

**غائب عن التقرير الحالي.** لم يُدرج في `83-K2-RPT-COMPARE §8.1` ولا في الفجوات الثلاث — **سقط من الحصر**. قالب `ج-4` يستوعبه بلا سطر جديد.

---

# القسم 6: الحالة

| البند | الحالة |
| :--- | :---: |
| `GAP-RPT-K2-01/02/03` | ✅ **الثلاثة مغلقة** |
| `DEF-K2-04` | ✅ مُصلَح |
| **`R10` الوسمان** | 🟡 **`DEC-219`** — مستجدّ |
| `DEC-216` أسماء مثبَّتة | 🟡 |
| `DEC-218` تحذير `H++` متقادم | 🟡 |
| ثغرة حقل القفل | 🟡 معلَنة |
| `DEBT-K2-LEN-01` | 🟡 |

**الترقيم التالي:** `DEC-219`.

---

> **[نهاية الوثيقة — `99-R11-TIEBREAK` v1.0 · `R11` منفَّذ · لا-حتمية مُصلَحة · `R10` مرصود]**
