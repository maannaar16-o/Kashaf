# 145-K3-ITEMMAP — `DEC-275`: إظهار خريطة بنود $K_3$ في محرّكها — وإغلاق `GAP-ITEM-K3-01` **بالبناء لا بالحجّة**

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**خريطة بنود K3 (`00-HANDOVER §8/ب`)**» — 2026-08-13 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-13 |
| **الأثر** | الدوائر الثلاث **مستوية**: لكلٍّ خريطتُها في محرّكها · نسخةٌ منقولة **أُسقطت** · الخريطة المستجدّة **مقيسة لا معروضة** · **`GAP-ITEM-K3-01` مُغلقة** · البصمات الخمس حرفياً |
| **التغيير المرافق** | `CHG-093` |
| **الحاكم** | `instrument_pin` · `م-2` · `00-HANDOVER §6①` · `DEC-199`/`DEC-200` · `DEC-270` (السابقة) |
| **الكود** | `145` — تمديد بمسوّغه: إغلاق فجوةٍ مجمَّدة بمادّةٍ استجدّت |

---

# §1 الفجوة كما خُتمت — ومسوّغها الذي تآكل

`119 §1/③` سجّلها بنصّها: «`k3_engine` لا يعرّف `ITEM_MAP` ولا `score_from_raw` ⇒ **مراجعة صدق المضمون لـ$K_3$ غير ممكنة** بالطريقة نفسها قبل إظهار الخريطة». وجُمّدت.

**والمسوّغ كان صحيحاً يوم كُتب.** ثم استجدّ أمران:

| المستجدّ | الأثر على المسوّغ |
| :--- | :--- |
| `DEC-250` — الموقع | خريطة $K_3$ صارت **مُفكّكةً من الجدول المختوم `41 §5.3` ومقابَلةً آلياً** عند كل بناء — لكنها في **طبقة البناء**، ونسخةً منقولة |
| `DEC-270` — تسطيح $K_4$ | خريطة $K_4$ أُدخلت **محرّكها** بقيدٍ صريح: **تُقرأ ولا تُعرَّف**، وتُقابَل بالجدول المختوم آلياً. **فصارت السابقة قائمة ومقبولة** |

⇒ فبقيت $K_3$ **وحدها بلا خريطة في محرّكها** بين ثلاث دوائر: $K_2$ لها `K2_ITEM_MAP`، و$K_4$ لها `ITEM_MAP`. **وعدمُ الاستواء هذا لم يكن قراراً — كان أثراً باقياً.**

---

# §2 الحسم — **وحدّ `instrument_pin` هو الفاصل**

السؤال الحاكم ليس «أنُظهر الخريطة؟» بل: **أهذا مسٌّ بطبقة القياس أم إظهارٌ لها؟**

| ما لم يقع | ما وقع |
| :--- | :--- |
| ❌ تعريف بندٍ أو تغيير قطبه | ✅ **نقلٌ حرفي** من الجدول المختوم `41 §5.3` |
| ❌ إضافة معادلة أو وزن | ✅ **مقابلةٌ آلية** بفكٍّ مستقلٍّ للجدول عند كل بناء |
| ❌ تغيير درجةٍ واحدة | ✅ **البصمات الخمس حرفياً** — والبرهان أن السلوك لم يتحرك |

**والخريطة كانت تعمل أصلاً**: `bridge.spK3` يحسب بها منذ `DEC-250`. فما جرى **إظهارُ ما هو نافذ**، لا إدخالُ جديد. ولو كان الإظهار مسّاً لكان `DEC-270` مسّاً — وقد قُبل بشرطه نفسه.

## وما لم يُضَف — **بمسوّغه**

`score_from_raw` **لم يُضَف** إلى `k3_engine` مع أن نصّ الفجوة يذكره. الحساب قائم في `bridge.spK3`، **وتكراره في المحرّك يُنشئ سلطةً ثانية على معادلةٍ واحدة** (`م-2`) — وهو عين الدَين الذي سُدِّد في `DEC-273` (قائمة في ثلاثة مواضع) و`DEC-274` (قاعدة في موضعين). **فالمُظهَر خريطةٌ لا معادلة**، والفجوة تُغلق بما كانت تطلبه فعلاً: **أن تُرى البنود المغذّية لكل مهارة**.

---

# §3 التنفيذ — وإسقاطُ نسخةٍ ثانية

| الطبقة | ما جرى |
| :--- | :--- |
| `k3_engine.py` | `ITEM_MAP` (5 × 11) بأكواد المهارات — **مشتقّةً من فكّ الجدول المختوم** لا من نسخة |
| `engines.js` / وحدة `K3` | التوأم الحرفي، ومُصدَّرٌ كأختيه (`DEC-199`) |
| `build_site.py` | **النسخة المنقولة أُسقطت**: `K3_MAP` تُشتقّ الآن من المحرّك كما تُشتقّ خريطة $K_2$ — فنسخةٌ ثانية في ملفّ البناء كانت **سلطةً ثانية** |
| القيد المزدوج | باقٍ وأقوى: `validate_maps` يقابل **الجدول المختوم بالمحرّك** مباشرةً بدل مقابلته بنسخة |

---

# §4 الحقل يُضاف فاحصاً — **وإلا لم يُضَف**

خريطةٌ تُعرض بلا قياس **حقلٌ لا يفحص**. فأُضيف إلى `parity_surface` مسحُ **ب-٢**: خريطة $K_3$ في بايثون ⇄ JS — **55 بنداً**، كما يُمسح مسحُ $K_2$ (56 بنداً).

## والحرس مُختبَر على فشله — من الطرفين

| الحقنة | الحارس الذي رصد | النتيجة |
| :--- | :--- | :--- |
| بندٌ مبدَّل في **توأم JS** (`ST[0]`: 5 ← 6) | `parity_surface` | ❌ **رُصد** — `ب-٢/ITEM_MAP_K3/ST[0][0]: 5 ≠ 6` |
| بندٌ مبدَّل في **محرّك بايثون** (`EP[0]`: 3 ← 4) | `build_site.validate_maps` | ❌ **رُصد** — «الجدول المفكوك لا يطابق `k3_engine.ITEM_MAP`» |

**وكلٌّ رصد ما يخصّه:** التوأم يقيس التطابق بين النسختين، والقيد المزدوج يقيس الوفاء للجدول المختوم. **ولا واحد منهما يُغني عن الآخر** — فالنسختان قد تتطابقان على خطأٍ واحد.

## 🔎 وعثرةٌ منهجية تُقيَّد

بعد استرجاع الحقنة بقي `__pycache__` حاملاً **الشيفرة المترجَمة للنسخة المحقونة**، فأبلغت البوابة عن عيبٍ في ملفٍّ سليم. أُزيل المخبأ فاستقام الحكم.

> **درسٌ يُقيَّد:** حقنُ عيبٍ ثم استرجاعُه **لا يكتمل بإعادة الملف وحده** — يلزم إبطال ما اشتُقّ منه. وإلا **أُخذ أثرُ الحقنة برهاناً على عيبٍ لا وجود له**.

---

# §5 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| **`GAP-ITEM-K3-01`** | ✅ **مُغلقة بالبناء لا بالحجّة** — الخريطة مُظهَرة في المحرّك، ومراجعة `119` صارت ممكنة على $K_3$ |
| استواء الدوائر | ✅ ثلاثتها بخرائطها في محرّكاتها |
| مصدر الحقيقة | ✅ **نسخةٌ منقولة أُسقطت** — والقيد المزدوج صار بالمحرّك مباشرةً |
| القياس | ✅ **ب-٢ مستجدّ** — 55 بنداً · والحرسان مُختبَران على فشلهما من الطرفين |
| البصمات الخمس | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` · `e32207bbb8853560` · `a0f83d78b8adbf53` — **حرفياً** |
| المتصفح | ✅ `K3.ITEM_MAP` حاضرة — 5 مهارات × 11 · **55 خانة فريدة** · صفر خطأ صفحة |
| الأدوات | **22/22** |

---

# §6 ما لم يُمسّ

- ❌ **طبقة القياس** — لا بند ولا قطب ولا معادلة ولا وزن.
- ❌ **أي درجة أو تصنيف أو نصّ تقرير** — البصمات الخمس برهانه.
- ❌ **`score_from_raw`** — لم يُضَف بمسوّغه (`§2`).
- ❌ **عرف مفاتيح الجسر** — `bridge.spK3` يستقبل الخريطة بالأسماء كما كان؛ والتحويل في طبقة البناء. **تغييرُه لا يزيد صدق المصدر شيئاً** فتُرك.

---

**[نهاية `145-K3-ITEMMAP` — `DEC-275` · الترقيم التالي `DEC-276` · `CHG-093` · `GAP-ITEM-K3-01` مُغلقة]**
