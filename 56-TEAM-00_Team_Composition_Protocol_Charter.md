# 56-TEAM-00_Team_Composition_Protocol_Charter.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `56-TEAM-00` — ميثاق بروتوكول تركيب الفريق |
| **الإصدار** | v1.0 ← **v1.2** — ميثاق معتمد **ومشغَّل** (`DEC-277`/`DEC-278`) · `§6` بختم `DEC-281` · **و`§7` قواعد التوليد بختم `DEC-282`** |
| **حالة الاعتماد** | ✅ **معتمد ومشغَّل** — رُفع التأجيل بأمر المالك (`DEC-277 §3`) والمحرّك يعمل (`DEC-278`) |
| **التصنيف الاستراتيجي** | [خارج النموذج — قابل للتنفيذ] — أداة تشغيلية تُركِّب من مكوّنات معتمدة، صفر مساس بالنظرية |
| **السند** | `51-MATRIX-01` (28 زوجاً) · `51-MATRIX-02` (5 أزواج قطبية) · `51-MATRIX-04` (العمى الجماعي) · `56-REPORT-ENGINE` §5.2/§6 (G1–G4، وضع الفريق) |
| **الوظيفة** | تحليل تشكيلة فريق معطاة: التغطية العدسية، العمى الجماعي المتوقع، أزواج الصدام/التكامل — قبل التشكيل أو أثناء التقييم |

---

# القسم 1: عقد المدخل (Input Contract)

| الحقل | البيان |
| :--- | :--- |
| قائمة الأعضاء | لكل عضو: رمز/اسم مستعار + العدسات المهيمنة والمساندة (رمز `H+/H++/M+` لا الأرقام الخام) |
| الحد الأدنى | عضوان؛ يتحسّن التحليل كمّياً مع 3+ |
| مصدر البيانات | تقارير أفراد معتمدة عبر `56-REPORT-ENGINE` (فردية أو قائد) حصراً — **صفر بيانات مُختلَقة** (البند 10) |
| فحص السلامة | رفض أي عضو بلا تصنيف مكتمل عبر المحرك؛ تقرير فجوة لا ملء |

---

# القسم 2: البنية المعتمدة (9 أقسام)

| # | القسم | يستدعي | الحالة |
| :--- | :--- | :--- | :--- |
| 1 | رأس + قائمة الأعضاء المشفَّرة | — | جاهز |
| 2 | خريطة التغطية العدسية الجماعية | `G1` (`M-04`) | ✅ معتمد |
| 3 | العمى الجماعي المتراكم (تركيبات الهيمنة المشتركة) | `M-04` §2.1 | ✅ معتمد |
| 4 | مفارقة القطبية للفريق (متجانس-أعمى / قطبي-مبصر) | `M-04` §2.2 | ✅ معتمد |
| 5 | مصفوفة الأزواج (صدام/تكامل/هجين) بين كل عضوين متقاطعين | `G2` (`M-01`) | ✅ معتمد |
| 6 | **القطبية بين شخصين** (تعريف معتمد أدناه §3) | `M-02` + قاعدة جديدة | 🆕 مصمَّمة الآن |
| 7 | سلاسل الارتداد المتزامنة تحت ضغط مشترك | `G3` (`M-03`) | ✅ معتمد |
| 8 | توصية التشكيل (العدسة الغائبة المطلوب استقطابها) | مُشتق من 2+3 | ✅ معتمد منطقياً |
| 9 | قفل العرض (إنذار بنيوي لا حكم كفاءة) | `G4` | ✅ معتمد |

---

# القسم 3: تعريف "القطبية بين شخصين" [معتمد بقرار مالك المشروع — الخيار 1]

**التعريف المعتمد:** يتحقق **صدام قطبي بيني** بين عضوين A وB إذا وفقط إذا:
- كان العضو A **مهيمناً** (SP>70%) في بُعد X من الأزواج الخمسة القطبية (`51-MATRIX-02` §1.1: A↔E، R↔St، O↔H، C↔H، S↔E)،
- **و** كان العضو B **مهيمناً** في البُعد **القطبي المقابل** لـX ضمن نفس الزوج.

**مثال توضيحي [لا تشغيل فعلي]:** إن كان العضو "زيد" مهيمناً في `E` (المتفهم) والعضو "سارة" مهيمنة في `A` (التحليلي)، يتحقق صدام قطبي بيني على الزوج `A↔E`؛ يُستدعى نص الصدام والتكامل من `51-MATRIX-01` (`DY-05`) بصياغة **بينية** (بين عضوين) لا **داخلية** (داخل فرد واحد كما في `M-02` الأصلية للبطاقات القطبية الفردية).

## 3.1 الفرق عن القطبية الفردية (تمييز حاكم)
| | القطبية الفردية (`M-02`) | القطبية البينية (هنا) |
| :--- | :--- | :--- |
| المصدر | فرد واحد يحمل قطبين مشتعلين معاً | فردان، كلٌّ يحمل أحد القطبين |
| النص المستدعى | بطاقة `PT-0#` (تعايش داخلي) | صف `DY-##` من `M-01` بصياغة تفاعل بيني (صدام/تكامل بين شخصين) |
| القفل | "ثراء تشغيلي مزدوج العدسة" لفرد واحد | "فرصة تكامل بنيوي" بين عضوين — لا تنافس ولا تفاضل |

## 3.2 لماذا هذا الخيار كافٍ (لا تكرار مع القسم 5)
القسم 5 (مصفوفة الأزواج العامة) يغطي **كل** تقاطع بين عضوين على أي زوج من الـ28. القسم 6 **يُبرِز خصيصاً** الأزواج الخمسة القطبية من بينها بوسم "قطبي" — طبقة تأكيد لا تحليل مستقل موازٍ.

---

# القسم 4: الأقفال والمحظورات (موروثة إلزامياً)

| القفل | المصدر |
| :--- | :--- |
| صفر مقارنة تفاضلية بين الأعضاء ("الأفضل/الأضعف") | PUR (قواميس `55-USER` الثمانية) |
| صفر كشف درجات خام إلا بإذن صريح | محظور وضع القائد (`56-REPORT-ENGINE-APPENDIX-B` §4/2) |
| صفر توليد — كل خلية تستدعي وحدة معتمدة حرفياً | `M-01`/`M-02`/`M-04` مباشرة |
| قفل العرض (G4) | إنذار بنيوي وتوصية تشكيل — لا حكم كفاءة ولا لغة عيادية |
| صفر لمس K1/K3/K4 | أقفال §6 من البروتوكول |

---

# القسم 5: قاعدة التحجيم عند تعدد الأعضاء (n>4) — [مثبَّتة نهائياً]

**[معتمد — مؤكَّد تشغيلياً عبر `56-TEAM-P002` على n=5]:** عدد الأزواج الممكنة = C(n,2)؛ عند n=4 → 6 أزواج، n=5 → 10 أزواج.
- **يُعرض كاملاً دائماً:** الأزواج **القطبية** (§6) — بلا استثناء مهما كان حجم الفريق.
- **يُعرض بالكامل حتى n=4** (≤6 أزواج): كل أزواج مصفوفة §5 العامة.
- **عند n>4:** تُفصَّل بالكامل (فقرة صدام + تكامل + كيان هجين) **الأزواج القطبية فقط**؛ بقية الأزواج تُعرض **مختصرة** (اسم الكيان الهجين + رمز `DY-##` فقط، بلا فقرة سردية).

---

# القسم 6: سجل الفجوات

| كود | البيان | الخطورة | القرار |
| :--- | :--- | :--- | :--- |
| **GAP-TEAM-01** | ✅ **مغلقة** — القاعدة مؤكَّدة تشغيلياً على n=5 (`56-TEAM-P002`)؛ منصوصة نهائياً في §5 أعلاه | مغلقة | — |
| **GAP-TEAM-02** | ✅ **مغلقة بقاعدة دائمة** (`DEC-281`) — **تقاطعُ عضوين على العدسة نفسها لا خليةَ مختومةً له**: `51-MATRIX-01` جدولُ **28 زوجاً متمايزة** (`C(8,2)`) و`§2/5` يقول «بين كل عضوين **متقاطعين**». **والقاعدة: يُعلَن الخلوّ** — تُطبع العدستان في خانة التقاطع (`A–A`) وبقيّةُ الخانات شرطات، **بلا خليةٍ تُخترع وبلا بديلٍ يُنتقى وبلا جملةٍ تُكتب**. كشفها التشغيلُ لا المراجعة (`56-TEAM-P001` مهيمناتُ أعضائه متمايزة) | مغلقة | قاعدةٌ دائمة بختم المالك 2026-08-14 |
| **إحاطة** | التشغيل (تجريبي أو حقيقي) **مؤجَّل بقرار المسار أ** — هذا الميثاق تصميم معتمد لا نموذج مُختبَر | — | ينتظر أمراً لاحقاً بالتشغيل |
| **إحاطة موروثة** | التشغيل الحقيقي يحتاج ≥2 عضوين ببيانات حقيقية (القيد نفسه الذي واجه Pilot الأصلي؛ لدينا حالياً فرد حقيقي واحد فقط) | — | ينتظر بيانات إضافية أو تشغيلاً تركيبياً موسوماً |

---

---

# القسم 7: قواعد التوليد التشغيلية — [مختومة `DEC-282`]

الميثاق يوجب مخرجاً في ثلاثة مواضع **ولا ينصّ على قاعدة توليده**. وقُرئت الثلاث من التشغيل التجريبي المعتمد `56-TEAM-P001` — **مقروءةً من مخرجٍ معتمد لا مخترَعة** — ورُفعت قراءاتٍ في `DEC-278 §4`، **ثم ختمها المالك قواعد** 2026-08-14.

| # | الموضع | **القاعدة المختومة** | دليلُها في `56-TEAM-P001` |
| :--- | :--- | :--- | :--- |
| **①** | **تقاطع الزوج** (`§2/5`) | **يُقدَّم الزوج القطبي** (`51-MATRIX-02 §1.1`) إن وُجد بين مهيمنَي العضوين؛ **وإلا فأعلى مهيمنٍ لكلٍّ** — الترتيب بالـ`SP` تنازلياً وكسرُ التعادل بترتيب `41 §5.2` | صفوفه الثلاثة متسقة معها: زوجان قطبيان وواحدٌ بأعلى المهيمنَين |
| **②** | **التركيبة الموثَّقة** (`§2/3`) | تنطبق حين تكون أبعادُها **مهيمنة في الفريق** **و** يكون **كلُّ** ما تطلبه غائباً فعلاً — **الاحتواء التام لا التقاطع الجزئي** | التقاطع الجزئي يستدعي **ثلاثاً** والتشغيل المعتمد يستدعي **واحدة** |
| **③** | **الارتداد** (`§2/7`) | **تُعرض كل عدسةٍ مهيمنة** لكل عضو — لا واحدةٌ منها | التشغيل يعرض لـ`T-01` عدسته **الثانية** (`C`) لا الأولى (`A`) ⇒ **لا قاعدة اختيارٍ تُستخرج منه**، والعرض الكامل يُغني عن قاعدةٍ تُخترع ويشمل ما عرضه (`م-8`) |

**والقواعد الثلاث مقيسةٌ باسمها** في `test_report_team` (الفحص الثامن) لا محفوظةً ببصمةٍ وحدها — **فالبصمة تُثبت عدم التغيّر لا الصحّة** (`DEC-281 §4`). وحُقنت مخالفةٌ لكلٍّ منها فسقط فحصُها.

**وحدُّ القراءة مُعلَن:** ما قُرئ من `56-TEAM-P001` **مُثبَتٌ على مداه** — ومداه ثلاثة أعضاء بمهيمنات متمايزة. وخارجَه تُطبَّق القاعدة بختمها لا ببرهانه (`DEC-281 §3`).

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| عقد المدخل | ✅ محدَّد |
| البنية (9 أقسام) | ✅ معتمدة — 8 من المكوّنات جاهزة فعلياً، 1 مصمَّم حديثاً (§6) |
| تعريف القطبية البينية | ✅ معتمد (الخيار 1) + تمييز عن الفردية |
| الأقفال الموروثة | ✅ مثبَّتة |
| قاعدة التحجيم | ✅ **مثبَّتة نهائياً** — مؤكَّدة على n=5 |
| سجل الفجوات | ✅ `GAP-TEAM-01` مغلقة · `GAP-TEAM-02` **مغلقة بقاعدة دائمة** (`DEC-281`) |
| قواعد التوليد (§7) | ✅ **مختومة** (`DEC-282`) — ثلاثٌ مقروءةٌ من `56-TEAM-P001` **ومقيسةٌ باسمها** |
| **حالة الميثاق** | ✅ **معتمد بالكامل ومشغَّل — صفر فجوة مفتوحة — والتقاطعُ الخالي يُعلَن ولا يُملأ** |
