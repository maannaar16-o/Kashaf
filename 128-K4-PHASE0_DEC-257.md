# 128-K4-PHASE0 — `DEC-257`: فتح بناء دائرة الإنجاز — المرحلة ٠ (تسوية الأساس)

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «ابدأ» على تصور بناء $K_4$ — تنفيذ المرحلة ٠ ببنودها الأربعة قبل أي بناء |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-12 |
| **الأثر على المحرّكات والحزم والأداة** | **صفر** — تغيير وثائقي صرف · البصمات الثلاث ثابتة · طبقة القياس لم تُمسّ (`instrument_pin`) |
| **التغيير المرافق** | `CHG-074` |
| **المبدأ الحاكم** | `DEC-136` — التكييف المشروط لا النسخ · كل نقل باختباره الثلاثي كتابةً (`77-METHOD-TRANSFER §4`) |

---

# §1 توحيد الرمز — `K4-PER` (قرار المالك 2026-08-12)

## ① نص القرار

> رمز المهارة السابعة «المثابرة وإكمال المهام» يُوحَّد على **`K4-PER`** — رمز الدستور `14-CORE-K4` — في **كل ما يُبنى من الآن** من وثائق $K_4$ وأكوادها.

## ② برهان أن الخلاف رمزي محض

قوبل جدول `14-CORE-K4 §4` (مصفوفة تجميع الدرجات) بجدول `41-MEASURE §5.4` (أوعية $K_4$) صفاً صفاً:

| المقارنة | النتيجة |
| :--- | :--- |
| أسماء المهارات السبع | **متطابقة حرفياً 7/7** |
| مجموعات البنود (11 خانة لكل مهارة) | **متطابقة مجموعةً مجموعةً 7/7** — لا يختلف إلا ترتيب العرض (موضع صفّي `F`/`TI` وذيل صف `OR`) |
| الرموز | متطابقة في ستة — **والفارق الوحيد: السابعة** (`K4-PER` في الدستور مقابل `K4-SP` في وثيقة القياس) |

فالخلاف **تسمية رمز لا بنية** — لا مساس بأي بند أو وعاء أو معادلة.

## ③ قيد التكافؤ الموثَّق — الوثائق المختومة لا تُحرَّر

| الموضع الحامل لـ`K4-SP` | حالته | الحكم |
| :--- | :--- | :--- |
| `41-MEASURE §5.4` (+ هامش `RSK-018` في `§8`) | وثيقة قياس مختومة (`instrument_pin`) | **لا تُحرَّر** |
| هوامش `40-MEASURE` (رأس v5: «77ب نص استراتيجي في K4-SP») | وثيقة الأداة المختومة | **لا تُحرَّر** |
| متن `19-SEED-K4` (أزواج البذور) | متن تاريخي يُحفظ حرفياً (§2 أدناه) | **لا يُحرَّر** |

**القيد النافذ:** كل ذِكر لـ`K4-SP` في المواضع أعلاه **يُقرأ `K4-PER`** — تكافؤ تام: نفس المهارة، نفس الاسم «المثابرة وإكمال المهام»، نفس البنود الأحد عشر (9ب، 32ب، 45ب، 57ب، 68ب، 77ب، 84ب، 88ب، 91ب، 93ب، 94ب).

## ④ الإغلاق

**`DEBT-K4-NAMING-01` يُغلق بشقّيه:** الاسم حُسم بتأكيد المالك (2026-08-11): «المثابرة وإكمال المهام» — و«تحمل الضغوط» يبقى حصرياً اسم `K3-ST` صوناً لجدار `DEC-072` · والرمز حُسم بقرار المالك (2026-08-12): `K4-PER`.

---

# §2 إعادة تسجيل `19-SEED-K4` — بذورٌ بسند مُعاد تأسيسه

## ① الاكتشاف الموثَّق: شبكة السند شبحية بكاملها

فُحص كل ما استشهدت به الوثيقة (بحث شامل في قاعدة المعرفة):

| المُستشهَد به في `19-SEED-K4` | حالته اليوم |
| :--- | :--- |
| `K4-MX-01…04` (الحدود · التآزر · الاحتكاك · التدريب) | **لا وجود لها** — لا تظهر في قاعدة المعرفة إلا داخل `19-SEED-K4` نفسها |
| `K4-LOGIC-01` · `K4-REP-01` · `K4-LOGIC-QA-01` · `K4-API-01` | **لا وجود لها** — كذلك |
| `DEC-052…061` (سلسلة السند المعلنة) | أرقامها في سجل القرارات النافذ **تخص قرارات أخرى بالكامل** (`DEC-054` = ميثاق الفريق `56-TEAM-00` · `DEC-057…061` = قرارات بناء $K_3$) — استشهاد إلى ترقيم جلسة سابقة لم يُرحَّل |

فالوثيقة كانت تختبر منطقاً (`K4-LOGIC-01`) **لم يوجد قط في قاعدة المعرفة**، بسندٍ لم يعد يدل على شيء.

## ② التسوية — على `م-9`: يُصنَّف ويُسند إلى حدثه، لا إغلاق تجميلي

البذور **لا تُحذف** (مادتها التصميمية قيّمة) **ولا تُرمَّم استشهاداتها بأثر رجعي** (ترميم السند القديم تزوير تاريخ). تُعاد تسجيلها:

> **التصنيف النافذ:** «**مقاصد تصميم بسند مُعاد تأسيسه**» — سندها النافذ هو `14-CORE-K4` (المهارات السبع بتعريفاتها وبنودها) + هذا القرار (`DEC-257`). كل ما سواه ساقط.

| يبقى حياً | يسقط |
| :--- | :--- |
| السيناريوهات العشرة **مقاصدَ اختبارٍ** لمنطق تقرير $K_4$ متى بُني (ولم يُبنَ بعد) | سلسلة السند القديمة كلها (`DEC-052…061` بمدلولها الساقط) |
| القاعدة المركزية: «العينة تختبر المنطق ولا تتحول إلى قاعدة معرفية أو معيار أو وزن» | افتراض وجود `K4-MX-01…04` و`K4-LOGIC-01` و`K4-REP-01` |
| روح معيار القبول (لا مستويات جديدة · لا أوزان · لا إعادة توزيع بنود) | «المسار التالي المصرَّح به» القديم (`K4-LOGIC-QA-01`/`K4-API-01`) — مسار $K_4$ الآن هو مراحل التصور `٠…٧` |

## ③ وسمان نافذان على القراءة

- **أزواج البذور** (`K4-TI×K4-PER` · `K4-F×K4-PF` · …) **مادة مرشَّحة** لسجل العلاقات في المراحل القادمة — فرضيات موسومة `[اشتقاق غير محقّق]` (`م-5`)، **لا علاقات مثبتة**؛ إثباتها بدليل بنيوي مكتوب في مرحلته.
- مصطلح «**الصمامات**» في المتن هو مصطلح الدستور نفسه (`14-CORE-K4 §2`: «7 صمامات تنفيذية») — لا مصطلح جديد.

## ④ التنفيذ

كتلة إعادة تسجيل تُدرج في **رأس** `19-SEED-K4` (v1.0 ← v1.1) تنص على التصنيف الجديد وسقوط السند القديم وقيد تكافؤ الرمز — **والمتن يُحفظ تحتها حرفياً** شاهداً تاريخياً. والوثيقة **تُفهرس** في `00-INDEX` (لم تكن مفهرسة).

---

# §3 سجل النقل — مفتوح (`GAP-TRF-02` تُغلق)

`77-METHOD-TRANSFER §7` نصّ: السجل «يُنشأ عند أول نقل فعلي» — **وأول نقل فعلي وقع في هذه المرحلة نفسها**: تسوية `§2` أعلاه طبّقت `م-9` و`م-5`، وقيد التكافؤ في `§1` طبّق `م-2`.

- **أُنشئ** `77-METHOD-TRANSFER-LOG_Transfer_Register.md` (الكود: `77-LOG`) — سجل حي يقيّد كل نقل **قبل استعماله** بنتيجة اختباره الثلاثي كتابةً؛ افتُتح بأربعة قيود (`TRF-001…004`) هي نقول هذه المرحلة.
- **حُجزت أكواد الوثائق سلفاً** (درس `GAP-CPL-02` — لا ترقيم يُرتجل وقت الحاجة): **`129…135`** لوثائق مراحل بناء $K_4$ **`١…٧`** بالترتيب (`129` = ميثاق الطبيعة المزدوجة — المرحلة ١ · … · `135` = المرحلة ٧). الحجز ترقيمي لا مضموني — مضمون كل وثيقة يُختم في مرحلتها.

---

# §4 ديون الأداة داخل $K_4$ — تُثبَت كما هي (الأداة مقفلة)

هذه **حدود تفسير** تلزم كل طبقة تفسير تُبنى لـ$K_4$ — **ولا يُصلَح منها شيء في الأداة** (`instrument_pin` · ن-7/④):

| الدَّين | موضعه في $K_4$ | الحدّ التفسيري النافذ |
| :--- | :--- | :--- |
| `RSK-018(41)` — البند 77ب نص استراتيجي (`K2-St`) يغذّي وعاء المثابرة | `K4-PER` (خانة من 11) | درجة `PER` تحمل مكوّناً استراتيجياً مقبولاً صراحةً (`DEC-037`) — لا يُبنى تمييز على 77ب وحده |
| بقايا `GAP-Q-07`: 9ب ≈ 94ب | **كلاهما `K4-PER`** | زوج شبه-توأم داخل الوعاء نفسه ⇒ التغطية المضمونية أضيق من خاناته الإحدى عشرة |
| بقايا `GAP-Q-07`: 41ب ≈ 45أ | **كلاهما `K4-PF`** | كذلك |
| بقايا `GAP-Q-07`: 14ب ≈ 27أ | **كلاهما `K4-F`** | كذلك |

**اكتشاف مرافق:** بقايا `GAP-Q-07` الثلاث كلها **أزواج داخل أوعية $K_4$** — فهي دَين $K_4$ خالص، وقيدها هنا هو «إثباتها داخل $K_4$» الذي طلبته المرحلة ٠. **القاعدة:** لا يُستدل بعدد البنود على ثراء تغطية الوعاء، ولا يُخترع أي تصحيح أو وزن تعويضي.

**ملاحظة ترميز تُسجَّل ولا تُصلَح:** الرمز `RSK-018` مزدوج الاستخدام في قاعدة المعرفة — في `02-MASTER` خطرُ إرهاق التقييم المفرد، وفي `41-MEASURE §8` الدَّين العاملي 77ب. كلاهما مختوم في موضعه؛ **العرف النافذ: الإحالة المقيَّدة** — `RSK-018(41)` و`RSK-018(02)`.

---

# §5 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| طبيعة التغيير | وثائقي صرف — لا كود، لا حزم، لا أداة |
| الأدوات الستّ عشرة | 16/16 |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **حرفياً** |

---

**[نهاية `128-K4-PHASE0` — `DEC-257` · الترقيم التالي `DEC-258` · `CHG-074`]**
