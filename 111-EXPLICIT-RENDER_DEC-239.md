# 111-EXPLICIT-RENDER — `DEC-239`: `ن-8` وحارس `ح-6`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | ①-أ حافة الإخراج · ②-ب نصّ + حارس · ③-ج توسيع القائمة بمسح |
| **حالة الاعتماد** | **مشتق** — تعديل حَوْكَمي في `00-PROTOCOL`، لا تعديل في النموذج |
| **الأدوات** | **15/15** · البصمات الثلاث ثابتة · **صفر مساس بالمخرجات** |

---

# القسم 1: المسح أولاً — لأن القائمة تُبنى لا تُفترض

اختيارك **③-ج** فرض الترتيب: القائمة قبل النصّ، والنصّ قبل الحارس.

## المسح الديناميكي — بصمات ④…⑨ في المخرجات الفعلية

| السطح | النتيجة |
| :--- | :--- |
| متن التقارير (142) | ✅ **صفر بصمة** |
| كتل التدقيق مُسلسَلة | 242 `true`/`false` · 8 `null` |

**والثاني ليس تباعداً.** `json.dumps` و`JSON.stringify` يتطابقان على `true` و`null`. التباعد في ⑥⑦ يقع عند **الإكراه النصّي** (`str(True)` = `True`) لا عند **التسلسل**.

> تمييزٌ لولاه لسُجِّلت **250 «إصابة» كاذبة** ولانهار المقترح تحت وزنها.

## المسح الساكن — 121 استيفاءً عند مواضع الإصدار

بعد استبعاد بناء المفاتيح والاختبار الذاتي: **48 مرشَّحاً**، كلّها إيجابيات كاذبة عند الفحص:

| المرشَّح | الحكم |
| :--- | :--- |
| أحرف مفردة (`d` · `c` · `t` · `b`) | رموز أبعاد ونصوص — مُتحقَّق تشغيلياً |
| `res.stitch` | نصّ أو `None`، **ومحروس بـ`if`** قبل الاستيفاء |
| `H[key]` · `H3[key]` | قاموس عناوين، قيمه نصوص |
| قاموسان | **التقاط خاطئ لنمطي** — ليسا استيفاءً |

---

# القسم 2: النتيجة عكس المتوقَّع — وهذا حدّد شكل الحارس

**الكود القائم خالٍ من الأصناف ④…⑨.**

⇒ قيمة `ن-8` **وقائية بحتة**. ولو بُني `ح-6` كاشفَ عيوب لكان أداةً تبحث عمّا لا وجود له. فبُني **بوّابة انحدار**: تمنع المستجدّ، لا تعالج قائماً.

---

# القسم 3: عنصر واحد حقيقي — `GAP-STRICT-K3-01`

`k3_engine.py:367`:

```python
raise RuntimeError({"gaps": gaps, "violations": violations,
                    "missing_content": content.missing()})
```

الحمولة **قاموس**. `str(e)` يعطي `{'gaps': [], …}` — تمثيل بايثون. **ولا نظير له في JS إطلاقاً**؛ ولو صيغ هناك لأعطى `[object Object]`.

| الحقل | البيان |
| :--- | :--- |
| **الصنف** | ④ · وسطحٌ **أحادي التنفيذ** — نظير نمط `GAP-ISO-JS-01` |
| **الأثر** | مسار `strict=True` **خامل** (`k3_report.py:83` لا يمرّره) · لا يمسّ تقريراً ولا بصمة |
| **الخطورة** | 🟡 متوسطة |
| **العلاج** | صياغة الحمولة نصّاً وإحياء نظيرها في JS · أو إسقاط المسار — **كلاهما يمسّ محرّكاً محكوماً** |
| **الحالة** | 🔒 **مجمَّد** |

---

# القسم 4: الأصناف التسعة — مقيسة لا مُتخيَّلة

| # | الصنف | بايثون | JS | صادفناه؟ |
| :--- | :--- | :--- | :--- | :---: |
| ① | قائمة → نصّ | `['A', 'R']` | `A,R` | ✅ `DEC-235` |
| ② | صحيح-عشري | `4.0` | `4` | ✅ `DEC-235` |
| ③ | تقريب النصف | `2.67` | `2.68` | ✅ `DEC-238` |
| ④ | **قاموس → نصّ** | `{'a': 1}` | **`[object Object]`** | `GAP-STRICT-K3-01` |
| ⑤ | **ترتيب المفاتيح** | `['10','2','a']` | **`['2','10','a']`** | لا |
| ⑥ | منطقي | `True` | `true` | لا |
| ⑦ | العدم | `None` | `null` | لا |
| ⑧ | أسّي عند الحدود | `1e+16` | `10000000000000000` | لا |
| ⑨ | صفر سالب | `-0.0` | `0` | لا |

**الأخطر ما لم نصادفه:**
- **④** نصٌّ بلا معنى يصل المستفيد.
- **⑤** JS يرتّب المفاتيح **العددية** تصاعدياً وبايثون يحفظ الإدراج — **إعادة ترتيب صامتة لا يخطئ فيها أحدهما**.

**لا يُضاف صنف عاشر إلا بعد إثباته تشغيلياً** — وهذا ما يمنع القائمة من التضخّم إلى شعار.

---

# القسم 5: الحارس `ح-6` — وحدّه مُصرَّح به

`guard_interp.py` يمسح مواضع الإصدار في الطرفين. كل استيفاء إمّا **مصاغ صراحةً** (`_num` · `_round2` · `.join` · `String` …) وإمّا **مسجَّل في `interp_registry.json` بسببٍ مكتوب**. وما عداه يوقف البوّابة.

| البند | العدد |
| :--- | ---: |
| استيفاءات مفحوصة | **62** |
| مصاغة صراحةً | 6 |
| مسجَّلة بسبب مكتوب | **56** |
| يتيمة | **0** |

## استيفاء `ن-7`

| الشرط | البرهان |
| :--- | :--- |
| ① عيب معلوم | سقط على النمطين **التاريخيين** `{LENSES}` و`{round(w, 2)}` — من هذه الجلسة نفسها قبل `DEC-235`/`238` |
| ② قياس واحد | وجود استيفاء غير مصاغ ولا مسجَّل — لا غير |
| ③ تكافؤ النسخ | أداة واحدة تمسح الطرفين ⇒ لا مسألة تكافؤ |
| ④ لا عتبة | أي استيفاء يتيم يوقف |

## الحدّ — أُصرّح به ولا أُخفيه

> `ح-6` **كشفٌ لا برهان.** نوع المتغيّر لا يُحسم ساكناً في اللغتين. تقديمه كبرهان تامّ يكون «حقلاً لا يفحص» — وهو النمط المرفوض في `DEC-236` و`DEC-238`.

---

# القسم 6: ما لا يحلّه `ن-8`

| الصنف | من يحلّه |
| :--- | :--- |
| غياب بنيوي لحقل | `parity_surface` |
| حارس معطَّل يعيد قيمة ثابتة | استثارة بعيب معلوم (`ن-7/①`) |
| اختلاف ترتيب الحساب | `parity_py` |

ثلاث من ستّ فجوات هذه الجلسة كانت من أصناف أخرى. `ن-8` يعالج **الصياغة** وحدها.

---

# القسم 7: اختبار القبول

| | النتيجة |
| :--- | :--- |
| الأدوات | **15/15** |
| البصمات الثلاث | ثابتة |
| مساس بالمخرجات | **صفر** — الكود القائم كان مطابقاً لـ`ن-8` أصلاً |

---

**[نهاية `111-EXPLICIT-RENDER` — `DEC-239` · `ن-8` معتمد · `GAP-STRICT-K3-01` مجمَّدة]**
