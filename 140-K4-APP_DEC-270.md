# 140-K4-APP — `DEC-270`: تسطيح دائرة الإنجاز في الأداة — الحزمة والجسر والتبويب الثالث

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفذ البندين ② و③**» — 2026-08-13 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-13 |
| **الأثر** | **الأداة تُمسّ لأول مرة لـ$K_4$**: `packs.js` يضمّ حزمتها · `bridge.js` يحسب أوعيتها · الأداة تعرض **تبويباً ثالثاً** وسطحاً عابراً مستقلاً · البصمات **الأربع ثابتة حرفياً** · **طبقة القياس لم تُمسّ** (`instrument_pin`) |
| **التغيير المرافق** | `CHG-087` |
| **الحاكم** | `DEC-266` (رفع `DEC-186` عن $K_4$ بشرطه) · `DEC-268` (السطحان) · `DEC-269` (تصويب البناء والحرس) · `DEC-050` · `DEC-199`/`DEC-200` |
| **الكود** | `140` — تمديد بمسوّغه: تنفيذُ بندين مؤجَّلين بأمر سابق |

---

# §1 اكتشافٌ بنيوي سبق البناء — **الدوائر الثلاث تقسّم الأداة تقسيماً تاماً**

قبل وصل أي خانة فُحص العزل (درس `DEC-027`: خانةٌ حُسبت لدائرتين):

| الدائرة | الخانات |
| :--- | :---: |
| $K_2$ | 56 (8 × 7) |
| $K_3$ | 55 (5 × 11) |
| $K_4$ | 77 (7 × 11) |
| **المجموع** | **188** |

**و`94 × 2 = 188`.** فالدوائر الثلاث **تقسّم كل خانات الاستبيان تقسيماً تاماً**: صفر تقاطع بين أي دائرتين، وصفر تكرار داخلياً، **وصفر خانة مهملة**. هذه حقيقةٌ بنيوية لم تكن مثبتة قبل هذه المرحلة — **وصارت مفحوصة آلياً** في `build_site.validate_maps`.

---

# §2 البند ② — ضمّ الحزمة: مصدرُ حقيقةٍ واحد

| البند | التنفيذ |
| :--- | :--- |
| الإعلان | `CONTENT_K4` أُضيفت إلى جدول `build_packs.py` كبقية الحزم الاثنتي عشرة — فصارت **ثلاث عشرة** |
| فحص السلامة | تخضع لـ`verifyPacks` ولبصمة `SHA` كغيرها (`a3d413c11d58061b` · 17,915 بايت) |
| **التشييم** | `K4_PACK` يُشيَّم إلى **`PK.PACKS.CONTENT_K4`** — أي إلى الحزمة المضمومة نفسها، **لا نسخة ثانية** (`م-2`) |
| **الرفض باقٍ نافذاً** | `k4RequirePack` لم يُحذف: لو غابت الحزمة يُرفع «حزمة K4 غير محمَّلة — لا تقرير بلا حزمة». **والحرس أثبت عمله فعلاً**: أول شوط دخان بعد الضمّ أخفق بهذه الرسالة عينها لأن التشييم كان يشير إلى موضع فارغ — فالرفض كشف الوصل الناقص قبل أن يُصدر تقريراً معطوباً |

---

# §3 البند ③ — الوصل: من الإجابات إلى التبويب الثالث

## ① خريطة الأوعية — **تُقرأ ولا تُعرَّف**

| الطبقة | التنفيذ |
| :--- | :--- |
| المحرك التوأم | `ITEM_MAP` (7 × 11) أُضيفت إلى `k4_engine.py` **ووحدة `K4` في `engines.js`** — والتطابق بينهما مفحوص |
| مصدرها | منقولة من **الجدول المختوم** `41 §5.4` (≡ `14-CORE-K4 §4`) · وقيد التكافؤ المختوم مُطبَّق: صفّ `K4-SP` **يُقرأ `PER`** (`128 §1`) |
| **التحقق** | `build_site.validate_maps` **يفكّ الجدول من الوثيقة ويقارنه بخريطة المحرك** — فلا نقل يدوي يُعتمد بلا مقابلة (`instrument_pin`) |
| مصدر الأداة | `EN.K4.ITEM_MAP` من المحرك مباشرة — كما تفعل $K_2$؛ **لا نسخة في التطبيق** |

## ② الجسر

`bridge.spK4(answers, K4_MAP)` — سبعة صمامات · `MAX_RAW = 66` · فحص «11 بنداً» لكل وعاء · وعقد المدخل يوقف عند بندٍ متروك. **ولا دالّة تُمرّر قيمة من دائرة إلى أخرى** (`DEC-205`).

## ③ التوليد والعرض

| الموضع | التنفيذ |
| :--- | :--- |
| `dualreport.generate` | يُولِّد $K_4$ **دائرةً ثالثة مستقلة** — فشلها لا يُسقط سابقتيها ولا العكس (جدار العزل) |
| **التبويب الثالث** | «تقرير الإنجاز (K4)» بجوار التفكير والانفعال · والنصّ الحاكم صار: «التقارير **الثلاثة** مستندات منفصلة… لا يُقابَل بند من أحدها ببند من الآخر» |
| **السطح العابر** | **كتلة مستقلة مطويّة خارج متن أي تقرير**، لا تظهر إلا إن تحقق مشغِّلها من نطاقات $K_4$ وحدها · وبتصديرٍ مستقلٍّ باسمه |
| التصدير | `Markdown` · `JSON` · طباعة — لكل تبويب على حدة، كعرف الأداة |
| **لوحة `DEC-186`** | صارت **$K_1$ وحدها**، وفيها تصريحٌ مكتوب: «دائرة الإنجاز (K4) خرجت من هذه اللوحة إلى تقرير معتمد — `DEC-266`» |
| اكتمال الاستبيان | `RAW_MAPS` تضمّ خانات $K_4$ — فالاكتمال يشمل الدوائر الثلاث (188 خانة) |

---

# §4 التحقق — شوط دخان في متصفح حقيقي

لا يكفي أن يُبنى الملف؛ يلزم أن **يعمل**. فشُغِّلت الأداة المولَّدة في Chromium وأُجري التوليد **داخل المتصفح**:

| الفحص | النتيجة |
| :--- | :--- |
| حزمة $K_4$ ومحركها وجسرها ومركّبها وباني سطحها في `window` | ✅ الخمسة حاضرة · 7 صمامات |
| **تقرير $K_4$ يُبنى فعلاً** | ✅ **8,540 حرفاً · 7 أقسام** |
| **السطح العابر يُبنى مستقلاً** | ✅ **1,294 حرفاً · 6 قيود** |
| **السطح العابر لا يُدمج في المتن** | ✅ فصلٌ محفوظ |
| التبويب والكتلة في نص الأداة | ✅ |
| لوحة `DEC-186` لـ$K_1$ وحدها | ✅ |
| أخطاء الصفحة | ✅ **صفر** |

---

# §5 الحرس — يحرس الوصل بدل التأجيل

حرس `DEC-269` كان يتحقق أن $K_4$ **غير مُسطَّح** (لأن البند ③ كان مؤجَّلاً). **فقُلب ليحرس الوصل نفسه**:

- محرك $K_4$ في الحزمة الموحَّدة · حزمتُه مشيَّمة إلى `packs.js` · **ورسالة الرفض باقية نافذة**.
- أربعة مواضع وصلٍ في الأداة المولَّدة: التبويب · كتلة السطح العابر · `spK4` · `CONTENT_K4`.
- لوحة `DEC-186` لـ$K_1$ وحدها **بتصريحها المكتوب**.
- و`docs/` **مزامنة مع بناء طازج** كما كان.

> **قاعدة مستفادة تُقيَّد:** حين يُنفَّذ بندٌ مؤجَّل، **يُقلب حرسُه لا يُحذف** — فالحرس الذي كان يحرس التأجيل يصير يحرس التنفيذ، ولا تبقى فترةٌ بلا حرس.

---

# §6 ما لم يُمسّ

- ❌ **طبقة القياس** — لا بند ولا معادلة ولا وزن (`instrument_pin`) · الخريطة **قُرئت** من الجدول ولم تُعرَّف.
- ❌ **`DEC-186` على $K_1$** — قائم كما هو، ولوحتها الداخلية باقية.
- ❌ **`DEC-050`** — لا تقابل بندي ولا درجة تُشتق من دائرة لأخرى · والعزل مبرهَن بالتقسيم التام.
- ❌ البصمات الأربع — **ثابتة حرفياً** (المحرك والتقرير لم يتغيّرا؛ التغيير في الوصل).
- ❌ `DEBT-K4-FIELD-01` — الدَين قائم. **وتقييدٌ صُوِّب في `DEC-271`**: وسم `[اشتقاق غير محقّق ميدانياً]` (`TRF-002`) **يحكم وثائق قاعدة المعرفة** التي منها نُقل المحتوى، **ولا يراه قارئ التبويب نصّاً**. والدَين مُعلَن في `open_debts` **وصار مقيساً** بدرجة المشرف ⑩ · وعرضُه للقارئ **مسألةٌ مرفوعة** (`141 §8`).

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| **الجواب على سؤال المالك** | ✅ **تقرير $K_4$ يعمل الآن على الموقع** — مُتحقَّقاً في متصفح حقيقي |
| العزل | ✅ **مبرهَن ومفحوص**: 56 + 55 + 77 = 188 = كل خانات الأداة · صفر تقاطع |
| خريطة الأوعية | ✅ مقابَلة بالجدول المختوم آلياً · والتوأمان متطابقان |
| البصمات الأربع | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` · `7361580541033f31` — **حرفياً** |
| الحزم | **13** (كانت 12) · `CONTENT_K4` بفحص سلامتها |
| الأدوات | **20/20** + شوط دخان في متصفح |

---

**[نهاية `140-K4-APP` — `DEC-270` · الترقيم التالي `DEC-271` · `CHG-087` · البنود ①②③④ كلها منفَّذة]**
