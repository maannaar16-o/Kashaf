# 52-MATRIX-K3-01_Hub_Interaction_Map.md
## خريطة التفاعل المحوري لدائرة الانفعال — `IR` مركزاً

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `52-MATRIX-K3-01` — **v2.0** (تحلّ محل `Sequential_Handoff_Map` v1.0) |
| **حالة الاعتماد** | ✅ **معتمدة** (`DEC-099`) — البنية المرجعية للتفاعل داخل `K3` |
| **يَنسخ** | v1.0 (السلسلة الخطية `EP→IR→BI→CF→ST`) — **مُلغاة بنيوياً** بـ`DEC-095`/`DEC-098` |
| **السند** | `60-K3-TOPOLOGY` v1.0 (`DEC-098`) · `58-K3-VISION` (`DEC-095`) · `59-K3-IMPACT` (`DEC-096`) |
| **الحدّ الحاكم** | طبقة **تفسير** حصراً · صفر أوزان/عتبات · اقتران فئوي لا مقارنة مقدارية (`DEC-089` المخفَّف) |

---

# القسم 1: البنية

`IR` **محور** · `BI`/`CF` **ذراعان** · `EP` **مُضاعِف رباعي** · `ST` **قاعدة حمل**. العلاقات العشر كلها **مباشرة**، في أربع عوائل. لا «قفزات مشتقة» بعد اليوم.

---

# القسم 2: العائلة ① — المحور (Hub): `IR–BI` · `IR–CF`

**الطبيعة:** ثنائية الاتجاه (قاعدة **د-2**): **خدمة** نازلة من المركز، و**عودة شحنة** صاعدة من الذراع.

## 2.1 `IR ↔ BI`

| الحقل | البيان |
| :--- | :--- |
| **الخدمة (`IR→BI`)** | حالة داخلية مضبوطة الشدة والاتجاه تصل البوابة، فيقرر الكبح على مادة مهذَّبة لا خام |
| **العودة (`BI→IR`)** | كبح متكرر لمخرج لم تُبرَّد شحنته ← **احتقان يعود للمركز** طلباً للاحتواء (مثال المالك: «يغلي داخلياً») |
| **ما لا يُنقل** (نقاء) | ❌ `IR` لا تقرر السماح/المنع (وظيفة البوابة) · ❌ `BI` لا تعدّل الحالة الداخلية (وظيفة المركز) |
| **نمط الفشل** | مركز ضعيف ← شحنة خام تُرهق البوابة · ذراع ضعيفة ← تسرّب سلوكي رغم مركز سليم |
| **الأثر عند الاختلال** | **احتواء بكلفة** (د-3): البوابة القوية تمنع الظهور ولا تُنهي الشحنة |

## 2.2 `IR ↔ CF`

| الحقل | البيان |
| :--- | :--- |
| **الخدمة (`IR→CF`)** | شدة مضبوطة تُتيح مراجعة هادئة وتمنع «الاسترسال مع الحالة الانفعالية» (نص التعريف المعتمد) |
| **العودة (`CF→IR`)** | فشل التكيّف أمام مستجدّ ← **توليد سياق داخلي** (ضيق/توتر) يعود للمركز (قاعدة **د-1**، مثال المالك الثاني) |
| **ما لا يُنقل** | ❌ `IR` لا تُنتج البديل السلوكي · ❌ `CF` لا تخفض الشدة مباشرة (تعدّل النمط لا الشحنة) |
| **نمط الفشل** | مركز ضعيف ← مراجعة مشوَّشة بالانفعال · ذراع ضعيفة ← جمود يولّد شحنة متجددة |
| **الأثر عند الاختلال** | مركز قوي **يحتوي** شحنة الجمود ولا **يُنجز** التكيّف (د-3) |

---

# القسم 3: العائلة ② — التضخيم (Amplify): `EP–{IR, BI, CF, ST}`

**الطبيعة:** `EP` **مُضاعِف إدراكي** يرفع دقة مُدخَل كل عنصر (لا يؤدي وظيفته). أحادية الاتجاه: `EP→X` (لا عودة شحنة موثّقة).

| الزوج | ما تضيفه `EP` القوية | أثر `EP` الضعيفة |
| :--- | :--- | :--- |
| `EP→IR` | فهم دقيق للموقف ← **خفض مباشر لحدة الشحنة** (نص التعريف المعتمد) + مادة صادقة للضبط | ضبط على تمثيل مشوّه («تنظيم أعمى») |
| `EP→BI` | تقدير أصوب لما يستحق المنع (كبح انتقائي لا شامل) | بوابة تقرر على قراءة خاطئة للموقف |
| `EP→CF` | تشخيص أدق لما تغيّر فعلاً ← مراجعة في محلّها | تكيّف على تشخيص خاطئ، أو عمى عن الحاجة للتغيير |
| `EP→ST` | **رصد مبكر لتراكم الحمل** (`ST §1.2/م1`) ← إنذار في وقته | عمى عن التراكم ← انهيار بلا مقدمات |

**🔑 خاصية حاكمة:** `EP` **طبقة ثقة عامة** — ضعفها لا يعطّل عنصراً بعينه، بل **يُسقط موثوقية قراءة الملف كله**. *(وتتعدّى أثرها إلى أدوات `K2` — `VR-7`، خارج نطاق هذه المصفوفة.)*

---

# القسم 4: العائلة ③ — التحميل (Load): `{IR, BI, CF}–ST`

**الطبيعة:** بقايا عمل كل عنصر تنزل **حملاً تراكمياً** على `ST`. الاتجاه: نازل — و`ST` **مُكامل زمني** لا مستقبِل حدث.

| الزوج | نوع الحمل النازل |
| :--- | :--- |
| `IR→ST` | شحنة مرتفعة **مزمنة** (ضبط ناقص) ← استنزاف مستمر لسعة الاحتمال |
| `BI→ST` | كبح متراكم **بلا تسليم** للمراجعة ← احتقان صامت |
| `CF→ST` | نمط جامد مستمر ← تحمّل **«حِمل موضوعي + خطأ مستمر»** (استنزاف مضاعف) |

**🔑 `ST` المصبّ الأعظم:** يستقبل من **ثلاث** قنوات مباشرة (+ تضخيم `EP`). لذا **تظهر الأعراض فيه غالباً بينما الجذور موزّعة** — أساس مبدأ «الجذر ≠ موضع الظهور» (`M3-03`).

---

# القسم 5: العائلة ④ — الجانبي (Lateral): `BI–CF`

| الحقل | البيان |
| :--- | :--- |
| **الطبيعة** | تكامل الذراعين: ما تحظره البوابة يُسلَّم لذراع المراجعة لإيجاد بديل |
| **ما لا يُنقل** | ❌ `BI` لا تولّد البديل · ❌ `CF` لا تمنع المخرج |
| **قاعدة `DEC-072`** ✅ | **باقية نصاً** — «السكون بعد الحظر»: نيّة نشأت ولم تُنفَّذ→`K4` · لا نيّة نشأت→`CF` · تجمّد استنزاف→`ST` |
| **نمط الفشل** | حظر بلا تسليم ← الاحتقان يقفز إلى `ST` (العائلة ③) |

**🔀 تفرّع `BI` (باقٍ):** فرع **السماح** يغادر `K3` نحو السلوك/`K4` — `BI` تبقى **بوابة الدائرة السلوكية الوحيدة**.

---

# القسم 6: قواعد الدوران الخمس — تطبيقها على هذه الخريطة

| القاعدة | تجسيدها هنا |
| :--- | :--- |
| **د-1** ضعف عنصر × مستجدّ ← سياق داخلي | عودة `CF→IR` (§2.2) |
| **د-2** الحافات المحورية ثنائية | العائلة ① بشقّيها (خدمة/عودة) |
| **د-3** احتواء الشحنة ≠ تعويض الوظيفة | §2.1 و§2.2 — الكلفة مدفوعة، الوظيفة معطَّلة |
| **د-4** عناصر أي دائرة مولِّدة | مُدخَلات عابرة تدخل الدورة من `K2`/`K4` *(تفصيلها في الطبقة العابرة)* |
| **د-5** الفجوة العلائقية مولِّدة | سياق يولَّد من تفاعل معادلتين *(خارج التقرير الفردي — تنبيه مسجَّل)* |

---

# القسم 7: ما يصمد من v1.0 (بلا هدم)

✅ تفرّع `BI` · ✅ قاعدة `DEC-072` · ✅ `ST` مُكاملاً زمنياً وخط دفاع أخير · ✅ حقل «ما لا يُنقل» (النقاء الوظيفي) · ✅ مبدأ «الجذر ≠ موضع الظهور» · ✅ `M3-07` (غير متأثرة).
🔄 **يُنسخ:** التسلسل الخطي `H1…H4` · تصنيف «المسارات المشتقة الست» (صارت حافات مباشرة في عوائل).

---

# القسم 8: الفجوات

| الكود | البيان | الحالة |
| :--- | :--- | :--- |
| `DEBT-K3-NORM-01` | لا تقنين ← **الحكم المقارن** بين عنصرين محظور (الاقتران الفئوي مسموح) | 🔴 مفتوح |
| `GAP-M3-01-01` | العودة `BI→IR`/`CF→IR` مشتقة **منطقياً** من أمثلة المالك؛ غير محقّقة ميدانياً | 🟡 مفتوحة — تُوصف لا تُقاس |
| `DEBT-K3-UPREG-01` | «اتجاه» `IR` (الرفع) معرَّف نظرياً **غير مقيس** | 🔴 مفتوح |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| البنية | ✅ محور/ذراعان/مُضاعِف/قاعدة حمل |
| العلاقات العشر | ✅ مباشرة في 4 عوائل (2+4+3+1) |
| ثنائية الاتجاه | ✅ في العائلة المحورية فقط (بسند أمثلة المالك) |
| حقول النقاء | ✅ «ما لا يُنقل» في كل حافة محورية/جانبية |
| **حالة الوثيقة** | ✅ **v2.0 — معتمدة (`DEC-099`)** |
