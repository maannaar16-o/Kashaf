# 133-K4-CROSSING — `DEC-263`: الطبقة العابرة للدوائر — سجل العبور وسطحه وتسوية بقية `RSK-005`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفذ المرحلة ٥**» — 2026-08-12 · وهي الشق الثاني من التوجيه المؤسِّس: «**وكذلك العلاقات التفاعلية بين المهارات وباقي الدوائر**» |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-12 |
| **الأثر على المحرّكات والحزم والأداة** | **صفر** — وثيقة تفسيرية · البصمات الثلاث ثابتة · لا قياس، لا وزن، لا عتبة · **ولا جدار عزل يُفتح** (`parity_isolation.py` يحرس ما يحرسه بلا تغيير) |
| **التغيير المرافق** | `CHG-080` |
| **الحاكم** | `129-K4-DUAL-CHARTER §5` (`DEC-258`): الطبقة **مؤسَّسة مبدأً** بجدرانها المختومة ومدخلها المقيَّد — و`DEC-050` (تجميد التقابل البندي) · `DEC-072` (قواعد الحدود التشخيصية) · `DEC-095`/`VR-7` · `DEC-135` (رفع بقية `RSK-005`) · `DEC-136` (`TRF-015…018`) |
| **الكود المحجوز** | هذه الوثيقة تستوفي حجز `133` — المرحلة ٥ (`77-LOG §2`) |

---

# §0 المنهج ونطاق المرحلة

- **الطبقة تُبنى ولا تُخترع:** كل واجهة عبور في هذا السجل **قائمة مختومة سلفاً** في وثيقتها؛ الجديد الذي تنتجه هذه المرحلة هو **الترسية على جانب $K_4$** — أيّ صمام تقع عليه الواجهة، وأيّ بطاقة توثّق مظهرها — وهو بعينه ما اشترطه `DEC-135`: «يقتضي عملاً على $K_4$»، ولم يكن ممكناً قبل بناء المراحل `٠…٤`.
- **السجل فهرس لا سلطة** (`TRF-016`/`م-2`): كل قيد يسمّي **موطنه المختوم**، والسلطة تبقى هناك. **لا قاعدة عبور جديدة تُنشأ هنا، ولا واحدة تُعدَّل.**
- **الجدار جدارٌ قبل أن يكون واجهة** (`TRF-017`/`م-1`): وظيفة الجدار **الإسنادية** (أيّ دائرة يُنسب إليها العطل) تسبق وظيفته العبورية وتبقى قائمة كما هي — نص مانع للازدواج في كل قيد يمسّ جداراً.
- **كل اشتقاق موسوم** `[اشتقاق غير محقّق ميدانياً]` (`TRF-002`) — سارٍ على الوثيقة كلها.
- **سقف «ما لا يُدَّعى» العام** (`129 §4`) سارٍ فوق الحقل الخاص بكل قيد، **وفوقه في هذه الطبقة تحديداً**: ❌ لا تقابل بندي عبر الدوائر (`DEC-050`) · ❌ لا استدلال درجةِ دائرةٍ على درجة أخرى · ❌ لا مفردة عيادية (`14-CORE-K4 §5`) · ❌ `DEC-244` و`DEC-246`.
- **شرط الاستدعاء فارغ** في كل قيد — هذه المرحلة **تفهرس وتقرر السطح، ولا تشغّل قراءة**.

---

# §1 هوية الطبقة — ثلاثة تمييزات تحفظها من الانهيار

| تُميَّز عن | الفارق |
| :--- | :--- |
| **سجل العلاقات الداخلية** (`130`) | ذاك **داخل $K_4$** بين صمامَيه بمنطق مردود التنفيذ؛ وهذه **بين $K_4$ ودائرة أخرى** بمنطق **الإسناد والتدفق**. لا نوع من الرباعية+تفاقم يُستعمل هنا، ولا قيدُ عبورٍ يُقرأ علاقةً داخلية |
| **جدران العزل** | الجدار يجيب: «لمن يُنسب هذا العطل؟» — والقيد العابر **يفهرس** ذلك الجدار واجهةً ويرسّيه على صمام. الجدار لا يُستهلك ولا يُعاد تشريعه (`TRF-017`) |
| **التقابل البندي المجمَّد** (`DEC-050`) | العبور **دائرة-لدائرة وظيفي**؛ والتقابل البندي **محظور اختراعه** — والسهم في هذه الطبقة **اتجاه تدفق لا جسر تطابق** (`00-VERTICAL-MAP §5`) |

**والاستجابة المعمارية مستوفاة:** `VR-7/③` اشترط أن «يُصمَّم $K_4$ بخاصية العبور من البداية» — استوفاه الميثاق تأسيساً (`129 §5`)، وتستوفيه هذه المرحلة **بناءً**: طبقةً مستقلة **لا حقناً في العلاقات الداخلية**.

---

# §2 سجل العبور — تسع واجهات (`K4-XR-01…09`)

> **قيد ترميز:** البادئة `K4-XR` **مقصودة تفادياً لاصطدام رمزي**: الرمزان `X-01`/`X-02` مشغولان في قاعدة المعرفة (مميّز متشابهات $K_3$ — `52-MATRIX-K3-05`)، ودرس `RSK-006` يحظر ازدواج الرموز.
> **قالب القيد:** الواجهة · النص الحاكم بموضعه · مرساة الطرف المقابل · **مرساة $K_4$** (الجديد) · ما لا يُدَّعى · شرط الاستدعاء (فارغ).

## أ. واجهة التدفق

### `K4-XR-01` — التدفق الوارد: $K_2$ → (عبر $K_3$) → $K_4$

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `14-CORE-K4 §1` نصاً: «تستقبل **"خريطة الفهم" والقرارات المعرفية المصقولة** من دائرة التفكير ($K_2$)، **والمستقرة عبر دائرة الانفعال** ($K_3$)، لتقوم بترجمتها وتحويلها إلى فعل حركي فيزيائي مادي» |
| **مرساة الطرف المقابل** | `00-VERTICAL-MAP §2` (التدفق التتابعي — إطار معتمد، الانتقالات `[مشتق]`) · `12-CORE` س13 |
| **مرساة $K_4$** | **رأس المسار**: الواردُ يدخل حملاً حياً عند `WM` — `50-DEPTH-K4-WM §1.2` («ما يستقبله») — **[مشتق]** |
| **ما لا يُدَّعى** | ❌ **لا خط من عدسة $K_2$ بعينها إلى صمام $K_4$ بعينه** (`DEC-050` — «`K2-O` يقابل `K4-OR`» مثالٌ محظور نصاً) · ❌ السهم اتجاه تدفق لا جسر تطابق · ❌ لا يُقرأ من نطاق دائرة نطاقُ أخرى |
| **شرط الاستدعاء** | — |

## ب. واجهات الإسناد مع $K_1$

### `K4-XR-02` — $K_1$ ⚔️ `K4-TI`: المحرك الفطري مقابل تحويل النية

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `14-CORE-K4 §3/ب` نصاً: «المفتاح ($K_1$) يولد الدافع ويخلق "النية" للانطلاق، بينما الإنجاز ($K_4$) هو النظام الذي "يحول هذه النية إلى فعل مادي". **انعدام المبادرة لا يعني دائماً ضعف مهارة $K_4$، فقد يكون غياباً للمحرك الفطري من الأساس في $K_1$**» |
| **مرساة الطرف المقابل** | `12-CORE` (المفاتيح الثمانية — توليد الدافع) |
| **مرساة $K_4$** | `50-DEPTH-K4-TI §0.1` (الجدار الحاكم للبطاقة) و`§1.3` («تمييز إسنادي واجب قبل القراءة») |
| **ما لا يُدَّعى** | ❌ انعدام البدء **لا يُنسب لـ`TI` تلقائياً** · ❌ ولا يُعكس: قوة `TI` لا يُستدل منها على قوة $K_1$ · **نص مانع للازدواج:** الجدار يؤدي إسناده، والقيد يفهرسه — لا إسناد مزدوج |
| **شرط الاستدعاء** | — |

## ج. واجهات الإسناد مع $K_3$ (تلتزم `DEC-050` حرفياً — قواعد تشخيصية لا أوزان)

### `K4-XR-03` — المبدأ العام: الحالة ($K_3$) ⚔️ السلوك ($K_4$)

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `14-CORE-K4 §3/ب` نصاً: «الانفعال ($K_3$) تُدير "الحالة والشدة"... بينما الإنجاز ($K_4$) تُدير "السلوك الميداني"» · و`13-CORE-K3 §5` (قفل الحالة ≠ السلوك) · **والسؤال الجذري** المختوم في `DEC-072 §1`: «هل رافق التوقف/الانحراف مؤشر داخلي محسوس... أم كانت الحالة الداخلية هادئة/محايدة بينما تعطّل التنفيذ الخارجي فقط؟» |
| **مرساة الطرف المقابل** | `00-VERTICAL-MAP-APPENDIX §1` (`DEC-072`) — معتمد وقابل للتعديل بدليل مناقض |
| **مرساة $K_4$** | البطاقات السبع تحمل السقف في `§0.1` كلٌّ منها؛ والصمامان الأعلى تماساً: `50-DEPTH-K4-PER §0.1` و`50-DEPTH-K4-F §0.1` |
| **ما لا يُدَّعى** | ❌ **لا تقابل بندي $K_3$/$K_4$** (`DEC-050` ملتزَم حرفياً) · ❌ السؤال الجذري **تمييزُ إسنادٍ لا أداةُ قياس** — لا درجة تُشتق منه |
| **شرط الاستدعاء** | — |

### `K4-XR-04` — `K3-ST` ⚔️ `K4-PER`: قلق واعٍ مقابل هدوء وملل — **وقاعدة الحالة غير القابلة للإسناد**

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `14-CORE-K4 §3/ب` نصاً: «التوقف عن العمل بسبب (قلق أو إنهاك نفسي) هو خلل في الانفعال. التوقف عن العمل بسبب (ملل أو تعب سلوكي) **رغم الهدوء النفسي** هو خلل في الإنجاز» · وسؤال الفصل في `52-MATRIX-K3-05 X-02`: «هل كان هادئاً وقت التوقف أم مضغوطاً داخلياً؟» · و`DEC-072 §2.5` (تثبيت المعيار + **إبقاء الحدّية مفتوحة صراحة**) |
| **مرساة الطرف المقابل** | `52-MATRIX-K3-05 X-02` · `50-DEPTH-K3-ST` |
| **مرساة $K_4$** | `50-DEPTH-K4-PER §0.1` (الجدار الحاكم) و`§1.3` (شرط «رغم الهدوء النفسي» في مظهر الانسداد) — **وحدُّ الاستنزاف داخل تعريف `PER` نفسه**: «دون استنزاف النظام» (`14-CORE-K4 §2/7`) |
| **قاعدة الحالة غير القابلة للإسناد** 🆕 | حيث لا يقوم المؤشر الفاصل (الإنهاك الصامت — `GAP-X-EXH-01` · §4): **لا إسناد لأيٍّ من الدائرتين — تُعرض القراءتان ويُسأل** (`م-4` · `TRF-015`). هذه **قاعدة عرضٍ لا مؤشرٌ مخترع**؛ وهي ما تحوّل الحالة الحدّية من صمتٍ إلى مخرجٍ منضبط |
| **ما لا يُدَّعى** | ❌ **لا يُصنَّف الإنهاك الصامت قسراً** لأيٍّ من الدائرتين (`52-MATRIX-K3-05 X-02` نصاً: «لا يُصنَّف قسراً») · ❌ وجودُ مسار استنزاف داخل $K_4$ (§4) **لا يُثبت** أن حالةً بعينها سلكته |
| **شرط الاستدعاء** | — |

### `K4-XR-05` — `K3-IR` ⚔️ `K4-TI`: الاستثارة الناجحة ليست بدءاً

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `DEC-072 §2.2` نصاً: «"الاستثارة الداخلية الناجحة" تُقاس بالحالة الشعورية المرصودة (طاقة، تأهب)، لا بحدوث الفعل التالي. **لا يُستنتج ضعف `IR` من مجرد عدم البدء**» |
| **مرساة الطرف المقابل** | `50-DEPTH-K3-IR §1.5` (بصمة التنظيم — الاستثارة) |
| **مرساة $K_4$** | `50-DEPTH-K4-TI §1.1`/`§1.3` — سؤال التشغيل «هل أبدأ الآن؟» ومظهر «لم يبدأ أصلاً» يُفحصان **منفصلَين** عن حالة الشحن الداخلي |
| **ما لا يُدَّعى** | ❌ لا يُستنتج ضعف `IR` من عدم البدء · ❌ ولا العكس: البدء الحاصل لا يُقرأ دليلَ سلامة `IR` |
| **شرط الاستدعاء** | — |

### `K4-XR-06` — `K3-BI` ⚔️ `K4-TI`/`K4-PF`: السكون بعد الحظر الناجح

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | جدول `DEC-072 §2.4` (ومرآته `52-MATRIX-K3-05 X-03`): «نشأت نيّة/بديل داخلي واضح، لكنه لم يُنفَّذ ⇒ **`K4` (`TI`/`PF`)** — مهمة `BI` انتهت بنجاح؛ التقصير في التنفيذ لا في الحظر» · ولا نيّة ولا بديل ⇒ `K3-CF` · وتجمّد/إرهاق ⇒ `K3-ST` |
| **مرساة الطرف المقابل** | `50-DEPTH-K3-BI` — وسؤالها ينتهي عند لحظة القرار («هل أسمح؟») بلا امتداد نحو $K_4$ |
| **مرساة $K_4$** | `50-DEPTH-K4-TI §1.3` (لم يبدأ رغم قيام النية) · `50-DEPTH-K4-PF §1.3` (الانشغال بثانوي بدل المختار) |
| **ما لا يُدَّعى** | ❌ **لا توسيع لتعريف `BI`** (نص `DEC-072 §2.4`) · ❌ الإسناد لـ$K_4$ **مشروط** بقيام نيّة/بديل واضح — وبدونه يذهب لفرعَي $K_3$ في الجدول نفسه، لا لـ$K_4$ |
| **شرط الاستدعاء** | — |

### `K4-XR-07` — `K3-EP` و`K3-CF` مقابل $K_4$: **كفاية الجدار العام** (قيد توثيق كفاية — لا قاعدة)

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `DEC-072 §2.1`: `EP` × $K_4$ «تماس شبه معدوم (مُدخل إدراكي بحت) — الجدار العام كافٍ. **لا فجوة**» · `DEC-072 §2.3`: `CF` × $K_4$ «تماس خفيف — لا قاعدة إضافية مطلوبة» |
| **مرساة الطرف المقابل** | `50-DEPTH-K3-EP §1.6` («مقابل $K_4$: تصف حالة إدراكية داخلية، لا سلوكاً حركياً ولا نتيجة ميدانية») · `50-DEPTH-K3-CF §1.6` |
| **مرساة $K_4$** | لا صمام بعينه — **والقيد يوثّق كفايةً لا فراغاً**: بناء $K_4$ لم يكشف تماساً جديداً يستدعي قاعدة، فتغطية `DEC-072` تبقى **5/5** |
| **ما لا يُدَّعى** | ❌ «لا قاعدة» ليست «لا حدّ» — الجدار العام (`K4-XR-03`) نافذ عليهما · ❌ ولا يُقرأ القيد إذناً بتماسٍ لم يُوثَّق |
| **شرط الاستدعاء** | — |

## د. واجهة الإسناد مع $K_2$

### `K4-XR-08` — $K_2$-`O/R` ⚔️ $K_4$: الجدولة الذهنية مقابل اللوجستيات المادية

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `41 §6/③` نصاً (قفل عزل التخطيط عن الحركة اللوجستية): «يُفصل حدّياً بين **جودة صياغة المسار الحرج وبناء الجداول والخطط والترتيب على الورق** كعملية معرفية بحتة ($K_2$)، وبين **المباشرة الميدانية الخشنة والتعامل الفيزيائي مع الأدوات والوقت والمقاومة اللوجستية** على الأرض ($K_4$)» · و`14-CORE-K4 §3/ج`: «$K_4$ **لا تُخطط بل تنفذ**... إذا كانت الخطة واضحة منطقياً ($K_2$ يعمل) ولكن التطبيق اللوجستي على الأرض فوضوي أو لم يبدأ، فالخلل... في النظام التنفيذي الميداني» |
| **مرساة الطرف المقابل** | `50-DEPTH-K2-O` · `50-DEPTH-K2-R` (عمليتان معرفيتان) |
| **مرساة $K_4$** | `50-DEPTH-K4-OR §0.1` (الجدار **الحاكم** لبطاقتها) و`§1.3` (تمييز إسنادي: الفوضى ميدانية لا إدراكية) · وتخصيصان مجاوران: `50-DEPTH-K4-TM §0.1` (مقابل $K_2$-`R`) · `50-DEPTH-K4-PF §0.1` (مقابل $K_2$) |
| **ما لا يُدَّعى** | ❌ لا يُقرأ من وضوح الخطة الذهنية حكمٌ على التنفيذ **ولا العكس** · ❌ ولا تقابل بندي (`DEC-050`: «`K2-O` يقابل `K4-OR`» محظور نصاً — والجدار **يفصلهما** ولا يزاوجهما) |
| **شرط الاستدعاء** | — |

## هـ. واجهة الانعكاس — مبدأ مختوم بلا تشغيل

### `K4-XR-09` — `VR-7`: انعكاس $K_3$/$K_4$ على جودة تعبير أدوات $K_2$

| الحقل | البيان |
| :--- | :--- |
| **النص الحاكم** | `58-K3-VISION VR-7` (`DEC-095`) نصاً: «$K_3$ و$K_4$ **طبقتا تنفيذ** تعالجان الفجوة بين التفكير ($K_2$) والتصرّف؛ مهاراتهما **تنعكس على كل أدوات $K_2$** (تعزيزاً/إضعافاً لجودة تعبيرها)» — والرسم في `§3.2` يضع سهم الانعكاس `[مشتق]` |
| **مرساة الطرف المقابل** | `58-K3-VISION §3.2` · `10-CORE` (`P = C + G` — `VR-7` تفصيلٌ لآليتها لا نقضٌ لها) |
| **مرساة $K_4$** | **لا ترسية تشغيلية** — البناء استوفى شقّ «التصميم بخاصية العبور» (`129 §5` · `VR-7/③`)؛ أما شقّ **الانعكاس** فيبقى **مبدأً مختوماً بلا صياغة تشغيلية** |
| **ما لا يُدَّعى** | ❌ **لا يُقرأ الانعكاس درجةً على درجة** — «تعديل جودة التعبير» ليس إذناً باشتقاق نطاق $K_2$ من نطاق $K_4$ (يهدم `DEC-050` وسقف `129 §4`) · ❌ ولا يُبنى عليه نص في تقرير أي دائرة قبل قرار وموادّ |
| **شرط الاستدعاء** | — · **تشغيله قرارٌ مستقل** — لا يُفتح ارتجالاً |

---

# §3 سطح القراءة العابرة — القرار المختوم

`129 §5` نصّ أن «تقارير الدوائر منفصلة، **والقراءة العابرة سطحها الخاص بقرارها الخاص**». يُتَّخذ القرار الآن:

| البند | الحكم |
| :--- | :--- |
| **① سطح مستقل** | القراءة العابرة **لا تُطبع داخل تقرير $K_2$ أو $K_3$ أو $K_4$ بأي صورة** — سطحها مستقل، وجدران عرض العزل باقية على حالها |
| **② لا يُبنى الآن** | السطح **مقرَّر لا مبنيّ**: بناؤه (وأي لغة قارئ له) مرحلةٌ وقرارٌ لاحقان — وهذه المرحلة تفهرس وتقرر فقط |
| **③ حمولته حين يُبنى** | قيود هذا السجل **بحقول «ما لا يُدَّعى» ملازمةً**، وقاعدة الحالة غير القابلة للإسناد (`K4-XR-04`) — **ولا شيء سواها**: لا درجة عابرة، ولا تقابل بندي، ولا رقم يُشتق من دائرة لأخرى |
| **④ ليس باباً خلفياً** | لا يجوز أن يُملأ به **فراغٌ داخلي** في أي دائرة: فجوات `130 §3` تبقى فجوات، وفراغ `132 §4/④` يبقى فراغاً — العبور لا يُنتج علاقة داخلية ولا نمطاً |
| **⑤ الأداة مقفلة** | لا أثر على الكود أو الحزم: حارس العزل (`parity_isolation.py`) يحرس ما يحرسه بلا تغيير، والبصمات الثلاث ثابتة |

---

# §4 بقية `RSK-005` — التسوية بالتصنيف (`TRF-018`/`م-9`)

## ① الشرط المرفوع وما استُوفي منه

`DEC-135` رفع البقية بنداً مستقلاً بنصها: «**الإنهاك الصامت بلا قلق واعٍ — `X-02` لا يحسمه**؛ يقتضي **عملاً على $K_4$**». **العمل على $K_4$ أُنجز** (المراحل `٠…٤`)، وحصيلته على هذا البند بالتحديد:

| ما كان | ما صار بعد بناء $K_4$ |
| :--- | :--- |
| «لا مؤشر باطني كافٍ في خط الأساس» — والجانبان صامتان (`DEC-072 §4`) | **جانب $K_4$ لم يعد صامتاً**: ① لفظ الاستنزاف **داخل تعريف `PER` المختوم** («دون استنزاف النظام» — `14-CORE-K4 §2/7`)، فالحالة ليست غريبة عن مفردات $K_4$ · ② و**ثلاثة مسارات بنيوية موثقة يُقترب بها من حدّ الاستنزاف بلا أي حالة $K_3$**: `K4-REL-03` (سندٌ طويل فوق احتكاك دائم — دليله نصاً «يقترب من حدّ الاستنزاف الذي يحذّر منه التعريف نفسه») · `K4-REL-08` (المثابرة تُصرف في سباقات إنقاذ) · `K4-PAT-01` (الاحتكاك الثلاثي) |
| الحالة الحدّية بلا مخرج منضبط | لها **قاعدة**: تُعرض القراءتان ويُسأل (`K4-XR-04` · `م-4`) — لا إسناد قسري ولا صمت |

## ② ما لم يُستوفَ — بصراحة، بلا إغلاق تجميلي

**وجودُ مسارٍ لا يُثبت سلوكَه.** إثبات أن حالةً بعينها سلكت مسار الاستنزاف $K_4$ يقتضي إما **قراءة تغيّرٍ عبر الزمن** — محظورة (`DEC-244`) — أو **تحققاً ميدانياً** لا يملكه النموذج (`TRF-002`). والمؤشر **الفاصل** (ما يميّز الإنهاك الصامت عن الهدوء) **مفقود على جانب $K_3$**، واستيفاؤه يحتاج **محتوى جديداً في `13-CORE-K3` نفسها** — وهو ما حجزه `DEC-072 §4` **لقرار المالك** صراحةً: «لا اجتهاداً هنا».

## ③ التسوية النافذة

| الحقل | البيان |
| :--- | :--- |
| **الكود** | **`GAP-X-EXH-01`** — الإنهاك الصامت بلا قلق واعٍ |
| **الحالة** | 🟡 **مفتوحة — مضيَّقة ومسمّاة الجانب** (لا مغلقة) |
| **ما استُوفي** | شرط `DEC-135` («عمل على $K_4$») — **مستوفى**: جانب $K_4$ موثَّق بمسارات وحدٍّ داخل التعريف · والحالة الحدّية صار لها مخرج منضبط |
| **الناقص وجانبه** | **مؤشر فاصل على جانب $K_3$** — لا على جانب $K_4$ |
| **مطلوبه** | محتوى جديد في `13-CORE-K3` — **قرار مالك** (`DEC-072 §4`) |
| **الحكم النافذ حتى استيفائه** | **لا يُصنَّف قسراً** — قاعدة `K4-XR-04` |
| **أثره على `RSK-005`** | يبقى **Mitigated** (لا Closed) — و**بند «يقتضي عملاً على $K_4$» يُشطب مستوفىً**، فتصير البقية بنداً واحداً محدَّد الجانب بدل بندٍ عامّ |

## ④ 🔑 مرفوع للمالك — يُسجَّل ولا يُعطِّل

> **`GAP-X-EXH-01`**: هل تُقدَّم مادةٌ لـ`13-CORE-K3` تُنشئ مؤشراً فاصلاً للإنهاك الصامت (فيُغلق البند)، أم تبقى الحالة **معروضةً بلا إسناد** قاعدةً دائمة؟ **لا مؤشر يُخترع حتى القرار** — والقاعدة الحالية تعمل في الأثناء بلا تعطيل.

---

# §5 ما لا يعبر أبداً — سجل المحظورات

| المحظور | سنده |
| :--- | :--- |
| **التقابل البندي** بين عوامل $K_3$/$K_4$ أو بينهما وبين $K_2$ — ومنه «`K2-O` يقابل `K4-OR`» و«`K2-E` يقابل `K3-IR`» | `DEC-050` (`00-VERTICAL-MAP §4`) — تجميد **معتمد كقاعدة** |
| **استدلال درجةٍ على درجة** عبر الدوائر (بأي اتجاه، ومنه `VR-7`) | سقف `129 §4` · `K4-XR-09` |
| **إنشاء علاقة داخلية أو نمط** من قيد عابر | `§3/④` · قاعدتا الإثبات والصمت (`TRF-007`/`TRF-012`) |
| **قراءة فارق زمني** أو **ادّعاء صدق** | `DEC-244` · `DEC-246` |
| **مفردة عيادية** في أي قيد أو سطح | `14-CORE-K4 §5` (التطهير اللغوي الهندسي) |
| **مسّ طبقة القياس** أو أي جدار عرض | `instrument_pin` · `§3/⑤` |

---

# §6 فراغات موثقة

| الفراغ | البيان |
| :--- | :--- |
| **الواجهة الصادرة من $K_4$** | لا نص مختوم في دستور $K_4$ يصف واجهةً **خارجةً** منه (`§1` يصف الوارد فقط)؛ وسهم الانعكاس في `VR-7` مبدأ بلا تشغيل (`K4-XR-09`). **يُسجَّل فراغاً موثقاً لا يُملأ** |
| **$K_4$ × $K_1$ فيما وراء `TI`** | الجدار المختوم يخصّ المبادرة تحديداً؛ ولم يقم نص لواجهةٍ بين $K_1$ وبقية الصمامات — فراغ موثق |
| **`GAP-K4-CASES-01`** | حالات تحقق المالك — مفتوحة (`129 §6`)، وتسري على هذا السجل كما تسري على الميثاق |

---

# §7 بوابة النقد الذاتي (`م-7`) — واكتشافٌ يُصوَّب

- **الاكتشاف:** `م-4` («يُعرض ويُسأل») **استُعمل منذ `DEC-258 §2/③`** ثم في بطاقات العمق و`132`، **بلا قيدٍ في `77-LOG`** — خلافاً لقاعدة السجل «لا نقل بلا قيد · القيد **قبل** الاستعمال».
- **الحكم:** خلل **إجرائي في التوثيق لا في المنقول** — النقل يجتاز اختباره الثلاثي. صُوِّب بقيدٍ جديد يُعلن تأخره (`TRF-015`، على قاعدة `77-LOG §3/3`: التصويب قيدٌ جديد لا تعديل)، **ولم يُرمَّم تاريخ** بادّعاء أنه كان مقيَّداً.
- فحوص أخرى: الرمز `K4-XR` اختير تفادياً لاصطدام `X-01`/`X-02` المشغولين (درس `RSK-006`) ✓ · لا قيد عابر أُنشئ بلا نص مختوم ✓ · لا فجوة داخلية مُلئت من العبور ✓ · العنوان يطابق المضمون (`Check-10`) ✓.

---

# §8 ما لا تدّعيه هذه المرحلة

- ❌ لا تقيس ولا تضيف بنداً أو وزناً أو عتبة أو مستوى — ولا تمسّ طبقة القياس.
- ❌ **لا تُنشئ قاعدة عبور جديدة ولا تعدّل واحدة** — السجل فهرسٌ بمرساتين (`TRF-016`).
- ❌ لا تفتح `DEC-050` ولا تخفّف من أي جدار — بل تفهرسها وتثبّت محظوراتها.
- ❌ لا تغلق `RSK-005` ولا `GAP-X-EXH-01` — تضيّق وتسمّي الجانب وتُبقي المفتوح مفتوحاً.
- ❌ لا تبني سطحاً ولا لغة قارئ — السطح مقرَّر لا مبنيّ.
- ❌ لا صدق ميداني (`DEC-246`) — كل شيء `[اشتقاق غير محقّق ميدانياً]`.

---

# §9 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| طبيعة التغيير | وثائقي صرف — لا كود، لا حزم، لا أداة |
| توجيه المالك المؤسِّس | **شقّاه مستوفيان**: الداخلي (`130`) والعابر (هذه الوثيقة) |
| قيود النقل | `TRF-015…018` — منها **تصويب قيد متأخر** (`م-4`) رُصد ببوابة النقد الذاتي — `77-LOG` v1.5 |
| تغطية `DEC-072` | 5/5 مهارات $K_3$ مفهرسة ومرسّاة على جانب $K_4$ |
| الأدوات الستّ عشرة | 16/16 |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **حرفياً** |

---

**[نهاية `133-K4-CROSSING` — `DEC-263` · الترقيم التالي `DEC-264` · `CHG-080` · قيود العبور `K4-XR-01…09` · التالي `K4-XR-10`]**
