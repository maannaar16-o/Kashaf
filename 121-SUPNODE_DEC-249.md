# 121-SUPNODE — `DEC-249`: ضمّ `_sup_node.js` إلى الجرد المختوم

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | ضمّ مِحمَل Node المُعاد بناؤه `_sup_node.js` إلى قاعدة المعرفة |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-09 |
| **الأثر على الكود** | **صفر منطق جديد** — ربط وحدات فقط |
| **التغيير المرافق** | `CHG-067` |

---

# §1 الفجوة — `GAP-INV-SUPNODE-01`

`parity_supervisor.py:106` يستدعي طرفه JS عبر ملفٍّ اسمه `_sup_node.js` (قابل للتبديل بـ`SUP_NODE`) — **والملف غائب عن جرد `00-MANIFEST_2026-08-09` كاملاً**، وغير مذكور في أي وثيقة من قاعدة المعرفة. عاش في مجلّد عمل الجلسات السابقة ولم يُضَمّ قط.

**الأثر:** بوابة الترحيل (`§3.5` — الأدوات الستّ عشرة) **تسقط عند الأداة الثالثة** بـ`MODULE_NOT_FOUND` في أي بيئة جديدة. فجوة جرد لا فجوة منطق.

**لماذا لم تُرصد قبل اليوم:** الجلسات السابقة ورثت مجلّد العمل نفسه، والملف كان حاضراً فيه دون أن يكون في قاعدة المعرفة — **حقلٌ يعمل ولا يُجرَد**، وهو قريب الصنف من محذور `00-HANDOVER §6①`.

---

# §2 إعادة البناء — محتوى الملف وحدوده

الملف **مِحمَل لا محرّك**: يقرأ ملف الحمولات من `argv[2]`، يربط الوحدات بالنطاق العام **بالترتيب نفسه الذي تعتمده `build_supervisor_html.py`** (`packs` → `sp_gate` → `engines` → `reports` → `supervisor_core`)، ثم يمرّر كل حمولة إلى `RawahilSupervisor.grade` كما هي ويكتب الناتج `JSON`.

| الضابط | البيان |
| :--- | :--- |
| **صفر منطق** | لا حساب ولا تحويل ولا افتراض — تمرير خام |
| **مسار الاستثناء** | يطابق نظيره البايثوني في `parity_supervisor.py:118`: `[["EXC", false, name: message]], ["EXC"]` |
| **سند الترتيب** | `build_supervisor_html.py` ①→⑤ — المصدر المولِّد لأداة المتصفح نفسها |

---

# §3 برهان الأمانة — البصمة لا الظنّ

إعادة البناء لا تُقبل بالمراجعة البصرية بل بالقياس:

| المقياس | النتيجة |
| :--- | :--- |
| الحمولات | **36** — كاملة |
| الدرجات المقارنة | **429/429** — صفر تباعد |
| بصمة أحكام المشرف | **`6b324f996856eac3`** — **حرفياً** كما في `00-HANDOVER §2` |
| الأدوات الستّ عشرة بعد الضمّ | **16/16** · والبصمات الثلاث ثابتة |

⇒ لو انحرف الربط أو الترتيب أو مسار الاستثناء **لانكسرت البصمة**. ثباتها حرفياً هو دليل الأمانة الوحيد المعتدّ به — اتّساقاً مع أن البصمات الثلاث «لم تتزحزح مرّة واحدة».

---

# §4 القرار

| `DEC-249` | **ضمّ `_sup_node.js` إلى الجرد المختوم** — سدّاً لـ`GAP-INV-SUPNODE-01` كي لا تسقط بوابة الترحيل في أي بيئة لاحقة. يُرفع من `.gitignore` ويُودَع بالمستودع. **صفر منطق جديد · صفر مسّ بالمحرّكين · البصمات الثلاث ثابتة** | ضمّ جرد |
| :--- | :--- | :--- |

**ويبقى خارج الجرد عمداً** (`CHG-054` نافذ): `packs.js` (مولَّد) · `k3_contentpack.py` (نسخة قانونية تُنشأ بـ`cp`) · `Kashaf.html` · `golden_k3_PRE-DEC-215.json`.

---

**[نهاية `121-SUPNODE` — `DEC-249` · الترقيم التالي `DEC-250` · `CHG-067`]**
