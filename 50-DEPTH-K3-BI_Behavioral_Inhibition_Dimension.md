# 50-DEPTH-K3-BI_Behavioral_Inhibition_Dimension.md

---

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `50-DEPTH-K3-BI` |
| **السلسلة** | `50-DEPTH-K3` — تطبيق ثالث للقالب المعتمد (`EP` v1.0، `DEC-067`) |
| **المهارة** | كبح جماح النفس الميكانيكي (`K3-BI` / Behavioral Inhibition) |
| **الإصدار** | v1.1 — **معتمد ومُواءَم أحادياً** (`DEC-069` ثم مواءمة `DEC-076`/`DEBT-K3-ALIGN-01`) |
| **حالة الاعتماد** | ✅ معتمد · فجوة `RSK-005` **مطوَّقة بقاعدة معتمدة** (`DEC-072`) · **بصمة §1.5 مُواءَمة أحادياً** (قوة كبح، لا اتجاه بوابة) |
| **الوثيقة الموازية** | `55-USER-K3-BI` (قفل تزاوج) |
| **السند المؤسس** | `13-CORE-K3 §3/§4/§5` · `14-CORE-K4 §…` (نص الجدار العام) · `DEC-057…067` · `41-MEASURE §3.3/§5.3` · `02-MASTER RSK-005` (مفتوحة) |
| **الوثائق المصدرية** | `13-CORE-K3` كامل · `14-CORE-K4` (الفاصل مع الانفعال) · `50-DEPTH-K3-{EP,IR}` v1.0 |

**⚠️ تنبيه افتتاحي إلزامي:** هذه أول مهارة يصبح فيها **`RSK-005`** (جدول حدود $K_3$/$K_4$ الصارم، لا يزال مفتوحاً مشروعياً) **مؤثراً فعلياً لا نظرياً فقط**. القسم 5 أدناه يستخدم **حصراً** الجدار العام الموثّق (`13-CORE-K3 §5` و`14-CORE-K4`)، ويرسم بدقة **حدود ما هو معروف مقابل ما هو مجهول بعد** بدل تعميم مطمئن كاذب. لم أخترع تفاصيل الجدول الناقص.

## 0.1 أقفال العزل المنطبقة

| القفل | النص الحاكم | المصدر |
| :--- | :--- | :--- |
| **داخلي `IR` ⚔️ `BI`** | التحكم يضبط المعايرة الداخلية؛ الكبح يحظر المخرج الخارجي حصراً. | `13-CORE-K3` §4 |
| **داخلي `BI` ⚔️ `CF`** | الكبح فرملة لحظية لإيقاف الاندفاع؛ المرونة تعالج ما بعد الكبح لإعادة تكييف المسار. | `13-CORE-K3` §4 |
| **خارجي `K3` ⚔️ `K4` (الأهم هنا)** | $K_3$ تدير الحالة (State)؛ $K_4$ تدير السلوك الحركي (Behavior). | `13-CORE-K3` §5 · `14-CORE-K4` |

## 0.2 قاعدة الوسم

كل وحدة: **[معتمد]** · **[مشتق]** · **[مقترح]** · **[فجوة]**. بطاقة سباعية المحاور مطابقة للقالب.

---

# القسم 1: البطاقة المعمارية — `K3-BI`

## 1.1 المحور الأول: الهوية التنفيذية والوظيفة — **[معتمد]**

| الحقل | البيان |
| :--- | :--- |
| **التعريف البنيوي الحرفي** | مهارة حظر وقفل تنفيذ المخرجات؛ تمنع الاستثارة الانفعالية الثائرة من التحول القسري الفوري إلى فعل حركي اندفاعي أو قول طارئ غير مدروس المآلات. |
| **سؤال التشغيل الداخلي** | *هل أسمح للمخرج السلوكي بالمرور الآن؟* |
| **وعاء الحساب الإحصائي (11 بنداً)** | 2(أ) · 18(أ) · 33(أ) · 47(أ) · 59(أ) · 69(أ) · 78(أ) · 85(أ) · 89(أ) · 92(أ) · 94(أ) |
| **المرساة العصبية** | Fronto-limbic — مستوى الدائرة، لا المهارة. |

## 1.2 المحور الثاني: مراحل التشغيل الداخلية — **[مشتق]**

| المرحلة | الوظيفة |
| :--- | :--- |
| **م1 — استقبال الشحنة المعدَّلة** | تسلّم ما أنتجه `IR` (شحنة بعد التعديل، لا خام). |
| **م2 — تقييم جاهزية المخرج** | فحص هل الدافع السلوكي/اللفظي الوشيك مدروس المآلات أم اندفاعي غير محسوب. |
| **م3 — البوابة الثنائية** | قرار حاسم: سماح أو حظر — **صمام لا مرشّح تدريجي** (لا حالة وسطى بنيوياً). |
| **م4 — التفريع** | عند السماح: تسليم للتنفيذ (خارج حدود $K_3$). عند الحظر: تسليم لـ`CF` لإعادة تكييف الاستجابة. |

**⚠️ ملاحظة بنيوية حاكمة:** `BI` هي **أول مهارة تتفرّع فيها السلسلة** (§1.4) — ليست خطاً واحداً كـ`EP→IR`، بل مفرق: مسار يخرج من $K_3$ (عند السماح) ومسار يبقى داخلها (عند الحظر، نحو `CF`). المفرق الخارج هو بالضبط موضع `RSK-005`.

## 1.3 المحور الثالث: المشغّل والمعطّل — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المشغّل الأمثل** | شحنة مُعدَّلة (لا خام) واردة من `IR` + نافذة زمنية كافية لتقييم المآلات قبل ذروة الاندفاع. |
| **المعطّل** | (أ) شحنة خام غير معدَّلة (عطل موروث من `IR`). (ب) ذروة اندفاع أسرع من سرعة تقييم البوابة. (ج) حظر متكرر متراكم دون تسليم فعلي لـ`CF` (بوابة مغلقة معلّقة بلا معالجة). |

## 1.4 المحور الرابع: الموقع في السلسلة التعاقبية — **[مشتق]**

| البند | البيان |
| :--- | :--- |
| **المصدر الأعلى** | `K3-IR` حصراً. |
| **المصبّان الأدنيان (تفرّع)** | عند السماح: **خارج $K_3$** (منطقة `RSK-005` غير المحسومة). عند الحظر: `K3-CF` داخلياً. |
| **نقطة الانهيار عند الغياب** | ضعف `BI` = تسرّب اندفاعي (فعل/قول متهور يفلت رغم معرفة الشخص بعواقبه) — لا "تنظيم أعمى" كما في `EP`/`IR`، بل **تسرّب بوابة**. |

## 1.5 المحور الخامس: بصمة التنظيم — **[معتمد — مُواءَم أحادياً بـ`DEC-076`]**

| البند | البيان |
| :--- | :--- |
| **الطبيعة كما تُقاس** | `BI` **أحادية القياس**: تقيس **قوة الكبح** (مدى القدرة على منع المخرج الاندفاعي)، على سلّم رتيب ضعيف ← قوي. الفحص الحرفي للبنود (`41-MEASURE-CONFLICT §3`) أثبت أنها جميعاً مصاغة بمنطق «كبح ناجح = درجة أعلى». |
| **القطب المضاد غير مقيس (تنبيه حرج)** | «التجمّد / فرط الكبح» (البوابة لا تُفتح حتى لفعل سليم) **لا يُقاس**، بل **يسجّل درجة عالية زائفة** (يبدو انضباطاً ممتازاً). فالأداة عاجزة بنيوياً عن تمييز الكبح الصحي من التجمّد. يُحظر ادّعاء رصد التجمّد من الدرجة (`URS §6 بند 6`). |
| **قراءة الدرجة الصحيحة** | `SP` منخفضة = **تسرّب/ضعف كبح** (اندفاع يفلت). `SP` مرتفعة = **كبح قوي** — دون أن يُميَّز الصحي من المفرط. لا يُقرأ التجمّد من رقم منخفض (خطأ النسخة الأصلية)؛ التجمّد إن وُجد يقبع في الطرف **المرتفع** لا المنخفض. |

## 1.6 المحور السادس: الحدود الداخلية والخارجية — **[معتمد استنساخ + مشتق تخصيص]**

| الحد | النص | الحالة |
| :--- | :--- | :--- |
| **داخلي `IR`⚔️`BI` (أعلى)** | التحكم يضبط الداخل؛ الكبح يحظر الخارج. | **[معتمد]** حرفي |
| **داخلي `BI`⚔️`CF` (أسفل)** | الكبح فرملة لحظية؛ المرونة تعيد التكييف بعدها. | **[معتمد]** حرفي |
| **خارجي — $K_1$** | لا علاقة بمصدر الدافع، فقط بالسماح لمخرجه. | معتمد |
| **خارجي — $K_2\text{-E}$** | غير منطبق فعلياً هنا — `BI` تحكم مخرج **الذات**، لا تقرأ نوايا الآخرين. تماس منخفض جداً. | مشتق |
| **خارجي — $K_4$ (المحور الحرج)** | **ما هو معتمد:** $K_3$ تقرر السماح/المنع (حالة)؛ $K_4$ تنفّذ وتُقيّم جودة/استمرارية ما سُمح به (سلوك). **ما هو مفتوح (`RSK-005`):** عند حظر ناجح لا يعقبه بديل بنّاء (سكون تام بعد المنع) — هل هذا السكون إنجاز مكتمل لـ`BI` (البوابة أدت وظيفتها)، أم امتداد يفحص لماذا لم يُطلق `K4-TI` بديلاً، أم أن `BI` لم يُسلّم شيئاً أصلاً لـ`CF` لتوليد بديل؟ **لا حسم هنا.** | **[فجوة — `RSK-005`، صياغة محددة في §6]** |

## 1.7 المحور السابع: إشارات القوة والضعف الملحوظة — **[مشتق]**

### 1.7.1 إشارات القوة

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 | الاستقبال الواثق | يتلقى شحنة `IR` المعدَّلة دون مقاومة أو إنكار لوجودها. |
| م2 | التقييم السريع | يقيّم مآلات الفعل الوشيك خلال نافذة زمنية كافية دون تسرّع. |
| م3 | البوابة الحاسمة | يوقف فعلاً اندفاعاً وشيكاً (صمت لحظة قبل رد حاد، يد تتوقف قبل فعل متهور). |
| م4 (سماح) | التمرير بلا تردد زائد | يسمح للفعل السليم بالمرور دون تجميد يعطّل التنفيذ. |
| م4 (حظر) | التسليم الفعلي لـ`CF` | يحوّل الممنوع فعلياً لإعادة تكييف، لا يتركه معلَّقاً. |

### 1.7.2 إشارات الضعف

| المرحلة | الإشارة | الوصف |
| :--- | :--- | :--- |
| م1 | الاستقبال الخام | يستقبل شحنة غير معدَّلة (خلل موروث من `IR` ينعكس هنا). |
| م2 | فوات نافذة التقييم | يتصرف قبل أن "يفكر" — الفعل يسبق التقييم. |
| م3 | تسرّب البوابة | يعرف أنه يجب أن يتوقف لكنه لا يستطيع فعلياً. |
| م3 | تجمّد البوابة | لا يسمح حتى لفعل سليم مدروس بالمرور — كبح مفرط. **⚠️ ملاحظة قياس (`DEC-076`):** نمط سلوكي **ملحوظ** لكنه **غير مقيس بالدرجة** (يسجّل درجة عالية زائفة)؛ لا يُستدعى بشرط `SP` منخفض، ولا يُدَّعى رصده من التقرير الرقمي. |
| م4 | التعليق بلا تسليم | يترك الممنوع معلّقاً دون تحويله لـ`CF` — تراكم كبت متكرر. |

**🔒 قفل الخلط:** إشارات وصفية-نمطية لا تشخيصية. "تسرّب البوابة" (طرف مقيس، درجة منخفضة) و"تجمّد البوابة" (طرف غير مقيس، لا يُقرأ من الدرجة) أوصاف لانحراف تشغيلي، لا حكماً شخصياً.

---

# القسم 2: التشريح العميق

**[مشتق]** — لماذا "صمام لا مرشّح": التعريف الحاكم يصف حسماً («حظر وقفل») لا تدرجاً؛ هذا يميّز `BI` عن `IR` (الذي يعدّل الشدة تدريجياً) — `BI` تقرر فقط: يمر أم لا، ثم تنتهي مهمتها عند حدود $K_3$.

**[مشتق — مُواءَم `DEC-076`]** — لماذا التفرّع مهم منهجياً: «ضعف `BI`» في **الدرجة** يعني **تسرّباً** (كبح ضعيف) حصراً. أما «التجمّد» (منع ما يجب أن يمر) فنمط **سلوكي ملحوظ لا رقمي** — يقبع في الطرف المرتفع للدرجة وغير مقيس. فالتمييز بين الفرعين يُقرأ من **الملاحظة السلوكية** لا من الرقم (على عكس ما أوحت النسخة الأصلية).

---

# القسم 3: طيف الشدة الثماني لـ `K3-BI`

**[معتمد — تطبيق مباشر `DEC-061`/`DEC-064`]** نفس البنية والدلالة الموحّدة لكل $K_3$ — لا إعادة فتح.

**⚠️ ملاحظة مُصحَّحة (`DEC-076`):** الأداة أحادية رتيبة (قوة كبح). `SP` **منخفضة = تسرّب/ضعف كبح** حصراً (لا لبس). أما «التجمّد» فلا يظهر في الطرف المنخفض بل يقبع مخفياً في الطرف **المرتفع** (كبح مفرط يبدو انضباطاً)، **وهو غير مقيس**. النسخة الأصلية أخطأت إذ ساوت بين تسرّب وتجمّد في الرقم المنخفض. لا يُدَّعى تمييز اتجاه من الدرجة.

---

# القسم 4: خريطة التسلسل-الحدود-الانهيار

| الحقل | البيان |
| :--- | :--- |
| **مع `IR` (السابقة)** | استهلاك أحادي للشحنة المعدَّلة. |
| **مع `CF` (فرع الحظر)** | تسليم الممنوع لإعادة التكييف. |
| **مع خارج $K_3$ (فرع السماح)** | **منطقة `RSK-005` — غير مرسومة الحدود بدقة بعد.** |
| **حد الفشل المتسلسل** | `BI` ضعيفة (تسرّب) ← اندفاع يفلت فعلياً رغم معرفة العواقب. `BI` مفرطة (تجمّد) ← لا فعل يمر إطلاقاً، حتى السليم منه. |

---

# القسم 5: جدران العزل الخارجية

| الدائرة المقابلة | التطبيق الخاص بـ `BI` | الحالة |
| :--- | :--- | :--- |
| $K_1$ | لا علاقة بمصدر الدافع. | معتمد |
| $K_2\text{-E}$ | تماس منخفض جداً — `BI` ذاتية المخرج لا قرائية للآخر. | مشتق |
| $K_4$ | **جزئي — انظر §1.6 و§6.** المبدأ العام معتمد؛ التفصيل الحرج مفتوح. | **معتمد جزئياً + فجوة موثّقة** |

---

# القسم 6: سجل الفجوات

| كود | البيان | الأثر | الخطورة | القرار المؤقت | المطلوب |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`RSK-005` — تجلٍّ على `BI`** | ✅ **مطوَّقة بقاعدة معتمدة (`DEC-072`).** "سكون بعد حظر ناجح" يُسند: `K4` (نيّة نشأت ولم تُنفَّذ) · `CF` (لا نيّة نشأت) · `ST` (تجمّد استنزاف). | محسوم عبر `00-VERTICAL-MAP-APPENDIX` | متوسطة (متبقٍّ: إنهاك صامت) | قاعدة معتمدة مطبَّقة | لا شيء عاجل |
| **ملاحظة — طبيعة البوابة** | ✅ **محسومة بـ`DEC-076`.** `SP` منخفضة = تسرّب حصراً؛ التجمّد غير مقيس (طرف مرتفع). لا ادّعاء اتجاهي من الدرجة. | — | — | مغلقة | — |
| **DEBT-K3-ALIGN-01 على `BI`** | ✅ **مُنفَّذ.** بصمة §1.5 مُواءَمة أحادياً (قوة كبح)؛ إشارة التجمّد وُسمت «غير مقيسة». | — | — | مغلق | — |

---

# القسم 7: ملحق الربط القياسي

| العنصر | القيمة |
| :--- | :--- |
| وعاء `K3-BI` | 2(أ) · 18(أ) · 33(أ) · 47(أ) · 59(أ) · 69(أ) · 78(أ) · 85(أ) · 89(أ) · 92(أ) · 94(أ) — 11 بنداً |
| `MaxRaw` | 66 |
| `SS = x − 2z + y` ≡ `x + 3y − 22` | **[معتمد، `DEC-028`]** |
| `SS_max` | 77 |
| `SP = SS ÷ 66 × 100` | **[معتمد، `41 §3.3`/`DEC-065`]** |
| `SP_max` | ≈ 116.7% |
| ديون عاملية | لا شيء مسجل رسمياً على الوعاء (الدين هنا بنيوي-حدودي `RSK-005`، لا عاملياً بمعنى `RSK-016`) |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| مطابقة القالب (7 محاور) | ✅ |
| اكتشاف بنيوي جديد | ⚠️ `BI` نقطة تفرّع (لا خط واحد) — أول ظهور لهذا النمط في $K_3$ |
| بصمة التنظيم | ✅ **أحادية القياس** (قوة كبح) بعد `DEC-076` — التجمّد قطب غير مقيس موثَّق |
| جدار $K_4$ | ✅ **مطوَّق** بقاعدة `DEC-072` (لا مفتوحاً بعد الآن) |
| فجوات مرصودة | `RSK-005` مطوَّقة · ملاحظة البوابة محسومة · `DEBT-K3-ALIGN-01` مُنفَّذ — **صفر مفتوح حرج** |
| **حالة الوثيقة** | ✅ **v1.1 — معتمدة ومُواءَمة أحادياً (`DEC-069` + `DEC-076`)** |
