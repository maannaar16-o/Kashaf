# 90-REPORTS-03 — المرحلة ③ : نقل طبقة التقارير (المحسوم منها)

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** (المحسوم) · **مؤجَّل بأمر المالك** (المعلَّق — §5) |
| **نطاق الاستدلال** | `k2_report.py` · `k3_report.py` · `k2_framing` (الملحق أ) · `packs.js` · `87-PARITY §4` |
| **الحصيلة** | **75 تقريراً · صفر تباعد نصّي** · بصمة المتن `7f10e667ae4254cd` |
| **الإصدار** | v1.0 |

---

# القسم 1: المنقول — القرارات المطبَّقة

| القرار | الأثر في `reports.js` |
| :--- | :--- |
| `DEC-183` | حذف عمود `SP` من لوحة $K_2$ · و$K_3$ كان ممتثلاً أصلاً (صفر نسبة · صفر رمز) |
| `DEC-187` | لوحة $K_2$: `\| البُعد \| الرمز \| الحالة \|` — الرمز والكلمة بلا نسبة |
| `DEC-188` | «قبل أن تقرأ» حرفياً بلا تعديل |
| `DEC-190` | `resolveSlot` + `_CARD` + `validateSlots` — نقل حرفي للحارس |
| `DEC-195/ج` | ② المشترك فقط · ③ كل خاتمة في قسم مهارتها |
| `DEC-196` | `bandLabel()` يوقف عند نطاق مجهول — لا وسم افتراضي |
| `DEC-197/ج` | `skillHeading()` يشتقّ الاسم المرادف من الخاتمة بالتعبير النمطي |

**ملاحظة على `DEC-183`:** طُبِّق في **بايثون أولاً** ثم نُقل — فالمصدر والنسخة يتغيّران معاً، ولا تسبق نسخةٌ أخرى (`DEC-199`).

## نطاق $K_3$ المنقول

الأقسام **① ② لافتة الثقة ③** — أي كل ما لا يعتمد على طبقة التركيب النصّي (`CONNECTIVES` · `TEMPLATES` · `ROOT_QUESTIONS`). الأقسام ⑤…⑨ تلحق في المرحلة ④.

---

# القسم 2: حزمة سادسة استُحدثت — `BANNER_K3`

**لافتة الثقة** (`ر-4`) كانت تعيش **في الكود** (`k3_contentpack.TRUST_BANNER`) لا في حزمة. فلم يكن للطرف JS مصدر يقرأ منه.

**الحلّ:** استُخرجت إلى `k3_banner.json` وضُمّت إلى المولّد — **لا نُسخت يدوياً**. فصار المصدر واحداً للطرفين، وأي تعديل عليها يمرّ بالبصمة.

| | قبل | بعد |
| :--- | :--- | :--- |
| الحزم | 5 · 207,549 بايت | **6 · 207,734 بايت** |
| `BANNER_K3` | — | `54908df1fdfcbfe7` |

---

# القسم 3: التكافؤ النصّي — تنفيذ `DEC-199` على طبقة التقارير

`parity_reports.py` يقارن **نصّ التقرير كاملاً** بعد تطبيع `87-PARITY §4` (توحيد `\n` · حذف الفراغ الذيلي · حذف الأسطر الطرفية).

```
k2          45 تقريراً   ✅ متطابق
k3          30 تقريراً   ✅ متطابق
──────────────────────────────────
✅ تكافؤ نصّي تامّ — 75 تقريراً · صفر تباعد
   بصمة المتن الكلي: 7f10e667ae4254cd
```

---

# القسم 4: ثلاثة تباعدات رُصدت وحُسمت

أول تشغيل أخرج **٣٠ تباعداً**. حُلّت بالترتيب، وكلها في **الطرف JS أو في المقارن** — لا في خط الأساس:

| # | التباعد | التشخيص | الحكم |
| :--- | :--- | :--- | :--- |
| ① | لافتة الثقة (`ر-4`) غائبة في JS | **نقص في نقلي** — النصّ في الكود لا في حزمة | JS خاطئ ⇒ صُحِّح بالحزمة السادسة |
| ② | سطر زائد في ذيل `ALL-HIGH` | **قطع خاطئ في المقارن** — سطر الوصل `C-02` يسبق العنوان ⑤ | المقارن خاطئ ⇒ صُحِّح |
| ③ | 57 سطراً زائداً في `ALL-LOW` | القسم ⑤ **غائب** حين لا مهارة عالية ⇒ سقط القطع كله | المقارن خاطئ ⇒ عُمِّم القطع |

> **الفارق الجوهري:** ① عيب في **المنتج** — لو مرّ، لصدر تقرير $K_3$ بلا لافتة ثقة لمن ملاحظته محدودة، أي خرق `ر-4`. و②③ عيبان في **الأداة** — يُنتجان إنذاراً كاذباً ولا يمسّان المخرج. والتمييز بينهما هو ما منع تصحيح المحرك لإرضاء مقارن معيب.

---

# القسم 5: المؤجَّل — بأمرك

| البند | الوصف | لماذا أُجِّل |
| :--- | :--- | :--- |
| `DEC-189 §8.1` | إعادة البناء إلى **٩ أقسام** (الدمج الطبقي) | تتوقف على `TC-K2-01` |
| `TC-K2-01` | نطاق كتالوج المركز | **صار قابلاً للحسم بالوظيفة** بعد `DEC-190` |
| `GAP-RPT-K2-01` | `R1` كتل الشدة الثماني | غائبة عن الطرفين |
| `GAP-RPT-K2-02` | `R9` قواميس التطهير `PUR-X-##` | الحارس الحالي بقائمة مختصرة |
| `GAP-RPT-K2-03` | `R11` العرض المزدوج `St+H` | غائب |
| `RSK-DUP-01` | مانع الازدواج | **قد يسقط** بتوزيع الوظائف (`84 §ملحق`) |

**الوضع الراهن للتقرير:** ٨ أقسام في $K_2$ — **بنية `k2_report` الحالية**، منقولة بأمانة. وهي حالة مؤقتة معلَنة لا نهائية.

---

# القسم 6: الانحدار الشامل بعد النقل

| المجموعة | النتيجة |
| :--- | :---: |
| `test_packs` (`ح-1·ح-2·ح-3`) | ✅ |
| `parity_py` (114 حالة منطق) | ✅ `ef7b161006c30529` |
| `parity_reports` (75 تقريراً) | ✅ `7f10e667ae4254cd` |
| `test_golden_k2` · `test_report_k2` | ✅ |
| `test_golden_k3` · `test_report_k3` | ✅ |

**ستّ مجموعات · صفر انحدار.**

---

# القسم 7: المخرجات

| الملف | الوصف |
| :--- | :--- |
| `reports.js` | مُركّبا $K_2$ (٨ أقسام) و$K_3$ (①②③) |
| `packs.js` | **مُعاد توليده** — ست حزم |
| `parity_reports.py` · `parity_reports_js.js` | تكافؤ المتن النصّي |
| `k3_banner.json` | لافتة الثقة مستخرَجة من الكود |
| `SAMPLE_K2_JS.md` · `SAMPLE_K3_JS.md` | عيّنتان مولَّدتان من JS |

---

# القسم 8: الخطوة التالية

| المرحلة | الحالة |
| :--- | :---: |
| ① نقل المحرّكين | ✅ |
| ② تضمين الحزم | ✅ |
| ③ نقل التقارير (المحسوم) | ✅ |
| ④ أقسام $K_3$ ⑤…⑨ + الواجهة والتصدير | 🟢 جاهزة |
| ⑤ حسم المعلَّق (`TC-K2-01` → ٩ أقسام) | ⬜ **آخر خطوة بأمرك** |

**الترقيم التالي:** `DEC-205`.

---

> **[نهاية الوثيقة — `90-REPORTS-03` v1.0 · 75 تقريراً بصفر تباعد · المعلَّق مؤجَّل بأمر المالك]**
