# 00-PROTOCOL_Self_Critique_Gate.md
## بوابة النقد الذاتي — خطوة قياسية قبل اعتماد وثيقة تُعيد بناء وثيقة قائمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `00-PROTOCOL-CRITIQUE-GATE` |
| **الإصدار** | **v1.2 — ✅ معتمد** (`DEC-120` + `DEC-193` + `DEC-194/198/199`) |
| **التصنيف** | **قاعدة إجرائية (حوكمة)** — لا تمسّ البنية النظرية ولا القياس ولا الدوائر |
| **المنشأ** | تجربة إعادة بناء `52-MATRIX-K3-04` (2026-07-21): البناء المباشر أنتج v1.0 بستة عيوب، أخرجها **نقد ذاتي منهجي** قبل الاعتماد |
| **التوسعة** | جلسة `Mode 03-Build` (2026-08-03): **الفحص 10** + شرط تفعيل ثانٍ + **`ن-7`** — كلها مستخرَجة من عيوب وقعت في تلك الجلسة نفسها |
| **الموضع** | يُقيَّد في `01-MASTER-Governance` ضمن قواعد العمل |

---

# القسم 1: شرط التفعيل — **ضيّق بقصد**

> تُفعَّل البوابة حين تُعاد بناء **وثيقة قائمة** كان أساسها **قد تغيّر** بقرار لاحق.

| ✅ تُفعَّل | ❌ لا تُفعَّل |
| :--- | :--- |
| وثيقة بُنيت على بنية/قاعدة عُدِّلت لاحقاً *(مثل `M3-04` المبنية على السلسلة الخطية بعد اعتماد البنية المحورية)* | وثيقة **جديدة** لا تُعيد بناء شيء *(`62` · `65` · `67` · `68`)* |
| نسخة جديدة تُلغي حكماً في نسخة معتمدة | **رقعة نصّية** لا تغيّر حكماً *(رقعة `M3-06`)* |
| وثيقة تُغيّر **موضع** مفهوم بين طبقتين | تحديث إصدار توثيقي بحت *(`61` v1.1)* |

**علّة التضييق:** النقد على كل وثيقة يُبطئ العمل بلا عائد. الخطر الحقيقي محصور في **البناء فوق أساس متبدّل** — حيث تُورَث نتائج سقط مرجعها، ويُملأ الفراغ من الذاكرة لا من المصدر.

---

## 1-ب. شرط التفعيل الثاني — المخرج الآلي (`DEC-193`)

> تُفعَّل البوابة كذلك على **كل مخرج آلي متكرر** (محرك تقارير · مولّد وثائق · أداة تفاعلية) عند **أول إصدار** وعند **كل تعديل يمسّ طبقة السحب أو الإسناد** — بقائمة مختصرة: الفحوص **1 · 4 · 10** حصراً.

| ✅ تُفعَّل | ❌ لا تُفعَّل |
| :--- | :--- |
| مخرج آلي متكرر عند أول إصدار أو تعديل طبقة السحب *(`DEF-K2-01`)* | تشغيل روتيني لمحرك لم يتغيّر |

**علّة الشرط الثاني:** الشرط الأول ينشأ من **وثيقة**، و`DEF-K2-01` نشأ من **كود يُنتج مخرجاً متكرراً** — فما كانت البوابة لتُفعَّل عليه أصلاً. وخطر المخرج الآلي **تراكمي**: يتكرر في كل إصدار بخلاف الوثيقة التي تُراجَع مرة.

---

# القسم 2: قائمة الفحص العشرة

مشتقة من العيوب التي أخرجها النقد فعلاً — لا من تصوّر نظري:

| # | الفحص | السؤال | ما أخرجه في `M3-04` |
| :--- | :--- | :--- | :--- |
| **1** | **تتبّع السند** | هل كل خانة/جملة مردودة إلى مصدر بعينه — أم مُلئت من الوثيقة القديمة أو من الذاكرة؟ | صفّ `ST` معطوب في 3 خانات |
| **2** | **محور التصنيف** | هل الوثيقة تصنّف على محور غير محور مصدرها؟ فإن فعلت، هل **صرّحت** بذلك؟ | تصنيفان بديا متعارضين وهما محوران |
| **3** | **المصطلحات المستوردة** | هل أُعيد استعمال مصطلح من النسخة القديمة في موضع يحظره الأساس الجديد؟ | «احتواء بعديّ» للتهذيب |
| **4** | **التناقض الداخلي** | هل يناقض قسمٌ قسماً آخر في الوثيقة نفسها؟ | `EP` «خارج المصفوفة» ولها صف فيها |
| **5** | **الجسور** | هل تترك الوثيقة فراغاً بين مستواها والمستوى التالي بلا إحالة؟ | حافيّ ← دائري بلا جسر |
| **6** | **الشروط غير المقيسة** | هل تتضمن شرطاً لا تقيسه الأداة ولا يُشتق منها؟ | «المخرج الظاهر سليم» |
| **7** | **تصريح نطاق التغطية** | هل تغطي أقل من مصدرها؟ فإن فعلت، هل بيّنت أن ذلك **اختيار مبدئي** لا نقص؟ | 14 خانة من 28 بلا تصريح |
| **8** | **مصدر واحد للحقيقة** | هل تُنشئ مصدراً ثانياً يتباعد عن الأصل مع الوقت؟ | مصفوفة موازية للسجل |
| **9** | **إنصاف النسخة القديمة** | هل وُصفت نتائجها بـ«أخطاء» وهي كانت **متسقة مع أساسها آنذاك**؟ | ثلاث نتائج «سقط مرجعها» لا أخطاء |
| **10** | **مطابقة العنوان للمضمون** | هل يحمل كل عنوان/حقل/فتحة **مضمونه المقصود** — أم استُدعي المحتوى بموضع أو رقم فانزاح عن عنوانه؟ | `DEF-K2-01`: «بصمتك» عرضت «ما لا تعنيه هذه العدسة» في 6 من 8 مراكز |

**الفحص 1 هو الأثقل** — أكثر العيوب نشأ منه: وراثةُ تصوّر من النسخة القديمة تُملأ به خانة بلا رجوع للمصدر.

**الفحص 10 هو الأخفى** — لا يظهر في القراءة بل في مطابقة المحتوى بعنوانه. مرّ `DEF-K2-01` على **أربعة تقارير مقروءة** لأن النصّ كان عربياً سليماً والإسناد خاطئاً.

---

# القسم 3: شكل التنفيذ

| # | القاعدة |
| :--- | :--- |
| **ن-1** | النقد يُعرض **قبل** طلب الاعتماد، لا بعده — وفي **مخرج مستقل** عن الوثيقة |
| **ن-2** | **يُعلن نتيجته حتى لو كانت «لا عيوب»** — وإلا صار طقساً شكلياً |
| **ن-3** | يُصنَّف كل عيب: 🔴 **خطأ صريح** (مخالفة سند) · 🟡 **عيب منهجي** (بلا مخالفة) · ⚪ **ملاحظة تحسين** |
| **ن-4** | يُطرح **سؤال البقاء**: هل بقاء هذه الوثيقة بصفتها الحالية مبرَّر أصلاً، أم يلزم **إعادة تموضع**؟ |
| **ن-5** | لمالك المشروع **إعفاء** أي وثيقة من البوابة بكلمة |
| **ن-6** | يُقيَّد أثر النقد في **سجل التغيير** داخل الوثيقة المنقَّحة، ليبقى الفرق مرئياً |
| **ن-7** | **ما يُفحص آلياً يُحرَس آلياً.** كل فحص قابل للترميز يُنفَّذ حارساً داخل المحرك **يوقف الإصدار** عند الشذوذ، ولا يعود بقيمة فارغة صامتة (بروتوكول صفر هلوسة · البند 10). **ويُشترط في الحارس أربعة:** ① **يُختبَر على عيب معلوم قبل اعتماده** — حارس لم يسقط على عيب واقع لا يُعتمد (`DEC-194`). ② **كل فحص يقيس شيئاً واحداً** — الفحص الذي يخلط عيبين متمايزين يُنتج إنذاراً كاذباً على أحدهما ويطمس الآخر (`DEC-198`). ③ **تكافؤ النسخ** — متى نُفِّذ المنطق نفسه في بيئتين، يُشترط تطابق مخرجهما على مجموعة الانحدار نفسها (`DEC-199`)، **وعند التباعد يُجمَّد الطرفان** حتى يُحسم أيّهما يوافق خط الأساس — لا أسبقية للتنفيذ الأسبق (`DEC-200`). ④ **لا عتبة مخترعة** — الحارس يوقف عند الحالة القاطعة ويُبلِّغ المقياس، ولا يخترع وزناً (`DEC-224`). القائمة البشرية تكمّل الحارس ولا تُغنِي عنه |

**السؤال ن-4 هو الأعلى قيمة:** هو الذي أخرج `M3-04` من «مصفوفة تُصحَّح» إلى «قاعدة وقراءة + ملحق مشتق» — وأزال خطر المصدر الثاني نهائياً بدل معالجته خانةً خانة.

---

# القسم 4: حدود البروتوكول

1. **إجرائي لا معرفي** — لا يعتمد حقائق ولا يعدّل بنية ولا يمسّ القياس.
2. **لا يُلغي المراجعة السيادية** المستمرة على كل مخرج — بل يُضيف بوابةً في حالة بعينها.
3. **لا يُطبَّق أثراً رجعياً** على وثائق معتمدة سلفاً.
4. **لا يُستعمل ذريعةً للتأجيل** — النقد مخرج واحد، لا دورة مفتوحة.

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| شرط التفعيل | إعادة بناء وثيقة قائمة فوق أساس متغيّر — **حصراً** |
| شرط التفعيل الثاني | **مخرج آلي متكرر** — فحوص 1·4·10 حصراً (`DEC-193`) |
| قائمة الفحص | **10 بنود**، كلها مشتقة من عيوب واقعة |
| قواعد التنفيذ | **`ن-1 … ن-7`** — و`ن-7` بأربعة شروط |
| أول تطبيق | `52-MATRIX-K3-04` v1.0 ← v1.1 (**6 عيوب**، منها 3 أخطاء صريحة) |
| أول تطبيق للشرط الثاني | `Mode 03-Build` 2026-08-03 — **تسعة عيوب** في محرّكَي $K_2$/$K_3$ والأداة |
| **حالة الوثيقة** | ✅ **v1.2 — معتمدة (`DEC-120` + `DEC-193`)** |

---

# ملحق: سجلّ التغيير

| الإصدار | القرار | ما تغيّر |
| :--- | :--- | :--- |
| v1.0 | `DEC-120` | التأسيس — 9 فحوص · `ن-1…ن-6` · شرط تفعيل واحد |
| **v1.1** | `DEC-193` · `DEC-198` | **الفحص 10** · **شرط التفعيل الثاني** · **`ن-7`** بشرطَي «الاختبار على عيب معلوم» و«فحص واحد لكل قياس» |
| **v1.2** | `DEC-199` · `DEC-200` · `DEC-224` | `ن-7` تُوسَّع بشرطَي **تكافؤ النسخ** (وتجميد الطرفين عند التباعد) و**لا عتبة مخترعة** |

## الأخطاء التي وَلَّدت الشروط الأربعة

| الشرط | الخطأ الذي أنتجه |
| :--- | :--- |
| ① يُختبَر على عيب معلوم | حارس `ص-3` في $K_3$ أعطى «سليم» **ست مرات** قبل تصحيحه — كان يتوقف مسحه قبل موضع العيب |
| ② كل فحص يقيس شيئاً واحداً | فحص الخاتمة أعطى **١٨ إنذاراً كاذباً** لأنه خلط «هوية الخاتمة» بـ«اسم المهارة» |
| ③ تكافؤ النسخ + تجميد الطرفين | `DEF-K3-02`: `gates_fired` خالف السند في **الطرفين معاً** — ولو حكمت الأسبقية لَوَرِث JS خطأ بايثون |
| ④ لا عتبة مخترعة | `ت-6`: التنفيذ الحرفي بالشظايا أعطى **١٠٠٪ إنذاراً كاذباً**؛ والعتبة وزنٌ لا يُخترَع |

> **الشروط الأربعة كلها مستخرَجة من أخطاء وقعت أثناء بناء الحرّاس أنفسهم** — لا من تصوّر نظري. وهذا امتداد لمنشأ البروتوكول نفسه.

---

# `ن-8` — الصياغة الصريحة عند حافة الإخراج

> **`DEC-239`** · نطاقه **حافة الإخراج حصراً** (الخيار ①-أ)

**النصّ:**

> كل قيمة تعبر من الحساب إلى **مخرج** — نصّ تقرير · كتلة تدقيق · رسالة استثناء · تصدير — تُصاغ نصّاً **بصيغة مكتوبة صراحةً**، ولا يُتّكل على تصيير اللغة التلقائي.
>
> **لا يسري داخل الحساب:** الصياغة الصريحة في الداخل ضجيجٌ يُنشئ سطح انجراف جديداً بدل أن يسدّ واحداً.

## منشؤه — ثلاث حالات، علاجها واحد

| الموضع | السلوك الضمني | الصيغة الصريحة |
| :--- | :--- | :--- |
| `DEC-235` — قائمة | `['A', …]` مقابل `A,…` | الفاصل `·` |
| `DEC-235` — عدد | `4.0` مقابل `4` | `_num` |
| `DEC-238` — تقريب | مصرفي مقابل نصفٍ لأعلى | `_round2` |

## أصناف التباعد التسعة — **مقيسة تشغيلياً لا مُتخيَّلة**

| # | الصنف | بايثون | JS |
| :--- | :--- | :--- | :--- |
| ① | قائمة → نصّ | `['A', 'R']` | `A,R` |
| ② | صحيح-عشري | `4.0` | `4` |
| ③ | تقريب النصف | `2.67` | `2.68` |
| ④ | **قاموس → نصّ** | `{'a': 1}` | **`[object Object]`** |
| ⑤ | **ترتيب المفاتيح** | `['10','2','a']` | **`['2','10','a']`** |
| ⑥ | منطقي | `True` | `true` |
| ⑦ | العدم | `None` | `null` |
| ⑧ | أسّي عند الحدود | `1e+16` | `10000000000000000` |
| ⑨ | صفر سالب | `-0.0` | `0` |

**تمييز لازم:** ⑥ و⑦ يتباعدان عند **الإكراه النصّي** لا عند **التسلسل**: `json.dumps` و`JSON.stringify` يتطابقان على `true`/`null`. فكتلة التدقيق المُسلسَلة سالمة، والخطر في `f"…{x}…"` و`` `…${x}…` ``.

**لا يُضاف صنف عاشر إلا بعد إثباته تشغيلياً.** القائمة مقيسة، وهذا ما يمنعها من التضخّم إلى شعار.

## الحارس `ح-6` — وحدّه مُصرَّح به

`guard_interp.py` يمسح مواضع الإصدار في الطرفين. كل استيفاء يجب أن يكون **مصاغاً صراحةً** أو **مسجَّلاً في `interp_registry.json` بسببٍ مكتوب**؛ وما عداه يوقف البوّابة.

> **الحدّ:** `ح-6` **كشفٌ لا برهان**. نوع المتغيّر لا يُحسم ساكناً في اللغتين، فهو **بوّابة انحدار** تمنع المستجدّ، لا إثبات خلوّ. وتقديمه كبرهان تامّ يكون «حقلاً لا يفحص» — وهو النمط المرفوض في `DEC-236` و`DEC-238`.

**استيفاء `ن-7` عليه:** ① سقط على العيب المعلوم — النمطان التاريخيان `{LENSES}` و`{round(w, 2)}` من هذه الجلسة نفسها · ② يقيس شيئاً واحداً: وجود استيفاء غير مصاغ ولا مسجَّل · ③ يمسح الطرفين بأداة واحدة فلا مسألة تكافؤ · ④ لا عتبة: أي استيفاء يتيم يوقف.

## ما لا يحلّه

| الصنف | من يحلّه |
| :--- | :--- |
| غياب بنيوي لحقل | `parity_surface` — مقارنة حقلاً بحقل |
| حارس معطَّل يعيد قيمة ثابتة | استثارة بعيب معلوم (`ن-7/①`) |
| اختلاف ترتيب الحساب | `parity_py` |

`ن-8` يعالج **الصياغة** وحدها.

