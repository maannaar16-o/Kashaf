# 57-K3-ENGINE_Operational_Specification.md
## المواصفة التشغيلية لمحرك تقارير دائرة الانفعال ($K_3$) — v2.0

# القسم 0: رأس الحوكمة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `57-K3-ENGINE-SPEC` |
| **الإصدار** | **v2.1 — ✅ معتمد** (`DEC-129` · مُصحَّح بـ`DEC-133`) · مرّت **بوابة النقد الذاتي** |
| **🔄 علّة إعادة البناء** | v1.1 بُنيت **قبل** طبقتَي الاقتران والتركيب و`URS` v3.0. وحدّها الوجودي («صفر إسناد جذر · صفر `URS`») **سقط نصفه** |
| **السند الحاكم** | `41 §3/§5.3/§7` · `13-CORE-K3 §6` · `DEC-028/062/064/076/082/090` · **`M3-06` v1.0** · `M3-07` v1.2 · **`URS` v3.0** · `62`/`64`/`65`/`67`/`68`/`71` |
| **🔒 تثبيت إصدار الأداة** | **`40 v5.0` + `41 v4.2`** — **لم يُمسّ**. أي إصدار أحدث يستوجب مطابقة جديدة قبل التشغيل |
| **🔄 قاعدة الأسبقية** (`DEC-127`/ج) | خط الأساس يحكم أولاً · **`M3-06` v1.0 يحكم في قواعد السحب حصراً** · **هذه المواصفة تحكم في التشغيل والصياغة وبنية التقرير** |
| **🔄 الحدّ الوجودي** | **عتبات مطلقة ✅ باقية · صفر مقارنات ✅ باقية · إسناد الجذر 🔄 يُعرض ويُسأل (لا يُنطق حكماً) · `URS` 🔄 مفعَّل (v3.0)** |
| **ديون موروثة مقبولة** | `DEBT-K3-EPPURITY-01` (`DEC-094`) · `GAP-Q-09` — تُقيَّد في `audit` |

---

# القسم 1: طبقة الإدخال (`L1-a`) — ⚪ **بلا تغيير**

## 1.1 عقد المدخل
| المُدخل | التعريف | القيد |
| :--- | :--- | :--- |
| **`x`** | مجموع الأوزان السداسية (1–6) لبنود الوعاء الأحد عشر (`DEC-028`)؛ المتروك = 0 | `x ∈ [(11−z), (11−z)×6]` |
| **`y`** | بنود المرحلة الأولى **المؤكِّدة** | — |
| **`z`** | بنود المرحلة الأولى **المتروكة** | — |
| **قيد إلزامي** | | **`y + z = 11`** لكل مهارة |

## 1.2 أوعية المهارات الخمس — **[معتمد حرفياً: `41 §5.3`]**
`K3-EP`: 3ب · 12أ · 20ب · 23أ · 33ب · 38أ · 51أ · 63أ · 74أ · 83أ · 88أ
`K3-IR`: 2ب · 11أ · 20أ · 35أ · 48أ · 60أ · 71أ · 80أ · 86أ · 90أ · 93أ
`K3-BI`: 2أ · 18أ · 33أ · 47أ · 59أ · 69أ · 78أ · 85أ · 89أ · 92أ · 94أ
`K3-CF`: 3أ · 11ب · 18ب · 21أ · 36أ · 50أ · 62أ · 72أ · 81أ · 87أ · 91أ
`K3-ST`: 5أ · 12ب · 21ب · 24أ · 35ب · 39أ · 47ب · 53أ · 65أ · 75أ · 84أ

## 1.3 قواعد ثابتة
**الجُمل التوضيحية** (`40 v5.0`): ما بين قوسين **لا يدخل القياس**. · **البيانات الناقصة** (`41 §7`): بند بلا اختيار → صفر في المرحلتين، ويزيد `z` — **لا اجتهاد**.

## 1.4 فحص السلامة
`y + z = 11` · اتساق `x`/`z` (`REC-01`) · اكتمال 55 خانة → **⛔ إيقاف + تقرير فجوة تشغيلية** عند أي خرق. لا إصلاح صامت.

---

# القسم 2: الحساب والتصنيف (`L1-b`) — ⚪ **بلا تغيير**

```
SS = x − 2z + y          |  SP = SS ÷ 66 × 100
MaxRaw = 66 · SS_max = 77 · SP_max ≈ 116.7%
```

**الترميز الثماني:** `OUT` (`SP<0`) · `L−` (0–20) · `L` (20–40) · `M` (40–50) · `M+` (50–70) · `H` (70–85) · `H+` (85–100) · `H++` (>100).

**التثليث الحاكم (`DEC-064`) — لغة التقرير الوحيدة:** `L−`/`L`/`M` → **حضور محدود** (🔄 **`SP < 50%`**) · `M+` → **كفاءة أساسية** (🔄 **`50–70%` — والحدّ `50.0` منها**، `DEC-133`: إزالة تداخل الحدّ) · `H`/`H+`/`H++` → **قدرة عالية** (>70%) · `OUT` → فجوة تشغيلية (§8.1).

> **🔒 قفل `ق1`:** يُحظر عرض `SP` أو الكود الثماني للمستخدم. النطاق الثلاثي هو اللغة الوحيدة؛ الرقم في `audit` فقط.
> **🔑 وهذا القفل بعينه** ما يجعل طبقة الاقتران مشروعة: نطاقات مطلقة بلا أرقام ⇒ لا مقارنة.

---

# القسم 3: `G1` — بوابة الثقة

| الشرط | الإجراء |
| :--- | :--- |
| `EP` في حضور محدود أو `OUT` | ① `audit.low_trust = true` ② لافتة الثقة ③ **كل** ما بعدها بصيغة متحفّظة — **بما فيه القراءة المركّبة** (`ح-3`) |

**🔒 لا إيقاف.** · **🔄 قفل `DEC-108`:** وَسْم الثقة **لا يعبر الدائرة** — يُحظر تعديل يقين قراءة `K2`/`K4` به.
**دَين مقبول:** `DEBT-K3-EPPURITY-01` — يُقيَّد في `audit`.

---

# القسم 4: `L3` — استدعاء مداخل المستخدم

**المرجع الحصري:** `P7` في `55-USER-K3-*`. **دائم** → التعريفية والأقفال والختامي · **`SP ≥ 50%`** → إشارات القوة · **`SP ≤ 50%`** → إشارات تحتاج انتباهاً · **حمل مرتفع/`OUT`–`L`** → «حالة الضغط القصوى».
**ترتيب العرض:** `EP → IR → BI → CF → ST` — **يطابق مسار المعالجة** (منبع → مركز → أطراف → مصبّ).
**قفل التزاوج:** وثائق موسومة الإصدار؛ أي تعديل في توأم العمق أو في السجل **يجمّد السحب**.

---

# القسم 5: `G2` — بوابة الزيف 🛡️ إلزامية

| الشرط | الإجراء |
| :--- | :--- |
| أي من `IR`/`BI`/`CF`/`ST` > 70% | **كتلة التحقق الموحّدة** — مرة واحدة — بأسئلة تلك المهارات فقط |
| لا مهارة > 70% | ⛔ لا تظهر الكتلة |
| `EP` عالية | ⛔ لا سؤال (`FR-05` معطَّل) |
| الأربع > 70% | الكتلة بأسئلتها الأربع + جملة `HF-05` |

**المصدر النصّي الملزم:** `REFLECT §1.3` — **يُنسخ حرفياً**. · **قيود:** صفر تشخيص · صفر خفض درجات · صفر لغة عيادية · **تُصدَّر بالتأكيد**.

---

# القسم 6: 🔄 الطبقات الجديدة

## 6.1 `L-CPL` — الاقتران
تُفعَّل الخلايا **عند الأقطاب فقط** (`ح-1`): 🔄 **`SP < 50%`** · `>70%` · والنطاق `50–70%` **محايد — لا سحب**. 🔄 **مهارة في `OUT` تُستبعد من الاقتران** ويُصرَّح بذلك (`C-16`).
**المصدر:** 28 خلية (`62`/`62-B`/`62-C`) · **الحدّ البنيوي:** 6 متزامنة كحدّ أقصى (10 مع `EP`).
**`EP ≤50%`** ← تُلحق **لاحقة التعاقب** بكل خلية طرفُها `IR`. · **لا قطب** ← ⛔ **لا قراءة مركّبة؛ الصمت مشروع.**

## 6.2 `L-CMP-أ` — التركيب
ترتيب بنيوي: **المحور ← الجانبي ← التحميل ← التغذية** (لا ترتيب أهمية) · **الكُلف تُجمع وصفياً لا حسابياً** · **المخرج قراءة مركّبة واحدة لا سرد خلايا** · الصياغة من **`71`** حصراً.

## 6.3 `L-CMP-ب` — التعرّف على النمط
`HF-01` · `HF-02` (بجذرين) · `HF-04` — بشروطها في `M3-06 v1.0 §2`.
**🔒 `HF-03` و`HF-05` لا يُنفَّذان هنا** — موضعهما `G1` و`G2` (منع الازدواج). · **النمط يتقدّم على خلاياه.** · شرط ناقص ← **يُقرأ جزئياً موسوماً**، ولا يُعلن تاماً.

## 6.4 `G3` — بوابة الجذر 🔄 **مرفوعة مشروطة**
**المحرك لا يُسنِد — يعرض ويسأل.** المخرج بأربعة مكوّنات **مجتمعة**؛ سقوط أحدها **يُبطله**:
① الوقائع · ② الفرضية **ومعها البديل** · ③ وَسْم «اشتقاق بنيوي غير محقّق ميدانياً» · ④ سؤال من `68` (`RQ-01…04`).
**سؤال واحد لكل تقرير** · **لا يُدمج بكتلة `G2`** · الاستجابة: تأكيد ← «مُقرَّر من الشخص» · نفي ← **تُسحب ولا تُعاد** · صمت ← معروضة لا مُسنَدة.
🔴 **الإسناد الحاسم يبقى محظوراً.**

## 6.5 `G4` — بوابة الاحتواء 🔄 **مرفوعة**
ثلاث حالات مطلقة: **قوي** (الخمس >70% — تحفّظ `HF-05` مشدَّد) · **ضعيف** (الخمس ≤50% — غير حاسمة بحكم `EP`، و`HF-04` قائم) · **مركّب** (أي تكوين آخر — جرد الحافات وكُلفها).
🔒 **محظور:** ترتيب المهارات · «أيّها ينكسر أولاً» · إسناد سببي مقارن · أي تسمية.

## 6.6 `L-DSP` — العرض
وَسْمان **اثنان فقط، غير رقميين**: **الثقة** (`G1`) · **الحمل**: «تُقرأ الصورة وأنت **تحت الحمل، لا في الراحة**».
🔒 **مانع الازدواج (`ح-9`):** وَسْم الحمل **مرة واحدة**، ولا يُعاد اشتقاق حمل منه؛ وخلايا التحميل **لا تحكم على سعة `ST`**.

---

# القسم 7: 🔄 بنية التقرير (`L7`)

| # | القسم | المصدر الملزم | الشرط |
| :--- | :--- | :--- | :--- |
| ١ | فاتحة العهد | نص ثابت | دائم |
| ٢ | خريطة الدائرة — تلاحظ ← تهدّئ ← تكبح ← تكيّف ← تحتمل | `55-USER-K3-* §0-ب` | دائم |
| ٣ | لافتة الثقة | `G1` | شرطي |
| ٤ | **خمسة أقسام مهارية** | `L3`/`P7` | دائم |
| ٥ | كتلة التحقق الموحّدة | `REFLECT §1.3` | شرطي (`G2`) |
| **٦** 🔄 | **القراءة المركّبة** — كيف تتفاعل قدراتك وأين تقع الكلفة | **`71`** · `G4` | شرطي (`L-CPL` فعّالة) |
| **٧** 🔄 | **سؤال موضع الجذر** | `68` · `REFLECT §1.4` | شرطي (`G3`) |
| ٨ | «هل هذا أنا؟» — 8 أسئلة فصل | `REFLECT §2` | دائم |
| ٩ 🔄 | **الإطار التأملي** — الاحتواء والتضخيم | `M3-04` v1.1 | دائم |
| ~~١٠~~ | ~~ختم الشفافية~~ | ⛔ محذوف من الواجهة (`DEC-092`) — في `audit` | — |

**✅ قرار ترتيبي محسوم (`ق7` · `DEC-129`):** وُضعت كتلة التحقق (٥) **قبل** القراءة المركّبة (٦) لأن التحفّظ يتعلق بالدرجات التي تقوم عليها القراءة، فيصل القارئ إليها وقد عرف حدودها. وسؤال الجذر (٧) بعدها، فيفصلهما القسم ٦ — تنفيذاً لحدّ «لا يُدمجان».
**🔄 القسم ٩:** كان «مبادئ عامة **بلا ربط بدرجاتك**» — وكان ذلك تحايلاً على غياب `URS`. الآن الربط مشروع، ويتم في القسم ٦؛ فيبقى ٩ **إطاراً عاماً** بلا تكرار.

---

# القسم 8: الحالات الخاصة

**8.1 `OUT`** — النص المعتمد (`DEC-092`) بلا تغيير: «يستهلك من طاقتك حالياً أكثر مما يخدمك… قراءة لحالتك في هذه المرحلة» + مدخل «حالة الضغط القصوى». صفر لغة أزمة أو تهويل.
**8.2 كل الدرجات في حضور محدود** → حالة `G4` **ضعيف**: جملة السياق بلا افتراض + **القراءة كلها غير حاسمة** (`EP` داخل النطاق) + لا كتلة تحقق.
**8.3 كل الدرجات في قدرة عالية** → حالة `G4` **قوي**: الكتلة بأسئلتها الأربع + جملة `HF-05`. **ولا تُقرأ حالة «قوي» طمأنينةً** — هي الأشدّ تحفّظاً.
**8.4 🔄 لا قدرة في قطب** (الخمس في النطاق الأوسط) → **لا قراءة مركّبة، ولا نصّ حالة من `G4` أيضاً** (`DEC-133` — إصلاح اتساق: كان نصّ «مركّب» يَعِد بكلفة لا وجود لها). التقرير يقف عند الأقسام الخمسة. **الصمت مشروع.**
**8.5 🔄 مهارة في `OUT`** → تُستبعد من طبقة الاقتران، **ويُصرَّح باستبعادها** بجملة `C-16`؛ ولا تختفي بلا تفسير.

---

# القسم 9: بنك الصياغة — الضمانة البنيوية

> ### 🔑 **المحرك يجمّع ولا يؤلّف.**

| المكوّن | المصدر الحصري |
| :--- | :--- |
| تعريف المهارة · وصف النطاق · الإشارات · الأقفال | `55-USER-K3-*` (`U-01` · §2 · `U-08…11`) |
| كتلة التحقق · أسئلة الفصل | `REFLECT §1.3` · `§2` — **حرفياً** |
| 🔄 **القراءة المركّبة وحالة الاحتواء والوَسْمان** | **`71` — القوالب `T-01…T-10` و§3** |
| 🔄 **سؤال الجذر** | `68` · `REFLECT §1.4` |
| جمل الوصل | **قائمة مغلقة معتمدة** — `GAP-CMP-03` |

**الأثر:** `G5` تصبح فحصاً بنيوياً شبه مضمون — المحرك **لا يستطيع** إنتاج لغة محظورة لأنه لا يؤلّف.

---

# القسم 10: `G5` — بوابة العزل · **16 فحصاً**

**السبعة المستبقاة:** سلوك ملتبس بلا سؤال فصل · ألفاظ قواميس التطهير · خلط `EP`/`K2-E` · خلط `ST`/مثابرة `K4` · نسبة «السكون بعد الحظر» لـ`BI` · ادّعاء اتجاه/قطب داخل مهارة (`DEC-076`) · صعود/هبوط لمهارة مفردة (`DEC-082`).

**🔄 التسعة الجديدة:** ترتيب المهارات أو التنبؤ بالانكسار (`DEC-115`) · إسناد سببي مقارن (`NORM-01`) · تسمية الشخص بالنمط (`65 §5`) · إسناد جذر حاسم بلا المكوّنات الأربعة (`67`) · ذكر الحمل مرتين (`ح-9`) · تنبؤ زمني (`TEMPORAL-01`) · تقديم «محايد على الحافة» أفضليةً (`ح-10`) · عبور وَسْم الثقة إلى `K2`/`K4` (`DEC-108`) · نسبة أي أثر إلى `K4` (`RSK-005`).

---

# القسم 11: المخرج

```json
{
  "report_md": "…",
  "audit": {
    "sp": {"EP":0.0,"IR":0.0,"BI":0.0,"CF":0.0,"ST":0.0},
    "codes": {"EP":"M+"}, "bands": {"EP":"core"},
    "cells_activated": ["CPL-IR.BI-WS"],
    "patterns_recognized": [{"code":"HF-01","status":"full|partial"}],
    "containment_state": "strong|weak|composite",
    "cost_map": [{"cell":"CPL-IR.BI-WS","locus":"internal|to_ST|external"}],
    "root_question": {"code":"RQ-01","response":"confirmed|denied|none","attribution_source":"person|none"},
    "gates_fired": ["G1:low_trust=false","G2:high=[IR,ST]","G3:asked","G4:composite","G5:violations=0"],
    "conditional_layers": ["G3:via-67"],
    "frozen_layers": [],
    "urs_version": "3.0",
    "entries_used": ["K3-EP-U-01","T-02","RQ-01"],
    "engine_version": "0.2",
    "instrument_pin": "40 v5.0 + 41 v4.2",
    "accepted_debts": ["DEBT-K3-EPPURITY-01 (DEC-094)"],
    "inherited_gaps": ["GAP-Q-09","GAP-CMP-02","GAP-CMP-03"],
    "transparency_note_internal": "لا مقارنة · لا إسناد جذر حاسم · قراءة مركّبة فئوية بلا تجميع رقمي"
  }
}
```
🔒 **`audit` داخلي بالكامل** — يحمل `SP` المحظور عرضه (`ق1`) وملاحظة الشفافية المحذوفة من الواجهة (`DEC-092`).

---

# القسم 12: نقاط القرار

| # | القرار | الحالة |
| :--- | :--- | :--- |
| ق1…ق6 | النطاق فقط · وضع كامل · `41 §7` · مواصفة ثم كود · نص `OUT` · أسئلة الفصل مجمَّعة | ✅ محسومة (`DEC-091/092/093`) |
| **ق7** | **ترتيب القسمين ٥ و٦** | ✅ **محسوم (`DEC-129`):** كتلة التحقق **قبل** القراءة المركّبة · **📌 الترتيب المقترح مني — يُسحب بكلمة** |

---

# القسم 13: سجل إعادة البناء — v1.1 ← v2.0

| # | v1.1 | v2.0 | السند |
| :--- | :--- | :--- | :--- |
| 1 | «صفر إسناد جذر · صفر `URS`» | **يُعرض ويُسأل · `URS` مفعَّل** | `DEC-117`/`DEC-121` |
| 2 | `frozen_layers: [G3,G4,URS]` | `[]` + `conditional_layers` | `DEC-115/117/121` |
| 3 | لا طبقة اقتران ولا تركيب ولا عرض | **6 طبقات جديدة** (§6) | `DEC-102…117` |
| 4 | وَسْم الحمل **غائب** | **مُضاف** + مانع الازدواج | `DEC-103` |
| 5 | `G5` بسبعة فحوص | **16** | `70 §4` |
| 6 | 7 أقسام في التقرير | **9** | `DEC-121`/`DEC-117` |
| 7 | القسم ٧ «بلا ربط بدرجاتك» | الربط مشروع في القسم ٦ | `DEC-121` |
| 8 | `M3-06` مسودة بأسبقية مطلقة | **v1.0 معتمدة بأسبقية محدودة النطاق** | `DEC-127`/`DEC-128` |

**⚪ المستبقى بلا مساس:** الأوعية · المعادلات · الترميز · التثليث وقفل `ق1` · `G1`/`G2` · `P7` وترتيب العرض · بنك الصياغة مبدأً · نص `OUT` · حذف ختم الشفافية · الديون المقبولة · **`instrument_pin`**.

---

# القسم 14: 🔄 مصفوفة الالتزام — كل قرار حاكم وأين يتجسد

> **منقولة من `57-Blueprint §8` محدَّثةً** بقرار `DEC-130` — الوثيقة الأصل مؤرشفة.

| القرار | تجسيده في المحرك | الحالة |
| :--- | :--- | :--- |
| `DEC-064` | `L1-b` — النطاقات الثلاثة و`OUT` **لغة التقرير الوحيدة** للدرجات | ⚪ يصمد |
| `DEC-076` | صفر ادّعاء اتجاهي داخل مهارة — **بنك الصياغة لا يحتويه أصلاً** + فحص `G5` | ⚪ يصمد |
| `DEC-090` | `G2` — كتلة واحدة موحّدة + أسئلة الفصل مجمَّعة | ⚪ يصمد |
| `DEC-072` | قفل «السكون بعد الحظر» في مداخل `BI` · **وقيد `RQ-03`: توليد البديل لا تنفيذه** | 🔄 توسّع |
| قفل التزاوج | المحرك يقرأ نسخاً **موسومة الإصدار**؛ تعديل توأم عمق أو خلية **يجمّد السحب** | ⚪ يصمد |
| `DEC-092` | ختم الشفافية **محذوف من الواجهة** → `audit.transparency_note_internal` | ⚪ يصمد |
| **`DEC-082`** | 🔄 قراءة الاحتواء الدائرية **تتم آلياً في `G4`** — ومادتها **كُلف الخلايا** لا شكل الدرجات *(كان: لا قراءة دائرية · الإطار التأملي فقط)* | 🔄 **تبدّل** |
| **`DEC-089`** | 🔄 نطاقه **ضاق**: `G3` مرفوعة مشروطة · `G4` مرفوعة · والباقي **حظر الحكم المقارن** *(كان: غياب `G3`/`G4` بنيوياً)* | 🔄 **تبدّل** |
| `DEC-101` · `DEC-121` | `URS` v3.0 يتجسد في `G4` + **القسم ٦** من التقرير | 🆕 |
| `DEC-103` | `ST` طرف في `L-CPL` · وَسْم في `L-DSP` · **مانع الازدواج** (`ح-9`) + فحص `G5` | 🆕 |
| `DEC-108` | وَسْم الثقة **لا يعبر الدائرة** — `G1` + فحص `G5` | 🆕 |
| `DEC-115` | حالات الاحتواء الثلاث في `G4` + حظر الترتيب والتنبؤ — فحص `G5` | 🆕 |
| `DEC-116` | القسم ٩ (الإطار التأملي: الاحتواء والتضخيم) + `ح-12` | 🆕 |
| `DEC-117` | `G3` بمكوّناتها الأربعة المجتمعة + حدّ السؤال الواحد | 🆕 |
| `DEC-120` | **هذه الوثيقة نفسها** مرّت ببوابة النقد قبل اعتمادها | 🆕 |
| `DEC-127` | أسبقية `M3-06` **محدودة النطاق** — §0 | 🆕 |

## 14.1 واجهة مستقبلية محجوزة

| الواجهة | الحجز |
| :--- | :--- |
| **دمج `56-REPORT-ENGINE`** (`DEC-062`) | عقد المخرج (§11) مصمَّم وحدةً قابلة للتضمين · و`audit` يُعلم المحرك الأمّ بالطبقات المشروطة والديون |

**📌 تحققت ثلاث حجوزات من أربع** كانت مرسومة في `Blueprint §7`: `L4.5` لـ`G3`/`G4` · قسم `URS` · قبول إصدار أداة أحدث (صار **المسار ب**). والرابع أعلاه هو الحيّ الوحيد.


---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| القياس | ⚪ **لم يُمسّ** — `40 v5.0` + `41 v4.2` |
| الحدّ الوجودي | 🔄 مستبدل — نصفه باقٍ ونصفه سقط |
| الطبقات | ✅ 5 بوابات + 4 طبقات |
| بنية التقرير | ✅ **9 أقسام** |
| `G5` | ✅ **16 فحصاً** |
| بنك الصياغة | ✅ «يجمّع ولا يؤلّف» — محفوظ |
| نقاط القرار | ✅ **كلها محسومة** (ق1…ق7) |
| بوابة النقد | ✅ مُطبَّقة |
| مصفوفة الالتزام | ✅ **مُستوعَبة** (§14) — `Blueprint` مؤرشف (`DEC-130`) |
| **حالة الوثيقة** | ✅ **v2.0 — معتمدة (`DEC-129`)؛ تحلّ محل v1.1 · جاهزة للترجمة إلى كود بعد سدّ `GAP-CMP-02/03`** |
