# 108-ISO-REVIVE — `DEC-236`: إحياء تدقيق العزل في JS

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | المسار **أ-1** — إحياء المسار في JS |
| **حالة الاعتماد** | **مشتق** · مسّ توقيع `run` **بإذن صريح** |
| **الأدوات** | **ثلاث عشرة** · البصمات الثلاث ثابتة |

---

# القسم 1: ما كان معطَّلاً

| النقطة | ما تمسحه | بايثون | JS |
| :--- | :--- | :---: | :---: |
| **مستوى التقرير** | النصّ المُصدَر فعلاً — **وتوقف** | ✅ | ✅ |
| **مستوى المحرّك** | نصوص المصدر المختارة للملف | ✅ | ❌ `auditIsolation([])` |

نتيجة نقطة المحرّك تُكتب حقلاً باسم `isolation` في كتلة التدقيق (`k2_report.py:413` · `reports.js:401`). فكان JS **يُبلغ `clean` دائماً** — لا لأنه فحص، بل لأنه لم يفحص.

---

# القسم 2: الإصلاح

**`engines.js`** — `run({ sp, raw, content = null, strict = false })`، و:

```js
const audit = auditIsolation(content ? content.textsFor(profile) : []);
```

**`reports.js`** — `K2_CONTENT_ADAPTER` نظيرٌ حرفي لـ`ContentPack.texts_for`:
نصّ العمى أولاً، ثم لكل عدسة غير المركز `presence` فـ`recommendation`، بترتيب مفاتيح `sp` نفسه. **والمفقود يُتجاوَز صمتاً** كما في بايثون — لا يُرفع استثناء، لأن `texts_for` يبتلع `MissingContentError`.

**فصل الطبقات محفوظ:** `engines.js` لا يعرف `PACKS`؛ يستقبل محوّلاً. التغذية مسؤولية طبقة التقرير كما في بايثون.

---

# القسم 3: البرهان — ثلاثي، لا اكتفاء بـ`clean`

| # | القياس | النتيجة |
| :--- | :--- | :--- |
| **أ** | نصوص المصدر المُغذّاة — `texts_for` ↔ `textsFor` | 56 ملفاً · **826 نصّاً** · متطابقة **ترتيباً ومحتوىً** |
| **ب** | حقل `isolation` في كتلة التدقيق | 56 تقريراً متطابقاً |
| **ج** | **عيب معلوم** (`ن-7/①`) — تلوّث مُصطنَع بخمسة ألفاظ محظورة | **5/5 مرصودة في الطرفين** · و`strict` يوقف الطرفين **بالرسالة نفسها** |

> **تطابق `clean` قبل الإصلاح كان مصادفةً لا تكافؤاً.** الحزم نظيفة، والطرف الآخر يعيد `clean` مهما كان المُدخل. البرهان أن يُغذّى الطرفان بالمُدخل نفسه، وأن يتطابقا على **مُدخل ملوَّث**.

---

# القسم 4: فراغ ثالث مسدود — `parity_isolation.py`

**كتلة التدقيق نفسها لم تكن مقارَنة قط** بين النسختين: `parity_py` يقارن المنطق، `parity_reports` يقارن النصّ. وهذا ما سمح لـ`auditIsolation([])` أن تعيش مثبَّتة على فارغ **دون أن يسقط شيء**.

## حدود التكافؤ المقيس — بعد `DEC-236`

| يُقاس | المقياس | الحجم |
| :--- | :--- | :--- |
| منطق المحرّك | `parity_py` | 125 حالة |
| نصّ التقرير | `parity_reports` | 142 تقريراً |
| أحكام المشرف | `parity_supervisor` | 429 درجة |
| رسائل الاستثناءات | `parity_messages` | 12 مسار |
| **تدقيق العزل وكتلة التدقيق** | **`parity_isolation`** | 826 نصّاً · 56 حقلاً |

---

# القسم 5: تصويب تقدير خطورة

سجّلتُ `GAP-ISO-JS-01` عند اكتشافها **🟠 عالية**. الفحص الكامل أظهر التقدير **مبالَغاً**: بوابة **مستوى التقرير** — التي تمسح النصّ المُصدَر وتوقف — **كانت متماثلة وعاملة في الطرفين**. المتباعد نقطة المحرّك وحدها.

**الخطورة الصحيحة: 🟡 متوسطة.** صُوِّب في `01-MASTER` و`02-MASTER` ولم يُترك.

---

# القسم 6: اختبار القبول

مسّ توقيع `run` لم يمسّ مخرجاً:

| | النتيجة |
| :--- | :--- |
| البصمات الثلاث | `2711c24d8155819b` · `36ae94bfd5a8b60f` · `6b324f996856eac3` — **ثابتة** |
| الأدوات | **13/13** |
| الأداتان | 12/12 بصمة · نحو سليم · **142 تقريراً مطابقاً** · **صفر تباعد في `isolation`** |

| الأداة | الحجم | `SHA` |
| :--- | ---: | :--- |
| `Kashaf_v2_FIXED_DEC-230-236.html` | 1,111,109 | `04d06e2249b00072…` |
| `Supervisor.html` | 360,456 | `1e3a074f1a77f02a…` |

---

# القسم 7: الدرس المتراكم — ثلاث فجوات من مصدر واحد

`GAP-MSG-PARITY-01` · تصيير العشريات · `GAP-ISO-JS-01` — ثلاثتها من **سبب واحد**: سطحُ نقلٍ لم يُقَس. ونجت كلّها من كل الأدوات القائمة، لأن **ما لا يُقاس لا يسقط**.

والأخطر أن اثنتين كانتا تُنتجان **تطابقاً كاذباً**:
- `isolation` يعطي `clean` في الطرفين لأن أحدهما لا يفحص
- `y+z` يتطابق على الصحيح لأن العيّنات كانت صحيحة

**التطابق على مُدخل ضيّق ليس تكافؤاً.** والترجيح قائم بأن بقية أسطح النقل غير المقيسة تحمل نظائر — والمسح المنهجي مقترح **لم يرد به أمر**.

---

**[نهاية `108-ISO-REVIVE` — `DEC-236` · `GAP-ISO-JS-01` مُغلقة]**
