# 106-SUPERVISOR-HTML — `DEC-234`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أداة المشرف بصيغة `HTML` |
| **حالة الاعتماد** | **مشتق** — نقل بتكافؤ مقيس، لا منطق جديد |
| **المخرَج** | `Supervisor.html` — 357,917 بايت · `SHA` `1eaaebb3d91066f7…` |
| **الأدوات** | **إحدى عشرة** · البصمات الثلاث ثابتة |

---

# القسم 1: النقل تكافؤٌ مقيس لا ترجمة

النواة `supervisor_core.js` نظيرٌ حرفي لـ`supervisor.py`: **ترتيب الدرجات وأسماؤها ونصوص تفاصيلها** مطابقة. أي تباعد يُجمَّد الطرفان (`DEC-200`).

## مجموعة القياس — 36 حمولة · 429 درجة

| الصنف | ما يغطّيه |
| :--- | :--- |
| **سليم** | أربع حالات $K_2$ بمختصر وبلا مختصر · أربع حالات $K_3$ |
| **إفساد المتن** | حرف مزيد · سطر نقل · مسافة ذيلية · متن أجنبي |
| **إفساد السجلّ** | بصمة مزوّرة · حقل ملزِم ناقص · حقلان ناقصان · انجراف حزمة · `sp` تالف |
| **إفساد الإعلان** | `scopes` غائبة · قيمة خاطئة · نصّ لا مصفوفة · إعلان يخالف المحتوى · مختصر بلا `audit_brief` |
| **إفساد البنية** | مخطَّط مجهول · مخطَّط `v1.1` · دائرة مجهولة · `audit` ليس كائناً · `audit` غائب · متن غائب · متن فارغ |
| **تسرّب** | `SP%` · نسبة مجرّدة |

**تُقارَن كل درجة: الاسم · النتيجة · نصّ التفصيل.** بصمة أحكام المشرف:

**`6b324f996856eac3`** — متطابقة في **ثلاثة مواضع**: بايثون · النواة المصدرية · النواة **المستخرَجة من `Supervisor.html` نفسها**.

---

# القسم 2: تدقيق الأداة

| الفحص | النتيجة |
| :--- | :---: |
| بصمات الحزم | **12/12** |
| منطقة البيانات مطابقة حرفياً لـ`packs.js` | ✅ |
| توليد التقارير داخل الأداة مقابل المرجع | **142/142** |
| قفل صفر شبكة (`CPL-08A-03` · `DEC-110`) | ✅ `fetch`·`XHR`·`WebSocket`·`EventSource`·`sendBeacon` معطَّلة |
| موارد خارجية · خطوط شبكية | **صفر** |
| فحص نحوي لكل وحدة وسكربت | ✅ |

**الناتج مولَّد لا محرَّر:** `build_supervisor_html.py` يجمع الوحدات الخمس بعرف التشييم نفسه المستعمل في `Kashaf_v2.html` (`window.RawahilX`). التحرير اليدوي يفتح باب انجراف نسخة ثانية — وهو صنف `DEF-K2-01`.

---

# القسم 3: `DEF-SUP-01` — عيب مُغلق في الأداة المختومة أمس

| الحقل | البيان |
| :--- | :--- |
| **البيان** | `supervisor.py` تحسب بصمة المتن المُسلَّم **بعد تطبيعه**، والمحرّكان يسجّلانها على المتن **الخام** |
| **كيف نجا من الاختبار** | الاختبار الذاتي مرّ **مصادفةً**: مخرج المحرك مُطبَّع أصلاً فتصادف التساوي. اختبارٌ يمرّ لسبب غير الذي بُني له |
| **الأثر** | **يخفي تغيّر النقل للمسافات بدل أن يكشفه** — وهو صنف `GAP-DATA-01` بعينه (فساد كتلة $K_4$ عبر البريد) |
| **كيف اكتُشف** | لم يظهر باختبار، بل بمقارنة **طريقة الحساب** في المحرّكين بطريقتها في الأداة أثناء التحضير للنقل |
| **الإصلاح** | ④ تجزّئ الخام · ⑤ تميّز **«مطابق بعد التطبيع»** (⇒ النقل غيّر الملف لا المحرك) من **«مختلف جوهرياً»** |
| **الحالة** | ✅ **مُغلق** |

> **الدرس:** حارسٌ مرّ لا يعني حارساً صحيحاً. `ن-7/①` يشترط الاختبار على عيب معلوم — وهذا العيب لم يكن في مجموعة العيوب المعلومة.

---

# القسم 4: `GAP-MSG-PARITY-01` — فجوة مفتوحة ومطوَّقة

| الحقل | البيان |
| :--- | :--- |
| **البيان** | نصّ `InputContractError` متباعد بين المحرّكين: `k2_engine.py:245` يعرض `['A', 'R', …]` · `engines.js:202` يعرض `A,R,…` |
| **لماذا لم يُرصد قط** | `parity_py` يقارن **اسم الاستثناء وحده** (`type(e).__name__`). نصّ الرسالة **خارج كل مقياس قائم** |
| **الأثر** | أداتان تعرضان نصّين مختلفين للمدخل التالف نفسه. **لا يمسّ تقريراً ولا بصمة** |
| **الخطورة** | 🟡 متوسطة |
| **التطويق** | أداة المشرف تُبلّغ **اسم الاستثناء وحده** («عقد المدخل مرفوض: `InputContractError`») ⇒ حكمها متكافئ. **تحييد لا إصلاح** |
| **العلاج** | توحيد الصياغة في أحد المحرّكين — **يمسّ محرّكاً محكوماً · يحتاج أمر المالك** |

## حدود التكافؤ المقيس اليوم

| يُقاس | لا يُقاس |
| :--- | :--- |
| **منطق** المحرّك (`parity_py`) | **نصوص رسائل الاستثناءات** |
| **نصّ** التقرير (`parity_reports`) | |
| **أحكام** المشرف (`parity_supervisor`) | |

`GAP-MSG-PARITY-01` أول ما ظهر من هذا الفراغ — وقد لا يكون الأخير.

---

# القسم 5: الأداة من جهة المستعمِل

- **إفلات** ملفات `JSON` (أو اختيارها، أو لصق النصّ).
- **الختم** يُعلن الحكم: `سليم` أو `منجرف` — وإطاره الداخلي **ينكسر ويزيح** عند الانجراف.
- **سجلّ الدرجات** يعرض كل درجة بنتيجتها وتفصيلها.
- **«افحص الأداة نفسها»** يبني حمولة سليمة فتمرّ، ثم يفسدها بحرف واحد فتسقط — تحقّقٌ في يد المشرف لا في وثيقة.
- الحكم موقوف كلّياً إن سقطت `verifyPacks()` عند الإقلاع.

---

# القسم 6: ما لم يُنفَّذ

| البند | الحالة |
| :--- | :--- |
| توحيد رسائل الاستثناءات بين المحرّكين | `GAP-MSG-PARITY-01` — **يحتاج أمرك** |
| إدراج نصوص الاستثناءات في نطاق `DEC-199` | **مقترح — لم يرد به أمر** |
| `GAP-RETEST-01` — وثيقة قراءة الفارق الزمني | **بناء مُقرّ · مؤجَّل** |

---

**[نهاية `106-SUPERVISOR-HTML` — `DEC-234`]**
