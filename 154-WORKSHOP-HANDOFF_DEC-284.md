# 154-WORKSHOP-HANDOFF — `DEC-284`: **طريقا تسليمٍ وحمولةٌ واحدة** — والتنزيل بلا شبكة

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | أمر المالك «**نفّذ الطريق الأول**» بعد سؤاله عن رفع الورشة على الموقع — 2026-08-15 |
| **حالة الاعتماد** | **مختوم بأمر المالك** — 2026-08-15 |
| **الأثر** | مسار الورشة يعمل **بلا خادمٍ البتّة**: المشارك يُنزّل نتيجته ويسلّمها، والمالك يُدخلها بأمرٍ قائم · والبصمات الستّ **حرفياً** والبوابة **25/25** |
| **التغيير المرافق** | `CHG-103` |
| **الحاكم** | `DEC-277` (مسار الورشة وشرطه) · `DEC-279` (السطح وقفلُه) · `DEC-250` (وعد الموقع العام) · `م-2` (سلطةٌ واحدة) |
| **الكود** | `154` — تمديد بمسوّغه: طريقُ تسليمٍ ثانٍ في سطحٍ قائم |

---

# §1 السؤال الذي أنتج القرار — ولماذا لم يكن جوابُه حوكمياً أولاً

سأل المالك: **ما المشكلة لو رُفعت الورشة على الموقع؟**

**والجواب الأول تقنيٌّ بحت يسقط قبل أي كلامٍ عن الحوكمة:** `GitHub Pages` استضافةٌ ساكنة — **لا خادمَ فيها**. وصفحة الورشة تنتهي بـ`POST /submit`، فلا يوجد شيءٌ عند ذلك المسار. ⇒ المشارك يجيب **٢٨٢ خطوة**، وتُولَّد تقاريره الثلاثة، **ثم يسقط الإرسال في آخر خطوة** — أسوأُ موضعٍ ممكنٍ للفشل.

> **فالرفع لا يعمل، لا أنه «مخالف».** وتقديمُ الحكم الحوكمي على الحقيقة التقنية كان سيُخفي السبب الحقيقي.

**وأربع مشكلاتٍ تبدأ لو أُضيف خادمٌ حقيقي** — رُفعت للمالك ولم تُنفَّذ:

| # | المشكلة |
| :--- | :--- |
| ① | **القفل نصُّه ثابت ومعناه يتغيّر بمكانه**: «الأصل نفسه» على جهازك = جهازُك، وعلى مضيفٍ عام = **ذلك المضيف ومن يبلغه**. الكلماتُ لم تتغيّر وما تسمح به اتّسع |
| ② | **الرمز يصير الحارس الوحيد** بلا حدّ معدّل — وإن كان المخزن يرفض أي رمزٍ غير مُصدَر فلا يمتلئ بنفايات |
| ③ | **الحصيلة تنتقل إلى قرص غيره** — ونصُّ الإذن المعتمد **لا يقول ذلك**، فنقلُها يستوجب تعديلَ الإذن لا نقلَ ملفّات |
| ④ | `workshop_server.py` **بُني للحلقة المحلّية**: بلا `HTTPS` ولا حدّ معدّل — **يُستبدَل لا يُنشَر** |

**والمبدأ المستخلص:** ما يجعل التصميم آمناً اليوم ليس ذكاءه بل أن **الحيازة مادّية** — والنشرُ يبادل ذلك براحةٍ في الوصول.

---

# §2 المنفَّذ — طريقٌ ثانٍ يُغني عن الخادم لا يُضعفه

| الطريق | الشرط | ما يحدث |
| :--- | :--- | :--- |
| **الإرسال** | خادم الورشة يعمل | `POST /submit` — والمشرف بوابةُ قبولٍ قبل التخزين (`DEC-279`) |
| **التنزيل** 🆕 | **لا شيء** | `Blob` محلّي ⇒ ملفٌّ واحد يسلّمه المشارك بأي وسيلة، ويُدخله المالك بـ`workshop_store.py accept ملف.json` |

**والأمر الذي يستقبله قائمٌ منذ `DEC-279`** — لم يُكتب لهذا القرار: الطريق الثاني **يستعمل بابَ دخولٍ موجوداً**، فبوابةُ المشرف تحكمه كما تحكم كل حمولة.

---

# §3 القيود الأربعة — **مقيسةٌ بأسمائها**

| # | القيد | كيف يُقاس |
| :--- | :--- | :--- |
| ① | **بانٍ واحد للطريقين** | `WP.build(` يُستدعى **مرّةً واحدة** في السطح كلّه، والطريقان يستدعيان `buildPayload()` ⇒ **لا صيغةَ تسليمٍ ثانية تنجرف** (`م-2`) |
| ② | **التنزيل بلا شبكة البتّة** | صفر `fetch`/`XHR`/`WebSocket`/`sendBeacon` في مساره — `Blob` محلّي وحده ⇒ **قفلُ الشبكة لم يُمسّ** وبقيت تسعُ محاولاتٍ مرفوضةً وواحدةٌ تمرّ |
| ③ | **اسم الملف بالرمز لا باسم** | `rawahil-workshop-<CODE>.json` — والمخزن يعرف **رمزاً وتقريراً لا اسماً** (`DEC-277 §5`)، فاسمٌ في الملف يُبطل ذلك |
| ④ | **لا يُدَّعى وصولٌ لم يقع** | شاشة الختام تقول «**حُفظت** نتيجتك» للملف و«**وصلت** نتيجتك» للإرسال — فالمنزَّل **لم يبلغ المدرّب ولم يُحكَم بعد** |

**والقيد ④ ليس تجميلاً:** «وصلت» على ملفٍّ في هاتف المشارك **خبرٌ كاذب** — يظنّ أنه أدّى ما عليه فلا يسلّمه، فتضيع نتيجتُه بلا أن يعلم أحد.

---

# §4 البرهان — **بالتشغيل في متصفحٍ حقيقي بلا خادم**

فُتحت الصفحة من `file://` — **لا خادمَ في المعادلة أصلاً**:

| المقياس | الملاحَظ |
| :--- | :--- |
| الشاشة | «اكتمل الاستبيان» · زرّان: «أرسل إلى مدرّب الورشة» · «نزّل نتيجتك لتسلّمها بنفسك» |
| القفل حيّاً | `fetch` إلى `https://example.com` **مرفوض** — والصفحة على `file://` |
| الملف | `rawahil-workshop-ETDQ-EAAR.json` · `40,928` بايت |
| الختام | «**حُفظت** نتيجتك — سلّمه لمدرّبك ليُدخله» |
| الشاشة | **صفر `SP%`** · **صفر خطأ طرفية** |
| الإدخال | `accept` ⇒ «✅ مقبول ومخزَّن» · واللوحة تعرضه بدوائره الثلاث نظيفةً |
| **وملفٌّ معطوب** | ❌ **مردود** — «تقرير ساقط في التدقيق: البصمة تصف المُسلَّم» |

> **فبوابةُ القبول لم تُخترق بفتح الطريق الثاني** — تُقاس على الملف كما تُقاس على الطلب.

---

# §5 الحرّاس مُختبَرون على فشلهم — ثلاث حقنات

| الحقنة | النتيجة |
| :--- | :--- |
| صيغةُ تسليمٍ ثانية تُبنى في التنزيل | ❌ **رُصد** — «٢ استدعاءً للباني» |
| التنزيل يرسل نسخةً عبر الشبكة أيضاً | ❌ **رُصد** — `['fetch(']` |
| الختام يدّعي وصولاً لم يقع | ❌ **رُصد** — «لا يفرّق بين المُرسَل والمحفوظ» |

---

# §6 ما لم يُمسّ — **وما لم يُنشَر**

- ❌ **قفل الشبكة** — `CPL-WS-01` كما هو: تسعُ محاولاتٍ مرفوضة وواحدةٌ تمرّ، **والتنزيل ليس منها أصلاً**.
- ❌ **طبقة القياس** · **المحرّكات والتقارير** · **البصمات الستّ حرفياً**.
- ❌ **الموقع العام** — ووعدُ تذييله وقفلُ صفر الشبكة فيه.
- ❌ **`Workshop.html` لم تُرفع إلى `docs/`**: صارت **قابلةً** للرفع لأنها لم تعد تحتاج `/submit`، **والرفع أمرٌ مستقل** لم يصدر. ووضعُ صفحةٍ تقول «نتيجتك تصل مدرّبك» على أصل صفحةٍ تقول «إجاباتك لا تغادر جهازك` يستحق قراره الخاص.

---

# §7 برهان القبول

| المقياس | النتيجة |
| :--- | :--- |
| الطريق الثاني | ✅ **يعمل بلا خادمٍ البتّة** — مُثبَتٌ من `file://` |
| بوابة القبول | ✅ **تحكم الملف** — والمعطوب مردود |
| القيود الأربعة | ✅ **مقيسةٌ بأسمائها** |
| الحرّاس | ✅ **ثلاث حقنات · ثلاث إصابات** |
| البصمات | ✅ **ستٌّ حرفياً** |
| الأدوات | **25/25** |

---

**[نهاية `154-WORKSHOP-HANDOFF` — `DEC-284` · الترقيم التالي `DEC-285` · `CHG-103`]**
