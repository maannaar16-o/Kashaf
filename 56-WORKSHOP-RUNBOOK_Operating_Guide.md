# 56-WORKSHOP-RUNBOOK — دليل تشغيل الورشة

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `56-WORKSHOP-RUNBOOK` — دليل تشغيلٍ للمالك |
| **الإصدار** | v1.0 ← **v1.1** — 2026-08-15 · أُضيف `§10` (قناة الإسهام) |
| **السند** | `DEC-254` (قناة الإسهام) · `DEC-277` (المسار) · `DEC-279` (السطح والخادم) · `DEC-280` (اللوحة) · `DEC-284` (التسليم بالملف) · `DEC-285` (النشر) · `DEC-286` (حقوق صاحب البيان) |
| **الوظيفة** | **ما يُفعل، بالترتيب** — لا شرحُ لماذا؛ لماذا في وثائق القرارات |

> **ولماذا وثيقة:** خطواتُ التشغيل كانت تعيش في محادثة، والمحادثة تموت. **وما لا يُكتب في المستودع لا ينجو من الجلسة.**

---

# §1 التهيئة — مرّةً واحدة

مسارُ الورشة يحتاج **`python3` وحده**: صفر تثبيتٍ للحزم، **ولا `node`** (يلزم `node` للبوابة الكاملة لا للورشة).

```bash
git clone --depth 1 https://github.com/maannaar16-o/Kashaf.git
cd Kashaf
```

**على هاتف أندرويد:** `Termux` **من F-Droid** لا من متجر Play (نسخةُ المتجر مهجورة)، ثم `pkg install python git`.

---

# §2 قبل كل ورشة

```bash
python3 workshop_store.py issue "اسم المشارك"    # لكلٍّ من أفراد الفوج
python3 workshop_store.py codes                  # اطبع القائمة واحتفظ بها
```

**الرمز هو الهوية**: تعطيه للمشارك، وتحتفظ أنت بالربط. ولا كلمةَ مرور — لا تُطلب ولا تُقبل.

---

# §3 التشغيل — ثلاثة أوضاع، اختر واحداً

| الوضع | متى | كيف |
| :--- | :--- | :--- |
| **أ · بالرابط المنشور** | الأبسط لأجهزتهم | أعطهم `https://maannaar16-o.github.io/Kashaf/workshop.html` — يجيبون، **ينزّلون ملفاً**، ويرسلونه إليك |
| **ب · بجهازك وحده** | جلسةٌ واحدة تلو الأخرى | `python3 build_workshop_html.py` ثم `python3 workshop_server.py`، وافتح `http://127.0.0.1:8787/` |
| **ج · بشبكة القاعة** | أربعتهم معاً على هواتفهم | `python3 workshop_server.py --host 0.0.0.0` وأعطهم `http://<عنوانك>:8787/` |

**والوضع ج يطبع تحذيرَه** — تفتحُه عالماً بأنك فتحتَه.

⚠️ **في الوضع ج:** التقدّم ينجو من تحديث الصفحة **ولا ينجو من إغلاق التبويب**. فانقطاعُ الشبكة في منتصف ٢٨٢ خطوة يُفقد ما مضى.

---

# §4 الاستقبال

| الحالة | الأمر |
| :--- | :--- |
| وصل ملفٌّ من مشارك (الوضع أ) | `python3 workshop_store.py accept ملف.json` |
| أجاب على خادمك (ب/ج) | **لا شيء** — استُقبل وحُكم وخُزِّن |

**والمشرف بوابةُ قبولٍ لا فحصاً لاحقاً**: تقريرٌ يسقط في التدقيق **يُردّ ولا يُخزَّن**. فرسالةُ ❌ تعني أن شيئاً في الملف لا يطابق كتلةَ تدقيقه — **لا تُصلحه يدوياً**؛ أعِد التشغيل للمشارك.

---

# §5 بعد الورشة

```bash
python3 owner_console.py list          # من وصل، وحكمُ المشرف على كلٍّ
python3 owner_console.py show <رمز>    # الخام بدوائره الثلاث
python3 owner_console.py dist          # توزيع الفوج — وصفٌ لا معيار
python3 owner_console.py team          # خريطة التغطية والتكامل (عدسات K2)
python3 owner_console.py voids         # حدودُ اللوحة — اقرأها مرّةً على الأقل
```

---

# §6 حقوق صاحب البيان — **تُنفَّذ بأمرٍ لا بتحرير ملفّات**

```bash
python3 workshop_store.py export <رمز> [ملف.json]   # يسلّمه نسختَه
python3 workshop_store.py forget <رمز>              # يمحو السجلَّ وقيدَ الاسم معاً
```

**والمحو كاملٌ أو يُردّ**: لا يترك السجلَّ بلا اسم ولا الاسمَ بلا سجلّ.

---

# §7 أين تعيش البيانات — **وما ينبغي أن تنسخه**

| الملف | ما فيه | يدخل المستودع؟ |
| :--- | :--- | :---: |
| `workshop_data/` | التقارير **بالرمز، بلا اسم** | ❌ |
| `workshop_codes.json` | **الرمز ↔ الاسم** | ❌ |
| `Workshop.html` | الصفحة المولَّدة | ❌ |

**الاثنان الأولان يموتان مع جهازك إن لم تنسخهما.** وانسخهما **منفصلين**: نسخةُ الحصيلة وحدها **لا تحمل اسماً** (`DEC-286`) — فهي ما تشاركه إن احتجت، والسجلُّ يبقى عندك.

---

# §8 حدودٌ تُقرأ مرّةً — ثم تُذكر عند كل إغراء

| الحدّ | البيان |
| :--- | :--- |
| **لا فارقٌ زمني** | قياسان لفردٍ لا يُطرح أحدهما من الآخر (`DEC-244`) — نافذٌ مهما تكرّر القياس |
| **لا معيارَ من فوج** | التوزيع يصف فوجك ولا يصير عتبةً ولا رتبةً لفرد (`DEBT-K3-NORM-01`) |
| **لا اختبارَ تمييزٍ بأربعة** | ومن يدخل خريطة الفريق **يحترق كمادّةٍ له** — الاختيار لكل فوج (`DEC-277 §6`) |
| **لا حكم كفاءة** | «أداة تخادم وتشكيل، لا أداة تقييم أداء أو قرار توظيفي» — تُطبع في رأس كل تقرير فريق |

---

# §9 إذا تعثّر شيء

| العَرَض | الفعل |
| :--- | :--- |
| `Cannot find module './packs.js'` | `python3 build_packs.py` |
| صفحة الورشة لا تُبنى | شغّل `python3 gate.py` — يقول أين الخلل |
| ملفٌّ يُردّ في `accept` | تغيّر بعد توليده — أعِد التشغيل للمشارك، **ولا تُصلحه** |
| `workshop_server` لا يبدأ | المنفذ مشغول: `--port 8788` |
| اللوحة تقول «خامُه محجوب» | إذنُ ذلك السجلّ لا يطابق النصّ المعتمد — **وهذا مقصود** |

---

# §10 قناة الإسهام العام — **قناةٌ أخرى غير الورشة**

⚠️ **لا تخلطها بالورشة.** الفرقُ جوهريّ:

| | **الورشة** | **الإسهام العام** |
| :--- | :--- | :--- |
| من يجيب | فردٌ تعرفه، برمزٍ تُصدره | **أيُّ زائرٍ للموقع** |
| ما يصلك | تقاريره الثلاثة كاملةً بأرقامها | **٩٤ إجابة + إصدار الأداة + شهر الإسهام — لا غير** |
| الهوية | رمزٌ تربطه أنت باسم | **صفر هوية** — لا اسم ولا مُعرِّف ولا عنوان شبكة |
| الغرض | قراءةٌ فردية وخريطةُ فريق | **اختبار الأداة نفسها ميدانياً** |

## ① التهيئة — مرّةً واحدة

القيم الثلاث من لوحة `Cloudflare` التي نُشر عليها المستقبِل:

| المتغيّر | من أين |
| :--- | :--- |
| `CF_ACCOUNT_ID` | **Workers & Pages** — أو من الرابط: `dash.cloudflare.com/<المعرّف>/workers` |
| `CF_KV_NAMESPACE_ID` | **Storage & Databases → KV** → المخزن `CONTRIB` → `Namespace ID` |
| `CF_API_TOKEN` | **My Profile → API Tokens → Create Custom Token**: `Account` · `Workers KV Storage` · **`Read`** |

⚠️ **`Read` لا `Edit`** — الأداة تقرأ ولا تكتب، ومفتاحٌ أوسع يمنح صلاحيةً لا يحتاجها أحد.

## ② السحب والتحليل

```bash
export CF_API_TOKEN='…' CF_ACCOUNT_ID='…' CF_KV_NAMESPACE_ID='…'
python3 contrib_pull.py        # الدفعة + الشواذ
python3 contrib_analyze.py     # القراءة الأولية على أربعة محاور
```

**و`export` يعيش في الجلسة وحدها** — أعِدها بعد كل إغلاق، **ولا تضعها في ملفٍّ داخل المستودع**.

## ③ المخرَجات — وكلُّها مُسقَطة

`contrib_batch.json` · `contrib_anomalies.json` · `contrib_report.md` · `contrib_report.json` — **بياناتٌ ميدانية لا وثائقُ معرفة**، فلا تدخل المستودع.

## ④ ما تفعله الأداة وما **لا** تفعله

| ✅ تفعل | ❌ لا تفعل |
| :--- | :--- |
| تصف وترتّب على أربعة محاور | **لا تحكم** — ولا عتبةً تخترع (`ن-7/④`) |
| تعزل السجلّ المخالف **وتبلّغ** | **لا تُصلحه ولا تُسقطه صمتاً** |
| تطبع حدودها المعلَنة في التقرير | **ولا تدّعي صدقاً** للأداة (`DEC-246`) |

## ⑤ إذا تعثّر شيء

| العَرَض | الفعل |
| :--- | :--- |
| «متغيرات البيئة ناقصة» | لم تُصدَّر الثلاثة — أعِد `export` |
| القناة فارغة | **ليس عطلاً**: الأداة تنتظر ناساً لا كوداً |
| سجلٌّ في `contrib_anomalies.json` | **لا تُصلحه** — هو شاهدٌ على شيءٍ يستحق النظر |
| شككتَ في المحلّل | `python3 contrib_analyze.py --self-test` — سبعةُ فحوص بلا بيانات حقيقية |

---

**[نهاية `56-WORKSHOP-RUNBOOK` v1.1 — يُحدَّث مع كل قرارٍ يمسّ التشغيل]**
