# 87-PARITY — مواصفة تكافؤ النسختين (`DEC-199`)

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **معتمد** (`DEC-199` — الشرط ③ في `ن-7`) · **مقيس** (§2 — مسح تجريبي كامل) · **مشتق** (§3–§5) |
| **نطاق الاستدلال** | `k2_engine §MAX_RAW=42` · `k3_engine §MAX_RAW=66` · `00-PATCH_CRITIQUE-GATE` v1.2 |
| **القرار المؤسِّس** | **قبول الازدواج** بين بايثون و JS، مشروطاً بتطابق المخرج على مجموعة الانحدار نفسها · **وعند الاختلاف يُجمَّد الطرفان** (`DEC-200`) |
| **الإصدار** | **v1.2** — قاعدة التحكيم (`DEC-200`) + نطاق التجميد وقيوده الأربعة (`DEC-202`) |

---

# القسم 1: نصّ الشرط ③ في `ن-7`

> **تكافؤ النسخ** — متى نُفِّذ المنطق نفسه في بيئتين (بايثون · JS)، **يُشترط تطابق مخرج النسختين على مجموعة الانحدار نفسها** قبل اعتماد أيّهما، ويُعاد الفحص عند كل تعديل في أيّ منهما.

**الأثر البنيوي:** الازدواج مقبول، لكن **التباعد الصامت ممنوع**. النسخة الثانية ليست نسخة موازية حرة، بل **مرآة ملزَمة**.

---

# القسم 2: مسح المخاطر — مقيس لا مُقدَّر

أُجري مسح تجريبي على **كامل نطاق `SS`** في المحركين (`−42…+42` و`−66…+66` = 218 قيمة):

| الخطر المفترض | الاختبار | النتيجة |
| :--- | :--- | :---: |
| اختلاف حساب الفاصلة العائمة | مقارنة تمثيل IEEE-754 بتّاً ببتّ | ✅ **218/218 متطابقة** |
| اختلاف التقريب `round(v,1)` | بايثون ضد `Math.round(v*10)/10` | ✅ **صفر اختلاف** |
| اختلاف التقريب — النمط الثاني | بايثون ضد `Number(v.toFixed(1))` | ✅ **صفر اختلاف** |

> **الخلاصة المقيسة:** نطاق `SP` في الدائرتين **خالٍ من مواضع التقريب الحرجة**. الحدّ `50.0` (`DEC-133`) آمن في الاتجاهين. هذه نتيجة **مقيسة لا مفترضة** — ولولا القياس لبقيت المخاطرة مُقدَّرة بلا سند.

---

# القسم 3: 🔴 الخطر الحقيقي المرصود — تنسيق الأرقام

المسح كشف **تبايناً فعلياً** في موضع آخر:

| القيمة | بايثون | JS |
| :--- | :--- | :--- |
| `str(50.0)` | `"50.0"` | `"50"` |
| `json.dumps(50.0)` | `50.0` | `50` |

**الأثر:** لا يمسّ التقرير المُسلَّم (`SP` محذوف بـ`DEC-183`)، لكنه **يكسر أي مقارنة بصمة (hash)** على حقل `audit` — وهو بالضبط ما يعتمد عليه الشرط ③. ويمسّ **أداة المشرف** المستقبلية التي تعرض `audit` كاملاً.

**⇒ التطبيع إلزامي قبل المقارنة** (§4).

---

# القسم 4: مواصفة المقارنة

| البند | القاعدة |
| :--- | :--- |
| **ما يُقارَن** | ① نصّ التقرير المُصدَر كاملاً ② حقل `audit` مُقنَّناً |
| **تطبيع النصّ** | `\n` موحَّدة · حذف الفراغ الذيلي لكل سطر · حذف الأسطر الفارغة الطرفية |
| **تطبيع `audit`** | مفاتيح مرتَّبة تصاعدياً (`sort_keys`) · **الأعداد العشرية تُسلسَل نصّاً بمنزلة واحدة ثابتة** (`50.0` لا `50`) · بلا مسافات زائدة |
| **الترتيب** | الفرز بنقطة الترميز (code point) حصراً — **يُحظر** `localeCompare` وأي فرز حسّاس للغة |
| **البصمة** | `SHA-256` على النصّ المطبَّع · تُخزَّن في `golden_*` بجانب الحالة |
| **معيار النجاح** | **تطابق تام** — لا هامش تسامح |

---

# القسم 4-ب: قاعدة التحكيم عند الاختلاف — `DEC-200`

> **عند تباعد النسختين: يُجمَّد الطرفان معاً حتى يُحسم أيّهما يوافق خط الأساس.**
> **لا أسبقية لبايثون بحكم الأقدمية.** المرجع هو الوثيقة، لا التنفيذ الأسبق.

## السند التجريبي — ليس احتياطاً نظرياً

`DEF-K2-01` **دليل قاطع على أن بايثون يخطئ**: استدعى المداخل بالرقم خلافاً لـ`51-MATRIX-06 §3.1/R3`، وأنتج **٦ من ٨** بصمات تحت عناوين لا تخصّها، ومرّ على **أربعة تقارير**.

⇒ لو حكمت قاعدة «بايثون هو المرجع»، لكان النقل إلى JS **ترسيخاً للعيب** ولأصبح التطابق مع الخطأ **شهادة سلامة**.

## تقرير التباعد — صنف مخرج جديد

كل تباعد يُصدر تقريراً بالحقول التالية (نظير تقريرَي الفجوة والتعارض):

| الحقل | البيان |
| :--- | :--- |
| موقع التباعد | الوحدة · الدالة · الحالة من مجموعة الانحدار |
| مخرج بايثون | القيمة/النصّ |
| مخرج JS | القيمة/النصّ |
| **السند الحاكم** | الوثيقة والبند الذي يفصل — **أو تصريح بغيابه** |
| الحكم | أيّهما يوافق السند · أم **كلاهما يخالف** |
| القرار | تصحيح الطرف المخالف · أم تجميد الاثنين |
| المطلوب | قرار مطوّر البيانات عند صمت السند |

## سلّم الحسم

| الحالة | الإجراء |
| :--- | :--- |
| السند يفصل بوضوح | يُصحَّح المخالف · يُرفع التجميد |
| **كلاهما يخالف السند** | يُصحَّحان معاً — التطابق ليس المعيار، **الموافقة هي المعيار** |
| **السند صامت** | 🔴 **فجوة سياقية** — يبقى التجميد قائماً حتى قرار المالك (البند 11/14) |

## نطاق التجميد عند صمت السند — `DEC-202`

> **تُجمَّد النقطة المتباعدة وحدها، لا المنتج.**

وهذا **مشروط بأربعة قيود** تمنع تحوّله إلى باب إصدارٍ ناقص:

| # | الشرط | السبب |
| :--- | :--- | :--- |
| ① | **قابلية العزل** — التباعد في **طبقة المراجعة** (`audit`) لا في **طبقة التسليم** | تباعد في نصّ يقرأه المستفيد = تقريران مختلفان ببيئتين ⇒ **يُجمَّد المنتج**. والفصل قائم أصلاً بـ`ق1` و`DEC-183` |
| ② | **التسجيل** — تُقيَّد في `frozen_layers` داخل `audit` | الحقل **منصوص في `57 §audit`** — لا آلية مستحدثة |
| ③ | **التصريح** — أداة التكافؤ تستبعدها من المقارنة **وتطبعها في كل تشغيل** | التجميد الصامت أسوأ من التباعد |
| ④ | **المراجعة الدورية** — تُراجَع عند **كل إغلاق جلسة** | لا تسقط بالتقادم · ولا سقف عددي مخترَع |

### إنفاذ الشرط ③ — سجل التجميد داخل البصمة

سجلّ النقاط المجمَّدة **يدخل في حساب البصمة الشاملة**، فلا يُوسَّع صامتاً:

| الحالة | البصمة الشاملة |
| :--- | :--- |
| سجل فارغ | `52b88a9ab5ffeafe` |
| نقطة واحدة مضافة | `959539984dff5dea` |

⇒ إضافة نقطة مجمَّدة **تُغيّر بصمة النظام كله** وتظهر فوراً في أي مقارنة لاحقة.

**الحالة الراهنة:** ✅ **السجل فارغ** — لا نقطة مجمَّدة قائمة. الآلية **وقائية**، بُنيت قبل الحاجة إليها.

> **تمييز لازم:** فجوة `G3` (`88-PORT-01 §3.6`) **ليست** من هذا الصنف. النسختان **متطابقتان** فيها، والخلاف بينهما معاً وبين السند — أي **مطابقة سند** لا **تباعد نسخ**. تُحسم بـ`DEC-203` لا بهذه القاعدة.
---

# القسم 5: نطاق مجموعة الانحدار

| الطبقة | الحالات | المصدر |
| :--- | :--- | :--- |
| $K_2$ حقيقية | 4 | `golden_k2` |
| $K_2$ تركيبية | 8 | مركز واحد لكل عدسة |
| $K_3$ حقيقية | 6 | `golden_k3` |
| **حدود إلزامية** | `SP = 49.9` · `50.0` · `70.0` · `70.1` لكل مهارة | `DEC-133` · `DEC-184` |
| **عقد المدخل** | حالة `y+z ≠ 11` — **يجب أن ترفع الخطأ نفسه في النسختين** | `41 §7` |
| **الحارسان** | `validate_slots` · `ص-1…ص-6` — **يجب أن يُخرجا القائمة نفسها** | `ن-7/②` |

> **بند حاسم:** التكافؤ يشمل **سلوك الفشل** لا النجاح فقط. نسخة تنجح حيث تتوقف الأخرى = تباعد صامت من الصنف الذي يمنعه الشرط ③.

---

# القسم 6: مواضع التباعد المرشَّحة — للفحص عند البناء

| # | الموضع | السبب |
| :--- | :--- | :--- |
| 1 | التعبير النمطي `_ALT_RE` (`DEC-197/ج`) | سلوك `\s` و الحرف العربي يختلف بين محرّكَي `re` و JS |
| 2 | مطابقة العناوين بالكلمة المفتاحية (`SLOT_KW`) | تطبيع Unicode: `NFC` مقابل `NFD` في الهمزات والتشكيل |
| 3 | ترتيب مفاتيح الحزم | `sorted()` مقابل `Object.keys()` — الأخير غير مضمون للمفاتيح الرقمية |
| 4 | كسر التعادل عند تساوي `SP` | استقرار الفرز مضمون في الطرفين، لكن **الترتيب الابتدائي** يجب أن يُبنى بالطريقة نفسها |

**كلها قابلة للفحص آلياً** ضمن أداة التكافؤ — ولا يُعتمد أي منها بالافتراض.

---

# القسم 7: المطلوب عند بناء المرحلة ①

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | `parity_harness` — يُشغّل النسختين على المجموعة نفسها ويقارن البصمات | ⬜ |
| 2 | إضافة حقل `hash` إلى `golden_k2` و`golden_k3` | ⬜ |
| 3 | إدراج حالات الحدود الأربع لكل مهارة/عدسة | ⬜ |
| 4 | فحص تكافؤ **سلوك الفشل** (عقد المدخل · الحارسان) | ⬜ |
| 6 | قالب **تقرير التباعد** (§4-ب) داخل أداة التكافؤ | ✅ |
| 7 | سجل التجميد `parity_frozen.json` داخل البصمة | ✅ |
| 5 | مسح مواضع §6 الأربعة | ⬜ |

---

# ملحق: سجل القرارات

| الكود | البند | الحالة |
| :--- | :--- | :---: |
| `DEC-194` | حارس $K_3$ + شرط «يُختبَر على عيب معلوم» | ✅ |
| `DEC-196` | `BAND_LABEL["OUT"]` صريح + إيقاف عند المجهول | ✅ |
| `DEC-197/ج` | الاسم المزدوج في العنوان متى اختلف الاسمان | ✅ |
| `DEC-198` | «كل فحص يقيس شيئاً واحداً» | ✅ |
| `DEC-199` | قبول الازدواج + شرط تكافؤ النسختين | ✅ |
| `DEC-200` | تجميد الطرفين عند التباعد — لا أسبقية لبايثون · المرجع هو خط الأساس | ✅ |
| `DEC-201` | إصلاح `DEF-K3-02` — `gates_fired` يُبنى مبنيناً في المصدر | ✅ |
| **`DEC-202`** | **عند صمت السند: تُجمَّد النقطة وحدها بأربعة قيود** | ✅ |

**الترقيم التالي:** `DEC-204`.

---

> **[نهاية الوثيقة — `87-PARITY` v1.2 · الشرط ③ نافذ · أداة التكافؤ تُبنى مع المرحلة ①]**
