# 66-K3-STEP4_Resolution_Dossier.md
## ملف حسم الخطوة ④ — تمهيد `URS` v3.0

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `66-K3-STEP4-RESOLUTION` |
| **الإصدار** | **v0.1 — 🟡 قيد البناء التدريجي** (بند واحد محسوم من أربعة) |
| **السند** | `56-K3-URS` v2.1 (`DEC-073/078/082/083`) · `63-URS-REFRAME` (`DEC-101`) · `64` v1.1 · `65` · سجل الاقتران 28/28 |
| **الحدّ الحاكم** | **لا تُنتج هذه الوثيقة بصمة.** تحسم الشروط فقط · صفر أوزان وعتبات مستحدثة · طبقة القياس لا تُمسّ |

## لوحة التقدّم

| # | البند | الحالة |
| :--- | :--- | :--- |
| **1** | **حسم `GAP-URS-01`** (صيغة التجميع/العتبات) | ✅ **معتمد — `DEC-113`** |
| **2** | **تحديد المتبقي من `DEBT-K3-NORM-01` على `URS`** | ✅ **معتمد — `DEC-114`** · 🔴 `CON-03` مجمَّد |
| **3** | **إعادة صوغ الحالة «متفاوت» → «مركّب»** | ✅ **معتمد — `DEC-115`** |
| **4** | **مراجعة `DEC-087`** | ✅ **معتمد — `DEC-116`** (الخيار أ: `ح-12`) |

---

# القسم 1: حسم `GAP-URS-01`

## 1.1 نص الفجوة كما هو في خط الأساس

> **`GAP-URS-01`** — «لا صيغة تجميع/عتبات رقمية لقراءة **شكل التوازن** من الدرجات الخمس. **لم تُخترع.**» الحالة: مفتوحة (بنيوية). المطلوب: قرار قياسي عند بناء المحرك. *(`56-K3-URS §8`)*

**ملاحظة حاسمة:** الفجوة **ليست نقص معلومة** — هي **اقتضاء بنيوي**: بنية v2.1 (ملف قوة خماسي) تطلب استخراج «شكل» من خمسة أرقام، وذلك يستحيل بلا صيغة أو عتبات.

## 1.2 أثر `DEC-101` على شرط نشوء الفجوة

| المرحلة | في v2.1 | بعد `DEC-101` |
| :--- | :--- | :--- |
| من الدرجة إلى الحالة | تحتاج **عتبات** | ✅ عتبات `DEC-064` **المعتمدة** (`≤50` · `50–70` · `>70`) — لا مستحدثة |
| من الحالات إلى «الشكل» | تحتاج **صيغة تجميع** | ✅ **لا تجميع**: الشكل = **مجموعة الخلايا المفعَّلة** + الأنماط المتعرَّف عليها (`65`) |
| المخرج | مقدار/شكل مركّب | **قراءة مركّبة واحدة** بلا مقدار (`64 §4.2`) |

> ### 🔑 النتيجة: الفجوة **تنحلّ لا تُحلّ**
> لا يُخترع لها حلّ، بل **يسقط شرط نشوئها**: البصمة لم تعد مقداراً مركّباً يُحسب، بل **مجموعة فئوية تُقرأ**. ومجموعة لا تحتاج صيغة جمع.

## 1.3 تدقيق البقايا — أين كانت v2.1 تحتاج تجميعاً فعلاً؟

| الموضع | الحاجة في v2.1 | الحالة بعد الطبقات الجديدة |
| :--- | :--- | :--- |
| §2 الجدول الخماسي | مُدخَلات فقط | ✅ لا حاجة |
| §4 طبقة الثقة (`EP`) | وَسْم غير رقمي | ✅ لا حاجة |
| §5 مُعامل الحمل (`ST`) | كان يوحي بمُعامل حسابي | ✅ حُسم بـ`DEC-103`: **وَسْم إطاري واحد غير رقمي** |
| **§2.1 حالة الملف: قوي / ضعيف / متفاوت** | 🔴 **يحتاج قاعدة تصنيف للملف كله** | 🔴 **البقية الوحيدة** |

**الخلاصة الدقيقة:** `GAP-URS-01` تنحلّ في **ثلاثة مواضع من أربعة**، ويبقى لها **موضع واحد**: تصنيف الملف الثلاثي في `§2.1`. وهو بعينه البند 3 من هذه الخطوة.

## 1.4 مسار إغلاق الموضع المتبقي **[مشتق — يُنفَّذ في البند 3]**

`63 §5` أثبت الاتجاه سلفاً: قراءة الاحتواء (`DEC-082`) تبقى الجسر الشرعي، لكن **مادتها تصير الكُلف المعلَنة في الخلايا** بدل استنتاجها من شكل الدرجات. وكل خلية في السجل تحمل الحقل ⑤ (الكلفة) منصوصاً. فالتصنيف الثلاثي يُستبدل بقراءة **موضع الكلفة ونوعها**، لا بحساب على الدرجات.

**⇒ الإغلاق التام لـ`GAP-URS-01` مشروط بإنجاز البند 3.**

## 1.5 أثر الحسم على شرطَي البناء (`DEC-083`)

| الشرط | الحالة |
| :--- | :--- |
| ① اكتمال `52-MATRIX-K3` | ✅ مستوفى (`M3-01…M3-07` معتمدة · `M3-02` مدموجة بـ`DEC-088`) — وتجاوزها سجل الاقتران 28/28 |
| ② حسم `GAP-URS-01` | ✅ **مستوفى بهذا القرار** (بشرط البند 3) |

> **⇒ حاجب `DEC-083` على بناء `URS` يسقط.**
> 🔴 **ولا يعني ذلك رفع الحجب:** يبقى حاجب مستقل — **المتبقي من `DEBT-K3-NORM-01`** (البند 2). `URS` التنفيذي **ما زال محجوباً** حتى يُحسم.

## 1.6 ما لا يُدَّعى بهذا الحسم

1. ❌ لا يُدَّعى إنتاج صيغة تجميع أو عتبة — **لم يُخترع شيء**؛ سقط الاقتضاء.
2. ❌ لا يُدَّعى أن `URS` صار قابلاً للبناء — حاجب `NORM-01` قائم.
3. ❌ لا يُدَّعى إغلاق `GAP-URS-01` تاماً — إغلاقها مشروط بالبند 3.
4. ❌ لا مساس بـ`DEP-URS-A` (القراءة الاتجاهية داخل المهارة) — تبقى مؤجَّلة إلى الخيار A، ولا تفتحها هذه الطبقات.
5. ❌ لا مساس بطبقة القياس ولا بـ`41-MEASURE`.

## 1.7 نص القرار المقترح

> **`DEC-113` (مقترح):** `GAP-URS-01` **تنحلّ بنيوياً** بأثر `DEC-101`: البصمة مجموعة فئوية لا مقدار مركّب، فلا تقتضي صيغة تجميع، وعتباتها هي عتبات `DEC-064` المعتمدة. تُغلق الفجوة في ثلاثة مواضع، ويبقى موضع `URS §2.1` مرهوناً بالبند 3. **ويُعلن استيفاء شرطَي `DEC-083`** — مع بقاء `URS` محجوباً بحاجب `NORM-01` المستقل.

---

# القسم 2: البند 2 — المتبقي من `DEBT-K3-NORM-01` على `URS`

## 2.1 نص الحجب كما هو في خط الأساس

> `URS` «ملف قوة» يقرأ **شكل** الملف؛ **والشكل مقارنة**. فالحاجب ليس `GAP-URS-01` (عتبات) بل `DEBT-K3-NORM-01` (تقنين). *(`52-MATRIX-K3-FREEZE §1.2`)*

**علّة الحجب إذن محدَّدة بدقة:** لأن قراءة الشكل تقتضي **مقارنة بين مهارتين**، والمقارنة تقتضي تكافؤاً مثبَتاً بين مقاييس الخمس — وهو غير موجود.

## 2.2 قاعدة الفصل الحاكمة (`FREEZE §2`) — وموقع سجل الاقتران منها

| العملية | الحكم المعتمد | موقع السجل |
| :--- | :---: | :--- |
| قراءة مهارة **في نطاقها المطلق** (`DEC-064`) | ✅ مشروعة | شرط الاستدعاء (الحقل ⑦) |
| **رصد تجاور نطاقات كواقعة وصفية** («`BI` عالٍ و`IR` محدود») | ✅ **مشروعة وصفياً — حقيقة حسابية لا حكم** | 🔑 **هذا بعينه تعريف الخلية** |
| وصف **آلية** بنيوية (`M3-01`) | ✅ الطبقة البنيوية «صالحة الآن» | الحقل ③ |
| **حكم مقارن** («أقوى في `BI` من `IR`») | 🔴 محظور | ممنوع في كل السجل |
| **إسناد سببي مقارن** («قوة `BI` تحمل عبء `IR`») | 🔴 محظور | ممنوع — انظر 2.3 |
| تحفّظ القراءة الزائفة (`>70%`) · بوابة الثقة (`EP ≤50%`) | ✅ مشروعان (عتبات مطلقة) | `ح-4` · `ح-3` |

> ### 🔑 النتيجة: سجل الاقتران يقع **بكامله داخل المنطقة المشروعة**
> الخلية = **نطاقان مطلقان + تجاورهما وصفياً + آلية معتمدة**. ولا واحدة من هذه الثلاث مقارنة. **⇒ علّة حجب `URS` المنصوصة تسقط**: الشكل لم يعد يُقرأ من مقارنة الدرجات، بل من تجاور نطاقات مطلقة.

## 2.3 🛡️ تدقيق الامتثال — أين كان الخطر، وما الذي صدّه

الخطر الحقيقي في «الإسناد السببي المقارن» يقع في **خلايا الاحتواء والتهذيب** تحديداً — حيث يعمل طرف قوي على مخرج طرف ضعيف:

| الخلية | الادّعاء المحظور المحتمل | الصادّ النافذ (الحقل ⑥) |
| :--- | :--- | :--- |
| 3 `IR.BI-WS` | «قوة الكبح تحمل عبء التهدئة» | «`BI` القوي لا **يعالج** الشحنة» |
| 6 `IR.CF-SW` | «قوة التهدئة تعوّض ضعف التكيّف» | «`IR` القوي لا **يُنجز** التكيّف» |
| 7 `IR.CF-WS` | «المراجعة تعوّض ضعف الضبط» | «`CF` لا **تخفض** الشدة» |
| 15 `IR.ST-WS` | «قوة الاحتمال تحمل عبء التهدئة» | «`ST` لا **تخفض** الشحنة ولا تُنهي مصدرها» |
| 25 `EP.IR-SS` | «`EP` عالية **ترفع** `IR`» | محظورة نصاً (`DEC-108` قيد ①) |

> **الاستنتاج:** القاعدة `ح-6` — **«احتواء ≠ تعويض»** — التي وُضعت حفاظاً على النقاء البنيوي، هي **بعينها الجدار الذي يمنع الإسناد السببي المقارن**. توافق لا تصادم: السجل ممتثل لـ`NORM-01` بحكم بنيته، لا بحكم استثناء.

## 2.4 ما يبقى من `NORM-01` على `URS` — حصر نهائي

| # | المتبقي | الطبيعة | مسار الرفع |
| :--- | :--- | :--- | :--- |
| **1** | **حظر الحكم المقارن والإسناد السببي المقارن** | 🔵 **قيد دائم لا حاجب** — السجل ملتزم به بنيوياً | لا يُرفع إلا بـ`N1–N3` (تقنين) — ولا حاجة لرفعه |
| **2** | **`G4` — تركيب «قدرة الاحتواء» كحكم آلي** | 🔴 **مجمَّد جزئياً** بقدر اعتماده على «أي قوة **تنكسر أولاً**» (`URS §2.1`، حالة «متفاوت») — وهو **ترتيب بين المهارات** = مقارنة | ⇒ **يُرفع بإعادة الصوغ في البند 3** |
| **3** | **`G3` — بوابة الجذر / إسناد الجذر** | 🔴 **تعارض قائم — انظر 2.5** | قرار مالك |

**سابقة نصية مسنِدة:** `CF-06` (الانهيار المُقنَّع) **يصمد** في وثيقة التجميد نفسها «لأنه يقوم على عتبة **مطلقة** (كل الدرجات `>70%`)، لا مقارنة». وبالقياس: عبارتا «ملف قوي» و«ملف ضعيف» — بوصفهما **اشتراك الخمس في نطاق واحد** — مطلقتان مشروعتان. المشكلة محصورة في «متفاوت».

## 2.5 🔴 تعارض وثائقي — `CON-03` (إسناد الجذر)

| الحقل | البيان |
| :--- | :--- |
| **الوثيقتان** | `52-MATRIX-K3-FREEZE §4/⑥` (`DEC-089`): «أي حكم مقارن **أو إسناد جذر** — 🔴 محظور» ⟷ `52-MATRIX-K3-03` v2.0 (`DEC-099`): أنماط `HF-01…05` **بجذور مسمّاة** («جذر: `IR`») |
| **نوع التعارض** | نطاق تخفيف: `DEC-095` خفّف التجميد عن **الاقتران الفئوي** بنصّ صريح، ولم يتناول **إسناد الجذر** كمخرج محرك |
| **الأثر** | يمسّ `65-PATTERN-RECOG` (`DEC-112`) مباشرة: شرط `HF-01` يخلص إلى أن **الجذر في المركز**، وهو إسناد جذر |
| **الخطورة** | **عالية** — يمسّ وثيقة معتمدة للتوّ |
| **القرار المؤقت** | ⏸️ **تجميد الحكم.** لا أُرجّح بين وثيقتين معتمدتين بلا سند أسبقية صريح. |
| **التطويق المقترح `[مقترح]`** | تُعرض الأنماط **بصائر بنيوية لا قواعد محرك** — وهي الصيغة التي استعملها `M3-03` نفسه للمجمَّد. `65` يبقى معتمداً **كمنهج**، ويُوقَف استهلاكه في مخرج المحرك حتى الحسم. |
| **المطلوب من المالك** | ① هل يشمل تخفيف `DEC-095` **إسناد الجذر** حين يقوم على نطاقات مطلقة وتزامن بنيوي؟ · ② أم يبقى الإسناد محظوراً كمخرج ويُكتفى بالوصف؟ |

**ملاحظة تمييزية تُعين على القرار `[مشتق]`:** إسناد الجذر في `65` **لا يقوم على مقارنة مقدارية** — شرطه نطاقات مطلقة (`IR ≤50%`) + **تزامن** حافتين. فعلّة `NORM-01` (غياب التكافؤ بين المقاييس) **لا تنطبق عليه**. لكن حظر `FREEZE §4/⑥` **مطلق اللفظ**، فلا يُخصَّص بالاستنتاج.

## 2.6 نص القرار المقترح

> **`DEC-114` (مقترح):** علّة حجب `URS` بـ`NORM-01` — «الشكل مقارنة» — **تسقط** بأثر `DEC-101`: الشكل يُقرأ من **تجاور نطاقات مطلقة** وهو مشروع وصفياً بنصّ `FREEZE §2`، ومن **آليات** الطبقة البنيوية الصالحة. يبقى من `NORM-01` على `URS`: ① حظر الحكم/الإسناد المقارن **قيداً دائماً** (السجل ممتثل بنيوياً عبر `ح-6`) · ② تجميد `G4` بقدر حالة «متفاوت» — يُرفع بالبند 3 · ③ **`CON-03`** (إسناد الجذر) **مجمَّد** بانتظار قرار مستقل، ويُوقَف استهلاك `65` في مخرج المحرك حتى ذلك.

---

# القسم 3: البند 3 — إعادة صوغ الحالة «متفاوت» (`URS §2.1`)

## 3.1 النص المطلوب استبداله

> | ملف **متفاوت** | الاحتواء يعتمد على **أي قوة تنكسر أولاً** تحت الحمل | *(`56-K3-URS §2.1`, `DEC-082`)*

**موضع الخلل بدقة:** «أي قوة تنكسر أولاً» **ترتيب بين المهارات** — وهو حكم مقارن يقتضي تكافؤاً مثبَتاً بين المقاييس الخمس (`NORM-01`)، **وتنبؤ زمني** لا تسنده أداة نقطية (`DEBT-K3-TEMPORAL-01`). العلّتان مجتمعتان.

**ما يبقى صحيحاً في `DEC-082`:** قراءة **قدرة الاحتواء** على مستوى **الدائرة** (محصلة) مشروعة، والحظر منصبّ على الادّعاء الاتجاهي **داخل مهارة مفردة**. هذا الجسر **لا يُمسّ** — يُستبدل **مصدر مادته** فقط.

## 3.2 النص البديل المقترح — جاهز للإدراج في `URS` v3.0

> ### قراءة قدرة الاحتواء (v3.0)
> تُقرأ قدرة الاحتواء من **الخلايا المفعَّلة وكُلفها المنصوصة (الحقل ⑤)**، لا من شكل الدرجات ولا من ترتيبها.

| الحالة | شرطها — **مطلق** | قراءة الاحتواء | التحفّظ الإلزامي |
| :--- | :--- | :--- | :--- |
| **ملف قوي** | الخمس في نطاق `>70%` | لا خلايا ضعف مفعَّلة؛ النظام **يحتوي** الشدة ويُهبطها إلى النطاق المُدار | 🛡️ **مشدَّد:** هذا التكوين بعينه هو توقيع `HF-05` (الانهيار المُقنَّع) — كتلة `M3-07` إلزامية، ولا تُدَّعى أصالة القوة |
| **ملف ضعيف** | الخمس في نطاق `≤50%` | حافات المحور والتحميل مفعَّلة بصور التفاقم؛ الشدة **تصعد ولا تُحتوى** | 🔴 **`EP` داخل النطاق ⇒ `HF-03` نافذ**: القراءة كلها **غير حاسمة** بحكم `ح-3`. و`HF-04` قائم: `ST` قد تكون سليمة بعبء مضاعف |
| **ملف مركّب** *(بديل «متفاوت»)* | أي تكوين آخر — بما فيه وجود عناصر في النطاق المحايد | الاحتواء **موزّع**: تُذكر الخلايا المفعَّلة، **ما الذي يُحتوى، بأي آلية، وبأي كلفة، وأين تقع الكلفة** (داخلية · مُصدَّرة إلى `ST` · منتقلة إلى الخارج) | الصمت مشروع عن العناصر المحايدة (`ح-1`) |

> **🔒 المحظور صراحةً في الحالة المركّبة:**
> ❌ ترتيب المهارات · ❌ التنبؤ بأيّها «ينكسر أولاً» · ❌ إسناد كلفة أحدها إلى قوة الآخر (`ح-6`) · ❌ ضغط الملف في تسمية (`URS §6/4`).
> ✅ المسموح: **جرد الحافات المفعَّلة وكُلفها بالترتيب البنيوي** (`64 §4.1`)، وقراءة النمط إن تعرّفت عليه الطبقة (`65`) — **بحدود `CON-03`**.

## 3.3 لماذا هذا البديل مشروع

| المعيار | التحقق |
| :--- | :--- |
| مطلق لا مقارن | الشروط الثلاثة نطاقات `DEC-064`، والحالة المركّبة **جرد** لا ترتيب |
| بلا صيغة تجميع | لا حساب — قراءة مجموعة خلايا |
| مسنود نصاً | «الكلفة» حقل منصوص في كل خلية من الـ28 · وسابقة `CF-06`: عتبة مطلقة تصمد |
| يحفظ `DEC-082` | قراءة الاحتواء تبقى **دائرية** (محصلة)، ولا اتجاه داخل مهارة |
| يحفظ السلامة | الحالة «قوي» صارت تحمل تحفّظ `HF-05` — وهو **تشديد** على v2.1 لا تخفيف |

## 3.4 ما يفتحه هذا البند

| البند | الأثر |
| :--- | :--- |
| `GAP-URS-01` | ✅ **تُغلق تماماً** — سقط موضعها الأخير (`§1.4`) |
| **`G4`** (تركيب قدرة الاحتواء) | 🟢 **يُرفع تجميده** بقدر ما يُبنى على الخلايا وكُلفها؛ ويبقى **محظوراً** أي ترتيب انكسار |
| `NORM-01` على `URS` | يتبقى منه: القيد الدائم (ممتثَل) · و**`CON-03`** وحده حاجباً |
| **مصطلح «متفاوت»** | يُستبدل بـ**«مركّب»** — قرار تسمية يُعتمد أو يُصحَّح |

## 3.5 ما لا يُدَّعى

1. ❌ لا يُدَّعى أن `URS` v3.0 أُنتج — هذا **نصّ بند** لا وثيقة بصمة.
2. ❌ لا يُدَّعى رفع الحجب — `CON-03` قائم.
3. ❌ لا يُدَّعى أن الحالة «قوي» آمنة — بل صارت **الأكثر تحفّظاً**.
4. ❌ `DEP-URS-A` (القراءة الاتجاهية داخل المهارة) لا تُفتح بهذا البديل.

## 3.6 نص القرار المقترح

> **`DEC-115` (مقترح):** تُستبدل الحالة «متفاوت» في `URS §2.1` بالحالة **«مركّب»**، وتُعاد قراءة قدرة الاحتواء إلى **الخلايا المفعَّلة وكُلفها المنصوصة** بدل شكل الدرجات وترتيبها. يُحظر صراحةً ترتيب المهارات والتنبؤ بأيّها ينكسر أولاً. تُضاف التحفّظات الإلزامية الثلاثة (`HF-05` للحالة القوية · `HF-03`+`HF-04` للضعيفة · الصمت المشروع للمركّبة). **⇒ `GAP-URS-01` تُغلق تماماً · تجميد `G4` يُرفع بقدر بنائه على الخلايا.**

---

# القسم 4: البند 4 — مراجعة `DEC-087`

## 4.1 النصان

**الأصل المعتمد (`01-MASTER`):**
> **`DEC-087`** — قاعدة التعويض اللامتماثل: **لا تعويض صاعد إطلاقاً** · التعويض النازل ممكن **لكن بكلفة**. المبدأ: **قوة المهارة اللاحقة تُضخّم أثر عطل السابقة، لا تلغيه** (تهدئة متقنة لشعور لم يُفهم = إتقان يخدم الخطأ).

**التوجيه الأمامي (`60-TOPOLOGY §5/④`, `DEC-098`):**
> الصيغة المكتملة بعد **د-3**: **الاحتواء متبادل الاتجاه، والتعويض معدوم في الاتجاهين** — الأطراف تحتوي شحنة المركز الضعيف بكلفة (الغليان)، والمركز يحتوي شحنة الطرف الضعيف بكلفة (جهد الاحتواء) — ولا أحد يُغني عن وظيفة الآخر. الجوهر اللامتماثل باقٍ: **احتواء شحنة ≠ تعويض وظيفة**.

## 4.2 سبب المراجعة **[مشتق]**

الصيغة الأصلية تقوم على **سلسلة خطية**: «صاعد/نازل» يفترض ترتيباً بين المهارات. والطوبولوجيا المعتمدة (`DEC-098`) **بنية محورية** لا سلسلة: مركز · أطراف · مصبّ · منبع. فمصطلحا «الصعود والنزول» **فقدا مرجعهما**، لا معناهما.

## 4.3 🔴 ملاحظة سيادية — التوجيه يُسقط ثلث القاعدة

التوجيه في `60-TOPOLOGY §5` يعالج **التعويض** و**الاحتواء**، ولا يذكر **المكوّن الثالث** من `DEC-087`:

> «قوة المهارة اللاحقة **تُضخّم** أثر عطل السابقة، لا تلغيه — تهدئة متقنة لشعور لم يُفهم = **إتقان يخدم الخطأ**».

وهو المكوّن الذي وُصف في `01-MASTER` بأنه **«يقلب حدساً شائعاً»**. اعتماد التوجيه حرفياً **يُسقطه**. وهو غير مشمول بالاحتواء ولا بالتعويض: **مفهوم ثالث مستقل** — ولا يزال **مسنوداً بالخلايا**:

| الخلية | تجسيد التضخيم |
| :--- | :--- |
| `CPL-EP.IR-WS` | «التنظيم الأعمى»: مركز قادر يعمل على مادة مشوَّشة — الإتقان يخدم تمثيلاً معطوباً |
| `CPL-CF.ST-WS` | «حِمل موضوعي **+ خطأ مستمر**»: الاحتمال العالي يُطيل بقاء النمط الخاطئ |

**⇒ يُقترح استبقاؤه صراحةً في الصيغة الجديدة.**

## 4.4 الصيغة المقترحة — `DEC-087` v2

> ### قاعدة الاحتواء والتضخيم *(بديلة لقاعدة التعويض اللامتماثل)*
>
> **① التعويض معدوم في كل الاتجاهات.** لا عنصر يؤدي وظيفة عنصر آخر — لا مركزٌ يُنجز تكيّف الذراع، ولا ذراعٌ تضبط شدة المركز، ولا `ST` تُنهي مصدر الشحنة. *(نافذ عبر `ح-6` في الخلايا الـ28)*
>
> **② الاحتواء متبادل الاتجاه، وبكلفة دائماً.** المركز يحتوي شحنة الطرف بكلفة (جهد احتواء متكرر)؛ والطرف يحتوي مخرج المركز بكلفة (احتقان/غليان). **الكلفة منصوصة في الحقل ⑤**، ولا يُذكر احتواء بلا كلفته.
>
> **③ التضخيم قائم على موقع المعالجة.** عنصر في نطاق قدرة عالية يعمل على **مادة معطوبة واردة من موقع سابق** في مسار المعالجة (**منبع `EP` → مركز `IR` → أطراف `BI`/`CF` → مصبّ `ST`**) **يُضخّم أثر العطل ولا يلغيه** — «إتقان يخدم الخطأ».
>
> **الجوهر اللامتماثل باقٍ:** احتواء شحنة ≠ تعويض وظيفة.

**ما تحفظه الصيغة من الأصل:** الاتجاه اللامتماثل (③ يستبدل «صاعد/نازل» بـ«موقع في المسار») · مبدأ التضخيم كاملاً · حظر ادّعاء الإصلاح.
**ما تُلغيه:** «التعويض النازل ممكن لكن بكلفة» — ما كان يُسمّى تعويضاً نازلاً هو **احتواء بكلفة**، لا استبدال وظيفة.

## 4.5 أثر رجعي على السجل — **لا شيء**

دقّقتُ الخلايا الـ28: لا خلية تدّعي تعويضاً، وكل خلية احتواء تحمل نفي التعويض في الحقل ⑥. **⇒ السجل ممتثل للصيغة v2 قبل اعتمادها** — لأن `ح-6` طبّقت جوهرها منذ المرحلة (أ).

**مسألة تصنيفية — ✅ محسومة (`DEC-116`): الخيار (أ) — قاعدة تفسيرية.**

> ### 🔒 `ح-12` — قاعدة التضخيم
> حين يعمل عنصر في **نطاق قدرة عالية** على **مادة معطوبة واردة من موقع سابق** في مسار المعالجة (**منبع `EP` → مركز `IR` → أطراف `BI`/`CF` → مصبّ `ST`**)، تُقرأ كلفته **تضخيماً لأثر العطل السابق لا معالجةً له**. ويُحظر قراءة قوة العنصر اللاحق تخفيفاً للعطل السابق.
> **موضعها:** تقييد للحقل ⑤ — **لا تُنشئ نوع علاقة سادس**؛ الأنواع تبقى خمسة (`DEC-107`).

**الخلايا التي تسمّيها `ح-12` — بلا تعديل نصّي `[مشتق]`:**

| الخلية | ما هو **منصوص فيها أصلاً** |
| :--- | :--- |
| `CPL-EP.IR-WS` | «التنظيم الأعمى» — ضبط قوي على مادة مشوَّشة |
| `CPL-IR.ST-WS` | «النظام يستمر بلا انكسار ظاهر **والمصدر لم يُعالَج**» |
| `CPL-CF.ST-WS` | «حِمل موضوعي **+ خطأ مستمر**» — الاحتمال يُطيل بقاء النمط الخاطئ |

**⇒ `ح-12` تُسمّي ما هو مكتوب، ولا تضيف إليه.** لا تُعدَّل خلية.

## 4.6 دَين المزامنة الناتج

| الوثيقة | التعديل |
| :--- | :--- |
| **`52-MATRIX-K3-04`** (خريطة الاحتواء والتعويض) v0.1 | 🔄 **تُعاد بناءً** على `DEC-087` v2 — مبنية حالياً على السلسلة الخطية والمثلث الصاعد/النازل *(مقرَّرة سلفاً في `00-MANIFEST`)* |
| `52-MATRIX-K3-06` (فهرس الاسترجاع) | تحديث نص الحظر: «لا تعويض صاعد» ← **«لا تعويض في أي اتجاه»** |
| `01-MASTER` | قيد `DEC-087` v2 ورفع وسم «قيد المراجعة» |

## 4.7 نص القرار المقترح

> **`DEC-116` (مقترح):** يُستبدل `DEC-087` بـ**قاعدة الاحتواء والتضخيم** (§4.4): ① التعويض معدوم في كل الاتجاهات · ② الاحتواء متبادل وبكلفة دائماً منصوصة · ③ التضخيم قائم على موقع المعالجة (منبع → مركز → أطراف → مصبّ)، فقوةٌ تعمل على مادة معطوبة سابقة تُضخّم أثر العطل. يُلغى تعبير «التعويض النازل الممكن». يُوضع التضخيم **قاعدةً تفسيرية `ح-12`** لا نوع علاقة. **لا تُعدَّل أي خلية.** وتُعاد `M3-04` بناءً على الصيغة الجديدة.

---

# القسم 5: حصيلة الخطوة ④

| البند | الحالة |
| :--- | :--- |
| 1 · `GAP-URS-01` | ✅ `DEC-113` → مُغلقة تماماً بـ`DEC-115` |
| 2 · المتبقي من `NORM-01` | ✅ `DEC-114` — العلّة ساقطة · قيد دائم ممتثَل · `CON-03` مجمَّد |
| 3 · «متفاوت» → «مركّب» | ✅ `DEC-115` — `G4` يُرفع تجميده |
| 4 · `DEC-087` | ✅ `DEC-116` — قاعدة الاحتواء والتضخيم · `ح-12` |

**⇒ البنود الأربعة محسومة. يتبقى على `URS` v3.0 حاجب واحد: `CON-03`.**

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| البنود المحسومة | **4/4 — مكتملة** |
| `GAP-URS-01` | ✅ مُغلقة تماماً (`DEC-113`+`DEC-115`) |
| علّة حجب `NORM-01` على `URS` | 🟢 ساقطة (`DEC-114`) |
| `G4` تركيب قدرة الاحتواء | 🟢 مرفوع التجميد (`DEC-115`) |
| `DEC-087` | ✅ v2 معتمد (`DEC-116`) — التعويض معدوم · الاحتواء بكلفة · التضخيم `ح-12` |
| أثر رجعي على الخلايا الـ28 | ✅ **صفر** — السجل ممتثل مسبقاً |
| `CON-03` | ✅ **مغلق** (`DEC-117` — بروتوكول `67`) |
| **`URS` v3.0** | 🟢 **قابل للبناء — لا حاجب متبقٍ** (بأمر صريح) |
| دَين مزامنة جديد | `M3-04` تُعاد · `M3-06` نص الحظر · `01-MASTER` |
| صفر هلوسة | ✅ لا صيغة · لا عتبة · لا ترجيح بين وثيقتين متعارضتين |
| **حالة الوثيقة** | ✅ **v1.0 — البنود الأربعة محسومة (`DEC-113…116`) · `CON-03` مغلق (`DEC-117`)** |

---
