# 00-SESSION-UPDATE_2026-08-01 — إغلاق طبقة السحب في الدائرتين

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **معتمد ومنفَّذ ومُتحقَّق منه** — `DEC-190…198` مختومة بالكامل |
| **نطاق الاستدلال** | `84-K2-DEFECT` v1.1 · `85-K3-GUARD` · `86-K3-FIX` · `00-PATCH_CRITIQUE-GATE` v1.1 · `00-PATCH_DEC-191-192` |
| **الحصيلة** | عيبان مُغلقان · تعارض مُغلق · طبقة سحب محروسة في $K_2$ و$K_3$ |

---

# القسم 1: سجل القرارات — تسعة مختومة

| الكود | البند | الحالة |
| :--- | :--- | :---: |
| `DEC-183` | حذف `SP%` من الشاشة وكل التصديرات — $K_2$ و$K_3$ | ✅ |
| `DEC-186` | $K_1$/$K_4$ لوحة داخلية موسومة `[خارج نطاق التقرير]` | ✅ |
| `DEC-187` | $K_2$: الحالة + الرمز · $K_3$: كلمات النطاق فقط (`ق1`) | ✅ |
| `DEC-188` | «قبل أن تقرأ» يُبقى حرفياً | ✅ |
| `DEC-189` | بنية تقرير $K_2$ — **الدمج الطبقي** (٩ أقسام) | ✅ |
| `DEC-190` | إصلاح `DEF-K2-01` — استدعاء بالوظيفة + حارس `_CARD` | ✅ |
| `DEC-191` | إعادة توليد التقارير الأربعة + `test_report_k2` | ✅ |
| `DEC-192` | تصويب وسم «صفر انحدار» بالتحفّظ الطبقي | ✅ |
| `DEC-193` | الفحص 10 + شرط التفعيل الثاني + `ن-7` | ✅ |
| `DEC-194` | حارس $K_3$ (`ص-1…ص-6`) + شرط «يُختبَر على عيب معلوم» | ✅ |
| `DEC-195/ج` | فصل `circle_map`: المشترك في ② · الخاتمات في ③ | ✅ |
| `DEC-196` | `BAND_LABEL["OUT"]` صريح + `band_label()` يوقف عند المجهول | ✅ |
| `DEC-197/ج` | الاسم المزدوج في العنوان متى اختلف الاسمان | ✅ |
| `DEC-198` | شرط «كل فحص يقيس شيئاً واحداً» في `ن-7` | ✅ |

**الترقيم التالي:** `DEC-199`.

---

# القسم 2: `DEC-197/ج` — التنفيذ

**المبدأ:** الاسم المرادف **يُشتقّ من نصّ الخاتمة المعتمد** (`79 §2.2`) بتعبير نمطي، ولا يُؤلَّف ولا يُثبَّت في جدول يدوي. فإن تغيّر النصّ تبعه العنوان، وإن تعذّر الاستخراج **يتوقف الإصدار**.

**والقوس يظهر عند الاختلاف فقط** — فلا يُكتب `### الملاحظة (الملاحظة)`:

| المهارة | العنوان بعد التنفيذ |
| :--- | :--- |
| `EP` | `### الملاحظة — كفاءة أساسية` |
| `IR` | `### التهدئة (تهدئة الشدة) — حضور محدود` |
| `BI` | `### المنع (كبح الاندفاع) — قدرة عالية` |
| `CF` | `### المراجعة (التكيّف) — حضور محدود` |
| `ST` | `### الاحتمال — قدرة عالية` |

**حدود النقاء:** المسار ج **لا يمسّ نصّاً معتمداً من الطرفين** — `USER_NAME` باقٍ كما هو (`71 §19`)، والخاتمات باقية حرفياً (`79 §2.2` · `DEC-138`). التعارض حُلّ بالعرض لا بالحذف.

---

# القسم 3: حالة الحراسة

| الدائرة | طبقة الحساب | طبقة السحب |
| :--- | :--- | :--- |
| $K_2$ | `test_golden_k2` ✅ | `test_report_k2` + `validate_slots` ✅ |
| $K_3$ | `test_golden_k3` ✅ | `test_report_k3` + `k3_guard` (`ص-1…ص-6`) ✅ |

**فحوص حارس $K_3$ الستة:**

| الكود | يمنع |
| :--- | :--- |
| `ص-1` | قسماً مهارياً بمتن ناقص |
| `ص-2` | نصّاً مفقوداً يمرّ صامتاً |
| `ص-3` | قسماً عاماً يحمل خاتمة مهارية |
| `ص-4` | وسماً يُسحب من افتراض |
| `ص-5` | لغةً خارج `DEC-064` |
| `ص-6` | عنواناً بلا اسمه المرادف حين يختلف |

**برهان `ن-7/①` مُستوفى في الطرفين:** كل اختبار يُخرج انحرافات على الكود قبل الإصلاح — $K_2$: **١٠** · $K_3$: **٥** — وصفراً بعده.

---

# القسم 4: دَين مزامنة مفتوح

| الوثيقة | الموضع | المطلوب |
| :--- | :--- | :--- |
| `81-K3-REPORT-SYN01` | س53 · س64 · س74 | عناوين `### التهدئة —` · `### المنع —` · `### المراجعة —` تسبق `DEC-197/ج` ⇒ **يُعاد إصدارها** |
| `01-MASTER` · `02-MASTER` | سجل القرارات | إدراج `DEC-190…198` |
| `00-INDEX` | فهرس الملفات | إضافة `84` · `85` · `86` · `k3_guard` · `test_report_k2/k3` · `circle_map_v2` |
| `00-PROTOCOL_Self_Critique_Gate` | §1 · §2 · §3 | لصق رقعات `00-PATCH_CRITIQUE-GATE` v1.1 |

---

# القسم 5: الحصيلة المنهجية

عيبان لم يكشفهما أحد قبل هذه الجلسة، ومرّا على **عشرة تقارير مقروءة**:

| العيب | لماذا مرّ |
| :--- | :--- |
| `DEF-K2-01` | النصّ عربي سليم؛ الخلل في **الإسناد** لا في المتن |
| `DEF-K3-01` | أربع خاتمات من خمس **لم تكن تُعرض أصلاً**، فبقي التعارض كامناً |
| `TC-K3-02` | ظهر فقط حين **تجاور** الاسمان بعد إصلاح `DEF-K3-01` |

**ثلاثة دروس دخلت `ن-7`:**

1. **ما يُفحص آلياً يُحرَس آلياً** — القائمة البشرية تكمّل الحارس ولا تُغنِي عنه.
2. **الحارس يُختبَر على عيب معلوم** — حارس لم يسقط على عيب واقع لا يُعتمد.
3. **كل فحص يقيس شيئاً واحداً** — الخلط يُنتج إنذاراً كاذباً على أحدهما ويطمس الآخر.

> والثلاثة **مُستخرَجة من أخطاء وقعت في هذه الجلسة نفسها**: حارس `ص-3` أعطى «سليم» ست مرات قبل تصحيحه · وفحص الخاتمة أعطى **١٨ إنذاراً كاذباً** قبل فصله.

---

# القسم 6: الحالة — المرحلة ① مفتوحة

| البند | الحالة |
| :--- | :---: |
| $K_2$ — الحساب · السحب | ✅ محروستان |
| $K_3$ — الحساب · السحب | ✅ محروستان |
| `DEF-K2-01` · `DEF-K3-01` · `TC-K3-02` | ✅ **مغلقة** |
| `TC-K2-01` نطاق كتالوج المركز | 🟡 **مفتوح** — يُحسم داخل الدمج الطبقي |
| `GAP-RPT-K2-01/02/03` | 🟡 مفتوحة (`R1` الشدة · `R9` التطهير · `R11` `St+H`) |
| **المرحلة ①** — نقل المحركين إلى JS | 🟢 **بلا حاجز** |

---

> **[نهاية التحديث — الدائرتان محروستان · تسعة قرارات مختومة · الترقيم التالي `DEC-199`]**
