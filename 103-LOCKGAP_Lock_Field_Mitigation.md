# 103-LOCKGAP — تطويق ثغرة حقل القفل (`DEC-229`)

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُبرهَن** (`DEC-229`) |
| **نطاق الاستدلال** | `DEC-217/ب` · `98-R1-INTENSITY §4` · `51-MATRIX-06/R9` · `55-USER-INT8-*` |
| **الحصيلة** | **64 حقل قفل مفحوصاً · 3 ذِكر مقبول مسجَّل · صفر مستجدّ** |
| **الإصدار** | v1.0 |

---

# القسم 1: الثغرة كما أُعلنت

`DEC-217/ب` استثنى أسطر `🔒` من بوابة `R9`. السبب: **أقفال العزل تُصاغ بذكر المصطلح المحظور** ليمنع الخلط، فتُطلق البوابة عليها وهي أكثر النصوص التزاماً.

**الثغرة:** نصّ ضارّ يُدسّ في حقل قفل **يمرّ بلا فحص**.

## ما لا يسدّها

| الحارس | لماذا لا يكفي |
| :--- | :--- |
| `pack_sha` · `test_packs` | تحرس **الثبات لا المشروعية** — نصّ خاطئ ثابتُ البصمة يمرّ |
| `pur_scan_packs` | يفحص حقل `user` وحده · **لا يمسّ حقول القفل** |
| بوابة المخرج | مُستثناة صراحةً بـ`DEC-217/ب` |

⇒ **حقول القفل كانت خارج كل حارس.**

---

# القسم 2: الحلّ — رصد الاستجداد لا الحكم على المشروعية

**ما لا أستطيعه:** الحكم آلياً على أن ذِكراً داخل قفل «مشروع». التمييز بين «`X` ≠ `Y`» و«`X` هو `Y`» **حكم دلالي** — وبناء مُصنِّف له اختراعُ قاعدة لا سند لها.

**ما أستطيعه:** تثبيت **الحالة المقبولة اليوم**، ورصد أي **استجداد**.

## القياس — الحالة اليوم

فُحصت **64 حقل قفل**. الإصابات **ثلاث**:

| الوحدة | المصطلح | السياق |
| :--- | :--- | :--- |
| `K2-R-S-H+` | `تنفيذ ميداني` | «سيادة عدسة الجدوى **≠** تنفيذ ميداني (`K₄` مستقل)» |
| `K2-E-S-H+` | `شدة الانفعال` | «سيادة الفهم المعرفي **≠** شدة الانفعال (`K₃`)» |
| `K2-E-S-H` | `تشخيص` | «قراءتك معلومة تشغيلية، **لا** "تشخيص" (لغة محظورة)» |

**الثلاثة صيغتها نفيٌ أو فصل** — لا خلط. وسُجِّلت في `k2_lock_registry.json` **بسياقها كاملاً**، فالسجلّ يوثّق **لماذا قُبلت** لا أنها قُبلت فحسب.

---

# القسم 3: التنفيذ

`scan_lock_fields()` — **حزمة ثانية عشرة** `LOCKREG_K2`:

| البند | السلوك |
| :--- | :--- |
| مرحلة الفحص | **المحتوى** لا المخرج — تعمل حتى لو لم يُعرض القفل |
| النطاق | 64 حقل قفل × (`_FORBIDDEN` + 72 بند `PUR`) |
| القرار | إصابة **داخل** السجلّ ⇒ تمرّ · **خارجه** ⇒ تُرصد في `r9_gate` |
| الحقل الجديد | `lock_registry_size` — حجم السجلّ ظاهر في كل `audit` |

**الحجم في الـ`audit` ليس زينة:** لو وُسِّع السجلّ ليُسكت إصابة جديدة، **يتغيّر الرقم في كل تقرير** ويظهر في المقارنة.

## البرهان

| الاختبار | النتيجة |
| :--- | :---: |
| 56 حالة انحدار | ✅ **صفر إنذار** |
| حقن «اضطراب» في قفل `K2-A-S-M` | 🔴 **`{قفل-مستجدّ · K2-A-S-M · اضطراب}`** |
| التكافؤ بايثون ↔ JS | ✅ `r9_gate` و`lock_registry_size` متطابقان |

---

# القسم 4: ما تبقّى من الثغرة — بصراحة

**الحلّ لا يسدّ الثغرة، يُضيّقها.**

| ما صار محروساً | ما يبقى مكشوفاً |
| :--- | :--- |
| إدخال مصطلح محظور **جديد** في قفل | تغيير **صياغة** ذِكر مسجَّل من نفيٍ إلى إثبات |
| توسيع السجلّ صامتاً (يظهر في `audit`) | نصّ ضارّ **بلا مصطلح محظور** |

**مثال المكشوف:** لو صار `K2-E-S-H` يقول «قراءتك **تشخيص**» بدل «**لا** تشخيص» — المصطلح مسجَّل، والصيغة انقلبت، **والحارس لا يرى**.

**التطويق المتبقي الوحيد المضمون:** بصمة حقل القفل نفسه. `pack_sha` تغطّي الحزمة كاملة ⇒ أي تعديل في قفل **يُغيّر بصمة `INTENSITY_K2`** ويظهر في `audit`. **فالانقلاب الصياغي يُرصد — لكن كتغيّر حزمة لا كخرق قفل.**

⇒ **الثغرة مطوَّقة بطبقتين**: سجلّ للاستجداد + بصمة للتعديل. ولا واحدة منهما تحكم على المعنى.

---

# القسم 5: الحالة

| المجموعة | النتيجة |
| :--- | :---: |
| `parity_py` (125 حالة) | ✅ `2711c24d8155819b` |
| `parity_reports` (142 تقريراً · النطاقان) | ✅ `36ae94bfd5a8b60f` |
| `verify_regen` (86 حالة) | ✅ العقد مُستوفى |
| `audit` $K_2$ (56 حالة) | ✅ صفر تباعد |
| `test_packs` (**12 حزمة**) | ✅ |
| `test_golden_k2/k3` · `test_report_k2/k3` | ✅ |

**الأداة:** `Kashaf_v2.html` — **1,104,286 بايت** · اثنتا عشرة حزمة · `audit` بـ**22 حقلاً**.

**الترقيم التالي:** `DEC-230`.

---

> **[نهاية الوثيقة — `103-LOCKGAP` v1.0 · الثغرة مطوَّقة بطبقتين · وما يبقى مكشوفاً مُعلَن]**
