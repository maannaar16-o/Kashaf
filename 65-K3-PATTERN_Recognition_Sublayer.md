# 65-K3-PATTERN_Recognition_Sublayer.md
## المستوى الثاني لطبقة التركيب — قاعدة التعرّف على الأنماط (`HF`) بوصفها ثلاثيات غير قابلة للاختزال

| الحقل | البيان |
| :--- | :--- |
| **كود الوثيقة** | `65-K3-PATTERN-RECOG` |
| **الإصدار** | **v1.1 — ✅ معتمد** (`DEC-112` · مُصحَّح بـ`DEC-133`: شرط القناة النشطة) |
| **السند** | **`DEC-111`** (قرار المالك: أنماط `M3-03` ثلاثيات حقيقية) · `52-MATRIX-K3-03` v2.0 (`DEC-099`) · `64-COMPOSITION-METHOD` v1.0 (`DEC-106`) · سجل الاقتران 28/28 · `M3-07` |
| **الموقع** | **المستوى ② داخل طبقة التركيب** (`64 §2`, الطبقة ③ من المكدس) — بين تفعيل الخلايا وصياغة القراءة |
| **الحدّ الحاكم** | لا آلية جديدة · صفر أوزان وعتبات · احتمال لا حتمية · **يُسمّى النمط لا الشخص** |

---

# القسم 1: القرار المؤسِّس ومضمونه البنيوي

> **`DEC-111`:** الأنماط المتسلسلة في `M3-03` **ثلاثيات حقيقية لا تُختزل في ثنائيتين متجاورتين**.

## 1.1 ما الذي تحمله الثلاثية زائداً على ثنائياتها؟ **[مشتق]**

الخلية تصف **حافة ساكنة**. الثلاثية تضيف ثلاثة عناصر لا تحملها أي حافة منفردة:

| # | الفائض | مثاله في `M3-03` |
| :--- | :--- | :--- |
| **① التزامن** | تعطّل حافتين **في آنٍ** من مصدر واحد = دلالة جذر، لا دلالة حافتين | `HF-01`: «عطل **متعدد الأطراف في آنٍ** — لا طرف واحد» |
| **② الاتجاه السببي والتجدد** | مسار **طرف ← مركز ← قاعدة** مع **دورة متجددة** — لا مجرد حافتين متجاورتين | `HF-02`: فشل الذراع يولّد سياقاً يعود للمركز ثم ينزل حملاً |
| **③ التراكم العددي** | **عدّ القنوات** النشطة معاً — الحكم على المجموع لا على القناة | `HF-04`: «حمل من ثلاث قنوات يفوق سعة الاحتمال» |

**بهذا يصير «عدم الاختزال» مضموناً محدداً**، لا دعوى: الثنائيات تصف حافات، والثلاثية تصف **تزامناً واتجاهاً وتراكماً** — وهي معلومات غائبة عن كل حافة على حدة.

---

# القسم 2: 🔑 تصنيف الأنماط — **الخمسة ليست صنفاً واحداً** **[مشتق]**

فحص `M3-03` v2.0 يُظهر أن `DEC-111` **ينطبق على ثلاثة أنماط لا خمسة**:

| النمط | الشكل | صنف التعرّف |
| :--- | :--- | :--- |
| **`HF-01`** عطل المحور | مركز ← ذراعان (تزامن) | **أ — تسلسلي (ثلاثية)** |
| **`HF-02`** عطل الذراع مع العودة | ذراع ← مركز ← قاعدة (اتجاه + تجدد) | **أ — تسلسلي (ثلاثية)** |
| **`HF-04`** تشبّع القاعدة | ≥ قناتين ← قاعدة (تراكم) | **أ — تسلسلي (ثلاثية)** |
| **`HF-03`** عطل المُضاعِف | `EP` ← الملف كله | **ب — شامل (غير خلوي)** |
| **`HF-05`** الانهيار المُقنَّع | تكوين الدرجات كلها | **ب — شامل (غير خلوي)** |

## 2.1 🔒 قفل منع الازدواج — الصنف (ب) **لا يُنقل** إلى هذا المستوى

`HF-03` و`HF-05` **منفَّذان أصلاً في طبقتين قائمتين**، وإعادة تنفيذهما هنا ازدواج — وهو الداء نفسه الذي عولج في `DEC-103` (`ST`) و`DEC-108` (`EP`):

| النمط | موضعه النافذ | لا يُكرَّر في `65` |
| :--- | :--- | :--- |
| `HF-03` | **وَسْم الثقة** في طبقة العرض (`ح-3`: `EP ≤50%` ← قراءة غير حاسمة) | ✅ |
| `HF-05` | **كتلة `M3-07`** الموحّدة (`ح-4`: أي طرف `>70%`) | ✅ |

**النص المانع:** لا تُصدر قاعدة التعرّف حكم موثوقية ولا تحفّظ أصالة؛ ولا تُعيد طبقتا الثقة والتحقق سردَ ما تقوله الأنماط. **كل نمط يُنفَّذ في طبقته مرة واحدة.**

**مصالحة مقروءة `[مشتق]`:** عبارة `M3-03` في `HF-03` «مُدخَل مشوّه يصل **العناصر الأربعة معاً**» تصف **مدى الأثر**، ويحدّد `DEC-108/109` **مساره**: تعاقباً عبر `IR`، لا بحافات مباشرة. لا تعارض — وصف مدى مقابل تحديد مسار.

---

# القسم 3: شروط التعرّف — الصنف (أ)

> **قاعدة عامة:** التعرّف **يقرأ الخلايا المفعَّلة أصلاً**؛ لا يستدعي خلية جديدة ولا يخترع شرط نطاق. كل شرط أدناه مركّب من شروط استدعاء معتمدة (الحقل ⑦).

## 3.1 `HF-01` — عطل المحور (جذر `IR`)

| الحقل | البيان |
| :--- | :--- |
| **شرط التعرّف** | `IR ≤ 50%` **و** تفعُّل **حافتَي المحور معاً**: `CPL-IR.BI-W*` **و** `CPL-IR.CF-W*` |
| **الثلاثية** | `IR` (جذر) + `BI` + `CF` — عنصران طرفيان بحافتين متزامنتين |
| **الصورة** | تُقرأ من نطاقَي الذراعين: قويان ← **مكشوف/محتوى** بحسب الحافة · ضعيفان ← تسرّب مباشر |
| **ما يمنعه التعرّف** | إسناد العطل إلى `BI` أو `CF` — **التزامن هو الدليل** على أن الجذر في المركز |
| **الشرط الناقص** | تفعُّل **حافة واحدة** فقط ← **لا يُعلن `HF-01`**؛ تبقى قراءة حافة مفردة |

## 3.2 `HF-02` — عطل الذراع مع عودة الشحنة (جذر `CF` أو `BI`)

| الصورة | شرط التعرّف | الثلاثية |
| :--- | :--- | :--- |
| **جذر `CF`** | `CPL-IR.CF-SW` **و** `CPL-CF.ST-W*` | `CF` ← `IR` (احتواء) ← `ST` (حمل) |
| **جذر `BI`** | `CPL-BI.CF-SW` (حظر بلا تسليم) **و** `CPL-BI.ST-S*` | `BI` ← احتقان ← `ST` |

| الحقل | البيان |
| :--- | :--- |
| **العلامة الفارقة** | **الثبات الظاهري فوق عجز متكرر** — «ملف يبدو مستقراً انفعالياً مع تكرار العجز عن مجاراة التغيير» |
| **ما يمنعه التعرّف** | قراءة الاستقرار الظاهر دليلَ مرونة |
| **الشرط الناقص** | غياب طرف الحمل ← **`HF-02` جزئي**: يُقرأ بلا لاحقة القاعدة، ولا يُدَّعى تمام الدورة |

## 3.3 `HF-04` — تشبّع القاعدة

| الحقل | البيان |
| :--- | :--- |
| **شرط التعرّف** | تفعُّل **≥ قناتين نشطتين** **و** `ST` في النطاق المحدود. 🔄 **«القناة النشطة» معرَّفة مركزياً في `M3-04` v1.2 §2.1** — ولا تُعاد هنا: `CPL-IR.ST-W*` · `CPL-CF.ST-W*` · **`CPL-BI.ST-S*`** (`DEC-133`) |
| **الثلاثية** | مصدران (أو ثلاثة) ← `ST` — الحكم على **المجموع** لا على قناة |
| **التمييز الإلزامي** | 🔴 **`ST` قد تكون سليمة تحمل عبئاً مضاعفاً — لا معطوبة.** يُذكر نصاً كلما أُعلن النمط |
| **الحالة المقابلة** | ≥ قناتين نشطتين مع `ST` في نطاق القدرة العالية ← **ليس تشبّعاً**: هو «يحمل بكلفة» (الخليتان 15 و23)، ويبقى `DEBT-K3-TEMPORAL-01` نافذاً عليه |

---

# القسم 4: قواعد العرض والأولوية

| # | القاعدة | التعليل |
| :--- | :--- | :--- |
| **ع-1** | **النمط يتقدّم على خلاياه** — يُعرض أولاً بوصفه القراءة الحاكمة، ثم تُقرأ خلاياه تفصيلاً | مقتضى `DEC-111`: الفائض (تزامن/اتجاه/تراكم) يُفقد إن عُرضت الخلايا موزّعة |
| **ع-2** | **الخلية تُذكر مرة واحدة** — إن دخلت نمطاً فهي **مادته**، لا قراءة مستقلة مكرّرة | منع الازدواج |
| **ع-3** | خلية تصلح لنمطين ← تُنسب إلى **الأعلى في الترتيب البنيوي** (محور ← جانبي ← تحميل ← تغذية) | `64 §4.1` |
| **ع-4** | الخلايا خارج أي نمط تُقرأ كما هي بالترتيب البنيوي | `64 §4.2` |
| **ع-5** | تعرُّف نمطين معاً ← **لا ترجيح ولا دمج**: يُعرضان بترتيب جذريهما، وقاعدة عدم التعارض نافذة | `64 §4.3` |

---

# القسم 5: حدود النقاء

1. **🔒 يُسمّى النمط لا الشخص.** رموز `HF` توصيف **آلية**، لا تصنيف فرد. يُحظر في المخرج للمستخدم إسناد الرمز إليه أو تحويله لقباً (`URS §6/4`: ملف لا تسمية).
2. **`GAP-M3-03-02` نافذ:** `HF-01…04` **مشتقة من أمثلة المالك والبنية، غير محقّقة ميدانياً**. تُقدَّم بلغة ترجيح («يرجّح نمطاً…») ولا تُقدَّم كوقائع.
3. **`DEBT-K3-POLES-01` نافذ:** ما يقوم على تمييز القطب الزائف يبقى **بصيرة بنيوية لا قاعدة محرك**.
4. **احتمال لا حتمية · `P = C + G` · صفر لغة عيادية** — نافذة عبر التعرّف كما داخل الخلية.
5. **الشرط الناقص لا يُكمَّل بالتخمين:** إن تحقق بعض شروط النمط، يُقرأ **جزئياً وموسوماً**، ولا يُعلن تاماً.
6. **لا يعبر التعرّف إلى `K4`** — `RSK-005` نافذ.

---

# القسم 6: الفجوات

| الكود | البيان | الخطورة | المطلوب |
| :--- | :--- | :--- | :--- |
| **`GAP-PAT-01`** | **`HF-02` بجذر `BI`** مشتق من نمط الفشل في `M3-01 §5` (حظر بلا تسليم)، ولم يرد بمثال مالك صريح كنظيره `CF` | منخفضة | تحقّق ميداني أو تثبيت الاشتقاق |
| `GAP-M3-03-02` (موروثة) | `HF-01…04` غير محقّقة ميدانياً | متوسطة | تحقّق ميداني |
| `DEBT-K3-POLES-01` · `DEBT-K3-TEMPORAL-01` | نافذان على `HF-01/02` و`HF-04` | عالية | كما هي |

---

# القسم 7: محضر الاعتماد (`DEC-112`)

| # | البند | الحالة |
| :--- | :--- | :--- |
| 1 | مضمون «عدم الاختزال»: تزامن · اتجاه وتجدد · تراكم (§1.1) | ✅ معتمد |
| 2 | **تصنيف الأنماط صنفين** — (أ) تسلسلي: `HF-01/02/04` · (ب) شامل: `HF-03/05` (§2) | ✅ معتمد |
| 3 | **قفل منع الازدواج**: الصنف (ب) لا يُنقل إلى هذا المستوى (§2.1) | ✅ معتمد |
| 4 | شروط التعرّف الثلاثة (§3) | ✅ معتمد |
| 5 | قواعد العرض `ع-1…ع-5` (§4) | ✅ معتمد |
| 6 | حدود النقاء الست (§5) — وأولها **يُسمّى النمط لا الشخص** | ✅ معتمد |
| 7 | **إجراء مزامنة:** `64-COMPOSITION-METHOD` → **v1.1** بإضافة المستوى الثاني | ✅ **نُفِّذ** |

---

# ختم الحالة

| البند | الحالة |
| :--- | :--- |
| `DEC-111` | ✅ معتمد — الثلاثيات غير قابلة للاختزال |
| نطاق التطبيق | **3 أنماط** تسلسلية (`HF-01/02/04`) · نمطان شاملان يبقيان في طبقتيهما |
| الأشكال المتعرَّف عليها | 4 (`HF-01` · `HF-02` بجذرين · `HF-04`) |
| صفر آليات جديدة | ✅ كل شرط مركّب من شروط استدعاء معتمدة |
| صفر أوزان/عتبات | ✅ ملتزم |
| **حالة الوثيقة** | ✅ **v1.0 — معتمدة (`DEC-112`)؛ نافذة ومرفوعة الوقف بـ`DEC-117` — بشرط مرور مخرجها ببروتوكول `67`** |
