# 98-R1-INTENSITY — كتل الشدة الثماني + استثناء الأقفال

| الحقل | البيان |
| :--- | :--- |
| **وضع التشغيل** | صياغة تشغيلية · قياس وتدقيق |
| **حالة الاعتماد** | **منفَّذ ومُتحقَّق منه** (`R1` · `DEC-217/ب`) |
| **نطاق الاستدلال** | `51-MATRIX-06 §3.1/R1` + س62 · `55-USER-K2-A-INT8` · `55-USER-INT8-Batch1/2` · `41-MEASURE §4` |
| **الحصيلة** | **64/64 كتلة** · حزمة عاشرة · بوابة `R9` نظيفة |
| **الإصدار** | v1.0 |

---

# القسم 1: `GAP-RPT-K2-01` — مُغلق

`51-MATRIX-06` س62: «**64 كتلة (8×8) — معتمدة**». الاستخراج أعطى **64 بالضبط · صفر كتلة بلا نصّ مستخدم**.

**المطابقة العددية التامّة تحقّق مستقلّ ثانٍ** — بعد `72` بنداً في `PUR`. حين يطابق العدد المستخرَج العددَ المنصوص، يسقط احتمال السقوط الصامت.

## مصدران بصياغتين

| المصدر | النمط |
| :--- | :--- |
| `55-USER-K2-A-INT8` (بُعد `A`) | `**🗣️ صيغة المستخدم [مشتق]:**` · `**🏷️ دلالة 41 §4:**` |
| `Batch1` + `Batch2` (السبعة الباقية) | `**🗣️**` · `**🔒**` · `⚠️` |

استخرجتُ بنمطين منفصلين لا بنمط واحد متساهل — **التساهل كان سيُسقط بُعد `A` صامتاً** (أعطى صفر نصّ في المحاولة الأولى).

---

# القسم 2: التنفيذ

**حزمة عاشرة** `INTENSITY_K2` (`40e332a9b1b7b186` · 26,734 بايت).

القسم ② بعد اللوحة — **الثمانية إلزامية** كما ينصّ `R1`:

```
**ماذا يعني رمز كل عدسة عندك:**

- **التصوري (H) — M+ · مساندة فعّالة موثوقة:** التوليد عدسة فعّالة تُستدعى عند
  انسداد أو حاجة لكسر نمط، دون أن تنفرد بالقيادة.
  > 🔒 أول كتلة مشتعلة — مساندة نشطة.
```

**حارس `R1`:** كتلة غائبة **توقف الإصدار** (`SlotResolutionError`) ولا تُخمَّن.

**مواءمة رمزية:** المحرك يُخرج `L-` والحزمة تستعمل `L−` (ناقص `U+2212`). عولج بجدول `_BAND_ALIAS` صريح لا باستبدال صامت.

**الأثر على الطول:** 19,248 → **20,797** (+1,549 · +8٪). أخفّ مما قد يُظن لأن الكتل مقتضبة.

---

# القسم 3: ثغرة تغطية كشفها المسح

فحصتُ أي رموز تبلغها مجموعة الانحدار:

| مغطّى | غير مغطّى |
| :--- | :--- |
| `L-` `L` `M` `M+` `H` `H+` `H++` | **`OUT`** |

**`OUT` لم يكن مغطّى** رغم أنه أحد الثمانية. أُضيفت أربع حالات: `BAND-OUT` · `BAND-HPP` · `BAND-HP` · `BAND-Lminus`.

**ومسألة `H++`:** حسبتُ أقصى `SP` ممكن — `x=42 · y=7 · z=0 → SS=49 → 116.7٪` ⇒ **`H++` قابل للبلوغ فعلاً**. وهذا يوافق `51-MATRIX-06` س136 («كتل `H++` **فعّالة** — `GAP-DR-01` مغلقة») ويخالف تحذيراً باقياً داخل نصوص `INT8` («آلية بلوغ النطاق موقوفة»).

> 🟡 **تحذير متقادم مرصود** — نصّ `INT8` يحمل قيداً رُفع بقرار لاحق. لم أحذفه (نصّ معتمد)، وأرفعه للتصويب.

---

# القسم 4: `DEC-217/ب` — استثناء الأقفال

## المشكلة التي أظهرها `R1`

بوابة `R9` أطلقت **ثلاث إصابات فريدة**، كلها في **حقل القفل**:

| اللفظ | المصدر | النصّ |
| :--- | :--- | :--- |
| `تشخيص` | `K2-E-S-H` | «قراءتك معلومة تشغيلية، لا **"تشخيص"** (لغة محظورة)» |
| `تنفيذ ميداني` | `K2-R-S-H+` | «سيادة عدسة الجدوى ≠ **تنفيذ ميداني** (K₄ مستقل)» |
| `شدة الانفعال` | `K2-E-S-H+` | «سيادة الفهم المعرفي ≠ **شدة الانفعال** (K₃)» |

**القاعدة العامة المستخلصة:** أقفال العزل **تُصاغ بذكر المصطلح المحظور** ليمنع الخلط. فأي بوابة تحظر الألفاظ **تُطلق على الأقفال نفسها — وهي أكثر النصوص التزاماً**.

## التنفيذ

`strip_locks()` تُسقط أسطر `  > 🔒 ` قبل الفحص، **وتعدّها**:

```
r9_gate: "clean"  ·  r9_locks_excluded: 8
```

**العدّ ليس تزييناً** — بلا تصريحٍ يصير الاستثناء تجميداً صامتاً، وهو أسوأ من الإصابة.

## البرهان

| المدخل | النتيجة |
| :--- | :---: |
| «درجة انفعالك مرتفعة» — **خارج** قفل | 🔴 `['انفعالك', 'درجة انفعال']` |
| النصّ نفسه **داخل** قفل | ✅ مستثنى |

⇒ الاستثناء **موضعي لا شامل**: اللفظ نفسه يُحجب في المتن ويمرّ في القفل.

## 🟡 ثغرة معلَنة

نصّ ضارّ يُدسّ في **حقل قفل** يمرّ بلا فحص. الحزم محروسة ببصماتها (`test_packs`)، لكن الحراسة تحمي **الثبات لا المضمون**. مسجَّلة كحدّ معلوم لا كخطر مُهمَل.

---

# القسم 5: الانحدار

| المجموعة | النتيجة |
| :--- | :---: |
| `parity_py` (**122** حالة) | ✅ `68778fcbaa134933` |
| `parity_reports` (**83** تقريراً) | ✅ `948635675815ec66` |
| `audit` $K_2$ عبر 53 حالة | ✅ صفر تباعد |
| `test_packs` (**10 حزم**) | ✅ |
| `test_golden_k2` · `test_report_k2` | ✅ |
| `test_golden_k3` · `test_report_k3` | ✅ |

**الأداة:** `Kashaf_v2.html` — **1,091,695 بايت** · عشر حزم.

---

# القسم 6: الحالة

| البند | الحالة |
| :--- | :---: |
| `GAP-RPT-K2-01` `R1` | ✅ **مغلق** |
| `GAP-RPT-K2-02` `R9` | ✅ مغلق |
| `DEC-217/ب` | ✅ منفَّذ |
| تحذير `H++` المتقادم (§3) | 🟡 **`DEC-218`** |
| ثغرة حقل القفل (§4) | 🟡 معلَنة |
| `DEC-216` أسماء مثبَّتة في `T-04/T-08/C-12/RQ-04` | 🟡 مفتوح |
| `GAP-RPT-K2-03` `R11` `St+H` | ⬜ **الأخير** |
| `DEBT-K2-LEN-01` | 🟡 مفتوح |

**الترقيم التالي:** `DEC-218`.

---

> **[نهاية الوثيقة — `98-R1-INTENSITY` v1.0 · 64/64 · البوابة نظيفة والاستثناء معلَن ومعدود]**
