# 105-BUILD — `DEC-231` · `DEC-232` · `DEC-233` وفجوة `GAP-RETEST-01`

| الحقل | البيان |
| :--- | :--- |
| **الأمر** | تنفيذ البنود الثلاثة · وتسجيل وثيقة الفارق الزمني للبناء المستقبلي |
| **حالة الاعتماد** | **مشتق** — لا مفهوم جديد ولا وزن ولا عتبة |
| **اختبار القبول** | البصمتان `2711c24d8155819b` · `36ae94bfd5a8b60f` **لم تتغيّرا** · **10/10** أداة |

---

# `DEC-231` — تصدير `JSON` يُعلن نطاقاته

**المشكلة:** $K_3$ بلا نطاق مختصر، فكان `markdown_brief` و`audit_brief` دائماً `null`. والـ`null` **لا يُميّز** «لا نطاق مختصر لهذه الدائرة» من «مفقود أو تالف» — وهو الفراغ الصامت الذي يحظره `ن-7`.

**القرار:** النطاقات **مُعلَنة لا مستنتَجة**.

```json
{ "schema": "RAWAHIL-REPORT-v1.2",
  "circle": "K3",
  "scopes": ["full"],
  "delivery": { "markdown": "…" },
  "audit": { … } }
```

- `scopes` مصفوفة صريحة: `["full"]` لـ$K_3$ · `["full","brief"]` لـ$K_2$.
- الحقول الاختيارية (`subject` · `markdown_brief` · `audit_brief`) **تُحذف** حين لا وجود لها.
- المستهلك يقرأ الإعلان، فلا يستنتج من الغياب.

**أثر على البصمتين:** صفر — التصدير خارج نطاق `parity_py` و`parity_reports`. مُتحقَّق بالتشغيل.

---

# `DEC-232` — حارس `ح-5`

## العيب المعلوم مُبرهَن بمصنوع لا بفرضية

السطح الميت في `Kashaf_v2.html` يحمل:

| القالب | ما يُصيّره |
| :--- | :--- |
| `` `${d.sp}%` `` | نسبة بُعد $K_2$ في صفّ `CSV` |
| `` `${k.sp}%` `` | نسبة مهارة $K_3$/$K_4$ |
| `` `${avg}%` `` | متوسط على الشاشة |
| `` `${k.name}=${k.sp}%` `` | سطر في تصدير `MD` |

**هذه تُصيّر `73.5%` بلا الحرفين `SP` — فتنفذ من `ح-4` كاملةً.** أربع عيّنات مُصيَّرة: **نفذت 4/4 من `ح-4` · أوقفها `ح-5` 4/4**.

## حارسان مستقلّان — لا حارس واحد موسَّع

| | يقيس | النمط |
| :--- | :--- | :--- |
| `ح-4` | **الرمز** | `/SP\s*%/i` |
| `ح-5` | **القيمة** | `/\d+(?:[.,]\d+)?\s*%/` |

الدمج يخالف `ن-7/②`: `ح-4` وحده يمرّ عليه `73.5%`، و`ح-5` وحده يمرّ عليه رأس جدول `SP%` بلا قيمة.

## سجلّ النِّسَب المعتمدة — نصّ كامل مُسنَد، لا شظية

| المفتاح | النصّ |
| :--- | :--- |
| `INTENSITY_K2:/S/A/M+/lock` | كتلة الشدة — «فوق 50%» عتبة بنيوية لا درجة مفحوص |
| `CONTENT_K2:/R/lines/R-C-D/presence` | «التزام 100%» — صورة بلاغية لا درجة |

التسجيل **بالنصّ الكامل**: إن تغيّر في الحزمة سقط تسجيله وأُوقف الإصدار. تسجيل الرقم وحده كان سيفتح باباً لكل `50%`.

## الاستيفاء

| شرط `ن-7` | البرهان |
| :--- | :--- |
| ① عيب معلوم | أربع عيّنات من قوالب الأداة — 4/4 · صفر إنذار كاذب على ثلاثة نصوص مشروعة |
| ② قياس واحد | حارسان منفصلان بنمطين وسجلّين |
| ③ تكافؤ النسخ | 16 عيّنة × مقياسين — بايثون ↔ JS متطابقان |
| ④ لا عتبة | أي مطابقة توقف الإصدار |

**التوصيل:** سبع نقاط إصدار عبر `output_gate`/`outputGate` — الحارسان يُستدعيان تتابعاً ويُبلّغان منفصلين.

---

# `DEC-233` — أداة المشرف (`supervisor.py`)

تأخذ ملف `JSON` مُصدَّراً، وتُعيد التوليد من `audit["sp"]` **وحده**، وتقارن. **أداة نزاهة لا أداة قراءة** — لا تعرض ولا تفسّر ولا تحكم على المفحوص (`100-AUDIT-REGEN §5`).

## الدرجات العشر

| # | تقيس |
| :--- | :--- |
| ⓪ | المخطَّط معروف · الدائرة معلومة · `audit` حاضر |
| ① | الحقول السبعة الملزِمة |
| ② | النطاقات مُعلَنة (`DEC-231`) · واتّساق الإعلان بالمحتوى |
| ③ | المتن المُسلَّم حاضر |
| ④ | **البصمة المسجَّلة تصف المُسلَّم فعلاً** |
| ⑤ | إعادة التوليد من `audit` وحده تطابق المُسلَّم نصّاً |
| ⑥ | بصمة المُعاد = المسجَّلة |
| ⑦ | **انجراف الحزم** — تقريران بحزم مختلفة يُكشفان |
| ⑧ | النطاق المختصر يُدقَّق كالكامل إن أُعلن |
| ⑨ | `ح-4` و`ح-5` على **ما وصل المستفيد فعلاً** |

## مُختبَرة على العيب لا على النجاح وحده

الاختبار الذاتي يبني حمولة حقيقية للدائرتين فتمرّ، **ثم يفسدها بحرف واحد** فتسقط ④ و⑤ في الدائرتين. أداة تدقيق لا تسقط على إفساد مقصود **عمياء**.

```
python3 supervisor.py <ملف.json> [...]     # 0 سليم · 1 انجراف · 2 خطأ إدخال
python3 supervisor.py --self-test
```

## ⚠️ ما لم يُنفَّذ

**النقل إلى المتصفح.** الأداة بايثون. المشرف بلا بيئة بايثون لا يستعملها، وهذا يخالف روح `CPL-08A-03` (offline standalone). النقل يستلزم انضباط `DEC-199` (تكافؤ مقيس) — **بند مستقلّ، لم يرد به أمر.**

---

# `GAP-RETEST-01` — قراءة الفارق الزمني

| الحقل | البيان |
| :--- | :--- |
| **موقع النقص** | خط الأساس كلّه — لا وثيقة تحكم قراءة الفارق بين **قياسين للشخص نفسه** |
| **نوع الفجوة** | **مفهومية** — لا قياسية ولا رياضية |
| **التحليل** | `P = C + G` تُلزم نسبة الفارق إلى `G`، وحظر تفسير تغيّر الأداء كتغيّر في `C` نافذ. **لكن النسبة ليست قراءة**: لا تفصل تغيّر `G` الحقيقي عن أثر السياق · ضغط الموارد · **تعلّم الأداة** (المفحوص يتذكّر بنوده) · خطأ القياس |
| **الأثر** | تظهر **لحظة أول إعادة قياس**. وبدون وثيقة حاكمة يُملأ الفراغ بالاجتهاد — وهو ما يحظره `§10` |
| **الخطورة** | 🟠 عالية عند وقوعها · **لا تحجب اليوم** |
| **القرار** | **بناء مستقبلي مُقرّ من المالك** |
| **الصلة** | تمسّ `DEBT-K3-TEMPORAL-01` (`ST` زمنية-تراكمية والقياس نقطي) وقد تستوعبه |
| **تحذير مبكّر** | **لا يُصدَر أي تقرير إعادة قياس قبل بنائها.** |

---

# الأداة النافذة

| البند | البيان |
| :--- | :--- |
| المخرَج | `Kashaf_v2_FIXED_DEC-230-231-232.html` |
| الحجم | 1,104,286 → **1,108,490** بايت |
| `SHA-256` | **`039e3b5fc9aa36e5…`** |
| التدقيق | 12/12 بصمة حزمة · منطقة البيانات مطابقة حرفياً · نحو سليم · **142 تقريراً مطابقاً للمرجع** |

---

**[نهاية `105-BUILD` — `DEC-231`/`232`/`233` · `GAP-RETEST-01` مسجَّلة]**
